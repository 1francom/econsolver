# Feedback Analysis Report — 2026-07-15

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **5 open bugs** (4 code-complete / browser-validation-pending · 1 fix-later)
- **5 open features** (1 already shipped but not marked done · 1 unaddressed · 3 fix-later)
- **2 open UX** (both fix-later)
- **1 open performance** (code-complete / browser-validation-pending)

### ClaudeFB.md hygiene note
The X5 reconciliation table (2026-06-14) resolved a large number of items as "verified fixed" but the corresponding ClaudeFB.md bullets were never updated with strikethrough/✓. Items affected include: Anderson-Rubin CI, multiple statistics tests, resampling/bootstrap, Monte Carlo single variable, function graph from 0 / multiple equations, PlotBuilder color mapping, spatial auto-fit, geometry transformations/EPSG, geometry in point plots, show-steps button, pinned model persistence, derivatives with endogenous functions, studentized permutation tests, Calculate limits (see Features below). **Recommend a single pass to strike through these in ClaudeFB.md** — they currently inflate the apparent open count by ~15 entries.

---

## Bugs

### [high · browser-validation-pending] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS  
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`  
**BugTriage status:** Code-complete in commit `35f4b5a6` — OLS/WLS diagnostics now render from frozen `result.spec`. Browser validation pending Franco.  
**Suggestion:** This is unblocked — the code fix is landed. Franco should open ModelingTab, run an OLS, then verify that (1) all three plots render, (2) they do not update when sidebar variables change, and (3) restoring a pinned model re-renders the correct plots. If any of the three is broken, the frozen-spec reference in the diagnostic render path (`result.spec`) is the first place to check.  
**Invariants:** None — purely UI component state.

---

### [medium · browser-validation-pending] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models  
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/resultDisplay.jsx`  
**BugTriage status:** Code-complete in `35f4b5a6` — display frozen to `result.spec`. Browser validation pending Franco.  
**Suggestion:** Covered by the same commit as the residual plot fix above. Primary test: run OLS with variables A/B/C → see plots → add variable D without re-estimating → plots must NOT change. Secondary test: pin the model, switch estimator, restore the pin → plots must restore to the A/B/C state. If plots still mutate, check that the OLS display path reads `result.spec.xVars` rather than the live `xVars` state from the sidebar.  
**Invariants:** None.

---

### [medium · browser-validation-pending] · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems  
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`  
**BugTriage status:** Code-complete in commit `35f4b5a6` — native vertical resize + bounded responsive canvas sizing for PNG capture. Browser validation pending Franco.  
**Suggestion:** Verify the resize handle is visible and draggable in the collapsible Plot Builder panel, and that a PNG exported after resizing matches the in-app dimensions. If PNG dimensions still mismatch, the canvas is likely reading viewport dimensions before the ResizeObserver fires — confirm the canvas `width`/`height` attributes are set from the ResizeObserver callback, not a static measurement taken on mount.  
**Invariants:** None.

---

### [medium · browser-validation-pending] · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed  
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`  
**BugTriage status:** Code-complete in commit `5e105529` — Heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Browser validation pending Franco.  
**Suggestion:** Load a lat/lon dataset, open the Map tab, add a Heatmap layer, and confirm KDE tiles render, palette and opacity controls respond, and the legend appears. Verify HTML export produces a self-contained page. If KDE tiles are blank, check that the 5k-point sampling does not discard all rows when data is concentrated outside the initial viewport bounds.  
**Invariants:** None — purely a UI/Leaflet layer concern.

---

### [medium · fix-later] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47…  
**File:** `src/DataStudio.jsx` (`parseCSV` function, starting at line 41)  
**BugTriage status:** Fix-later — post-launch resilient-import pass. The current `parseCSV` substitutes `null` for missing column values (`vals[j] ?? ""`), but a malformed quoted-field on line 1583 causes the tokenizer to mis-align subsequent columns for the entire remainder of the file rather than throwing cleanly.  
**Suggestion:** In the `for` loop at `DataStudio.jsx:80`, wrap the `tokenize(lines[i])` call in a `try/catch`. On failure, push the row index to a `skippedRows[]` array and substitute `null` for all columns of that row (rather than aborting). After parsing completes, surface a non-blocking warning banner in the data viewer: "N rows skipped due to parse errors — rows: 1583, …". This matches the BugTriage recommendation of offering row-number visibility and a skip/repair preview path.  
**Invariants:** `parseCSV` is pure JS with no React imports — no invariants violated.

---

## Features

### [low · already shipped — ClaudeFB.md update needed] · Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx` (lines 1170, 1423, 1586, 2161)  
**Status:** **Limits are already implemented.** `activeTool` supports a `"limit"` mode (line 1170), the `lim Limit` tab appears in the toolbar (line 1586), and the result is computed via `limit(wrappedFn, a)` at line 1423 and displayed at line 2161.  
**Suggestion:** No code change needed. Update `ClaudeFB.md` to mark this item done with strikethrough/✓.  
**Invariants:** N/A.

---

### [low · unaddressed] · Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/calculate/workbench/WorkbenchCanvas.jsx` (lines 146–163), `src/components/calculate/workbench/ViewControls.jsx`, `src/components/calculate/workbench/EquationsPanel.jsx`  
**Status:** Horizontal reference lines (`refLines` with `kind:"h"`) are already supported in `WorkbenchCanvas.jsx` and editable via `ViewControls.jsx`. However, these are visual annotations managed separately from the equation list — users cannot express `y = 3.14` in the equation panel and see it plotted like a curve.  
**Suggestion:** In `EquationsPanel.jsx` (or its equation-evaluation loop in `Workbench.jsx`), detect when an expression evaluates to a numeric constant after parameter substitution (no free variable dependence). When detected, auto-promote it to a horizontal `refLine` entry (`kind:"h", value: <constant>, label: <eq display name>`) in the view's `refLines` array. This reuses all existing rendering infrastructure in `WorkbenchCanvas.jsx` without new math or canvas code. The promotion check can be a simple `isFinite(evaluated) && !dependsOnX(expr)` guard in the evaluation loop.  
**Invariants:** `WorkbenchCanvas.jsx` is pure JS/canvas — no React import restriction violated.

---

### [medium · fix-later] · Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept  
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`, `src/math/CausalEngine.js`  
**BugTriage status:** Fix-later — requires engine-wide contract change.  
**Suggestion:** Add a `noIntercept: boolean` checkbox to `ModelConfiguration.jsx` (general options section, near the SE type selector). Thread it as `spec.noIntercept` through `ModelingTab.estimate()`. In `LinearEngine.runOLS()`, when `noIntercept` is true, omit the prepended constant column from the design matrix `X` and subtract 1 from degrees of freedom. Update `varNames` to exclude `"(Intercept)"`. For FE/FD, `noIntercept` is a no-op (demeaning already removes the constant) — document this in a one-line comment. Validate against R's `lm(y ~ x - 1, data)` to 6 decimal places using the existing `engineValidation.js` harness.  
**Invariants:** `seType` must remain passed explicitly to the engine — `noIntercept` changes matrix construction only. Math files must not import from React.

---

### [low · fix-later] · Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/components/ModelingTab.jsx` (RDD result render section ~line 3375), `src/math/CausalEngine.js` (result shape at line 580), `src/components/modeling/results/` (new file recommended)  
**BugTriage status:** Fix-later — presentation enhancement.  
**Suggestion:** Create `src/components/modeling/results/SharpRDDSummaryTable.jsx`. The `runSharpRDD` result already contains all needed data: `{ late, se, tStat, pVal, h, nLeft, nRight, polyOrder, kernel, cutoff }`. Map these to R's `summary(rdrobust)` layout — header row (Coef / Std.Err. / z / P>|z| / 95% CI), one row for the RD estimate, plus a metadata footer (bandwidth, kernel, poly order, n left/right of cutoff). Render it below the existing RDD LATE display in `ModelingTab.jsx` inside a collapsible `<Section>`. No math or engine changes needed.  
**Invariants:** The new component must receive result props from `ModelingTab` — it must not import from `src/math/` or `src/core/`.

---

### [low · fix-later] · Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names  
**File:** `src/components/modeling/resultDisplay.jsx` (`CoeffTable`, line 266), `src/ReportingModule.jsx`  
**BugTriage status:** Fix-later.  
**Note:** `CausalEngine.js` line 578–580 already injects the actual `runCol` name into `polyNames`/`interNames`, so the real column name (e.g., `"score − c"`) does appear in `varNames`. Only the treatment indicator `"D (treatment)"` remains a fixed generic string (line 580).  
**Suggestion:** Add an optional `labelOverrides: Record<string, string>` prop to `CoeffTable` in `resultDisplay.jsx`. When rendering variable `v`, use `labelOverrides[v] ?? v` as the display string. In `ReportingModule.jsx`'s LaTeX block, expose a small inline-editable name table (source variable → display label) that populates `labelOverrides`. Persist overrides in the model result metadata so they survive tab switches. No math changes needed.  
**Invariants:** `CoeffTable` is a display-only component — no engine interaction.

---

## UX / Design

### [low · fix-later] · Clean · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option  
**File:** `src/services/exprEvalService.js` (`evalFilter`, line 101), `src/workers/exprEval.worker.js` (filter eval at line 55–60)  
**BugTriage status:** Fix-later — post-launch expression-language compatibility.  
**Suggestion:** Add a preprocessing step at the top of `evalFilter` in `exprEvalService.js` that rewrites R-style `%in%` syntax to JS before dispatch: `expr = expr.replace(/(\w+)\s*%in%\s*c\(([^)]+)\)/g, '[$2].includes($1)')`. This handles the common single-column case (e.g., `regionname %in% c("Spain", "France")` → `["Spain", "France"].includes(regionname)`) without touching the worker or the full evaluator. The rewrite must happen before `assertSafeExpr` is called — add `%in%` to the allowed-syntax note in `exprEval.worker.js` comments.  
**Invariants:** The preprocessing is pure JS string transformation, no React imports, no indexedDB interaction.

---

### [low · fix-later] · Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**BugTriage status:** Fix-later — routed to V4 spatial plots after launch.  
**Suggestion:** In `GeoLayerConfig.jsx`, add a per-layer "Filter rows" text input that accepts a JS boolean expression using the dataset's column names (same pattern as `evalFilter` in `exprEvalService.js`). When set, filter `layer.rows` through `evalFilter` before passing them to the rendering path in `SpatialGeoPlot.jsx`. This mirrors the filter builder in `CleanTab` and reuses the existing evaluator infrastructure with no new math.  
**Invariants:** `GeoLayerConfig.jsx` and `SpatialGeoPlot.jsx` are UI components — no math/core imports required.

---

## Performance

### [high · browser-validation-pending] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`  
**BugTriage / X5 status:** X5 reconciliation: "routed — Fase X4 code-complete; browser benchmark pending Franco (`plans/2026-05-22-fase-x4-performance.md`)."  
**Suggestion:** The architectural fixes are in place (DuckDB OPFS Parquet cache, 500-row display limit, SQL push-down for pipeline steps). Franco should load a CSV or .rds file >10 MB and verify: (1) the ⚡ DuckDB badge appears in the wrangling header, (2) tab-switching between Data/Clean/Model takes under 2 s, (3) a filter + log pipeline step executes on the full row count without freezing the UI. If tab-transition lag persists, profile which component triggers a full `rawData` re-render on tab mount and guard it with `useMemo`/`React.memo`.  
**Invariants:** Display must remain limited to 200–500 rows; pipeline steps must operate on the full DuckDB table. Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`.

---

## Suggested action order

1. **Browser-validate the 4 code-complete items** (Y~Y_hat/residual/Q-Q plots, frozen diagnostics, Plot Builder resize/PNG, Spatial heatmap) — all are one validation step from closure; if any fail, the fix is in commits `35f4b5a6`/`5e105529` already merged.
2. **Browser-validate DuckDB performance fix** (Fase X4) — 900k-row crash was the most-upvoted complaint and the code is complete; a green benchmark closes a high-visibility open item.
3. **Add resilient CSV import with per-row error recovery** (`DataStudio.jsx:parseCSV`) — low-risk localized change, directly unblocks users who hit malformed CSV files like the China/CHN line-1583 case.
4. **Preprocess `%in%` in `evalFilter`** (`exprEvalService.js`) — 1-line regex rewrite, zero risk, closes a usability gap for R users migrating filter expressions.
5. **Implement no-intercept option** (`ModelConfiguration.jsx` + engine changes) — medium effort, broadly useful across OLS/WLS/2SLS, no architectural risk.
6. **Add `SharpRDDSummaryTable` component** — presentation only, all data already in result object, isolated new file.
7. **Strike through ~15 verified-fixed items in ClaudeFB.md** — housekeeping, significantly reduces false open-item count and makes future analyst passes faster.
