# Feedback Analysis Report — 2026-06-22

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary

ClaudeFB.md contains **28 open bugs · 18 open features · 0 open UX · 1 open performance · 2 open other** (49 items without strikethrough/✓ markup).

**BugTriage cross-reference reduces the actionable set considerably:**

| Effective status | Count | Action |
|---|---|---|
| **Genuinely open** (BugTriage status = `open`) | 1 | Fix now |
| **Code-complete, browser validation pending** | 4 | Franco to validate |
| **Fix-later** (acknowledged backlog) | 6 | Schedule post-launch |
| **New, untriaged** (not in BugTriage yet) | 2 | Triage now |
| **Verified fixed** (ClaudeFB.md markup not yet updated) | 28 | Add strikethrough to ClaudeFB.md |
| **Wontfix / provider limitation** | 2 | No action |

> ⚠️ ClaudeFB.md is significantly stale: ~30 resolved items still show as open because strikethrough markers were never applied. A cleanup pass is recommended (see section at the end of this report).

---

## Bugs

### [critical] · Model · 2026-06-17

**Feedback:** `Pipeline only applies if dataset is also selected in Clean Module`

**File:** `src/App.jsx` (lines 2844–2854), `src/DataStudio.jsx` (lines 1134–1139)

**Suggestion:** `handleOutputReady` fires only when `WranglingModule` mounts inside `DataStudio`, which is gated to the dataset currently active in the **Clean tab** (`activeDatasetId={tabDsId("clean")}`). When a user selects a different dataset in the Model tab via DatasetManager, `WranglingModule` never mounts for that dataset, `outputs[tabDsId("model")]` stays `null`, and `modelCleanedData` falls back to raw data — bypassing all pipeline steps. Fix: when a tab switches its active dataset (`selectDataset`), proactively load that dataset's IDB pipeline and replay it via `runner.js`, then store the result in `outputs` keyed by dataset ID — without requiring a Clean-tab visit. A lightweight alternative is to mount a headless `WranglingModule` per unique dataset ID on first selection in any tab and let its existing `onReady` effect fire silently.

**Invariants:** Pipeline must replay on `rawData` via `runner.js` — never mutate in place. Persistence via `indexedDB.js` only (no localStorage).

---

### [medium] · Model · 2026-06-10 — CODE-COMPLETE (`35f4b5a6`), BROWSER VALIDATION PENDING

**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`

**File:** `src/components/PlotBuilder.jsx` (line 1626), `src/components/ModelingTab.jsx`

**Suggestion:** Commit `35f4b5a6` added `resize: "vertical"` to the plot viewport div (PlotBuilder.jsx:1626) and wires `downloadCombinedPNG` through `src/services/export/plotExporter.js`. Browser validation step: confirm drag-resize works, canvas reflows to the new height, and the exported PNG matches visible dimensions without blank padding.

**Invariants:** Inline styles via `C` object only — no external CSS files.

---

### [high] · Model · 2026-06-11 — CODE-COMPLETE (`35f4b5a6`), BROWSER VALIDATION PENDING

**Feedback:** `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS`

**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Commit `35f4b5a6` froze OLS/WLS diagnostics to `result.spec` so sidebar edits cannot mutate the displayed model. All three plot components are already wired. Browser validation: (1) run OLS — confirm all three plots render; (2) change variable selectors without re-estimating — confirm plots stay frozen; (3) restore a pinned model — confirm it re-renders the correct plots.

**Invariants:** Plot data must derive from the frozen `result.spec` — never from live sidebar selector state after estimation.

---

### [medium] · Spatial · 2026-06-13 — CODE-COMPLETE (`5e105529`), BROWSER VALIDATION PENDING

**Feedback:** `Heatmap must be fixed`

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`

**Suggestion:** Commit `5e105529` restored the heatmap as a Leaflet layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Browser validation: load a point dataset, switch to heatmap layer type, confirm KDE cells render with correct colour scaling and a visible legend.

**Invariants:** None specific to architectural invariants.

---

### [high] · Model · 2026-06-14 — CODE-COMPLETE (`35f4b5a6`), BROWSER VALIDATION PENDING

**Feedback:** `Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models`

**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`

**Suggestion:** Commit `35f4b5a6` freezes OLS/WLS display to `result.spec`. Browser validation: pin a model, change variable selectors (without re-running) — coefficient plots must stay frozen; restore the pinned model — plots must re-render from the saved spec.

**Invariants:** Diagnostic and coefficient plot state must derive from the estimation result — never from live UI selector state.

---

### [medium] · Data · 2026-05-10 — FIX-LATER (post-launch)

**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`

**File:** `src/DataStudio.jsx` (CSV loader path), `src/services/data/duckdb.js`

**Suggestion:** DuckDB's strict CSV parser aborts on rows with inconsistent column counts or non-UTF-8 bytes. Post-launch, implement resilient-import UX: catch per-row errors, offer a "skip malformed rows" toggle with a row-count preview, and surface the original line number for manual inspection. A `strict: false` fallback path that trims/pads rows to the header count covers most real-world cases.

**Invariants:** Never mutate the original source file. Repaired rows should be flagged in column-info metadata. Pipeline must still replay on `rawData`.

---

### [low] · Clean · 2026-06-14 — FIX-LATER (post-launch)

**Feedback:** `R-style %in% c(...) filter syntax is not accepted by the filter expression UI`

**File:** `src/components/wrangling/CleanTab.jsx`

**Suggestion:** Add a pre-processor in the filter expression path that rewrites `col %in% c("a","b")` → `["a","b"].includes(col)` before passing to the JS evaluator. Scope the change to the `filter` step's expression compilation in `runner.js`.

**Invariants:** The `filter` step must remain non-destructive (replays on `rawData`). No React in `runner.js`.

---

### [medium] · Model · 2026-06-14 — FIX-LATER (post-launch)

**Feedback:** `User cannot estimate supported regression families without an intercept`

**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/CausalEngine.js`, `src/math/PanelEngine.js`

**Suggestion:** Add an `intercept: boolean` flag (default `true`) to the estimation spec. In each engine, when `intercept === false`, skip prepending the ones column to `X`. Export scripts (`rScript.js`, `stataScript.js`, `pythonScript.js`) must emit the corresponding no-intercept syntax (`-1` in R formula, `noconstant` in Stata). SE type must still be passed explicitly — the `seType` contract does not change.

**Invariants:** SE type always passed explicitly to engines — never hardcoded inside engine functions (CLAUDE.md invariant).

---

### [low] · Model · 2026-06-14 — FIX-LATER (post-launch)

**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`

**File:** `src/ReportingModule.jsx`, `src/services/Persistence/indexedDB.js`

**Suggestion:** Add an editable `labelMap: { [colName]: string }` metadata object stored alongside the estimation result in `modelBuffer`. In the LaTeX table builder within `ReportingModule`, replace internal column names with `labelMap[col] ?? col`. Surface the label map as inline-editable fields in the result display UI, per coefficient row.

**Invariants:** Labels are display-only — underlying pipeline column references must not change. Persistence via `indexedDB.js`, not localStorage.

---

### [low] · Model · 2026-06-14 — FIX-LATER (post-launch)

**Feedback:** `RDD result lacks an R-style summary(rdrobust) compact table`

**File:** `src/components/ModelingTab.jsx` (result section), `src/math/CausalEngine.js`

**Suggestion:** `CausalEngine.runSharpRDD` already returns all required fields (LATE, SE, t/p, bandwidth, kernel, n_left, n_right). Add a `<RDDSummaryTable>` sub-component to the result display panel that formats them in rdrobust style. No math changes needed — pure presentation.

**Invariants:** No React imports in `src/math/`. The new component belongs in `src/components/modeling/`.

---

### [low] · Spatial · 2026-06-14 — FIX-LATER (post-launch)

**Feedback:** `Plot builder has no spatial row-filter control scoped to a layer`

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Add a per-layer filter input to `GeoLayerConfig.jsx` accepting a JS predicate (e.g. `row.year === 2010`). Apply the filter when building the layer's feature array in `geo.js` before passing to Observable Plot. Reuse the existing filter-step expression evaluator for consistency.

**Invariants:** Filter is display-only — must not mutate underlying dataset rows.

---

## Features

### Calculate · 2026-06-19 — NEW, UNTRIAGED

**Feedback:** `Add limits`

**File:** `src/components/tabs/CalculateTab.jsx` (line 1556 — tool tab list), `src/math/calcEngine.js`

**Suggestion:** Add a `"limit"` panel alongside the existing solver/derivative/symbolic/algebra/integral tools (CalculateTab.jsx:1556). The panel should accept a function expression, a variable, and a target value (including `Infinity`/`-Infinity`). Compute the one- and two-sided limits numerically: approach `x → a` via a geometric sequence of values from the left and right, then apply Richardson extrapolation for smooth functions. Display `lim⁻`, `lim⁺`, and a "limit exists" indicator when they agree. `calcEngine.js` already has `evalExpression` and `buildScope` — add a `numericalLimit(fn, x, xTarget, side)` helper there.

**Invariants:** `src/math/calcEngine.js` is pure JS — no React imports. New panel UI must use the `C` color object from `shared.jsx`.

---

### Model · 2026-05-11 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`

**File:** `src/math/CausalEngine.js` (`andersonRubinCI`), `src/components/ModelingTab.jsx`

**Note:** X5 reconciliation (2026-06-14) marks this **verified fixed**. ClaudeFB.md entry needs strikethrough — no code action required.

---

### Model · 2026-05-11 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Add multiple statistics tests`

**File:** `src/components/tabs/statsim/SampleTestPanel.jsx`, `src/math/SampleTests.js`

**Note:** X5 marks this **verified fixed** (StatWorkspace/SampleTests + inference validation harness). ClaudeFB.md markup pending.

---

### Simulate · 2026-05-18 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Add resampling, bootstrapping, sampling to the mean`

**File:** `src/math/Resampling.js`, `src/components/tabs/statsim/StatWorkspace.jsx`

**Note:** X5 marks this **verified fixed**. ClaudeFB.md markup pending.

---

### Simulate · 2026-06-08 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Add studentization for permutation tests`

**File:** `src/math/Resampling.js`

**Note:** X5 marks this **verified fixed**. ClaudeFB.md markup pending.

---

### Model · 2026-06-04 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Persistence across sessions of pinned models`

**File:** `src/services/Persistence/indexedDB.js` (model_buffer store), `src/services/modelBuffer.js`

**Note:** X5 marks this **verified fixed**. ClaudeFB.md markup pending.

---

### Model · 2026-06-06 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Add derivatives with endogenous functions (e.g. f(x) = 2x² − c(x), compute dc/dx symbolically)`

**File:** `src/math/symbolicDiff.js`, `src/components/tabs/CalculateTab.jsx`

**Note:** X5 marks this **verified fixed**. ClaudeFB.md markup pending.

---

### Spatial · 2026-05-30 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Geometry transformations and show EPSG`

**File:** `src/components/tabs/spatial/shared/crs.js`, `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`

**Note:** X5 marks this **verified fixed**. ClaudeFB.md markup pending.

---

### Spatial · 2026-05-20 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Add grids to the plot, not only in the OMS map`

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`

**Note:** X5 marks this **verified fixed** (`SpatialGeoPlot` grid layer). ClaudeFB.md markup pending.

---

### Explore · 2026-05-19 — FIX-LATER (same as CleanTab `%in%` bug)

**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`

**File:** `src/components/wrangling/CleanTab.jsx`

**Note:** This is the same issue as the `%in%` bug above. The fix in CleanTab's filter expression pre-processor would cover this too. BugTriage labels it `fix-later`.

---

### Spatial · 2026-05-30 — FIX-LATER (post-launch, routed to V4)

**Feedback:** `Allow spatial filters for plots`

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Note:** X5 routes to V4. See Bugs section (spatial layer filter) for the per-layer predicate suggestion.

---

### Calculate · 2026-06-03 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Add a "show steps button" for integrals, derivatives and optimizations`

**File:** `src/components/calculate/workbench/Workbench.jsx`

**Note:** X5 marks this **verified fixed** (Equation Workbench). ClaudeFB.md markup pending.

---

### Spatial · 2026-06-03 — VERIFIED FIXED PER X5 (ClaudeFB.md markup pending only)

**Feedback:** `Add geometry to plot points, not just lat and lon`

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`

**Note:** X5 marks this **verified fixed** (WKT point mode in both spatial builders). ClaudeFB.md markup pending.

---

### Simulate · 2026-06-02 — WONTFIX (documented in BugTriage)

**Feedback:** `You could add different types of quantiles like in R`

**Note:** BugTriage wontfix — "type-7 is the documented default shared with R; extra types add UX surface without launch value."

---

### Model · 2026-05-21 — FIX-LATER (post-launch, per X5)

**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`

**File:** `src/components/ModelingTab.jsx` (result section), `src/math/CausalEngine.js`

**Note:** See Bugs section (RDD summary table). No math changes needed.

---

### Model · 2026-05-25 — FIX-LATER (post-launch, per X5)

**Feedback:** `Add an option to estimate without intercept`

**File:** `src/math/LinearEngine.js`, `src/components/modeling/ModelConfiguration.jsx`

**Note:** See Bugs section (no-intercept option). Engine-wide spec change.

---

### Model · 2026-06-06 — FIX-LATER (post-launch, per X5)

**Feedback:** `Allow users to edit LaTeX table variable names`

**File:** `src/ReportingModule.jsx`

**Note:** See Bugs section (editable LaTeX labels). Post-launch reporting feature.

---

## Performance

### [high] · Data · 2026-05-08 — CODE-COMPLETE (Fase X4), BROWSER BENCHMARK PENDING

**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`

**File:** `src/DataStudio.jsx`, `src/pipeline/duckdbRunner.js`, `src/services/data/duckdb.js`

**Suggestion:** Fase X4 is code-complete per X5. The `.dta` path already routes files >10 MB through DuckDB → OPFS Parquet (BugTriage `fixed 2026-06-10 for .dta`). Franco should run the 900k-row benchmark to confirm tab transitions no longer stall. If they do, apply CLAUDE.md DuckDB strategy rules: (1) never pull full Arrow result into JS objects; (2) display table limit of 200–500 rows; (3) OPFS Parquet cache for reopen. Remaining gap: `.rds`, `.xlsx`, `.shp` large files still bypass DuckDB.

**Invariants:** Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js` (CLAUDE.md rule 5 — display limit ≠ computation limit).

---

## Other

### Data · 2026-05-10 — FIX-LATER

**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,...`

**File:** `src/DataStudio.jsx` (CSV loader), `src/services/data/duckdb.js`

**Suggestion:** See Bugs section (malformed CSV row) — same resilient-import fix applies. This entry predates the BugTriage row; the two should be merged.

**Invariants:** Never mutate the original file.

---

## ClaudeFB.md Markup Debt

The following items appear "open" in ClaudeFB.md but are **verified fixed** in BugTriage or X5 reconciliation. They only need `~~strikethrough~~` applied — no code action:

| Item | BugTriage / X5 status |
|------|----------------------|
| Explore graph does not fit screen | fixed (`35f4b5a6`) |
| `.dta` files wrong loaded | fixed 2026-06-10 |
| Grids border clipping | fixed 2026-05-18 |
| Grids do not work | fixed 2026-06-01 |
| Dataset deletion leaves stale variables | fixed |
| p-value calculation wrong | fixed 2026-06-05 (Franco confirmed) |
| Variables auto-reestimate | fixed 2026-06-01 |
| Add option to clear model | fixed ("Clear result" button) |
| Calculate graph from 0 / only 1 equation | fixed |
| PlotBuilder color references | fixed 2026-05-30 |
| Monte Carlo single variable | fixed 2026-05-30 |
| Spatial size adjust | fixed (fitBounds) |
| XLSX CDN failure | fixed 2026-06-01 |
| OECD 406 error | fixed 2026-06-03 |
| `.dta` duplicate columns | fixed 2026-06-03 |
| Calculate LaTeX export crash | fixed 2026-06-05 (Franco confirmed) |
| Poisson estimator interpretation | fixed 2026-06-03 |
| XSS feedback form (both CleanTab + Data entries) | fixed 2026-06-14 |
| IV-Poisson 2SLS interpretation | fixed 2026-06-14 (`bc2d7b9d`) |
| Anderson-Rubin CI | verified fixed (X5) |
| Multiple statistics tests | verified fixed (X5) |
| Grids in static spatial plot | verified fixed (X5) |
| Resampling / bootstrap / sampling mean | verified fixed (X5) |
| Geometry transformations and EPSG | verified fixed (X5) |
| Column name editing in Data Viewer | verified fixed (X5) |
| Geometry in point plots | verified fixed (X5) |
| Show steps for calculus | verified fixed (X5) |
| Pinned model persistence | verified fixed (X5) |
| Derivatives with endogenous functions | verified fixed (X5) |
| Studentized permutation tests | verified fixed (X5) |

---

## Suggested action order

1. **Fix the Pipeline/Clean Module coupling bug** (`src/App.jsx` lines 2844–2854) — the only BugTriage-`open` bug; pipeline steps are silently ignored whenever a user uses a dataset in any tab other than the one currently active in Clean.
2. **Browser-validate the four code-complete items** (Plot Builder resize/PNG · OLS residual plots · Spatial heatmap · Frozen diagnostics) — all ship in commits `35f4b5a6` / `5e105529`; Franco's sign-off closes them.
3. **Add "limits" tool to CalculateTab** (`src/components/tabs/CalculateTab.jsx`:1556 + `src/math/calcEngine.js`) — self-contained new tool panel; natural companion to the existing integral section; direct user demand from 2026-06-19.
4. **ClaudeFB.md strikethrough cleanup pass** — 30 resolved items still show as open; the markup debt makes the log misleading and adds noise to future analyst runs.
5. **Implement R `%in% c(...)` filter pre-processor** (`src/components/wrangling/CleanTab.jsx`) — low-effort; removes a recurring friction point for R-native users.
6. **Implement no-intercept estimation flag** (`src/math/LinearEngine.js`, `ModelConfiguration.jsx`, export scripts) — well-understood change; unblocks a standard class of restricted models.
