# Feedback Analysis Report — 2026-08-10

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **15 open bugs** · **14 open features** · **1 open UX** · **1 open performance**

Items marked *(code-complete, browser validation pending)* were coded in a prior session but have not been browser-confirmed by Franco. Items marked *(fix-later)* were triaged in the X5 reconciliation as deferred post-launch.

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work.
**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`)
**Suggestion:** The generated R script passes `gname = treatCol` to `att_gt()`, but Callaway-Sant'Anna requires `gname` to be a **numeric first-treatment-period cohort variable**, not a binary treatment indicator. If the user passes a binary column, R will throw "gname must be numeric" or produce invalid results. Add a validation check that warns when `treatCol` is binary before emitting the script, and document in the code comment that `gname` must be the cohort integer (e.g. `first_treat`). Also verify that the `base_period = "varying"` default matches what Litux's `CallawayEngine.js` uses — if the engine sets a fixed base period, the script should match.
**Invariants:** Architectural scripts live only in `src/services/export/rScript.js` — no changes to `src/math/`. Keep the Callaway spec field names (`treatCol`, `compGroup`, `csEstMethod`) consistent with `CallawayEngine.js`.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely.
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** This broad report most likely refers to the Stata tab in the code viewer failing to render a runnable do-file for some estimators. Cross-check every `transpileModel()` branch in `stataScript.js` against the `seType` fixes logged in the 2026-07-17 BugTriage entry (many panel estimators were previously emitting classical SE regardless of `seType`). Run the 168-cell matrix validation mentioned in BugTriage (`seTolerances.js`) and flag which branches fail. Focus on two-way cluster (`reghdfe`) syntax and `vce(hc2)`/`vce(hc3)` for linear models, as those were the known failures.
**Invariants:** `stataScript.js` is a pure export function — no React, no IndexedDB, no API calls. All `seType` values must be passed in from the caller; do not read them from module state.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise — e.g. computing ATE as the difference in means between treatment and control group.
**File:** `src/pipeline/runner.js` (`case "group_summarize"`, line 528), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** The `group_summarize` step likely supports only single-column aggregations (`mean(y)`, `sum(x)`) but not expressions that reference multiple groups or cross-group arithmetic. To compute ATE, users need a two-row result (treatment=0/1) and then a cross-row difference — which requires either: (a) a second pipeline step (`mutate` on the summarized table), or (b) a new "pivot and diff" pattern. The quickest fix is to add a hint in `ReshapeTab.jsx` that guides users toward the two-step pattern: `group_summarize → pivot_wider → mutate`. Longer term, evaluate whether a `"diff"` aggregation keyword (like `diff(mean(y))` across the grouping variable) belongs in the runner.
**Invariants:** Pipeline steps must replay on `rawData` — never mutate in place. `runner.js` is the source of truth; any new aggregation keyword must also be added to `registry.js` and `auditor.js`.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well.
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/loadLine.js`
**Suggestion:** `PlotBuilder.jsx` generates R/Python/Stata export scripts for plots (via `generateStataScript` / equivalents wired in `CodeEditor.jsx`) but calls these exporters without routing through `loadLine.js`'s `buildRLoadLine`/`buildPyLoadLine`/`buildStataLoadLine`. The session snapshot already carries `loadOpts` per dataset (implemented in the 5b session-aware Report-AI pass). Wire `allDatasets` and the active dataset's `loadOpts` through `PlotBuilder`'s export config object — the same way `CodeEditor.jsx` does for model exports — so the plot script begins with the correct `read_csv`/`haven::read_dta`/`import delimited` call.
**Invariants:** Load lines must honor per-dataset `loadOpts` (delimiter, sheet name, engine) — never hardcode `read.csv`. The `buildRLoadLine` / `buildPyLoadLine` / `buildStataLoadLine` helpers in `loadLine.js` are the single source of truth for this logic; do not duplicate it in `PlotBuilder.jsx`.

---

### [high] · UI · 2026-06-11 *(code-complete, browser validation pending)*
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`
**Suggestion:** Code was marked complete in commit `35f4b5a6` (2026-06-14) — the plots were already wired but broke due to a stale `result.spec` regression: `ModelingTab` was passing a mutable spec object instead of the frozen estimation result, so sidebar edits clobbered the display. The fix froze OLS/WLS diagnostics to `result.spec`. Franco must browser-validate: run OLS, confirm all three plots appear, change a variable in the sidebar, confirm the plots remain frozen until re-estimation.
**Invariants:** Diagnostic plots must read from the frozen `result` returned by the engine — never from live React state. `ResidualPlots.jsx` must have zero React imports from the math side; it receives pre-computed residuals as props.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases.
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer()` in `latexTable.js` uses a fixed `tabular` column spec. When a model comparison has many columns (e.g. 6+ models), the table overflows the page margin in LaTeX. Emit `\resizebox{\textwidth}{!}{ … }` around the `tabular` environment when the column count exceeds a threshold (e.g. ≥5 models), or switch to `\adjustbox{max width=\textwidth}` (requires `adjustbox` package — add to the package header). The simplest fix is the `resizebox` wrapper; add the LaTeX comment `% requires \usepackage{graphicx}` in the output.
**Invariants:** `latexTable.js` is a pure export function — no React, no side effects. `buildStargazer` signature must remain unchanged; the column-count logic lives inside the function body.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** The time series geom in `PlotBuilder.jsx` (`line` geom with a time x-axis) renders without user-configurable title, x-label, or y-label controls. Add a "Labels" section in the plot panel (following the existing `labels` panel pattern already present for other geoms) with `title`, `xLabel`, `yLabel` text inputs. Pass them to Observable Plot's `x: { label }` / `y: { label }` options and the `title` mark. Mirror these into the R export as `labs(title=…, x=…, y=…)` and the Python/Stata equivalents.
**Invariants:** Use inline styles via the `C` object — no CSS files. Observable Plot settings (`Plt.*`) are the render target; R/Py/Stata exports go through the existing `plotScript.js` / `stepTranslators.js` pattern.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer.
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The Data Viewer has column-header rendering but no click-to-sort interaction. Add click handlers to each column header that toggle ascending/descending sort state (a per-column `sortDir` state object). For DuckDB-backed datasets, inject `ORDER BY "${col}" ASC/DESC` into `getTablePage`'s SQL query so sorting happens server-side and is not limited to the 500-row preview. For JS-backed datasets, client-side sort with `[...rows].sort(...)`. Use the same two-arrow chevron pattern (↑↓/↑/↓) as standard data grids.
**Invariants:** Never mutate `rawData` — the sort is display-only, not a pipeline step. DuckDB queries follow the `getTablePage` pattern in `ExplorerModule.jsx` — add the `ORDER BY` clause there, not in `duckdbRunner.js`. Display limit ≠ computation limit; the sort must still operate on the full DuckDB table for large datasets.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a `case_when`, going back to Clean to see variable names, and coming back to find the work gone.
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` is a large controlled component with local `useState` for formula drafts, case_when rows, and expression strings. These are lost on unmount (tab switch). Lift the draft state into `WranglingModule`'s top-level state (alongside `pipeline`) so it survives tab navigation within the same session. Alternatively, use `sessionStorage` keyed by `pid + "featureTab_draft"` — simpler than lifting state and survives tab switches without IndexedDB overhead. The `Clean` tab already uses similar patterns for its filter builder; mirror that approach.
**Invariants:** Draft state must be scoped to `pid` — never bleed between projects. It is display state only, not a pipeline mutation. Persistence must use `pid`-keyed `sessionStorage` (not `localStorage`), matching the project-scoped pattern elsewhere.

---

### [medium] · UI · 2026-06-10 *(code-complete, browser validation pending)*
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Commit `35f4b5a6` added native vertical resize with bounded responsive canvas sizing, so PNG captures user-selected dimensions. Franco must browser-validate: drag the resize handle, confirm the canvas grows, export PNG, confirm the exported image matches the on-screen dimensions without cropping or stretching.
**Invariants:** `PlotBuilder.jsx` uses `ResizeObserver` (`useContainerWidth.js`) for responsive sizing — the PNG export must capture the `ResizeObserver`-reported width, not a hardcoded constant.

---

### [medium] · UI · 2026-06-14 *(code-complete, browser validation pending)*
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Commit `35f4b5a6` froze OLS/WLS display to `result.spec`. Franco must browser-validate: run OLS, add a variable in the sidebar, confirm plots do not change, pin the model to the buffer, switch to a different pinned model and back, confirm the original plots restore correctly.
**Invariants:** All plots must read from the frozen `result` passed as a prop — never from live `xVars`/`yVar` sidebar state.

---

### [medium] · UI · 2026-06-13 *(code-complete, browser validation pending)*
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Commit `5e105529` restored Heatmap as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Franco must browser-validate: load a lat/lon dataset, switch to Map → add Heatmap layer, confirm it renders, adjust opacity, export HTML.
**Invariants:** The heatmap KDE is computed client-side from the dataset's lat/lon columns — no server calls. The 5k-point cap is enforced at the Leaflet layer level, not by truncating `rawData`.

---

### [medium] · Math · 2026-07-09 *(needs confirmation)*
**Feedback:** Add options for fixed effects — not just entity and time, but also variables like country, state, sector; every categorical variable must be allowed as FE.
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage notes this is "likely fixed" by commit `045f5cda` (#104) which shipped N-way FE. Franco should confirm in-browser: open a dataset with a categorical `sector` or `country` column, run FE with that column as the FE dimension, verify the result is demeaned on that column. If the UI `ModelConfiguration.jsx` only offers `entityCol`/`timeCol` pickers and not arbitrary column pickers, that is the remaining gap.
**Invariants:** `runFEMulti` in `PanelEngine.js` already accepts an arbitrary `feCols` array — the gap (if any) is purely in the UI picker, not the math. SE type must be passed explicitly; `runFEMulti` must not hardcode SE.

---

### [medium] · Math · 2026-07-09 *(needs confirmation)*
**Feedback:** Add also FE with interactions.
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions`)
**Suggestion:** BugTriage notes this is "likely fixed" by commit `045f5cda` (#104). Franco should confirm: run a TWFE spec with `treat × time` as an interaction term alongside FE dimensions, verify the interaction coefficient appears in the output. The `factor_interactions` step in `runner.js` pre-expands interactions; check whether the FE within-demean step in `PanelWithinEngine.js` correctly handles pre-expanded interaction columns.
**Invariants:** `src/math/` and `src/core/` have zero React imports. SE type must be passed explicitly.

---

### [medium] · Data · 2026-05-10 *(fix-later)*
**Feedback:** Parse error: CSV Error on Line 1583 — `China,CHN,2010,2425.47…` (truncated).
**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/`
**Suggestion:** A malformed row mid-file causes the entire import to abort with no recovery options. The fix (deferred post-launch per X5) is: catch parse errors per-row, record the offending line number, and surface a recovery modal that offers (a) skip the bad rows and continue, or (b) show a diff preview of what was dropped. For DuckDB-backed imports (>10 MB), DuckDB's `COPY FROM` with `IGNORE_ERRORS=true` and `REJECTS_TABLE` can collect bad rows without aborting.
**Invariants:** `DataStudio.jsx` is the single entry point for file parsing. Any resilient-parse logic goes in `parseFile()`, not in the individual parsers. DuckDB error handling must use the `_duckdb` singleton pattern from `src/services/data/duckdb.js`.

---

## Features

### Model · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis.
**File:** `src/math/ModelHypothesis.js`
**Suggestion:** `ModelHypothesis.js` currently computes F-statistics for linear hypothesis tests (`R β = r`). The chi-square variant is simply `χ² = q·F` (where `q` is the number of restrictions), which follows a `χ²(q)` distribution asymptotically. Add a `chiSquare` field to the returned object and a chi-square p-value alongside the existing F-p-value. In `CoefficientTestPanel.jsx`, surface both statistics in the output row so users can choose which to cite (journals differ on convention). Export: `car::linearHypothesis(fit, hyp, test="Chisq")` in R.
**Invariants:** `ModelHypothesis.js` is in `src/math/` — zero React imports. The chi-square statistic is a deterministic derivation from the F-statistic, not a new numerical procedure.

---

### Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R.
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently runs one hypothesis at a time and discards the result on re-estimation. Add a "Save hypothesis" button that pushes the current test (hypothesis string + result) onto a `savedHypotheses` array in component state (backed by `sessionStorage` keyed by `pid`). Render the saved list as collapsible rows below the test input. The R export should emit `car::linearHypothesis(fit, c("β1 = 0", "β2 = 0"))` for the saved vector, which R evaluates as a joint Wald test. For individual tests, emit them as separate `linearHypothesis` calls.
**Invariants:** Saved hypotheses are display/export state only — not a pipeline step. Must be `pid`-scoped via `sessionStorage`. Export logic goes in `rScript.js`, not in `ModelHypothesis.js`.

---

### Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation.
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** The prompt already handles log-log (elasticity) and log-level (semi-elasticity) via functional-form rules (lines 99, 326–354 in `Prompts/index.js`). The gap is that the app does not automatically infer the functional form from the variable names (e.g. `ln_gdp`, `log_wage`). Add a rule in `INTERPRET_REGRESSION_PROMPT` that when a variable name starts with `log_`, `ln_`, `log(`, or `l_`, it should be treated as log-transformed and the elasticity/semi-elasticity rule applied accordingly. Alternatively, surface a "compute marginal effect / elasticity at the mean" panel in `CoefficientTestPanel.jsx` that computes `β * (mean(x) / mean(y))` client-side for the selected coefficient.
**Invariants:** All Anthropic API calls go through `AIService.js` only — no raw fetch. Client-side elasticity computation belongs in `ModelHypothesis.js` (pure math, no React), not in the prompt.

---

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria.
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` manages a flat ordered list of saved artifacts. Add a `groups: { [groupId]: { name, ids: string[] } }` field to the persisted artifact order structure, with a default `"Unsorted"` group. In `ModelBufferBar.jsx`, add a "New group" button that creates a named group and a drag-handle that allows re-assigning artifacts to groups. Render groups as collapsible sections. This is a medium-complexity UI feature — define the data shape in `artifactOrder.js` first (schema change), then update the UI.
**Invariants:** Persistence goes through `indexedDB.js` — never `localStorage`. Group membership is stored in `artifactOrder` (not in the artifact records themselves) so the artifact records remain valid across group reorganizations.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step.
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** `History.jsx` renders the pipeline sidebar with step cards. Each step card's `type` maps to a tab and sub-section (e.g. `case_when` → Feature tab → "Custom formula"; `filter` → Clean tab → "Filter builder"). Add a "✎ Edit" icon on each card that calls a new `onJumpToStep(step)` prop. In `WranglingModule.jsx`, handle this by: (1) switching the active tab to the step's owning tab, (2) setting a `draftStep` state that pre-fills the relevant sub-section's form with the step's current params. This is the most user-requested UX improvement in the 2026-07-17 batch.
**Invariants:** The active tab is controlled state in `WranglingModule.jsx` — History callbacks bubble up through props. Draft pre-filling is display-only and uses `sessionStorage` keyed by `pid`; it does not add a pipeline step until the user clicks Apply.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster.
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text `<input>` filter at the top of the variable column panel in `VariableSelector.jsx`. Filter the displayed variable chips client-side as the user types — case-insensitive substring match against the variable name and the dictionary label (from `dictMap`). Keep the filter state local to the component. This is a low-cost, high-impact UX improvement requiring ~20 lines of new code.
**Invariants:** Inline styles via the `C` object only. The filter is display-only — it does not modify `xVars`/`yVar` state. The underlying chip-click behaviour and `onToggle` callbacks are unchanged.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves).
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** In the equation workbench, a constant expression like `y = 5` has no `x` dependency, so the current function plotter cannot graph it (it generates `undefined` for all x values). Add a special case: if the parsed expression evaluates to a finite scalar independent of `x`, draw a `hline` mark at that y value using Observable Plot's `Plt.ruleY([value])`. Label it with the expression string. This mirrors PlotBuilder's `hline` geom and requires ~10 lines of additional plot-building logic.
**Invariants:** Inline styles via `C` object. Observable Plot is loaded via the CDN singleton pattern (no `npm install`). Constant detection must be done after symbolic parsing — do not regex the input string.

---

### Calculate · 2026-06-19
**Feedback:** Add limits.
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" section to the Calculate tab (alongside Derivatives, Integrals, Optimization). Implement using nmath.js's symbolic limit or a numerical approach: evaluate the expression approaching the limit point from both sides with decreasing `ε` (`x = point ± [1e-3, 1e-6, 1e-9, 1e-12]`) and check for convergence. Surface left-limit, right-limit, and whether they agree (the two-sided limit). For simple rational / polynomial expressions, nmath.js or Algebrite.js can give exact symbolic results; for transcendental expressions, numerical is sufficient. Show `±∞` and `DNE` cases explicitly.
**Invariants:** The Calculate tab is a pure client-side computation module — no Anthropic API calls, no IndexedDB for intermediate results. Use the same CAS library already imported in `CalculateTab.jsx` for symbolic work.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color.
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set and the scheme is categorical, the current legend shows color swatches with level names but no value annotations (e.g. counts per group, mean of `y` per color group). Add an optional "Show group stats" toggle in the Labels panel that appends a parenthetical `(n=N, mean=M)` annotation to each legend entry. Compute the stats client-side from `rows` (or via a DuckDB `GROUP BY` query for large datasets). Mirror into R export as `scale_color_manual(labels = c(...))` with stat-annotated strings.
**Invariants:** Inline styles via `C`. Observable Plot legend manipulation uses the `Plot.legend()` call pattern — do not mutate the DOM directly. DuckDB group-by queries follow the `getFilteredRowCount` pattern from `ExplorerModule.jsx`.

---

### Clean · 2026-07-19 *(scoped design needed)*
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform.
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `country_code` transform option to `FeatureTab`'s transform menu (alongside `log`, `sq`, `std`, etc.). The simplest implementation: bundle a small static JSON lookup table (~250 rows: `{iso2, iso3, name, name_short}`) as a module import, expose a `countryCode(colName, "iso2→iso3" | "name→iso2" | …)` helper, and map it to a new `country_code` pipeline step in `runner.js`/`registry.js`. Fuzzy name matching (for free-text country names) can reuse `fuzzyGroups` from `wrangling/utils.js` but should be opt-in — it has false-positive risk for similar country names.
**Invariants:** The static lookup table is pure data (no API calls). The pipeline step must be registered in `registry.js` and handled in `runner.js`. The `runner.js` / `registry.js` registry must stay in sync — enforced by `pipelineReliabilityValidation.mjs`.

---

### Model · X5 fix-later
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", etc., regardless of actual column names.
**File:** `src/services/export/latexTable.js`
**Suggestion:** Add a `labelOverrides: { [colName]: string }` optional parameter to `buildStargazer()`. In `ReportingModule.jsx`, render an inline editable list of variable labels (one row per coefficient, text `<input>`) that populates `labelOverrides`. Persist these per `pid` in `sessionStorage`. On export, substitute `labelOverrides[col]` for the default display name before passing to LaTeX.
**Invariants:** `latexTable.js` is a pure function — the override map is passed in, not fetched. No `localStorage` — use `pid`-scoped `sessionStorage` for the edit state in `ReportingModule.jsx`.

---

### Clean · X5 fix-later
**Feedback:** Add filter `regionname %in% c("Spain", "Basque Country")` R-style membership syntax.
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** `predicate.js` already has an `in` operator (`evalPredicate`, `predicateToSQL`). The gap is the UI in `CleanTab.jsx` / `FilterBuilder` — it does not expose a multi-value text input for the `in` operator. When `op === "in"`, render a tag-input widget (comma-separated, Enter to add) that builds the `values: string[]` array for the predicate. The predicate language and runner are already correct — this is purely a UI input gap.
**Invariants:** `predicate.js` is the single owner of the `in`/`nin` operator logic — do not re-implement membership checking in the filter UI. Any new operator variant must be added to `predicate.js` and nowhere else.

---

### Model · X5 fix-later
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust())` — compact table with coefficients, SEs, CIs, and bandwidth.
**File:** `src/components/modeling/ModelPlots.jsx`, `src/math/CausalEngine.js`
**Suggestion:** `runSharpRDD` returns an `EstimationResult` that includes the IK bandwidth, LATE, SE, and CI. Format these into a compact table (4–6 rows) modeled on `rdrobust`'s printed output: `Method · h · n_l · n_r · Coef · SE · CI_lower · CI_upper`. Mount it in `ModelPlots.jsx` above or below the scatter/RDD plot, collapsible. No new math needed — only a display component.
**Invariants:** The table reads from the frozen `result` prop — never from live sidebar state. Display components in `src/components/` may not import from `src/math/`.

---

### Spatial · X5 fix-later
**Feedback:** Allow spatial filters for plots.
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add a filter panel to `SpatialGeoPlot.jsx` that accepts a bounding-box draw (Leaflet draw) or a column-value predicate (using `predicate.js`) to subset the rows before rendering the geo plot. This is a V4 feature — lower priority than the bug fixes above. A minimal first cut: expose a `filterCol` / `filterValues` pair of dropdowns (categorical filter via the `in` operator) that subset `plotRows` before passing to `mkGeoLayer`.
**Invariants:** Spatial filtering must use `predicate.js` exclusively (`evalPredicate`) — never re-implement filter logic in the component. The filter is display-only and does not add a pipeline step.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Guide tour must be updated.
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`, line 11)
**Suggestion:** The tour was written before N-way FE, Sun-Abraham, Callaway-Sant'Anna, Bacon decomposition, CR2/CR3, the `tile` geom, and facet_wrap shipped. Audit `TOUR_STEPS` against the current feature set and update the step text. Key additions: (1) mention N-way FE in the Model step, (2) update the Explore step to mention `geom_tile` and facet_wrap, (3) update the Calculate step to mention symbolic differentiation/integration, (4) update the Spatial step to mention the pipeline integration. Per `CLAUDE.md` working conventions: **never write a count** ("14 estimators") — name the capabilities instead. The `HintBox` title must be the module name only ("Modeling"), not a verb phrase.
**Invariants:** `HelpSystem.jsx` is pure UI — no math imports. The `TOUR_STEPS` array is the canonical registry; adding a feature elsewhere without a TOUR_STEPS entry violates the working convention in `CLAUDE.md`.

---

## Performance

### Data · 2026-05-08 *(code-complete, browser validation pending)*
**Feedback:** Big datasets generate crashes (21 MB, over 900k rows).
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`
**Suggestion:** Fase X4 (non-materializing DuckDB pipeline, OPFS Parquet cache, 500-row preview split) is code-complete per the X5 reconciliation. Franco must run the browser benchmark: load the 21 MB / 900k-row dataset, measure tab-transition latency, confirm it is ≤2 s after the initial parse. If performance is still unacceptable, the next step per the DuckDB strategy in `CLAUDE.md` is to ensure the Parquet cache is being written to OPFS on first load (check the CSP `connect-src` includes `extensions.duckdb.org` — a documented failure mode from the bug-fixes section) and verify that `runner.js` operations run on the full DuckDB table, not the 500-row preview.
**Invariants:** Server-side computation is a non-starter (privacy-first constraint). Display limit (500 rows) must never be confused with computation limit (full dataset). `SELECT *` on large tables is prohibited — query only needed columns.

---

## Suggested action order

1. **CS DiD replication code** — high-value export that doesn't run; fix `gname` mapping in `rScript.js` line 1432 to validate that the passed column is a numeric first-treatment-period cohort variable (30 min).
2. **Plot replication missing load lines** — a research-integrity issue; the script omits the data load call entirely, making it non-reproducible out of the box. Wire `loadLine.js` into `PlotBuilder.jsx`'s export config (1–2 h).
3. **group_summarize ATE calculation** — frequently requested; unblock by adding a two-step hint in `ReshapeTab.jsx` and, if feasible, a `diff` aggregation mode in `runner.js`/`registry.js` (1–3 h depending on scope).
4. **Stata workbench audit** — run the 168-cell matrix validation and fix any failing branches in `stataScript.js` before the next user-facing release (2–4 h).
5. **Browser validation sweep** — four items marked *code-complete, browser validation pending* (OLS residual plots, Plot Builder resize, spatial heatmap, coefficient freezing) should be confirmed or reopened by Franco before they are silently assumed closed (1 h browser session).
6. **Data viewer sort** — low implementation cost (~30 lines), high daily usability gain; add column-header click-to-sort to `ExplorerModule.jsx`.
7. **Variable searcher in sidebar** — ~20 lines, removes friction for datasets with >20 columns; add to `VariableSelector.jsx`.
8. **Pipeline step → subsection navigation** — the most-requested UX item in the 2026-07-17 batch; medium complexity (~3 h) but high impact for iterative data cleaning workflows.
9. **Guide tour update** — low cost prose edit, high trust signal for new institutional users (LMU licensing context).
10. **LaTeX table resize** — one-liner `\resizebox` wrapper; unblocks multi-model comparison exports that overflow page margins.
