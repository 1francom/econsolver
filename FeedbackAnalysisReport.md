# Feedback Analysis Report — 2026-06-10

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 4 open bugs · 17 open features · 0 open UX/design · 1 open performance · 1 open other

> **Note on BugTriage / ClaudeFB.md discrepancies:** 13 items marked "fixed" in BugTriage.md still
> lack strikethrough in ClaudeFB.md (graph fit screen, border grid clipping, grids do not work,
> dataset-delete variable cleanup, p-value bug, auto-reestimate, clean model option,
> graph-from-0, single equation plot, color references, Monte Carlo single variable,
> spatial unit sizing, Poisson interpretation). These are treated as resolved; ClaudeFB.md
> should be updated with strikethrough + ✓ to match BugTriage.

---

## Bugs

### [medium] · Spatial · 2026-06-06
**Feedback:** Geocoding directions might be upgraded — geocoder address resolution quality needs improvement  
**File:** `src/components/tabs/spatial/analyze/GeocodeSection.jsx`, `src/services/data/geocoding.js`  
**Suggestion:** The current implementation uses Photon/Komoot with an optional bounding box. Resolution quality can be improved by (1) sending structured address fields (street/city/country) rather than a raw concatenated string when columns allow it, and (2) increasing the result-score threshold before accepting a match. Consider adding a "limit=3 + user-picks-best" confirmation step for low-confidence matches (Photon returns a `properties.score` field).  
**Invariants:** Must stay client-side or go through the existing Supabase proxy pattern — no new raw fetch to non-whitelisted origins.

---

### [high] · Data · 2026-05-13
**Feedback:** .dta files are wrong loaded and readed  
**File:** `src/services/data/parsers/stata.js`  
**Suggestion:** BugTriage records this as "pending" (a prior fix targeting format-119 variable name stride 33→129 landed 2026-06-03 for a separate .dta report). Investigate whether the 2026-05-13 complaint is a distinct variant — e.g. format-117/118 regression, label encoding, or a file generated by a newer Stata version. Add test fixtures covering format-117, -118, and -119 with known column counts and spot-check the first and last row values to catch stride regressions early.  
**Invariants:** Parser must live in `src/services/data/parsers/stata.js`; zero React imports.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated in source)  
**File:** `src/DataStudio.jsx` (`parseCSV` function, line 40)  
**Suggestion:** The file is smaller than 10 MB (otherwise DuckDB's `loadLargeCSV` would handle it), so the JS `parseCSV` path is hit. The error on line 1583 suggests a row with an unescaped quote, embedded newline inside a quoted field, or a non-UTF-8 character. Add a graceful fallback: if `parseCSV` throws after parsing ≥100 rows, retry with `TextDecoder("latin1")` and surface a warning banner. Also normalise line endings (`text.replace(/\r\n/g,'\n').replace(/\r/g,'\n')`) and strip BOM before parsing.  
**Invariants:** Encoding fallback logic belongs in `DataStudio.jsx`; never mutate `rawData` in place.

---

## Features

### Model · 2026-05-11
**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength  
**File:** `src/math/CausalEngine.js` (lines 14–15, 201), `src/components/modeling/results/IVResults.jsx` (lines 59–75)  
**Suggestion:** The math is already implemented (`arCI` in `CausalEngine.js` line 201) and displayed in `IVResults.jsx` (lines 59–75) for the single-endogenous-regressor case. The feature request likely refers to: (a) extending AR CIs to the multi-instrument/over-identified case (projection AR or conditional likelihood ratio), and/or (b) surfacing the AR CI more prominently — e.g. in the main coefficient table rather than only the side-panel. First confirm whether the user's 2SLS spec is single-endogenous-regressor and they simply haven't found the display; if so, close and update ClaudeFB.md.  
**Invariants:** New math must go in `src/math/CausalEngine.js`; no React imports there.

---

### Model · 2026-05-11
**Feedback:** Add multiple statistics tests  
**File:** `src/math/SampleTests.js`, `src/math/ModelHypothesis.js`, `src/components/tabs/statsim/SampleTestPanel.jsx`  
**Suggestion:** The request is vague; it likely refers to post-estimation tests not yet surfaced in the modeling tab. Candidates: Chow structural-break test, Hausman endogeneity test (computable from 2SLS first stage), Ramsey RESET, Sargan/Hansen J (already in `GMMEngine.js`), and two-sample Kolmogorov-Smirnov. Propose a collapsible "Additional Tests" panel in `ModelingTab.jsx` calling existing `SampleTests.js` and `ModelHypothesis.js` functions. Clarify scope with user before implementing.  
**Invariants:** New math functions belong in `src/math/SampleTests.js` or a new peer file; zero React.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/geo.js`  
**Suggestion:** Grid overlay support already exists in the Analyze tab via `SpatialEngine.js`. To add it to the Geo Plot tab, add a new layer type `"grid"` in `GeoLayerConfig.jsx` with a WKT column picker (grid cell polygons) and a count/value column for choropleth fill. The renderer in `SpatialGeoPlot.jsx` already handles WKT polygons via `wktToLeaflet`, so the main work is extending the layer-type selector and `mkGeoLayer` in `geo.js`.  
**Invariants:** All styling via `C` constants; no external UI libraries.

---

### Simulate · 2026-05-18
**Feedback:** Add resampling, bootstrapping, sampling to the mean  
**File:** `src/components/tabs/statsim/StatWorkspace.jsx` (lines 702–1058), `src/math/Resampling.js`  
**Suggestion:** Bootstrap, subsample, and permutation inference are already implemented in `StatWorkspace.jsx` (imports `bootstrapMean`, `subsampleMean`, `permutationTwoSampleMean`, etc. from `Resampling.js`). Confirm whether the user is looking for these features in a different location — e.g. the Simulate tab's DGP/Monte Carlo section — rather than the Stats Workspace sub-tab. If so, add a shortcut link or expose the resampling panel from `SimulateTab.jsx`. This may already be done; verify and close if so.  
**Invariants:** None — purely UI wiring.

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option  
**File:** `src/components/wrangling/CleanTab.jsx` (`FilterBuilder`, lines 606–767)  
**Suggestion:** An "in list" operator already exists in `FilterBuilder` (line 606: `{ v:"in", l:"in list" }`). The user likely hasn't discovered it, or the UX for entering multiple string values is unclear. Improve the "in list" value input to show a multi-tag input (comma-separated pills) with auto-complete from distinct column values, mirroring R's `%in% c(...)` mental model. The runner already handles `op:"in"` with an array of values (line 767) — this is a pure UX improvement.  
**Invariants:** Inline styles only via `C`; no CSS files.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/math/CausalEngine.js`, `src/components/ModelingTab.jsx`, new `src/components/modeling/results/RDDResults.jsx` (suggested)  
**Suggestion:** Create `src/components/modeling/results/RDDResults.jsx` mirroring the `IVResults.jsx` pattern. It should display: bandwidth (h), polynomial order, kernel, effective n (left/right), conventional/robust/bias-corrected LATE, SE, CI, p-value, and first-stage F for fuzzy RDD. All values are already available in the `CausalEngine.js` result object. Wire it into `ModelingTab.jsx` at the RDD result render site.  
**Invariants:** Inline styles via `C`; no new math in the UI file.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept  
**File:** `src/math/LinearEngine.js` (line 128), `src/components/ModelingTab.jsx`, `src/components/modeling/ModelConfiguration.jsx`  
**Suggestion:** `LinearEngine.js` always prepends a column of ones (line 128: `const k = xCols.length + 1`). Add an optional `noIntercept: boolean` parameter to `runOLS` and `runWLS`; when `true`, skip the ones column and adjust df to `n − k` (without the intercept column). Add a "Suppress constant" checkbox in `ModelConfiguration.jsx`. The DuckDB SQL fast-path in `duckdbOLS.buildOLSSuffStats` must also handle the no-intercept case by omitting `1.0` from the column list.  
**Invariants:** `seType` must still be passed explicitly; no hardcoding inside engine functions.

---

### Spatial · 2026-05-30
**Feedback:** Geometry transformations and show EPSG  
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`, `src/components/tabs/spatial/shared/crs.js`  
**Suggestion:** `CRSTransformSection.jsx` already handles CRS reprojection. Extend it to: (1) display the detected/assigned EPSG code for the active dataset alongside the geometry column header in the Analyze tab (read from `rawData._crs`), and (2) expose `PRESET_CRS` from `crs.js` as a labelled dropdown with EPSG codes (e.g. "EPSG:4326 — WGS84", "EPSG:32632 — UTM zone 32N") rather than raw WKT strings. Show a CRS badge in the data header row when `_crs` is set.  
**Invariants:** `crs.js` must remain zero-React; EPSG display logic lives in the component layer only.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/shared/wkt.js`  
**Suggestion:** Add a bounding-box filter to the Geo Plot tab: a "Filter by extent" toggle with min/max lat/lon inputs; rows outside the bbox are excluded from all layers for that render. A more advanced variant uses a reference WKT polygon as a clip mask via a `pointInRing` check (leverageable from `wkt.js` helpers). Filtering must use a derived local copy — never mutate `rawData`.  
**Invariants:** No mutation of `rawData`; use a derived filtered copy inside the component.

---

### Data · 2026-06-01
**Feedback:** Add an option to edit/assign column names directly in the Data Viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** The data viewer supports cell editing (prior fix). Extend the header row: make each column header cell double-click-editable (inline `<input>` on dblclick, blur/Enter to confirm). On confirmation, emit a `rename` pipeline step (`{ type:"rename", from: oldName, to: newName }`) via the existing `onStep` prop — this makes the rename non-destructive, replayable, and visible in the audit trail. The `rename` step type is already implemented in `runner.js`.  
**Invariants:** Must emit a `rename` step — never mutate `rawData` directly.

---

### Simulate · 2026-06-02
**Feedback:** You could add different types of quantiles like in R, but it might not be necessary  
**File:** `src/components/tabs/statsim/StatWorkspace.jsx`, `src/math/SampleTests.js`  
**Suggestion:** Low-priority. R supports 9 quantile interpolation types (default type 7). Add a `quantileType` selector (dropdown, default type 7 — linear interpolation) to the StatWorkspace descriptive-stats section. Implement type-1 (inverse of empirical CDF), type-7 (linear), and type-4 (lower) in `src/math/SampleTests.js`.  
**Invariants:** Math in `src/math/SampleTests.js` only; zero React.

---

### Spatial · 2026-06-03
**Feedback:** Add geometry to plot points, not just lat and lon  
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx` (lines 219–250), `src/components/tabs/spatial/plot/geo.js`  
**Suggestion:** `GeoLayerConfig.jsx` already has a mode toggle `[["latlon","Lat/Lon"],["wkt","WKT geometry"]]` (line 219). First confirm whether the WKT mode renders correctly — if so, this is a discoverability issue and a tooltip or hint is all that's needed. If WKT point mode is broken, investigate `mkGeoLayer` in `geo.js` for the WKT → GeoJSON point conversion path.  
**Invariants:** None.

---

### Calculate · 2026-06-03
**Feedback:** Add a "show steps button" for integrals, derivatives and optimizations  
**File:** `src/components/tabs/CalculateTab.jsx` (line 18 imports — `derivative`, `nthDerivative`, `integrate`, etc.)  
**Suggestion:** Add a collapsible "Show working" panel below each result. For derivatives: display the differentiation rule applied at each step (product rule, chain rule, constant rule) as a string array. For numerical integration: show the interval breakdown and partial sums. This requires extending the underlying math functions to optionally return `{ result, steps: string[] }`. The math utilities must stay zero-React; `CalculateTab.jsx` consumes the `steps` array for display only.  
**Invariants:** Math utility files must remain zero-React.

---

### Model · 2026-06-04
**Feedback:** Persistence across sessions of pinned models  
**File:** `src/services/modelBuffer.js`, `src/components/ModelingTab.jsx` (line 395)  
**Suggestion:** The infrastructure is already in place: `modelBuffer.setProject(pid)` loads from IndexedDB on mount (line 395), and `trimResult` persists a comparison-sufficient snapshot. If models are not restoring, the likely cause is a race condition where `bufferVersion` increments before the async `setProject` resolves, or that `trimResult` strips fields needed for the model-comparison table labels (`label`, `type`, `varNames`). Verify `getAll()` returns non-empty after `setProject` resolves, and check the model-comparison table renders correctly from a `trimResult`-projected record. Add a debug log in development mode showing how many models were loaded from IDB.  
**Invariants:** Persistence must use `indexedDB.js`; never localStorage.

---

### Calculate · 2026-06-06
**Feedback:** Add derivatives with endogenous functions (e.g. f(x) = 2x² − c(x), compute dc/dx symbolically)  
**File:** `src/components/tabs/CalculateTab.jsx` (line 18 imports), math utility files  
**Suggestion:** The existing symbolic differentiator already handles named unknown functions (CalculateTab.jsx header comment lines 9–10: "6. Symbolic derivative (unknown functions, product/chain rule)"). Test `derivative("2*x^2 - c(x)", "x")` — expected output `4*x - c'(x)`. If this already works, close the request and update ClaudeFB.md. If it fails, extend the differentiation engine to treat `f(x)` patterns as opaque symbols, appending `'` to their name in the output expression.  
**Invariants:** Math in utility files only; zero React.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names  
**File:** `src/math/CausalEngine.js` (lines 577–579, 844–847), `src/services/data/duckdbRDD.js` (lines 44–45), `src/math/EstimationResult.js` (line 553)  
**Suggestion:** Replace the hardcoded label strings with actual column names from the RDD spec. In `CausalEngine.js` `runSharpRDD`, substitute `treatVar` for `"D (treatment)"` and `runningVar` for `"running"` when constructing `varNames` (lines 577–579). Apply the same substitution in `runFuzzyRDD` (lines 844–847), `RDDSuffStatsEngine.js` (line 166), `EstimationResult.js` (line 553), and `duckdbRDD.js` (lines 44–45). This is a targeted label-only change and will propagate automatically to the LaTeX export and coefficient table.  
**Invariants:** Change only label strings — do not touch any matrix algebra; no React imports in math files.

---

### Simulate · 2026-06-08
**Feedback:** Add studentization for permutation tests  
**File:** `src/components/tabs/statsim/StatWorkspace.jsx` (lines 707, 1135–1147), `src/math/Resampling.js`  
**Suggestion:** Studentized permutation tests are already implemented in `StatWorkspace.jsx`: the `rsCompare` toggle (line 707) enables raw-vs-studentized side-by-side output; `studDiffMeans` (line 1135) pivots by within-permutation SE; regression-slope studentization is also present (lines 1285–1303). This feature appears fully implemented. Verify the user is not asking for a specific case not yet covered (e.g. studentization for one-sample or paired tests), then close and update ClaudeFB.md.  
**Invariants:** New math in `src/math/Resampling.js`; no React.

---

### Simulate / Resampling · 2026-06-08
**Feedback:** `Add studentization for permutation tests`
**File:** `src/math/Resampling.js` (`permutationTest`, line ~233), `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** `permutationTest` in `Resampling.js` already supports a `statName` argument. Add `statName === "studentizedDiff"` as a new option: the per-permutation statistic becomes `(ȳ_A − ȳ_B) / sqrt(var_A/n_A + var_B/n_B)` (Welch-style studentization). The observed statistic and the permutation null distribution both use this studentized ratio, making the test more robust to heteroskedasticity and unequal group sizes. Wire the option into `StatWorkspace.jsx` as a "Studentize" checkbox next to the permutation test controls.
**Invariants:** `src/math/Resampling.js` must remain zero-React. The `studentized` variant must still respect the `alternative` (two-sided/greater/less) parameter and the seeded RNG.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` (line 379 — 10 MB DuckDB routing threshold), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`  
**Suggestion:** Files >10 MB are already routed to `loadLargeCSV` (DuckDB path, line 379), so a 21 MB file should bypass the JS parser. The crash likely has one of two root causes: (1) COOP/COEP headers missing in production (`vercel.json`), disabling `SharedArrayBuffer` and forcing DuckDB-Wasm into single-threaded mode, which can OOM on 900 k rows; or (2) a downstream JS step (pipeline runner, ExplorerModule table render, or a `SELECT *` in `duckdbRunner.js`) materialising all 900 k rows as JS objects — a direct violation of CLAUDE.md DuckDB rule 1. Audit `vercel.json` for the required `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` headers, then grep `duckdbRunner.js` for `SELECT *` on un-paginated queries.  
**Invariants:** Never pull full 900 k rows into JS objects; always query only columns needed and filter/aggregate/paginate inside DuckDB.

---

## Other

### Data · 2026-05-10
*(Also listed under Bugs above — cross-referenced here for completeness.)*  
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated in source)  
**File:** `src/DataStudio.jsx` (`parseCSV`, line 40)  
**Suggestion:** The raw line resembles World Bank country-year data. A numeric value truncated in the feedback may contain a Unicode character, an unescaped quote, or CRLF inside a quoted field. Pre-process: strip BOM, normalise line endings, then fall back to `latin-1` encoding on parse failure. See full suggestion in Bugs section above.  
**Invariants:** Fix in `DataStudio.jsx` only; no `rawData` mutation.

---

## Suggested action order

1. **Fix hardcoded RDD variable labels** (`CausalEngine.js` lines 577–579, 844–847; `duckdbRDD.js` lines 44–45; `EstimationResult.js` line 553) — surgical label substitution, zero math risk, immediate user-visible improvement.
2. **Resolve .dta "wrong loaded" regression** (`src/services/data/parsers/stata.js`) — BugTriage is "pending"; blocks data loading for the core LMU audience using Stata files.
3. **Verify COOP/COEP headers in `vercel.json`** — if missing, DuckDB-Wasm falls back to single-threaded mode and OOMs on 900 k-row files; one-line fix with large performance impact.
4. **Geocoding quality** (`GeocodeSection.jsx` + `geocoding.js`) — score-threshold filter + structured-field mode; BugTriage is "open".
5. **CSV parse error recovery** (`DataStudio.jsx`) — encoding fallback + CRLF normalisation; low-effort, defensive.
6. **RDD rdrobust-style summary table** (new `RDDResults.jsx`) — high value for researchers comparing to R output; all data already available in result object.
7. **Estimate-without-intercept option** (`ModelConfiguration.jsx` + `LinearEngine.js` `noIntercept` param + DuckDB path guard).
8. **Investigate model-buffer persistence race** (`modelBuffer.js` + `ModelingTab.jsx` line 395) — if pinned models silently disappear on reload it breaks the multi-model comparison workflow.
9. **ClaudeFB.md housekeeping** — add strikethrough + ✓ to the 13 items BugTriage marks "fixed" but ClaudeFB.md has not updated.
