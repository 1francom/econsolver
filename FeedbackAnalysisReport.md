# Feedback Analysis Report — 2026-06-18

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.
> Supersedes 2026-06-17 report.

## Summary
- 7 open bugs · 5 open features · 0 open UX · 1 open performance · 1 open other

> **ClaudeFB.md hygiene note:** Many bullets from 2026-05-11 through 2026-06-14 batches are reconciled as "verified fixed" or "fix-now" in the X5 table but have not been struck through in their original bullet. Those are treated as resolved here (per X5/BugTriage authority). Only items where BugTriage status = `open` or `fix-later`, or items absent from all reconciliation tables, are treated as truly open.

---

## Bugs

---

### critical · Security · 2026-06-13
**Feedback:** ⚠️ SECURITY EVENT: XSS injection attempt submitted via feedback form — JavaScript payload in description field. (A second probe with `<script>` and `<img onerror>` tags followed immediately.)
**File:** `src/services/feedback/feedbackService.js` (client-side `sanitizeFeedback`); `supabase/functions/collect-feedback/index.ts` (server-side ingestion path — **currently no sanitization before the Supabase INSERT**)
**Suggestion:** The client-side `sanitizeFeedback()` added in `bc2d7b9d` escapes `&<>` before the INSERT, making stored payloads inert in the feedback-collector markdown dump. However, a direct HTTP POST to the Supabase REST endpoint bypasses the client entirely, so malicious payloads can still reach the `feedback` table. Add a Postgres `BEFORE INSERT OR UPDATE` trigger on the `feedback` table that escapes `<>&"'` in the `description` column before the row lands. Alternatively, add a `CHECK` constraint that rejects any description containing `<script`, `onerror`, or `javascript:`. This is the only remaining gap after the client-side fix.
**Invariants:** The trigger lives in a Supabase migration SQL file — no React, no `src/` changes required.

---

### high · Model · 2026-06-17
**Feedback:** Pipeline only applies if dataset is also selected in Clean Module
**File:** `src/WranglingModule.jsx` (`onReady` guard at line 219, `onComplete` at line 410); `src/components/ModelingTab.jsx` (receives `cleanedData` prop from `DataStudio`)
**Suggestion:** `DataStudio` mounts a single `WranglingModule` keyed on `activeDs.id` and passes its `onComplete` payload as `cleanedData` to `ModelingTab`. When a user switches active datasets in the Data tab without visiting Clean, `WranglingModule` may never emit `onComplete` because the `idbReady` guard at line 219 gates `onReady` — if the IDB pipeline load is still in flight, the emit is skipped and `ModelingTab` keeps stale data from the prior dataset. Fix: ensure `onReady` always fires with the raw dataset + the loaded pipeline (even if the pipeline is empty) once `idbReady` becomes true, regardless of whether the user has visited Clean. Check that `DataStudio` propagates the `onReady` payload through `onOutputReady` as the initial `cleanedData`.
**Invariants:** Pipeline must replay on `rawData` via `runner.js` — do not short-circuit by caching transformed data outside the runner. Persistence goes through `indexedDB.js`, not localStorage.

---

### high · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx` (result display, `result.spec` freezing — shipped in commit `35f4b5a6`)
**Suggestion:** The freeze was coded in `35f4b5a6` but **browser validation is still pending**. Smoke-test required: (1) run OLS, (2) add/remove a regressor in the sidebar, (3) verify the coefficient table and diagnostics panel do not update until the user clicks Estimate, (4) pin the model, switch estimators, restore the pin — confirm the frozen display comes back correctly. If step 3 fails, check whether `ModelingTab` is reading `result.spec` (frozen) or `xVars` (live sidebar state) to drive the results render.
**Invariants:** None new — this is a state-isolation fix already coded.

---

### high · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx` (`ResidualVsFitted`, `QQPlot`); `src/components/ModelingTab.jsx` (wires plots at lines ~2323–2325 for OLS, ~2379–2381 for spatial)
**Suggestion:** The plots are already wired. The root cause was the frozen-spec regression fixed in `35f4b5a6`. **Browser validation still pending.** Smoke-test: load a dataset, run OLS, confirm the "Residuals" and "Q-Q" tabs appear in the results panel with populated SVG charts. If charts are blank, inspect the result object in the browser console and verify `result.resid` and `result.Yhat` are non-empty float arrays. If they are empty, trace back to `LinearEngine.runOLS` to confirm `resid` and `Yhat` are being computed and attached to the result.
**Invariants:** `ResidualPlots.jsx` must not import from `src/math/` — it receives pre-computed arrays from the estimation result object only.

---

### medium · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx` (ResizeObserver, `resize: vertical` CSS equivalent via inline style); `src/components/ModelingTab.jsx` (Plot Builder container)
**Suggestion:** Vertical resize and bounded responsive canvas sizing were shipped in `35f4b5a6`. **Browser validation pending.** Smoke-test: open the Plot Builder in ModelingTab, drag the resize handle, confirm the canvas responds. Export as PNG and verify the image dimensions match the visible panel (not a fixed default). If PNG dimensions are wrong, the issue is likely that the Observable Plot `width` option is read from a stale layout measurement — fix by reading `container.offsetWidth` inside the `ResizeObserver` callback synchronously before the plot re-render.
**Invariants:** Resize must use native browser APIs (`ResizeObserver`, CSS `resize: vertical` via inline style) — no external drag libraries.

---

### medium · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` (heatmap Leaflet layer, restored in `5e105529`); `src/components/tabs/spatial/map/SpatialLayerEditor.jsx` (palette/opacity controls)
**Suggestion:** The Leaflet heatmap layer with KDE cells, palette/opacity controls, legend, and 5k-point cap was restored in `5e105529`. **Browser validation pending.** Smoke-test: load a dataset with lat/lon columns, open the Map tab in Spatial, add a Heatmap layer, verify the KDE colour gradient renders over the basemap with legend. If the layer is empty, check that the 5k-point cap sampling produces non-empty coordinate arrays before passing them to the Leaflet heatmap plugin (console-log the point array before the layer add call).
**Invariants:** Leaflet must be loaded via the `loadLeaflet` CDN singleton in `src/components/tabs/spatial/shared/leaflet.js` — do not add a second `<script>` tag or import it directly.

---

## Performance

---

### high · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (`parseFile` DuckDB routing, ~line 403); `src/services/data/duckdb.js` (OPFS Parquet cache, DuckDB singleton)
**Suggestion:** Fase X4 is code-complete: large `.dta` files are parsed once, registered as Arrow-backed DuckDB tables, and OPFS-cached. **Browser benchmark pending Franco.** Validate on a real 900k-row file: open the project, confirm the ⚡ DuckDB badge shows in the wrangling header, switch tabs several times and measure transition time. If lag persists, audit whether `ExplorerModule` is pulling the full Arrow result into JS objects on mount (violates the "DuckDB as data boundary" rule in CLAUDE.md). For large CSV/TSV, confirm `parseFile` routes to the DuckDB loader (not the inline JS `FileReader` path) when file size > 10 MB.
**Invariants:** `rawData` must never be truncated before `runner.js` — display is capped at 200–500 rows (virtualised) but all computations use the full DuckDB table. Persistence via OPFS / `indexedDB.js` only — never localStorage.

---

## Other

---

### medium · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...
**File:** `src/DataStudio.jsx` (inline CSV parsing path inside `parseFile`); no dedicated `src/services/data/parsers/` CSV module exists yet
**Suggestion:** The current CSV parser aborts on the first malformed row. BugTriage defers a full resilient-import pass to post-launch. Quick-win fix: wrap the per-row split in a `try/catch`, collect error rows with their original 1-indexed line numbers, and surface a non-blocking warning banner after import ("N rows skipped — download error log"). The full repair-preview UX (show the bad row, offer skip or manual fix) can follow as a second pass. Malformed rows should be skipped and their original text preserved in a downloadable error report rather than aborting the whole import.
**Invariants:** Parsed data must still land in the DuckDB path for large files. Skipped-rows list belongs in component state, not persisted to IndexedDB or localStorage.

---

## Features

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/workers/exprEval.worker.js` (filter mode, lines 55–61); `src/services/exprEvalService.js` (eval wrapper); `src/components/wrangling/CleanTab.jsx` (filter expression UI)
**Suggestion:** The filter mode evaluates arbitrary JS boolean expressions via `Function(…, return !!(${expr}))`. R's `%in%` operator is not valid JS, so expressions like `regionname %in% c(…)` throw a syntax error. Add a pre-processing step in `exprEvalService.js` (before the worker call) that rewrites: `x %in% c(v1, v2, …)` → `[v1, v2, …].includes(x)` and `c(v1, v2, …)` → `[v1, v2, …]` using a regex substitution. This is a pure string-transform pre-pass — the worker's `assertSafeExpr` sandbox check still runs on the transformed expression.
**Invariants:** The `assertSafeExpr` check must still run on the rewritten expression — the transform happens before that check, not after. Do not add R-parsing logic to `src/math/` (pure-JS zone, no UI dependencies allowed).

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js` (`runSharpRDD` return value, lines ~553–579); `src/components/modeling/ModelPlots.jsx` (RDD results render)
**Suggestion:** `runSharpRDD` already returns bandwidth, kernel, polynomial order, LATE, SE, CI, and `nLeft`/`nRight`. Add a `summaryTable` plain-object field to the result that mirrors the rdrobust format: estimator, method, kernel, bandwidth, N (left/right), point estimate, SE, z-stat, p-value, 95% CI. Render it as a compact monospaced table in `ModelPlots.jsx` (or a new `RDDResults.jsx`) when `result.type === "SharpRDD"`. The actual `runCol` and `treatCol` values should be substituted for the generic "running − c" / "D (treatment)" display labels — these are already on the result object.
**Invariants:** No React imports in `src/math/CausalEngine.js`. The `summaryTable` is a plain JS object assembled in the engine; rendering is the UI layer's responsibility only.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept
**File:** `src/math/LinearEngine.js` (`runOLS` line ~128 — `k = xCols.length + 1` hard-codes intercept); `src/components/modeling/ModelConfiguration.jsx` (estimator config UI)
**Suggestion:** Add an optional `noIntercept: boolean` flag in the model spec. In `runOLS` (and `runWLS`, `runFE`, `runFD`), when `noIntercept` is true, omit the ones-column prepend to the design matrix X and set `k = xCols.length`. Propagate the flag through the DuckDB suff-stats paths (drop `"(Intercept)"` from the expanded X column list in `buildOLSSuffStats`). Add a checkbox in `ModelConfiguration.jsx` labelled "No constant / suppress intercept". Export scripts must emit `-1` (R), `noconstant` (Stata), `+ 0` (Python/statsmodels) accordingly in `rScript.js`, `stataScript.js`, `pythonScript.js`.
**Invariants:** `seType` must still be passed explicitly to all engine variants — no hardcoding inside the no-intercept branch. Pipeline is non-destructive — the `noIntercept` flag lives in the spec object only.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` (Plot tab root); `src/components/tabs/spatial/map/SpatialPlotTab.jsx` (Map tab root); `src/components/tabs/spatial/plot/GeoLayerConfig.jsx` (layer config panel)
**Suggestion:** BugTriage routes this to V4 post-launch. When implemented: add a row-filter control in `GeoLayerConfig.jsx` that accepts a column + value/range predicate (reusing the `exprEvalService` filter-mode so users get the same JS-expression syntax as CleanTab, including the `%in%` rewrite suggested above). Apply the filter to the layer's data array before passing to `mkGeoLayer` / `mkSLayer` — this keeps the full dataset intact and filters at render time only, consistent with the non-destructive pipeline.
**Invariants:** Do not mutate the source `rows` array — filtering produces a view. No localStorage for layer filter state.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D × (running − c)" regardless of actual column names
**File:** `src/math/CausalEngine.js` (`varNames` array at line ~579, hardcodes display strings); `src/components/modeling/ModelComparison.jsx` (uses coefficient names from result); `src/ReportingModule.jsx` (Stargazer output)
**Suggestion:** Two-phase fix: (1) **Quick win** — in `CausalEngine.js`, replace the hardcoded `"D (treatment)"` with the actual `treatCol` value and `"running − c"` with the actual `runCol` value in `varNames`. This alone resolves 80% of the complaint with a two-line change and no UI work. (2) **Full feature** — add a `displayLabels: { [coefKey]: string }` map to the estimation result object. Expose an editable label panel in the Results area or `CodeEditor.jsx` where users can override any coefficient's display name. Persist overrides in the pinned model object. Substitute `displayLabels[coef] ?? coef` everywhere a coefficient name appears in export scripts and Stargazer output.
**Invariants:** No React imports in `src/math/CausalEngine.js`. Label substitution defaults (using actual column names) must live in the engine return value only. UI editing lives in `src/components/` only.

---

## Suggested action order

1. **[CRITICAL · Security]** Add Supabase Postgres `BEFORE INSERT` trigger on the `feedback` table to sanitize `description` at the database boundary — client-side `sanitizeFeedback` is bypassable by direct POST (`supabase/functions/` migration or new SQL migration file).
2. **[HIGH · Bug]** Diagnose and fix "pipeline only applies if dataset also selected in Clean Module" — check `WranglingModule.onReady` at line 219 of `src/WranglingModule.jsx`; ensure it emits unconditionally once `idbReady` is true.
3. **[HIGH · Browser validation]** Run browser smoke-tests for the four `35f4b5a6`/`5e105529` code-complete items: (a) frozen diagnostics/plots, (b) OLS residual plots (Y~Yhat, Resid vs Fitted, Q-Q), (c) Plot Builder resize + PNG export, (d) Spatial heatmap — all shipped but unconfirmed by Franco.
4. **[HIGH · Performance]** Run the Fase X4 browser benchmark on a real 900k-row file; if tab-transition lag persists, audit `ExplorerModule` for `SELECT *` violations and enforce the 500-row preview cap.
5. **[LOW · Quick win]** Replace hardcoded `"D (treatment)"` / `"running − c"` in `CausalEngine.js` line ~579 with actual `treatCol` / `runCol` values — resolves the editable-label complaint with a two-line change.
