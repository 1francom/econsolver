# Feedback Analysis Report — 2026-08-30

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary

- **9 open bugs** · **13 open features** · **3 open UX/Design** · **1 open performance** · **2 open other**

Open items are drawn from ClaudeFB.md (non-strikethrough, non-✓ entries) cross-referenced against BugTriage.md status fields. Items whose BugTriage row is `fixed`, `code-complete`, or `likely fixed` are excluded even when ClaudeFB.md still shows them open — they are pending Franco's browser confirmation, not code work. No new feedback rows arrived since the 2026-08-29 run; no fixes were committed in that window. Open-item set is unchanged.

---

## Bugs

### [high] · Simulate / Export · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/rScript.js`, `src/services/export/pythonScript.js`, `src/services/export/stataScript.js`
**Suggestion:** `SimulateTab` generates a snapshot for the DGP draw but the downstream analysis — statistic choice, number of draws, resampling config, sample tests run on simulated data — is not captured in `spec` or threaded into any script generator. Add a `simulateSpec` struct to the session snapshot that records the active analysis steps; have the three exporters emit that block after the DGP `set.seed(…)` preamble. R gets a `replicate(B, …)` loop; Python gets `np.random.seed` + a list comprehension; Stata gets a `simulate` command block. The DGP draw itself is already emitted — the gap is everything after it.
**Invariants:** All Anthropic calls go through `AIService.js` only — do not add a raw `fetch` to Anthropic to generate the script. Replication scripts must be generated by pure-JS exporter functions. No React imports in `src/math/`.

---

### [high] · Calculate / Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** Audit every `estimatorFamily` × `seType` combination emitted by `stataScript.js`. Known gaps: two-way cluster routing to `reghdfe` (recently added), panel HAC incorrectly emitting `vcov="NW"` instead of Driscoll-Kraay, and GMM/LIML which have no native Stata analogue. Add a smoke harness that runs every (family, seType) pair through `transpileModel` and asserts the output neither throws nor is an empty string — the 168-cell matrix pattern used for the HC2 fix. Also audit `CodeEditor.jsx`'s field whitelist: a missing entry silently drops a spec parameter, which is the exact bug class that broke Callaway CS DiD output.
**Invariants:** `seType` must be passed explicitly — never hardcoded inside `transpileModel`. Inline styles only (no CSS files, no Tailwind).

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` emits a fixed-width `tabular` environment. For wide tables (≥5 columns), switch to `tabularx` with `\linewidth` as the total width and `X` columns for text fields, or emit `\resizebox{\textwidth}{!}{\begin{tabular}…}` as a fallback for environments without `tabularx`. Expose a `tableStyle: "auto" | "tabularx" | "resizebox"` option in the Report tab. The column count is already available from the `columns` array length — gate the switch at `columns.length >= 5`.
**Invariants:** `latexTable.js` is a pure export helper — zero React imports, keep it that way. Persistence via `indexedDB.js` — do not use `localStorage` for the style preference.

---

### [medium] · Model / Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently holds a single hypothesis in local state and resets it on re-render. Change the panel state to a `hypotheses[]` array, stored in `sessionStorage` keyed to the active model's `spec.id`. Add a "+ Add hypothesis" button that appends a blank row; each row has its own constraint input and result display. The Joint tab's `generateJointHypothesisScript()` (added 2026-07-30) already accepts a constraint array — extend it to emit `car::linearHypothesis(fit, c("…","…","…"))` for multi-row tests in R. Persist the array in the model-buffer spec and include in `CodeEditor.jsx`'s whitelist.
**Invariants:** No React imports in `src/math/`. `sessionStorage` is acceptable for within-session state; use `indexedDB.js` for project-scoped persistence that survives reload.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The Explore time-series panel uses `aes.color` to split series by a group column, but the legend and reference-line logic that labels each group is not receiving the colour-scale mapping back from Observable Plot's returned channels. Reproduce by plotting a numeric variable over time with a categorical group column and checking whether legend entries match plotted colours. Likely fix: pass `Plot.color({ legend: true, scheme: … })` as a top-level scale declaration alongside the mark, and verify the group accessor uses the same column name the legend reads. The colour key shown in `renderLegend()` in `ExplorerModule.jsx` must also be aligned with what the plot actually draws.
**Invariants:** Inline styles only via the `C` object. No CSS files. Observable Plot 0.6 CDN — do not `npm install` a local copy.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** `PlotBuilder.jsx` already exposes a `Labels` panel (title, x-label, y-label) for point/line/bar geoms — check whether the time-series code path calls `buildPlotSpec()` with the same `labels` object or bypasses it. If it bypasses, thread the same `labels` object through the time-series render path and pass it to `Plot.plot({ x: { label: … }, y: { label: … }, title: … })`. This is a configuration-only change — no new UI surface needed, since the existing Labels panel should cover it once the render path honours the object.
**Invariants:** Inline styles only. No external UI libraries.

---

### [medium] · Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add a rule to `INTERPRET_REGRESSION_PROMPT` for the elasticity case: when both the dependent variable and a regressor are in log form (detectable from column names containing `"log_"` or `"ln_"`, or from pipeline steps of type `"log"` applied to those columns in the session snapshot), the coefficient IS an elasticity and must be labelled as such ("A 1% increase in X is associated with a β% change in Y"). Add a secondary rule for the semi-elasticity case (only one side is logged). The session snapshot already threads `pipeline` steps into the prompt — use those to detect log transforms without requiring user annotation.
**Invariants:** All Anthropic calls go through `callClaude()` in `AIService.js` only. No new `fetch` to Anthropic.

---

### [medium] · Clean / UX · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to continue
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` holds form state in React local state, which resets on tab navigation. Lift the draft state for each step type (case_when rows, mutate expression, if_else arms) into `sessionStorage` keyed by `pid + "_draft_" + stepType`. On mount, restore the draft if present; on discard or successful apply, clear it. Only draft the sub-panel that was active when the user navigated away — not every sub-panel simultaneously. This follows the same pattern as `History.jsx`'s pipeline-column collapse toggle (keyed by dataset id, introduced 2026-08-01).
**Invariants:** `sessionStorage` is acceptable for within-session tab state. `indexedDB.js` is the correct store for persistence that survives a page reload. Inline styles only.

---

### [low] · UX · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` constant)
**Suggestion:** Audit each `TOUR_STEPS` entry against the current UI: the pipeline-column collapse toggle (added 2026-08-01), the N-way FE column picker, the Distinct Values panel (Clean → Formatting), the Simulate sample-test panel, the spatial Analyze/Map/Plot three-tab layout, and the Model Buffer bar pinned-model groups (once that feature ships). Update `selector` strings to match current DOM ids/classes. Per CLAUDE.md: do NOT write counts ("N steps") — name the features instead. Run the tour in a browser after updating to verify each step highlights the correct element.
**Invariants:** Inline styles only; `HintBox` trigger button must read "Help"; the `title` prop is the MODULE name, not a sentence.

---

## Features

### · Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** `SpatialGeoPlot.jsx` renders an SVG geo-plot using Observable Plot / D3. `SpatialEngine.js`'s `assignRect` and `assignH3` already produce grid cell ids and WKT ring geometry per row. Expose a "Grid layer" option in `GeoLayerConfig.jsx` that reads those columns and draws the grid polygons as a filled/outlined `Plot.geo` mark coloured by a user-chosen column. The geometry strings are WKT; reuse the existing `parseWktRings` helper (`src/components/tabs/spatial/shared/wkt.js`) to convert them to D3-compatible paths.
**Invariants:** `src/math/` and `src/core/` must have zero React imports. Inline styles via the `C` object.

---

### · Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx` (or a new result-display component)
**Suggestion:** `runSharpRDD` and `runFuzzyRDD` already return bandwidth, kernel, polynomial order, effective n left/right, first-stage F (fuzzy), LATE estimate, SE, and CI. Compose these into a compact summary table matching `rdrobust`'s output format: Cutoff | h (left/right) | b (left/right) | N (left/right) | LATE | SE | p-value | CI. This is a display-only change — no new math required. Ensure `rScript.js` emits `summary(rdrobust(…))` and `stataScript.js` emits the `rdrobust` equivalent so the user can reproduce the table.
**Invariants:** Pure display change — no modifications to `src/math/` math code needed. Inline styles only.

---

### · Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a "Filter rows" section to `GeoLayerConfig.jsx` that reuses `pipeline/predicate.js`'s `evalPredicate` (the single owner of the filter operator vocabulary) to let users specify a column/operator/value condition per layer. Filter each layer's `rows` through `evalPredicate` before passing to the Observable Plot mark. This is a view-level filter (does not add a pipeline step), matching the convention used in the Data Viewer and Explorer QuickFilter. For DuckDB-backed data, compile through `predicateToSQL` so the filter runs server-side.
**Invariants:** `predicate.js` is the canonical single owner of filter operators — never re-implement a filter operator in any component. Inline styles only.

---

### · Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/services/export/latexTable.js`, `src/components/modeling/CodeEditor.jsx`
**Suggestion:** `latexTable.js` receives the `varNames` array from the estimation result (raw column names from `spec.xVars`). Add an optional `labelMap: Record<string, string>` parameter to `buildStargazer`; store it in the model's `spec` object so `CodeEditor.jsx`'s whitelist can carry it. Render an editable label grid in the Report tab's LaTeX panel — one row per variable, raw column name on the left, editable text input on the right. The RDD special-case labels ("D (treatment)" etc.) become the DEFAULT values of this map, not hardcoded strings, so users can override them.
**Invariants:** Persistence via `indexedDB.js` only — no `localStorage`. Inline styles only.

---

### · Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The Calculate workbench uses a CAS library (nerdamer or similar, already bundled). Most CAS libraries expose a `limit(expr, var, point, direction)` function. Add a "Limits" operation to the operation selector alongside Derivative/Integral/Optimize. Parse the user's expression with the existing expression parser; call the CAS `limit` function with the variable, point (`Infinity`, `-Infinity`, or a numeric constant), and optional direction (`+` / `-`). Display the closed-form result. If the CAS exposes step-by-step rules (L'Hôpital, factoring), surface them in the workbench's steps panel.
**Invariants:** No React imports in `src/math/` — if the CAS call is extracted into a helper, keep it pure JS. Inline styles only.

---

### · Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The equation canvas already plots multiple function curves over a shared x domain. Add a constant entry type: when the user types a bare numeric value (no `x` in the expression), detect it as a constant and render it as a horizontal `hline` mark at that y value — the same as the `hline` geom already implemented in `PlotBuilder.jsx`. Show a coloured dashed line with a label at the y value. This reuses the multi-equation canvas with no new canvas needed.
**Invariants:** Inline styles only. No external UI libraries.

---

### · General · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 supports secondary y-axes via faceting (`fy`), but a true dual-axis with different scales requires two overlaid Plot instances with CSS `position: absolute`. Scope: implement a "secondary Y" toggle per layer in `PlotBuilder.jsx` that renders a second `Plot.lineY` (or point/bar) with its own `y` scale on the right side, positioned absolutely over the primary canvas at the same width. The primary canvas must omit its right-side frame mark when a secondary Y layer is present so scales don't collide. Flag as an advanced option with a "caution: dual-axis plots can be misleading" note per dataviz best practice.
**Invariants:** Inline styles only — no CSS files, no Tailwind. Observable Plot 0.6 CDN only.

---

### · Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** `VariableSelector.jsx` renders the Y/X/W column-picker chips. Add a text input above the chip list that filters displayed columns by case-insensitive substring match against `headers`. The input should be uncontrolled and clear on estimator change (when the chip list resets). This is a pure display filter — it never changes which columns are selected or passed to the engine.
**Invariants:** Inline styles only. IBM Plex Mono for the input. Use `C.teal` for the active-match highlight.

---

### · Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, Observable Plot generates a colour scale with a legend showing swatches + category names but no count per group. Extend the legend render to include the row count per group, shown as a small grey number after each label: `Spain (452)`. Compute counts once — via SQL `GROUP BY` for DuckDB-backed data, or via `Array.from` with a `Map` groupBy for JS-backed data — as part of the same query that fetches plot rows. Add a `groupCounts(col)` helper to the plot-data pipeline for this purpose.
**Invariants:** DuckDB as the data boundary — query counts in SQL for large datasets, never pull all rows into JS objects. Inline styles only.

---

### · Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`, `src/pipeline/registry.js`
**Suggestion:** Each step in `History.jsx` already knows its `type` (from `STEP_REGISTRY`). Extend the registry with a `tab: "clean" | "feature" | "reshape" | "merge"` field per entry. Add an `onNavigate(tab, stepType)` callback that `History.jsx` calls when the user clicks a step's type label (not the step body, which still opens the edit panel). `WranglingModule` implements the callback by setting its active tab and, where possible, scrolling to or expanding the sub-panel for that step type.
**Invariants:** `STEP_REGISTRY` must stay in sync with `runner.js` at all times — adding a field to a registry entry must not break runner cases or `pipelineReliabilityValidation.mjs`. Inline styles only.

---

### · Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step type with a static iso2/iso3/country-name lookup table (~250 rows, inlined as a JS object). The step accepts `{ col, from: "iso2"|"iso3"|"name", to: "iso2"|"iso3"|"name" }`. In `runner.js`, resolve each value through the lookup; unrecognised values become `null` (matching the `recode`/`normalize_cats` convention). Expose it in `FeatureTab.jsx` under the Formatting section. Start with exact-match crosswalk only — fuzzy name matching via `fuzzyGroups` is a larger, separate scope.
**Invariants:** New step type requires changes to `runner.js`, `registry.js`, `stepValidator.js`, and all three exporter translators — R uses `countrycode::countrycode`; Python uses `pycountry`. Pipeline step must replay on `rawData` (non-destructive).

---

### · Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** The ModelingTab "◈ Plot Builder" section already populates result-augmented rows. Extend the Plot Builder's `point` geom with a "Bin by X" option: when enabled, group rows into `k` equal-width bins along the x-axis and plot bin means (ȳ in each bin vs. x̄ in each bin), reproducing `binsreg`/`rdplot`'s binned scatter. Expose `k` (number of bins, default 20) and `binBy: "equal-width" | "equal-count"` in the geom options panel. Compute bins using DuckDB `ntile(k)` for large datasets — never pull all rows into JS.
**Invariants:** DuckDB as data boundary for large datasets (DuckDB strategy rule 1). Inline styles only. Observable Plot 0.6 CDN.

---

### · Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Add an "Observation cap" slider (range 100–n, default n) in the Plot Builder's data panel. When set below n, sample the plot rows using DuckDB `USING SAMPLE k ROWS` (for DuckDB-backed data) or a Fisher-Yates shuffle capped at k (for JS data), so the plot renders a representative subsample. Display a "Showing X of N rows" notice beneath the plot. This is view-only — it must never affect estimation, pipeline steps, or `rawData`.
**Invariants:** Display limit ≠ computation limit (CLAUDE.md DuckDB rule 5). The cap must ONLY affect the plot render. Inline styles only.

---

## UX / Design

### · General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** `artifactOrder.js` already manages a project-scoped global order of saved artifacts. Extend the order structure with an optional `group: string` field per artifact id. In `ModelBufferBar.jsx`, render a group-tag badge on each pinned model card. Add a group-filter dropdown at the bar header so the user can view "All" or a specific group name. Persist group assignments in the `artifact_order` IndexedDB store alongside the order array. `ModelComparison.jsx` and the G13 coefficient-comparison plot should respect the active group filter.
**Invariants:** Persistence via `indexedDB.js` only — no `localStorage`. Inline styles via the `C` object.

---

### · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** (Cross-listed with Bug #8 above.) `FeatureTab.jsx` holds form state in React local state, which resets on tab navigation. Lift the draft state for each step type (case_when rows, mutate expression, if_else arms) into `sessionStorage` keyed by `pid + "_draft_" + stepType`. On mount, restore the draft if present; on discard or successful apply, clear it. Only draft the sub-panel that was active when the user navigated away.
**Invariants:** `sessionStorage` for within-session state; `indexedDB.js` for reload-surviving persistence. Inline styles only.

---

### · General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables — cross-module feature (Clean filters + Feature builder + maybe Explore)
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** Add a `lists: Map<string, string[]>` field to the `SessionStateProvider` context (analogous to the existing `datasets` registry). Lists are named string arrays — e.g. `{ "EU countries": ["Germany", "France", …] }`. Expose a "Lists" panel in the Data tab for creating/editing/deleting them. In `FilterBuilder` (CleanTab), add an `in_list` operator that resolves a named list at evaluation time via `predicate.js`'s `evalPredicate`. Persist lists in IndexedDB alongside the pipeline (new `project_lists` store in `indexedDB.js`).
**Invariants:** `predicate.js` is the single owner of filter operators — add `in_list` there, not in any component. Persistence via `indexedDB.js` only — no `localStorage`.

---

## Performance

### [critical] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile branch), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** This is the Fase X4 performance track. The `.dta` case was fixed 2026-06-10 (large .dta files now convert to DuckDB/OPFS). The remaining crash vectors are in-memory-only paths: `.xlsx` and `.shp` files >10MB still parse entirely in JS before the DuckDB fallback can engage. Prioritise: (1) auto-convert large `.xlsx` to Parquet immediately after SheetJS parses the sheet, then reduce to a 500-row UI preview; (2) add a progress indicator for any import >5MB so the browser does not appear frozen. The DuckDB performance strategy (CLAUDE.md rules 1–7) is the binding architectural constraint.
**Invariants:** Privacy-first — plaintext data never leaves the browser. All large-data operations must go through DuckDB, not through JS materialisation of the full table.

---

## Other

### · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated)
**File:** `src/services/data/parsers/`, `src/DataStudio.jsx`
**Note:** BugTriage 2026-06-14 marks this "fix-later: malformed CSV row aborts import instead of surfacing recovery options." Confirmed user-visible on real World Bank CSV exports. Fix path: wrap the DuckDB CSV import in a TRY/CATCH that retries with `IGNORE_ERRORS true` and surfaces a banner listing skipped line numbers and truncated content. If `IGNORE_ERRORS` is unavailable in the pinned DuckDB-WASM version, fall back to Papa Parse with `skipEmptyLines: true`, collecting `papaResults.errors` for the banner. The banner must show original line numbers so the user can fix the source file.
**Invariants:** Non-destructive pipeline — the raw import result still becomes `rawData`. Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`.

---

### · Clean / Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Note:** BugTriage 2026-06-14 marks this "fix-later." The `in`/`nin` operators already exist in `predicate.js` — the gap is that the filter UI does not expose a multi-value picker for them (users must type a comma-separated string, which is undiscoverable and fragile for values containing commas). Suggestion: add a tag-input widget to `FilterBuilder` for `in`/`nin` operators that lets users add/remove values interactively. No change to predicate evaluation is needed.
**Invariants:** `predicate.js` is the single owner of the `in`/`nin` operators — do not re-implement membership logic in `CleanTab.jsx`. Inline styles only.

---

## Suggested action order

1. **Fix Simulate replication script** (high severity — blocks core research-workflow reproducibility; users cannot export their Monte Carlo analysis to R/Python/Stata)
2. **Fix Stata workbench incompleteness** (high severity — affects every user who exports to Stata; audit the 168-cell emitter matrix and harden the CodeEditor whitelist)
3. **Fix time-series group references in Explore** (medium severity, wrong visual output — grouping is a fundamental time-series operation and currently broken)
4. **Add LaTeX table width scaling** (medium severity, low complexity — one switch to `tabularx`/`\resizebox` based on column count in `latexTable.js`)
5. **Add elasticity interpretation to AI narratives** (medium severity, low complexity — a rule addition to `INTERPRET_REGRESSION_PROMPT` using the already-available pipeline snapshot)
6. **Thread time-series labels through PlotBuilder render path** (medium severity, low complexity — the `Labels` panel exists; the render path just needs to honour it)
7. **Add variable searcher in VariableSelector** (medium UX, low complexity — a substring filter input over the existing chip list)
8. **Add resilient CSV import with row-skip banner** (medium severity data bug — DuckDB `IGNORE_ERRORS` + Papa Parse fallback with line-number reporting)
9. **Add country-code crosswalk pipeline step** (medium feature, medium complexity — static lookup table, full runner/registry/exporter coverage required)
10. **Update guide tour** (low severity, low complexity — selector audit + content refresh, no math or engine change)
