# Feedback Analysis Report — 2026-08-27

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **13 open bugs** (8 confirmed open · 4 code-complete/browser-validation pending · 1 discrepancy note)
- **12 open features**
- **0 open UX/design** (all resolved)
- **1 open performance**
- **1 open other**

Total: **27 open items** analysed.

---

## Bugs

---

### [high] · Export (CS DiD) · 2026-07-05

**Feedback:** Replication code for CS DiD 2021 does not work

**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`) · `src/math/CallawayEngine.js`

**Suggestion:** The `case "CallawayCS"` branch in `rScript.js` destructures `yVar`, `treatCol`, `entityCol`, `timeCol`, `xVars`, `compGroup`, `csEstMethod`, and `csAnticipation` directly from `model` (the flat spread), but `CallawayEngine.js` returns a result object whose spec fields may be nested under `result.spec` rather than at the top level. Verify the destructuring path matches the actual object shape that gets stored in `modelBuffer`; the panel-model-pinning fix (2026-08-18) changed how wrapped results are stored, and the CS DiD result may arrive at the exporter with a different field layout than assumed. Additionally, `base_period = "varying"` is hard-coded but R's `did::att_gt` expects `base_period = "universal"` or `"varying"` depending on aggregation choice — add a comment making this explicit. No architectural invariant risk.

**Invariants:** All export logic lives in `src/services/export/` — acceptable. No `src/math/` changes required.

---

### [medium] · Export (LaTeX) · 2026-07-05

**Feedback:** Adjust the size of LaTeX tables when the number of columns increases

**File:** `src/services/export/latexTable.js`

**Suggestion:** `buildStargazer` produces a fixed-width `\begin{tabular}` with no column-count adaptation. Wrap the `tabular` environment in `\resizebox{\textwidth}{!}{...}` (or `\adjustbox{max width=\textwidth}`) when the column count exceeds 5 (i.e., `models.length > 4`). Add `\usepackage{adjustbox}` to the preamble comment when the resize is applied. Keep the raw tabular for narrow tables so LaTeX authors can apply their own width. The `colsN` variable is already computed at line 86+ and can gate the decision. No architectural invariant risk.

**Invariants:** None.

---

### [high] · Export (Plot) · 2026-07-05

**Feedback:** Replication codes of the plots must have the lines to load datasets as well

**File:** `src/components/PlotBuilder.jsx` (line 1528–1542, `copyPlotScript`) · `src/services/export/plotScript.js`

**Suggestion:** When `scriptPreamble` is not provided (e.g., the PlotBuilder is mounted in ExplorerModule without a preamble), the generated R/Python code references `df_<name>` but never emits a load line. Fix: in `copyPlotScript`, when `preamble` is empty and `datasetName` is non-null, prepend a dataset-load preamble by calling `buildRLoadLine(datasetName, null)` (for R) / `buildPyLoadLine(datasetName, null)` (for Python) — the `loadLine.js` helpers already handle extension-based format inference. Stata's `buildStataPlot` already hard-codes `import delimited`, so extend it to accept a `filename` option. ExplorerModule passes `datasetName` prop already (line 2631); `PlotBuilder` already receives it (line 1366). No architectural invariant risk.

**Invariants:** None. The `loadLine.js` helpers are the canonical load-line source — use them rather than inlining `read_csv`.

---

### [medium] · Model · 2026-07-06

**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R

**File:** `src/components/modeling/CoefficientTestPanel.jsx` · `src/math/ModelHypothesis.js` · `src/services/export/rScript.js`

**Suggestion:** `CoefficientTestPanel` currently supports one Wald/chi-square joint test at a time and discards results when the user changes coefficients. Add a "Save test" button that appends the current `waldTest` result (constraints, chi-square, df, p-value) to a local list displayed below the panel. Then wire the saved list into the replication script: emit `car::linearHypothesis(fit, c("beta1 = 0", "beta2 = 0"))` (R) for each saved test when the model's `CodeEditor` generates its R script. No architectural invariant risk.

**Invariants:** Replication script changes belong in `src/services/export/rScript.js` only; the math stays in `src/math/ModelHypothesis.js`.

---

### [medium] · Explore · 2026-07-09

**Feedback:** Add labels for the Time series plot, title, axis, etc.

**File:** `src/ExplorerModule.jsx` · `src/components/PlotBuilder.jsx`

**Suggestion:** The PlotBuilder already has title/axis label inputs wired for all geoms. The Explore tab's "Time Series" sub-tab renders its own SVG ACF/PACF/line charts outside PlotBuilder (look for the `ACFChart` or equivalent component in `ExplorerModule.jsx`). Add `title`, `xLabel`, and `yLabel` state fields to that local chart renderer and pass them as SVG `<text>` elements. Alternatively, route the time-series view through PlotBuilder's `line` geom (which already has all label controls), removing the custom renderer. The second option is lower ongoing maintenance. No architectural invariant risk.

**Invariants:** None.

---

### [high] · Model · 2026-07-09 *(likely fixed — confirm)*

**Feedback:** Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable

**File:** `src/math/PanelEngine.js` (`runFEMulti`) · `src/components/modeling/ModelConfiguration.jsx`

**Suggestion:** N-way FE shipped in commit `045f5cda` (#104). BugTriage marks this "likely fixed — needs confirmation". Franco should verify: (a) the FE dimension picker in ModelConfiguration accepts arbitrary categorical columns (not just entity/time), (b) the EstimatorSidebar and ModelConfiguration's `PolyOrderSection`/FE chip section show non-panel variables, (c) a 3-way FE model with country × year × sector runs without error. Mark resolved in BugTriage once confirmed.

**Invariants:** None.

---

### [high] · Model · 2026-07-09 *(likely fixed — confirm)*

**Feedback:** Add also FE with interactions

**File:** `src/math/PanelEngine.js` · `src/pipeline/runner.js` (`factor_interactions`)

**Suggestion:** Shipped in `045f5cda` (#104) alongside N-way FE. Verify the interaction UI (ModelConfiguration) correctly builds interaction columns for FE models and that `factor_interactions` pipeline step is invoked for nested FE combinations. Mark resolved in BugTriage once confirmed.

**Invariants:** None.

---

### [medium] · Model / AI · 2026-07-09

**Feedback:** Add direct elasticity interpretation

**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)

**Suggestion:** The AI narrative already interprets log–log as elasticities and log–level as semi-elasticities (lines 353–354 of `Prompts/index.js`). The "direct" qualifier likely means: compute and display elasticities numerically in the result panel itself, not just in the AI narrative. In `EstimationResult.js` / `ModelingTab.jsx`, after OLS estimation, if both Y and a given X were log-transformed in the pipeline (detectable via `spec.pipeline`), label that coefficient "Elasticity" rather than "Coefficient" in the result table and compute `exp(β)` for the IRR row. This is a result-display enhancement, not a prompt change. No architectural invariant risk.

**Invariants:** No React in `src/math/`; any elasticity detection must live in the display layer (`ModelingTab.jsx` or `EstimationResult.js`).

---

### [high] · Calculate · 2026-07-17

**Feedback:** Stata workbench doesn't work completely

**File:** `src/components/calculate/workbench/exportScript.js` (line 79 `buildStata`)

**Suggestion:** The `buildStata` function has two Mata syntax bugs. (1) Floating statements like `xm = (x0+x1)/2; hh = 1e-6` appear *outside* a valid Mata function body — they follow the `end` keyword that closes the prior function definition, so they execute in global Mata scope where variables declared inside `buildStata`'s function cannot be referenced. Move the derivative/integral/optimize/root numeric code into the Mata function body itself, or emit a separate `mata:` block for interactive numeric evaluation. (2) Mata display uses `printf` or `displayas`, not Python-tuple syntax — `("label", value)` is a matrix construction in Mata, not a print call. Replace with `printf("label: %10.7f\n", value)`. These are export-code correctness bugs, not engine bugs; no math is affected. No architectural invariant risk.

**Invariants:** None.

---

### [low] · General · 2026-07-17

**Feedback:** Guide tour must be updated

**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS, line 11)

**Suggestion:** The TOUR_STEPS array does not reflect features added since approximately 2026-05. Audit each step against the current tab structure: add entries for Sun & Abraham, Callaway & Sant'Anna, N-way FE, Synthetic Control, the Plot Builder (G-track), Spatial Analytics (Analyze/Map/Plot sub-tabs), and the CR2/CR3 SE options. Per CLAUDE.md convention, **never write a count** ("11 geoms") — name the things. Remove any steps referencing deprecated UI locations (old sidebar layout, legacy WLS-as-estimator position). No architectural invariant risk.

**Invariants:** None.

---

### [high] · Explore · 2026-07-17

**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group

**File:** `src/components/wrangling/ReshapeTab.jsx` · `src/pipeline/runner.js` (`group_summarize`)

**Suggestion:** `group_summarize` aggregates (mean, sum, count, sd, median, etc.) within groups but cannot compute *between-group* differences in a single step. The CLAUDE.md bug log (2026-08-07) already documents the fix pattern: add a `splitByGroup` input mode to `SampleTestPanel` that accepts one outcome column and one binary group column (`y ~ treat` long format), so users can get the two-sample t-test (treatment mean − control mean) without a `pivot_wider` → `mutate` detour. Extend `ReshapeTab` with a "Between-group difference" aggregation type that emits a `group_summarize` step followed by an `inject_column` step carrying the computed difference. This avoids introducing a new step type. No architectural invariant risk.

**Invariants:** `runner.js` is the source of truth; any new aggregation must be handled there. No React in `src/math/`.

---

### [medium] · Model · 2026-07-17 *(discrepancy)*

**Feedback:** Add chi-square for linear hypothesis

**File:** `src/components/modeling/CoefficientTestPanel.jsx` · `src/math/ModelHypothesis.js`

**Suggestion:** A code inspection shows chi-square IS already displayed in `CoefficientTestPanel.jsx` (line 211: `χ²({jointResult.df}) = ...`). The BugTriage entry is marked open, but this appears to be a false open — the feature was likely shipped after the feedback was filed and BugTriage was never updated. **Recommend Franco browser-confirm and close this item.** If the request is specifically for an F-test *in addition to* chi-square for classical SE contexts (matching R's `car::linearHypothesis` default), that would be a separate small addition: compute `F = chiSq / df` and display `F(df, dfRes)` when SE type is "classical". No architectural invariant risk.

**Invariants:** None.

---

### [medium] · General · 2026-07-17

**Feedback:** Create groups for pinned models, plots, and maps

**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`

**Suggestion:** `artifactOrder.js` currently stores a flat global order array of artifact IDs. Add a `groups` map (`groupId → { label, ids[] }`) to the persisted order structure. Expose a "New group" button in `ModelBufferBar.jsx` that prompts for a label and assigns selected pinned entries to it. Render groups as collapsible sections in the comparison bar. `ModelComparison.jsx` should filter to the active group's IDs. This is self-contained in the persistence and bar components — no math changes needed. No architectural invariant risk.

**Invariants:** Use `indexedDB.js` for persistence — do not use localStorage.

---

### [medium] · Clean · 2026-07-17

**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean, and coming back to find it gone

**File:** `src/components/wrangling/FeatureTab.jsx`

**Suggestion:** `FeatureTab` holds all draft state (case_when conditions, mutate expression, if_else branches) in component-local `useState`, which is lost on tab navigation. Store the draft for each active sub-section key in a `useRef` or lift the state into `WranglingModule`'s tab-local state store (similar to how `CleanTab` persists its filter builder draft). Alternatively, write a lightweight draft to `sessionStorage` keyed by `pid + "_featureTab_draft"` on every keystroke, and restore it in a `useEffect` on mount. The `sessionStorage` approach avoids any prop-threading change. No architectural invariant risk.

**Invariants:** `sessionStorage` is acceptable for transient draft state (not persisted data). Do NOT use `localStorage`.

---

### [medium] · Data · 2026-07-17

**Feedback:** Add arrange functions in the data viewer

**File:** `src/DataStudio.jsx` (DataViewer component) · `src/services/data/duckdb.js` (`getTablePage`)

**Suggestion:** `getTablePage` already accepts `offset` and `limit` for pagination, but has no `ORDER BY` parameter. Add `sortCol` and `sortDir` optional parameters to `getTablePage`, appending `ORDER BY "${sortCol}" ${sortDir}` to the SQL query when provided. In the DataViewer, make column header cells clickable to cycle through ascending → descending → unsorted, storing sort state in `useState`. On large DuckDB-backed tables, sort inside SQL (efficient); on the 500-row JS preview, sort the in-memory array. No architectural invariant risk.

**Invariants:** Full-table sort must run in DuckDB via SQL, not by pulling all rows into JS (performance rule). The preview path may sort in-memory as a best-effort.

---

## Features

---

### Explore · 2026-05-19

**Feedback:** Add filter `regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")` — R-style membership filter in the filter expression UI

**File:** `src/components/wrangling/CleanTab.jsx` · `src/pipeline/predicate.js`

**Suggestion:** The canonical filter language in `predicate.js` supports `in` and `nin` operators on the backend. Add a recognition pass in the filter expression input that converts R-style `col %in% c("a","b")` to a `{ op:"in", col, val:["a","b"] }` predicate node, then feeds it into the existing `evalPredicate`/`predicateToSQL` pipeline. This avoids adding a new operator. No architectural invariant risk.

**Invariants:** `predicate.js` is the single owner of condition language — never re-implement filter operators elsewhere.

---

### Model · 2026-05-21

**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression

**File:** `src/components/modeling/results/` (RDD result display) · `src/math/CausalEngine.js`

**Suggestion:** Add a collapsible "rdrobust-style summary" section below the coefficient table for Sharp/Fuzzy RDD results. The R `summary(rdrobust(...))` output includes: Call, Cutoff, BW (MSE-optimal h on both sides), Effective n (left/right), Order polynomial, Kernel type, and the sharp point-estimate table (Conventional, Bias-corrected, Robust). `CausalEngine.js` already computes h, n_left, n_right, LATE, SE, and CI. Display these fields in the existing result panel format. No architectural invariant risk.

**Invariants:** No React in `src/math/`; the display layer is `ModelingTab.jsx` or a new `RDDSummaryPanel.jsx` in `src/components/modeling/`.

---

### Spatial · 2026-05-30

**Feedback:** Allow spatial filters for plots

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` · `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Add a layer-level filter input (a mini predicate builder) in `GeoLayerConfig.jsx` that filters the layer's rows before rendering. Compile the filter via `evalPredicate` from `predicate.js` applied to the layer's data slice in `SpatialGeoPlot.jsx` before the SVG render loop. For DuckDB-backed data, push the filter into SQL via `predicateToSQL`. No architectural invariant risk.

**Invariants:** `predicate.js` is the single owner of condition evaluation — do not reimplement filter logic in the spatial layer.

---

### Model · 2026-06-06

**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names

**File:** `src/services/export/latexTable.js` · `src/components/modeling/` (result display)

**Suggestion:** Add a `labelOverrides` map (column name → display label) to `buildStargazer`'s options object. In the ReportingModule LaTeX panel, render an editable table of variable names (auto-populated from `result.varNames`) before export and pass the overrides map. For RDD specifically, the hard-coded "D (treatment)"/"running − c" strings in `latexTable.js` should be replaced by the actual column names from the model spec, with optional user overrides. No architectural invariant risk.

**Invariants:** None.

---

### Calculate · 2026-06-19

**Feedback:** Add limits

**File:** `src/components/calculate/workbench/Workbench.jsx` · `src/components/calculate/workbench/operations.js`

**Suggestion:** Add a "Limit" operation type alongside derivative/integral/optimize in `operations.js`. Numerically compute `lim(f(x), x→a)` by evaluating `f(x)` at a sequence of points approaching `a` from both sides and checking convergence. Export as `Reduce[f[a + eps], eps -> 0]`-style comment in R/Python (no base-R `limit` function exists; suggest `pracma::lim` or `sympy.limit`). Display left/right limits and flag discontinuities. Implement in the workbench's SymPy/CAS path for symbolic equations. No architectural invariant risk.

**Invariants:** Keep computation in `operations.js` (pure JS), not in any UI component.

---

### Calculate · 2026-07-04

**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)

**File:** `src/components/calculate/workbench/LocusCanvas.jsx` · `src/components/calculate/workbench/EquationsPanel.jsx`

**Suggestion:** When an equation is a constant (no axis variable, or expression evaluates to a scalar), render it as a horizontal `hline` at `y = constant` on the `LocusCanvas`. Add a "Constant" checkbox or auto-detect by checking whether the parsed expression contains any variable. Export as `geom_hline(yintercept = value)` in R / `axhline(value)` in Python / `yline value` in Stata. This reuses the existing canvas rendering infrastructure. No architectural invariant risk.

**Invariants:** None.

---

### General · 2026-07-05

**Feedback:** Add plots with 3 axis (3D plots)

**File:** `src/components/PlotBuilder.jsx`

**Suggestion:** Observable Plot 0.6 (used by PlotBuilder) has no native 3D support. Integrating Three.js or Plotly.js would add a new CDN dependency — evaluate against the CSP allowlist (`vercel.json` `connect-src`) before committing to a library. A pragmatic scope: add a "3D scatter" geom that renders via Plotly's WebGL `scatter3d` trace, loaded lazily. The geom would accept x, y, z aesthetic mappings. CSP: Plotly is self-contained and can be bundled, so no new CDN entry is required. Flag as a post-launch enhancement; 3D is architecturally distinct from the 2D Observable Plot pipeline. No architectural invariant risk within that scope.

**Invariants:** Any new CDN script must be added to both `script-src` and `connect-src` in `vercel.json` (CSP bug has bitten three times — treat `connect-src` as the default suspect).

---

### Model · 2026-07-09

**Feedback:** Add a variable searcher in the variable slider, so users can type and find them faster

**File:** `src/components/modeling/VariableSelector.jsx`

**Suggestion:** Add a text `<input>` above the variable list in `VariableSelector.jsx` that filters the displayed headers by substring match (case-insensitive). Use a local `useState` for the filter string, reset it when headers change. Keep the full unfiltered list in the model; the search only affects display. This is a low-risk, self-contained UI change. No architectural invariant risk.

**Invariants:** None.

---

### Explore · 2026-07-11

**Feedback:** Add more labels in tables or boxes when plotting by color

**File:** `src/components/PlotBuilder.jsx` · `src/services/export/plotScript.js`

**Suggestion:** When `aes.color` is set, PlotBuilder already renders a legend (via Observable Plot's `color` scale). "More labels" likely means: (1) show the exact count or percentage per category in the legend, and/or (2) allow direct data labels on points/bars colored by a variable. Implement (2) as a `showColorLabels` toggle in the layer options that emits `Plot.text(data, {x, y, text: color_col, fill: ...})` alongside the primary mark. For (1), a pre-aggregation pass can append `n=X` to each legend swatch label. No architectural invariant risk.

**Invariants:** None.

---

### Clean · 2026-07-17

**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically

**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`

**Suggestion:** `History.jsx` renders each pipeline step as a card. Add an "Edit" (pencil) icon on each card that fires an `onEditStep(step)` callback wired up in `WranglingModule.jsx`. The handler maps `step.type` to a tab-name + sub-section scroll target (e.g., `case_when` → FeatureTab, `filter` → CleanTab FilterBuilder) and calls `setActiveTab(targetTab)` + scrolls the appropriate section into view using `scrollIntoView`. A `STEP_TYPE_TO_TAB` lookup map can be derived from `STEP_REGISTRY`'s `category` field. No architectural invariant risk.

**Invariants:** None.

---

### Clean · 2026-07-19

**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform

**File:** `src/components/wrangling/FeatureTab.jsx` · `src/pipeline/runner.js`

**Suggestion:** Add a `country_code` step type that accepts a source column (country name or ISO2/ISO3) and emits a new column with the target format. Bundle a static ~250-row ISO 3166-1 lookup table as a JSON constant in the step implementation (no network call). Wire a "Country Codes" card in `FeatureTab.jsx`'s transform section. Handle fuzzy matching with a normalised lowercase + punctuation-stripped comparison, similar to `fuzzyGroups` in `wrangling/utils.js`. Add to `runner.js` and `registry.js`. No architectural invariant risk.

**Invariants:** The lookup table must be bundled statically — no external fetch. `runner.js` is the source of truth for step execution; the step must replay on `rawData`.

---

### General · 2026-07-19

**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables

**File:** `src/services/session/sessionState.jsx` · `src/components/wrangling/CleanTab.jsx` · `src/components/wrangling/FeatureTab.jsx`

**Suggestion:** Add a "Named Lists" store to `sessionState.jsx` (parallel to `datasets`): `{ [listId]: { name, values: string[] } }`. Persist via `indexedDB.js`. Expose a List Manager panel (small collapsible sidebar) where users can create/edit/delete lists. In `CleanTab`'s filter builder, add a "value is in list" operator that resolves the named list to a `{ op:"in", val:[...] }` predicate at step-run time. In `FeatureTab`'s expression editor, expose lists as `LIST_name` identifiers. This is a medium-complexity feature requiring a new `sessionState` action and minor wiring in several tabs. No architectural invariant risk.

**Invariants:** Use `indexedDB.js` for persistence. `predicate.js` remains the single owner of filter operators.

---

## Performance

---

### [high] · Data · 2026-05-08

**Feedback:** Big datasets generate crashes (21MB) over 900k rows

**File:** `src/DataStudio.jsx` (parseFile branch) · `src/services/data/duckdb.js`

**Suggestion:** DuckDB Fase X4 was marked "code-complete, browser benchmark pending Franco" in the X5 reconciliation table (2026-06-14). This item was never explicitly marked fixed in BugTriage and remains without a ✓ in ClaudeFB. **Verify the 900k-row CSV path is routed through DuckDB (10MB threshold) and that OPFS Parquet caching is enabled end-to-end** — the CSP `connect-src` bug (2026-07-25) showed that a blocked DuckDB extension download silently degrades the full-table path. If browser testing confirms it's working, close this item. If crashes still occur, check whether `DataStudio.parseFile`'s DuckDB branch is entered for files between 10MB and 21MB and verify `extensions.duckdb.org` is in `connect-src`.

**Invariants:** Data never leaves the browser (privacy-first). Never route through a server-side compute path.

---

## Other

---

### Data · 2026-05-10

**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...

**File:** `src/DataStudio.jsx` (parseFile CSV branch) · `src/services/data/parsers/` (resilient-import path)

**Suggestion:** This is the "malformed CSV row can abort import" bug from BugTriage (2026-06-14, status `fix-later`). The BugTriage entry says: "retain original row number and offer skip/repair preview." Implement a resilient parse mode: catch per-row parse errors (already surfaced by DuckDB's verbose CSV error messages), display a "X rows could not be parsed — view them?" modal that shows the raw lines, and offer Skip / Repair options. DuckDB's `read_csv_auto` already exposes `errors_path` and `ignore_errors` options that could surface the bad rows without aborting the import. No architectural invariant risk.

**Invariants:** None.

---

## Suggested Action Order

1. **Fix plot replication load lines** (high · export) — ExplorerModule PlotBuilder script omits the dataset `read_csv`/`import delimited`; one-line fix in `copyPlotScript` using existing `buildRLoadLine` helpers. High user-visibility, trivial fix.
2. **Fix Stata workbench Mata syntax** (high · calculate) — `buildStata` in `exportScript.js` emits syntactically invalid Mata code; fix the two identified issues (standalone statements outside Mata scope, tuple-print syntax). Medium effort, reported by user as "doesn't work completely."
3. **Fix group_summarize cross-group difference** (high · explore) — ATE-as-difference-in-means is a fundamental econometric operation; add a `splitByGroup` mode to `SampleTestPanel` and wire it to the Reshape tab. Medium effort.
4. **Fix CS DiD replication code** (high · export) — validate the destructuring path of the Callaway result object in `rScript.js`; the engine is validated but the export is broken. Medium effort.
5. **Add data viewer arrange/sort** (medium · data) — extend `getTablePage` with `ORDER BY` and add clickable column headers. Medium effort, frequently useful for data inspection.
6. **Save FeatureTab draft state** (medium · clean) — `sessionStorage`-based draft restore when navigating away from a case_when mid-edit. Low effort.
7. **Confirm and close likely-fixed items** — N-way FE categorical + interactions (2026-07-09) and chi-square hypothesis test (2026-07-17) appear to be already implemented; Franco should browser-verify and update BugTriage status.
8. **LaTeX table width adaptation** (medium · export) — wrap in `\resizebox` when column count > 4. Low effort, high user-facing polish.
9. **Add variable searcher in model sidebar** (medium · feature) — single `useState` filter input in `VariableSelector.jsx`. Very low effort.
10. **Browser-validate pending code-complete items** — Plot Builder resize/PNG (code-complete `35f4b5a6`), Y~Yhat/residual/Q-Q (`35f4b5a6`), spatial heatmap (`5e105529`), and coefficient plots frozen (`35f4b5a6`) are all marked "browser validation pending Franco". Clearing these unblocks the BugTriage backlog significantly.
