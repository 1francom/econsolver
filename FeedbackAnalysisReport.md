# Feedback Analysis Report — 2026-06-23

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **6 open bugs** (5 code-complete pending browser validation · 1 unfixed) · **6 open feature/UX requests** (fix-later or new) · **1 open performance item** · **1 open other**

Cross-reference note: several ClaudeFB.md entries that still lack ~~strikethrough~~ were confirmed resolved in BugTriage or the X5 reconciliation table (.dta parsing, p-value bug, grid sizing, dataset-deleted stale vars, auto-reestimate on variable toggle, equation plotting, color mapping, Monte Carlo single-variable, resampling, LaTeX crash, Poisson interpretation, XLSX CDN failure, OECD 406, XSS payloads, Callaway-Santana split). These are tracked as resolved and are excluded below.

---

## Bugs

### [high] · Model · 2026-06-17
**Feedback:** Pipeline only applies if dataset is also selected in Clean Module  
**File:** `src/App.jsx` (lines 2811, 2838–2842, 2845–2847) · `src/DataStudio.jsx` (line 1138)  
**Suggestion:** `tabOutput("model")` is keyed by dataset ID and only gets populated when `WranglingModule.onComplete` fires — which only happens while the Clean tab is the active shell. When a user goes directly to the Model tab and selects a dataset that was never opened in Clean, `outputs[dsId]` is null and the model receives raw un-pipelined data. Fix: on dataset selection in any non-Clean tab, eagerly replay the saved pipeline from IndexedDB (via `loadPipeline(pid, dsId)` + `runPipeline`) and push the result into `outputs` state via `handleComplete`. Alternatively, persist the last `cleanedData` payload per dataset in IndexedDB on every `onComplete` call and hydrate it during `DataStudio` initialisation so `tabOutput` is always populated regardless of which tab the user visits first.  
**Invariants:** Pipeline must replay on `rawData` — never mutate in place (runner.js). IndexedDB is the required persistence layer — not localStorage.

---

### [high] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.  
**File:** `src/components/modeling/ResidualPlots.jsx` · `src/components/ModelingTab.jsx` · `src/components/modeling/ModelPlots.jsx`  
**Status:** Code-complete in commit `35f4b5a6` (frozen `result.spec` approach). **Browser validation by Franco is the remaining action.**  
**Suggestion:** Validate that (a) editing variable selectors after estimation leaves the displayed diagnostics unchanged, and (b) clicking a pinned model from the buffer re-renders exactly the frozen plots for that model. If either regresses, the fix is to ensure all diagnostic and coefficient plot components read from `result.spec` (the snapshot taken at estimation time), not from live component state.  
**Invariants:** none additional.

---

### [high] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS  
**File:** `src/components/modeling/ResidualPlots.jsx` · `src/components/modeling/ModelPlots.jsx` · `src/components/ModelingTab.jsx`  
**Status:** Code-complete in commit `35f4b5a6`. **Browser validation by Franco is the remaining action.**  
**Suggestion:** Confirm all three plots render for a simple OLS run (at least 2 regressors, ~30 rows). Verify that `result.residuals` and `result.fitted` are populated by `LinearEngine.runOLS` and reach `ResidualPlots` via `ModelingTab`. If plots still do not appear, check that the frozen-result-state fix in `35f4b5a6` did not accidentally sever the data path feeding residuals to `ResidualVsFitted` and `QQPlot`.  
**Invariants:** none additional.

---

### [medium] · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems  
**File:** `src/components/PlotBuilder.jsx` (line 1626) · `src/components/ModelingTab.jsx`  
**Status:** Code-complete in commit `35f4b5a6` (native `resize: "vertical"` wrapper, bounded `maxHeight: "70vh"`). **Browser validation by Franco is the remaining action.**  
**Suggestion:** Confirm the drag-resize handle is visible and functional in the ModelingTab embed, and that the exported PNG reflects the user-selected dimensions rather than the 400 px default. If the `ResizeObserver` still triggers a layout loop when Leaflet is present in the same view, scope the observer to the non-Leaflet plot wrapper (the Leaflet case is already isolated at PlotBuilder.jsx line 598).  
**Invariants:** none additional.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed  
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` · `src/components/tabs/spatial/map/SpatialLayerEditor.jsx` · `src/components/tabs/spatial/map/layers.js`  
**Status:** Code-complete in commit `5e105529` (Leaflet heatmap layer with KDE, palette/opacity controls, legend, HTML export, 5 k-point cap). **Browser validation by Franco is the remaining action.**  
**Suggestion:** Open the Map tab with a point dataset, add a Heatmap layer, and verify the gradient renders over the basemap with correct palette and opacity. Test the 5 k-point cap on a larger dataset; confirm the HTML export produces a self-contained file with the heatmap visible.  
**Invariants:** none additional.

---

### [medium] · Explore · 2026-05-11
**Feedback:** The graph does not fit the screen  
**File:** `src/components/PlotBuilder.jsx` · `src/ExplorerModule.jsx`  
**Status:** Code-complete in commit `35f4b5a6` (responsive canvas sizing via `ResizeObserver`). **Browser validation by Franco is the remaining action.**  
**Suggestion:** Resize the browser window while a plot is displayed in Explorer's Plot Builder tab and confirm the chart reflows without overflow or empty space. Pay particular attention to histogram and density geoms, which previously had fixed-height containers that overflowed narrow viewports.  
**Invariants:** none additional.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js` · `src/pipeline/duckdbRunner.js`  
**Status:** Code-complete for `.dta` files (Fase X4 — Arrow-backed DuckDB table, 500-row UI preview, OPFS Parquet cache). **Browser benchmark with a 900 k-row CSV is still pending Franco.**  
**Suggestion:** Load a 21 MB CSV (not .dta) in a browser session and confirm: (a) the file routes through the DuckDB path (DuckDB badge appears in wrangling header), (b) tab transitions complete in under 3 seconds, (c) pipeline steps (filter, log, lag) operate on the full 900 k rows. If CSV files still bypass DuckDB, extend the `>10 MB` dispatch in `DataStudio.parseFile` to cover CSV/TSV in addition to the existing `.dta` branch, converting to Parquet on first load as the architectural plan specifies.  
**Invariants:** Data must never leave the browser unencrypted. Full dataset must always be passed to runner.js / duckdbRunner.js — never truncate rawData before pipeline steps.

---

## Other

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47… (truncated)  
**File:** `src/DataStudio.jsx` (CSV parser branch)  
**Status:** Deferred post-launch per BugTriage 2026-06-14 ("resilient-import pass after launch").  
**Suggestion:** Add a `try/catch` around individual row parsing in the CSV path; collect malformed row indices and present a modal offering "skip N rows and continue" or "abort". Surface the original row number and the raw text so the user can fix the source file. The recovered DataFrame should be offered to the pipeline even if some rows are dropped.  
**Invariants:** Never silently drop rows — the user must be informed of any skipped data.

---

## Features

### [medium] · Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept  
**File:** `src/math/LinearEngine.js` (line 128 — `k = xCols.length + 1`) · `src/components/modeling/ModelConfiguration.jsx`  
**Status:** Deferred per BugTriage 2026-06-14 ("engine-wide contract/export compatibility change").  
**Suggestion:** Add a `noIntercept: boolean` flag to the estimation spec and thread it through all engines. In `runOLS`, when `noIntercept` is true, build the design matrix without the leading ones column and adjust `k`, `df`, and F-stat accordingly. Expose a checkbox in `ModelConfiguration.jsx` under "Estimation options". Ensure R/Stata/Python script export emits `lm(y ~ x - 1)` / `reg y x, noconstant` / `sm.OLS(y, x)` (no `add_constant`) when the flag is set.  
**Invariants:** SE type must still be passed explicitly — never hardcode inside the no-intercept branch. Zero-React rule applies to `src/math/`. Export scripts live in `src/services/export/`.

---

### [low] · Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/components/ModelingTab.jsx` (RDD result section) · `src/math/CausalEngine.js` (line 579 — hardcoded `"D (treatment)"` coefficient name)  
**Status:** Deferred per BugTriage 2026-06-14 ("presentation enhancement").  
**Suggestion:** Add a collapsible "rdrobust-style summary" panel in the RDD results section displaying: method (local polynomial, triangular kernel), polynomial order, bandwidth (h, b), effective N left/right of cutoff, conventional and robust point estimate, SE, z-stat, p-value, and 95% CI. All values are already present in `EstimationResult`; this is a display-only component. Also consider substituting the actual `runningVar` and `treatCol` column names for the hardcoded labels in `CausalEngine.js` line 579.  
**Invariants:** none.

---

### [low] · Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names  
**File:** `src/math/CausalEngine.js` (line 579) · `src/components/modeling/resultDisplay.jsx` · `src/services/export/latexTable.js`  
**Status:** Deferred per BugTriage 2026-06-14 ("post-launch reporting metadata feature").  
**Suggestion:** Introduce an optional `labelMap: Record<string, string>` field on `EstimationResult` (or a per-project label registry in `indexedDB.js`). The `resultDisplay.jsx` coefficient table renders `labelMap[coefName] ?? coefName`. An inline ✎ icon next to each label opens a text input. The `latexTable.js` export prefers `labelMap` values. For RDD, `CausalEngine` can pre-populate `labelMap` with `{ "D (treatment)": treatCol, "running − c": `${runVar} − ${cutoff}` }` so the table is meaningful by default without any user action.  
**Invariants:** Label edits must be persisted in IndexedDB — not localStorage. `src/math/` files must not import from React.

---

### [low] · Explore · 2026-05-19
**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option  
**File:** `src/services/exprEvalService.js` · `src/components/wrangling/CleanTab.jsx`  
**Status:** Deferred per BugTriage 2026-06-14 ("post-launch expression-language compatibility").  
**Suggestion:** Extend `evalFilter` in `exprEvalService.js` to recognise the R-style `col %in% c(val1, val2, ...)` membership syntax. A small regex pre-processor on the expression string can rewrite `x %in% c(a, b)` → `[a, b].includes(x)` before evaluation. The `FilterBuilder` UI in `CleanTab.jsx` could additionally expose an "is one of" operator with a tag-input widget for multi-value membership filters.  
**Invariants:** Pipeline filter steps must replay deterministically on `rawData` — the rewrite must produce stable output.

---

### [low] · Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` · `src/components/tabs/spatial/map/SpatialPlotTab.jsx`  
**Status:** Deferred per BugTriage 2026-06-14 ("route to V4 plots after launch").  
**Suggestion:** Add a collapsible "Layer filter" panel per layer in both the static GeoPlot and the Leaflet map tab. The filter accepts column-value conditions (similar to `FilterBuilder` in `CleanTab.jsx`) and applies them to the layer's row set before rendering, without mutating `rawData`. This is a display-only filter — it must not add a pipeline step or touch `rawData`.  
**Invariants:** Must not mutate rawData. No React imports in spatial math files.

---

### [low] · Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx` (line 1169 — `activeTool` state) · `src/math/calcEngine.js`  
**Status:** New request — not yet in BugTriage or X5 reconciliation.  
**Suggestion:** Add a "Limits" tool to the Calculate tab's tool selector alongside Derivative, Integral, and Solver. The implementation can use symbolic evaluation via `symbolicDiff.js` for polynomial limits, plus a numerical left/right-approach method (evaluate at `x₀ ± ε` for small ε) as a fallback for transcendental functions. The UI should accept `f(x)`, the point `x → a` (including `±∞`), and display the symbolic result with a KaTeX render. Wire into the existing `activeTool` state at `CalculateTab.jsx:1169`.  
**Invariants:** Math implementation must live in `src/math/` with no React imports. KaTeX with `trust: false` is the correct output path — not raw HTML injection.

---

## Suggested action order

1. **[UNFIXED · high] Pipeline not applied in Model unless Clean tab visited first** — `src/App.jsx` + `src/DataStudio.jsx`: replay saved pipeline from IndexedDB on dataset selection in any non-Clean tab and populate `outputs` state via `handleComplete`.
2. **[browser validation needed] Validate commits `35f4b5a6` and `5e105529` in browser** — covers five items: frozen diagnostics/pinned-model restore, OLS residual plots, Plot Builder resize + PNG export, Explore graph fit, and Spatial heatmap. Ask Franco to run these checks and strike through the corresponding ClaudeFB.md entries once confirmed.
3. **[browser benchmark needed] Confirm Fase X4 performance fix handles 900 k-row CSV** — if CSV files still bypass DuckDB, extend the `>10 MB` dispatch in `DataStudio.parseFile` to cover all large text formats, not just `.dta`.
