# Feedback Analysis Report — 2026-07-16

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **5 open bugs** · **5 open UX/features** · **1 open performance** · **0 open other**
- 4 of the 5 open bugs are code-complete pending browser validation by Franco (`35f4b5a6` / `5e105529`)
- 2 ClaudeFB entries marked "open" are already implemented in code — should be marked ✓

---

## Bugs

### [medium] · Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (`parseCSV`, line 41)
**BugTriage:** fix-later — "post-launch resilient-import pass"
**Suggestion:** `parseCSV` has no per-row error isolation — a malformed row (mismatched quotes, extra delimiters) aborts parsing entirely. Add a try/catch around the per-row tokenize call; on failure, record `{ _rowError: true, _rawLine: line, _rowIndex: i }` and continue. After parsing, if `errorRows.length > 0`, surface a non-blocking warning banner with a skip-and-proceed action rather than aborting the entire import. Exclude error rows from the Arrow table passed to DuckDB to preserve column type consistency.
**Invariants:** `_rowError` objects must not leak into the DuckDB Arrow schema. The pipeline must continue to replay on `rawData` (non-destructive; no mutation of the raw parse result).

---

### [high] · Model · 2026-06-10
**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`
**File:** `src/components/PlotBuilder.jsx` (line 1626), `src/components/ModelingTab.jsx`
**BugTriage:** code-complete (`35f4b5a6`), browser validation pending Franco
**Suggestion:** `PlotBuilder.jsx:1626` already wires `resize: "vertical"` with `minHeight: 220`/`maxHeight: "70vh"`, and the PNG export captures the resized canvas dimensions. This item needs a browser confirmation pass only — no code change expected. Franco should confirm: (1) drag handle appears at the plot's bottom edge; (2) PNG download reflects the user-resized height; (3) Observable Plot's dark-mode patch does not bleed into the export image.
**Invariants:** none

---

### [high] · Model · 2026-06-11
**Feedback:** `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS`
**File:** `src/components/modeling/ResidualPlots.jsx` (`ResidualVsFitted` at line 189, `QQPlot` at line 316), `src/components/ModelingTab.jsx`
**BugTriage:** code-complete (`35f4b5a6`), browser validation pending Franco
**Suggestion:** Both components exist and are wired. The `35f4b5a6` fix freezes OLS/WLS diagnostics to the estimation's frozen `result.spec`, preventing sidebar edits from clearing the plots. Franco should confirm: (1) after running OLS, all three panels (Y~Ŷ scatter, Residuals vs Fitted, Normal Q-Q) appear under Diagnostics; (2) toggling a variable in the sidebar does not re-draw or clear the panels; (3) restoring a pinned model restores the correct diagnostic plots.
**Invariants:** Residuals must be read from the frozen `result` object, never re-computed from live `cleanedData`. `ResidualPlots.jsx` is a pure renderer — it may not import from `src/math/` or trigger a new estimation.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** `Heatmap must be fixed`
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` (`buildHeatmapCells` at line 20; heatmap branch at line 224), `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**BugTriage:** code-complete (`5e105529`), browser validation pending Franco
**Suggestion:** Heatmap is restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, and a 5k-point cap. Franco should confirm: (1) selecting "Heatmap" in `SpatialLayerEditor` renders a visible KDE layer on the map; (2) palette and opacity sliders update the layer live; (3) HTML export produces a self-contained file with the heatmap rendered.
**Invariants:** none

---

### [high] · Model · 2026-06-14
**Feedback:** `Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.`
**File:** `src/components/ModelingTab.jsx` (lines 734–740 — `result.spec` snapshot), `src/components/modeling/ModelPlots.jsx`
**BugTriage:** code-complete (`35f4b5a6`), browser validation pending Franco
**Suggestion:** The fix captures a frozen `result.spec` at estimation time and drives all displayed plots from it, not from live sidebar state. Franco should confirm: (1) after estimation the coefficient plot shows the estimated spec; (2) toggling any variable in the sidebar does not alter the displayed plot; (3) clicking a pinned model in the buffer restores its coefficient plot and diagnostics exactly. If confirmed, add `result.spec` immutability to the "Key bugs fixed" section of CLAUDE.md.
**Invariants:** `result.spec` must not be mutated after `estimate()` sets it. `ModelPlots.jsx` must read variables from `result.spec.xVars`, not from live `xVars` state.

---

## UX / Features

### Explore/Clean · 2026-05-19 (fix-later)
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/services/exprEvalService.js`, `src/workers/exprEval.worker.js`
**BugTriage:** fix-later — "post-launch expression-language compatibility"
**Suggestion:** R's `%in%` is not valid JavaScript. In `exprEvalService.js`, add a pre-processing rewrite before the expression reaches the eval worker: `col %in% c("a","b")` → `["a","b"].includes(col)`, and `!col %in% c(...)` → `!["a",...].includes(col)`. Use a regex replace (e.g. `/(\w+)\s*%in%\s*c\(([^)]+)\)/g`). This is < 20 lines, requires no changes to `runner.js` or the pipeline registry, and unblocks the most common R-style membership filter pattern.
**Invariants:** The `filter` step in `runner.js` must not be modified — it passes the expression string to the eval worker unchanged. Only the `exprEvalService.js` pre-transform layer changes. The filter step must remain non-destructive (replays on `rawData`).

---

### Model · 2026-05-21 (fix-later)
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/modeling/results/` (no dedicated RDD file — falls back to main result block in `src/components/ModelingTab.jsx`), `src/math/CausalEngine.js`
**BugTriage:** fix-later — "presentation enhancement"
**Suggestion:** `CausalEngine.js` already returns all needed values (`bw`, `N_left`, `N_right`, `tau`, `se`, `pval`, `ci95`). Add a `RDDSummaryTable` component in `src/components/modeling/results/` that renders an rdrobust-style compact table (bandwidth, N left/right, coefficient, SE, t-stat, p-val, 95% CI). Wire it into the RDD results block in `ModelingTab.jsx` below the existing estimate card. No new math, no changes to `EstimationResult.js`.
**Invariants:** Display formatting only (`toFixed(4)` etc.) — no new fields in `EstimationResult.js`. No React in `src/math/`.

---

### Model · 2026-05-25 (fix-later)
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`
**BugTriage:** fix-later — "engine-wide spec/export compatibility"
**Suggestion:** Add a boolean `noIntercept` checkbox to `ModelConfiguration.jsx` scoped to OLS/WLS/2SLS. Thread it through `estimate()` in `ModelingTab.jsx` as `seSpec.intercept = false`. In `LinearEngine.runOLS`, when `intercept === false`, skip prepending the ones column to `X` and use uncentered R² (`1 − SSR / SST_uncentered`). For FE/FD, the flag should be a no-op (demeaning already absorbs the intercept). Update `rScript.js` / `pythonScript.js` / `stataScript.js` to emit `lm(y ~ x - 1)` / no `add_constant` / `reg y x, noconstant` respectively.
**Invariants:** `seType` must still be passed explicitly to the engine — `noIntercept` does not change the SE contract. No hardcoded intercept behavior inside math functions. No React in `src/math/`.

---

### Spatial · 2026-05-30 (fix-later)
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**BugTriage:** fix-later — "route to V4 plots after launch"
**Suggestion:** Add a per-layer attribute filter control to `GeoLayerConfig.jsx`: a column selector + value/range input. On change, pass pre-filtered rows (computed in JS) instead of the full data slice to the layer painter inside `SpatialGeoPlot.jsx`. This is a display-only filter — no pipeline step, no `rawData` mutation. Start with attribute equality/range filtering; defer geometry-intersection filtering (which would require `SpatialEngine.js` join logic) to a follow-up.
**Invariants:** Filtered rows must never be written to `rawData` or create a pipeline step. Spatial filter is view-only, scoped to the plot tab.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/components/modeling/resultDisplay.jsx`, `src/ReportingModule.jsx`
**BugTriage:** fix-later — "post-launch reporting metadata feature"
**Suggestion:** Add a `labelMap: Record<string, string>` to the model display layer (not to `EstimationResult.js`). Render each coefficient label as a lightly styled inline-editable element that writes into `labelMap` on blur. When generating the Stargazer/LaTeX table in `ReportingModule.jsx`, substitute coefficient names through `labelMap`. For RDD specifically, pre-populate `labelMap` from `result.spec` column names so the table is correct by default without any user editing.
**Invariants:** `labelMap` is display-only. It must never propagate to `EstimationResult.js` or any math engine. No React in `src/math/`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (OPFS restore at line 732; 500 MB hard cap at line 334), `src/services/data/duckdb.js`
**BugTriage / plan status:** Fase X4 code-complete 2026-06-14, browser benchmark pending Franco (`docs/superpowers/plans/2026-05-22-fase-x4-performance.md`)
**Suggestion:** The DuckDB-Wasm + OPFS + suffStatsCache + SQL push-down architecture is fully implemented. No code change expected — this is a benchmark pass. Franco should open the 900k-row fixture and time: (a) tab transition (< 5 s target), (b) OLS estimate with 5 covariates (< 5 s), (c) `group_summarize` (< 5 s), (d) CSV export. If any benchmark exceeds target, check: `window.__duckdb` to confirm the DuckDB singleton is initialized, and `window.__scCache` for suffStatsCache hit rate.
**Invariants:** All computation remains client-side (privacy-first). Display is capped at 500 rows but estimation/export must use the full DuckDB table.

---

## Stale ClaudeFB entries — recommend marking ✓

| Item | Evidence | Action |
|---|---|---|
| [2026-06-19 08:43] Calculate → "Add limits" | `CalculateTab.jsx:18` imports `limit`; full UI at lines 1586 + 2161–2194 (input, compute button, result display with left/right limits) | Mark ✓ in ClaudeFB |
| [2026-07-04 12:37] Calculate → "Allow plotting constants in graph (horizontal line)" | `src/components/calculate/workbench/ViewControls.jsx:30–39` manages `refLines` (`h`/`v` kinds); `WorkbenchCanvas.jsx:146–150` renders them as reference lines behind curves | Mark ✓ in ClaudeFB |

---

## Suggested action order

1. **Browser-validate the four code-complete model/spatial fixes** (Plot Builder resize, OLS residual plots, spatial heatmap, diagnostics freeze — all `35f4b5a6`/`5e105529`) — one QA session closes four open bugs simultaneously.
2. **Browser-benchmark the 900k-row dataset** for Fase X4 performance (tab transition, OLS estimate, group_summarize, export — all < 5 s) — closes the oldest open performance report.
3. **Add `%in%` rewrite in `exprEvalService.js`** (< 20 lines, no runner.js changes) — high user-visible impact for minimal scope; unblocks a common R-style filter pattern.
4. **Implement resilient CSV import** in `src/DataStudio.jsx:parseCSV` (per-row error isolation + skip-or-repair warning) — unblocks users with real-world messy exports.
5. **Add no-intercept toggle** in `ModelConfiguration.jsx` + `LinearEngine.js` — frequently requested by power users; well-scoped engine change with clear replication script implications.
