# Feedback Analysis Report — 2026-07-09

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 5 open bugs · 7 open features · 0 open UX · 1 open performance · 0 open other

Cross-reference methodology: items in ClaudeFB.md without ~~strikethrough~~ or ✓ were cross-checked against BugTriage.md and the X5 reconciliation table. Items that BugTriage marks "fixed" or that X5 marks "verified fixed" are treated as resolved even if ClaudeFB.md has not yet been updated with a strikethrough. Items marked "code-complete" in BugTriage remain open until browser-validated by Franco. Items marked "fix-later" or "wontfix" are excluded from this report. The 13 items below are the genuinely unresolved open set as of 2026-07-09.

---

## Bugs

### [high] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage marks this code-complete as of `35f4b5a6` (2026-06-14): the three plot components were already wired, and the display was fixed so OLS/WLS diagnostics now render from the frozen `result.spec` rather than live sidebar state. The only remaining action is browser validation by Franco — run an OLS model and confirm all three plots appear under the Diagnostics collapsible panel and remain stable when adding/removing variables without re-estimating.
**Invariants:** Diagnostic plots must read from the immutable `EstimationResult` returned by the engine, never from live `xVars`/`yVar` selector state. `ResidualPlots.jsx` must not import from `src/math/`.

---

### [high] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** BugTriage marks this code-complete as of `35f4b5a6` (2026-06-14): displayed model now reads from `result.spec` (the frozen estimation snapshot). Browser validation by Franco is the remaining step — add a variable after estimating and confirm plots and BP/DW/JB values do not change; then pin the model, switch estimators, restore the pin, and verify diagnostics reappear correctly.
**Invariants:** Never derive display state from live variable-selector state after estimation. The frozen `EstimationResult` is the single source of truth for display.

---

### [high] · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage marks this code-complete as of `35f4b5a6` (2026-06-14): the plot viewport now has a native vertical resize handle with bounded responsive canvas sizing, so PNG captures the user-selected dimensions. Requires browser validation — drag the resize handle in ModelingTab, then trigger PNG export at the resized dimensions and confirm no clipping or dark-theme style bleed in the exported image.
**Invariants:** PlotBuilder uses Observable Plot 0.6 loaded from CDN; any resize logic must not break the `ResizeObserver` or trigger layout thrash. Inline styles via `C` constants from `shared.jsx` only — no CSS files.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** BugTriage marks this code-complete as of `5e105529` (2026-06-14): heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Browser validation required — load a point dataset, select the Heatmap layer type in the Map tab, confirm the gradient renders, adjust opacity, and export HTML. If the layer fails to appear, check that the 5k-point cap is not silently filtering out all rows (debug by logging `pts.length` in the KDE builder).
**Invariants:** Leaflet is a CDN singleton loaded via `src/components/tabs/spatial/shared/leaflet.js` — never import it statically. The 5k-point cap is intentional to avoid OOM on large datasets; do not remove it.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... — import aborted on a malformed row.
**File:** `src/DataStudio.jsx` (CSV parse entry point ~line 403), `src/services/data/parsers/` (no dedicated CSV parser — DuckDB path governs files >10 MB)
**Suggestion:** BugTriage status is "fix-later — post-launch resilient-import pass." When a CSV parse error occurs, surface the offending line number and raw content in an inline warning banner rather than aborting the entire import. Offer two actions: "Skip bad rows and continue" (drops malformed lines, logs skipped count in the session log) or "Cancel". For the DuckDB large-file path, `read_csv_auto` supports `IGNORE_ERRORS=TRUE` — wire that flag behind a user opt-in toggle. For the JS path, wrap per-row parsing in a try/catch and collect failures before completing the import.
**Invariants:** Non-destructive pipeline invariant — partial data must not be stored silently under the same dataset key. Bad rows must be surfaced and user-acknowledged before the import completes. Never mutate `rawData` in place.

---

## Features

### Model · 2026-05-25 (fix-later)
**Feedback:** Add an option to estimate without intercept (suppressed constant).
**File:** `src/math/LinearEngine.js` (`runOLS`, `runWLS`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add a `noIntercept: boolean` flag to the engine function signatures following the same explicit-argument pattern as `seType`. In `runOLS`/`runWLS`, when `noIntercept=true`, skip prepending the ones column to the design matrix and adjust degrees of freedom to `n - k` (not `n - k - 1`). Expose the toggle as a checkbox in `ModelConfiguration.jsx` under Advanced Options. The flag must propagate through `estimate()` → engine → `EstimationResult.spec` so replication scripts emit the correct syntax (`lm(y ~ x - 1)` in R, `nocons` in Stata).
**Invariants:** `seType` architecture invariant applies symmetrically — `noIntercept` must be an explicit argument, never inferred internally. Zero React imports in `src/math/`. Validate against R `lm(y ~ x - 1)` to 6 decimal places on coefficients and 4 on SE before shipping.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))` — bandwidth, effective N, kernel, polynomial order, and LATE estimate in a compact header.
**File:** `src/components/ModelingTab.jsx` (RDD result display section), `src/components/modeling/resultDisplay.jsx`
**Suggestion:** `runSharpRDD` in `CausalEngine.js` already returns `bandwidth`, `nLeft`, `nRight`, `kernel`, and `polyOrder` in the `EstimationResult`. Add a compact summary panel — either inline in `ModelingTab.jsx` or extracted as `src/components/modeling/results/RDDSummaryPanel.jsx` — that renders these fields in an `rdrobust`-style header table above the coefficient row. No engine changes are needed; all data is already present in the result object.
**Invariants:** Any new display component is pure React/JSX and must not import from `src/math/`. Use `C` constants for all styles.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names.
**File:** `src/components/modeling/resultDisplay.jsx`, `src/ReportingModule.jsx`
**Suggestion:** Add an optional `coefLabels: { [internalName: string]: string }` map to `EstimationResult`. Expose an "Edit labels" pencil icon in the coefficient table that toggles inline text inputs per row, writing back to `coefLabels`. The LaTeX/Stargazer render in `ReportingModule.jsx` reads `coefLabels[name] ?? name`. The engine in `CausalEngine.js` does not need to change — the hardcoded `varNames` at line ~579 are the internal names; labels are purely a display-layer override.
**Invariants:** `src/math/` files must remain pure JS with no React imports. `coefLabels` must be stored per-result in `EstimationResult`, not globally, so pinned model comparison tables remain independent.

---

### Explore · 2026-05-19 (fix-later)
**Feedback:** Add `filter(regionname %in% c("Spain", "Basque Country"))` for filter option — R-style `%in% c(...)` membership expression syntax not accepted.
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/services/exprEvalService.js`
**Suggestion:** The FilterBuilder already has an `in`/`nin` operator with `Array.includes` evaluation. The missing piece is a pre-processing regex pass in `exprEvalService.js` that detects `<col> %in% c(<csv-values>)` and rewrites it to the internal `{op:"in", col:"...", values:[...]}` node before evaluation. This is a string-level transform — no full parser is needed. Also handle `x %in% 1:N` (integer range) for completeness.
**Invariants:** The runner replays on `rawData` — the translated filter must serialise to a valid `filter` pipeline step (`registry.js` type `"filter"`). Expression parsing belongs in `exprEvalService.js`, not inside `runner.js`.

---

### Spatial · 2026-05-30 (fix-later, deferred to V4)
**Feedback:** Allow spatial filters for plots — row-level predicate scoped to a specific layer in the static geo-plot.
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer filter row in `GeoLayerConfig.jsx`: a column selector and a value/expression input that subsets the layer's rows before the geometry render loop. The filter operates on the client-side data array already available in plot state — no DuckDB query or pipeline step is required. Store the filter expression in layer config alongside existing aesthetics (`color`, `size`, etc.).
**Invariants:** Spatial plot filters must never modify `rawData` or inject pipeline steps. All filtering must stay within the plot component's local render state. Inline styles via `C` constants only.

---

### Calculate · 2026-06-19 (new — no BugTriage entry)
**Feedback:** Add limits (mathematical limit as x → a) to the Calculate tab.
**File:** `src/components/tabs/CalculateTab.jsx` (Math Tools panel, tool selector strip at line 1556)
**Suggestion:** Add a "lim Limit" tool tab to the existing five-tab strip (`= Solve`, `∂ Derive`, `f′ Symb.`, `Σ Algebra`, `∫ Integrate`). Implement numerically: evaluate the expression at a sequence of values approaching the limit point from both sides (`a ± ε` for `ε = 1e-4, 1e-6, 1e-8`) and check for convergence. Display both one-sided limits and flag whether the two-sided limit exists. For well-behaved CAS expressions, also try symbolic substitution via the existing `cas.compile` path before falling back to numerics.
**Invariants:** All logic stays within `CalculateTab.jsx` or a new co-located helper (not `src/math/`). No new CDN dependency. No new pipeline step type — limits are a calculator output, not a data transformation.

---

### Calculate · 2026-07-04 (new — no BugTriage entry)
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves).
**File:** `src/components/calculate/workbench/ViewControls.jsx`, `src/components/calculate/workbench/operations.js`
**Suggestion:** The Equation Workbench (`src/components/calculate/workbench/`) already supports horizontal reference lines via the `+ horizontal` button in `ViewControls.jsx` (line 95), which writes to `view.refLines` rendered by `WorkbenchCanvas.jsx` (line 146-167). The gap is that constants defined in the variable workspace (e.g. `c = 5`, `MC = alpha * w / A`) cannot be plotted as equation curves because `sampleCurve` in `operations.js` (line 17) requires the expression to depend on the axis variable. Fix: in `runPlot`, detect when `freeVars` is empty (constant expression), evaluate `f(scope)` once, and emit a single horizontal line segment across the full `xRange` rather than returning an empty points array. Additionally, in `ViewControls.jsx`, expose workspace scalar variables in the "Add reference line" UI as a `<select>` dropdown so users can pick `y = MC` without typing a numeric value.
**Invariants:** Inline styles via `C` constants from `shared.jsx`. No new CDN dependency. Constant-expression curves must still respect the `eq.axis` check and the existing multi-curve palette logic in `WorkbenchCanvas.jsx` (line 186).

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows.
**File:** `src/DataStudio.jsx` (large-file parse branch ~line 403), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Fase X4 is code-complete per the X5 reconciliation table — large CSV/TSV files are converted to Parquet via DuckDB and persisted to OPFS; tab transitions no longer re-materialise the full JS object array. Browser benchmark is the remaining action: load the 21 MB / 900k-row CSV fixture, navigate between all tabs (Data → Clean → Explore → Model → Simulate), and confirm no OOM crash and tab-transition latency under 2 seconds. Verify the `⚡ DuckDB` badge appears in the wrangling header and that model statistics report full `n` (not the 500-row preview cap, which was the separate DuckDB n=500 bug fixed in `c9c5bd3e`).
**Invariants:** Privacy-first constraint — computation must remain entirely client-side; no server-side data egress. Display is always capped at 200–500 rows for UI rendering, but all pipeline operations (`runner.js`, `duckdbRunner.js`) must operate on the full dataset. Never truncate `rawData` before passing it to a pipeline engine.

---

## Suggested action order

1. **Browser-validate four `35f4b5a6` / `5e105529` code-complete bugs** — OLS residual plots, frozen diagnostics display, Plot Builder resize/PNG, and spatial heatmap are all done in code; a single browser session with Franco unblocks all four.
2. **Browser-benchmark Fase X4 large-dataset performance** — Load the 900k-row CSV, confirm no OOM crash and sub-2 s tab transitions; the longest-standing open performance item.
3. **No-intercept regression toggle** — Requires engine changes in `LinearEngine.js` plus R validation; broad impact across OLS/WLS/RDD/2SLS estimation.
4. **Resilient CSV import** — Malformed-row skip/repair UX in `DataStudio.jsx`; unblocks users with messy real-world data without requiring a full re-download.
5. **Calculator limits tool** — Self-contained addition to `CalculateTab.jsx`; no cross-cutting dependencies.
6. **Plot constants in Workbench** — Small extension to `operations.js` + `ViewControls.jsx`; batch with limits work for efficiency.
7. **Editable LaTeX coefficient labels** — Display-layer only in `resultDisplay.jsx`; no engine changes required.
8. **RDD `rdrobust`-style summary panel** — `EstimationResult` already has the data; pure UI addition.
9. **R `%in% c(...)` filter syntax** — Pre-processing pass in `exprEvalService.js`; low-risk, satisfies power-user cleaning workflows.
10. **Spatial plot layer filters** — Deferred to V4 per BugTriage; implement as local render-state filter in `GeoLayerConfig.jsx`.
