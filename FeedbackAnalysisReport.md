# Feedback Analysis Report — 2026-07-05

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 7 open bugs (1 critical · 3 high · 2 medium · 1 performance) · 8 open features · 0 open UX · 0 open other

Notes on scope:
- Items marked "code-complete … Browser validation pending Franco" in BugTriage are included as open: the fix is committed but not user-confirmed.
- Items marked "verified fixed" or "fixed" in BugTriage but not yet ✓ in ClaudeFB are excluded — the resolution evidence is sufficient.
- Items labelled "fix-later" in BugTriage are included as open features/bugs pending post-launch work.

---

## Bugs

### [critical] · Model · 2026-07-02
**Feedback:** When using DuckDB, the number of observations reported in model output uses the 500-row browser preview, not the full raw row count — affects n, degrees of freedom, t-statistics, F-statistics, and p-values.

**File:** `src/components/ModelingTab.jsx` — the `estimate()` function dispatches to the SQL suff-stats path when `shouldUseSQLPath` returns true; when it returns false (e.g. factor interactions, SC, Callaway, Sun-Abraham, spatial, poly RDD), it falls through to the JS path. On the JS path the `rows` constant (line 420: `cleanedData?.cleanRows ?? []`) is the 500-row preview for DuckDB-backed datasets. The `getFullRows` useCallback calls `extractAllRows` correctly, but `estimate()` does not call `getFullRows` before dispatching to JS engines — it uses `rows` directly.

**Suggestion:** In `estimate()`, before the "classic JS path" branch (around line 922), call `const jsRows = await getFullRows()` and substitute `jsRows` wherever `rows` is passed as data to JS engines. This mirrors the pattern already used by `runSpecCurve` and `runAllSubsets`. Also update the ModelingTab header display (around line 1952: `{rows.length} obs`) to show `cleanedData?._duckdb?.rowCount ?? rows.length` for an accurate pre-estimation count.

**Invariants:** Pure JS fix — no React in math files, no localStorage, no new API calls.

---

### [high] · Model · 2026-06-10 (code-complete; browser validation pending)
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.

**File:** `src/components/PlotBuilder.jsx` (line 1626 — native CSS `resize: "vertical"` wrapper; lines 1163–1165 `canvasW`/`canvasH` state) + `src/components/ModelingTab.jsx`.

**Suggestion:** Commit `35f4b5a6` added `resize: "vertical"` CSS and bounded-canvas sizing. If browser validation shows residual issues, verify that (a) the `ResizeObserver` inside PlotBuilder fires after the user drags the resize handle, updating `canvasW`/`canvasH`, and (b) PNG export reads from `canvasRef.current` (the `<canvas>` element, not the wrapper `<div>`) so the exported image matches the user-selected height.

**Invariants:** Inline styles via `C` constants only — no CSS files.

---

### [high] · Model · 2026-06-11 (code-complete; browser validation pending)
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.

**File:** `src/components/ModelingTab.jsx` (lines 2565–2575 — QQPlot and ResidualVsFitted render) + `src/components/modeling/ResidualPlots.jsx`.

**Suggestion:** Commit `35f4b5a6` froze OLS/WLS diagnostics to `result.spec`. During browser validation confirm: (a) `result.resid` is populated on the estimation result (check that `runOLSFromSuffStats` copies `.resid` from the classical solve), (b) `QQPlot` and `ResidualVsFitted` receive non-null residuals, and (c) the tab containing them is rendered unconditionally for OLS/WLS result types.

**Invariants:** `ResidualPlots.jsx` must remain a pure UI component — no math imports.

---

### [high] · Model · 2026-06-14 (code-complete; browser validation pending)
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.

**File:** `src/components/ModelingTab.jsx` (result/spec binding for diagnostic and coefficient display) + `src/components/modeling/DiagnosticsPanel.jsx`.

**Suggestion:** Commit `35f4b5a6` claims displayed plots and diagnostics now derive from `result.spec`. Verify: (a) toggling X variables after estimation does not alter the displayed coefficient table, (b) clicking a pinned model in the buffer re-renders all plots from that model's stored result, and (c) `DiagnosticsPanel` receives its data from the pinned `result` — not from live `xVars`/`yVar` state. If (c) fails, confirm the `DiagnosticsPanel` render site passes `result.resid` and `result.spec.xVars`.

**Invariants:** None.

---

### [medium] · Spatial · 2026-06-13 (code-complete; browser validation pending)
**Feedback:** Heatmap must be fixed.

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` + `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`.

**Suggestion:** Commit `5e105529` restored the Heatmap as a Leaflet layer with a 5k-point KDE cap. During browser validation confirm: (a) the "Heatmap" type appears in the layer-type picker (line 973 of SpatialLayerEditor.jsx shows it in the array), (b) `buildHeatmapCells` (line 20) produces non-empty cells when lat/lon columns are selected, and (c) the `leaflet.heat` plugin CDN origin is present in `vercel.json` `connect-src` — following the precedent of the DuckDB WASM fix (2026-07-02 CLAUDE.md bug note).

**Invariants:** Any new CDN dependency must appear in both `script-src` and `connect-src` in `vercel.json`.

---

### [medium] · Data · 2026-05-10 (fix-later)
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 — malformed row aborts import entirely instead of surfacing recovery options.

**File:** `src/DataStudio.jsx` (Papa Parse call in the CSV branch of `parseFile`) + `src/services/data/parsers/`.

**Suggestion:** Extend the Papa Parse `error` callback to accumulate bad rows (original line numbers + raw content) rather than aborting. After parsing completes, surface a non-blocking warning badge showing the skip count and offering a "Download skipped rows as CSV" action. The `skipEmptyLines` and `error` callback are already wired — this is a targeted extension, not a rewrite.

**Invariants:** Non-destructive pipeline — the cleaned row array passed to `rawData` must visibly account for any skipped rows.

---

### [performance] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows.

**File:** `src/DataStudio.jsx` (`parseFile` DuckDB dispatch branch) + `src/services/data/duckdb.js` (singleton init, OPFS Parquet cache).

**Suggestion:** Fase X4 is code-complete. Browser benchmark (`plans/2026-05-22-fase-x4-performance.md`) should confirm: (a) 21MB+ CSV loads without crashing into a DuckDB Arrow table, (b) UI preview is limited to 500 rows, (c) pipeline steps run on the full DuckDB table, and (d) re-opening the same project loads from OPFS Parquet. If the benchmark fails on the deployed Vercel site but not locally, the root cause is likely missing `Cross-Origin-Embedder-Policy: require-corp` / `Cross-Origin-Opener-Policy: same-origin` headers in `vercel.json` (required for `SharedArrayBuffer` in DuckDB multi-thread mode).

**Invariants:** Data must never leave the browser. Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`.

---

## Features

### Calculate — 2026-07-04 (new)
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves).

**File:** `src/components/calculate/workbench/ViewControls.jsx` (lines 28–39 — `refLines` UI already present) + `src/components/calculate/workbench/WorkbenchCanvas.jsx` (lines 146–172 — geom_hline/geom_vline already rendered) + `src/components/calculate/workbench/operations.js` (`sampleCurve` function).

**Suggestion:** The horizontal reference-line plumbing already exists (`refLines` with `kind: "h"`) — the gap is discoverability. First, verify that entering a numeric constant (e.g. `5`) as an equation expression in `EquationsPanel` works: `sampleCurve` compiles the expression and evaluates it per x, so a constant should yield a flat line. If `cas.compile("5", [])` fails for empty `freeVars`, add a special case: if the expression evaluates to a finite scalar, synthesise `[{x: a, y: scalar}, {x: b, y: scalar}]` directly. Second, make the existing `refLines` panel in `ViewControls.jsx` more discoverable — e.g. a "⊣ Add reference line" button near the axis controls.

**Invariants:** None.

---

### Clean / General — 2026-07-01 (new)
**Feedback:** Add confirmation buttons for all critical functions like clearing all the datasets, clearing all the pipeline, signing out, etc.

**File:** `src/App.jsx` (line 1832 `handleClearAll` — fires without confirmation; line 2282 `signOut()` — fires on a single click).

**Suggestion:** Add a two-click inline confirmation pattern: on first click, set a `confirmPending` state for 4 seconds and change the button label to "Confirm?" (tinted red/gold); a second click within that window fires the action; the timer resets on blur. Apply to: (1) "Clear all projects" (`App.jsx` line 2048), (2) "Sign out" (`App.jsx` line 2282), (3) any future "Delete dataset" or "Reset pipeline" actions not already covered by the mid-pipeline delete warning in `WranglingModule.jsx` (line 322). A single local `useState` per button — no new component or modal needed.

**Invariants:** Inline styles via `C` constants only. No modal library.

---

### Calculate — 2026-06-19 (new)
**Feedback:** Add limits.

**File:** `src/components/tabs/CalculateTab.jsx` (line 1168 — `activeTool` state; line 309 — `\lim_{x\to 0}` button already in Math Pad) + `src/components/calculate/workbench/operations.js` + optionally `src/math/` for a pure-JS limit engine.

**Suggestion:** Add a "Limits" tab to the `activeTool` switcher. Implement numerical one-sided and two-sided limits via the Richardson extrapolation pattern: evaluate `f(c ± ε)` for decreasing ε (1e-4 → 1e-10), detect convergence, report ±∞ or "DNE" if left/right limits diverge. Wire into the existing equation scope so workspace variables are in scope. If `cas.limit(expr, var, val)` is available in the nerdamer CAS already loaded, use it for symbolic limits. Add the tool below the "Integral" section using the same `ResultBox` + `ErrBox` pattern. Any pure math moved to `src/math/` must have zero React imports.

**Invariants:** `src/math/` files must remain pure JS — no React imports.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names.

**File:** `src/ReportingModule.jsx` (LaTeX Stargazer builder) + `src/math/CausalEngine.js` (`runSharpRDD` hardcodes `varNames`).

**Suggestion:** (1) In `CausalEngine.runSharpRDD`, replace hardcoded `varNames` like `"D (treatment)"` with the actual column names from the spec (`treatVar`, `runningVar`, `cutoff`). (2) In `ReportingModule.jsx`, before rendering the LaTeX table, show a row of editable text inputs (one per coefficient) pre-filled with the variable names; edits update a local `labelOverrides` state that the LaTeX builder applies. This is a display-only change — the coefficient vector is unchanged. Confine all display-label logic to `ReportingModule.jsx`; `EstimationResult.js` may carry an optional `displayLabels` map as pure data, but `src/math/` files must not render or format labels.

**Invariants:** No display logic in `src/math/` or `src/core/`.

---

### Spatial · 2026-05-30 (fix-later)
**Feedback:** Allow spatial filters for plots.

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` + `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`.

**Suggestion:** Add per-layer row-filter controls to `GeoLayerConfig.jsx` following the column/operator/value pattern from `CleanTab.jsx`'s `FilterBuilder`. Each layer already receives a data slice from parent rows; a layer-level filter reduces it before rendering without touching `rawData`. Implement as one column-selector + one comparator chip (=, <, >, ≠, in) + one value input. Note: BugTriage marks this "fix-later / V4 plots after launch" — treat as post-launch if resources are constrained.

**Invariants:** No mutation of `rawData`; filter applies to the layer's display slice only.

---

### Model · 2026-05-25 (fix-later)
**Feedback:** Add an option to estimate without intercept.

**File:** `src/components/modeling/ModelConfiguration.jsx` + `src/math/LinearEngine.js` (constant column prepend) + `src/math/CausalEngine.js`, `src/math/PanelEngine.js`, `src/math/NonLinearEngine.js` + `src/services/export/rScript.js`, `pythonScript.js`, `stataScript.js`.

**Suggestion:** Add a `noIntercept: boolean` checkbox to `ModelConfiguration` (visible for OLS/WLS/2SLS/Logit/Probit/Poisson). Pass it through each engine's option object; gate the constant-column prepend on `!noIntercept` and adjust df accordingly. Update all three replication script generators: append `- 1` or `+ 0` to R formulas, pass `fit_intercept=False` in Python, append `, noconstant` in Stata. BugTriage notes this is an "engine-wide spec/export compatibility" change — coordinate across all engines before shipping to avoid partial support.

**Invariants:** `seType` must still be passed explicitly — `noIntercept` is a separate spec flag. Export scripts must reflect the UI setting.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — including bandwidth, effective obs left/right of cutoff, LATE estimate, SE, t-stat, p-value, CI.

**File:** `src/components/ModelingTab.jsx` (RDD result display section around line 3319) + `src/math/CausalEngine.js` (`runSharpRDD` return value).

**Suggestion:** Add a collapsible "RDD Summary" panel below the coefficient table for RDD results. The fields needed (`bandwidth`, `beta`, `se`, `tStats`, `pVals`) are already returned by `runSharpRDD`. Add `nLeft` and `nRight` (observations within bandwidth on each side of the cutoff) to the `runSharpRDD` return — they are computed during the local bandwidth filter and simply need to be exposed. Render in a two-column styled table matching existing result UI (`C` constants, `mono` font). No new math required.

**Invariants:** Display-only — math files may expose new fields as data, but no rendering logic in `src/math/`.

---

### Explore / Clean · 2026-05-19 (fix-later)
**Feedback:** Add R-style `%in% c(...)` membership filter syntax.

**File:** `src/services/exprEvalService.js` (async filter path used by `runner.js` line 1795) + `src/pipeline/runner.js` (line 273 — sync `new Function` filter path).

**Suggestion:** Before evaluation, pre-process the filter expression to transpile `x %in% c("a","b")` → `["a","b"].includes(x)` using a targeted regex: `expr.replace(/(\w+)\s*%in%\s*c\(([^)]+)\)/g, (_, v, args) => \`[${args}].includes(${v})\`)`. Apply this transpilation in `exprEvalService.js` before the expression is dispatched to the Web Worker. Also expose a syntax hint in `CleanTab.jsx`'s FilterBuilder. Security: the transpilation happens before the Worker receives the expression — do not evaluate untrusted expressions on the main thread (the existing `runner.js` line 1805 warning already documents this).

**Invariants:** SECURITY: filter expressions must go through the Web Worker path in `exprEvalService.js`, not the main-thread `new Function` path, for user-authored expressions. Transpilation is a string transform, not an eval.

---

## Suggested action order

1. **Fix DuckDB n=500 observation count [critical]** — corrupts all inference for large-n DuckDB estimations; call `getFullRows()` in `estimate()` JS-path branch before dispatching to JS engines (`ModelingTab.jsx`).
2. **Browser-validate Plot Builder resize + OLS residual plots + diagnostics freeze + heatmap [high+medium]** — four code-complete items in commits `35f4b5a6`/`5e105529` all need Franco's sign-off; group into one validation session.
3. **Confirmation buttons for destructive actions [feature, low effort]** — a two-state `confirmPending` pattern in `App.jsx` (2 locations) prevents accidental project and session loss; high user-value for low implementation cost.
4. **Add limits to Calculate tab [feature]** — straightforward extension of existing derivative/integral pattern using the nerdamer CAS already loaded.
5. **Implement R-style %in% filter syntax [feature]** — one regex pre-processing line in `exprEvalService.js`; high usability payoff for researchers who copy R filter expressions into the UI.
- **7 open bugs** (1 critical · 3 high · 3 medium) · **6 open features** · **1 open UX** · **1 open performance** · **0 open other**
- Total open items: **15**
- Several high-priority bugs (Plot Builder resize, OLS residual plots, diagnostics freeze, Spatial Heatmap) are code-complete in commit `35f4b5a6` / `5e105529` but await browser validation by Franco.

---

## Bugs

### [critical] · Model / DuckDB · 2026-07-02
**Feedback:** When using DuckDB, the number of observations reported in model output uses the 500-row browser preview, not the full raw row count — affects n, degrees of freedom, t-statistics, F-statistics, and p-values.

**File:** `src/components/ModelingTab.jsx` (line 1952 — header display `{rows.length} obs`); also the JS fallback branch for non-SQL-path estimators

**Suggestion:** Two probable failure sites. (1) The Modeling Lab header bar at line 1952 always renders `rows.length` (the 500-row preview slice) rather than `cleanedData._duckdb.rowCount` — fix by substituting `rowCount > rows.length ? rowCount : rows.length` (the same expression already used in `dispatchCtx.n` at line 934). (2) Verify that every estimator that bypasses `shouldUseSQLPath` calls `getFullRows()` before passing rows to `_runEstimation` — the JS fallback at line 1861 already does this for the main OLS path, but Synthetic Control, LSDV, Callaway-Sant'Anna, and Sun-Abraham need to be audited. The SQL path correctly computes n via `COUNT(*) AS n` in `duckdbOLS.js:76`, so suff-stats estimators should already produce the correct result.n — compare `result.n` vs `cleanedData._duckdb.rowCount` after estimation to confirm.

**Invariants:** `src/math/` must remain React-free. DuckDB handles must not be passed into engine functions — extraction belongs in ModelingTab, engines receive plain JS arrays.

---

### [high] · Model / PlotBuilder · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.

**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Code is marked complete in commit `35f4b5a6` (native vertical resize handle, responsive canvas bounded by user-selected dimensions, PNG captures user size). This item awaits browser validation by Franco — no further code change required until validated. If validation reveals a remaining PNG quality issue (color resolution, legend clipping), check the `devicePixelRatio` multiplier in the PlotBuilder canvas capture path.

**Invariants:** No external UI libraries. Inline styles via the `C` object from `shared.jsx`.

---

### [high] · Model / Diagnostics · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.

**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Code is marked complete in commit `35f4b5a6` — all three plots were already wired; the fix froze OLS/WLS diagnostics to the estimation's `result.spec` so sidebar edits cannot mutate the displayed model. Awaiting browser validation by Franco. If plots still do not appear after validation, check whether `result.resid` and `result.Yhat` are populated: the suff-stats SQL path sets `resid: null, Yhat: null` (LinearEngine.js:219) and relies on the lazy residuals thunk (`_hasLazyResiduals = true`, ModelingTab.jsx:1828) — confirm the thunk is resolved before ResidualPlots renders.

**Invariants:** Residual computation must stay in ModelingTab or ResidualPlots. `src/math/LinearEngine.js` must not import React or trigger side effects.

---

### [high] · Model / Diagnostics mutate · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.

**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`

**Suggestion:** Code is marked complete in commit `35f4b5a6` — diagnostics now render from the frozen `result.spec`, not live sidebar state. Awaiting browser validation by Franco. If mutation still occurs, confirm that `ModelPlots.jsx` and `DiagnosticsPanel.jsx` read from `result.spec` (not from live `xVars` / `yVar` state) and that the pinned model restore path calls `setResult(pinnedModel)` in a way that triggers a full re-render of both components.

**Invariants:** Do not introduce a second copy of spec state — derive all display from `result.spec`. The model buffer in `services/modelBuffer.js` stores trimmed results; restoring a pinned model must reconstruct the full spec.

---

### [medium] · Spatial / Heatmap · 2026-06-13
**Feedback:** Heatmap must be fixed.

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`

**Suggestion:** Code is marked complete in commit `5e105529` — Heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Awaiting browser validation by Franco. If the heatmap still fails to render, check whether the Leaflet heatmap plugin CDN origin is included in the `connect-src` CSP entry in `vercel.json` (the CLAUDE.md DuckDB bug note explains this class of failure: a CDN origin present in `script-src` but absent from `connect-src` silently breaks runtime fetch).

**Invariants:** Any new CDN dependency must be added to both `script-src` and `connect-src` in `vercel.json`.

---

### [medium] · Data / CSV · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...

**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/`

**Suggestion:** Tracked as "fix-later" in BugTriage. A malformed row in an otherwise well-formed CSV should not abort the entire import. Add a resilient-import path: catch per-row parse errors, record the offending row numbers and original text, let the rest of the file load, and surface a warning modal showing "N rows skipped — row 1583, …" with a repair/skip confirmation. The DuckDB path handles this more gracefully already; the issue is in the synchronous JS CSV parser used for small files.

**Invariants:** `runner.js` must always receive the recovered `rawData`, not a partially mutated intermediate. Skipped rows should be logged in the audit trail via `auditor.js`, not silently dropped.

---

---

## Features

### Model · RDD summary table · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression.

**File:** `src/components/modeling/results/` (no existing RDD-specific results component; parallel to `IVResults.jsx`), `src/math/CausalEngine.js`

**Suggestion:** Tracked as "fix-later" in BugTriage. `CausalEngine.js` already returns bandwidth, kernel, polynomial order, effective n, and LATE with SE. Add a `RDDResults.jsx` component modelled on `IVResults.jsx` that renders these in an R-style compact table: header row (estimator / kernel / bandwidth), then rows for LATE, SE, t-stat, p-value, 95% CI, and effective n left/right of the cutoff. Wire it into `resultDisplay.jsx` for the `"RDD"` type.

**Invariants:** Inline styles via `C` from `src/components/modeling/shared.jsx`. No CSS files or external UI libraries.

---

### Model · no-intercept estimation · 2026-05-25
**Feedback:** Add an option to estimate without intercept.

**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/index.js`

**Suggestion:** Tracked as "fix-later" in BugTriage. Add a `noIntercept: boolean` toggle in `ModelConfiguration` and thread it through `dispatchEstimation` → each engine that supports it. In `LinearEngine.runOLS`, when `noIntercept` is true, build `X` without the leading `1` column; df becomes `n − k` where k does not include the constant. The same flag must propagate to `runOLSFromSuffStats`, `buildOLSSuffStats` (dummySQL omits constant), and all SE paths. Also update R/Stata/Python replication scripts to emit `−1` / `nocons` / `+0`.

**Invariants:** `seType` must still be passed explicitly — do not hardcode SE variant inside the no-intercept branch. The `noIntercept` flag must be part of `result.spec` so pinned models restore correctly. `src/math/LinearEngine.js` has zero React imports.

---

### Spatial · plot-layer filters · 2026-05-30
**Feedback:** Allow spatial filters for plots.

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Tracked as "fix-later" in BugTriage (route to V4 post-launch). Add a per-layer filter row inside `GeoLayerConfig` — a column picker and value/range input that subsets the layer's rows before passing to `mkGeoLayer` in `geo.js`. Reuse a simple column-equals-value predicate applied in `SpatialGeoPlot.jsx`; no DuckDB query needed since the spatial data is already materialised as JS rows in state.

**Invariants:** Filters must operate on the JS row array already in component state. Never mutate `rawData` — always derive the filtered view. Non-destructive pipeline invariant does not apply here (this is a display filter, not a pipeline step), but the display filter must be visually distinguished from a data-modifying step.

---

### Calculate · limits · 2026-06-19
**Feedback:** Add limits.

**File:** `src/components/tabs/CalculateTab.jsx`

**Suggestion:** Not previously tracked. Add a "Limits" section to the Calculate workbench alongside derivatives and integrals. The symbolic CAS already wired for derivatives in CalculateTab likely supports `limit(expr, x, a)` — expose it with a UI input for expression, variable, approach value, and direction (left / right / both). Render the result as a LaTeX expression using the existing KaTeX path (the same `renderToString(..., { trust: false })` call already used for derivatives).

**Invariants:** Any pure-JS CAS helper must go into `src/math/` or `src/core/` with zero React imports. The KaTeX `trust: false` flag must remain set on all render calls — XSS safety.

---

### Clean · confirmation for destructive operations · 2026-07-01
**Feedback:** Add confirmation buttons for all critical functions like clearing all the datasets, clearing all the pipeline, signing out, etc.

**File:** `src/WranglingModule.jsx` (pipeline clear, `setPipeline([])`), `src/components/workspace/DatasetManager.jsx` (dataset delete — cascade-confirm already at line 771), `src/App.jsx` (sign-out at line 2282)

**Suggestion:** Not previously tracked. For pipeline clear, wrap the action in a modal confirm — reuse the pattern of `confirmDeleteStep` (WranglingModule.jsx:328). For sign-out (App.jsx:2282), a `window.confirm()` guard or a lightweight inline modal is sufficient. Verify `DatasetManager.jsx:771` cascade-confirm covers all dataset delete paths including multi-dataset clear. Keep the confirm UI minimal: Cancel / Confirm buttons with `C.bg` / `C.teal` inline styles.

**Invariants:** Inline styles only via the `C` object. No external modal libraries. Confirmation must be a modal overlay, not a route change or `window.alert()`.

---

---

## UX / Design

### Clean · R-style filter syntax · 2026-05-19
**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option.

**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/runner.js` (`filter` step handler)

**Suggestion:** Tracked as "fix-later" in BugTriage. Preferred approach: expose a dedicated "value is one of" operator in the `FilterBuilder` UI that generates a `{op: "in", values: [...]}` step config — this avoids fragile R-syntax parsing and is more discoverable than free-text expression input. Add an `"in"` operator case in the `filter` step handler in `runner.js` (and in `duckdbRunner.js` for the SQL path: `WHERE col IN (v1, v2, …)`). The alternative — parsing R `%in% c(...)` syntax — is brittle and should be avoided.

**Invariants:** Any new step operator must be added to both `runner.js` (JS path) and `duckdbRunner.js` (SQL path). `STEP_REGISTRY` must be updated so `pipelineReliabilityValidation.mjs` continues to pass. Non-destructive pipeline invariant applies.

---

---

## Performance

### Data · large datasets crash · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows.

**File:** `src/DataStudio.jsx` (parseFile routing, DuckDB dispatch), `src/services/data/duckdb.js`

**Suggestion:** Tracked as "routed" in X5 reconciliation — Fase X4 code-complete per `plans/2026-05-22-fase-x4-performance.md`, pending browser benchmark by Franco. If the benchmark confirms the crash is resolved, mark this item closed. If crashes persist, investigate: (1) the CSV path for small-to-medium files that bypass DuckDB — consider lowering the DuckDB threshold from 10MB to 5MB; (2) ensure the 200–500 row display cap is enforced everywhere via `getTablePage` rather than materialising the full JS array on render; (3) verify OPFS Parquet cache is hit on re-open so re-import is not repeated on tab transitions.

**Invariants:** Data must never leave the browser (privacy-first). No server-side computation. `runner.js` must always run on the full DuckDB table, not the 500-row preview. Display is the only layer that may truncate to 200–500 rows.

---

---

## Suggested action order

1. **Fix DuckDB n=500 critical bug** — patch ModelingTab.jsx:1952 header display to use `rowCount`, then audit Synthetic Control / LSDV / Callaway / Sun-Abraham JS fallback paths (corrupts all inference statistics)
2. **Browser-validate commit `35f4b5a6`** — resolves Plot Builder resize, OLS residual plots (Y~Yhat / Residual vs fitted / Q-Q), and diagnostics freeze in one pass
3. **Browser-validate commit `5e105529`** — resolves spatial heatmap
4. **Browser-validate Fase X4 large-dataset performance** — 900k-row crash; benchmark to confirm regression is gone before marking closed
5. **Add confirmation dialogs for destructive operations** — low implementation cost, high user-safety value; reuse `confirmDeleteStep` pattern from WranglingModule.jsx:328
6. **Add Calculate limits feature** — well-scoped, no architectural change; CAS already supports it
7. **Resilient CSV row-error recovery** — malformed row should not abort import; add skip-and-report path in DataStudio.jsx parseFile
