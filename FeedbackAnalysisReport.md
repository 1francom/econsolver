# Feedback Analysis Report — 2026-06-19

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.
> Supersedes 2026-06-18 report.

## Summary

- **7 open bugs** (1 truly open · 4 code-complete pending browser validation · 1 deferred · 1 security follow-up)
- **5 open features** (all deferred post-launch)
- **0 open UX/Design** items
- **0 open Performance** items (Fase X4 code-complete; tracked under bugs)
- **1 open Security follow-up** (server-side XSS guard missing)

> Cross-reference note: 28 ClaudeFB.md entries that are not strikethrough have corresponding `fixed` or `wontfix` entries in BugTriage.md and are excluded from this report. Only genuinely open or deferred items appear below.

---

## Bugs

### critical · Model · 2026-06-17

**Feedback:** "Pipeline only applies if dataset is also selected in Clean Module"
**File:** `src/App.jsx` (lines 2844–2848), `src/WranglingModule.jsx`
**Suggestion:** `handleComplete` in `App.jsx` keys the pipeline output under `tabDsId("clean")`, the dataset currently active in the Clean tab. `modelCleanedData` resolves via `tabOutput("model")` which uses `tabDsId("model")`. When these per-tab IDs diverge — e.g. the user loads data and navigates straight to the Model tab without ever touching Clean — `tabDsId("clean") !== tabDsId("model")`, so `outputs[tabDsId("model")]` is undefined and Model falls back to raw data with no pipeline applied. Fix: when `handleComplete` fires, also write the output under the dataset ID of every other tab whose `activeDatasetIds` entry matches the same underlying dataset, or add a fallback in `tabOutput` so tabs without an independent selection cascade to `outputs[tabDsId("clean")]` when both tabs share the same dataset.
**Invariants:** Keep pipeline replay non-destructive (`runner.js` is the source of truth). Route the fix through the existing `outputs` map — do not introduce direct inter-tab state coupling.
**BugTriage status:** open

---

### high · Model · 2026-06-11

**Feedback:** "Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS"
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` — frozen-result-state regression fixed so OLS/WLS diagnostics render from the estimation's frozen `result.spec` rather than live sidebar state. **Action needed: browser validation by Franco.** If plots still appear blank, verify that `result.spec` is fully populated before `ResidualPlots` renders and that the OLS result object (not `undefined`) is being passed down.
**Invariants:** Residual plots must read exclusively from the frozen `EstimationResult` — never recompute from live sidebar `xVars`/`yVar`.
**BugTriage status:** code-complete `35f4b5a6` — browser validation pending

---

### high · Model · 2026-06-14

**Feedback:** "Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models."
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` — display is frozen to `result.spec`. **Action needed: browser validation by Franco.** Verify that (1) toggling variables after a completed estimation leaves the displayed coefficients and diagnostics unchanged, and (2) loading a pinned model fully restores its frozen display state.
**Invariants:** Plot and diagnostic data must come exclusively from `EstimationResult` — no live sidebar variables.
**BugTriage status:** code-complete `35f4b5a6` — browser validation pending

---

### medium · Model · 2026-06-10

**Feedback:** "Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems"
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` — native vertical resize added with bounded responsive canvas sizing, so PNG captures the user-selected dimensions. **Action needed: browser validation by Franco.** If PNG dimensions are still wrong, ensure the `ResizeObserver` in `PlotBuilder.jsx` updates the Observable Plot spec `width`/`height` before the export canvas is drawn (the export path must read the current rendered dimensions, not the initial defaults).
**Invariants:** No external UI libraries. All resize handles must use inline styles via the `C` object from `shared.jsx`.
**BugTriage status:** code-complete `35f4b5a6` — browser validation pending

---

### medium · Spatial · 2026-06-13

**Feedback:** "Heatmap must be fixed"
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete in commit `5e105529` — Heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5 k-point cap. **Action needed: browser validation by Franco.** Confirm the Heatmap layer type appears in the layer editor, renders correctly on the map, and exports to HTML without errors.
**Invariants:** Spatial map layers must go through `SpatialLayerEditor` and use Leaflet only — no additional mapping CDN.
**BugTriage status:** code-complete `5e105529` — browser validation pending

---

### medium · Data · 2026-05-10

**Feedback:** "Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47..." (malformed row aborts import)
**File:** `src/DataStudio.jsx` (parseFile → CSV branch), `src/services/data/parsers/`
**Suggestion:** Wrap the CSV parser in a row-level error boundary so a single malformed row surfaces a recovery modal — showing the 1-based line number and the offending content — with options to skip the row or abort. For the DuckDB large-file path, `COPY ... (IGNORE_ERRORS true)` can skip bad rows without aborting, then surface a count of skipped rows in the UI banner. The repair path should emit a `patch` pipeline step, not mutate the raw data in place.
**Invariants:** Raw data is never mutated — offer repair via non-destructive pipeline steps (`patch`). IndexedDB is used for persistence, not localStorage.
**BugTriage status:** fix-later (post-launch)

---

### critical · Security · 2026-06-13

**Feedback:** Two XSS injection attempts via feedback form (JS alert payload; HTML `<script>` + `<img onerror>` payload).
**File:** Supabase Edge Function / DB trigger (feedback ingestion), `src/services/` (feedbackService)
**Suggestion:** Client-side `sanitizeFeedback` (`&<>` escape + 5 000-char cap) landed in 2026-06-14 and makes stored content inert in the current write-only path. However, client sanitization is bypassable by a direct POST to the Supabase endpoint. Add a Postgres trigger (`BEFORE INSERT`) on the `feedback` table that applies the same escaping, or add an input validation guard in the Edge Function before the `INSERT`. Document a durable invariant in the DDL comment: any future component rendering `feedback.description` must use escaped text output — never `dangerouslySetInnerHTML`.
**Invariants:** No feedback text must ever reach `AIService.js` raw. The `callClaude` choke-point must not receive unescaped user-submitted text. KaTeX `renderToString(..., trust:false)` already prevents XSS in the Calculate tab — maintain `trust:false`.
**BugTriage status:** client fix done 2026-06-14; Supabase server-side guard open

---

## Features

### Model · 2026-05-21

**Feedback:** "Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression"
**File:** `src/components/modeling/ModelPlots.jsx`, new component `src/components/modeling/results/RDDResults.jsx`
**Suggestion:** `runSharpRDD` in `src/math/CausalEngine.js` already returns `bandwidth`, `polyOrder`, `kernelType`, `nLeft`, `nRight`, `tau` (LATE), `se`, `tStat`, and confidence intervals. Create a `RDDResults.jsx` component (mirroring the pattern of `IVResults.jsx`) that formats these into an rdrobust-style block: effective N left/right, bandwidth, kernel, polynomial order, coefficient, SE, 95% CI, and p-value. Mount the component in `ModelPlots.jsx` below the scatter plot when `result.type === "RDD"`.
**Invariants:** New component must live in `src/components/modeling/` — not in `src/math/`. Use `C` + `mono` from `src/components/modeling/shared.jsx`. No new math computation — read only from the frozen `EstimationResult`.

---

### Model · 2026-05-25

**Feedback:** "Add an option to estimate without intercept"
**File:** `src/math/LinearEngine.js` (line 128 — `k = xCols.length + 1`), `src/components/modeling/ModelConfiguration.jsx`, `src/services/export/rScript.js`, `src/services/export/pythonScript.js`, `src/services/export/stataScript.js`
**Suggestion:** Add a `noIntercept: boolean` field to the estimation spec. In `LinearEngine.runOLS` (and equivalent paths in `PanelEngine`, `CausalEngine`, `NonLinearEngine`), make the intercept column conditional on `!noIntercept` and adjust `k` accordingly. Thread `noIntercept` through all three export scripts (`- 1` in R formula, `fit_intercept=False` in sklearn/statsmodels, `noconstant` in Stata). Add a checkbox toggle in `ModelConfiguration.jsx`. Validate the new path against `lm(y ~ x - 1)` in `engineValidation.js` before shipping.
**Invariants:** `seType` must still be passed explicitly; df (`n − k`) must be correct when `k` excludes the intercept. Zero React in `src/math/`.

---

### Explore · 2026-05-19

**Feedback:** "Add `filter(regionname %in% c('Spain (Espana)', 'Basque Country (Pais Vasco)'))` for filter option"
**File:** `src/services/exprEvalService.js`, `src/components/wrangling/CleanTab.jsx` (`FilterBuilder` formula mode, ~line 876)
**Suggestion:** In `exprEvalService.js`, pre-process the expression string before evaluation: detect the R-style pattern `col %in% c(...)` with a regex such as `/(\w+)\s+%in%\s+c\(([^)]+)\)/g` and rewrite it to a JS array `.includes()` call (e.g. `["Spain (Espana)","Basque Country (Pais Vasco)"].includes(row["regionname"])`). This is a purely lexical transform at eval time — the `filter` step stores the original `expr` string unchanged, so re-running the pipeline re-applies the transform correctly. Separately, the existing `FilterBuilder` simple-mode `"in"` operator (`op: "in"`) already supports multi-value selection — a tooltip explaining this as the GUI equivalent of `%in%` would reduce the need for the formula path.
**Invariants:** `exprEvalService.js` is pure JS — no React imports. Filter replays on `rawData` via `runner.js` unchanged (the rewrite happens at eval time only).

---

### Spatial · 2026-05-30

**Feedback:** "Allow spatial filters for plots"
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer filter field to `GeoLayerConfig.jsx` — a simple text input accepting a JS predicate expression (reuse `exprEvalService.evalFilter`). Before the layer's rows are passed to `mkGeoLayer` in `geo.js`, apply the filter predicate. This keeps all computation in the browser and does not touch the spatial math engine. Limit filter scope to already-computed spatial rows (post-`SpatialEngine`) — do not re-trigger spatial operations.
**Invariants:** No mutation of source dataset rows. Filter operates on derived spatial rows only. Spatial tab has its own `rows`/`headers` state — do not reach into `WranglingModule`.

---

### Model · 2026-06-06

**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/components/modeling/resultDisplay.jsx` (`ExportBar` / `buildLatex`, ~line 729), `src/math/CausalEngine.js` (`varNames` assembly for RDD result)
**Suggestion:** Two-part fix. (1) In `CausalEngine.js`, assemble RDD `varNames` from the actual column names in the estimation `spec` (the treatment dummy col, running variable col, and interaction) rather than hardcoding generic strings — this fixes the root cause. (2) In `resultDisplay.jsx`, add a `labelOverrides: { [colName]: string }` map in component state; render an ✎ icon next to each coefficient row; clicking opens an inline text input. `buildLatex` substitutes any override before rendering. Persist overrides in the `EstimationResult` under a `displayLabels` field so they survive pinned-model restore.
**Invariants:** Label overrides are presentation-only and must never affect coefficient values, SE, or downstream math. No React in `src/math/`. Use `C` + `mono` for the inline editor styling.

---

## Suggested action order

1. **[BUG · critical] Fix pipeline-dataset coupling in `App.jsx`** — `handleComplete` writes to `tabDsId("clean")` but Model reads from `tabDsId("model")`; pipeline never reaches Model when the two differ. Affects every multi-tab workflow.
2. **[BUG · high] Browser-validate `35f4b5a6`** — a single session confirms or unblocks three independent high-priority items simultaneously: OLS residual plots, frozen diagnostics, and Plot Builder resize/PNG.
3. **[BUG · medium] Browser-validate `5e105529` heatmap** — standalone spatial session; confirm Heatmap layer renders and HTML export works.
4. **[SECURITY] Add Supabase server-side XSS guard** — DB trigger or Edge Function validation on the `feedback` table; client sanitization is bypassable by direct POST.
5. **[BUG · medium] Resilient CSV import** — row-level error boundary with skip/repair modal so a single malformed row doesn't abort a large import.
6. **[FEATURE · medium] No-intercept toggle** — engine-wide spec change; validate against R `lm(y ~ x - 1)` before shipping.
7. **[FEATURE · low] R `%in%` filter rewrite in `exprEvalService.js`** — small lexical transform with high value for researchers copying R filter expressions.
8. **[FEATURE · low] RDD `varNames` fix + summary table** — fix hardcoded generic labels in `CausalEngine.js`, then add `RDDResults.jsx` display panel.
9. **[FEATURE · low] Editable LaTeX labels** — inline ✎ editor in `resultDisplay.jsx`; depends on item 8 for the RDD label root cause.
10. **[FEATURE · low] Spatial plot row-filter in `GeoLayerConfig.jsx`** — self-contained layer filter; route to V4 post-launch.
