# Feedback Analysis Report — 2026-07-13

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **7 open bugs** (5 code-complete pending browser validation · 1 fix-later · 1 sync RLS code-complete)
- **8 open features** (1 already shipped but not marked ✓ · 7 genuine gaps)
- **0 open UX / Design** (all UX items resolved per BugTriage)
- **1 open performance** (large-dataset crash — Fase X4 code-complete, browser benchmark pending)
- **1 open other** (malformed CSV row abort — deferred to post-launch resilient-import pass)

Cross-reference method: ClaudeFB.md items not marked ~~strikethrough~~ or ✓ were matched against BugTriage.md status column and the 2026-06-14 X5 reconciliation table. Items marked "fixed" in BugTriage or "verified fixed" in X5 are excluded. Items marked "code-complete, browser validation pending" remain open. No new ClaudeFB.md batches since 2026-07-04.

**Changes since 2026-07-12 report:**
- No new feedback rows since the 2026-07-04 batch. Open-item set is identical.
- "Add limits" (Calculate · 2026-06-19) re-confirmed already implemented at `CalculateTab.jsx:1586` — ClaudeFB.md still needs a ✓ update (see Features section below).

---

## Bugs

### [high] · Model · 2026-06-14
**Feedback:** `Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.`  
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`  
**Suggestion:** BugTriage marks this code-complete as of `35f4b5a6` (2026-06-14): displayed model now reads from `result.spec` (the frozen estimation snapshot). Browser validation by Franco is the remaining step — add a variable after estimating and confirm plots and BP/DW/JB values do not change; then pin the model, switch estimators, restore the pin, and verify diagnostics reappear correctly.  
**Invariants:** Never derive display state from live variable-selector state after estimation. The frozen `EstimationResult` is the single source of truth for display.

---

### [high] · Model · 2026-06-11
**Feedback:** `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS`  
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`  
**Suggestion:** BugTriage marks this code-complete as of `35f4b5a6` (2026-06-14): all three plot components were already wired; the display was fixed so OLS/WLS diagnostics render from the frozen `result.spec` rather than live sidebar state. Validate in the browser by running an OLS model and confirming all three plots appear in the Diagnostics panel and remain stable when adding/removing variables without re-estimating.  
**Invariants:** Diagnostic plots must read from the immutable `EstimationResult` returned by the engine, never from live `xVars`/`yVar` selector state. `ResidualPlots.jsx` must not import from `src/math/`.

---

### [high] · Model · 2026-06-10
**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`  
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`  
**Suggestion:** BugTriage marks this code-complete as of `35f4b5a6` (2026-06-14): the plot viewport now has a native vertical resize handle with bounded responsive canvas sizing, so PNG captures the user-selected dimensions. Validate: drag the resize handle in ModelingTab, trigger PNG export at the resized dimensions, and confirm no clipping or dark-theme style bleed in the exported image.  
**Invariants:** PlotBuilder uses Observable Plot 0.6 loaded from CDN; resize logic must not break the `ResizeObserver`. Inline styles via `C` constants from `shared.jsx` only — no CSS files.

---

### [medium] · Explore · 2026-05-11
**Feedback:** `The graph does not fit the screen`  
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`  
**Suggestion:** BugTriage marks this code-complete as of `35f4b5a6` (2026-06-14). Validate in the browser: confirm PlotBuilder fills available viewport width in both the standalone Explorer tab and the embedded ModelingTab instance. If sizing still fails, check that the embedding container passes `width: "100%"` inline rather than a fixed pixel value.  
**Invariants:** Container sizing must use `flex: 1` or `width: 100%` inline — no external CSS.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** `Heatmap must be fixed`  
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`  
**Suggestion:** BugTriage marks this code-complete as of `5e105529` (2026-06-14): heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Browser validation required — load a point dataset, select the Heatmap layer type in the Map tab, confirm the gradient renders, adjust opacity, and export HTML. If the layer fails to appear, check that the 5k-point cap is not silently filtering out all rows.  
**Invariants:** Leaflet is a CDN singleton loaded via `src/components/tabs/spatial/shared/leaflet.js` — never import it statically. The 5k-point cap is intentional; do not remove it.

---

### [high] · Sync · 2026-07-06
**Feedback:** `Sharing a project with another user fails with an RLS error ("Share not found or you are not the intended recipient.") even when the recipient email is correct`  
**File:** `src/services/sync/shareEngine.js`, `supabase/migrations/20260706120000_fix_project_shares_email_case.sql`  
**Suggestion:** BugTriage marks this code-complete (migration applied to live DB 2026-07-06): both `share_recipient_read` and `share_blobs_recipient_read` RLS policies now lowercase the JWT email claim; `share_blobs_owner_select` was added for owner self-restore. Browser validation is the remaining step: (1) owner creates a share, (2) recipient with a mixed-case email opens the token link → project imports successfully, (3) owner revokes share → `list()` no longer returns the blob, (4) owner self-restores → succeeds. Confirm via Supabase dashboard that all 7 policies are present on the `shares` table and `storage.objects`.  
**Invariants:** All Supabase egress stays within `src/services/sync/` — never add raw `fetch` to Anthropic API or Supabase storage elsewhere. The `pullShare` error mapping to actionable messages must be preserved.

---

### [medium] · Data · 2026-05-10 (fix-later)
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`  
**File:** `src/DataStudio.jsx` (CSV parse entry point ~line 403), `src/services/data/parsers/` (DuckDB path governs files >10 MB)  
**Suggestion:** When a CSV parse error occurs, surface the offending line number and raw content in an inline warning banner rather than aborting the entire import. Offer two actions: "Skip bad rows and continue" (drops malformed lines, logs skipped count to the session log) or "Cancel". For the DuckDB large-file path, `read_csv_auto` supports `IGNORE_ERRORS=TRUE` — wire that flag behind a user opt-in toggle. For the JS path, wrap per-row parsing in a try/catch and collect failures before completing the import.  
**Invariants:** Non-destructive pipeline invariant — partial data must not be stored silently under the same dataset key. Bad rows must be surfaced and user-acknowledged before the import completes. Never mutate `rawData` in place.

---

## Features

### Model · 2026-05-25 (fix-later)
**Feedback:** `Add an option to estimate without intercept (suppressed constant)`  
**File:** `src/math/LinearEngine.js` (`runOLS`, `runWLS`), `src/components/modeling/ModelConfiguration.jsx`  
**Suggestion:** Add a `noIntercept: boolean` flag to the engine function signatures following the same explicit-argument pattern as `seType`. In `runOLS`/`runWLS`, when `noIntercept=true`, skip prepending the ones column to the design matrix and adjust degrees of freedom to `n - k` (not `n - k - 1`). Expose the toggle as a checkbox in `ModelConfiguration.jsx` under Advanced Options. The flag must propagate through `estimate()` → engine → `EstimationResult.spec` so replication scripts emit the correct syntax (`lm(y ~ x - 1)` in R, `nocons` in Stata).  
**Invariants:** `seType` architecture invariant applies symmetrically — `noIntercept` must be an explicit argument, never inferred internally. Zero React imports in `src/math/`. Validate against R `lm(y ~ x - 1)` to 6 decimal places on coefficients and 4 on SE before shipping.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** `Add for RDD a summary table as in R with summary(rdrobust(...)) — bandwidth, effective N, kernel, polynomial order, LATE`  
**File:** `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/resultDisplay.jsx`  
**Suggestion:** `runSharpRDD` in `CausalEngine.js` already returns `bandwidth`, `nLeft`, `nRight`, `kernel`, and `polyOrder` in the `EstimationResult`. Add a compact summary panel — either inline in `resultDisplay.jsx` or as a new `src/components/modeling/results/RDDSummaryPanel.jsx` — that renders these fields in an `rdrobust`-style header table (effective n, bandwidth, kernel, polynomial order, LATE, robust CI) above the coefficient row. No engine changes are needed; all required data is already present in the result object.  
**Invariants:** Any new display component is pure React/JSX and must not import from `src/math/`. Use `C` constants for all inline styles.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`  
**File:** `src/components/modeling/resultDisplay.jsx`, `src/ReportingModule.jsx`  
**Suggestion:** `resultDisplay.jsx` currently has no label-override mechanism. Add an optional `coefLabels: { [internalName: string]: string }` map to `EstimationResult`. Expose an inline ✎ edit icon next to each coefficient row that toggles a text input, writing back to `coefLabels`. The LaTeX/Stargazer render in `ReportingModule.jsx` reads `coefLabels[name] ?? name`. For RDD, initialize defaults from `result.spec.xVar` / `result.spec.cutoff` rather than the current hardcoded `varNames`. Persist `coefLabels` per pinned model in the `model_buffer` IndexedDB store.  
**Invariants:** `src/math/` files must remain pure JS with no React imports. `coefLabels` must be stored per-result in `EstimationResult`, not globally, so pinned model comparison tables remain independent. Persistence in `indexedDB.js` only — never `localStorage`.

---

### Explore · 2026-05-19 (fix-later)
**Feedback:** `Add filter(regionname %in% c("Spain", "Basque Country")) for filter option — R-style %in% c(...) membership expression syntax not accepted`  
**File:** `src/services/exprEvalService.js`, `src/components/wrangling/CleanTab.jsx`  
**Suggestion:** The FilterBuilder already has an `in`/`nin` operator with `Array.includes` evaluation. Add a pre-processing regex pass in `exprEvalService.js` that detects `<col> %in% c(<csv-values>)` and rewrites it to the internal `{op:"in", col:"...", values:[...]}` node before evaluation. This is a string-level transform — no full parser is needed. Also handle `x %in% 1:N` (integer range) for completeness.  
**Invariants:** Filter steps replay on `rawData` via `runner.js` — the translated filter must serialise to a valid `filter` pipeline step registered in `registry.js`. Expression parsing belongs in `exprEvalService.js`, not inside `runner.js`.

---

### Spatial · 2026-05-30 (fix-later, deferred to V4)
**Feedback:** `Allow spatial filters for plots`  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**Suggestion:** Add a per-layer filter row in `GeoLayerConfig.jsx`: a column selector and a value/expression input that subsets the layer's rows before the geometry render loop. The filter operates on the client-side data array already available in plot state — no DuckDB query or pipeline step is required. Store the filter expression in layer config alongside existing aesthetics (`color`, `size`, etc.).  
**Invariants:** Spatial plot filters must never modify `rawData` or inject pipeline steps. All filtering stays within the plot component's local render state. Inline styles via `C` constants only.

---

### Calculate · 2026-06-19 ⚠️ ALREADY IMPLEMENTED — ClaudeFB.md needs ✓
**Feedback:** `Add limits (mathematical limit as x → a) to the Calculate tab`  
**File:** `src/components/tabs/CalculateTab.jsx:1586`  
**Status:** Feature is already shipped. The "lim Limit" tab exists in the six-tool strip at line 1586 (`"limit"` in the `activeTool` state at line 1170), the limit UI renders at line 2161, and the `limit` function is imported at line 18 from the math engine.  
**Suggestion:** Franco should open the Calculate tab, confirm the "lim Limit" tool evaluates `lim(x → 0) sin(x)/x = 1` and `lim(x → ∞) 1/x = 0` correctly, then mark the ClaudeFB.md entry ✓. No code change needed.  
**Invariants:** N/A.

---

### Calculate · 2026-07-04
**Feedback:** `Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)`  
**File:** `src/components/calculate/workbench/ViewControls.jsx`, `src/components/calculate/workbench/operations.js`, `src/components/calculate/workbench/WorkbenchCanvas.jsx`  
**Suggestion:** A `+ horizontal` button already exists at `ViewControls.jsx:95` (writes to `view.refLines`, rendered by `WorkbenchCanvas.jsx:146–167`). The gap is that workspace scalar variables (e.g. `c = 5`, `MC = alpha * w / A`) cannot be directly selected as horizontal reference lines without typing numeric values. Fix: in `ViewControls.jsx`, add a `<select>` dropdown next to `+ horizontal` that lists the current scalar workspace variables; on selection, set `refLine.value` to the computed scalar. Additionally, in `operations.js`, detect when `sampleCurve` is called with an expression that has no free variables (pure constant), evaluate once, and emit a single horizontal line segment across the full `xRange` so that `y = MC` (a computed expression) can be added directly as an equation curve rather than only as a reference line.  
**Invariants:** Inline styles via `C` constants from `shared.jsx`. No new CDN dependency. Constant-expression curves must respect the multi-curve palette logic in `WorkbenchCanvas.jsx:186`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`  
**File:** `src/DataStudio.jsx` (large-file parse branch ~line 403), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`  
**Suggestion:** Fase X4 is code-complete per the X5 reconciliation table — large CSV/TSV files are converted to Parquet via DuckDB and persisted to OPFS; tab transitions no longer re-materialise the full JS object array. Browser benchmark is the remaining action: load the 21 MB / 900k-row CSV fixture, navigate between all tabs (Data → Clean → Explore → Model → Simulate), and confirm no OOM crash and tab-transition latency under 2 seconds. Also verify the `⚡ DuckDB` badge appears in the wrangling header and that model statistics report the full row count — not the 500-row preview cap (the separate DuckDB n=500 bug was fixed in `c9c5bd3e`). If COOP/COEP headers are not yet active on Vercel, enabling `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` in `vercel.json` unlocks `SharedArrayBuffer` and parallel DuckDB execution (CLAUDE.md §DuckDB rule 4).  
**Invariants:** Privacy-first constraint — computation must remain entirely client-side; no server-side data egress. Display is always capped at 200–500 rows for UI rendering, but all pipeline operations must run on the full dataset. Never truncate `rawData` before passing it to `runner.js` or `duckdbRunner.js`.

---

## Suggested action order

1. **Browser-validate the `35f4b5a6` + `5e105529` bundle** — four open bugs in one session: frozen diagnostics display, OLS residual plots, Plot Builder resize/PNG, Explore graph fit, and spatial heatmap. Each only needs a run-and-confirm step.
2. **Browser-validate the sync share RLS round-trip** — confirm create → mixed-case-email import → revoke → owner-restore all succeed with the 2026-07-06 migration applied.
3. **Mark Calculate "Add limits" ✓ in ClaudeFB.md** — feature ships at `CalculateTab.jsx:1586`; one-line confirmation + ✓ close.
4. **Browser-benchmark Fase X4 large-dataset performance** — 900k-row CSV, confirm sub-2 s tab transitions, no OOM; the longest-standing open performance item.
5. **No-intercept regression toggle** — engine changes in `LinearEngine.js` + R validation; broad impact across all OLS/WLS/RDD/2SLS estimation paths.
6. **Resilient CSV import** — malformed-row skip/repair UX in `DataStudio.jsx`; unblocks users with messy real-world data without a full re-download.
7. **Plot constants in Workbench** — small extension to `ViewControls.jsx` to wire workspace scalar variables as selectable reference lines; `+ horizontal` button already exists.
8. **Editable LaTeX coefficient labels** — display-layer only in `resultDisplay.jsx`; no engine changes required.
9. **RDD `rdrobust`-style summary panel** — `EstimationResult` already has the data; pure UI addition, new `RDDSummaryPanel.jsx`.
10. **R `%in% c(...)` filter syntax** — pre-processing pass in `exprEvalService.js`; low-risk, satisfies power-user cleaning workflows.
11. **Spatial plot layer filters** — deferred to V4 per BugTriage; implement as local render-state filter in `GeoLayerConfig.jsx`.
