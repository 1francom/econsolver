# Feedback Analysis Report — 2026-06-06

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **6 open bugs** (2 needs-repro · 3 open · 1 pending) · **15 open features** · **0 open UX** · **1 open performance** · **1 open other**
- Cross-referenced ClaudeFB.md against BugTriage.md. Items marked `fixed` in BugTriage are excluded even if ClaudeFB.md still shows them without strikethrough.

---

## Bugs

### critical (needs-repro) · Model · 2026-05-21
**Feedback:** `URGENT There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible`
**File:** `src/math/LinearEngine.js:80` (`pValue` function), `src/math/EstimationResult.js:260` (result assembly)
**Suggestion:** BugTriage audit found no code-level desync; the likely cause is a UI column-ordering confusion — the coefficient table layout is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig` and the user may have read β̂ = 0.49 as the t-stat. Before writing any fix, ask Franco for a screenshot and the estimator name. If a real desync is confirmed, add a post-assembly assertion in `EstimationResult.js` that throws when `|t| > 0.1` but `p > 0.5` to surface the bug reproducibly.
**Invariants:** `src/math/` must have zero React imports; any assertion must not import UI code.

---

### high (open) · Report / AI · 2026-05-25
**Feedback:** `AI does not recognize the pipeline steps in the Script generation`
**File:** `src/ReportingModule.jsx`, `src/services/AI/sessionSnapshot.js`, `src/services/AI/Prompts/index.js:661` (`UNIFIED_SCRIPT_PROMPT`)
**Suggestion:** CLAUDE.md item 5b describes this as "VALIDATION PENDING 2026-05-30" — the session snapshot plumbing is implemented but not yet verified. The fix path is: (1) confirm `buildSessionSnapshot` is actually called and its output is non-empty in `ReportingModule`'s `useMemo`; (2) verify the snapshot is threaded into `AIUnifiedScript._buildModelScript` via the `dataLoadOpts`/`snapshot` config key; (3) check that `UNIFIED_SCRIPT_PROMPT` rule 8 (walk pipeline in order) reaches the model. If the snapshot block is empty or omitted, trace back to `WranglingModule`'s `onReady`/`onComplete` payloads and confirm `pipeline` + `loadOpts` are populated before the Report tab renders.
**Invariants:** All Anthropic calls must go through `AIService.js`. Do not add raw `fetch` to Anthropic in `ReportingModule.jsx`.

---

### high (open) · Model · 2026-05-28
**Feedback:** `Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators`
**File:** `src/components/modeling/EstimatorSidebar.jsx:28` (CS DiD entry exists), `src/math/CallawayEngine.js` (engine exists), `src/math/NonLinearEngine.js` (`runSunAbraham`), `src/components/ModelingTab.jsx:2476` (CallawayCS result block)
**Suggestion:** The math engines already exist: `runSunAbraham` in `NonLinearEngine.js` and `runCallawayCS` in `CallawayEngine.js`. The EstimatorSidebar already has a "CS DiD" entry. The outstanding work is wiring: (1) verify `CallawayCS` is actually dispatched in `ModelingTab.estimate()`; (2) add a "Sun-Abraham (SA21)" estimator entry in the sidebar beside or replacing the generic "Event Study" TWFE; (3) add a clear deprecation notice on the generic TWFE event study option explaining it is biased under staggered treatment. The existing EventStudy (TWFE) should not be removed — it is useful for balanced treatment timing.
**Invariants:** New estimator entries in `EstimatorSidebar.jsx` must use the `C` color constants; `src/math/` engines must stay React-free.

---

### high (pending) · Data · 2026-05-13
**Feedback:** `.dta files are wrong loaded and readed`
**File:** `src/services/data/parsers/stata.js`
**Suggestion:** A related bug (format-119 variable name stride fixed 2026-06-03) was closed, but BugTriage still shows this earlier entry as "pending". Check whether the 2026-06-01 `.dta` fix (stride 33→129) also resolves the original 2026-05-13 complaint, then close this ticket if so. If not, add a test fixture (small anonymised `.dta` in `src/math/__validation__/`) and step through `stata.js` column-parsing logic against it, paying attention to the variable-name buffer and value-label mappings for both format 117/118 and 119.
**Invariants:** Parser lives in `src/services/data/parsers/`; must not import React or pipeline code.

---

### medium (needs-repro) · Calculate · 2026-06-02
**Feedback:** `crash latex export`
**File:** `src/ReportingModule.jsx:272` (`latex` useMemo), `src/services/export/latexTable.js` (`buildStargazer`)
**Suggestion:** BugTriage notes the code audit found no crash path and marked it "possibly transient". Reproduce by: (1) pinning at least one model; (2) switching to the Report tab and triggering the LaTeX output. If `buildStargazer` crashes, the most likely culprits are `null`/`undefined` coefficient arrays when a non-OLS result (e.g. EventStudy, SC) is pinned — `buildStargazer` may assume a flat `beta`/`se` array. Add a guard that skips or stubs entries where `result.beta` is absent. If it cannot be reproduced, close the ticket.
**Invariants:** Export functions in `src/services/export/` must be pure JS with no React imports.

---

### medium (open) · Data · 2026-06-05
**Feedback:** `Don't round the numbers when inserting, deleting or overwriting`
**File:** `src/App.jsx` (cell-edit pathway — `editCell`/`onCellEdit` match found here), `src/ExplorerModule.jsx` (data table render, `toFixed` calls at lines 86, 229, 334)
**Suggestion:** When a numeric cell is committed in the edit flow (`App.jsx`), the value is likely passed through `parseFloat` + `toFixed` before being written back. Fix: store raw string input until pipeline execution; only convert to number in `runner.js` type casting, not at edit time. Separately, ensure the data-table renderer uses the raw stored value (not a re-formatted copy) when displaying the just-edited cell.
**Invariants:** Pipeline must not mutate `rawData` in place; cell edits should be expressed as a pipeline `recode`/`type_cast` step or a direct `rawData` override, not an in-place mutation. Persistence via `indexedDB.js`, never `localStorage`.

---

## Features

### Model · 2026-05-11
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js:28` (`andersonRubinCI` already implemented), `src/components/modeling/results/IVResults.jsx:59` (already rendered when `second.arCI` is set)
**Suggestion:** The math and the display block already exist. The user may not be seeing the CI because `arCI` is only populated for the single-endogenous-regressor case (`CausalEngine.js:203`). Check whether the UI block in `IVResults.jsx` is rendered in the current prod build. If it is already exposed, add a short label next to the CI output explaining it is weak-IV-robust (Anderson-Rubin). If it is hidden or missing, trace why `second.arCI` is null in the result passed to `IVResults`.
**Invariants:** `src/math/CausalEngine.js` must remain React-free.

---

### Model · 2026-05-11
**Feedback:** `Add multiple statistics tests`
**File:** `src/math/SampleTests.js`, `src/components/tabs/statsim/SampleTestPanel.jsx`
**Suggestion:** `SampleTests.js` has one-sample mean t, variance χ², and generic parameter t/z. "Multiple statistics tests" likely means adding: Kolmogorov-Smirnov (one- and two-sample), Mann-Whitney U (two-sample non-parametric), F-test for equality of variances, and optionally Granger causality. Add the math in `SampleTests.js` (pure JS) and wire new test-type options in `SampleTestPanel.jsx`. Keep each test self-contained with its own input fields shown/hidden via a test-type selector.
**Invariants:** `src/math/SampleTests.js` must have zero React imports.

---

### Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx` (or a new `RDDResults.jsx`)
**Suggestion:** Add a collapsible "rdrobust-style summary" section in the RDD results area that replicates the canonical R output: bandwidth selection method, kernel, polynomial order, effective observations left/right, bias-corrected bandwidth, and the main `Coef / SE / t / p` row. Bandwidth and kernel are already in `result.bandwidth` / `result.kernel` from `CausalEngine`. Model the layout on the existing `IVResults.jsx` pattern. No new math needed.
**Invariants:** Display-only change; `src/math/` must not change.

---

### Model · 2026-05-25
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/math/LinearEngine.js:128` (intercept column prepended unconditionally), `src/components/ModelingTab.jsx` (model config), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add a `noIntercept: boolean` flag to the engine call signature. In `LinearEngine.runOLS` / `runWLS`, skip prepending the ones column when `noIntercept=true`; adjust `df = n - k` accordingly and remove the intercept F-stat. Expose a checkbox in `ModelConfiguration.jsx` labeled "No intercept (suppress constant)". Guard the DuckDB SQL dispatch path to fall back to JS when `noIntercept=true` (the SQL path always includes an implicit constant).
**Invariants:** SE type must still be passed explicitly; no hardcoding inside the engine. DuckDB path must not silently ignore `noIntercept`.

---

### Model · 2026-06-04
**Feedback:** `Persistence across sessions of pinned models`
**File:** `src/services/Persistence/indexedDB.js`, `src/components/ModelingTab.jsx` (pinnedModels state)
**Suggestion:** `indexedDB.js` already has `saveCoachChats`/`loadCoachChats` as a per-project store pattern (v6 schema). Add `savePinnedModels(pid, models)` and `loadPinnedModels(pid)` functions using a new `pinned_models` object store (add in the `onupgradeneeded` block, bump DB version to v7). In `ModelingTab`, call `savePinnedModels` on every `setPinnedModels` update and call `loadPinnedModels(pid)` in a `useEffect` on mount. Serialise each pinned model as JSON; strip non-serialisable large arrays (`resid`, `Yhat`) if needed to keep storage lean.
**Invariants:** Persistence must use `indexedDB.js`, never `localStorage`. `src/math/` must not be touched.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/shared/constants.js`
**Suggestion:** The existing `GridSection.jsx` (analyze tab) already generates rectangular and H3 grid geometry as WKT column output saved to the dataset. In the geo-plot tab, add a "Grid overlay" layer type in `GeoLayerConfig.jsx` that accepts a WKT column as input and renders it as a semi-transparent fill via the existing `wktToLeaflet` / `parseWktRings` path in `wkt.js`. The user first runs GridSection to generate the grid column, then selects it as a plot layer — no new math needed.
**Invariants:** Inline styles only via `C` object; no CSS files.

---

### Spatial · 2026-05-30
**Feedback:** `Geometry transformations and show EPSG`
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx:12–13` (only two EPSG presets hardcoded), `src/components/tabs/spatial/shared/crs.js` (`PRESET_CRS`)
**Suggestion:** Extend the CRS selector from a two-item dropdown to a free-text input with a searchable preset list (EPSG:4326, 3857, 32618–32638 UTM, 25832, 31468 — extend `PRESET_CRS` in `crs.js`). Show the detected input CRS next to the column selector by parsing the first non-null WKT value: if coordinates fall in ±180/±90 range, suggest EPSG:4326; otherwise suggest projected. Display the detected CRS as a read-only badge.
**Invariants:** `crs.js` is plain JS in `src/components/tabs/spatial/shared/`; React imports are allowed in the section component but not in `crs.js`.

---

### Spatial · 2026-05-30
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a "Filter rows" collapsible inside `GeoLayerConfig.jsx` that applies client-side to the layer's data before rendering. Reuse the condition evaluation logic from `CleanTab.jsx:759` (`evaluateCondition`) — extract that pure function into `src/components/wrangling/utils.js` so it can be shared without importing the full `CleanTab`. This is a display-only filter local to the plot layer, not a pipeline step.
**Invariants:** Do not add a pipeline `filter` step here — this must be purely a render-time filter. Pipeline invariant (replay on `rawData`) is not affected.

---

### Spatial · 2026-06-03
**Feedback:** `Add geometry to plot points, not just lat and lon`
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/geo.js`, `src/components/tabs/spatial/shared/wkt.js`
**Suggestion:** In `GeoLayerConfig`, add a "Geometry column (WKT)" input alongside the existing lat/lon selectors. When a WKT column is provided, render polygon/line geometries via `parseWktRings` on the SVG canvas (`GeoPlotCanvas.jsx`) instead of point markers. Auto-detect layer type (point vs polygon vs line) from the first WKT value using `looksLikeWktValue` and `isGeometryHeader` from `src/components/tabs/spatial/shared/guess.js`.
**Invariants:** Inline styles via `C`; no external UI libraries.

---

### Spatial · 2026-06-05
**Feedback:** `Allow for colour selection with the "color by" option, useful for scales also`
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/shared/color.js` (`buildColorScale`, `CAT_PALETTE`)
**Suggestion:** In `GeoLayerConfig`, add a color scheme picker (palette swatches + a sequential / diverging / categorical toggle) shown when "Color by" is set. Pass the selected scheme name to `buildColorScale` in `color.js`. For continuous variables, add min/max override inputs to fix the scale domain. Render a small inline legend swatch row to preview the ramp. The `CAT_PALETTE` and `buildColorScale` exports in `color.js` provide the primitives.
**Invariants:** Inline styles only via `C`; no Tailwind or external CSS.

---

### Explore · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/components/wrangling/CleanTab.jsx:598` (FilterBuilder, `in list` op already exists at line 598)
**Suggestion:** The "in list" operator already evaluates correctly (`CleanTab.jsx:759`). The UX gap is paste-friendliness: users want to paste an R-style `c("val1", "val2")` string. In the multi-value input area of `FilterBuilder` (around line 689), add a "Paste R vector" affordance — a small `<textarea>` that strips `c(...)` syntax, splits on commas, trims quotes/whitespace, and appends values to the current `values` array. No changes to `runner.js` needed.
**Invariants:** None — purely UI change in `CleanTab.jsx`.

---

### Data · 2026-06-01
**Feedback:** `Add an option to edit/assign column names directly in the Data Viewer`
**File:** `src/ExplorerModule.jsx` (data table header row), `src/pipeline/runner.js` (`rename` step type already exists)
**Suggestion:** Make column header cells in the data viewer double-click-editable. On double-click, replace the header text with an `<input>` pre-filled with the current name. On blur or Enter, dispatch a `rename` pipeline step (`{type: "rename", from: oldName, to: newName}`) which is already implemented in `runner.js`. This keeps the non-destructive pipeline invariant intact.
**Invariants:** Pipeline must replay on `rawData` (runner.js); never mutate `rawData` in place. Use `indexedDB.js` for persistence.

---

### Simulate · 2026-05-18
**Feedback:** `Add resampling, bootstrapping, sampling to the mean`
**File:** `src/components/tabs/statsim/StatWorkspace.jsx:704` (bootstrap section, already imports `bootstrapMean`, `bootstrapStatistic` from `src/math/Resampling.js`)
**Suggestion:** `StatWorkspace.jsx` already imports and uses `bootstrapMean`, `bootstrapStatistic`, and `permutationTwoSampleMean`, and the Resampling section has a Bootstrap mode (line 1054). This feature appears already implemented. Verify whether the "Resampling" collapsible is visible and functional in prod. If stale, mark the ClaudeFB.md item as resolved. If "sampling to the mean" as a specific sampling distribution workflow is still missing, add a mode that draws N samples of size k, computes the sample mean for each, and plots the resulting distribution.
**Invariants:** Resampling math must live in `src/math/Resampling.js` (pure JS, no React).

---

### Simulate · 2026-06-02
**Feedback:** `You could add different types of quantiles like in R, but it might not be necessary`
**File:** `src/components/tabs/statsim/StatWorkspace.jsx:285` (quantile mode for distributions)
**Suggestion:** The distribution panel already has a "Quantile" mode for theoretical distributions. For empirical column quantiles, add a "Quantile type" dropdown (types 1–7 matching R's `quantile(..., type=N)`) next to percentile inputs. Implement type 1 (inverse empirical CDF) and type 7 (linear interpolation, R default) at minimum. Add the math as a small utility in `src/math/` or inline in the component.
**Invariants:** Any standalone math utility added to `src/math/` must have zero React imports.

---

### Calculate · 2026-06-03
**Feedback:** `Add a "show steps button" for integrals, derivatives and optimizations`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Symbolic step-by-step derivation in pure JS is limited; the most practical path is a "Show steps" button that calls `AIService.callClaude` with a prompt requesting a LaTeX step-by-step derivation of the current expression. Display the response in a collapsible panel below the result. A fully offline approach using math.js's symbolic differentiation is feasible only for polynomials and would need substantial work for integrals and optimizations.
**Invariants:** AI call must go through `src/services/AI/AIService.js` (`callClaude`). Never add raw `fetch` to the Anthropic API in `CalculateTab.jsx`.

---

## Performance

### critical · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (file load path), `src/pipeline/duckdbRunner.js`, `src/services/data/duckdb.js`
**Suggestion:** The CLAUDE.md DuckDB performance strategy (rules 1–7) is the agreed remediation. Most impactful immediate steps: (1) **Auto-convert large CSV to Parquet on first load** — after DuckDB ingests a CSV >10 MB, run `COPY (SELECT * FROM t) TO 'data.parquet' (FORMAT PARQUET)` and swap the table reference; (2) **OPFS persistence** — cache the Parquet in OPFS so re-opening skips re-import; (3) **Verify COOP/COEP headers in `vercel.json`** (`Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Embedder-Policy: require-corp`) to unlock `SharedArrayBuffer` for threaded DuckDB-Wasm.
**Invariants:** Dataset must never leave the browser (privacy-first). Full dataset must be used for pipeline steps and model estimation — display truncation at 200–500 rows only.

---

## Other

### Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated)`
**File:** `src/DataStudio.jsx:42` (`parseCSV`), `src/services/data/duckdb.js` (large-file DuckDB ingest path)
**Suggestion:** Line 1583 is likely a DuckDB ingest error for a file routed through the large-file path. The truncated value `2425.47...` suggests a numeric field with unexpected trailing text (e.g. "2425.47 (est.)"). Fix: in the DuckDB CSV loader, set `IGNORE_ERRORS=TRUE` for initial ingest and surface a warning banner listing error rows and the raw offending values. Alternatively, fall back to the JS `parseCSV` path for the specific row. Reproduce with the World Bank GDP CSV (row context matches World Bank format).
**Invariants:** None specific.

---

## Suggested action order

1. **[critical] p-value display confusion** — ask Franco for a screenshot; if it is a column-label confusion, add column header tooltips in the coefficient table (zero code risk, high user-trust impact).
2. **[high] Report AI pipeline steps** — validate CLAUDE.md pending item 5b on Vercel with a `;`-delimited CSV, then close the ticket or patch `ReportingModule`'s snapshot wiring.
3. **[high] Event study Sun-Abraham/Callaway-Santana wiring** — `CallawayEngine.js` and `runSunAbraham` already exist; verify `CallawayCS` dispatch in `ModelingTab.estimate()` and confirm the sidebar entry renders correctly — this is a 1–2 hour integration task.
4. **[medium] Numbers rounded in data viewer** — trace numeric coercion in the cell-edit path in `App.jsx`; store raw string until pipeline cast.
5. **[high] Pinned model persistence** — add `savePinnedModels`/`loadPinnedModels` to `indexedDB.js` (v7 schema bump); high user-visible value, low risk.
6. **[critical] Large CSV crashes** — implement Parquet auto-conversion and OPFS caching in `DataStudio.jsx` + `duckdb.js`; verify COOP/COEP headers in `vercel.json`.
