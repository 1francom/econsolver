# Feedback Analysis Report — 2026-07-03

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **6 open bugs** · **7 open features** · **0 open UX** · **1 open performance** · **0 open other**
- 4 of the 6 bugs are marked `code-complete` in BugTriage but **browser validation is still pending** (Franco must confirm).
- 1 critical bug (DuckDB n=500) is `open` in BugTriage with no fix in place — highest priority.
- All XSS probes, `.rds` parse errors, and items reconciled in X5 as `verified fixed` are excluded here.

---

## Bugs

---

### critical · Model / DuckDB · 2026-07-02
**Feedback:** When using DuckDB, the number of observations reported in model output uses the 500-row browser preview, not the full raw row count — affects n, degrees of freedom, t-statistics, F-statistics, and p-values.

**File:** `src/components/ModelingTab.jsx` (lines 826–934), `src/pipeline/duckdbRunner.js`, `src/services/data/duckdb.js`

**Suggestion:** `ModelingTab.jsx:934` already computes `rowCount = cleanedData?._duckdb?.rowCount ?? 0` and passes `n: rowCount > rows.length ? rowCount : rows.length`. However, the ternary is fragile — if `rowCount` is 0 (DuckDB not yet initialised when `estimate()` fires) it silently falls through to `rows.length` (500-row preview cap). The fix is to assert `rowCount` is non-zero before dispatching any SQL path, and to throw/abort rather than fall back to the preview length for the `n` parameter specifically. Also audit every JS-fallback branch in `estimate()` to ensure it reads `cleanedData._duckdb.rowCount` rather than `rows.length` when a DuckDB table is present.

**Invariants:** Math result `n` must equal full dataset row count — never the display-truncated preview. DuckDB is the data boundary (DuckDB performance rule #1); JS `rows` is only for display, not estimation.

---

### high · Spatial / Map · 2026-06-13
**Feedback:** Heatmap must be fixed.

**File:** `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`

**Suggestion:** BugTriage records this as `code-complete` (`5e105529` — Leaflet KDE heatmap layer with palette/opacity controls, legend, HTML export, 5k-point cap restored). **Action needed: browser-validate on Vercel.** Load a point dataset, add a Heatmap layer in the Map tab, confirm the KDE surface renders, palette/opacity controls respond, legend appears, and HTML export downloads correctly. Mark `~~fixed~~` in ClaudeFB.md once confirmed.

**Invariants:** none.

---

### high · Model / Diagnostics · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.

**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx` (lines 2563–2565)

**Suggestion:** BugTriage records this as `code-complete` (`35f4b5a6` — `ResidualVsFitted` and `QQPlot` components exist and are wired; frozen-result-state regression fixed so they render from the estimation's `result.spec`). **Action needed: browser-validate.** Run an OLS model, verify the Residuals / Q-Q tabs appear and render. Also check that editing the variable selector after estimation does not clear the plots. Mark `~~fixed~~` in ClaudeFB.md once confirmed.

**Invariants:** none.

---

### medium · Model / Plot Builder · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.

**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** BugTriage records this as `code-complete` (`35f4b5a6` — native vertical resize with bounded responsive canvas sizing). **Action needed: browser-validate.** Open the Plot Builder in the Model tab, drag the resize handle, confirm canvas follows, then export PNG — check that the exported image dimensions match the visible canvas and that axis labels / tick marks are not cut off. Mark `~~fixed~~` in ClaudeFB.md once confirmed.

**Invariants:** none.

---

### medium · Model / Diagnostics · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.

**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`

**Suggestion:** BugTriage records this as `code-complete` (`35f4b5a6` — OLS/WLS display frozen to `result.spec`, so sidebar edits cannot mutate displayed model). **Action needed: browser-validate.** Estimate an OLS model, then add/remove regressors in the variable panel without re-estimating — confirm coefficient plots and diagnostic output remain unchanged. Then pin the model, switch to another, restore the pin — confirm the frozen display is correct. Mark `~~fixed~~` in ClaudeFB.md once confirmed.

**Invariants:** Result display must read from the immutable estimation spec, not from live UI state.

---

### medium · Data / CSV · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...

**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/`

**Suggestion:** BugTriage marks this `fix-later` — "Resilient-import UX after launch." A single malformed row currently aborts the entire import. The fix is to wrap the CSV parse in a try/catch-per-row strategy (or pass `skipErrors: true` to the CSV library), collect bad row indices, and surface a recovery modal showing the first N bad rows with options to skip them or abort. This is a UX boundary (user input) so validation here is appropriate. Since all CSV parsing funnels through `DataStudio.jsx:parseFile`, the change is localised.

**Invariants:** Any skip/repair preview must not mutate `rawData` — it should produce a cleaned copy that is loaded as a new pipeline step (`inject_column` or a new `drop` targeting the bad rows by index).

---

## Features

---

### Clean / Filter expression language · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option.

**File:** `src/components/wrangling/CleanTab.jsx`, `src/workers/exprEval.worker.js`

**Suggestion:** BugTriage marks this `fix-later`. The filter expression UI currently evaluates JS-style predicates. The simplest fix is to add a pre-processing step in the filter evaluator that rewrites R-style `%in% c("a","b")` syntax to JS `["a","b"].includes(...)` before the expression is passed to `exprEval.worker.js`. No engine changes needed. Add a hint in the filter builder UI showing both syntaxes.

**Invariants:** Filter expressions are evaluated in `exprEval.worker.js` — keep all expression rewriting in the worker or in `CleanTab.jsx` before the step is committed to the pipeline. Never add a parser to `runner.js` (it must stay pure step-replay).

---

### Model / RDD output · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression.

**File:** `src/components/ModelingTab.jsx` (Sharp RDD result rendering, ~line 3340), `src/math/CausalEngine.js` (`runSharpRDD` return value)

**Suggestion:** BugTriage marks this `fix-later` (presentation enhancement). The compact `rdrobust` summary layout includes: call line, bandwidth (h), effective observations left/right, LATE point estimate, robust confidence interval, and first-stage F. All of these are already present in the `runSharpRDD` result object — this is purely a UI layout addition. Add a `<SharpRDSummaryTable>` sub-component inside `ModelingTab.jsx`'s RDD result panel (or a new `src/components/modeling/results/SharpRDDResults.jsx`) that renders the table from the existing result fields. No engine changes required.

**Invariants:** `src/math/CausalEngine.js` result contract must not change — only the UI display layer adds the table. No React in math files.

---

### Model / Estimation · 2026-05-25
**Feedback:** Add an option to estimate without intercept.

**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`, `src/math/CausalEngine.js`, `src/math/NonLinearEngine.js`

**Suggestion:** BugTriage marks this `fix-later` — "engine-wide spec/export compatibility." Add a `noIntercept: boolean` field to the estimation spec and thread it through every engine's design-matrix construction step (removing the leading column of ones when true). Replication scripts (`rScript.js`, `pythonScript.js`, `stataScript.js`) must emit `+ 0` / `- 1` / `noconstant` flags. This is a cross-cutting change touching ~7 files. The DuckDB suff-stats builders (`duckdbOLS`, `duckdbWithin`, etc.) also need a branch since their `X'X` SQL templates prepend an intercept column unconditionally.

**Invariants:** `seType` must still be passed explicitly — the no-intercept flag only affects design matrix construction, not SE computation. Persist `noIntercept` in IndexedDB via the model spec; never in `localStorage`.

---

### Spatial / Plot filters · 2026-05-30
**Feedback:** Allow spatial filters for plots.

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** BugTriage marks this `fix-later` (routed to V4). The minimal V4 scope is a column-value filter dropdown in `GeoLayerConfig.jsx` (e.g. filter by a categorical region column). A full spatial predicate filter (point-in-polygon) would call `SpatialEngine.spatialJoin` on the filtered rows. Scope the V4 delivery to the column-value variant only; the polygon-intersection variant can follow.

**Invariants:** Spatial operations must not mutate `rawData`; filtered views feed through the pipeline (`runner.js`). No React imports in `SpatialEngine.js`.

---

### Model / LaTeX output · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names.

**File:** `src/EconometricsEngine.js:373` (hardcoded `varNames` array), `src/components/modeling/resultDisplay.jsx`, `src/ReportingModule.jsx`

**Suggestion:** BugTriage marks this `fix-later`. The root cause is `EconometricsEngine.js:373` hardcoding `["(Intercept)", "D (treatment)", "running − c", "D × (running − c)", ...controls]`. The primary fix is to derive these names from `spec.runningVar` and the treatment column name in `CausalEngine.js:runSharpRDD` at result construction time. The secondary (post-launch) UX enhancement is an editable label map in `resultDisplay.jsx` stored per model in the model buffer, allowing any coefficient name to be overridden for Stargazer export.

**Invariants:** Label overrides are display metadata only — they must not affect estimation or SE computation. Store them in IndexedDB (`model_buffer`), never in `localStorage`.

---

### Calculate / Limits · 2026-06-19
**Feedback:** Add limits.

**File:** `src/components/tabs/CalculateTab.jsx`

**Suggestion:** No existing limits implementation found. Add a `lim(expr, var, val)` function to the expression evaluator (`exprEval.worker.js`) using numerical one-sided limits: evaluate `expr` at `val ± ε` for decreasing ε sequences and check convergence. For simple 0/0 or ∞/∞ indeterminate forms, apply L'Hôpital's rule symbolically if the embedded CAS supports it, or fall back to numerical. Display `lim_{x→a} f(x) = L` rendered via KaTeX alongside the step-by-step workbench. Scope the symbolic path to rational/trigonometric/exponential cases first.

**Invariants:** All expression evaluation runs in `exprEval.worker.js` — no inline `eval()`. KaTeX rendering uses `trust: false` (existing invariant).

---

### Clean / UX safety · 2026-07-01
**Feedback:** Add confirmation buttons for all critical functions like clearing all the datasets, clearing all the pipeline, signing out, etc.

**File:** `src/components/workspace/WorkspaceBar.jsx:202` (has `window.confirm` for "Clear ALL local data"), `src/App.jsx:1826` (`deleteProject`), `src/WranglingModule.jsx` (pipeline clear)

**Suggestion:** `WorkspaceBar.jsx:202` already gates "Clear ALL local data" behind `window.confirm`. Gaps are: (1) `deleteProject` in `App.jsx:1826` — add a confirm before the call; (2) pipeline clear in `WranglingModule.jsx` — confirm before `replacePipeline([])`; (3) sign-out is low-risk but a confirm is reasonable. Implement a reusable `<ConfirmModal>` component (inline styles, `C` palette) stored at `src/components/workspace/ConfirmModal.jsx`, and replace all `window.confirm` calls with it for consistent UX.

**Invariants:** Inline styles only via the `C` object from `shared.jsx`. The modal must not block the React render thread — use conditional rendering in the parent, not a blocking dialog.

---

## Suggested action order

1. **[critical] Fix DuckDB n=500 model corruption** — `ModelingTab.jsx:934` ternary falls through to `rows.length` when `rowCount === 0`; assert non-zero before SQL dispatch and abort rather than silently use the preview length.
2. **[high] Browser-validate 4 code-complete items** — heatmap (`5e105529`), residual plots + diagnostics freeze + Plot Builder resize (`35f4b5a6`) — all need Franco's sign-off before ClaudeFB.md entries can be struck through.
3. **[medium] Browser-validate Fase X4 large-file performance** — 21MB / 900k-row fixture on Vercel; confirm no crash, DuckDB badge shows correct count.
4. **[medium] Resilient CSV import** — wrap CSV parse in `DataStudio.jsx` with per-row error collection + recovery modal; scope is local to `parseFile`.
5. **[medium] Confirmation dialogs for destructive actions** — add `<ConfirmModal>` at `src/components/workspace/ConfirmModal.jsx`; wire to `deleteProject`, pipeline clear, and replace `window.confirm` calls.
6. **[low] Fix hardcoded RDD variable names** — derive from spec in `CausalEngine.js:runSharpRDD`; unblocks editable LaTeX labels downstream.
7. **[low] R `%in%` filter syntax** — pre-processing rewrite in the filter evaluator; low-risk, high user-facing polish.
8. **[feature] Calculator limits** — `lim()` in `exprEval.worker.js` with numerical convergence + KaTeX display.
9. **[feature] RDD rdrobust-style summary table** — UI-only; all data already in result object; no engine changes.
10. **[feature/post-launch] Estimate without intercept, spatial plot filters, editable LaTeX labels** — cross-cutting or V4; defer until items 1–9 above are closed.
