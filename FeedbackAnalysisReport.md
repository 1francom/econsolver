# Feedback Analysis Report — 2026-08-17

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **15 open bugs** · **12 open features** · **3 open UX** · **1 open performance** · **1 open other**
- **4 code-complete items** awaiting browser validation by Franco (not counted as open)
- **6 items in ClaudeFB.md** are confirmed fixed in BugTriage but not yet struck through — ClaudeFB.md housekeeping needed

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work  
**File:** `src/services/export/rScript.js:1432–1476`, `src/math/CallawayEngine.js:290–315`  
**Suggestion:** `CallawayEngine.js` returns `{ type:"CallawayCS", attgt, aggregations, … }` — it does **not** include `yVar`, `treatCol`, `entityCol`, `timeCol`, or `xVars` in the result. `rScript.js:1435` destructures those fields directly from `model`, so they are all `undefined`, producing an emitted script with no column names. Fix: in `_runEstimation` (ModelingTab.jsx ~line 843), add a CallawayCS-specific branch that embeds `{ yVar, treatCol: csTreatCol, entityCol: csEntityCol, timeCol: csTimeCol, xVars: csXCols, compGroup: csCompGroup, csEstMethod, csAnticipation }` onto `dispatch.result` (parallel to the `specExtras` path for other estimators). Update the `rScript.js` destructuring to read from those embedded fields.  
**Invariants:** math file (`CallawayEngine.js`) must stay React-free — the fix belongs in `ModelingTab` and the exporter, not the engine.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`, `src/services/export/loadLine.js`  
**Suggestion:** `PlotBuilder.jsx`'s replication script generator calls into `plotScript.js` but does not prepend a `buildRLoadLine`/`buildPyLoadLine`/`buildStataLoadLine` call (as `rScript.js` already does for model scripts). Pass the active dataset's `loadOpts` and `filename` into the plot script generator and prepend the load line at the top of the emitted R/Python/Stata code. The infrastructure (`loadLine.js`) already exists — it just isn't wired into the plot export path.  
**Invariants:** none.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group  
**File:** `src/pipeline/runner.js` (`group_summarize` step, ~line 528), `src/components/wrangling/ReshapeTab.jsx`  
**Suggestion:** `SampleTestPanel.jsx` already has a two-sample mean-test mode that takes two columns, but econometric data is long-format (one outcome column + one group column). Add a `splitByGroup` helper in `SampleTests.js` that partitions a column by a binary group indicator and feeds the two resulting arrays to `twoSampleMeanTest` — matching R's `t.test(y ~ treat)` interface. Surface it in `ReshapeTab`'s `group_summarize` UI as an optional "cross-group stat" row (e.g., Difference / Ratio / t-test). CLAUDE.md already describes this exact gap and the correct fix shape (see 2026-08-07 entry).  
**Invariants:** math helpers in `src/math/SampleTests.js` must remain React-free.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`  
**Suggestion:** No diagnostic detail is available in the feedback. The most likely culprits (given recent changes): (1) the HC2/HC3 two-way cluster rewrite in `stataScript.js` that routes to `reghdfe` for two-way clustering — verify `reghdfe` is in the emitted `ssc install` list; (2) the N-way FE export path, which may emit unsupported syntax for `xtreg` when 3+ dimensions are selected; (3) the CS DiD Stata path, which likely has the same `undefined` field problem as the R path (see above). Investigate by running the workbench against a known-good OLS spec first to isolate whether the failure is universal or estimator-specific.  
**Invariants:** none.

---

### [high] · Export / Math · 2026-07-09
**Feedback:** FE must accept any categorical variable (country, state, sector), not just entity/time; and Add also FE with interactions  
**File:** `src/components/modeling/ModelConfiguration.jsx` (`FEColumnPicker`, line 66), `src/math/PanelEngine.js` (`runFEMulti`)  
**Suggestion:** BugTriage marks both as "likely fixed" (N-way FE shipped in `045f5cda`, #104). `FEColumnPicker` reads from `panel.feCols` (PanelTab-declared columns), so any column set in PanelTab as a FE dimension should already work. **Action needed:** Franco should browser-validate with a non-entity/non-time categorical variable (e.g., sector) to confirm. If the picker only shows `entityCol`/`timeCol` when `panel.feCols` is empty (line 74 fallback), the fix is to populate `panel.feCols` from PanelTab's multi-dimension picker — check whether PanelTab exposes a third FE column slot.  
**Invariants:** none.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js:168`  
**Suggestion:** `latexTable.js` emits a bare `\begin{tabular}{...}` with no size control. When the column count is high (e.g., >5 model columns), the table overflows the page width. Wrap the `tabular` environment in `\adjustbox{max width=\textwidth}{ ... }` (requires `\usepackage{adjustbox}` in the preamble) and emit that package in the header block. Alternatively, use `\resizebox{\textwidth}{!}{ ... }`. Add the package to the emitted LaTeX preamble comment so users know to include it.  
**Invariants:** none.

---

### [medium] · Export · 2026-07-06
**Feedback:** No way to run more than 1 hypothesis test and save them for R replication  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `CoefficientTestPanel` currently runs one hypothesis at a time. Add a `savedTests` list in component state (array of `{label, indices, h0s}`). A "Save test" button appends the current configuration; the panel renders the saved list and the `generateRHypothesisCode(savedTests)` function in `ModelHypothesis.js` can emit a single `car::linearHypothesis` call per saved test. For persistence across sessions, store `savedTests` in IndexedDB under the project's model buffer key.  
**Invariants:** persistence must use IndexedDB (`services/Persistence/indexedDB.js`), not localStorage.

---

### [medium] · Export · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`  
**Suggestion:** The PlotBuilder `Labels` panel (which handles title, x-axis, y-axis labels for other geoms) is likely not wired into the time-series chart path in `ExplorerModule.jsx`. Check the `renderTimeSeries` / ACF/PACF rendering path and add the same label props that the main PlotBuilder already exposes. If the time-series plot is a separate SVG component, thread the label state from the Labels panel into it.  
**Invariants:** none.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)  
**Suggestion:** The prompt already handles log-log (elasticity) and log-level (semi-elasticity) cases at lines 99 and 353. The gap is likely that the AI doesn't compute and narrate **direct partial elasticity** for level-level models: `e_{x,y} = β × (x̄/ȳ)`. Add a rule to `INTERPRET_REGRESSION_PROMPT` instructing the model to compute the average elasticity at means when the user's result type is OLS and neither Y nor X is log-transformed — emitting `β × (x̄/ȳ)` as the point estimate with a note that it varies across observations.  
**Invariants:** All Anthropic calls must go through `AIService.js`. The rule goes in `Prompts/index.js`, not inline.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square variant for linear hypothesis tests  
**File:** `src/math/ModelHypothesis.js:179` (`waldTest`)  
**Suggestion:** `waldTest` currently returns a Wald F-statistic (`W/q` where `q` = number of restrictions). Add an optional `statistic = "chi2" | "F"` parameter; when `"chi2"`, return `W` directly (without dividing by `q`) and compute the p-value from a chi-squared distribution with `df = q` instead of an F distribution. The chi-squared Wald is the large-sample form (F × q ≈ χ²(q) as n→∞) and is what `car::linearHypothesis` emits by default with `test = "Chisq"`. Expose a toggle in `CoefficientTestPanel.jsx`.  
**Invariants:** math helper in `src/math/` — zero React imports.

---

### [medium] · UI · 2026-07-17
**Feedback:** Groups for pinned models, plots, and maps so not everything is compared at once  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` manages a flat global artifact order. Add a `groups` field (`{ [groupId]: { label, artifactIds[] } }`) to the persisted artifact order structure. In `ModelBufferBar.jsx`, add a group label header above each group's model chips, with a collapse toggle and a "Create group" affordance (drag-to-group or name-based assignment). Start with a single "Ungrouped" default group so existing users are unaffected.  
**Invariants:** persistence must use IndexedDB (`indexedDB.js`), not localStorage.

---

### [medium] · UI · 2026-07-17
**Feedback:** Unfinished work in transformation subsections is lost when navigating away (e.g. writing a case_when, going to Clean, then back)  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** `FeatureTab` uses local `useState` for `case_when` rows and `if_else` inputs — navigation destroys them. Lift the draft state to `WranglingModule.jsx` (or into a lightweight `useRef`/`sessionStorage` key under the current `pid`) so it survives tab switches within the same session. A `pid`-scoped `sessionStorage` key is the simplest fix; IndexedDB is only needed for cross-session persistence.  
**Invariants:** IndexedDB is the canonical persistence layer for pipeline/data. `sessionStorage` is acceptable for ephemeral UI draft state (short-lived, no cross-session requirement here).

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange (sort) control in the data viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** Add a column header click handler that sets `(sortCol, sortDir)` in local state. For DuckDB-backed datasets, push `ORDER BY ${sortCol} ${sortDir}` into `getTablePage`'s SQL query (the function already handles WHERE — adding ORDER BY is a parallel change). For the in-memory JS path, sort `rows` before slicing to the display page. A small ▲/▼ icon in the header cell signals the active sort. This mirrors what `getTablePage` already does for ordered exports.  
**Invariants:** Never pull the full Arrow result into JS objects for large datasets — sort must remain inside the DuckDB query for large tables.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line: 1583 — "China,CHN,2010,2425.47..." (row content truncated)  
**File:** `src/DataStudio.jsx` (CSV parsing path), `src/services/data/parsers/`  
**Suggestion:** The malformed-CSV row is currently causing an import abort. BugTriage (2026-06-14) has this as "fix-later — post-launch resilient-import pass." Implement a graceful fallback: catch the parse error per-row, record the original line number, and surface a pre-import preview showing which rows failed and why, with options to skip them or fix them inline. The `papaparse` library already supports `skipEmptyLines` and a `step` callback for per-row error capture — hook into that pattern.  
**Invariants:** none.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array, line 11)  
**Suggestion:** `TOUR_STEPS` was last described in CLAUDE.md as covering 9 steps. The app has grown significantly (new tabs: Spatial, N-way FE, CS DiD, Synthetic Control, Sun-Abraham, Bacon decomposition, Plot Builder geoms, CI builder, etc.). Audit each `TOUR_STEPS` entry against the current UI and add/update steps for the major workflow expansions. Per CLAUDE.md conventions: never write counts ("9 steps") — name the capabilities. The `HintBox` in each module is the secondary help surface; the tour is the first-run orientation.  
**Invariants:** none.

---

## Features

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`, `src/math/cas/` (CAS engine)  
**Suggestion:** Add a `limit(expr, var, val, direction?)` function to the `CalculateTab` expression parser and CAS engine. For the symbolic path: implement L'Hôpital / polynomial factoring for `0/0` and `∞/∞` indeterminate forms, or wrap `mathjs`'s limit support if already bundled. Surface it alongside derivatives and integrals in the workbench UI with `direction` options (→a⁻, →a⁺, →a). LaTeX export should render as `\lim_{x \to a} f(x)`.  
**Invariants:** `src/math/` must have zero React imports.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g., draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** Constants are already a degenerate case of a function (`f(x) = c`). The graph renderer likely filters these out or mis-handles them. Check that the equation parser accepts `y = 3.14` or `f(x) = 5` as valid inputs and maps them to a constant-valued function before passing to the chart. If the chart requires a `y(x)` callback, wrap the constant: `x => c`. The `hline` geom in PlotBuilder already does this — borrow that pattern.  
**Invariants:** none.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", etc. regardless of actual column names  
**File:** `src/components/modeling/resultDisplay.jsx` (or model result table component)  
**Suggestion:** Add an editable label overlay: a `varLabels` map (`{ [originalName]: string }`) in `ModelingTab` state, persisted to the model's spec via `specExtras`. The LaTeX table builder reads `varLabels[name] ?? name` when emitting row labels. Expose an "Edit labels" mode (pencil icon) in the result table that shows inline text inputs for each variable row. Scope to the active result only — pinned models carry their own `varLabels` snapshot from estimation time.  
**Invariants:** persistence must use IndexedDB for cross-session survival; session state is acceptable for ephemeral label editing.

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) — R-style membership filter syntax  
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`  
**Suggestion:** `predicate.js` is the canonical owner of the filter dialect (`OPERATORS`, `evalPredicate`, `predicateToSQL`). The `in` operator already exists; the gap is that the filter UI (`FilterBuilder`) doesn't offer a free-text multi-value input for membership lists. Add a multi-value chip input (comma-separated tags) for the `in`/`nin` operators in `FilterBuilder` inside `CleanTab.jsx`. The `predicateToSQL` path already handles `in` via `IN (...)`.  
**Invariants:** `predicate.js` is the single owner of the condition language — never re-implement filter operator evaluation elsewhere.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — compact table showing Sharp RD estimates using local polynomial regression  
**File:** `src/components/modeling/results/` (RDD result component), `src/math/CausalEngine.js`  
**Suggestion:** Add a collapsible `rdrobust`-style summary panel below the existing RDD result that shows: bandwidth (IK), effective N left/right, polynomial order, kernel, point estimate, bias-corrected CI (pending CCT bandwidth), SE (conventional / robust). Most of these are already in the result object from `runSharpRDD`. The formatting matches rdrobust's `print.rdrobust` output for UX familiarity.  
**Invariants:** none.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider — for large datasets, users need to type and find variables faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a text filter input at the top of each variable panel (Y, X, W, Z). Filter the displayed column list to those whose name contains the typed string (case-insensitive). Keep the input visible while typing; clear on blur or via an ×  button. No state persistence needed — this is a transient display filter.  
**Invariants:** none.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set, the current legend shows swatches but no value annotations on data points. Add a `showColorLabels` toggle in the PlotBuilder Labels panel that, when enabled, adds a text mark at each point/bar centroid with the group name. For categorical point plots, this is `Plt.text(data, {x, y, text: colorCol, fill: colorScheme, …})`. Ensure the text color inherits from the color scale so labels are legible without a separate legend.  
**Invariants:** none.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** Each pipeline step in `History.jsx` already knows its `type`. Add a `navigateToStep(step)` callback that maps step type to the corresponding tab/subsection: e.g., `filter` → Clean tab / Filter section, `mutate` → Feature tab / Formula section, `group_summarize` → Reshape tab. Call `setActiveTab` and, where the subsection has its own state (e.g., the active FeatureTab sub-mode), set that too. The step's params can pre-populate the form (like a deep link into edit mode).  
**Invariants:** none.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`  
**Suggestion:** Add a `country_code` pipeline step: input column + conversion direction (name→ISO2, name→ISO3, ISO2↔ISO3, ISO2→name, ISO3→name). Embed a compact static lookup table (~250 rows) as a JSON import — no external API needed. Wire it as a FeatureTab UI card (similar to the existing `dummy` or `recode` cards) and a runner.js step case. Keep the lookup small to avoid bundle-size impact.  
**Invariants:** runner.js steps must replay on `rawData` — never mutate in place. No React imports in math/core.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables — cross-module feature  
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`  
**Suggestion:** Design first, implement second (per ClaudeFB.md note). Proposed: a "Named Lists" store in `sessionState.jsx` — a `{ [listName]: string[] }` map — accessible from the filter `in` operator dropdown (type a list name to expand it) and from FeatureTab's `case_when` value inputs. Persist to IndexedDB under the project key. This makes the feature cross-module naturally since `sessionState` is already the cross-module state layer. Start with a simple CRUD UI (add/edit/delete list) as a sidebar panel in the Clean tab.  
**Invariants:** session-scoped state must go through `sessionState.jsx` (React context). Persistence uses IndexedDB.

---

## UX / Design

### UX · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation  
**File:** `src/components/ModelingTab.jsx` (~line 843)  
**Suggestion:** BugTriage marks this as "code-complete 2026-06-14 (`35f4b5a6`) — browser validation pending Franco." OLS/WLS diagnostics now render from `result.spec` (frozen at estimation). **Franco: browser-validate** by estimating an OLS model, then changing a variable in the sidebar and confirming that the displayed coefficient plot and diagnostics do NOT change until the user re-runs estimation.  
**Invariants:** none.

---

### UX · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems  
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`  
**Suggestion:** BugTriage marks this as "code-complete 2026-06-14 (`35f4b5a6`) — browser validation pending Franco." Plot viewport now has native vertical resize with bounded responsive canvas sizing. **Franco: browser-validate** by resizing the plot panel and confirming PNG export captures the user-selected dimensions correctly.  
**Invariants:** none.

---

### UX · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS  
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`  
**Suggestion:** BugTriage marks this as "code-complete 2026-06-14 (`35f4b5a6`) — browser validation pending Franco." All three plots were already wired. **Franco: browser-validate** by running an OLS model and confirming the three diagnostic plots render correctly and remain frozen when the sidebar is edited.  
**Invariants:** none.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`  
**Suggestion:** DuckDB-WASM integration (Fase 0–8) and OPFS Parquet caching are code-complete per CLAUDE.md, addressing the root cause. CLAUDE.md's "Fase X4 performance" plan is code-complete but browser benchmark is **pending Franco**. If crashes still occur, the most likely residual causes are: (1) Arrow-to-JS materialization of a full 900k-row result somewhere in the pipeline UI (use `SELECT *` guard — always paginate/limit in the display layer); (2) a missing `connect-src` CSP entry for a DuckDB extension domain blocking the parquet extension (see the 2026-07-25 fix pattern); (3) SharedArrayBuffer threading disabled (check COOP/COEP headers on Vercel). **Franco: run the 900k-row benchmark and capture the browser console for any blocked-resource or OOM trace.**  
**Invariants:** display is limited to 200–500 rows; full computation always runs on the DuckDB table. Never truncate `rawData` before passing to `runner.js`.

---

## Suggested action order

1. **Fix CS DiD R replication code** — embed `yVar`/`treatCol`/`entityCol`/`timeCol` in CallawayCS result so `rScript.js` can emit valid column names (`src/components/ModelingTab.jsx` + `src/services/export/rScript.js`).
2. **Fix plot replication code missing dataset load lines** — prepend `buildRLoadLine`/`buildPyLoadLine` to `plotScript.js` export output (`src/services/export/plotScript.js`, `loadLine.js`).
3. **Add cross-group math to group\_summarize / SampleTestPanel** — long-format two-sample interface for ATE as difference-in-means (`src/math/SampleTests.js`, `src/components/wrangling/ReshapeTab.jsx`).
4. **Diagnose Stata workbench failure** — isolate whether it's universal or estimator-specific; likely same undefined-field issue as the CS DiD R bug (`src/services/export/stataScript.js`).
5. **Browser-validate code-complete items** (Franco) — diagnostics freeze (#19), Plot Builder resize (#16), Y~Yhat / Q-Q plots (#17), spatial heatmap (#18); these are blocked on browser confirmation, not further code changes.
6. **Data viewer sort control** — push `ORDER BY` into `getTablePage` SQL query for large-n datasets; sort JS array for small ones (`src/ExplorerModule.jsx`).
7. **FeatureTab draft persistence** — save `case_when` / `if_else` draft to a `pid`-scoped `sessionStorage` key to survive tab navigation (`src/components/wrangling/FeatureTab.jsx`).
8. **Chi-square variant for hypothesis tests** — single-parameter addition to `waldTest` with a `"chi2"` mode (`src/math/ModelHypothesis.js`).
9. **LaTeX table `\adjustbox`** — wrap `tabular` with `\adjustbox{max width=\textwidth}` and add the package to the preamble (`src/services/export/latexTable.js`).
10. **Guide tour update** — audit `TOUR_STEPS` in `HelpSystem.jsx` against the current feature set and add entries for Spatial, N-way FE, CS DiD, Synthetic Control, Sun-Abraham, Bacon decomposition, Plot Builder geoms.
