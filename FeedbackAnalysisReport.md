# Feedback Analysis Report — 2026-07-14

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **5 open bugs** · **7 open features** · **0 open UX/Design** · **1 open performance** · **0 open other**
- 4 of the 5 bugs are code-complete in commits `35f4b5a6` / `5e105529` / `2026-07-06` and are waiting for browser validation by Franco — one session could close all four.
- 1 feature request ("Add limits") is likely already shipped; needs a quick UX confirmation before marking closed.

Cross-reference method: ClaudeFB.md items not marked ~~strikethrough~~ or ✓ were matched against BugTriage.md status column and the 2026-06-14 X5 reconciliation table. Items marked "fixed" in BugTriage or "verified fixed" in X5 are excluded even if not yet struck through in ClaudeFB.md. Items marked "code-complete, browser validation pending" remain open. No new feedback batches since 2026-07-04.

---

## Bugs

### [high] · Model · 2026-06-11 + 2026-06-14
**Feedback:** (1) `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.` (2) `Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.`
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** Both issues are code-complete in commit `35f4b5a6` (2026-06-14): all three residual/QQ plot components were already wired, and OLS/WLS diagnostics now read from the frozen `result.spec` rather than live sidebar state. Browser validation is the remaining action — run an OLS model, confirm all three plots appear, then add a variable without re-estimating and confirm the displayed plots and BP/DW/JB values do not mutate. Also restore a pinned model and verify diagnostics reappear correctly.
**Invariants:** All display must read from the frozen `EstimationResult` object, never from live `xVars`/`yVar` selector state. `ResidualPlots.jsx` must not import from `src/math/`.

---

### [medium] · Model · 2026-06-10
**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`
**File:** `src/components/PlotBuilder.jsx` (resize container at line 1626: `resize: "vertical"`), `src/services/export/plotExporter.js`
**Suggestion:** Code-complete in commit `35f4b5a6` (2026-06-14) — the plot viewport now has a native vertical resize handle with bounded responsive canvas sizing (`minHeight: 220`, `maxHeight: 70vh`), so PNG captures user-selected dimensions. Browser validation: drag the resize handle in ModelingTab, trigger PNG export at the resized height, and confirm no clipping or dark-theme style bleed in the exported image. If PNG dimensions still mismatch, check that `downloadCombinedPNG` reads the post-resize `clientHeight` rather than a stale value captured at mount.
**Invariants:** Inline styles via `C` constants from `shared.jsx` only — no CSS files. The `ResizeObserver` pattern already in PlotBuilder must not be broken.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** `Heatmap must be fixed`
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete in commit `5e105529` (2026-06-14) — heatmap restored as a Leaflet KDE layer with palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Browser validation: load a point dataset, select the Heatmap layer type in the Map tab, confirm the gradient renders, adjust opacity and palette, and export HTML. If the layer does not appear, verify the 5k-point cap is not silently filtering all rows.
**Invariants:** Leaflet must be loaded through the existing CDN singleton in `src/components/tabs/spatial/shared/leaflet.js` — never import it statically. The 5k-point cap is intentional.

---

### [high] · Sync · 2026-07-06
**Feedback:** `Sharing a project with another user fails with "Share not found or you are not the intended recipient." even when the recipient email is correct`
**File:** `src/services/sync/shareEngine.js`, Supabase migration `20260706120000_fix_project_shares_email_case.sql`
**Suggestion:** Code-complete 2026-07-06 — RLS policies now lowercase the JWT email claim before comparing; `share_blobs_owner_select` was added so owners can read their own share blobs. Migration applied to live DB. Browser validation: (1) owner creates a share targeting a mixed-case email, (2) recipient opens token link → project imports successfully, (3) owner revokes → blob no longer listed, (4) owner self-restores → succeeds. Confirm via Supabase dashboard that all 7 policies are present on `shares` table and `storage.objects`.
**Invariants:** All Supabase egress must stay within `src/services/sync/` — no raw `fetch` to Supabase storage elsewhere. The `pullShare` user-facing error message mapping must be preserved.

---

### [medium] · Data · 2026-05-10 (fix-later)
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...` — malformed CSV row aborts the entire import
**File:** `src/DataStudio.jsx` (`parseCSV` at line 41; `loadLargeCSV` DuckDB path at line 383)
**Suggestion:** Wrap the per-row body of `parseCSV` in a try/catch and collect malformed rows into a `_parseErrors` array rather than throwing. After import, surface a dismissible banner: "N rows skipped — see details" with a log of skipped line numbers and raw text. For the DuckDB large-file path, DuckDB's `read_csv` supports `IGNORE_ERRORS=TRUE` — wire it behind a user opt-in toggle surfaced in the import banner. Both paths should complete the import before surfacing the warning, so the user can proceed with clean rows and decide whether to repair the source file.
**Invariants:** Non-destructive pipeline invariant: partial data must not be silently stored under the same dataset key without user acknowledgement. Malformed rows must never enter `rawData.rows`. Errors attach as `_parseErrors` metadata, not as a new top-level key.

---

## Features

### Explore/Clean · 2026-05-19 (fix-later)
**Feedback:** `Add filter(regionname %in% c("Spain", "Basque Country")) R-style membership syntax for the filter option`
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder section), expression evaluator used in `src/pipeline/runner.js` `filter` case
**Suggestion:** Add a pre-processing regex pass that detects `<col> %in% c(<csv-values>)` and rewrites it to the existing `{op:"in", col:"...", values:[...]}` filter node before evaluation. This is a string-level transform — no full parser is needed. Also handle bare `1:N` integer ranges. The transform belongs in the expression pre-processor, not inside `runner.js` itself, so the emitted pipeline step remains a standard `filter` step already registered in `registry.js`.
**Invariants:** Filter steps must replay on `rawData` via `runner.js` without mutation. Expression parsing must not introduce a new step type. No new npm dependencies.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** `Add for RDD a summary table as in R with summary(rdrobust(...)) — bandwidth, effective N, kernel, polynomial order, LATE, robust CI`
**File:** `src/math/CausalEngine.js` (result object returned by `runSharpRDD`), `src/components/ModelingTab.jsx` (RDD display branch ~line 3323)
**Suggestion:** `runSharpRDD` already returns `bandwidth`, `nLeft`/`nRight`, `kernel`, `polyOrder`, and full coefficient/SE/CI rows in the `EstimationResult`. Create a new `src/components/modeling/results/RDDSummaryPanel.jsx` that formats these into a compact header table mirroring `rdrobust` output (effective N, bandwidth, kernel, polynomial order, LATE, robust 95% CI). Render it above the existing coefficient table in ModelingTab. No engine changes needed — all data is already present.
**Invariants:** The new component is pure React/JSX — zero imports from `src/math/`. All inline styles via `C` constants.

---

### Model · 2026-05-25 (fix-later)
**Feedback:** `Add an option to estimate without intercept (suppressed constant)`
**File:** `src/math/LinearEngine.js` (`runOLS` line 128: `const k = xCols.length + 1`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add a `noIntercept: boolean` parameter to `runOLS`/`runWLS` following the same explicit-argument pattern as `seType`. When `true`, skip prepending the ones column to the design matrix and use `k = xCols.length` for degrees of freedom. Expose the toggle as a checkbox in `ModelConfiguration.jsx`. Thread it through `estimationDispatch.js` into every engine call that builds a design matrix. Replication scripts (`rScript.js`, `stataScript.js`, `pythonScript.js`) must emit the correct syntax (`lm(y ~ x - 1)`, `nocons`, `fit_intercept=False`). Validate against R `lm(y ~ x - 1)` to 6 decimal places on coefficients and 4 on SE.
**Invariants:** `seType` architecture invariant applies symmetrically — `noIntercept` must be explicit, never inferred. Zero React imports in `src/math/`. All engines that currently default-add an intercept column must be updated (LinearEngine, CausalEngine WLS, GMMEngine).

---

### Spatial · 2026-05-30 (fix-later, deferred to V4)
**Feedback:** `Allow spatial filters for plots — row-level filtering scoped to a map/geo-plot layer`
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add an optional "Filter rows" field to `GeoLayerConfig.jsx` — a column selector plus value/expression input that subsets the layer's data before the geometry render loop. Store the filter as `{ col, op, value }` in layer config alongside existing aesthetics. Apply it inside `SpatialGeoPlot.jsx` before passing rows to `mkGeoLayer`. No DuckDB query or pipeline step needed — filtering is purely on the local JS `rows` array already in plot state.
**Invariants:** Spatial plot filters must never modify `rawData` or inject pipeline steps. All filtering stays within local render state. Inline styles via `C` constants only.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/math/CausalEngine.js` (lines 577–579: `varNames` construction), `src/services/data/duckdbRDD.js` (line 44: hardcoded `"D (treatment)"`), `src/components/modeling/resultDisplay.jsx`
**Suggestion:** Two-part fix. (1) In `CausalEngine.js`, derive default `varNames` from the model spec — e.g. `treatVar[0]` instead of `"D (treatment)"`, `runningVar[0] + " − " + cutoff` instead of `"running − c"`. Mirror this in `duckdbRDD.js`. (2) Add an optional `coefLabels: { [internalName]: string }` display-layer map to `EstimationResult`, editable via an inline ✎ icon in `resultDisplay.jsx`. The LaTeX renderer reads `coefLabels[name] ?? name`. `ModelComparison.jsx` already has a `varLabels` pattern (line 331) — extend the same mechanism to the single-model result view. Persist `coefLabels` per pinned model in the `model_buffer` IndexedDB store.
**Invariants:** `coefLabels` is display metadata only — it must never alter coefficient order or the underlying `EstimationResult` data. Persistence in `indexedDB.js` only, never `localStorage`. Zero React imports in `src/math/`.

---

### Calculate · 2026-06-19 ⚠️ LIKELY ALREADY IMPLEMENTED — needs ClaudeFB.md ✓
**Feedback:** `Add limits (mathematical limit as x → a) to the Calculate tab`
**File:** `src/components/tabs/CalculateTab.jsx:1586`
**Suggestion:** The feature already ships. The `"limit"` entry is in the six-tool toolbar strip at line 1586 (`"lim Limit"`), the `activeTool === "limit"` render block fires at line 2161, and the `limit` function is imported at line 18 from the math engine. Franco should open Calculate, confirm `lim(x → 0) sin(x)/x = 1` evaluates correctly, and mark the ClaudeFB.md entry ✓. No code change is needed.
**Invariants:** N/A.

---

### Calculate · 2026-07-04
**Feedback:** `Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)`
**File:** `src/components/tabs/CalculateTab.jsx` (function graph section; scope at line 1542 already exposes constants `pi` and `e`, but there is no horizontal-line layer)
**Suggestion:** In the function graph renderer, detect when a user-entered expression resolves to a scalar (independent of the free variable `x`) — evaluate it once and add a `{ type: "hline", y }` descriptor to the layer list, rendering it as a dashed horizontal line with a label. For workspace variables that are scalars (e.g. `c = 5`, `MC = alpha * w / A`), add a dropdown next to the existing equation input rows that lists available scalar workspace variables and auto-inserts them as horizontal reference lines when selected. This mirrors the PlotBuilder `hline` geom concept without importing ObservablePlot into CalculateTab.
**Invariants:** Inline styles via `C` constants from `shared.jsx`. No new CDN dependencies. Constant-expression lines must participate in the multi-curve colour rotation logic already present in the graph canvas.

---

## Performance

### [high] · Data · 2026-05-08 (pending browser benchmark)
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (DuckDB routing ~line 381), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Fase X4 code-complete per X5 reconciliation — large CSV/TSV files are routed to DuckDB, converted to Parquet, and persisted to OPFS; tab transitions no longer re-materialise the full JS object array. Browser benchmark is the remaining action: load the 21 MB / 900k-row CSV fixture, navigate all tabs (Data → Clean → Explore → Model → Simulate), and confirm no OOM crash and tab-switch latency under 2 seconds. Also verify the `⚡ DuckDB` badge appears in the wrangling header and that model output reports the full row count (separate n=500 cap bug was fixed in `c9c5bd3e`). Enabling `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` in `vercel.json` would unlock `SharedArrayBuffer` and full parallel DuckDB execution per CLAUDE.md §DuckDB rule 4.
**Invariants:** Privacy-first: all computation must be client-side — no server-side data egress. Pipeline operations must always run on the full dataset. Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`.

---

## Suggested action order

1. **Franco: browser-validate commit `35f4b5a6`** — closes OLS residual plots (missing + diagnostics freeze) and Plot Builder resize/PNG in one session.
2. **Franco: browser-validate commit `5e105529`** — confirms Spatial heatmap layer working; load a point dataset → Map tab → Heatmap layer.
3. **Franco: verify project sharing RLS round-trip** — mixed-case email, create → import → revoke → owner-restore, confirms the 2026-07-06 migration is live and correct.
4. **Franco: confirm "lim" tab in Calculate** — if visible and working, mark ClaudeFB.md item ✓ (no code change needed).
5. **Dev: fix RDD hardcoded `varNames`** in `CausalEngine.js:577–579` and `duckdbRDD.js:44` to use actual spec column names — prerequisite for LaTeX label editing.
6. **Dev: implement plotting constants in CalculateTab** — detect scalar expressions and render as dashed `hline`; small, self-contained change.
7. **Franco: run Fase X4 browser benchmark** — 900k-row CSV, sub-2 s tab transitions, no OOM crash; closes the longest-standing performance item.
8. **Dev: no-intercept regression toggle** — `noIntercept` flag in `LinearEngine.js` + R validation + replication script updates; higher effort, wide engine impact.
9. **Dev: resilient CSV import** — malformed-row skip/repair UX in `DataStudio.jsx`; unblocks users with messy real-world CSVs.
10. **Dev: editable LaTeX coefficient labels** — display-only in `resultDisplay.jsx` + `coefLabels` in `EstimationResult`; no engine changes.
11. **Dev: `rdrobust`-style RDD summary panel** — pure UI addition; all data already in `EstimationResult`.
12. **Dev: R `%in% c(...)` filter syntax** — pre-processing pass in expression evaluator; low-risk.
13. **Dev: spatial plot layer filter** — deferred to V4; `GeoLayerConfig.jsx` local render-state filter.
