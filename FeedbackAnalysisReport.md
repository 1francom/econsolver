# Feedback Analysis Report — 2026-09-21

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **16 open bugs** (4 code-complete pending Franco browser confirmation) · **10 open features** · **2 open UX/design** · **1 open performance** · **0 open other**

> **Note on items not listed here:** Many ClaudeFB.md bullet points that lack ~~strikethrough~~ / ✓ markers are already resolved per BugTriage.md (e.g. Stata .dta format fixes, share RLS, p-value bug, join pipeline propagation, group-summarize `any()`). Those are treated as fixed. Items listed below are either explicitly "open" or "fix-later" in BugTriage.md, or are unreferenced there and not confirmed resolved.
>
> **Already-done items that ClaudeFB never marked:** `Add limits` (CalculateTab has `"limit"` tool, line 1595 `CalculateTab.jsx`); Anderson-Rubin CIs (X5: "verified fixed"); Multiple statistics tests (X5: "verified fixed"); Estimate without intercept (CLAUDE.md: "DONE 2026-07-18").

---

## Bugs

### [critical] · Performance / Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Per CLAUDE.md "Fase X4 code-complete; browser benchmark pending Franco" — this is in the DuckDB-WASM performance track. Confirm browser benchmark of the OPFS Parquet cache + SQL push-down path on a real 900k-row dataset. If the crash still reproduces, check whether the OPFS write succeeded (DuckDB parquet-extension CSP issue documented in CLAUDE.md), and whether Arrow materialisation on tab-transition is still happening rather than query-only access.
**Invariants:** DuckDB as data boundary; never pull full Arrow result into JS objects (CLAUDE.md performance rule 1).

---

### [critical] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 (CSV import aborts on malformed row)
**File:** `src/services/data/parsers/`, `src/DataStudio.jsx`
**Suggestion:** BugTriage categorises this "fix-later — post-launch resilient-import UX." Suggested approach: catch per-row parse errors, collect them, allow the import to proceed with the bad rows either skipped or flagged, and surface a "N rows skipped due to parse errors (lines: 1583, …)" banner. The row number is already available at parse time.
**Invariants:** None specific.

---

### [high] · Export / Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** BugTriage says "open." The July 2026 SE-type fix pass corrected HC2/HC3 and panel SE routing, but a broad "doesn't work completely" suggests remaining gaps (e.g. `ibn.` factor coding in no-intercept specs marked UNVERIFIED, `joinby` for many-to-many joins, interaction-only factor coding under no-intercept, two-way cluster routing to `reghdfe`). Audit `stataScript.js` against R output by running the export harness (`replicationIntegrityValidation.mjs`) and tracing any `UNVERIFIED` or `NOTE:` comments to confirm they produce valid `.do` syntax. A focused round of `reghdfe`-based panel SE plus the `ibn.`/factor edge cases is the likely root cause.
**Invariants:** None — export files are pure JS outside `src/math/` and `src/core/`.

---

### [high] · Export / Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx` (`generateSimScript`, line 519)
**Suggestion:** `generateSimScript` emits only the DGP variable draws (`distExprR`/`distExprPy`/`distExprStata`). It does not emit: (a) the downstream OLS/single-variable Monte Carlo loop, (b) `SampleTestPanel` tests run on simulated data, nor (c) summary stats from `StatWorkspace`. Extend `generateSimScript` to accept the active MC mode and emit a `for`-loop with estimation/test calls matching what the UI ran. The `dgpScript.js` expressions are already the single source for the draw step — only the analysis wrapper is missing.
**Invariants:** `src/math/` and `src/core/` must have zero React imports — keep export logic in `SimulateTab.jsx` or a new `src/services/export/simScript.js`.

---

### [high] · Model · 2026-07-09
**Feedback:** Add options for fixed effects — not just entity and time, but country, state, sector, every categorical variable must be allowed as FE
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/PanelEngine.js` (`runFEMulti`)
**Suggestion:** BugTriage says "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation." The N-way FE math is validated (18/18 R checks in CLAUDE.md). The remaining question is whether the UI allows users to select arbitrary categorical columns as FE dimensions, not only the two "entity/time" pickers in `ModelConfiguration.jsx`. If the pickers are still limited to panel-declared columns, add a multi-select for extra FE dimensions that routes to `runFEMulti` without requiring a panel declaration.
**Invariants:** SE type must be passed explicitly to `runFEMulti` — never hardcoded. No React in `PanelEngine.js`.

---

### [high] · Model · 2026-07-09
**Feedback:** Add also FE with interactions
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions`)
**Suggestion:** BugTriage says "likely fixed — shipped in `045f5cda` (#104); needs confirmation." Verify that FE×covariate interactions (e.g. `country×female`) can be specified in the ModelConfiguration UI and are passed to `runFEMulti` as interaction columns, not just as separate FE dimensions. If the `factor_interactions` pipeline step is required upstream, confirm that it is reachable from the Modeling tab's variable selector without requiring a separate Clean step.
**Invariants:** No React in `PanelEngine.js`.

---

### [high] · Model · 2026-06-11 (code-complete since 2026-06-14)
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage says "code-complete `35f4b5a6` — all three plots were already wired; fixed the OLS/WLS display freeze to `result.spec`; browser validation pending Franco." This is over 3 months old. If still failing in-browser, check that `result.spec` is populated for all OLS paths (including DuckDB suff-stats path) and that `ResidualVsFitted` / `QQPlot` receives `resid` and `Yhat` arrays, not `null`. The suff-stats path may not materialise residuals — `duckdbResiduals.sampleResiduals` returns a 5k-row sample, verify it is passed to the plot components.
**Invariants:** None.

---

### [medium] · Model · 2026-06-14 (code-complete since 2026-06-14)
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** BugTriage says "code-complete `35f4b5a6` — OLS/WLS display frozen to `result.spec`; browser validation pending Franco." Also over 3 months old. Confirm that `result.spec` is populated correctly for panel, IV, and non-linear estimators too (the fix may only have targeted OLS/WLS). Non-OLS results should derive their display from `result.spec` consistently.
**Invariants:** None.

---

### [medium] · Spatial · 2026-06-13 (code-complete since 2026-06-14)
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** BugTriage says "code-complete `5e105529` — restored Heatmap as a Leaflet map layer with KDE cells; browser validation pending Franco." Over 3 months old. If still broken in-browser, check whether the Leaflet heatmap plugin loads correctly (CSP `connect-src` issue documented in CLAUDE.md could block CDN-loaded plugins), and whether the deterministic 5k-point cap is applied before the KDE computation.
**Invariants:** CSP `connect-src` must include every CDN loaded at runtime (CLAUDE.md key bug).

---

### [medium] · Model · 2026-06-10 (code-complete since 2026-06-14)
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage says "code-complete `35f4b5a6` — plot viewport has native vertical resize (`resize: "vertical"`)." `PlotBuilder.jsx:2194` confirms `resize: "vertical"` is set. If PNG export quality is still wrong, check that the SVG-to-Canvas rasterisation reads `contentRect` dimensions (already noted in the file comment at line 2194) rather than the original 400px default.
**Invariants:** None.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` (time-series section, approx. lines 1460–1640)
**Suggestion:** BugTriage says "open." The time-series sub-tab groups by a color/group column and overlays one line per group. If group references (legend labels, color mapping) break, the likely cause is that the group-split path for the flat sorted series passes the wrong column key when `tCol` (time column) and `colorCol` are both set. Inspect the group-aggregation step (around line 1460) and verify the `groupBy` value matches what the SVG line renderer expects.
**Invariants:** None.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.
**File:** `src/ExplorerModule.jsx` (time-series section), `src/components/PlotBuilder.jsx`
**Suggestion:** BugTriage says "open." The time-series line chart in ExplorerModule is an SVG custom renderer — it does not use PlotBuilder, so it lacks the labels panel that PlotBuilder provides. Add a compact labels row (title, x-axis label, y-axis label) above the chart, matching the pattern in other Explore sub-tabs. If the chart is eventually migrated to PlotBuilder, the labels come for free.
**Invariants:** Inline styles via `C` object only — no CSS files.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`
**Suggestion:** The current output uses a plain `\\begin{tabular}{...}` with no width adjustment. When a model has many regressors the table overflows the page margins in LaTeX. Wrap the table in `\\resizebox{\\linewidth}{!}{...}` or emit `\\usepackage{adjustbox}` with `\\begin{adjustbox}{max width=\\linewidth}`. Alternatively, emit `\\begin{tabular*}{\\linewidth}{@{\\extracolsep{\\fill}}...}` for tables that should stretch. Check whether `ReportingModule.jsx` needs to thread a `wide` flag to `latexTable.js`.
**Invariants:** None.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** BugTriage says "open." `ModelHypothesis.js` and `CoefficientTestPanel.jsx` support one hypothesis at a time. Extend `CoefficientTestPanel` with a saved-tests list: each test is pushed to a `savedTests` array (state in ModelingTab), displayed in a summary table, and exported via `generateJointHypothesisScript()` (already added 2026-07-30) as a batch of `car::linearHypothesis` calls. The save button can reuse the existing "copy test code" path.
**Invariants:** None.

---

### [low] · Clean · 2026-05-19
**Feedback:** Add filter(`regionname %in% c("Spain", "Basque Country")`) for filter option — R-style `%in%` membership syntax not accepted
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** BugTriage says "fix-later." `predicate.js` owns the canonical condition language. The `in`/`nin` operators already cover the `%in%` semantic. The gap is that `FilterBuilder` in `CleanTab.jsx` doesn't surface an easy "in list" editor — users expect a free-text field where they can paste values. Adding a multi-value tokeniser chip input for `in`/`nin` operators, with a tooltip showing the equivalent R `%in%`, would close this without changing the predicate language.
**Invariants:** `predicate.js` is the single owner of filter operators — never re-implement a filter operator elsewhere (CLAUDE.md architectural invariant).

---

### [low] · Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** BugTriage says "fix-later — route to V4 plots." A per-layer bounding-box or attribute filter (analogous to the row-filter in Clean) would let users show only a geographic subset without adding a pipeline step. A minimal implementation: add a `GeoLayerConfig` panel entry that accepts a predicate (using `evalPredicate` from `predicate.js`) applied to the layer's rows before rendering.
**Invariants:** Use `evalPredicate` from `src/pipeline/predicate.js` — never re-implement filter logic.

---

### [low] · Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/components/modeling/resultDisplay.jsx`
**Suggestion:** BugTriage says "fix-later." Add a `displayNames` map to `EstimationResult` (or to the spec's metadata), editable via an inline double-click in the coefficient table or a dedicated "Edit labels" modal. `latexTable.js` already receives the coefficient names — it just needs to accept an override map.
**Invariants:** None.

---

## Features

### Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation of coefficients
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`), `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage says "open." The AI narrative already computes log-log elasticity and log-level semi-elasticity via rules in `INTERPRET_REGRESSION_PROMPT` (lines 99, 364–367). A non-AI path is also useful: add an "Elasticity" chip in the coefficient table that shows `β × (X̄/Ȳ)` (point elasticity at the mean) computed from the dataset's column means and the estimated β. This does not require an AI call and matches what R's `margins` package computes by default for linear models.
**Invariants:** All Anthropic calls through `AIService.js` only.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Open. Add a small search input at the top of the variable list panel that filters the displayed column names client-side. The list already renders from `headers` — a `filteredHeaders = useMemo(() => headers.filter(...), [headers, searchQuery])` with a text input is sufficient.
**Invariants:** Inline styles via `C` object only.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Open. When `aes.color` is set, PlotBuilder currently shows a legend via Observable Plot's default. Users want richer labels — e.g. value labels on bars per group, or annotations on line endpoints. Add a "Color labels" checkbox in the labels panel that, when enabled, appends a `Plot.text` mark with the series name at the last data point for line geoms, and uses `showValues` (already supported for bar) for grouped bars.
**Invariants:** None.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/modeling/results/` (RDD results component), `src/math/CausalEngine.js`
**Suggestion:** BugTriage says "fix-later." `runSharpRDD` already returns bandwidth, kernel, polynomial order, N left/right, and the full SE and CI. A dedicated `RDDSummaryTable` component (modelled on `rdrobust`'s output format: Method, Kernel, BW, N obs left/right, Coef, SE, CI) would match R's output 1:1 and improve replication confidence.
**Invariants:** No React in `CausalEngine.js`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Open. The function graph in CalculateTab plots parametric curves. A constant `y = k` is a degenerate case — add support for defining a "constant" entry in the expression list that evaluates to a fixed value, rendered as a horizontal line. Alternatively, allow `f(x) = 5` (a constant function) in the existing parser; if the parser already handles it and the bug is purely rendering, verify the y-domain isn't clipped to exclude the constant value.
**Invariants:** Inline styles only, no external UI libraries.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Open. The binned scatter (partial regression plot) is currently fixed-format. Expose controls for bin count, x-axis range (useful for RDD), residualisation toggle (raw vs. conditional), and whether to show the regression line. These map directly to `binsreg` / `rdplot` arguments in R. A collapsible "Plot options" panel below `ModelPlots` is the right surface.
**Invariants:** None.

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Open. "Zoom by observation count" means sampling a random subset for display when the scatterplot is dense. PlotBuilder already has a `plotRows` concept — add a slider for max-observations (defaulting to e.g. 5000) that subsamples via `plotRows.slice` or a DuckDB `USING SAMPLE` call when the dataset is large. This is a display-only operation and must never affect model estimation.
**Invariants:** Display limit ≠ computation limit (CLAUDE.md performance rule 5). Do not truncate `rawData` passed to estimation.

---

### UX / Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Open. The pipeline sidebar (`History.jsx`) lists all steps. Add a click handler that maps step type → Clean/Feature/Reshape/Merge tab + sub-section, then dispatches a `setActiveTab` + `setActiveSection` event. The mapping is mechanical — `filter`/`drop_na`/`fill_na` → Clean, `log`/`sq`/`dummy`/`mutate` → Feature, `group_summarize`/`pivot_longer` → Reshape, `join`/`append` → Merge. A `STEP_TYPE_TO_SECTION` lookup table (≈30 entries) covering `STEP_REGISTRY` is all that is needed.
**Invariants:** Pipeline step list must stay in sync with `registry.js` — use `STEP_REGISTRY` as the source, not a parallel list.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`
**Suggestion:** Open. Add a `country_code` pipeline step in runner.js with a bundled static lookup table (≈250 rows, iso2 / iso3 / country name). Expose it in FeatureTab as a column-transform with a mode selector (name→iso2, name→iso3, iso2→iso3, iso2→name, iso3→name). The lookup is entirely client-side, no network call required.
**Invariants:** New step types must be added to `runner.js` AND `registry.js` — the two must stay in sync (enforced by `pipelineReliabilityValidation.mjs`).

---

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** BugTriage says "open." Extend `artifactOrder.js` to add a `group` string to each `ArtifactId`. In `ModelBufferBar.jsx`, add a group label row in the buffer bar and a "New group / Move to group" context menu on each pinned model chip. Persist group assignments per project in IndexedDB alongside `artifactOrder`.
**Invariants:** Persistence in `indexedDB.js`, not localStorage (CLAUDE.md architectural invariant).

---

## UX / Design

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** BugTriage says "open." `FeatureTab.jsx` resets form state on unmount. Use `sessionStorage` keyed by dataset `pid` + section name to persist in-progress form state (draft case_when rows, partial mutate expression, etc.) across tab switches within the same session — matching the pattern `History.jsx` uses for pipeline-hide state. This is per-session only (never persisted to IndexedDB), which avoids stale draft conflicts across projects.
**Invariants:** IndexedDB for pipeline/data storage — `sessionStorage` is acceptable here for transient draft state within a single session only.

---

### General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`, line 11)
**Suggestion:** BugTriage says "open." TOUR_STEPS has 9 steps (see line 309 `{TOUR_STEPS.length}`). Features shipped since the last tour update include: N-way FE, Sun-Abraham/Callaway-Sant'Anna DiD, Goodman-Bacon decomposition, PlotBuilder tile/facet, Spatial pipeline integration, model buffer, split-pane divider, and the Credits system. Review each `TOUR_STEPS` entry and update step text + target selectors. CLAUDE.md rule: never write counts into help copy — name the things instead.
**Invariants:** Same inline-styles/C pattern as the rest of HelpSystem.jsx.

---

## Suggested action order

1. **Confirm browser validation for 4 code-complete items (OLS diagnostics, coef freeze, Heatmap, PlotBuilder resize)** — these are 3+ months old; either mark them ✓ in ClaudeFB.md or re-open with the specific repro. Unblocks tidying the bug backlog significantly.
2. **Fix Stata workbench completeness** (`stataScript.js`) — Stata is the primary replication tool for LMU academic users; a broken workbench directly undermines the institutional licensing pitch.
3. **Extend Simulate replication script** (`SimulateTab.jsx generateSimScript`) — a research simulation that can't be reproduced defeats the point of the tool; extending it to cover the downstream OLS/test loop closes a workflow gap users have hit directly.
4. **FE arbitrary categorical + interactions confirmation** (`ModelConfiguration.jsx`) — N-way FE math ships (18/18 R checks), but if the UI still limits pickers to two declared panel columns, the feature is unreachable for the primary academic use case.
5. **Multiple hypothesis test save for R** (`CoefficientTestPanel.jsx`, `ModelHypothesis.js`) — directly enables replication of Wald tests in PS2-PS7 workflows; `generateJointHypothesisScript` already exists.
