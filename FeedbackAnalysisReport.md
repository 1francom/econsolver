# Feedback Analysis Report — 2026-07-22

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **15 open bugs** (6 high · 6 medium · 3 low) · 5 code-complete pending browser validation · 2 needs-confirmation
- **8 open features**
- **0 open performance** (DuckDB + .dta fast-path resolved main crash; no new items)
- **0 open other**

Cross-reference notes:
- Many ClaudeFB.md items without strikethrough are confirmed resolved in the X5 reconciliation table and BugTriage — those are excluded here.
- 5 bugs are **code-complete** (committed) but awaiting Franco's browser validation before closing.
- 2 bugs are marked **"likely fixed"** in BugTriage (N-way FE shipped `045f5cda`/\#104) but unconfirmed in browser — included with a flag.
- Items in the fix-later category are tracked as low-priority.

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`) · `src/math/CallawayEngine.js`
**Suggestion:** The `CallawayCS` export block emits `did::att_gt()` + `aggte()` but has never been R-validated (the R harness `callawayRValidation.R` → `callawayBenchmarks.json` is listed as "pending Franco" in CLAUDE.md). Audit the emitted argument names against the `did` 2.x API: confirm `control_group`, `anticipation`, `base_period = "varying"`, and all four `aggte(type = ...)` calls are runnable. Also check that `treatCol` is being used as a first-treatment-year cohort column (what `att_gt`'s `gname` expects), not a binary indicator. Add a comment in the emitted R clarifying that `gname` must be 0 for never-treated units.
**Invariants:** `rScript.js` is pure JS export logic — no React, no Anthropic API calls. Keep all R-validation harnesses in `src/math/__validation__/`.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/services/export/plotScript.js` · `src/components/PlotBuilder.jsx` (line 1334) · `src/ExplorerModule.jsx` (line 2234)
**Suggestion:** `plotScript.js` already has `buildRLoadLine`/`buildPyLoadLine` imported (line 4) and used in `geoLoadLines` for the geo-plot variant. The regular `buildGgplot` path receives only `{ dfVar }` — no `filename` or `loadOpts`. Fix in two steps: (1) in `ExplorerModule.jsx` line 2234, add `allDatasets={allDatasets}` to the `PlotBuilder` mount; (2) in `PlotBuilder.jsx` `copyPlotScript`, pass `datasets: allDatasets` to `buildGgplot` / `buildMatplotlibPlot` so the function can call `geoLoadLines` (or a parallel helper) and prepend the correct `buildRLoadLine(filename, loadOpts)` call before the plot block.
**Invariants:** `plotScript.js` is pure JS. Load lines must honour `loadOpts` format (haven/readr/xlsx) — never hardcode `read.csv`. Non-destructive pipeline invariant unaffected.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/services/export/stataScript.js` · `src/components/modeling/CodeEditor.jsx` (line 74)
**Suggestion:** The 2026-07-18 HC2/HC3 pass fixed many Stata emission sites but BugTriage still shows this as open. Audit every `case` branch in `stataScript.js` for: (a) null/empty returns on recently-added estimators (Sun-Abraham, Callaway-Sant'Anna, N-way FE via `reghdfe`, CR2/CR3); (b) invalid Stata syntax introduced by the HC2 pass — `xtreg` with two cluster variables is a Stata syntax error; the `reghdfe` fallback added for two-way cluster may be incomplete for non-panel models. Run a 168-cell matrix check (all estimator × SE-type combos for Stata) and confirm no branch returns empty or throws.
**Invariants:** `stataScript.js` is pure JS; no React, no Anthropic API calls. `seType` must be honoured per the explicit-SE invariant.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (line 528, `group_summarize` case) · `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` supports single-column aggregations (mean/sum/sd/count…) per group row but has no mechanism for cross-group arithmetic. Two complementary actions: (1) immediately add a tooltip in `ReshapeTab.jsx` explaining the two-step idiom — `group_summarize → pivot_wider(names_from=treatment, values_from=Y_mean) → mutate(ATE = treated − control)` — at zero cost; (2) optionally add a `"diff"` aggregation fn in `runner.js` that operates on exactly two group levels and returns the difference of group means, using a `refLevel` field in the agg spec. If option 2 is taken, add `"diff"` to `registry.js` and all three script exporters.
**Invariants:** `runner.js` must replay on `rawData` — never mutate in place. Any expression eval must use the safe-eval path, not bare `eval()`. No React in `runner.js`. New step type requires `registry.js` sync (T5 enforces this).

---

### [high] · Model · 2026-07-09 *(likely fixed — needs browser confirmation)*
**Feedback:** Add options for fixed effects, not just entity and time, but also variables like country, state, sector — every categorical variable
**File:** `src/math/PanelEngine.js` (`runFEMulti`) · `src/components/modeling/ModelConfiguration.jsx` (`FEColumnPicker`)
**Suggestion:** N-way FE shipped in `045f5cda` (#104) and is validated. BugTriage marks this "likely fixed — needs confirmation." Browser-test: pick a `sector` column (not declared in the Panel tab) as a FE dimension in `FEColumnPicker`. If the picker only shows columns from `panel.feCols` (entity + time), add an "Add dimension +" button that lists all non-numeric `headers` and merges the selection into the estimation's FE array without mutating the stored panel declaration.
**Invariants:** `seType` must be passed explicitly from the sidebar; `FEColumnPicker` must not default-set SE type. Zero React imports in `src/math/`.

---

### [high] · Model · 2026-07-09 *(likely fixed — needs browser confirmation)*
**Feedback:** Add also FE with interactions
**File:** `src/math/PanelEngine.js` · `src/components/modeling/ModelConfiguration.jsx` (`FEColumnPicker` interaction chips)
**Suggestion:** FE interactions shipped in `045f5cda`. Browser-test end-to-end: Panel tab → select two columns as an interaction pair → Update panel index → Model tab → `FEColumnPicker` shows `A×B` chip → selecting it runs `runFEMulti` with the interaction as a FE dimension. If the chip is absent, check that `PanelTab.jsx` writes `panel.interactionCols` on "Update panel index" and that `DataStudio` persists it through `panelInfo` state.
**Invariants:** Zero React imports in `src/math/`. SE type remains explicit.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js` (line 168, `\begin{tabular}{${colFmt}}`)
**Suggestion:** `buildStargazer` emits a fixed-width `tabular` with no overflow handling. When model count > 4, the table overflows the page. Wrap the `tabular` in `\resizebox{\linewidth}{!}{ ... }` (requires `\usepackage{graphicx}`) when `models.length > 3`. Alternatively, add a `longtable` option for very wide outputs. Emit a `% Requires: \usepackage{graphicx}` preamble comment. The flag can be auto-detected (`models.length > 3`) or exposed as an `options.autoResize` parameter.
**Invariants:** `latexTable.js` is pure JS with no React or side effects.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` shows only the most recent test result and has no history. Add a `savedTests: Array<{ type, spec, result }>` array to component state; after each test run, push to it. Render saved tests as a scrollable list with a "Copy all as R block" button that calls the existing `copyScript` path for each test and concatenates them. `ModelHypothesis.js` already generates a valid `car::linearHypothesis` call per test — no engine changes needed.
**Invariants:** `savedTests` is React state only — no IndexedDB persistence required. Math files must remain zero-React.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`) · `src/services/AI/AIService.js` (`interpretRegression`)
**Suggestion:** Before building the user prompt in `interpretRegression`, inspect `spec.yVar` and each `spec.xVar` for log-transformation markers (`ln_`, `log_`, `_log` prefixes/suffixes, or log steps in the pipeline). Inject an explicit `VARIABLE TYPES` block into the user prompt listing which variables are logged. Add dedicated rules: "when both Y and X are logged, the coefficient is a constant elasticity; when only Y is logged, the coefficient is a semi-elasticity (×100% gives the %-change)." Also offer a pure-client elasticity toggle: `ε = β × mean(X)/mean(Y)` at the mean, computed from the already-available `result.means` — no additional AI call.
**Invariants:** All Anthropic calls must go through `AIService.js`. The detection logic belongs in `AIService.js`, not in any `src/math/` or `src/core/` file.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js` (lines 203–213, joint Wald test) · `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already computes `chiSq` and `chiSqPval` at lines 203–213 for the joint Wald test, and `CoefficientTestPanel.jsx` already displays them (line 211). For the single-coefficient test, add `chiSq: stat ** 2` and `chiSqDf: 1` to its return value (since `t²(df₁,df₂) → χ²(1)` asymptotically). Display `χ²(1) = t²` in the single-test result panel alongside the existing t-stat row. For the joint test panel, also surface `F = chiSq / q` for parity with R's `linearHypothesis` dual output.
**Invariants:** `ModelHypothesis.js` must remain zero-React. This is a display enhancement on values already computed.

---

### [medium] · Data · 2026-06-14 *(fix-later)*
**Feedback:** Parse error on line 1583 of a CSV — malformed row aborts entire import
**File:** `src/DataStudio.jsx` (CSV parse branch) · `src/services/data/parsers/`
**Suggestion:** Wrap the CSV parse loop in per-row try/catch: on error, record `{ _rowError: true, _rawLine, _rowIndex }` and continue. After parse, if error rows exist, surface a dismissible warning banner ("N rows skipped — row numbers: [list]") and exclude error rows from the Arrow schema passed to DuckDB. Offer two recovery options: "Skip bad rows" (re-run with DuckDB `IGNORE_ERRORS=TRUE`) or "Review in editor" (open raw line in a modal). Mirror the resilient approach already used in the DTA parser hardening.
**Invariants:** Error rows must NOT propagate into `rawData` or the DuckDB Arrow schema. Skip list is transient state (component `useState`) — do not persist to IndexedDB. Non-destructive pipeline: `rawData` must reflect what was actually parsed.

---

### [low] · Clean · 2026-06-14 *(fix-later)*
**Feedback:** R-style `%in% c("a","b")` membership syntax not accepted in filter expression UI
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder) · `src/services/exprEvalService.js`
**Suggestion:** Add a pre-parse rewrite in the filter expression input handler that transforms `col %in% c("a","b")` → `["a","b"].includes(col)` before the expression is stored in the pipeline step. Use a regex with proper quoted-string tokenisation. Surface `%in%` as a hint in the filter UI autocomplete. The stored step spec must record the rewritten JS-compatible expression for replay correctness.
**Invariants:** Stored `filter` step must use JS-evaluable syntax so it can replay on `rawData`. Non-destructive pipeline invariant applies.

---

### [low] · Model · 2026-06-14 *(fix-later)*
**Feedback:** RDD result lacks an R-style `summary(rdrobust(...))` compact table
**File:** `src/components/ModelingTab.jsx` (RDD result section) · a new `RDDSummaryBlock` component
**Suggestion:** `runSharpRDD` already returns `bw`, `N_left`, `N_right`, `tau`, `se`, `pval`, `ci95`, kernel, and polynomial order. Add a `RDDSummaryBlock` presentational component that renders a compact two-column block mirroring rdrobust output: bandwidth, kernel, poly order, N (Full / Effective Left / Right), Conventional/Robust rows. Wire it above the coefficient table in `ModelingTab.jsx`. No math changes; no `EstimationResult.js` changes.
**Invariants:** Display-only component; must use inline styles via `C`. No imports from `src/math/` inside the UI component.

---

### [low] · General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array, ~9 entries)
**Suggestion:** `TOUR_STEPS` predates several shipped features: Spatial tab pipeline integration, N-way FE, Sun-Abraham/CS DiD estimators, the model buffer bar, NL command bar, and AI coach. Audit each entry against the current 7-tab layout in `WorkspaceBar.jsx` and `APP_CAPABILITY_MAP`. Update the Model step text to reflect the current estimator count. Add steps for Spatial and Report if missing. Low-effort text-only change.
**Invariants:** Per CLAUDE.md: any new workspace tab added to the tour must also have an entry in `APP_CAPABILITY_MAP` in `src/services/AI/appCapabilityMap.js`.

---

### Code-complete — browser validation pending

| # | Bug | Commit | File |
|---|-----|--------|------|
| CC-1 | Plot Builder resize + PNG export | `35f4b5a6` | `src/components/PlotBuilder.jsx`, `ModelingTab.jsx` |
| CC-2 | OLS Y~Ŷ / residual-fitted / Q-Q plots | `35f4b5a6` | `src/components/modeling/ResidualPlots.jsx`, `ModelingTab.jsx` |
| CC-3 | Diagnostics frozen to `result.spec` (no sidebar mutation) | `35f4b5a6` | `src/components/ModelingTab.jsx`, `ModelPlots.jsx` |
| CC-4 | Spatial heatmap restored (Leaflet KDE layer) | `5e105529` | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` |
| CC-5 | HC2/HC3 export: R emits `sandwich::vcovHC`, Stata `vce(hc2)` | 2026-07-18 | `src/services/export/rScript.js`, `stataScript.js`, `pythonScript.js` |

**Action needed:** Franco should browser-test all five in one session and update BugTriage status from "code-complete" to "fixed" or reopen if issues are found.

---

## Features

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When a user defines a pure scalar expression (e.g. `MC = 0.5`) in the equation pad, detect it as a constant (no axis variable) and render it as a horizontal dashed `<line>` spanning the x domain at `y = value`, labeled with the constant name. The existing SVG canvas already draws function curves; add a branch in the render loop for numeric scalars. No new CDN dependency needed.
**Invariants:** Inline styles via `C`. No external charting libraries — use the existing SVG canvas. No imports from `src/math/` unless using an already-imported function.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider so users can find variables faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text filter `<input placeholder="Filter variables…">` at the top of the variable list in `VariableSelector.jsx`. On keystroke, filter the displayed `headers` to case-insensitive substring matches. Hide the input when `headers.length ≤ 10` to avoid visual noise on small datasets. Pure display change, local to the component — no state lifted to parent.
**Invariants:** Inline styles via `C` from `src/components/modeling/shared.jsx`. No external autocomplete libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx` (legend section) · `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set, extend the legend to show per-series row counts `(n = X)` and optionally the mean of the Y aesthetic. For the Observable Plot legend, post-process the SVG `<text>` nodes to append counts — or render a compact supplementary stat table below the chart using `C`-styled inline divs. In `ExplorerModule.jsx`'s bar/histogram charts, counts are already in the `items` array — surface them as `(n=X)` text labels.
**Invariants:** Inline styles via `C`. No external tooltip or legend libraries.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step to jump to its subsection automatically
**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`
**Suggestion:** Add a small "✏ Edit" icon to each step card in `History.jsx` that fires a new `onJumpToStep(step)` callback. `WranglingModule` maps `step.type` to the correct tab using the `STEP_REGISTRY` category field (cleaning → "clean", feature → "feature", reshape → "reshape", merge → "merge") and calls the tab-switcher. Start with tab navigation only — form pre-population per step type can follow incrementally. Clicking "Jump to" must not trigger a pipeline replay.
**Invariants:** Non-destructive pipeline — the existing step array must not be mutated. Any replacement must go through the standard `onAdd` → `dispatchStep` path and be undoable.

---

### Model · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Add a `groupId: string | null` field to the artifact order entries in `artifactOrder.js`. In `ModelBufferBar.jsx`, add a "New group" control that creates a named group; render artifacts under collapsible group headers; limit comparison to the active group. Persist `groupId` alongside artifact order in the `v9 model_buffer` IndexedDB store. MVP option: label chip on each pinned model card without drag-and-drop.
**Invariants:** Persist to `indexedDB.js` only — never `localStorage`. Inline styles via `C`.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections (e.g. case_when lost when navigating away)
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` stores `cwNN`, `cwCases`, `cwDefault`, and similar drafts in component-local `useState`, which resets on unmount. Write drafts to `sessionStorage` keyed by `${pid}::caseWhenDraft` on every change (debounced); hydrate on mount; clear on successful `onAdd`. Apply the same pattern to the `if_else` panel and other multi-field forms. Show a small teal dot on the Feature tab when an unsaved draft exists.
**Invariants:** Use `sessionStorage` scoped by `pid` for transient drafts only — not IndexedDB and never `localStorage` (deprecated). Draft state must never flow into `runner.js` as a committed pipeline step.

---

### Data · 2026-07-17
**Feedback:** Add arrange/sort functions in the data viewer
**File:** `src/ExplorerModule.jsx` (data table, `<thead>` area)
**Suggestion:** Add `sortCol: string | null` and `sortDir: "asc" | "desc"` to local state. Clicking a column header cycles `unsorted → asc → desc`. Apply a `[...displayRows].sort(...)` on the 200-row display slice before rendering. Add a ↑↓ glyph in active header cells using `C.teal`. For DuckDB-backed tables, companion the sort with `ORDER BY col dir LIMIT 500` via `duckdb.js` so the preview reflects the full dataset order. This is display-only — it must NOT add an `arrange` pipeline step or modify `rawData`.
**Invariants:** Must not modify `rawData` or add pipeline steps. Display sort is view-only. The `arrange` step already exists for users who want a persistent sort in the pipeline.

---

### Clean · 2026-07-19 *(needs scoping with Franco)*
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx` · `src/pipeline/runner.js` · new `src/services/data/countrycodes.js`
**Suggestion:** Implement as a `recode_country` pipeline step with a bundled ISO-3166 crosswalk JSON file (~250 rows, ~15 KB). Step takes `sourceCol` (auto-detect iso2/iso3/name from values) and `targetFormat` (`iso2 | iso3 | name`). Runner.js looks up each value; unmatched values become `null`. Expose in `FeatureTab.jsx` under a new "Country Codes" chip in the Formatting section. **Design decision pending:** fuzzy name matching (using existing `fuzzyGroups`) risks false matches for ambiguous country variants — confirm scope with Franco before enabling. Exact-match-only is safe to ship immediately.
**Invariants:** New pipeline step requires: `runner.js` case + `registry.js` entry + UI panel + translators in `rScript.js`/`pythonScript.js`/`stataScript.js`. Crosswalk JSON must live in `src/` — no runtime fetch. `pipelineReliabilityValidation.mjs` T5 will catch any registry drift.

---

## Suggested action order

1. **Fix plot replication code — add dataset load lines** (`PlotBuilder.jsx` + `ExplorerModule.jsx:2234` + `plotScript.js`) — confirmed gap, ~30-line fix using the existing `geoLoadLines` pattern; unblocks reproducibility for every plot export.
2. **Fix CS DiD 2021 replication code** (`rScript.js` `CallawayCS` case) — only committed estimator with an unvalidated export; run the generated script in R and diff against `CallawayEngine.js` output to isolate argument mismatch.
3. **Audit Stata replication scripts end-to-end** (`stataScript.js`) — the HC2/HC3 pass fixed many sites but BugTriage still shows this open; 168-cell matrix check will catch remaining invalid syntax.
4. **Browser-validate all 5 code-complete items** (CC-1 through CC-5 table above) — zero implementation; Franco clears all 5 in one session and closes them in BugTriage.
5. **group_summarize cross-group UX** (`ReshapeTab.jsx` tooltip) — at minimum, add the two-step ATE idiom tooltip at zero engineering cost; `postAggs` extension is optional.
6. **LaTeX table auto-width** (`latexTable.js`) — one-line `\resizebox` wrapper; high polish for multi-model thesis tables.
7. **Persist case_when draft** (`FeatureTab.jsx` → `sessionStorage`) — ~15-line fix; high UX impact for users writing multi-case transforms.
8. **Add data viewer column sort** (`ExplorerModule.jsx`) — client-side sort on preview slice; common expectation for any data table.
9. **Browser-confirm N-way FE categorical picker and FE interactions** (`ModelConfiguration.jsx`) — "likely fixed" since `045f5cda` but unverified; confirm or finish wiring the arbitrary-column FE dimension picker.
10. **Update guide tour** (`HelpSystem.jsx`) — pure text update; high value for LMU onboarding demo.
