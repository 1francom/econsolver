# Feedback Analysis Report — 2026-08-09

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **13 open bugs** · **11 open features** · **1 open performance** · **1 open other**

Cross-reference note: all items marked ✓ or with BugTriage status = `fixed` / `code-complete` are excluded. Items marked `likely fixed` in BugTriage are also excluded from action order. Four previously code-complete items (Plot Builder resize, residual plots, spatial heatmap, diagnostics freeze) are pending Franco's browser validation and are not re-listed here.

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work  
**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS":`), `src/math/CallawayEngine.js`  
**Suggestion:** The `att_gt()` call in rScript.js reads `yname`, `gname`, `idname`, `tname` from `model` (the spec root), but `CallawayEngine` stores these under `result.spec.*`. Trace what `model` is when `transpileModel` is called for a `CallawayCS` result — if `model.treatCol` resolves to undefined, the call becomes `gname = "undefined"` and R throws immediately. Add a structured smoke test: generate a CallawayCS script, parse it with a regex, and assert that none of the field values are the string `"undefined"`. If the fix is confirmed to be field access, update the destructuring to reach through `model.spec ?? model`.  
**Invariants:** All Anthropic API calls must still flow through `AIService.js` only; no new math in export files.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/ExplorerModule.jsx` (PlotBuilder mount, around line 443), `src/components/PlotBuilder.jsx` (prop `scriptPreamble`, line 1366)  
**Suggestion:** `PlotBuilder` accepts a `scriptPreamble` function that, when provided, prepends a dataset-load block to the exported script. ExplorerModule never passes this prop, so the "Copy script" button in the Explore tab always emits a scriptless fragment. Fix: in ExplorerModule's PlotBuilder render, pass `scriptPreamble={(lang) => buildLoadPreamble(lang, activeDataset)}` — `buildLoadPreamble` should call `buildRLoadLine` / `buildPyLoadLine` (already in `src/services/export/loadLine.js`) and set the correct `dfVar`. The Stata path already emits a documented "assumes dataset loaded" comment in `buildStataPlot`; it should emit the actual load line instead.  
**Invariants:** `src/math/` and `src/core/` must have zero React imports — this change lives in ExplorerModule and loadLine.js, both outside those directories. No new raw `fetch` to Anthropic.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`  
**Suggestion:** Identify which Stata blocks fail to render by testing each estimator type against `generateStataScript`. Common failure points: (1) `factor_interactions` step translator emits unsupported Stata syntax; (2) `xtreg` with two-way cluster routes to `reghdfe` but the package may not be in the install block; (3) newer SE types (CR2/CR3) emit an explicit "no Stata equivalent" note that may confuse users expecting runnable code. Run the 168-cell SE × estimator matrix already referenced in BugTriage to find which cells emit errors or empty strings, and patch each failing `case` in `stataScript.js`.  
**Invariants:** Stata export must never emit code it knows will fail silently — a commented note is always better than broken syntax. The pipeline export invariant (steps replay on rawData) is not affected by export scripts.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group  
**File:** `src/pipeline/runner.js` (line 528, `case "group_summarize":`), `src/components/wrangling/ReshapeTab.jsx`  
**Suggestion:** The current `group_summarize` step supports `mean/sum/count/min/max/sd/median/quantile` per column but has no cross-group arithmetic. Users want a single step to produce `mean(y | treatment=1) - mean(y | treatment=0)` as a scalar. Two approaches: (a) Add a post-aggregation `expr` field to the step spec, evaluated over the result rows via `mutate`-style logic (`a.mean_y_1 - a.mean_y_0`); (b) Document and expose the pipeline pattern `[group_summarize → mutate]` in ReshapeTab's UI with a helper template. Option (b) is non-breaking and respects the non-destructive replay invariant. Add the aggregated-difference template to the `group_summarize` UI with a one-click "Difference in means" preset.  
**Invariants:** Pipeline steps must always replay on `rawData` — the step spec must be fully deterministic from its stored JSON, with no side effects.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js`  
**Suggestion:** `latexTable.js` emits a fixed `\begin{tabular}` block. For tables with many models (>5 columns), the table overflows `\textwidth`. Wrap the `tabular` in `\resizebox{\textwidth}{!}{...}` or prepend `\begin{adjustbox}{max width=\textwidth}` / `\end{adjustbox}` (requires the `adjustbox` LaTeX package). Count emitted columns at generation time — if column count > 5, add the wrapper. Include `\usepackage{adjustbox}` in the preamble comment block already present in the output. This is a pure string-generation change.  
**Invariants:** None — this is a pure export utility function with no React, math, or AI involvement.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** The joint Wald tab already supports multi-coefficient testing (χ² display confirmed in code at line 210). The gap is *persistence* — test selections are pure component state and are lost on navigation. Add an "Export test" button (next to the existing "copy test code" buttons) that saves the current single or joint test definition (model id, selected terms, null values, alternative) to IndexedDB under a `hypothesis_tests` store keyed by `pid`. Restore the list on remount. Add a "Copy all as R" button that emits a `linearHypothesis(fit, ...)` block per saved test.  
**Invariants:** Use `indexedDB.js` for persistence — not localStorage. The IndexedDB schema already has `coach_chats` and `model_buffer` stores; add `hypothesis_tests` following the same pattern.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/ExplorerModule.jsx` (time series section, around lines 157–165)  
**Suggestion:** The time series chart is rendered as an inline SVG/Observable plot without label controls. Add a collapsible options row (matching the pattern used by `SvgBarChart`'s scale toggle added 2026-07-20) that exposes title, x-axis label, and y-axis label inputs. Pass these through as `Plot.plot({ title, x: {label}, y: {label} })`. Store them in local component state (no persistence needed). Also expose them in the `renderPinnedPlot` path so saved comparisons retain labels.  
**Invariants:** Styling via inline styles only — `C` object from `shared.jsx`. No new CSS files.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`, around lines 99 and 353)  
**Suggestion:** The prompt already handles log–log (elasticity) and log–level (semi-elasticity) in rules at lines 99 and 353. The gap is *automatic detection* when both Y and X are log-transformed columns: the AI currently only applies the elasticity rule if the column names contain `ln_` or `log_`. Add a detection rule: if `yVar` or `xVars` contain `log_`, `ln_`, `l_` prefixes, or end in `_log`, flag those in the user context block. Also add a rule for level–log (marginal elasticity: `100·β / mean(y)`) which is missing. Changes are to `INTERPRET_REGRESSION_PROMPT` and the user-context block builder in `AIService.js` (`interpretRegression`). IMPORTANT: prompt changes must preserve the `SHARED_CONTEXT` cached block — only modify the `user` block content passed to `callClaude`.  
**Invariants:** All Anthropic API calls must go through `AIService.js` only. `SHARED_CONTEXT` is the cached block — do not enlarge it beyond the 2048-token Haiku cache minimum.

---

### [medium] · UI · 2026-07-17
**Feedback:** Missing chi-square variant for linear hypothesis tests  
**File:** `src/components/modeling/CoefficientTestPanel.jsx` (line 210), `src/math/ModelHypothesis.js`  
**Suggestion:** The joint Wald test tab already emits `χ²(df)` at line 210. The likely gap is (a) chi-square is not shown in the *single-coefficient* test view (which currently displays only t/z), or (b) there is no F-statistic equivalent shown alongside χ² in the joint tab. For (a): add an F-version row `F = χ²/df, df2 = model df` when `result.statLabel === "t"`. For (b): `waldTest()` returns both `chiSq` and `chiSqPval` but not F — add `F: chiSq / q, pF: 1 - fCDF(chiSq/q, q, df)` to the return object and display it next to χ² in the joint result block. This is a UI-only addition; math is correct.  
**Invariants:** `src/math/ModelHypothesis.js` is in `src/math/` — zero React imports allowed. The F-stat computation must use the same `fCDF` already in `LinearEngine.js` (import via the math barrel `src/math/index.js`).

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` stores a flat ordered array of artifact IDs. Extend the schema to support named groups: `{ groups: [{ id, name, artifactIds: [] }], ungrouped: [] }`. Add a "New group" button in `ModelBufferBar.jsx` that prompts for a name (inline text input, no modal needed) and moves selected pinned models into it. Render groups as collapsible sections in the buffer bar. Persist the group structure in the `pipelines` IndexedDB store under `artifactOrder_<pid>` (same key, extended schema). The compare selection logic must be scoped within a group by default, with an "across groups" toggle.  
**Invariants:** Use `indexedDB.js` — not localStorage. Inline styles only via `C` object.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean, and coming back to case_when to continue writing  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** FeatureTab uses local React state for the active transformation panel (case_when cases, mutate expression, etc.). When the user navigates away (tab change in WranglingModule), this state is destroyed. Add a `draft` ref on WranglingModule or FeatureTab that captures the serializable subset of the open sub-panel's state on `onBlur`/tab change, and restores it when the sub-panel is re-opened within the same session. Use `sessionStorage` keyed by `pid + "_featureTab_draft"` (sessionStorage is acceptable for ephemeral draft state as it is not long-term pipeline/data storage — only `pipeline` and `rawData` must use IndexedDB). Clear the draft when the step is successfully added via `onAdd`.  
**Invariants:** IndexedDB is required for pipeline/data persistence. Ephemeral UI draft state (not a pipeline step) may use sessionStorage.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** The data viewer already fetches pages via `getTablePage` (which supports `ORDER BY` in SQL per the existing comment). Add a sortable header click handler: clicking a column header sets `[sortCol, sortDir]` state; subsequent `getTablePage` calls include `ORDER BY "${sortCol}" ${sortDir}` in the SQL. For non-DuckDB datasets (JS path), apply `[...rows].sort(...)` over the current preview. Show an ascending/descending indicator on the active sort column. This matches the pattern already used in `getFilteredRowCount` which passes a `WHERE` clause.  
**Invariants:** DuckDB is the data boundary for large datasets — sort must be pushed into SQL, not applied to the 500-row preview object. `SELECT *` on large tables must be avoided; the existing paginated `getTablePage` already handles this.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array, line 11)  
**Suggestion:** The tour step at line 40 still lists "14 estimators: OLS · WLS · FE · FD · TWFE · 2×2 DiD · 2SLS/IV · Sharp RDD · Logit · Probit · GMM · LIML · Synthetic Control" — this count and list are outdated (Sun-Abraham, Callaway-Sant'Anna, Event Study, Panel LSDV, Poisson FE, Fuzzy RDD, N-way FE are all live). Per CLAUDE.md convention: **never write a count into help copy** — list the estimators by name or use a phrase like "a growing suite of estimators." Also update the Explore step (line 34) to mention facets, tile geom, and the ACF/PACF view; update Calculate (line 58) to mention limits. Do not change the step `id` values — they are used as tour navigation anchors.  
**Invariants:** No architectural constraints apply. No counts in help copy (CLAUDE.md working convention).

---

## Features

### Explore · 2026-05-19
**Feedback:** Add filter(`regionname %in% c("Spain", "Basque Country")`) for filter option — R-style `%in%` membership syntax  
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`  
**Suggestion:** `predicate.js` already has an `in` operator (`evalPredicate` handles array membership). The gap is that the NL command bar and filter expression input don't accept the R `%in% c(...)` syntax as an alias. Add a pre-parse step in `CleanTab.jsx`'s filter expression field (or in the NL pipeline prompt) that translates `col %in% c("a","b")` to a canonical `{op:"in", value:["a","b"]}` predicate. This is a UX convenience alias, not a new operator.  
**Invariants:** `predicate.js` is the single owner of condition evaluation — never re-implement filter operators in component code. The canonical `in` operator already exists; only add a surface-level alias parser.

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — Call, LATE, SE, bandwidth, kernel  
**File:** `src/math/CausalEngine.js`, `src/components/modeling/` (results display)  
**Suggestion:** `runSharpRDD` already returns LATE, SE, bandwidth (h), and kernel. Add a compact summary card component (following the BaconPanel pattern) that renders these fields in a fixed-layout grid mimicking `summary(rdrobust)` output. No new estimation logic needed — read from `result.late`, `result.se`, `result.bandwidth`, `result.kernel`, and the IK bandwidth formula parameters. Wire into `ModelPlots.jsx` or the RDD results section.  
**Invariants:** No React in `src/math/`. New display component lives in `src/components/modeling/`.

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**Suggestion:** Add a per-layer bounding-box or attribute filter in `GeoLayerConfig` — a "Filter rows" input that evaluates against the layer's dataset rows using `evalPredicate` from `predicate.js`. Filtered rows are passed to `mkGeoLayer` instead of the full dataset. This reuses the existing predicate system and does not require a new pipeline step. For attribute filters, expose the same operator vocabulary as CleanTab's `FilterBuilder`.  
**Invariants:** `predicate.js` is the canonical filter evaluator — do not re-implement operators. Spatial filter state is ephemeral view state, not a pipeline step.

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", etc. regardless of actual column names  
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/export/latexTable.js`  
**Suggestion:** Add an editable label map in the model result state: `{[termId]: customLabel}`. Render each coefficient row in the results table with a double-click-to-edit label (inline `contenteditable` span or small text input on focus). Pass the label map through to `latexTable.js` as `labelOverrides`. This is session-scoped — persist in the pinned model object in the model buffer. No IndexedDB schema change needed (the model buffer already stores arbitrary spec fields).  
**Invariants:** Inline styles only via `C` object. Label map stored in model buffer (IndexedDB) — not localStorage.

### Calculate · 2026-06-19
**Feedback:** Add limits (e.g. `lim x→∞ f(x)`)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The symbolic CAS already handles derivatives and integrals. Add a "Limit" mode that exposes point and direction inputs (x → a, from left/right/both) and calls a limit-evaluation routine. For finite limits, substitute the value numerically with a delta approach or use the CAS `limit()` function if available. For `x → ∞` / `x → -∞`, evaluate at a large substitution value. Render the result in the same LaTeX output area.  
**Invariants:** No React in `src/math/` — if a `computeLimit` helper is added there, it must be pure JS. Styling via inline styles only.

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The function graph canvas already supports multiple equation curves. A constant `c = k` is equivalent to `f(x) = k` — add a toggle that treats a numeric assignment `c = 5` as a horizontal line at y = 5 and includes it in the plot with a distinct dash style. This requires only a parser addition: if `expressionText.match(/^[a-zA-Z]\s*=\s*[\d.]+$/)`, add a horizontal line geom instead of a function curve.  
**Invariants:** Inline styles only. No new external dependencies — the canvas already uses Observable Plot or SVG directly.

### General · 2026-07-05
**Feedback:** Add plots with 3 axis (3D plots)  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot (the current renderer) does not support 3D. A 3D scatter/surface would require either Plotly.js (large CDN dependency) or a WebGL canvas. This is a significant investment — recommend a scoped first step: add a "Bubble" geom that encodes a third variable as point size, delivering the "three variables" use case without a full 3D renderer. True 3D plots should be deferred to after core feature-parity work is complete.  
**Invariants:** If adding a CDN dependency, ensure its fetch origin is in `vercel.json`'s `connect-src` as well as `script-src` (per the documented CSP bug pattern in CLAUDE.md). No raw `fetch` to Anthropic.

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** `VariableSelector` renders the Y/X/W variable lists from `xwCols`. Add a text input above each column list that filters the rendered chips by substring match. Store the filter string in local component state (no persistence). This is a pure UI change — the underlying `xVars`/`yVar`/`wVars` arrays are unchanged; only display is filtered. The "Add all matching" affordance should be considered but kept optional (it changes the selection).  
**Invariants:** Inline styles only via `C` object from `shared.jsx`. No new libraries.

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/ExplorerModule.jsx` (categorical bar and scatter sections), `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set in PlotBuilder, the legend currently shows color swatches but no value counts or percentage labels. Add an optional "Show values on legend" toggle that appends `n=K` or `K%` to each legend entry (computed from the data). For the ExplorerModule bar chart, the `scale_data` toggle already exists — extend it to add per-category count labels on the bars when `color by` is active.  
**Invariants:** Inline styles only. Label data must be pre-computed from the dataset (not fetched); for DuckDB-backed datasets, compute via a `GROUP BY color_col COUNT(*)` SQL query.

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx` (new case), `src/pipeline/runner.js` (new step type), `src/pipeline/registry.js`  
**Suggestion:** Add a `country_code` pipeline step with spec `{ col, fromType:"name"|"iso2"|"iso3", toType:"name"|"iso2"|"iso3", outCol }`. Include a small static lookup table (~250 rows, ~8KB) as a JS const in a new `src/pipeline/countryLookup.js` file. Wire into FeatureTab under a "Country" section and into `runner.js`/`registry.js`. Add translators for R (`countrycode` package), Python (`pycountry`), and a Stata note. **Registry and runner must be kept in sync** (enforced by `pipelineReliabilityValidation.mjs`).  
**Invariants:** New step type must be added to both `runner.js` AND `registry.js`. Registry and runner sync is enforced by `pipelineReliabilityValidation.mjs` — run it after adding the step. SE type passing convention does not apply to this step.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`  
**Suggestion:** Per CLAUDE.md and BugTriage, the DuckDB Fase X4 fix is code-complete but **browser benchmark pending Franco**. The fix routes large files through DuckDB with OPFS Parquet persistence and a 500-row UI preview. This item should transition to `validated` once Franco runs the browser benchmark on a 900k-row CSV. No new code change suggested — confirm the existing fix works and update BugTriage status.  
**Invariants:** Data must never leave the browser (privacy-first). DuckDB is the data boundary for large datasets. OPFS Parquet cache requires `extensions.duckdb.org` in `connect-src` (see documented CSP bug).

---

## Other

### Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line 1583 · Original Line: China,CHN,2010,2425.47... (truncated in source)  
**File:** `src/services/data/parsers/` (CSV path), `src/DataStudio.jsx`  
**Suggestion:** BugTriage marks this "fix-later" with a note for a resilient-import pass. The error suggests a row with a field containing an unescaped comma or a truncated line. Add a resilient CSV parser mode: on a parse error, log the offending row number and value, skip the row, and continue rather than aborting the entire import. Surface a post-import banner: "2 rows skipped due to parse errors — row 1583, row 4102. Download error log?" This pattern exists for the `.dta` format (column warnings) and should be applied uniformly.  
**Invariants:** The pipeline replays on `rawData` — if `rawData` was partially loaded (rows skipped), that partial state must be stable and deterministic on replay. The skipped-row list should be stored in `rawData._parseWarnings` for display in the Data Quality Report.

---

## Suggested action order

1. **Plot replication code missing load lines** (`src/ExplorerModule.jsx` + `plotScript.js`) — small change, high user pain: exported scripts don't run without a load line.
2. **Stata workbench broken** (`src/services/export/stataScript.js`) — affects every Stata-using user; run the 168-cell SE matrix to locate exact failures.
3. **CS DiD replication code broken** (`src/services/export/rScript.js` `case "CallawayCS"`) — trace spec field access; a likely `undefined` field lookup.
4. **group_summarize can't compute difference in means** (`src/pipeline/runner.js`) — high request frequency; "ATE" is the primary use case for DiD users.
5. **Data viewer sort/arrange** (`src/ExplorerModule.jsx`) — low implementation cost (SQL `ORDER BY` already in the page query), high daily-use impact.
6. **Save hypothesis tests to replicate in R** (`src/components/modeling/CoefficientTestPanel.jsx`) — adds IndexedDB persistence to existing working Wald panel.
7. **Variable searcher in model sidebar** (`src/components/modeling/VariableSelector.jsx`) — pure UI filter, no math changes.
8. **LaTeX table auto-resize** (`src/services/export/latexTable.js`) — a single `\resizebox` wrapper, minimal risk.
9. **Guide tour update** (`src/components/HelpSystem.jsx`) — text-only, zero risk.
10. **Country code step** (`src/pipeline/runner.js` + `registry.js` + `FeatureTab.jsx`) — small static lookup table, adds a full pipeline step.
