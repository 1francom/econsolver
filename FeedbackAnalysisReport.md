# Feedback Analysis Report — 2026-08-21

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary
- **12 open bugs** · **12 open features** · **3 open UX/design** · **1 open performance** · **2 open other**

Items marked "code-complete / browser-validation pending" in BugTriage are noted inline — they count as resolved for the purposes of this report but need Franco's sign-off before closing.

---

## Bugs

### high · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 (Callaway & Sant'Anna) does not work  
**File:** `src/services/export/rScript.js`, `src/math/CallawayEngine.js`  
**Suggestion:** `CallawayEngine.js` is listed in the Estimators table as "R validation pending" and the BugTriage entry is explicitly `open`. The R export path in `rScript.js` needs a dedicated `"CallawayDiD"` branch that emits `csdid::att_gt()` / `csdid::aggte()` with the correct `time`, `idname`, `tname`, `gname` arguments. Validate round-trip against an R fixture before merging.  
**Invariants:** All Anthropic calls via `AIService.js` only; SE type must be passed explicitly.

---

### high · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js` (1410 lines)  
**Suggestion:** `stataScript.js` is the longest single export file and carries the highest surface area for regressions. Audit each estimator's Stata emitter against its R counterpart and confirm: (1) two-way cluster routes to `reghdfe` (already in the 2026-07-17 fix notes), (2) FE with N-way dims uses `absorb()`, (3) Poisson FE uses `ppmlhdfe`. Reproduce the workbench failure scenario (which estimator / SE combo triggers the broken code) to pin the regression before fixing.  
**Invariants:** None directly, but Stata emitters may not use `_duckdb` meta — export must be purely data-driven from the result object.

---

### high · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group_summarize, like ATE as difference in means between treatment and control group  
**File:** `src/pipeline/runner.js` (`group_summarize` case ~line 528), `src/components/wrangling/ReshapeTab.jsx`  
**Suggestion:** Per CLAUDE.md (2026-08-07 bug entry), the capability exists via `twoSampleMeanTest` and `SampleTestPanel` already has a two-sample mode — the gap is the **input shape**: data arrives as one outcome column + one group column, not two separate columns. Add a `splitByGroup` helper to `ReshapeTab`'s group_summarize UI that lets users pick a group column alongside the statistic, and wire a `diff_means` aggregate type in runner.js that computes `mean(y | g=1) − mean(y | g=0)` in a single pass. Do NOT also wire it for paired tests or correlation (different semantic).  
**Invariants:** Non-destructive pipeline: result replays on `rawData`. No React imports in math files.

---

### high · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`  
**Suggestion:** `plotScript.js` already imports and calls `buildRLoadLine`/`buildPyLoadLine` from `loadLine.js` for geo layers (see lines 559, 600–608, 1001, 1069–1072). The bug is that the `datasets` array is not being passed into `buildGgplot` / `buildMatplotlibPlot` from `PlotBuilder.jsx` (the call sites pass only `{ dfVar }` with no `datasets` key). Wire the `datasets` prop (already on the `PlotBuilder` component signature) through to the export call so the load-line builder can resolve filenames. Add a regression test asserting R and Python output both begin with `df <- read.*` or equivalent.  
**Invariants:** None.

---

### medium · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`  
**Suggestion:** `latexTable.js` builds `buildStargazer` output. When the model count exceeds ~4, the generated table overflows the page width. Wrap the `tabular` environment in `\resizebox{\textwidth}{!}{...}` when the number of models (columns) exceeds a threshold (e.g. > 3), or emit `\begin{adjustbox}{max width=\textwidth}` if `adjustbox` is preferred. Expose the choice as a comment so users can override. Stata's default `outreg2` uses `\resizebox`; R's `modelsummary` auto-scales.  
**Invariants:** None.

---

### medium · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis  
**File:** `src/math/ModelHypothesis.js`  
**Suggestion:** `ModelHypothesis.js` already computes `chiSq` and calls `pchisq` (line 206). The chi-square statistic for a Wald test on a linear restriction `Rβ = r` is `W = (Rβ−r)' [R V R']^{-1} (Rβ−r)`, where `V = vcov`. Add a `waldChiSq` result field alongside the existing F result; it equals `F × q` (where `q` is the number of restrictions). Wire a "χ²" column into `CoefficientTestPanel.jsx`. This is the same numeric value reexpressed so the toggle is cosmetic but important for users who cite Wald tests.  
**Invariants:** SE type must be passed explicitly into the vcov used for V; never hardcode HC inside the test.

---

### medium · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47…  
**File:** `src/services/data/parsers/`, `src/DataStudio.jsx`  
**Suggestion:** BugTriage lists this as "fix-later (resilient-import UX after launch)". The current parser aborts on the first bad row. Implement a `resilientCSVParse` wrapper that catches per-row exceptions, tags the row with `_parseError: true`, and continues; at the end, surface a banner ("3 rows could not be parsed — see highlighted rows"). The original row number should be preserved in the error tag so users can locate the raw data. This unblocks the World Bank dataset class that generates this exact error pattern.  
**Invariants:** Don't mutate rawData in place — flag rows as errored, let pipeline replay on them.

---

### medium · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`  
**Suggestion:** PlotBuilder already has a Labels panel for title/x-label/y-label on point/line/bar geoms. The time-series view in ExplorerModule (`ACF`/`PACF` / correlation plot) uses a separate SVG renderer that never received the Labels panel. Two approaches: (1) route time-series through PlotBuilder (preferred — eliminates a parallel render path) or (2) add explicit `chartTitle`, `xLabel`, `yLabel` state fields to the ExplorerModule time-series sub-section. Either way, include label fields in the pin/compare payload so saved plots carry their labels.  
**Invariants:** No external UI libraries.

---

### medium · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** The existing DuckDB path in `getTablePage` already emits `ORDER BY` for single-column sorts (confirmed by CLAUDE.md notes on `getTablePage`). Add clickable sort chevrons to the data-viewer column headers: a state `{ col, dir }` toggles `ASC`→`DESC`→`null` (reset). For DuckDB-backed datasets, inject `ORDER BY col dir` into `getTablePage`'s SQL. For JS-backed preview, sort the in-memory `rows` array before slicing. The invariant: always sort on the full DuckDB table, never on the 500-row preview.  
**Invariants:** DuckDB as the data boundary — no `SELECT *` on large tables; paginate after sorting.

---

### medium · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** `FeatureTab.jsx` already has `useState("if_else" | "case_when")` and `case_when` state but it's ephemeral. Save the in-progress `case_when` / `if_else` form to `sessionStorage` keyed by `pid + stepMode` whenever state changes (debounced 500 ms), and restore it on mount. `sessionStorage` is appropriate here — it's tab-scoped, lives until close, and avoids polluting IndexedDB with draft data. Show a "Resume draft" banner with a discard option.  
**Invariants:** IndexedDB for pipeline/data persistence; `sessionStorage` is acceptable for ephemeral draft UI state. Do not persist partial steps to the pipeline — only committed steps go to runner.js.

---

### medium · UI · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `CoefficientTestPanel.jsx` likely renders a single hypothesis at a time. Add a "hypothesis queue": an array of `{label, R, r}` entries that the user can accumulate and then run as a batch. For each entry in the queue, call `testLinearHypothesis` and tabulate results. The R replication emitter should call `car::linearHypothesis(fit, c("…", "…"))` once per entry in the queue, outputting a vector of F-statistics and p-values. This directly maps to R's `linearHypothesis` multiple-hypothesis API.  
**Invariants:** SE type must be threaded into each hypothesis test call; never hardcode inside `ModelHypothesis.js`.

---

### low · Data · 2026-07-17 (previously 2026-06-10)
**Feedback:** No arrange (sort) control in the data viewer [duplicate context: also filed 2026-07-17]  
*(Combined with the medium entry above — same root cause.)*

---

## Features

### Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)  
**Suggestion:** `INTERPRET_REGRESSION_PROMPT` already has elasticity/semi-elasticity rules (log-log → elasticity, log-level → semi-elasticity, lines 99/353–354). The gap is **direct (point) elasticity** for non-log models: `ε = β × (x̄ / ȳ)`, computed at the sample means. Add a post-estimation field `pointElasticities` to `EstimationResult.js` (populated from `xMeans`/`yMean` — already available from OLS sufficient stats) and a collapsible "Elasticities at sample means" table in `ModelingTab.jsx`. Update `INTERPRET_REGRESSION_PROMPT` with a rule that cites the direct elasticity when the model is linear-in-levels.  
**Invariants:** Zero React in `src/math/`; the computation lives in `LinearEngine.js` or `EstimationResult.js`.

---

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The Calculate tab already does symbolic derivatives, integrals, and optimization via its CAS workbench. Add a `limit(f, x, a, direction?)` operation using the same CAS library: one-sided (`+`/`-`) and two-sided limits, with L'Hôpital fallback. Display result and LaTeX form. Since the CAS already exposes `renderToString` for LaTeX export, the "show steps" button (already implemented) can enumerate limit rules applied.  
**Invariants:** None — CalculateTab is a pure React UI component; no math imports from `src/math/`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The function graph in CalculateTab renders one or more curves. Add support for constant expressions (a numeric result or a named constant like `c = 3.5`) as a horizontal rule layer. When the user enters a constant in the equation list, detect it (evaluates to a number for any x), and plot it as `geom_hline` / `hline` layer. PlotBuilder already has an `hline` geom that can serve as the visual model.  
**Invariants:** None.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a text input filter at the top of each variable panel (Y, X, W). On each keystroke, filter the displayed header chips to those containing the search string (case-insensitive). Clear button resets. Store filter state locally in the component (no persistence needed). This is especially important for datasets with 50+ columns (e.g. World Bank). Use a simple `.filter(h => h.toLowerCase().includes(q))` on the headers array.  
**Invariants:** Inline styles via `C` object from `shared.jsx`; no external UI libraries.

---

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` manages a flat ordered array of `{type, id}` entries. Add an optional `groupId` field to each entry and a `groups: { id, name }[]` array in the persisted project artifact order. In `ModelBufferBar.jsx`, render group headers as collapsible dividers between artifact rows. Provide an "Add group" button and drag-and-drop (or up/down arrows) to assign artifacts to groups. Persist through `saveArtifactOrder`. The key design decision: groups are display-only (a pinned model belongs to one group but still participates in all comparisons unless the user explicitly excludes it).  
**Invariants:** IndexedDB persistence via `indexedDB.js`; `artifactOrder.js` is the single owner of display order.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** `History.jsx` renders pipeline step rows. Each step has a `type` that maps to a tab+subsection (e.g. `rename`→Clean/Rename, `log`→Feature/Transform, `join`→Merge). Add a "✏️ Edit" icon on hover that fires a `onNavigateToStep(step)` callback. `WranglingModule.jsx` receives it and calls `setActiveTab` + `setActiveSubsection` for the step type. This eliminates the workflow break described in the feedback.  
**Invariants:** Pipeline is non-destructive — navigating to a step never re-runs the pipeline; editing fires a separate `replaceStep` action.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`  
**Suggestion:** Add a `country_code` pipeline step: given a source column (country name or code), a `from` format (`name|iso2|iso3`), and a `to` format, produce a new column using a small static crosswalk table (~250 rows, ~10 KB). Bundle the table as a JSON constant in the step (no network call). Expose it in FeatureTab → Formatting section. Add `country_code` to `registry.js` and `runner.js`, and emit `dplyr::left_join(countrycode::codelist, by=c(col="country.name.en"))` in R.  
**Invariants:** Non-destructive pipeline; registry must stay in sync with runner.js.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set, PlotBuilder renders a color legend. Enhance the legend to optionally show per-series summary stats (n, mean, or min/max) as a mini data table next to the legend chips. Add a "Show stats in legend" toggle in the Labels panel. The stats are derived from the already-available `rows` prop, grouped by the color column. This is purely a display enhancement — no change to the Observable Plot render path.  
**Invariants:** No external UI libraries; inline styles via `C` object.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** Add for RDD a summary table as in R — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/components/ModelingTab.jsx`, `src/math/CausalEngine.js`  
**Suggestion:** Deferred per BugTriage. When implemented, `CausalEngine.js` already returns bandwidth (`bw`), polynomial order, kernel, number of observations on each side, and the LATE estimate. Render a compact pre-formatted block matching `rdrobust`'s `summary()` output: Call / Method / Polynomial order / Kernel / BW Selector / Bandwidth / Number of Obs. This is a read-only display component — no engine changes needed.  
**Invariants:** None.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names  
**File:** `src/ReportingModule.jsx`, `src/services/export/latexTable.js`  
**Suggestion:** Deferred per BugTriage. Add a `labelOverrides: { [colName]: string }` field to the model spec. In `buildStargazer`, substitute `labelOverrides[col] ?? col` when writing row headers. Expose a simple rename table in the Report tab's model section.  
**Invariants:** Label overrides are display-only — they must never propagate back to estimation or the pipeline.

---

### Clean · 2026-05-19 (fix-later)
**Feedback:** Add filter(regionname %in% c("Spain", "Basque Country")) — R-style %in% membership syntax  
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`  
**Suggestion:** Deferred per BugTriage. `predicate.js` is the single owner of the condition language (`OPERATORS`, `normalizeOp`, `evalPredicate`, `predicateToSQL`). The `in` operator already exists. The UX gap is that users expect to type R-like `%in% c(...)` syntax. Add a thin parser that converts `col %in% c(v1, v2)` to the canonical `{op:"in", value:[v1,v2]}` predicate node. Wire it to `FilterBuilder`'s text-input mode (not the dropdown mode).  
**Invariants:** Single owner: predicate.js. Never re-implement filter operators elsewhere.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot (static spatial plot), not only in the OMS map  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`  
**Suggestion:** `SpatialGeoPlot.jsx` is the SVG/Observable Plot geo-plot tab. Grid cells from `GridSection`/`AggregateToGridSection` output a WKT polygon per cell. These can be rendered as a standard WKT polygon layer in `SpatialGeoPlot` — the parser already handles `POLYGON` WKT. The user needs a way to specify that a dataset represents a grid (a boolean hint or auto-detected from `_gridId` column presence) so the layer picks a choropleth fill rather than an outline style.  
**Invariants:** No changes to `src/math/` for this; it's a pure UI layer config.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`, line 11)  
**Suggestion:** Per CLAUDE.md (2026-07-30 audit): "TOUR_STEPS in HelpSystem.jsx" describes a two-month-old app. Review all 9 registered steps against the current module/tab structure. Key changes since the last tour update: N-way FE, Sun-Abraham, Callaway-Sant'Anna estimators, Spatial tab with Analyze/Map/Plot sub-tabs, PlotBuilder geoms (now 12+), CR2/CR3 SE chips. **Never write a count** ("14 estimators") — enumerate by name. Update the `target` selectors to match current DOM structure after any recent refactors.  
**Invariants:** None.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
**File:** Design phase — no file yet  
**Suggestion:** This needs a design decision before implementation: where do lists live? Options: (a) session-scoped via `sessionState.jsx` React context (`SessionStateProvider`), giving lists app-wide access but losing them on reload; (b) IndexedDB-persisted per pid, accessed by a new `useListStore()` hook. Recommendation: store in IndexedDB with a simple `{pid, name, values[]}` schema. Expose lists as named tokens in `FilterBuilder`'s value field (e.g. `@my_list`) and in the `in` operator's value picker. Design document recommended before building.  
**Invariants:** IndexedDB (not localStorage) for persistence. No server-side storage — privacy-first.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot (the CDN library used by PlotBuilder) does not support 3D natively. True 3D scatter/surface plots require a separate library (e.g. Plotly.js or Three.js). Recommendation: add a "3D mode" toggle that swaps the render path to Plotly.js (loaded from CDN, same pattern as Leaflet). Since Plotly.js is large (~3 MB), load it lazily only when 3D mode is activated. The CSP `connect-src` in `vercel.json` must include `cdn.plot.ly` (same class of bug as the DuckDB extension / Leaflet sourcemap entries in CLAUDE.md). Needs a spec doc before building.  
**Invariants:** CSP: any new CDN origin must be added to both `script-src` AND `connect-src` in `vercel.json` — this is the most frequently re-introduced bug in the project (see CLAUDE.md CSP bug entry).

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` (parseFile branches for `.rds`, `.xlsx`, `.shp`), `src/services/data/duckdb.js`  
**Suggestion:** Per BugTriage, `.dta` files > 10 MB already route through DuckDB with OPFS Parquet caching (fixed 2026-06-10). The remaining gap is `.rds`, `.xlsx`, and `.shp` — these still parse entirely in JS, allocate a full in-memory object, and cause tab lag or OOM on large files. Apply the same pattern: parse into a typed array, register as an Arrow-backed DuckDB table, reduce to 500-row preview, persist to OPFS. Priority order by user impact: `.xlsx` (largest files in practice) → `.rds` (already fast for the sizes tested) → `.shp` (geometry data rarely exceeds 100 MB). Track with a checklist in `plans/`.  
**Invariants:** DuckDB as the data boundary. Display limit (500 rows) ≠ computation limit (full table). OPFS Parquet cache invalidation must use the existing `deleteProject` cascade.

---

## Code-complete items pending browser validation

These are marked in BugTriage but have not been closed — Franco should validate and either mark ✓ or reopen:

| Item | Code commit | Pending action |
|------|-------------|----------------|
| PlotBuilder resize + PNG export | `35f4b5a6` | Franco browser-validate |
| Y~Ŷ / Residual-fitted / Q-Q plot for OLS | `35f4b5a6` | Franco browser-validate |
| Spatial heatmap restored as Leaflet layer | `5e105529` | Franco browser-validate |
| Diagnostics frozen to `result.spec` | `35f4b5a6` | Franco browser-validate |
| HC2/HC3/HAC SE in all replication scripts | 2026-07-17 2nd pass | Franco browser-validate |

Once validated, close the ClaudeFB.md items with `~~...~~ ✓`.

---

## Confirmed-fixed items not yet marked in ClaudeFB.md

These items appear open in ClaudeFB.md but are listed as fixed in BugTriage — they should receive ~~strikethrough~~ + ✓ in the next collector pass:

- [2026-05-11] · Explore — Graph does not fit screen (fixed `35f4b5a6`)  
- [2026-05-13] · Data — .dta wrong loaded (fixed 2026-06-10)  
- [2026-05-13] · Spatial — Border grids clipping (fixed 2026-05-18 Sutherland-Hodgman)  
- [2026-05-20] · Spatial — Grids do not work (fixed 2026-06-01)  
- [2026-05-20] · General — Dataset delete leaves stale vars (fixed)  
- [2026-05-21] · Model — p-value / t-value wrong (fixed 2026-06-05)  
- [2026-05-21] · Model — Auto-reestimate on variable toggle (fixed 2026-06-01)  
- [2026-05-21] · Model — Add option to clear model (fixed)  
- [2026-05-22] · Calculate — Function graph from zero (fixed)  
- [2026-05-25] · Calculate — Only 1 equation plotted (fixed)  
- [2026-05-19] · Explore — Color references in PlotBuilder (fixed 2026-05-30)  
- [2026-05-19] · Simulate — MC for single variables (fixed 2026-05-30)  
- [2026-05-18] · Simulate — Resampling/bootstrap (fixed)  
- [2026-05-30] · Spatial — Adjust size to spatial unit (fixed — fitBounds)  
- [2026-05-30] · Spatial — Geometry transformations/EPSG (fixed)  
- [2026-06-01] · Data — .dta duplicate columns (fixed 2026-06-03)  
- [2026-06-02] · Calculate — LaTeX crash (fixed 2026-06-05)  
- [2026-06-02] · Model — Poisson interpretation (fixed 2026-06-03)  
- [2026-06-08] · Simulate — Studentized permutation (fixed)  
- [2026-06-14] · Model — 2SLS Poisson (fixed `bc2d7b9d`)  
- [2026-06-17] · Model — Pipeline only applies if dataset in Clean (fixed 2026-06-25)  
- [2026-07-05] · Clean — Share Project RLS error (fixed 2026-07-14)  
- [2026-07-13] · General — Shared projects email mismatch (fixed 2026-07-14)  
- [2026-05-25] · Model — Estimate without intercept (DONE 2026-07-18 per CLAUDE.md)  
- [2026-06-03] · Spatial — Add geometry to point plots (verified fixed)  
- [2026-06-03] · Calculate — Show steps for calculus (verified fixed)  
- [2026-06-04] · Model — Persistence of pinned models (verified fixed)  
- [2026-06-06] · Calculate — Derivatives with endogenous functions (verified fixed)  
- [2026-06-08] · Simulate — Studentized permutation tests (verified fixed)  

---

## Items needing BugTriage confirmation (likely fixed, needs Franco)

| BugTriage status | Item | Needs |
|---------|------|-------|
| "likely fixed — needs confirmation" | [2026-07-09] FE with any categorical variable (N-way FE, PR #104) | Franco confirm UI wiring for non-entity/time FE columns |
| "likely fixed — needs confirmation" | [2026-07-09] FE with interactions (PR #104) | Franco confirm `factor_interactions` step visible in ModelConfiguration |

---

## Suggested action order

1. **Fix plot replication load lines** (high, surgical — `datasets` prop not passed to `buildGgplot`; 2 lines of change, unblocks every PlotBuilder export user)  
2. **CS DiD R replication code** (high export — `CallawayEngine.js` validated but replication not wired; blocks LMU users reproducing Callaway & Sant'Anna results)  
3. **group_summarize difference-in-means (ATE)** (high pipeline — capability exists in `SampleTestPanel`; gap is UX shape; high user impact for policy analysts)  
4. **Browser-validate 5 code-complete items** (`35f4b5a6`, `5e105529`) and close them in ClaudeFB.md / BugTriage  
5. **Stata workbench audit** (high — surface area is large; reproduce failure first, then fix)  
6. **Chi-square for hypothesis tests** (medium math — cosmetic variant of existing F-test; `waldChiSq = F × q`)  
7. **Data viewer sort/arrange** (medium UX — DuckDB ORDER BY already exists; add column-header click)  
8. **LaTeX table auto-sizing** (medium export — `\resizebox{\textwidth}{!}{...}` wrapper when models > 3)  
9. **Mark 25+ confirmed-fixed items in ClaudeFB.md** (hygiene — reduces noise in future collector runs)  
10. **Direct elasticity interpretation** (medium AI — post-estimation field in `EstimationResult.js` + one new prompt rule)
