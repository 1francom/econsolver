# Feedback Analysis Report — 2026-08-05

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **9 open bugs** · **13 open features** · **2 open UX/Design** · **1 open performance** · **0 other**
- Total: **25 open items** analysed across ClaudeFB.md and BugTriage.md
- 2 ClaudeFB items found already implemented in source (limits, plot constants) — marked as **resolved in code, not in docs**
- 1 BugTriage discrepancy flagged: "Estimate without intercept" still listed as `fix-later` but CLAUDE.md confirms it shipped 2026-07-18

---

## Bugs

### [high] · Export / Model · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js:1432` (CallawayCS case), `src/math/CallawayEngine.js`
**Suggestion:** The `att_gt()` call hard-codes `base_period = "varying"` and `control_group = "nevertreated"`, ignoring what CallawayEngine actually used. Verify that `treatCol` in the export path refers to the *first treatment period* (integer year) — if the user's column is a binary 0/1 indicator, `att_gt` silently misbehaves. Confirm the `did` package argument signature against the emitted version; `base_period` was renamed in `did` 2.0. Add a `# requires: did >= 2.0.0` comment and expose `control_group` and `est_method` from `model.spec`.
**Invariants:** Export code lives in `src/services/export/` — no math changes needed. Do not call Anthropic API here.

---

### [high] · Export / Explore · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/services/export/plotScript.js:343` (buildGgplot), `src/services/export/plotScript.js:798` (buildMatplotlibPlot)
**Suggestion:** `buildGgplot` and `buildMatplotlibPlot` do not call `buildRLoadLine`/`buildPyLoadLine`; they accept a `dfVar` name but never emit a `read_csv(...)` line. The geo variants (`buildGeoGgplot`, `buildGeoMatplotlib`) already call the helper at lines 559 and 1001. Mirror that pattern: accept a `{ filename, loadOpts }` option, call the appropriate load-line builder, and prepend the result before `library(ggplot2)` / `import pandas`. `PlotBuilder.jsx` already passes `datasetName` — thread `filename` and `loadOpts` from the dataset registry in the same way geo plots do.
**Invariants:** `plotScript.js` is pure export — no React. Keep load lines optional so `scriptPreamble` callers (ModelingTab) are not broken.

---

### [high] · Export / Model · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx:74`, `src/services/export/stataScript.js`
**Suggestion:** The feedback targets the Stata tab in CodeEditor, not the Calculate workbench. Likely causes: (a) panel estimators may be missing `xtset entityCol timeCol` before `xtreg`/`areg`; (b) the two-way cluster fix from 2026-07-18 routes to `reghdfe` but may not have reached every estimator branch; (c) `date_parse` translator now exists in `stataScript.js` but may emit commands that fail on already-numeric columns. Reproduce by loading a panel CSV, running TWFE with clustered SE, copying the Stata script, and running in Stata 17 — fix the specific failing command.
**Invariants:** `stataScript.js` is pure export. Honour `seType` end-to-end per the 2026-07-18 fix (verify panel branches are covered).

---

### [high] · Pipeline / Explore · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/ExplorerModule.jsx:1780` (FN_OPTS), `src/pipeline/runner.js:528` (group_summarize case)
**Suggestion:** Current `FN_OPTS` covers mean/median/sum/count/min/max/sd but has no cross-group difference. Add a `diff_in_means` aggregate type: given a binary group column and a value column, compute both group means in the runner case and append a single-row result with `mean_treated`, `mean_control`, and `diff_in_means` columns. Wire as a new `fn:"diff_in_means"` path inside the existing `group_summarize` runner case. Longer-term, allow a free-text expression that references previously defined aggregations.
**Invariants:** `runner.js` is pure JS — no React imports. Pipeline steps must replay on `rawData`, never mutate in place.

---

### [medium] · Export / Report · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js:36`, `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` generates `colFmt = "l" + " r".repeat(colsN)` with no width limit. Wrap the `tabular` environment in `\resizebox{\textwidth}{!}{...}` (or `\adjustbox{max width=\textwidth}{}`) when `colsN > 4`. Add `\usepackage{adjustbox}` to the preamble comment block.
**Invariants:** `latexTable.js` is a pure string builder — no React, no Anthropic calls.

---

### [medium] · UI / Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` (time series rendering block)
**Suggestion:** The time-series sub-tab renders directly to SVG without title/axis-label controls. Add `title`, `xLabel`, `yLabel` state fields and thread them into the renderer. The replication scripts from `exploreStatScript.js` should also emit `labs(title=..., x=..., y=...)`. Alternatively, route the time series through PlotBuilder's `line` geom which already exposes these controls.
**Invariants:** Inline styles only via `C` object. No CSS files. No React in `exploreStatScript.js`.

---

### [medium] · Math / UI / Model · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js:203`, `src/components/modeling/CoefficientTestPanel.jsx:211`
**Suggestion:** `waldTest` already returns `chiSq` and `chiSqPval`; the joint-test panel in `CoefficientTestPanel` already renders `jointResult.chiSq`. The gap is that individual coefficient rows show only t/p with no χ²(1) = t² equivalent. Add a χ²(1) column next to each coefficient row, and expose a toggle chip to switch the joint-test display between F(k,df) and χ²(k) labelling. The math is already present — this is a display-only change in `CoefficientTestPanel.jsx`.
**Invariants:** No React in `ModelHypothesis.js`. All UI changes belong in `CoefficientTestPanel.jsx`.

---

### [medium] · UI / Data · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Add column-header click-to-sort: maintain `[sortCol, sortDir]` state, derive a sorted display array with `useMemo` (sort the 500-row display sample only — never mutate `rawData`), and render a ▲/▼ chevron on the active sort column. For DuckDB datasets, issue `ORDER BY col ASC/DESC LIMIT 500` via `duckdb.js` so the sort is accurate across the full table. Do not add an `arrange` pipeline step for the viewer — the existing `arrange` step in Clean covers persistent sorts.
**Invariants:** Non-destructive — display sort only. For DuckDB, query via `duckdb.js`; do not pull the full Arrow table into JS objects.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line 1583 — malformed row aborts entire import
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/parsers/`
**Suggestion:** Implement a resilient parse mode: collect row-level errors, skip malformed rows, and surface a post-parse banner ("3 rows skipped on lines 1583, 2047 — download error log"). Store bad lines in `rawData._parseErrors` so the user can adjust the pipeline after load. This avoids a full abort on a single bad row.
**Invariants:** Parsers in `src/services/data/parsers/` are pure functions — no React. Do not add React to parsers; surface the banner in `DataStudio.jsx` or `WranglingModule.jsx`.

---

## Features

### Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** Add a "Save hypothesis" button that appends the current test (constraint vector, h0 values, results) to a `savedHypotheses` array. Render the list as collapsible cards below the input. Thread the saved list into replication exports via `statInferenceScript.js` — emit a `car::linearHypothesis()` block per saved test in R, equivalent blocks in Python/Stata.
**Invariants:** Persist via `indexedDB.js`, never `localStorage`. No React in `ModelHypothesis.js`.

---

### Model / AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT), `src/services/AI/AIService.js`
**Suggestion:** The prompt already has log-log elasticity and log-level semi-elasticity rules. The gap is computed (evaluated) elasticity for level-level regressions: elasticity = β × mean(X)/mean(Y). Thread `result.means` (already in `EstimationResult`) into the `interpretRegression` user prompt in `AIService.js`. Add a rule to `INTERPRET_REGRESSION_PROMPT` instructing the model to compute and quote the point elasticity at the mean when requested.
**Invariants:** All Anthropic calls through `callClaude()` in `AIService.js` only. `Prompts/index.js` contains prompt text only — no business logic.

---

### General / UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend the artifact order schema (stored in `indexedDB.js` model_buffer store) with a `group` string field. Render model/plot/map cards in `ModelBufferBar` grouped under collapsible headings. Add a "Group" dropdown per card backed by `artifactOrder.js`'s `getArtifactOrder`/`saveArtifactOrder` helpers.
**Invariants:** Persist via `indexedDB.js` only. Inline styles from `C`.

---

### Pipeline / Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Add a click handler on step cards in `History.jsx` that emits an `onNavigate(stepType)` callback to `WranglingModule`. `WranglingModule` maps `stepType` to the appropriate tab/sub-tab and calls `setActiveTab`/`setActiveSub`. No URL changes needed — this is a controlled imperative navigate-to-tab.
**Invariants:** `History.jsx` only emits the callback — never navigates directly. No React in `runner.js`.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary(rdrobust)
**File:** `src/math/CausalEngine.js` (runSharpRDD), `src/components/ModelingTab.jsx`
**Suggestion:** `runSharpRDD` already returns LATE, SE, bandwidth, kernel, and polynomial order in `EstimationResult`. Surface these in a compact pre-result table in the RDD result panel: effective N (left/right), h (IK bandwidth), kernel, poly order, LATE, 95% CI, robust p-value. No new math needed.
**Invariants:** Display only — no changes to CausalEngine.js math. Inline styles from `C`.

---

### Model · 2026-06-14
**Feedback:** Allow users to edit LaTeX table variable names
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** Add an optional `labelOverrides: { [colName]: string }` parameter to `buildStargazer`. Render an "Edit labels" expand panel in `ReportingModule` with one text input per coefficient row; feed the resulting map as `labelOverrides`. Default to the data dictionary labels where available.
**Invariants:** `latexTable.js` is a pure function — pass overrides as a parameter, not via module state.

---

### Spatial · 2026-06-14
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add a "Filter rows" collapsible panel in `SpatialGeoPlot` exposing column-value and bounding-box filters. Subset the `rows` prop before passing to the renderer. The `guessLatCol`/`guessLonCol` helpers in `src/components/tabs/spatial/shared/guess.js` identify geometry columns automatically.
**Invariants:** Inline styles from `C`. Spatial filter must not mutate `rawData` — subset the local prop only.

---

### Model / UI · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a `<input type="text">` search field at the top of each variable panel (Y, X, W, Z). Filter `headers` client-side by `header.toLowerCase().includes(q.toLowerCase())`. Clear search on variable selection.
**Invariants:** Inline styles from `C`. No new state outside the component.

---

### Explore / UI · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, annotate each legend entry with its group N (e.g. "Germany (n=42)"). Add an optional "show counts" mode: pre-compute a `groupBy` count map over `rows` and append the count to each legend label via a custom Observable Plot legend mark.
**Invariants:** Inline styles from `C`. No React in `plotScript.js`.

---

### Clean / UI · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a "Country code" section under FeatureTab. Ship a static lookup table (~250 rows, ~8 KB) mapping iso2 ↔ iso3 ↔ country name — no network call. Emit a standard `add_column` or `mutate` pipeline step so the step replays on `rawData`. The existing `fuzzyGroups` utility in `wrangling/utils.js` can back fuzzy name matching with a threshold guard.
**Invariants:** Static lookup only — no external API calls. Emit a standard pipeline step type.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session
**File:** `src/services/session/sessionState.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** Needs a design session before implementation. Natural home: a new `lists` store in IndexedDB (project-scoped), surfaced through `sessionState`. `CleanTab` filter builder would gain a "From list" option; `FeatureTab` `if_else`/`case_when` builder would gain a "values in list" condition. Recommend a ClaudePlan spec doc before any code.
**Invariants:** Persist via `indexedDB.js`. Do not use `localStorage`. Lists must be project-scoped.

---

### Clean · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain", "Basque Country")) R-style membership syntax
**File:** `src/components/wrangling/CleanTab.jsx`
**Suggestion:** Add an `in` operator to the filter builder that accepts a comma-delimited value list. Extend the `filter` step schema with `{ col, op: "in", values: [...] }` and handle it in the `filter` runner case in `runner.js`. Display as a multi-value chip input in the UI.
**Invariants:** Non-destructive — filter step replays on `rawData`. Schema must be forward-compatible for a `not_in` operator.

---

### Explore · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`
**Suggestion:** Observable Plot 0.6 does not natively support dual-y axes. Implement via a secondary SVG group with an independent scale: add a `y2` aesthetic mapping channel for `line` layers; render y2 layers on a separate group; draw a right-side axis via `Plot.axisY` with `anchor:"right"`. For exports: R uses `scale_y_continuous(sec.axis = sec_axis(...))`, Python uses `matplotlib.twinx()`, Stata uses `twoway ... || ... , yaxis(2)`.
**Invariants:** Observable Plot CDN version pinned at 0.6. Inline styles from `C`.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx:11` (TOUR_STEPS)
**Suggestion:** The 9-step tour (Welcome + 8 modules) may have stale descriptions for Spatial (now has Analyze/Map/Plot sub-tabs), Simulate (StatWorkspace + SampleTestPanel), and Calculate (full Workbench with limits tab). Review each `TOUR_STEPS` entry and update its `description`, `anchor`, and `placement` to match the current layout. Also verify that tour anchor elements still exist in the DOM after recent tab redesigns.
**Invariants:** No math or pipeline changes. Inline styles from `C`.

---

### Clean / UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections (e.g. case_when)
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/WranglingModule.jsx`
**Suggestion:** `FeatureTab` initialises `case_when`/`if_else`/`mutate` state fresh on every render. Add a `draftStep` object in `WranglingModule` state (keyed by `stepType`) that persists the in-progress configuration. Restore the draft when the user navigates back. Clear on Apply or Cancel. Use `sessionStorage` (session-scoped ephemeral, not `localStorage` which is deprecated for data storage per CLAUDE.md, not `indexedDB` which is for committed pipeline state).
**Invariants:** Use `sessionStorage` for draft only. The applied step still goes through `runner.js` and remains non-destructive.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/pipeline/duckdbRunner.js`, `src/services/data/duckdb.js`
**Suggestion:** Fase X4 is code-complete per the X5 reconciliation (Parquet → OPFS pipeline). The open item is browser validation by Franco — no code change needed. Confirm: (a) 21 MB CSV loads without crash, (b) ⚡ DuckDB badge appears, (c) pipeline runs on full 900k rows while viewer shows 500 rows, (d) after reload the OPFS Parquet cache restores the full table correctly (the `opfsCacheKey` persistence bug was fixed 2026-07-22).
**Invariants:** DuckDB as data boundary — never pull full Arrow result into JS. Parquet cache in OPFS. COOP/COEP headers required for threaded Wasm.

---

## ⚠️ Discrepancy flags

### BugTriage stale: "Estimate without intercept"
BugTriage row (2026-06-14 Model) status is `fix-later`, but **CLAUDE.md confirms this shipped 2026-07-18** (`runOLS noIntercept`, chip in `ModelConfiguration`, DuckDB guard, exports across all three languages). Update BugTriage status to `fixed`.

### Code-complete items awaiting browser validation
These are merged but unverified by Franco — not counted in the open-items total above:
- Plot Builder resize + PNG export aesthetics (`35f4b5a6`)
- OLS Y~Ŷ / Residual-vs-fitted / Q-Q plots (`35f4b5a6`)
- Spatial heatmap layer (`5e105529`)
- Coefficient plots freeze on sidebar/pinned-model change (`35f4b5a6`)
- HC2/HC3 replication code in R/Stata (2026-07-18)
- N-way FE with categorical variables + FE interactions (`045f5cda` #104) — BugTriage says "likely fixed, needs confirmation"

### ClaudeFB items already implemented — docs need updating
- **[2026-06-19] "Add limits" (Calculate):** `lim Limit` tab is live in `Workbench.jsx:1586`; `calcEngine.js` exports `limit()`. Mark done in ClaudeFB.md.
- **[2026-07-04] "Allow plotting constants in the graph":** `ViewControls.jsx` has `addRef` for `v`/`h` reference lines rendered in `WorkbenchCanvas.jsx:148`. Mark done in ClaudeFB.md.

---

## Suggested action order

1. **group_summarize cross-group math** (`ExplorerModule.jsx`, `runner.js`) — blocked user from computing ATEs; add `diff_in_means` aggregate type. Core research workflow, ~2h effort.
2. **CS DiD replication code broken** (`rScript.js:1432`) — shipped estimator's R export is broken; investigate `treatCol` type mismatch and `did` package API version. Medium effort.
3. **Plot replication code missing load lines** (`plotScript.js:343`, `plotScript.js:798`) — all ExplorerModule plot exports are incomplete scripts; mirror what geo variants already do. Small effort (~1h).
4. **Stata workbench broken** (`stataScript.js`, `CodeEditor.jsx`) — one of three replication languages is non-functional; reproduce with panel + clustered-SE spec and fix the specific error. Medium effort.
5. **LaTeX table auto-width** (`latexTable.js:36`) — wrap wide tables in `\resizebox{\textwidth}{!}{}` when `colsN > 4`. Trivial effort, high professional-output impact.
6. **Fix BugTriage discrepancy** — flip "estimate without intercept" to `fixed`; confirm browser-validation items with Franco.
7. **Mark two ClaudeFB items as done** — limits and plot constants already implemented in source.
