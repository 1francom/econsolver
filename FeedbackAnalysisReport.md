# Feedback Analysis Report — 2026-09-17

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 11 open bugs (confirmed) + 3 code-complete/browser-pending + 3 likely-fixed/needs-confirmation · 15 open features · 0 open UX (subsumed into Bugs) · 1 open performance · 1 open other

**Cross-reference notes:** Items confirmed fixed in BugTriage.md or the X5 reconciliation table are excluded even if not marked ~~strikethrough~~ in ClaudeFB.md (e.g. arrange in data viewer, join pipeline context, any() in grouped expressions, plot load-lines, group_summarize ATE, chi-square hypothesis test — all fixed per BugTriage but not yet back-marked in ClaudeFB). Items marked "likely fixed — needs confirmation" are retained at reduced severity; "code-complete, browser validation pending" items are retained as medium-severity open until Franco confirms in-browser.

---

## Bugs

### [critical] · Model · 2026-07-22 / 2026-07-28 (likely fixed — needs Franco confirmation)
**Feedback:** `P-values with small numbers!!!!!!!! beta=0.0001 and se=0.002 can never give a p-value < 0.001 with ***`
**File:** `src/math/LinearEngine.js` (functions `tCDF`, `betainc`); also `src/math/calcEngine.js` (`_incompleteBeta`, independently fixed)
**Suggestion:** BugTriage records fix commit `216b1ecb` (2026-07-28) replacing the Riemann-sum beta approximation with Lentz's continued-fraction method. Franco should confirm by running a regression where |t| < 1 (e.g. β ≈ 0.0001, SE ≈ 0.002) and verifying p > 0.5. If any residual failure appears, check `calcEngine.js`'s `_incompleteBeta` — a second independent copy was fixed separately for the chi-square panel (BugTriage row 73); the two fixes must agree. The p-value bug had a companion entry in `calcEngine.js` where `pt()` landed in the divergent branch for every insignificant t-stat (returns ~0.19 instead of ~0.99 at df=100k).
**Invariants:** `src/math/` — no React imports. Both fix commits must keep SE type passed explicitly; never hardcode inside engine functions.

---

### [high] · Calculate · 2026-07-17
**Feedback:** `Stata workbench doesn't work completely`
**File:** `src/services/export/stataScript.js`, `src/components/modeling/CodeEditor.jsx`
**Suggestion:** "Doesn't work completely" is under-specified — collect the exact error from Franco before patching. Known documented gaps in CLAUDE.md that are candidates: (1) `ibn.` factor coding under no-intercept is documented as "UNVERIFIED against a live Stata"; (2) `ivregress` with HAC emits a NOTE without a working DO-file equivalent — users who paste that block get a missing-command error; (3) `merge 1:m` for many-to-many joins is flagged UNVERIFIED; (4) the two-way cluster path routes to `reghdfe` correctly but depends on `reghdfe` being installed (it is an SSC package). Start by reproducing the failure with the estimator/SE combination Franco was using, then check which path in `generateStataScript()` (line 15 of `stataScript.js`) is taken.
**Invariants:** `stataScript.js` is pure JS — no React imports. SE type must arrive from the caller; never default inside.

---

### [high] · Simulate · 2026-07-28
**Feedback:** `Update the replication script, currently it replicates only the DGP`
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (no dedicated simulate exporter exists)
**Suggestion:** Add a `generateSimulateScript()` function in `src/services/export/simulateScript.js` (R, Python, Stata variants) that emits: (1) the DGP seed and distribution parameters already exported, (2) resampling/bootstrap loop if that panel was used, (3) any sample test calls (`t.test`, `var.test`, etc.) from `SampleTestPanel`, and (4) any estimation block if a model was run on the simulated draw. Wire `SimulateTab`'s "Copy script" button to call this function using the component's existing `dgpParams`, `resamplingState`, and `sampleTestResults` state slices. The new file should be pure JS (no React imports) placed in `src/services/export/` like the other exporters.
**Invariants:** New exporter must be pure JS with zero React imports. All Anthropic calls go through `AIService.js` — not this file.

---

### [high] · Explore · 2026-07-28
**Feedback:** `References of the group in time series doesn't work`
**File:** `src/ExplorerModule.jsx` (functions `SvgMiniTimeSeries` at line 237, main TS chart at ~line 1682)
**Suggestion:** The root cause is that the group-by legend lives in the HTML DOM (`<div>` beside the `<svg>`) while SVG/PNG export captures only the `<svg>` element — so exported images have colored lines but no label identifying which group is which. The `colorFor` function and `seriesColors` state are correct for interactive use (legend shows color swatches + group names). Fix: render the legend INSIDE the SVG as `<text>` elements after the last series line, positioned in the top-right corner of the chart area (matching ggplot's default legend placement). The `SvgMiniTimeSeries` component (pinned preview) has no legend at all and shows the same symptom — add the same inline legend there. Guard with `series.length > 1` so single-series charts are unchanged.
**Invariants:** No React imports needed — this is JSX inline in `ExplorerModule.jsx`. Use `C` color tokens; inline styles only.

---

### [high] · Model · 2026-07-06
**Feedback:** `Add vector for running more than 1 hypothesis and save them to replicate in R`
**File:** `src/components/modeling/CoefficientTestPanel.jsx`, `src/math/ModelHypothesis.js`
**Suggestion:** `CoefficientTestPanel.jsx` currently runs one joint test at a time with no persistence across tests. Add a `savedTests` array in local state (or `useRef` if persistence beyond the session is not required). Each time the user clicks "Run test," push `{ constraints, nulls, result, script }` onto the array. Render a collapsible "Test history" section below the test form showing prior tests with their F/χ², p-value, and a "Copy R" button that calls `generateJointHypothesisScript` (already implemented in `ModelHypothesis.js` at line 473). If cross-session persistence is needed, write to the model's IDB entry via `indexedDB.js` — do not use `localStorage` (deprecated for data storage). The "save to R" part is already solved by `generateJointHypothesisScript`; only the accumulation UI is missing.
**Invariants:** Use `indexedDB.js` for any persistence that must survive page reload. No `localStorage` for data. No React in `src/math/`.

---

### [medium] · Model · 2026-07-09 (likely fixed — needs browser confirmation)
**Feedback:** `Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable`
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage marks this "likely fixed — N-way FE shipped in `045f5cda`; needs confirmation." Ask Franco to browser-test: (a) declare entity + time in PanelTab, (b) add a third FE column (e.g. `sector`) via the FE column picker in ModelConfiguration, (c) confirm the model runs via `runFEMulti` and the coefficient table omits the absorbed column. The N-way FE validation harness (`panelNwayFeRValidation.R` → `panelNwayFeBenchmarks.json`) passes 18/18 checks, so the math is correct — the only open question is whether the UI surfaces the picker for dimensions beyond entity/time.
**Invariants:** `runFEMulti` / `demeanByFE` are in `src/math/PanelEngine.js` — no React imports there.

---

### [medium] · Model · 2026-07-09 (likely fixed — needs browser confirmation)
**Feedback:** `Add also FE with interactions`
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions`)
**Suggestion:** BugTriage marks this "likely fixed — FE interactions shipped in `045f5cda`." Browser-confirm by running `y ~ x1:x2 | entity + time` and checking that the interaction term appears in the coefficient table at the expected value. Per CLAUDE.md the N-way FE validation passes. If interactions are silently absorbed (coefficient missing), the `factor_interactions` step's product column may be treated as a regressor rather than a pre-demean column — trace through `estimationDispatch.js` to confirm the interaction columns reach `demeanByFE` before projection.
**Invariants:** Zero-React rule in `src/math/`.

---

### [medium] · Explore · 2026-07-09
**Feedback:** `Add labels for the Time series plot, title, axis, etc.`
**File:** `src/ExplorerModule.jsx` (~line 1455 — time series state; ~line 1587 — controls grid)
**Suggestion:** The time series controls grid currently has four selectors (Time axis, Variable Y, Aggregation, Group by). Add a fifth row (or a collapsible "Labels" section) with three text inputs for Plot title, X-axis label override, and Y-axis label override — matching the pattern already used at line 981 for the histogram/bar tab (`[plotTitle,setPlotTitle]`). Wire these into the SVG `<text>` elements that already render the axis labels at lines ~1740–1744 (`{tCol}` and `{agg}({yCol})`). The SVG title can go at `y={PAD.t - 5}` centered on `x={PAD.l + iW/2}`. Also thread the labels into the pin/compare params so a pinned time series chart carries the title.
**Invariants:** Inline styles via `C`; no external libraries. The `labInput` helper used at line 981 can be reused as-is.

---

### [medium] · Model · 2026-07-09
**Feedback:** `Add direct elasticity interpretation`
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** The AI prompt at lines 99, 364–365 already covers log-log elasticity and log-level semi-elasticity as general rules. The gap is that the app never displays a computed elasticity value in the coefficient table UI without relying on AI narrative. Add a `computeElasticity(beta, xMean, yMean, functionalForm)` helper in `src/math/LinearEngine.js` that returns the point elasticity `β × (x̄ / ȳ)` for a level-level model and `β` for a log-log model. Expose the result as an optional column in `EstimationResult.coefficients` (e.g. `elasticity`). The coefficient table in `ModelingTab.jsx` can then show a "Elast." column when the value is defined, matching Stata's `margins` or R's `marginaleffects` output. This is purely a math/display feature — no AI call needed.
**Invariants:** Helper goes in `src/math/LinearEngine.js` — no React imports. The AI prompt in `AIService.js` already interprets elasticity in narratives; this adds a numeric display, not a duplicate call.

---

### [medium] · General · 2026-07-05
**Feedback:** `Adjust the size of LaTeX tables when the number of columns increases`
**File:** `src/services/export/latexTable.js` (line 39: `const colFmt = "l" + " r".repeat(colsN)`)
**Suggestion:** The table currently uses a fixed `{l r r r …}` column spec regardless of how many models are compared. For more than ~5 columns this overflows the text width in most LaTeX document classes. Wrap the `tabular` in `\resizebox{\linewidth}{!}{…}` (or emit `\begin{adjustbox}{max width=\linewidth}`) when `colsN > 4`. Both approaches require no additional LaTeX packages beyond those already expected (booktabs). Alternatively, emit a `\setlength\tabcolsep{4pt}` line at the top of the table environment. The simplest correct fix: detect `colsN > 5` and emit `\resizebox{\textwidth}{!}{\begin{tabular}…\end{tabular}}` instead of the plain `\begin{tabular}`. The example table in the file's docstring (line 227) uses 8 columns and would benefit from this immediately.
**Invariants:** Pure JS, no React imports. Result is a plain string — no side effects.

---

### [medium] · Clean · 2026-07-17
**Feedback:** `Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases`
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** The current architecture remounts `FeatureTab` on each tab switch, losing any partial form state. Add a `draftState` `useRef` (or `sessionStorage` keyed by `<pid>/<datasetId>/featureDraft`) that is written on every form keystroke and read on mount. For each sub-section (case_when, if_else, mutate, log, etc.) store a separate draft slot keyed by the section name. The restore should be conditional on the draft being for the same dataset — stale drafts from a different dataset should not auto-restore. This is a pure UX improvement: no pipeline step is staged or committed until the user clicks "Add step."
**Invariants:** Use `sessionStorage` (not `localStorage`, not IndexedDB) since this is ephemeral in-session state, not pipeline data that needs persistence across page reloads. IndexedDB is for pipeline/data storage; this is a transient draft.

---

### [medium] · General · 2026-07-17
**Feedback:** `Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria`
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** Extend `artifactOrder.js`'s `ArtifactId` scheme (which already tracks `type` and `id`) with an optional `group` string field. Add a "New group" button to `ModelBufferBar.jsx` that prompts for a group name and assigns the current selection. Render groups as collapsible sections inside the buffer bar. Persist the group assignments through `getArtifactOrder`/`saveArtifactOrder` in `artifactOrder.js` — these already read/write per-project to `indexedDB.js`. The comparison plot (G13) should filter to the user's currently open group. Do not change `trimResult.js` or `modelBuffer.js` internals — the grouping is a display/ordering concern, not a data-model concern.
**Invariants:** Use `indexedDB.js` for persistence. No `localStorage`. Inline styles via `C`.

---

### [low] · Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583`
**File:** `src/services/data/parsers/` (CSV path in `src/DataStudio.jsx`, or DuckDB ingest path)
**Suggestion:** This is a resilient-import gap noted in BugTriage as "fix-later." The CSV parser should not abort the entire file on a malformed row. Instead: (1) catch the DuckDB `CSV Error on Line N` exception, (2) re-try the import with `IGNORE_ERRORS=TRUE` (DuckDB's CSV reader option), and (3) surface a banner "Imported N rows; K rows on lines X, Y, Z skipped due to parse errors" using the existing warning banner pattern. If the CSV is small enough for the JS path, similarly wrap `papaparse`'s `skipEmptyLines` mode and collect `errors[]` from the result. Do not silently discard rows without informing the user.
**Invariants:** `DataStudio.jsx` orchestrates file loading. DuckDB calls go through `src/services/data/duckdb.js`. No React imports in parser helpers.

---

### [low] · General · 2026-07-17
**Feedback:** `Guide tour must be updated`
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` registry, ~9 steps)
**Suggestion:** The TOUR_STEPS array describes the app as it existed ~2 months ago. Audit each step against the current UI: the WLS toggle (now under OLS, not a sidebar estimator), the W selector (controls only, not weights/instruments/controls), the FE column picker (now multi-dimensional), and the Plot Builder (now 12 geoms including tile). Also update the 8 HintBox modules per the CLAUDE.md working convention — "never write a count into help copy." Replace "14 estimators" / "11 geoms" with named lists. The tour step for the Pipeline sidebar must now say it can be collapsed via the ⟩ button.
**Invariants:** `HelpSystem.jsx` is React — inline styles, `C` color constants. `TOUR_STEPS` is a plain array of `{ target, heading, body }` objects — no math code.

---

## Code-complete · Browser Validation Pending

These items have confirmed code fixes committed but await Franco's in-browser confirmation before closing.

### [medium] · Model · 2026-06-10 · Plot Builder resize / PNG export
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Status:** Code-complete `35f4b5a6`. Plot viewport has native vertical resize; PNG captures user-selected dimensions. **Action:** Franco to resize the plot, export PNG, confirm dimensions match and no aesthetic clipping.

### [medium] · Model · 2026-06-11 · OLS Y~Ŷ / Residual-vs-Fitted / Q-Q plots
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Status:** Code-complete `35f4b5a6`. Diagnostics now render from frozen `result.spec` — sidebar edits cannot mutate the displayed model. **Action:** Franco to run OLS, confirm all three plots appear and do not change when adding/removing variables without re-estimating.

### [medium] · Spatial · 2026-06-13 · Heatmap display broken
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Status:** Code-complete `5e105529`. Heatmap restored as a Leaflet layer with KDE cells, palette/opacity controls, legend, HTML export, deterministic 5k-point cap. **Action:** Franco to open Map tab, add a Heatmap layer, confirm KDE cells render and the legend shows the palette.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (parseFile dispatch), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Fase X4 is documented as code-complete — large CSVs are imported to DuckDB, reduced to a 500-row UI preview, and cached via OPFS Parquet. The OPFS cache avoids re-import on reload. **Action:** Franco to browser-benchmark a 900k-row CSV (reimport, tab switch, model estimation on the full table) and confirm <2s tab-transition latency. If a crash still occurs, check (a) whether the OPFS Parquet write is blocked by the `connect-src` CSP (`extensions.duckdb.org` must be in `connect-src` — fixed 2026-07-25 per CLAUDE.md), and (b) whether `SharedArrayBuffer` is active (requires COOP/COEP headers in `vercel.json`).
**Invariants:** All DuckDB calls via `src/services/data/duckdb.js` singleton. Never pull full Arrow result into JS objects.

---

## Features

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** The static geo-plot (Plot tab in Spatial) renders SVG via Observable Plot. Add a "Grid layer" option in `GeoLayerConfig.jsx` that renders an H3 or rectangular grid from the `SpatialEngine.js` grid-assign output as filled polygons with WKT outlines. The grid data is already produced by the `sp_grid_assign` pipeline step and available as a column in the dataset. Parse those WKT polygons via `wkt.js`'s `parseWktRings` and render as SVG `<polygon>` elements in `GeoPlotCanvas.jsx`. Scope: rectangular grids first (simpler WKT); H3 hexagons second.
**Invariants:** `src/math/SpatialEngine.js` is pure JS — no React. `GeoPlotCanvas.jsx` is the SVG canvas; grid rendering belongs there, not in `SpatialTab.jsx`.

---

### Explore · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js` (`OPERATORS`)
**Suggestion:** The `in` operator already exists in `predicate.js` (`OPERATORS`) and `evalPredicate`. The gap is the UI: `FilterBuilder` shows a single text input for the value, not a multi-value list input. For `in`/`nin` operators, replace the single `<input>` with a tag-style multi-value input (comma-separated chips, matching the existing FeatureTab `case_when` chip pattern). Internally store `value` as an array of strings and update `predicateToSQL` (which already handles `IN (?)` with array binding) accordingly. Check that the NL-to-pipeline path also emits `{op:"in", value:[...]}` for membership checks.
**Invariants:** `predicate.js` is the single owner of all filter operators — never re-implement a filter operator elsewhere.

---

### Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/modeling/results/` (RDD result component area), `src/math/CausalEngine.js`
**Suggestion:** `runSharpRDD` already returns `{ late, se, tStat, pVal, ci95, h, kernel, polyOrder, n, nLeft, nRight }`. Render these as a compact two-column table (`Estimate | CI 95%`, `SE | h (IK)`, `N left | N right`, `Kernel | Polynomial order`) in a collapsible "RD Summary" panel below the main coefficient table, styled like the existing DiagnosticsPanel. This is pure UI — no new math. Map the existing result fields; no new engine output is needed for the base case (p=1, triangular kernel, IK bandwidth).
**Invariants:** No React in `src/math/`. The result component is in `src/components/modeling/`.

---

### Spatial · 2026-05-30
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a row-filter control to each geo-plot layer (in `GeoLayerConfig.jsx`) that uses `predicate.js`'s `evalPredicate` to filter the layer's rows before rendering. The filter UI should reuse `FilterBuilder` from `wrangling/CleanTab.jsx` (already available as a standalone component). Since filtering runs on already-processed rows (not raw pipeline data), this can be a view-only filter — it does not add a pipeline step. Flag this clearly in the UI ("Layer filter — display only, not saved to pipeline").
**Invariants:** `predicate.js` is the single owner of filter logic. Never re-implement `evalPredicate`.

---

### Model · 2026-06-06
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/services/export/latexTable.js` (row label generation), `src/components/modeling/` (result display)
**Suggestion:** Add an optional `labelOverrides: { [varName]: string }` parameter to `buildStargazer()`. In the Modeling UI, render an editable label map in the CodeEditor's LaTeX tab — a small table where each coefficient row name has an `<input>` beside it. On change, pass the overrides when the user copies/downloads the LaTeX. Persist the overrides in the model's `spec` (already serialized to IDB). This affects display only — coefficient values are never altered. The RDD-specific labels ("D (treatment)" etc.) are currently hardcoded in the result-display component; they should instead fall through to the user-editable map.
**Invariants:** `latexTable.js` is pure JS. Model spec persistence goes through `indexedDB.js`. Inline styles in the UI panel.

---

### Calculate · 2026-06-19
**Feedback:** `Add limits`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" section to the Calculate workbench (alongside the existing derivative, integral, and optimization sections). Use `math.js` (already loaded via the Calculate tab's CAS) to evaluate `math.parse(expr).limit(...)` — or fall back to numeric evaluation at `x → a±ε`. Cover: one-sided limits (`x → a⁻`, `x → a⁺`), two-sided, and limits at `±∞`. Display the result symbolically where possible and numerically as a fallback. Export as `limit(f, x, a)` in R (pracma package), `sp.limit(f, x, a)` in Python (sympy), and `di` / `_n` style in Stata (no direct equivalent — emit a NOTE).
**Invariants:** CalculateTab is React — inline styles via `C`. CAS operations must not import from `src/math/` or `src/core/` (those are for the econometrics engine, not the algebra workbench).

---

### Calculate · 2026-07-04
**Feedback:** `Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When the user enters a bare constant (e.g. `y = 5` or just `5`) in the equation list, detect it as a constant (parse it with `math.js`; if `evaluate({x: 0}) === evaluate({x: 1})`, it is constant) and render it as a horizontal `<line>` SVG element spanning the full x-domain at `y = constant`. Use a dashed stroke to visually distinguish it from function curves. Allow a label (the constant's expression string or a user-entered name). This mirrors ggplot's `geom_hline(yintercept = c)` behavior. Extend the R export to emit `geom_hline(yintercept = c)` and the Python export to emit `ax.axhline(y = c)`.
**Invariants:** CalculateTab is React; inline styles via `C`. No imports from `src/math/`.

---

### General · 2026-07-05
**Feedback:** `Add plots with 3 axis`
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 (the CDN version in use) does not natively support a secondary y-axis. The standard workaround is to normalize the secondary series to the primary scale, draw it as a separate line/bar, and annotate manually — or to use two overlapping SVG layers with independent y-scales drawn outside the Plot container. Given the architecture (single `Plt.plot()` call), the cleanest scope is: add a "Secondary Y-axis" toggle per layer that normalizes that layer's values to `[0,1]` and draws a right-side scale as an SVG `<text>` overlay (not a Plot `<scale>`). Full dual-axis support requires replacing `Plt.plot()` with a two-canvas approach, which is a larger refactor. Flag the limitation in the HintBox.
**Invariants:** Observable Plot is loaded from CDN (`cdn.jsdelivr.net`) — no local copy. The Plot container is a `<div ref>` in `PlotBuilder.jsx`. Inline styles via `C`.

---

### Model · 2026-07-09
**Feedback:** `Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster`
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a search `<input>` above the variable chip list in `VariableSelector.jsx`. Filter the displayed chip list as the user types (case-insensitive substring match against the header name). Keep selected variables always visible regardless of the filter (a filtered view that hides your selection is confusing). Clear the filter when a chip is added or the panel closes. No new state management needed beyond a local `useState` for the filter string — this is purely a display filter on the existing `headers` array.
**Invariants:** Inline styles via `C`. No external component libraries.

---

### Explore · 2026-07-11
**Feedback:** `Add more labels in tables or boxes when plotting by color`
**File:** `src/components/PlotBuilder.jsx` (legend rendering, ~line 987)
**Suggestion:** When `aes.color` is set, PlotBuilder renders an Observable Plot legend (color swatch + label). The feedback suggests the labels are too sparse — likely the legend only shows the color swatch values, not count or summary stats per group. Add an optional "Show group stats" toggle in the Plot Builder's Labels panel that, when on, appends `(n=N)` to each legend entry using `d3.rollup` or a plain `Map` over the rows. For categorical tile fills, the existing `TILE_SCHEMES` legend could similarly add the min/max of the fill column at either end of the color ramp.
**Invariants:** Observable Plot legend is rendered by `Plt.legend()` inside the CDN-loaded Plot object. Custom text overlays use SVG `<text>` marks, not DOM manipulation.

---

### Clean · 2026-07-17
**Feedback:** `Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step`
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step card in `History.jsx` already has a `step.type` field. Add a click handler on each step card that dispatches a `navigateToStep(step)` event (via a prop callback or the session context) to `WranglingModule.jsx`. `WranglingModule` then maps the step type to the correct tab and sub-section: e.g. `filter` → Clean tab → Filter subsection; `log` → Features tab → Transforms subsection; `group_summarize` → Reshape tab. Set the `activeTab` and any sub-section index state accordingly. For step types with a form (e.g. `case_when`), also pre-populate the draft state (see the save-progress suggestion above). This requires no new API — only routing through existing `setActiveTab` / sub-section state.
**Invariants:** `History.jsx` must not import from `src/math/` or `src/core/`. Navigation routing stays inside `WranglingModule.jsx`.

---

### Clean · 2026-07-19
**Feedback:** `Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform`
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js` (new step `country_code`)
**Suggestion:** Add a `country_code` pipeline step with a small bundled lookup table (~250 rows: ISO2, ISO3, country name variants, UN M49 code). The step accepts a source column (containing country names or codes) and a target format (iso2/iso3/display-name), adds a new column with the mapped value, and leaves unmatched rows as `null`. The lookup table should be a static JSON file at `src/pipeline/data/countryCodes.json` imported at the top of `runner.js`. Wire the UI into `FeatureTab.jsx` under a "Country Codes" subsection in the Transforms tab, and register in `registry.js`. Export to R as `countrycode::countrycode(col, origin, destination)`, Python as `pycountry`, Stata as `kountry` (SSC) with a NOTE.
**Invariants:** The runner step must replay on `rawData` — never mutate in place. Register in `registry.js` to keep it in sync with `runner.js` (T5 validation enforces this).

---

### General · 2026-07-19
**Feedback:** `Think about how to construct lists, save them and use them during the session when filtering or constructing variables — cross-module feature`
**File:** `src/services/session/sessionState.jsx` (new action `ADD_LIST`/`REMOVE_LIST`/`UPDATE_LIST`), `src/components/wrangling/CleanTab.jsx`
**Suggestion:** Add a `namedLists` map to the `SessionStateProvider` context (similar to how `datasets` is a registry). Each list has a `name` and `values: string[]`. Expose a "Lists" flyout (e.g. a `⊞ Lists` button in `WorkspaceBar.jsx`) where users create/edit lists. In `FilterBuilder`, add a `list_in` / `list_nin` operator that resolves its `value` to `namedLists[listId]` at runtime. In `FeatureTab.jsx`'s `case_when` and `if_else` panels, allow selecting a list for the `in` check. Persist named lists to `indexedDB.js` per project. Export `in` with resolved values (since list names are session-local, not portable to R scripts).
**Invariants:** `sessionState.jsx` is React Context — fine here. Use `indexedDB.js` for project-scoped persistence. `predicate.js` remains the single owner of filter logic; extend `OPERATORS` there.

---

### Model · 2026-07-22
**Feedback:** `Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators`
**File:** `src/components/modeling/ModelPlots.jsx`
**Suggestion:** The current binned scatter plot uses fixed defaults. Expose: number of bins (slider, default 20, range 5–100), quantile vs equal-width binning toggle (matching R `binsreg`'s `nbins`/`binsmethod` options), confidence interval level (80/90/95/99%), and polynomial degree for the within-bin fit line. Wire these as a collapsible "Scatter options" panel below the plot, using the existing `Btn`/`Chip` style from `shared.jsx`. Extend the R export to pass these as `binsreg(y, x, ..., nbins=N, degree=P, ci=c(a,b))` arguments.
**Invariants:** `ModelPlots.jsx` is React — inline styles via `C`. No new math in this component — call existing engine functions with the new arguments.

---

### Model · 2026-07-22
**Feedback:** `Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations`
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** Add an "N cap" slider (or numeric input) to the plot controls in `ModelPlots.jsx` that samples at most N rows before passing to the plot renderer. Use a deterministic sample (every k-th row, or `rows.sort(() => 0.5 - seededRandom()).slice(0, N)` with a fixed seed so the plot is reproducible). Show the current N in the plot footer alongside the full dataset size. This is a VIEW filter — it never changes the estimation, only the scatter cloud density. The same control could be added to `PlotBuilder.jsx`'s global plot options. Clearly label it "Display sample (N rows)" to avoid confusion with estimation sample.
**Invariants:** Estimation always runs on the full dataset. The N-cap only affects the rendered point cloud. Inline styles via `C`.

---

## Suggested action order

1. **Confirm P-value fix (critical, 5 min):** Ask Franco to run one regression with |t| < 1 and verify p > 0.5 — if confirmed, close all three duplicate BugTriage entries (2026-07-22, 2026-07-28 ×2) and back-mark ClaudeFB.md.
2. **Confirm browser-pending code-complete items (high, 30 min total):** Franco tests Plot Builder resize/PNG, OLS residual plots, and Spatial heatmap — three separate 10-min sessions; close their BugTriage rows on confirmation.
3. **Fix time series group legend in SVG export (high, ~2h):** Group-by legend is HTML-only; exported SVG/PNG show colored lines with no labels — clear regression for users who export figures. The fix is localized to ~30 lines of SVG `<text>` rendering in `ExplorerModule.jsx`.
4. **Fix Stata workbench (high, ~1d):** Collect the exact Franco error first; then patch the narrowest failing path in `stataScript.js`. Likely one of the documented UNVERIFIED stubs.
5. **Complete Simulate replication script (high, ~2d):** New `simulateScript.js` exporter covering resampling + tests + estimation. Blocks a real research workflow where users simulate data, run tests, and need a reproducible script.
6. **Add hypothesis test history / save-to-R (medium, ~1d):** `CoefficientTestPanel.jsx` local state; `generateJointHypothesisScript` already exists.
7. **Add time series axis labels (medium, ~2h):** Three text inputs wired to existing SVG `<text>` elements — smallest-effort remaining UI gap.
8. **Fix CSV parse-error resilience (medium, ~1d):** DuckDB `IGNORE_ERRORS=TRUE` re-try + banner — prevents data-load failures on common messy CSVs.
9. **LaTeX table auto-resize (medium, ~2h):** One-line `\resizebox` wrapper in `latexTable.js` when `colsN > 5`.
10. **Add country-code transform step (medium, ~1d):** Static lookup table + runner step + FeatureTab UI — high teaching value for LMU Munich international datasets.
