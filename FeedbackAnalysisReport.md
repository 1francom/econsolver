# Feedback Analysis Report — 2026-09-19

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **6 open bugs** · **11 open features** · **3 open UX/design** · **0 open performance** · **0 open other**
- 4 additional low-priority items deferred ("fix-later")
- 5 items are **likely fixed but unconfirmed** by browser testing (noted at bottom)

---

## Bugs

### [high] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/tabs/CalculateTab.jsx` (line 66 — `generateCalcScript` Stata branch)  
**Suggestion:** Audit the Stata code-generation branch in `generateCalcScript`. Currently the scalar/matrix/local generators are present but expressions like `any()`, compound conditional logic, and symbolic math calls have no Stata equivalents and silently emit nothing. Extend the Stata branch to cover every case handled in the R and Python branches, and emit explicit `/* NOT SUPPORTED IN STATA */` comments for unsupported operations rather than silent omission.  
**Invariants:** Pure JS generation function — no React, no external calls. Must not add `fetch` to the Anthropic API; all AI calls go through `AIService.js`.

---

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script — currently it replicates only the DGP  
**File:** `src/components/tabs/SimulateTab.jsx` (Monte Carlo logic ~line 834), `src/services/export/statInferenceScript.js`, `src/services/export/statInferenceGroupScript.js`  
**Suggestion:** The "Copy R / Python / Stata" output in SimulateTab builds only the DGP draw, not the downstream loop (resampling, one-sample tests, or model estimation on the simulated data). Extend the script builder to emit a full MC loop: sample the DGP N times, run the configured downstream test (mean/variance/two-sample/OLS) per draw, and collect the output distribution. The `runMonteCarlo` function already aggregates results server-side — mirror that loop in the exported script.  
**Invariants:** `src/math/` and `src/core/` are pure JS — no React or fetch calls may be added there. All AI calls go through `AIService.js`.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work  
**File:** `src/ExplorerModule.jsx` (line 237 — `SvgMiniTimeSeries`; line 1749 — main chart legend)  
**Suggestion:** `SvgMiniTimeSeries` (used for pinned time-series cards) draws colored lines but renders **no group legend**. The series `grp` key is used only for the SVG `key` prop (line 271), so a pinned chart with multiple groups is uninterpretable — lines have different colors but no labels. Add a compact inline legend below the SVG (group name + color swatch) matching the main chart's color palette so pinned charts remain interpretable without the interactive panel. The color for `si` should also match the main chart's `COLORS[]` constant.  
**Invariants:** Inline styles via the `C` / `T` (theme) objects only — no external CSS or UI libraries.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.  
**File:** `src/ExplorerModule.jsx` (time series panel ~line 1455), `src/components/PlotBuilder.jsx`  
**Suggestion:** The time series line chart hard-codes its axis labels as `tCol` and `agg(yCol)` strings (lines 1740–1744). Add optional text-input fields for custom chart title, x-axis label, and y-axis label in the time series controls panel (similar to the PlotBuilder's existing `labelsPanel`). Persist them in the `tsView` pin params so exported/pinned charts carry the user's annotations.  
**Invariants:** Inline styles only. The SVG chart is pure JS with no external charting lib — new inputs should live in the React wrapper, not in the `<svg>`.

---

### [medium] · General · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js` (line 169 — `\begin{tabular}` builder), `src/ReportingModule.jsx`  
**Suggestion:** `latexTable.js` emits a plain `\begin{tabular}` with no width guard — on a model with many covariates the table overflows the page margin. Wrap the `tabular` in `\resizebox{\textwidth}{!}{…}` (from the `graphicx` package) when the column count exceeds a threshold (e.g. > 6). Emit `\usepackage{graphicx}` in a comment above the block. Keep the threshold configurable internally (a constant) so it can be tuned without touching the public API.  
**Invariants:** Pure JS export function, no React. The generated LaTeX is a string — no DOM manipulation.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `CoefficientTestPanel` currently runs one hypothesis at a time and discards it on the next run. Add a "Save hypothesis" button that appends the current test (constraint string + result) to a local list, and a "Copy all to R/Python/Stata" button that emits the full vector of `car::linearHypothesis(fit, c("H1","H2","H3"))` calls. The saved list should be scoped to the currently-estimated model (cleared on re-estimation) and stored in component state (no persistence required). `generateJointHypothesisScript()` in `ModelHypothesis.js` already handles single tests; extend it to accept an array.  
**Invariants:** `src/math/ModelHypothesis.js` is pure JS — no React imports permitted.

---

## UX / Design

### [medium] · General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/components/workspace/DatasetManager.jsx`  
**Suggestion:** Extend the `artifactOrder` service with a `group` string field per artifact (default `"default"`). In `ModelBufferBar`, add a "Group…" rename chip per pinned model. In the model comparison table and the buffer bar, render one collapsible section per group. Persist group assignments in IndexedDB alongside the existing artifact order. Start with a minimal implementation: free-text group name input, no drag-and-drop between groups.  
**Invariants:** Persistence must use `indexedDB.js` — no `localStorage`. Inline styles only.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going to Clean to look up variable names, coming back and finding it cleared  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** Each transformation panel (case_when, mutate, if_else, etc.) loses its in-progress form state when the user navigates to a different tab within the Clean module. Lift the draft state for each builder into a `useRef` or a `sessionStorage` key scoped to `pid + stepType` so switching tabs does not unmount and destroy it. Only clear the draft on explicit Apply/Cancel. This does not require IndexedDB — sessionStorage is appropriate here as it is transient per-session UI state (not data or pipeline state).  
**Invariants:** Do not use `localStorage` for pipeline or data storage. Session UI drafts may use `sessionStorage`.

---

### [low] · General · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` constant)  
**Suggestion:** Audit `TOUR_STEPS` against the current UI. Known stale entries: the Pipeline column location was changed, WLS is now an OLS toggle not a separate sidebar estimator, the W selector is controls-only (not "weights / instruments / controls"), and `tile` is the 12th geom not the 11th. Update each step's `text` field to describe the current UI state. Per the working conventions: never write a count ("14 estimators") — name the things instead.  
**Invariants:** `TOUR_STEPS` is plain JS data — no React or AI calls.

---

## Features

### [medium] · Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** Add a "Limits" entry to the Calculate tab's operation menu. The CAS/symbolic engine already handles derivatives and integrals; `lim_{x→a} f(x)` can be approximated numerically (left/right/two-sided) with a configurable approach value `a`, and many common closed-form limits reduce to derivative quotients (L'Hôpital's rule). Start with numeric evaluation and emit the symbolic form as a comment in the replication script.  
**Invariants:** Math logic must live in `src/math/` or `src/components/tabs/CalculateTab.jsx` — zero React imports in any `src/math/` file.

---

### [medium] · Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The function graph canvas already plots arbitrary expressions. Extend it so a scalar variable defined in the Variables section (type `Scalar`) is rendered as a dashed horizontal line at its value when overlaid on the graph. This reuses the existing graph rendering path — simply detect scalar variables and append an `hline` rule at their value (similar to the `hline` geom in `PlotBuilder.jsx`). No new UI surface needed beyond a "show as reference line" checkbox in the variable row.  
**Invariants:** Inline styles only. No external charting library — the graph is rendered in a `<canvas>` or SVG element inline in CalculateTab.

---

### [medium] · Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a debounced text-input filter above each variable list in `VariableSelector`. Filter the displayed column names client-side using `name.toLowerCase().includes(query)`. Keep the search box always visible (not a toggle) since its presence signals the feature exists. Clearing the input restores the full list. No fuzzy matching needed — exact substring is sufficient and cheaper.  
**Invariants:** Inline styles only. State lives in `VariableSelector` — no global state change.

---

### [medium] · Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators  
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`  
**Suggestion:** The current binned scatter is fixed at 20 quantile bins. Expose: `nbins` (slider, 5–100), `absorb` (boolean — partial out controls before binning, i.e. added-variable plot mode), and `xrange` (min/max cutoff). These map directly to `binsreg`'s / `rdplot`'s most-used arguments. Render controls in a collapsible panel under the plot, not in the main config sidebar, so they are only visible when the plot is expanded.  
**Invariants:** Inline styles only. Plot rendering stays in `ModelPlots.jsx` — no new math files needed (binning is already in `LinearEngine.js`).

---

### [medium] · Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations  
**File:** `src/components/PlotBuilder.jsx`, `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** Add an "Observations" slider in the PlotBuilder's labels/settings panel that sub-samples the plot data to N rows before rendering (random sample, seeded for reproducibility). This reduces visual clutter at large n without affecting estimates. The sub-sampled count should be shown in the plot footer. Emit a `set.seed()` / `np.random.seed()` call in the replication script when a sub-sample is active.  
**Invariants:** Sub-sampling is display-only — it must never affect the estimation path, the pipeline, or any persisted data. Inline styles only.

---

### [medium] · Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`), `src/services/AI/AIService.js`  
**Suggestion:** Extend `INTERPRET_REGRESSION_PROMPT` with a dedicated elasticity rule: when `result.type` is `"OLS"` or `"FE"` and the user-supplied variable dictionary flags a variable as "log-transformed" (or the column name starts with `log_`/`ln_`), the narrative should compute the semi-elasticity or elasticity interpretation automatically (log-log = percentage change; log-linear = percentage change in Y per unit change in X; linear-log = unit change in Y per 1% change in X). The `inferVariableUnits` output already captures log-status — pass it alongside `result.spec` in the user block.  
**Invariants:** All Anthropic calls go through `callClaude()` in `AIService.js`. No raw `fetch` to the Anthropic API. `SHARED_CONTEXT` cached block must remain present on every call.

---

### [medium] · Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When a layer uses `aes.color` to split by a categorical variable, PlotBuilder renders colored points/lines but no legend labels on the chart itself (they only appear in the legend box). Add a "show color labels" toggle (default off) that renders the group name next to the last point of each series (or at the right edge for line charts) using an `Plt.text` mark — this is already how Observable Plot's `tip`/`text` marks work for inline labeling. Emit `geom_text(..., hjust = 0)` in R and an annotation loop in Python/Stata.  
**Invariants:** No changes to `src/math/` or `src/core/`. Inline styles only for any new UI controls.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** Each step card in the History sidebar already knows its `type` (e.g. `"case_when"`, `"lag"`, `"join"`). Add a small edit icon (✎) to each step card that emits a `onEditStep(step)` callback. In `WranglingModule`, handle it by (1) switching to the appropriate tab (Clean/Feature/Reshape/Merge) and (2) populating the relevant builder form with the step's current params so the user can modify and re-apply it. The routing is a `switch(step.type)` on the STEP_REGISTRY's `category` field, which already maps steps to tabs.  
**Invariants:** `STEP_REGISTRY` in `registry.js` must stay in sync with `runner.js`. The step must be re-applied via the normal pipeline dispatch path — never mutate pipeline steps in place (non-destructive pipeline invariant).

---

### [low] · Clean · 2026-07-19
**Feedback:** Add a function for Country Codes (iso2/iso3/country-name crosswalk)  
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`  
**Suggestion:** Add a `country_code` pipeline step (Formatting subtab in FeatureTab) that looks up a column against a small static crosswalk table (iso2 ↔ iso3 ↔ common name, ~250 rows bundled as a JS constant). The step emits a new column with the target format. Fuzzy matching is out of scope for v1 — exact match only. Use the existing `wrangling/utils.js` pattern for lookup transforms. Register the new step type in `registry.js` and add a `runner.js` case; update the R/Py/Stata translators.  
**Invariants:** Registry must stay in sync with runner. No external API call — lookup table is bundled client-side (privacy-first). `src/math/` must remain React-free.

---

### [low] · General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`  
**Suggestion:** This is a design-first item. A session-scoped named-list store (e.g. `{ "EU countries": ["DE","FR","IT",...] }`) would allow reuse across filter predicates, `case_when` conditions, and `recode` maps. The natural home is `sessionState.jsx` as a new `lists` key alongside `datasets`. The UI entry point could be a small "Lists" panel in the WorkspaceBar or a "Save as list" button on any `%in%` filter builder. **Recommendation: design the data model first** (list name, type: string/numeric, values array, source dataset) before implementation, since it touches four modules (Clean filter, Feature case_when, Explore filter, pipeline serialization).  
**Invariants:** Persistence for named lists should use IndexedDB (`indexedDB.js`), not localStorage.

---

### [low] · General · 2026-07-05
**Feedback:** Add plots with 3 axis (3D plots)  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot (the current CDN library) does not support 3D rendering. A 3D scatter would require a separate library (e.g. Plotly.js via CDN). The simplest scoped approach is a 2D scatter with a third variable encoded as point SIZE (`aes.size` mapping) or an interactive pseudo-3D using two panels (top + side projections). This is lower value than the other open features; recommend deferring until the 2D PlotBuilder feature set is stable.  
**Invariants:** External scripts may only be loaded from `cdnjs.cloudflare.com` or `cdn.jsdelivr.net/npm/`. Any new CDN dependency must also be added to `connect-src` in `vercel.json` (see CSP bug history). If Plotly is added, `vercel.json` must list `cdn.jsdelivr.net` and `plot.ly` in `connect-src`.

---

## Fix-Later Items (deferred, low priority)

| Date | Module | Feedback | File | Notes |
|------|--------|----------|------|-------|
| 2026-05-19 | Clean | R-style `%in% c(...)` syntax not accepted by filter UI | `src/components/wrangling/CleanTab.jsx` | `predicate.js` owns the canonical filter language; `%in%` should translate to the `in` operator |
| 2026-05-21 | Model | RDD result lacks `summary(rdrobust)` compact table | `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx` | Presentation only; current estimates available elsewhere |
| 2026-05-30 | Spatial | Plot builder has no spatial row-filter for layers | `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` | Route to V4 spatial plots pass |
| 2026-06-06 | Model | LaTeX table variable names not editable (RDD always shows "D (treatment)") | `src/ReportingModule.jsx`, model result display | Metadata labelling; blocked on a clean label-override API |

---

## Pending Confirmation (not yet browser-verified)

These items are code-complete or "likely fixed" per commit notes and BugTriage, but have not been confirmed in the browser by Franco:

| Date | Module | Feedback | Commit/PR | Status |
|------|--------|----------|-----------|--------|
| 2026-07-22/28 | Model | P-values wrong for small/insignificant t-stats (3 reports) | `216b1ecb` — `betacf`/`betainc` Lentz fix | Needs Franco confirmation |
| 2026-07-09 | Model | FE must accept any categorical variable; FE with interactions | `045f5cda` (#104) — N-way FE shipped | Needs Franco confirmation |
| 2026-06-10/11 | Model | Plot Builder size + residual/QQ plots missing | `35f4b5a6` — frozen `result.spec` wiring | Needs Franco confirmation |
| 2026-06-13 | Spatial | Heatmap display broken | `5e105529` — Leaflet KDE layer restored | Needs Franco confirmation |

---

## Suggested action order

1. **[high / Calculate]** Fix Stata workbench in `CalculateTab.jsx` — Stata branch of `generateCalcScript` silently omits unsupported operations; high-touch module for LMU users.
2. **[high / Simulate]** Extend SimulateTab replication script to include the full Monte Carlo loop, not just the DGP draw — export integrity gap.
3. **[medium / Explore]** Add group legend to pinned time series mini-charts (`SvgMiniTimeSeries` in `ExplorerModule.jsx`) — currently uninterpretable when grouped.
4. **[medium / Model]** Add multiple hypothesis test accumulation + "Copy all" to `CoefficientTestPanel` + `ModelHypothesis.js` — blocked research workflow.
5. **[medium / General]** Fix LaTeX table overflow for wide models in `latexTable.js` via `\resizebox{\textwidth}{!}{}` — cosmetic but affects publication-ready output.
6. **[medium / Clean]** Draft-state persistence for transformation builders in `FeatureTab.jsx` — high annoyance factor, sessionStorage fix is trivial.
7. **[medium / Model]** Add variable searcher in `VariableSelector.jsx` — low effort, high usability gain for large datasets.
8. **[medium / Explore]** Add time series label controls (title, axis) — affects all pinned time series pins.
9. **[medium / Model]** Extend elasticity interpretation in `INTERPRET_REGRESSION_PROMPT` — one-line prompt addition with high research value.
10. **[low / General]** Update guide tour `TOUR_STEPS` in `HelpSystem.jsx` — stale since WLS toggle, tile geom, and pipeline sidebar changes.
