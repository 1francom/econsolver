# Feedback Analysis Report — 2026-07-08

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 7 open bugs · 7 open features · 0 open UX · 1 open performance · 0 open other
- **5 bugs are code-complete and blocked only on browser validation by Franco** — these should be the first action.
- 5 features are explicitly deferred ("fix-later"); 2 are newly submitted with no triage entry.

Cross-reference note: items that are struck-through or marked ✓ in ClaudeFB.md, or confirmed "fixed" in BugTriage.md and the X5 reconciliation table, are excluded. All 15 items below are unresolved as of 2026-07-08.

---

## Bugs

### medium · Explore · 2026-05-11
**Feedback:** The graph does not fit the screen.
**File:** `src/components/PlotBuilder.jsx` (line ~1626), `src/ExplorerModule.jsx`
**Suggestion:** Code-complete in `35f4b5a6` — the plot container now uses `resize: "vertical"` with `minHeight: 220` / `maxHeight: "70vh"` and a `ResizeObserver` at line 1171 that redraws the SVG on container resize. Browser validation needed: open the Plot Builder in ExplorerModule, load a wide dataset, confirm the canvas fits the viewport without horizontal overflow or clipping, and confirm the resize handle allows vertical stretching. If overflow persists, inspect whether the `ResizeObserver` is attached to the correct container element.
**Invariants:** Inline styles only — no external CSS or Tailwind. `C` constants from `shared.jsx` for all colour values.

---

### high · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code is complete (`35f4b5a6`) — OLS/WLS diagnostic plots now render from the frozen `result.spec` rather than live sidebar state. Task is to open a browser session, run an OLS model, and confirm all three plots (Y~Ŷ scatter, Residual vs Fitted, Q-Q) appear and remain stable when adding/removing variables without re-estimating.
**Invariants:** Diagnostic results are derived from the frozen `EstimationResult` — no direct mutation of `rawData` or React state from the math layer.

---

### high · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** Code is complete (`35f4b5a6`) — OLS/WLS display is frozen to `result.spec`. Browser validation needed: add a variable after estimation and confirm the plotted coefficients and BP/DW/JB values do not change; then pin the model, swap to a different estimator, restore the pin, and verify diagnostics reappear correctly.
**Invariants:** Diagnostic panels must read only from the immutable `EstimationResult` returned by the engine, never from live selector state.

---

### high · Sync · 2026-07-06
**Feedback:** Sharing a project with another user fails with an RLS error ("Share not found or you are not the intended recipient.") even when the recipient email is correct.
**File:** `supabase/migrations/20260706120000_fix_project_shares_email_case.sql`, `src/services/sync/shareEngine.js`
**Suggestion:** Migration is applied and all 7 RLS policies are verified present. Remaining step is a browser end-to-end test: create a share → log in as the recipient → import the share. Also test owner self-restore and `revokeShare` list. If "Object not found" reappears, the `share_blobs_owner_select` policy may not have been applied — re-apply the migration and verify with `list_policies` in the Supabase dashboard.
**Invariants:** RLS changes are Supabase-side only. `shareEngine.js` is the sole egress path for share operations — no localStorage usage allowed for share metadata.

---

### medium · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx` (line ~1626), `src/components/ModelingTab.jsx`
**Suggestion:** Code is complete (`35f4b5a6`) — the plot viewport has a native CSS `resize: vertical` wrapper with bounded responsive canvas sizing. Browser validation needed: drag the resize handle to confirm the canvas reflows correctly, then trigger PNG export at the resized dimensions and confirm no clipping or style bleed in the exported image.
**Invariants:** Styles must use inline objects with `C` constants from `shared.jsx` — no CSS file edits.

---

### medium · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code is complete (`5e105529`) — Heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5 k-point cap. Browser validation needed: load a point dataset with lat/lon columns, select Heatmap layer type, confirm the gradient renders, adjust opacity, and export HTML.
**Invariants:** Map rendering is Leaflet-based (CDN singleton via `src/components/tabs/spatial/shared/leaflet.js`). The 5 k-point cap is intentional to avoid OOM on large datasets — do not remove it.

---

### medium · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47… — import aborted on malformed row.
**File:** `src/DataStudio.jsx` (CSV parse path), `src/services/data/parsers/` (no dedicated CSV parser — DuckDB path governs files >10 MB)
**Suggestion:** When a CSV parse error occurs, surface the offending line number and raw content in an inline warning banner rather than aborting the import. Offer "Skip bad rows and continue" (drops malformed lines, logs skipped count) or "Cancel". For the DuckDB path, `read_csv_auto` supports `IGNORE_ERRORS=TRUE` — wire that flag to a user opt-in. For the JS/Papa Parse path, wrap per-row parsing in a try/catch and collect failures.
**Invariants:** Non-destructive pipeline invariant applies — partial data must not be stored silently under the same dataset key; bad rows must be surfaced and user-acknowledged before import completes.

---

## Features

### Clean · 2026-05-19 (fix-later)
**Feedback:** Add filter(`regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")`) for filter option — R-style `%in% c(...)` membership expression syntax.
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder, line ~631–770), `src/services/exprEvalService.js`
**Suggestion:** The FilterBuilder already has an `in`/`nin` operator and `Array.includes` evaluation (CleanTab.jsx:767–768). The missing piece is a pre-pass parser in `exprEvalService.js` that detects `<col> %in% c(<csv-vals>)` syntax via regex and translates it to the internal `{op:"in", values:[...]}` node before evaluation. No changes to `runner.js` or `registry.js` are needed.
**Invariants:** The runner replays on `rawData` — the translated filter must serialise to a valid `filter` pipeline step. Expression parsing belongs in `exprEvalService.js`, not inside the runner.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))` — showing bandwidth, effective N, kernel, and polynomial order alongside the LATE estimate.
**File:** `src/math/CausalEngine.js` (Sharp RDD result), `src/components/ModelingTab.jsx` (RDD result display section)
**Suggestion:** `runSharpRDD` already returns `bandwidth`, `nLeft`, `nRight`, `kernel`, and `polyOrder` in the `EstimationResult`. Add a compact summary panel in `ModelingTab.jsx` (or extract to `src/components/modeling/results/RDDSummaryPanel.jsx`) that renders these fields in an `rdrobust`-style header table above the coefficient row. No engine changes are needed — the data is already present.
**Invariants:** Any new display component is pure React/JSX and must not import from `src/math/`. Use `C` constants for all styles.

---

### Model · 2026-05-25 (fix-later)
**Feedback:** Add an option to estimate without intercept (suppressed constant).
**File:** `src/math/LinearEngine.js` (`runOLS`, `runWLS`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add a `noIntercept: boolean` flag to the engine function signatures. In `runOLS`/`runWLS`, when `noIntercept=true`, skip prepending the ones column to the design matrix and adjust degrees of freedom to `n - k` (rather than `n - k - 1`). Expose the toggle as a checkbox in `ModelConfiguration.jsx` under Advanced options. Follow the `seType` pattern: the flag is passed explicitly from the UI → `estimate()` → engine, never inferred or hardcoded inside the engine.
**Invariants:** `seType` architecture invariant applies symmetrically — `noIntercept` must be an explicit argument to engines, never inferred internally. Validate against R `lm(y ~ x - 1)` to 6 decimal places on coefficients and 4 on SE before shipping.

---

### Spatial · 2026-05-30 (fix-later, routed to V4)
**Feedback:** Allow spatial filters for plots — row-level spatial predicate (e.g. filter to a bounding box or polygon) scoped to a specific layer.
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer bounding-box filter control in `GeoLayerConfig.jsx`: an "Apply map view as filter" button that reads the current SVG viewport bounds and pre-filters rows before the geometry render loop. For a more interactive version, add a draw-mode rectangle that stores bbox coordinates in layer config. This is view-level only — it must not inject a `filter` pipeline step or modify `rawData`.
**Invariants:** Spatial filtering is a display-layer concern — must not mutate `rawData` or add pipeline steps without explicit user action.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names.
**File:** `src/math/CausalEngine.js` (line 579, hardcoded `varNames` array), `src/ReportingModule.jsx` (Stargazer/LaTeX render path)
**Suggestion:** Add a `coefLabels` override map to `EstimationResult` (e.g. `{ "D (treatment)": userInputLabel }`). Expose an editable two-column table in `ReportingModule.jsx` (or a `LabelEditor.jsx` sub-component) mapping internal coefficient names to display labels. The LaTeX/Stargazer render reads `coefLabels[name] ?? name`. The engine at `CausalEngine.js:579` does not need to change — label editing is purely a display-layer concern.
**Invariants:** `src/math/` files must remain pure JS with no React imports. All label editing logic belongs in `ReportingModule.jsx` or `src/components/modeling/`. Labels should be stored per-result in `EstimationResult.coefLabels`, not globally.

---

### Calculate · 2026-06-19 (new — no BugTriage entry yet)
**Feedback:** Add limits (mathematical limit as x → a) to the Calculate tab.
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" mode alongside the existing Derivative/Integral/Optimize modes. Implement numerically: evaluate the expression at values approaching the limit point from both sides (e.g. `a ± 1e-7, 1e-8, 1e-9`) and display `lim x→a f(x) = L`. For discontinuous cases surface both one-sided limits. The existing CAS expression evaluator already handles standard functions — no new CDN dependency is needed.
**Invariants:** All logic stays within `CalculateTab.jsx`. No new pipeline step required — limits are a calculator output, not a data transformation. No imports from `src/math/`.

---

### Calculate · 2026-07-04 (new — no BugTriage entry yet)
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves).
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When a user enters an expression with no `x` variable (e.g. `y = 3`), detect it as a constant and render a horizontal line at that y-value on the same canvas as the function plots. Check whether the parsed AST contains any `x`-dependent node; if not, add a `{type:"hline", y:c}` layer. The `hline` geom already exists in `PlotBuilder.jsx` and can serve as an implementation reference.
**Invariants:** Inline styles only. The canvas must continue to handle mixed constant + function expressions simultaneously (e.g. `y = sin(x)` and `y = 2` plotted together on the same axes).

---

## Performance

### high · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows.
**File:** `src/DataStudio.jsx` (large-file branch, ~line 403), `src/services/data/duckdb.js`
**Suggestion:** Fase X4 is code-complete — large CSV/TSV files are converted to Parquet via DuckDB and persisted to OPFS; tab transitions no longer re-materialise the full JS array. Browser benchmark still pending. To validate: load the 21 MB / 900 k-row CSV, navigate between tabs (Data → Clean → Model → Explore), and confirm no OOM crash and tab-transition latency under 2 s. Verify the `⚡ DuckDB` badge appears in the wrangling header and all model statistics use full `n` (not the 500-row preview cap).
**Invariants:** Display is always capped at 500 rows for UI rendering, but all pipeline operations (`runner.js`, `duckdbRunner.js`) must operate on the full dataset. Never truncate `rawData` before passing it to a pipeline engine.

---

## Suggested action order

1. **Browser-validate the five `35f4b5a6` / `5e105529` code-complete fixes** (Explore graph fit, Residual/Q-Q plots, Diagnostics freeze, Plot Builder resize, Spatial heatmap) — one browser session unblocks the entire code-complete backlog.
2. **Browser-verify the Share/Sync RLS fix** (`create share → recipient import → owner self-restore`) — live auth-path regression risk.
3. **Browser-benchmark Fase X4 large-dataset performance** — load the 900 k-row fixture, confirm no crash, sub-2 s tab transitions, and full `n` in model output.
4. **Resilient CSV import** (malformed-row skip/repair UX in `DataStudio.jsx`) — blocks real-world messy datasets from aborting silently.
5. **No-intercept estimation toggle** — engine change in `LinearEngine.js` required; validate against R `lm(y ~ x - 1)` before shipping.
6. **Calculator limits** (`CalculateTab.jsx`) — self-contained addition, no cross-cutting dependencies.
7. **Plot constants in graph** (`CalculateTab.jsx`) — batch with limits work for efficiency.
8. **Editable LaTeX coefficient labels** (`ReportingModule.jsx`) — display-layer only, no engine changes.
9. **RDD rdrobust-style summary panel** — `EstimationResult` already has the data; pure UI addition.
10. **R `%in% c(...)` filter syntax** (`exprEvalService.js` pre-pass) — low-risk, satisfies a power-user workflow.
