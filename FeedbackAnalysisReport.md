# Feedback Analysis Report — 2026-06-20

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.
> Supersedes 2026-06-19 report.

## Summary

- **6 open bugs** (1 critical, 3 high, 2 medium)
- **0 open UX / design**
- **1 open performance** (code-complete, pending browser benchmark)
- **6 open features** (fix-later or newly open)
- **1 open other** (security follow-up)
- **⏳ 4 items code-complete pending browser validation by Franco**

Cross-reference note: many ClaudeFB.md bullets lack ~~strikethrough~~ but are confirmed fixed in BugTriage or the X5 reconciliation table. This report excludes those. Only items with BugTriage status `open` / `fix-later` / `code-complete (pending validation)` or no BugTriage entry are included.

---

## Bugs

### [critical] · Model / Pipeline · 2026-06-17

**Feedback:** "Pipeline only applies if dataset is also selected in Clean Module"

**File:** `src/App.jsx:2845–2853` (`handleComplete`, `handleOutputReady`), `src/DataStudio.jsx:1139`

**Suggestion:** `tabOutput(tab)` is keyed by dataset ID (`outputs[tabDsId(tab)]` at `App.jsx:2811`), and the only code paths that populate `outputs` are (a) `handleComplete` — which stores the Clean tab's active dataset output, and (b) `handleOutputReady` — which is called from `DataStudio.onReady` at line 1139, a callback only fired by the `WranglingModule` mounted in the Clean tab. If the Model tab's `activeDatasetIds["model"]` resolves to a dataset that was never the Clean tab's active dataset, `tabOutput("model")` is always null and `rawToCleaned()` is used, silently dropping all pipeline steps. Fix: when `tabDsId("model")` changes to a dataset with no stored output, trigger a background pipeline run for that dataset from IDB — either by exposing a `runForDataset(dsId)` method on the `DataStudio` ref, or by having each dataset's `WranglingModule` auto-emit `onReady` for all loaded datasets on project open, not just the one currently visible in Clean.

**Invariants:** Pipeline must replay on `rawData` non-destructively (runner.js). No React in math files. Do not read from localStorage.

---

### [high] · Model · 2026-06-11 ⏳

**Feedback:** "Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS"

**File:** `src/components/ModelingTab.jsx:2325`, `src/components/modeling/ResidualPlots.jsx`

**Suggestion:** Code shipped in commit `35f4b5a6` — plots are wired and the OLS/WLS display is frozen to `result.spec` so sidebar edits cannot mutate the displayed model. Requires browser validation by Franco: load a dataset, run OLS, confirm all three plots (Y~Ŷ scatter, Residual vs Fitted, Q-Q) render below the coefficient table; then change a variable in the sidebar and confirm the plots do not update until re-estimation.

**Invariants:** Inline styles via `C` object only — ResidualPlots.jsx is a UI-only component.

---

### [high] · Model · 2026-06-14 ⏳

**Feedback:** "Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models."

**File:** `src/components/ModelingTab.jsx`

**Suggestion:** Code shipped in commit `35f4b5a6` — diagnostics now render from the estimation's frozen `result.spec`. Requires browser validation by Franco: run an OLS, note the displayed coefficients, add/remove variables in the sidebar, and confirm the coefficient panel stays frozen until the user explicitly re-runs estimation; also restore a pinned model from the model buffer and confirm its coefficients replace the display correctly.

**Invariants:** None beyond the standard inline-styles rule.

---

### [medium] · Model · 2026-06-10 ⏳

**Feedback:** "Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems"

**File:** `src/components/PlotBuilder.jsx:1626`, `src/components/ModelingTab.jsx`

**Suggestion:** Code shipped in commit `35f4b5a6` — the plot viewport has a native vertical resize handle (`resize: "vertical"`, `minHeight: 220`, `maxHeight: "70vh"`) and bounded canvas sizing so PNG captures user-selected dimensions. Requires browser validation by Franco: drag the plot container's resize handle and confirm the canvas fills the new height; export PNG and confirm it matches the on-screen dimensions without clipping or whitespace.

**Invariants:** Inline styles only. No CSS files.

---

### [medium] · Spatial · 2026-06-13 ⏳

**Feedback:** "Heatmap must be fixed"

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx:20–843`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`

**Suggestion:** Code shipped in commit `5e105529` — heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, and HTML export with a deterministic 5k-point cap. Requires browser validation by Franco: open Spatial → Map tab, add a heatmap layer with lat/lon columns, confirm KDE cells render on the basemap, adjust opacity/palette, and confirm the legend updates correctly.

**Invariants:** Leaflet loaded via CDN singleton in `src/components/tabs/spatial/shared/leaflet.js`. No server-side computation — all KDE is client-side.

---

### [medium] · Data · 2026-05-10

**Feedback:** "Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47..." (malformed CSV row aborts import with no recovery option)

**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/` (CSV path)

**Suggestion:** Post-launch resilient-import pass: catch row-level parse errors, record the original row number and raw text, and surface a "Skip N malformed rows / Show preview" dialog before committing the dataset. For DuckDB-backed large files, the DuckDB CSV reader's `IGNORE_ERRORS` mode can be enabled and skipped-row counts surfaced in the import summary (`duckdb.js`). For the JS path in `DataStudio.parseFile`, wrap the per-row parser in a try/catch, accumulate errors, and prompt the user instead of hard-aborting.

**Invariants:** Skipped rows must be reported, never silently dropped. The non-destructive pipeline invariant applies to post-import data — skipped rows affect raw data intake, which is outside the pipeline's scope.

---

## Performance

### [high] · Data · 2026-05-08 ⏳

**Feedback:** "Big datasets generate crashes (21MB) over 900k rows"

**File:** `src/DataStudio.jsx` (parseFile large-file branch), `src/services/data/duckdb.js`

**Suggestion:** Fase X4 is code-complete (CSV/TSV >10 MB routed to DuckDB, 500-row UI preview, OPFS Parquet cache). Requires browser benchmark by Franco: load the 900k-row / 21 MB fixture and confirm (a) no tab-transition lag, (b) pipeline steps run on the full DuckDB table, not the 500-row preview, (c) the ⚡ DuckDB badge appears, and (d) re-opening the project loads from the OPFS Parquet cache. If any check fails, inspect `DataStudio.jsx:parseFile` large-CSV branch and `duckdb.js:loadCSVToDuckDB`.

**Invariants:** Per CLAUDE.md DuckDB rules: `SELECT *` on large tables is forbidden; display limit (500 rows) must never truncate the computation dataset that pipeline and estimation operate on.

---

## Features

### Model · 2026-05-21

**Feedback:** "Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression"

**File:** `src/components/modeling/results/FuzzyRDDResults.jsx`, `src/components/modeling/results/index.js`, `src/math/CausalEngine.js`

**Suggestion:** `CausalEngine.runSharpRDD` already returns `h`, `b` (IK bandwidths), `n_left`, `n_right`, `order`, `kernel`, `bwselect`. Add a `SharpRDDSummaryTable` sub-component inside the sharp-RDD results panel that renders these as a compact block mirroring R's `summary(rdrobust)` output: Bandwidth (h, b), Kernel, Polynomial order, Obs (left, right), Effective obs (left, right), then the existing coefficient / SE / 95% CI / p-value table. Wire it into `src/components/modeling/results/index.js`. `FuzzyRDDResults.jsx` can receive a parallel treatment.

**Invariants:** UI only — do not re-run estimation inside the component. Inline styles via `C` object.

---

### Model · 2026-05-25

**Feedback:** "Add an option to estimate without intercept"

**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`, `src/math/CausalEngine.js`, `src/math/NonLinearEngine.js`, `src/math/GMMEngine.js`

**Suggestion:** Add an `includeIntercept` boolean (default `true`) to the estimation spec, exposed in `ModelConfiguration` as a checkbox and passed into every engine that builds the design matrix. In `LinearEngine`, the intercept column (`ones`) is conditionally included; `varNames` must exclude `"const"` when absent. This is an engine-wide contract change — `CausalEngine` (RDD), `PanelEngine` (FD), `NonLinearEngine`, `GMMEngine`, and the DuckDB suff-stats paths all need to respect the flag. The replication-script exporters (`rScript.js`, `pythonScript.js`, `stataScript.js`) must emit `-1` / `noconstant` / `nocons` when the intercept is removed. Suggested approach: ship as a `LinearEngine` / `WLS`-only option first, then extend to other engines.

**Invariants:** `seType` must continue to be passed explicitly — never hardcode inside engine functions. The `EstimationResult` shape must carry `includeIntercept` so `ModelComparison` and replication exports can reflect the spec.

---

### Clean · 2026-05-19

**Feedback:** "Add filter(regionname %in% c('Spain (Espana)', 'Basque Country (Pais Vasco)')) for filter option"

**File:** `src/services/exprEvalService.js`, `src/pipeline/runner.js:273`

**Suggestion:** The filter expression runner at `runner.js:273` compiles user expressions to JS via `new Function(...)`. Add a `preprocessFilterExpr(expr)` utility in `exprEvalService.js` that transforms R-style `%in%` membership syntax before compilation: replace `x %in% c(a, b, c)` → `[a, b, c].includes(x)` using a regex like `/(\w+)\s*%in%\s*c\(([^)]+)\)/g`. Call it in the `filter` case at `runner.js:273` before passing to `new Function`. This covers the dominant use case without a full R-to-JS transpiler.

**Invariants:** The `new Function` sandbox is already guarded by the `safeH` header list — pre-processing must not expand the expression surface in a way that bypasses that guard. Only transform `%in% c(...)` patterns, not arbitrary R syntax.

---

### Model · 2026-06-06

**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"

**File:** `src/services/export/latexTable.js:44` (`varNames` loop), `src/ReportingModule.jsx:280` (`buildStargazer` call)

**Suggestion:** Add an optional `varLabels` map (`{ [varName]: string }`) to the LaTeX export config, editable in the Report tab via a small inline table (one row per coefficient, pre-filled with the current display name, editable in place). In `buildStargazer` at `latexTable.js:44`, replace `v` with `varLabels?.[v] ?? v` when building row headers. RDD display names (`"D (treatment)"`, etc.) come from `varNames` returned by `CausalEngine` — those stay as defaults but become overridable. Persist `varLabels` in the project's IndexedDB `model_buffer` store alongside the result.

**Invariants:** Persistence must use `src/services/Persistence/indexedDB.js` — not localStorage. Inline styles only via `C` object.

---

### Spatial · 2026-05-30

**Feedback:** "Allow spatial filters for plots"

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Add a per-layer filter row in `GeoLayerConfig` (column selector + operator dropdown + value input, matching the pattern of `CleanTab.jsx`'s `FilterBuilder`). Apply the filter to the layer's rows client-side before passing to `mkGeoLayer` in `src/components/tabs/spatial/plot/geo.js`. This is non-destructive — it does not mutate `rawData` or touch the pipeline. For the Leaflet map tab, the same per-layer filter pattern can be added to `SpatialLayerEditor`.

**Invariants:** Filters must apply only to the render-path copy of layer rows, never to `rawData`. No React in `src/math/` or `src/core/`.

---

### Calculate · 2026-06-19

**Feedback:** "Add limits"

**File:** `src/components/tabs/CalculateTab.jsx:1556` (tool tab bar), `src/components/tabs/CalculateTab.jsx:1169` (activeTool state)

**Suggestion:** Add a `"limit"` entry to the tool tab bar at `CalculateTab.jsx:1556` alongside the existing solver/derivative/symbolic/algebra/integral buttons. The limit UI needs: expression input, variable name, approach point (may be `±∞`), and side (both / left / right). Implement numerical evaluation as a pure-JS utility (no React) that evaluates the expression at `x₀ ± ε` for decreasing `ε` sequences and checks convergence; expose it as `computeLimit(fn, x0, side)` importable into `CalculateTab.jsx`. For `±∞` limits, substitute a large value (e.g. `1e10`). Display the result with the existing KaTeX render path. A `"show steps"` column showing the ε-sequence converging is a nice-to-have.

**Invariants:** `src/math/` and `src/core/` must have zero React imports — implement the limit engine as a pure-JS utility (can live in `src/services/math/limits.js` or alongside existing `evalExpression` utilities) and import it into `CalculateTab.jsx`.

---

## Other / Security

### Security follow-up · 2026-06-13

**Feedback:** XSS injection attempts via feedback form. Client-side `sanitizeFeedback` is now in place at the choke point, but BugTriage notes "Hard server-side boundary (Supabase trigger/validation) recommended as follow-up since client sanitization is bypassable by direct POST."

**File:** `supabase/functions/` (edge function or DB trigger on the feedback table)

**Suggestion:** Add a Postgres `BEFORE INSERT` trigger on the feedback `description` column that rejects payloads containing `<script`, `javascript:`, `onerror=`, `onload=` patterns and enforces a 5000-character cap at the DB layer. Alternatively, add input validation inside the existing Supabase edge function that receives feedback before writing to the table. This makes sanitization durable even if the client-side call is bypassed by a direct API POST.

**Invariants:** All Anthropic API calls must go through `AIService.js` — this is a Supabase DB / edge-function concern, not an AI service concern. No client data leaves the browser in plaintext to the app server per the privacy-first constraint — the feedback path is the only exception and must be hardened.

---

## Suggested action order

1. **Fix pipeline-not-applied-in-Model bug** (`src/App.jsx:2852`, BugTriage `open`) — critical regression that silently drops all data cleaning from Model tab when a different dataset is selected there than in Clean.
2. **Franco browser-validates commits `35f4b5a6` and `5e105529`** — four code-complete items (OLS residual plots, frozen diagnostics, Plot Builder resize, spatial heatmap) are blocking sign-off on recent work; validate all four in one browser session.
3. **Add server-side XSS guard** (`supabase/functions/`) — client sanitization is bypassable by direct POST; add a DB trigger or edge-function check before launch.
4. **Add limit support to Calculator** (`src/components/tabs/CalculateTab.jsx:1556`) — only fully open feature with no existing code path; pure-JS utility plus one new tool tab.
5. **Wire `%in%` filter syntax** (`src/services/exprEvalService.js`) — low-effort regex pre-processing step, high user value for multi-value categorical filters.
6. **LaTeX variable name editing** (`src/services/export/latexTable.js:44`) — RDD hard-codes display names regardless of actual column names; `varLabels` override map is a self-contained change to `buildStargazer`.
