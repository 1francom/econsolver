# Feedback Analysis Report — 2026-06-05

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 6 open bugs (2 needs-repro · 1 pending · 3 confirmed open) · 14 open features · 0 open UX · 1 open performance · 0 open other

---

## Bugs

### [high] · Report / AI · 2026-05-25
**Feedback:** AI does not recognize the pipeline steps in the Script generation
**File:** `src/services/AI/sessionSnapshot.js`, `src/ReportingModule.jsx`, `src/services/AI/Prompts/index.js`
**Suggestion:** `buildSessionSnapshot` and `serializeSnapshot` in `sessionSnapshot.js` are implemented (CLAUDE.md pending item 5b) but marked "VALIDATION PENDING 2026-05-30". Verify that `snapshot.pipeline` is non-empty when `interpretRegression` and `generateUnifiedScript` fire in `ReportingModule.jsx`, and that the serialised pipeline block reaches the user payload in `UNIFIED_SCRIPT_PROMPT`. Add a `console.warn` guard if `snapshot.pipeline` is empty so the gap surfaces in the browser console during validation.
**Invariants:** All Anthropic calls must go through `AIService.js` only. Pipeline steps must not be re-derived from UI state; use the serialised snapshot.

---

### [high] · Model / Panel · 2026-05-28
**Feedback:** Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators — currently only generic TWFE event study, which is biased under staggered treatment
**File:** `src/components/modeling/EstimatorSidebar.jsx`, `src/components/ModelingTab.jsx`, `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Both math engines exist (`NonLinearEngine.runSunAbraham` and `src/math/CallawayEngine.js`). `CallawayCS` is already a sidebar entry but Sun-Abraham is only reachable as a sub-variant of "EventStudy" via the Poisson toggle. Add `{ id: "SunAbraham", label: "Sun-Abraham (2021)", group: "Panel", desc: "IW interaction-weighted event study — robust to staggered treatment" }` as an explicit entry in `EstimatorSidebar.jsx` and route its dispatch in `ModelingTab.jsx` directly to `runSunAbraham`. Add a visible "biased under staggered adoption" warning label to the generic TWFE "Event Study" entry.
**Invariants:** `src/math/` files are already React-free — no new React imports may be added there. SE type must be passed explicitly to `runSunAbraham` via `seOpts`.

---

### [high] · Data · 2026-05-13
**Feedback:** .dta files are wrong loaded and readed
**File:** `src/services/data/parsers/stata.js`
**BugTriage status:** pending (a format-119 stride fix was applied 2026-06-03 for a duplicate-column bug, but this 2026-05-13 item may describe a different loading problem)
**Suggestion:** Reproduce with the original file used on 2026-05-13. Audit `stata.js` `readValLabels` and `decodeString` for format-117 edge cases (variable labels with multibyte characters, date-storage columns, and strL string columns). If the file is format-117, check that the dta117 branch handles all column types. Compare the loaded column count and types against `codebook` output from Stata itself.
**Invariants:** Pure JS parser — no React imports needed.

---

### [critical → needs-repro] · Model / Math · 2026-05-21
**Feedback:** **URGENT** There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible
**File:** `src/math/EstimationResult.js`, `src/components/modeling/resultDisplay.jsx`
**BugTriage status:** needs reproducer — full audit of all 11 engines found no t/p desync. Most likely cause: user read the β̂ column (0.49) as the t-column. Table layout is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig`.
**Suggestion:** Add a column-header tooltip in `src/components/modeling/resultDisplay.jsx` that spells out each column name on hover (e.g. "β̂ — estimated coefficient", "t — test statistic"). This prevents β̂/t-stat confusion at zero implementation cost. If a screenshot confirms a genuine desync, the fix would be in `buildCI95` / `pValue` in `EstimationResult.js` around lines 217 and 260.
**Invariants:** None specific.

---

### [critical → needs-repro] · Calculate · 2026-06-02
**Feedback:** crash latex export
**File:** `src/components/tabs/CalculateTab.jsx` (lines 283–296: `copyLatex`, `downloadTex`, `openOverleaf`)
**BugTriage status:** needs-repro — code audit found no crash path; may have been transient.
**Suggestion:** The three export paths all read the `latex` state variable. A crash would occur if `latex` is `undefined` at call time (e.g. a KaTeX parse error leaves `latex` in an inconsistent state, or the equation field is empty). Guard `copyLatex` and `downloadTex` with `if (!latex) return;` before lines 284 and 289. Also verify that `src/components/calculate/workbench/katexLoader.js` does not leave `latex` in a torn state after a render exception.
**Invariants:** None specific.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated)
**File:** `src/DataStudio.jsx` (CSV / DuckDB large-file path), `src/services/data/` (auto-delimiter detection)
**Suggestion:** This looks like a mid-file encoding break or a quoted field with an embedded newline that the CSV parser misreads. The large-file path goes through DuckDB's `read_csv_auto`; surface the DuckDB error message verbatim in the UI instead of re-wrapping it. Try adding `ignore_errors=true` to the DuckDB `read_csv_auto` call and display a warning count ("N rows skipped due to parse errors") using the existing ⚡ DuckDB banner pattern.
**Invariants:** DuckDB result must stay in DuckDB — do not pull the 900k-row result into a JS array. Badge pattern from `WranglingModule` should be reused.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/WranglingModule.jsx`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Per CLAUDE.md's agreed DuckDB performance rules: (1) confirm the CSV→Parquet auto-convert path fires for files >10 MB on first load so subsequent queries hit cached Parquet; (2) ensure `WranglingModule` never materialises the full 900k-row Arrow result into a JS object array — use `SELECT … LIMIT 500` for the preview and run full computations inside DuckDB SQL; (3) add OPFS persistence so re-opening the project skips re-import. The crash itself is most likely a JS heap OOM during a `SELECT *` Arrow materialisation in the wrangling or explorer path.
**Invariants:** Dataset must never leave the browser (privacy-first). No server-side computation. Use `indexedDB.js` (not localStorage) for any persistence metadata. Display limit = 200–500 rows; computation limit = full dataset via DuckDB.

---

## Features

### Model · 2026-05-11
**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength
**File:** `src/math/CausalEngine.js`, `src/components/modeling/results/IVResults.jsx`
**Suggestion:** Anderson-Rubin CI inverts the AR test statistic over a grid of null hypotheses for β. Add `computeARConfidenceSet(y, x, z, beta_grid, alpha)` to `CausalEngine.js` that returns `[lo, hi]` by scanning a β grid and checking whether the AR F-statistic exceeds the χ²(k_z)/k_z critical value. Expose it in `IVResults.jsx` as a collapsible "Weak-instrument robust CI" row below the standard CI.
**Invariants:** `src/math/` must have zero React imports. SE type must be passed explicitly.

---

### Model · 2026-05-11
**Feedback:** Add multiple statistics tests
**File:** `src/math/SampleTests.js`, `src/components/tabs/statsim/SampleTestPanel.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** `SampleTests.js` already implements one-sample t, variance χ², and generic parameter z/t. Extend it with two-sample Welch t-test, Mann-Whitney U, Kolmogorov-Smirnov, and chi-square goodness-of-fit as pure-JS functions. Wire them into `SampleTestPanel.jsx` via a test-type selector dropdown.
**Invariants:** `src/math/` and `src/core/` must have zero React imports.

---

### Simulate · 2026-05-18
**Feedback:** Add resampling, bootstrapping, sampling to the mean
**File:** `src/math/Resampling.js`, `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** `Resampling.js` already exists. Verify whether the StatWorkspace "Resampling" tab surfaces bootstrap CI for the mean and other statistics. If `Resampling.js` only implements basic sample-without-replacement, extend it with `bootstrapCI(data, statFn, B=2000, alpha=0.05)` using percentile and BCa methods, then wire the result into the StatWorkspace resampling panel.
**Invariants:** Pure JS in `src/math/Resampling.js` — no React imports.

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/pipeline/runner.js` (`filter` step)
**Suggestion:** Add an "is one of" operator to `FilterBuilder` in `CleanTab.jsx` that renders a multi-value tag input (comma-separated entry). In `runner.js`, the `filter` step evaluates a predicate — add an `in` predicate that checks `values.includes(row[col])` for categorical columns. The DuckDB SQL path should emit `WHERE col IN ('v1','v2',...)`. Register the new operator in `registry.js` if the operator set is stored there.
**Invariants:** Pipeline steps must replay on `rawData` — the `filter` step result must be derivable from `rawData` + step params alone. Registry must stay in sync with `runner.js`.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/geo.js`
**Suggestion:** The SVG geo-plot tab currently supports point/polygon layers but not a grid overlay. Add a "Grid overlay" layer type in `GeoLayerConfig.jsx` that accepts a grid-cell dataset (output of `GridSection`) and renders it as filled rectangles in `GeoPlotCanvas.jsx`. Reuse the H3/rect grid aggregation already computed by `GridSection`; pass the centroid lat/lon + value column as layer config.
**Invariants:** No external UI libraries. Inline styles via the `C` object from `shared.jsx`.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/modeling/results/` (RDD result display), `src/components/ModelingTab.jsx`
**Suggestion:** Create `src/components/modeling/results/RDDResults.jsx` mirroring the `rdrobust` summary format: bandwidth (h, b), kernel, polynomial order, effective n (left/right), conventional/bias-corrected/robust estimate rows with SE and CI. The data is already in the result object from `CausalEngine.runSharpRDD`; it needs a dedicated display component analogous to the existing `IVResults.jsx`.
**Invariants:** No React imports in `src/math/`.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`
**Suggestion:** Add a "No intercept" checkbox to `ModelConfiguration.jsx`. Pass `{ noIntercept: true }` through the existing `opts` parameter. In `LinearEngine.js` `runOLS`, when `noIntercept` is true, skip prepending the ones column to `X`. Propagate to the DuckDB suff-stats path: the SQL X'X builder must omit the constant column. R² is undefined without an intercept — show a warning label in the result table.
**Invariants:** SE type must be passed explicitly — `noIntercept` must not change which SE variant is used internally.

---

### Spatial · 2026-05-30
**Feedback:** Geometry transformations and show EPSG
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`, `src/components/tabs/spatial/shared/crs.js`
**Suggestion:** `CRSTransformSection.jsx` and `crs.js` already implement CRS transformation via `loadProj4` and `PRESET_CRS`. Add an `epsg` field to each entry in the `PRESET_CRS` object in `crs.js` (e.g. `{ name: "WGS84", epsg: 4326, proj4: "+proj=longlat ..." }`). Display the EPSG code in `CRSTransformSection.jsx` after transformation. For free-form WKT input, parse the `AUTHORITY["EPSG","..."]` substring to extract the code.
**Invariants:** No external UI libraries; inline styles via the `C` object.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a filter row to `GeoLayerConfig.jsx` that allows the user to select a column and operator/value filter (reuse the same operator set as `FilterBuilder` in `CleanTab.jsx`). In `GeoPlotCanvas.jsx`, apply the filter before rendering layer features so only matching geometries are drawn. The filter operates on the in-memory layer data (already a JS array at render time for the SVG plot).
**Invariants:** No external UI libraries.

---

### Data · 2026-06-01
**Feedback:** Add an option to edit/assign column names directly in the Data Viewer
**File:** `src/ExplorerModule.jsx`, `src/pipeline/runner.js` (`rename` step)
**Suggestion:** In the data-viewer table header in `ExplorerModule.jsx`, make column headers double-click-editable: render a text input on double-click, and on blur/Enter dispatch a `rename` pipeline step with `{ from: oldName, to: newName }`. This reuses the existing `rename` step in `runner.js` without any new step type. Show a pencil icon on header hover to signal editability.
**Invariants:** Must dispatch a pipeline step (non-destructive replay on `rawData`) — never mutate `rawData` directly. Use `indexedDB.js` for persistence.

---

### Simulate · 2026-06-02
**Feedback:** You could add different types of quantiles like in R, but it might not be necessary
**File:** `src/components/tabs/statsim/StatWorkspace.jsx`, `src/math/Resampling.js`
**Suggestion:** R supports 9 quantile types differing in interpolation. Types 7 (default R), 6 (Excel/SPSS), and 2 (SAS) cover most use cases. Add a "Quantile type" selector (dropdown: Type 1, 6, 7, 8, 9) in `StatWorkspace.jsx` and implement the interpolation variants in a `quantile(data, p, type=7)` helper in `Resampling.js`. Low priority — implement only if quantile accuracy is relevant to the target use case.
**Invariants:** Pure JS in `src/math/` — no React imports.

---

### Spatial · 2026-06-03
**Feedback:** Add geometry to plot points, not just lat and lon
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`, `src/components/tabs/spatial/shared/wkt.js`
**Suggestion:** `wkt.js` already has `wktToLeaflet` and `parseWktRings`. In `GeoLayerConfig.jsx`, allow a "geometry column" source (WKT column) in addition to lat/lon. In `GeoPlotCanvas.jsx`, when `layer.geomCol` is set, call `parseWktRings` to get ring coordinates and render them as SVG `<polygon>` / `<polyline>` elements. This mirrors what the Leaflet Map tab already does via `wktToLeaflet`.
**Invariants:** No external UI libraries.

---

### Calculate · 2026-06-03
**Feedback:** Add a "show steps button" for integrals, derivatives and optimizations
**File:** `src/components/tabs/CalculateTab.jsx`, `src/math/symbolicDiff.js`, `src/math/cas/`
**Suggestion:** `symbolicDiff.js` computes symbolic derivatives but returns only the final expression. Add a `diffWithSteps(expr)` function that returns an array of `{ rule, before, after }` intermediate steps (power rule, chain rule, product rule). Render them in `CalculateTab.jsx` as a collapsible "Show derivation" panel below the result, using the existing collapsible HintBox/Section pattern. For integrals, surface integration-by-substitution steps if the CAS backend in `src/math/cas/` supports it.
**Invariants:** `src/math/` must have zero React imports. Steps are pure data structures; rendering belongs in the `CalculateTab.jsx` component only.

---

### Model · 2026-06-04
**Feedback:** Persistence across sessions of pinned models
**File:** `src/services/Persistence/indexedDB.js`, `src/components/ModelingTab.jsx`, `src/services/modelBuffer.js`
**Suggestion:** `indexedDB.js` already has per-`pid` stores; the v6 `coach_chats` store (`saveCoachChats`/`loadCoachChats`/`deleteCoachChats`) is a direct template to follow. Add a `pinnedModels` object store keyed by `pid` with `savePinnedModels(pid, models)` and `loadPinnedModels(pid)` functions. In `ModelingTab.jsx`, call `savePinnedModels` on every `setPinnedModels` update (debounced ~500 ms) and `loadPinnedModels` on mount inside `useEffect`. Must use `indexedDB.js` — localStorage is explicitly deprecated.
**Invariants:** Must use `services/Persistence/indexedDB.js`. Scoped strictly to the current `pid` — no cross-project bleed.

---

## Suggested action order

1. **AI script pipeline recognition** (`sessionSnapshot.js` / `ReportingModule.jsx`) — confirmed open, blocks core research-output reproducibility; partially implemented but not validated.
2. **Event Study UI split: Sun-Abraham as first-class sidebar entry** (`EstimatorSidebar.jsx`, `ModelingTab.jsx`) — open bug; engines exist, wiring is a small change; critical for methodological correctness under staggered DiD.
3. **Pinned model persistence across sessions** (`indexedDB.js`, `ModelingTab.jsx`) — most recent feedback (2026-06-04), high UX impact, straightforward implementation following the `saveCoachChats` template.
4. **Large-dataset 900k-row crash** (`DataStudio.jsx`, `WranglingModule.jsx`) — performance blocker for PhD-student users with real panel data; enforce `SELECT … LIMIT 500` preview and confirm Arrow materialisation is the crash cause.
5. **Anderson-Rubin CI for IV** (`CausalEngine.js`, `IVResults.jsx`) — high methodological value for weak-instrument robustness; well-defined math, no architectural changes needed.
6. **p-value / t-stat display confusion** (`resultDisplay.jsx`) — add column-header tooltips to prevent recurring misreads; zero-risk fix that closes a needs-repro bug.
