# Feedback Analysis Report — 2026-08-24

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary

- **6 open bugs** (1 critical, 3 high, 2 medium) · **7 open features** · **8 open UX/design** · **1 open performance** · **0 open other**

Items tagged `code-complete [validation pending]` have code merged but have not received Franco's browser confirmation.

---

## Bugs

---

### high · Export · 2026-07-05
**Feedback:** `Replication code for CS DiD 2021 does not work`
**File:** `src/services/export/rScript.js` (line 1432 — `CallawayCS` case), `src/math/CallawayEngine.js`
**Suggestion:** The `CallawayCS` R export block at `rScript.js:1432` emits `att_gt()` with `base_period = "varying"`, but the `did` package's default is `"universal"`. Verify the `compGroup` and `csAnticipation` fields are present on the `model` object reaching this case; if `model.spec` wrapping is involved, destructure from `model.spec` first. Add at least one smoke-test fixture (a 3-cohort balanced panel) to the export-integrity harness in `src/services/export/__validation__/` to prevent silent regressions.
**Invariants:** Export functions must be pure JS in `src/services/export/`; no React imports. The math engine (`CallawayEngine.js`) must not be called from an export function — use the already-computed `result` object only.

---

### high · Export · 2026-07-05
**Feedback:** `Replication codes of the plots must have the lines to load datasets as well`
**File:** `src/components/PlotBuilder.jsx` (prop `scriptPreamble`, line 1528–1538), `src/ExplorerModule.jsx` (line 2631 — `<PlotBuilder>` mount)
**Suggestion:** `ExplorerModule.jsx` mounts `PlotBuilder` without a `scriptPreamble` prop. The generated script therefore starts at the plotting layer with no `df` defined. Pass a `scriptPreamble` function to the `<PlotBuilder>` in `ExplorerModule.jsx` that calls `buildRLoadLine` / `buildPyLoadLine` / `buildStataLoadLine` (already in `src/services/export/loadLine.js`) using the dataset's `datasetName` and `loadOpts`. The `PlotBuilder` already conditionally prepends `preamble` at line 1538 and switches to `plot_df` as a variable name when a preamble is present — the Explore tab just never passes one.
**Invariants:** `buildRLoadLine` et al. are pure export utilities (no React). The `PlotBuilder` component must not import load-line helpers directly; those belong in the parent.

---

### high · Pipeline · 2026-07-17
**Feedback:** `Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group`
**File:** `src/pipeline/runner.js` (line 528 — `group_summarize` case), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** The engine already has `twoSampleMeanTest` and `splitByGroup` in `src/math/SampleTests.js`. The gap is the input shape: users need a long-format "by group" path (outcome col + group col → ATE as difference) rather than two separate columns. Add a `diff_means` aggregate type to the `group_summarize` step that computes `mean(y | group=1) − mean(y | group=0)` inline, or expose the existing `SampleTestPanel` two-sample mode in `ReshapeTab` as a post-summarise overlay. Either approach avoids teaching users a three-step workaround (group_summarize → pivot_wider → mutate, which silently names columns `"0"` and `"1"` and breaks `mutate`'s identifier filter).
**Invariants:** `runner.js` steps must replay on `rawData`; never mutate in place. No React in `src/math/SampleTests.js`.

---

### high · Export · 2026-07-17
**Feedback:** `Stata workbench doesn't work completely`
**File:** `src/services/export/stataScript.js`, `src/components/modeling/CodeEditor.jsx`
**Suggestion:** The BugTriage entry is open with no root-cause note. Audit `generateStataScript()` for estimator cases that fall through without a `case` match (the function signature is at line 14), and run the 168-cell SE × estimator verification matrix described in the HC2 fix notes against the Stata column specifically. Known gaps from prior fixes: (1) two-way clustering routes to `reghdfe` — confirm `reghdfe` is emitted and the package note is included; (2) FE HAC Stata was emitting `"NW"` (now should be Driscoll-Kraay). Add a Stata-column fixture in the export-integrity harness that covers at least OLS, FE, FD, 2SLS, RDD, Poisson, and CallawayCS paths.
**Invariants:** All three export functions (`rScript.js`, `pythonScript.js`, `stataScript.js`) must honour `seType` and `clusterVar` from `config` — never hardcode SE variant inside the translator.

---

### medium · Export · 2026-07-05
**Feedback:** `Adjust the size of LaTeX tables when the number of columns increases`
**File:** `src/services/export/latexTable.js` (line 36 — `colFmt`), `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` builds `colFmt = "l" + " r".repeat(colsN)` with no width guards. When `colsN` exceeds ~6, the tabular overflows a standard page. Add a `\resizebox{\textwidth}{!}{...}` wrapper or switch to `tabularx` with `X` columns when `colsN > 4`. A lighter option is to emit `\footnotesize` inside the table environment when `colsN > 5`. Include the column count check in `buildStargazer`'s options so callers can override.
**Invariants:** `latexTable.js` is pure JS with no React. The output string must remain valid LaTeX — no broken environment delimiters.

---

### medium · Math · 2026-07-17
**Feedback:** `Add chi-square for linearhypothesis`
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx` (line 211)
**Suggestion:** `waldTest()` in `ModelHypothesis.js` already computes `chiSq` and `chiSqPval` (confirmed at lines 203–213), and `CoefficientTestPanel.jsx` already renders `jointResult.chiSq` at line 211. The gap is likely that the Wald chi-square panel is only shown for the *joint* test, not for single-coefficient hypotheses where the user supplies `H₀: β = c`. Confirm that (a) the per-coefficient test path also calls `waldTest` and (b) the result includes `chiSq`. If the single-coefficient path only returns a t-statistic, add `chiSq: tStat**2, chiSqPval` so both F and χ² appear together (they are equivalent for q=1). Update the R snippet emitted by `ModelHypothesis.modelHypothesisRSnippet` to include `linearHypothesis(..., test = "Chisq")`.
**Invariants:** `ModelHypothesis.js` is pure JS in `src/math/`; no React imports permitted.

---

## Features

---

### Calculate · 2026-06-19
**Feedback:** `Add limits`
**File:** `src/components/tabs/CalculateTab.jsx`, `src/components/calculate/workbench/operations.js`
**Suggestion:** Add a `limit` operation to the Calculate workbench. The CAS engine in `src/math/cas/` (already used for derivatives and integrals) can implement one-sided and two-sided limits via the existing symbolic evaluation infrastructure. Surface it as a new "Limit" button alongside "Derivative" and "Integral" in the workbench panel, with inputs for variable and limit point. Export should emit `lim(f(x), x, a)` (Python SymPy syntax) and `\lim_{x \to a} f(x)` in LaTeX.
**Invariants:** No React in `src/math/`; the CAS engine files must remain pure JS.

---

### General · 2026-07-05
**Feedback:** `Add plots with 3 axis`
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot (0.6 CDN, already loaded) does not natively support a third quantitative axis. The practical scope here is a dual-Y-axis chart (two y-scales) or a bubble chart (size encodes a third variable). The bubble case is already possible with `aes.size` if it is wired into the point geom's `r` accessor — check whether `PlotBuilder` supports a size aesthetic already. For dual-Y, Observable Plot requires two separate `Plot.plot` calls overlaid with CSS `position:absolute`, which is complex; this is best flagged as a design session item rather than a drop-in addition.
**Invariants:** All styles inline via the `C` object; no CSS files.

---

### Model · 2026-07-09
**Feedback:** `Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster`
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text `<input>` filter at the top of each variable panel (Y, X, W) in `VariableSelector.jsx` that filters the displayed header chips by substring match as the user types. Keep existing chip styling (from `shared.jsx`); hide non-matching chips rather than unmounting them so reordering state is preserved. Clear the filter on selection.
**Invariants:** Inline styles only via `C`; no external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** `Add more labels in tables or boxes when plotting by color`
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, the legend currently shows color swatches. Extend the legend to optionally show the group count (n) or summary statistic (mean Y) per category alongside the swatch. This can be done by pre-aggregating `rows` by the color column inside `PlotBuilder` and appending the label to each legend entry. A toggle (`"color labels: off / n / mean"`) in the Labels panel controls it.
**Invariants:** Aggregation must happen client-side on `rows` (the current in-memory dataset), not via DuckDB, to keep the plot responsive.

---

### Clean · 2026-07-19
**Feedback:** `Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk`
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`
**Suggestion:** Add a `country_code` pipeline step type. Bundle a static lookup table (~250 rows: iso2, iso3, country name, continent) as a JS module in `src/services/data/countryCodes.js`. The step takes an input column (auto-detected as iso2/iso3/name via pattern matching) and output type, and adds the mapped column. Wire it into `FeatureTab.jsx` as a new sub-section under "Features" (similar to `lag`/`lead`). Register in `registry.js` and add to `runner.js`. Export translators: R uses `countrycode::countrycode()`, Python uses `pycountry`, Stata uses `kountry`.
**Invariants:** The lookup table must be pure JS data (no React). `runner.js` must replay on `rawData`. Register in `registry.js` and keep pipeline reliability validation green.

---

### General · 2026-07-19
**Feedback:** `Think about how to construct lists, save them and use them during the session when filtering or constructing variables`
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`, `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** This is a design-first item. The natural home is a session-scoped list store inside `sessionState.jsx` (`SessionListsContext`), keyed by user-chosen names and persisted via `indexedDB.js`. Lists would be usable in `filter` predicates (`value IN @list_name`) and `case_when` expressions. Before implementation, Franco should confirm the primary use case (geographic lists for filter vs. value sets for recoding) since the two have different UIs and pipeline-step contracts. Do not implement until scope is agreed.
**Invariants:** Persistence must use `indexedDB.js`, never `localStorage`. Any new pipeline step type must be registered in `registry.js` and handled in `runner.js`.

---

### Clean · 2026-07-17
**Feedback:** `Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step`
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Add an "edit" icon (✎) to each step card in `History.jsx`. On click, dispatch an event (or callback) to `WranglingModule.jsx` that sets the active wrangling tab and, within the tab, scrolls to or highlights the relevant section. The step's `type` field maps to tab (`log`/`sq`/`std` → Feature, `filter`/`normalize_cats` → Clean, `join`/`append` → Merge, `group_summarize`/`pivot_longer` → Reshape). A lookup table in `WranglingModule` translates `step.type → { tab, section }`.
**Invariants:** Inline styles only. `History.jsx` is a pure display component; the navigation logic should live in `WranglingModule.jsx`.

---

## UX / Design

---

### UX · 2026-07-17
**Feedback:** `Guide tour must be updated`
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS registry, line 11)
**Suggestion:** Audit `TOUR_STEPS` against the current module list. Known stale items from the 2026-07-30 HintBox audit: WLS is now an OLS toggle (not a sidebar estimator), W is controls-only, the estimator list has grown since the tour was written, tile/facet geoms added, CR2/CR3 SE types not mentioned. Update each TOUR_STEPS entry to reflect the current state. Per CLAUDE.md: never write a count into help copy — name items instead of saying "N estimators". Also update `HintBox` copy in the 8 help panels where outdated descriptions were flagged.
**Invariants:** No React-state changes needed; this is a pure copy update.

---

### UX · 2026-07-17
**Feedback:** `Create groups for pinned models, plots, and maps, so not everything is compared at the same time`
**File:** `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/artifactOrder.js`
**Suggestion:** Add a `group` (string) field to each artifact entry in `artifactOrder.js` and to the model buffer entries in `modelBuffer.js`. In `ModelBufferBar.jsx`, render a collapsible group header row above each group's models. Groups are created by drag-and-drop or a "New Group" button. Groups are persisted in `indexedDB.js` as part of `saveArtifactOrder`. This is a medium-effort UX addition; a simpler V1 is just a user-editable label field per artifact that the comparison table filters by.
**Invariants:** Persistence via `indexedDB.js` only (never `localStorage`). `ModelBufferBar.jsx` uses inline styles via `C`.

---

### UX · 2026-07-17
**Feedback:** `Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases`
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Persist the draft state of the active builder (if_else and case_when form fields) into component state at the `WranglingModule` level — not `FeatureTab` local state — so navigating away to another tab and back restores the draft. The fields to persist are: `mode`, `cwCases`, `cwNN`, `ifElseCond`, `ifElseTrue`, `ifElseFalse`, `newCol`. Use a `useRef`-backed draft store in `WranglingModule.jsx` (no IndexedDB needed — just survives tab switches within the same session). Clear the draft when the step is committed.
**Invariants:** Do not persist drafts to `indexedDB.js` — they are transient session state. Do not use `localStorage`.

---

### UX · 2026-07-17
**Feedback:** `Add arrange functions in the data viewer`
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Add clickable column headers to the data viewer table that toggle sort order (ascending → descending → unsorted). For DuckDB-backed datasets, push `ORDER BY col [ASC|DESC]` into `getTablePage`'s SQL query (the comment at ExplorerModule.jsx line 162 already references `ORDER BY` for pagination). For small in-memory datasets, sort `filteredRows` before slicing the display window. Show a ▲/▼ indicator in the header. This is a display-only sort (does not add an `arrange` pipeline step); make that distinction clear to the user via a tooltip.
**Invariants:** The display sort must never mutate `rawData` — only the rendered slice. Pipeline `arrange` step in `runner.js` remains the non-destructive sort for persisted pipeline operations.

---

### UX · fix-later · 2026-07-09
**Feedback:** `Time series plot missing label controls (title, axis, etc.)`
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** The Labels panel in `PlotBuilder.jsx` (already present for other chart types) needs to surface controls for the time series geom specifically: chart title, x-axis label, y-axis label, and legend title. Confirm whether the time series plot in `ExplorerModule.jsx` uses the `PlotBuilder` layer system or a separate bespoke renderer (the Explorer's ACF/PACF path is separate); if separate, the label controls need to be added there too.
**Invariants:** Inline styles via `C`. No external charting libraries beyond Observable Plot 0.6 (already loaded).

---

### UX · fix-later · 2026-06-14
**Feedback:** `LaTeX coefficient table display names cannot be edited independently of source columns`
**File:** `src/services/export/latexTable.js` (line 22 — `varLabels` option), `src/components/modeling/resultDisplay.jsx`, `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` already accepts a `varLabels` options map (line 22). The gap is a UI to populate it: add an editable label overlay in `resultDisplay.jsx` (or `ReportingModule.jsx`) that lets users type a display name per variable name, stored in component state and passed into `buildStargazer({ varLabels })`. The RDD hardcoded names ("D (treatment)", "running − c", etc.) should be replaced with the actual column names for the label-override mechanism to be useful.
**Invariants:** `latexTable.js` is pure JS. Labels are display-only; they must not be written back to `rawData` or `pipeline`.

---

### UX · fix-later · 2026-07-09
**Feedback:** `Add direct elasticity interpretation`
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)
**Suggestion:** The existing prompt (lines 99–104) already describes elasticity for log-log and semi-elasticity for log-level. The gap is a **UI-level display** of the computed elasticity value alongside the coefficient — computed directly as `β × (mean_x / mean_y)` for level-level models, without waiting for the AI narrative. Add an "Elasticity at mean" column to the coefficient table in `resultDisplay.jsx` when the user enables it via a toggle. This is a pure math computation that belongs in `src/math/LinearEngine.js` (or a small helper) and must not involve an AI call.
**Invariants:** Math helpers in `src/math/` must have zero React imports. The AI narrative update (if any) is a secondary concern; the computed column is primary.

---

## Performance

---

### high · Performance · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (parseFile branches per format), `src/services/data/duckdb.js`
**Suggestion:** The `.dta` → DuckDB path is code-complete (BugTriage 2026-06-10). The remaining gap is that `.rds`, `.xlsx`, and `.shp` files above 10MB are still parsed entirely in JS and never routed through DuckDB's OPFS Parquet cache. For each format: (1) parse the raw bytes into an Arrow-compatible record batch; (2) call `duckdb.js`'s existing OPFS cache path to write a Parquet file; (3) return the 500-row preview with the `_duckdb` handle. The OPFS cache path and DuckDB ingestion utilities already exist — this is a wiring task per format, not new infrastructure.
**Invariants:** Data never leaves the browser. The OPFS Parquet path must go through `duckdb.js`; do not open the DuckDB connection from `DataStudio.jsx` directly. Per the performance strategy: always pass `_duckdb` through the `onComplete` payload (that was the DuckDB n=500 bug fixed in #100).

---

## Validation-pending items (code-complete, browser confirmation needed)

These are tracked in BugTriage as `code-complete` but have not received Franco's browser sign-off. They are not "open" bugs but are listed here as a reminder:

| Item | BugTriage date | File | Status |
|------|----------------|------|--------|
| Plot Builder resize + PNG aesthetics | 2026-06-10 | `PlotBuilder.jsx`, `ModelingTab.jsx` | `35f4b5a6` — pending Franco |
| Y~Yhat / Residual / Q-Q plots for OLS | 2026-06-11 | `ResidualPlots.jsx`, `ModelingTab.jsx` | `35f4b5a6` — pending Franco |
| Spatial heatmap restored | 2026-06-13 | `SpatialPlotTab.jsx`, `SpatialLayerEditor.jsx` | `5e105529` — pending Franco |
| HC2/HC3 R replication code | 2026-07-17 | `rScript.js`, `stataScript.js` | code-complete 2026-07-18 — pending Franco |

---

## Suggested action order

1. **[critical] Plot replication code omits load lines** (`ExplorerModule.jsx` → pass `scriptPreamble` to `PlotBuilder`) — a one-line prop addition; highest user-visible correctness gap since exported scripts currently fail to run.
2. **[high] CS DiD 2021 R replication code broken** (`rScript.js:1432`) — confirm field destructuring from `model` vs `model.spec`, add smoke fixture to harness.
3. **[high] group_summarize cannot compute ATE as difference in means** (`runner.js`, `ReshapeTab.jsx`) — medium effort; the math already exists in `SampleTests.js`, only the input-shape adapter and UI toggle are missing.
4. **[high] Stata workbench incomplete** (`stataScript.js`) — audit all estimator cases for fallthrough; run the SE × estimator matrix against Stata output.
5. **[medium] LaTeX table overflow with many columns** (`latexTable.js`) — add `\resizebox` or `tabularx` wrapper when `colsN > 4`.
6. **[medium] Arrange/sort in data viewer** (`ExplorerModule.jsx`) — high user ergonomics value; DuckDB path already has `ORDER BY` infrastructure.
7. **[medium] Save draft state in FeatureTab** (`FeatureTab.jsx`) — prevents user frustration when navigating between Clean and Feature sub-tabs during construction.
8. **[medium] Chi-square display in CoefficientTestPanel** — verify single-coefficient path calls `waldTest` and surfaces `chiSq`; likely a one-line display addition.
9. **[low] Guide tour update** (`HelpSystem.jsx`) — copy-only change; should be bundled into the next release to avoid confusing new users.
10. **[low] LaTeX variable label editing** (`latexTable.js` + `resultDisplay.jsx`) — `varLabels` API already exists; UI is the missing piece.
