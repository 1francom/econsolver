# Feedback Analysis Report — 2026-07-26

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **15 open bugs** · **12 open features** · **4 open UX** · **1 open performance** · **1 open other**
- 4 additional items are **code-complete but browser-validation pending** (marked below — Franco must confirm before they can be closed).
- 2 items in BugTriage are marked "likely fixed" (N-way FE / FE interactions shipped in `#104`; need browser confirmation by Franco).

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js` (case `"CallawayCS"` ~line 1432), `src/math/CallawayEngine.js`
**Suggestion:** The `case "CallawayCS"` block in `rScript.js` constructs the `att_gt()` call but extracts spec fields directly from `model` instead of `model.spec`. Verify that the `result` object passed to `buildModelBlock` for a CallawayCS estimation has `yVar`, `treatCol`, `entityCol`, `timeCol`, etc. at the top level rather than nested under `spec`; add a spec-unwrapping guard matching how `SunAbraham` and `TWFE` are handled. Also confirm `CallawayEngine.js` produces a result with a `type: "CallawayCS"` discriminant so the dispatch in the report generators reaches this branch at all.
**Invariants:** Fix touches only the export layer — math stays in `CallawayEngine.js`. No React imports in math files.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/services/export/loadLine.js`, `src/components/PlotBuilder.jsx`
**Suggestion:** `PlotBuilder.jsx`'s "Copy R" / "Copy Python" export path does not currently call `buildRLoadLine` / `buildPyLoadLine` from `loadLine.js`. Thread the dataset's `loadOpts` (already carried in `sessionState` as `DatasetMeta.loadOpts`) into `PlotBuilder` via props, then prepend the load line to the exported snippet using the same helpers already used by `rScript.js` and `stataScript.js`. The Explorer's `renderPinnedPlot` copy-code path needs the same treatment.
**Invariants:** None violated. Do not add a raw `fetch` — `loadLine.js` is a code-string helper only.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (case `"group_summarize"`, line 528), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` collapses rows but only exposes within-group aggregation (mean, sum, sd, etc.) — there is no cross-group arithmetic. The cleanest approach: add a `group_diff` pipeline step that takes the collapsed output of a prior `group_summarize`, specifies two group key values and a metric column, and emits the difference as a new scalar column. Alternatively, document and expose a two-step workflow: `group_summarize` → `pivot_wider` by the treatment column → `add_column` computing the difference. Either approach keeps the non-destructive replay invariant intact.
**Invariants:** Any new step must be added to both `runner.js` AND `registry.js` and stay in sync (enforced by `pipelineReliabilityValidation.mjs`). No React imports in `runner.js`.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** The 2026-07-17 HC2 replication pass identified that `xtreg`/`areg` accept only one cluster variable, so two-way cluster now routes to `reghdfe`; further regressions likely remain (e.g. `vce(hc2)` on `ivregress` which does not support it, or `reghdfe` calls missing the `absorb()` clause). Audit every estimator branch in `stataScript.js` against Stata syntax rules: verify every `reghdfe` call has `absorb()`, every `ivregress` uses only `vce(robust)`/`vce(cluster)`, and `xtpoisson`/`xtnbreg` panel paths are wired. Add `# NOTE: requires ssc install hdfe` wherever `reghdfe` appears.
**Invariants:** None violated.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** In `latexTable.js`, detect when the number of model columns exceeds a threshold (e.g., > 5) and wrap the table with `\resizebox{\textwidth}{!}{}` or switch to the `tabularx` environment so it scales to page width. Also reduce the font size to `\footnotesize` or `\tiny` for > 8 columns as a fallback. Expose the scaling strategy as a config option threaded through `ReportingModule`.
**Invariants:** None violated.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` currently runs a single hypothesis at a time. Add a "+" button to accumulate multiple hypothesis objects (each `{ label, r, q }` matching `testLinear`'s contract) into a local array, display them as a removable list, and run all with a single "Test All" action. For R replication, emit one `car::linearHypothesis(fit, c("H1", "H2", ...))` vector call per test group in `ModelHypothesis.js`'s snippet generator.
**Invariants:** `ModelHypothesis.js` is in `src/math/` — keep it pure JS with zero React imports.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js`
**Suggestion:** `testLinear` currently returns only an F-statistic. Add a `chi2` field: `chi2 = F * q` where `q` is the number of restrictions (rank of `R`), and a corresponding p-value using a chi-squared CDF with `q` degrees of freedom. Expose both F and χ² in the `CoefficientTestPanel` result display, and add the matching `pchisq(r * Fstat, r)` line to the R replication snippet.
**Invariants:** `ModelHypothesis.js` is in `src/math/` — no React imports.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add an ELASTICITY RULE section to `INTERPRET_REGRESSION_PROMPT`. Detect four specification patterns from `result.spec.xVars` and `result.spec.yVar`: (1) log-log → "a 1% increase in X is associated with a β% change in Y (elasticity = β)"; (2) log-level → "a 1-unit increase in X changes Y by approximately 100·β%"; (3) level-log → "a 1% increase in X changes Y by β/100 units"; (4) level-level with interaction → remind the user to compute the marginal effect at a specific value. These column names (with `log_` prefixes from `FeatureTab`) are already in the user payload via `serializeSnapshot`.
**Invariants:** All Anthropic calls go through `AIService.js` only — no raw fetch in the Prompts file.

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend the `artifactOrder` schema (currently a flat ordered array of artifact IDs) to support named groups: `{ groups: [{ id, name, artifactIds[] }], ungrouped: artifactId[] }`. Add a "New Group" button in `ModelBufferBar.jsx` that prompts for a name and lets users drag pinned models into it; collapsible group sections replace the flat buffer strip. Persist the group structure via `saveArtifactOrder` (already IDB-backed).
**Invariants:** Persistence must use `indexedDB.js` — not `localStorage`.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` resets its local state on every tab switch because the draft lives in component-local `useState`. Lift the in-progress transform state (partial `case_when` branches, the current `if_else` expression, etc.) into `WranglingModule`'s shared state or a dedicated `pendingFeature` key so it survives tab transitions within the same session. Session-scoped React state is sufficient — no IndexedDB persistence needed for drafts.
**Invariants:** None violated.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Add sortable column headers to the data viewer table in `ExplorerModule.jsx` — clicking a header cycles through ascending / descending / unsorted for display purposes only (no pipeline step emitted, no mutation of `rawData`). Add a small tooltip "display sort only — not saved to pipeline" to avoid confusion. If the user wants a persistent sort, direct them to the `arrange` step in the Clean tab.
**Invariants:** Display sort must not mutate `rawData` or emit a pipeline step — it is view-only. No IndexedDB writes.

---

### [low] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** The time series path in `ExplorerModule.jsx` passes through a stripped-down config that omits the labels panel already present for other geoms in `PlotBuilder`. Wire the existing labels panel (title, x-axis label, y-axis label) into the time series / ACF / PACF configuration object so the same controls appear consistently.
**Invariants:** None violated.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` registry)
**Suggestion:** `TOUR_STEPS` covers 9 steps from the original MVP. New tabs and panels added since (Spatial, Simulate, N-way FE, BaconPanel, Callaway DiD) are not covered. Review each step against current `WorkspaceBar.jsx` tab names and DOM target elements; add steps for Spatial, Simulate, and Report; remove or update any step referencing a renamed element. Low risk, pure UI update.
**Invariants:** None violated.

---

### [medium] · UI — Browser validation pending · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete as of `35f4b5a6` — native vertical resize with bounded canvas sizing wired. **Action required: Franco must browser-validate** resize interaction and PNG export dimensions before this can be closed. If validation reveals regressions, the resize handle likely needs a `ResizeObserver` cleanup on unmount to prevent stale handler references.
**Invariants:** None violated.

---

### [high] · UI — Browser validation pending · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete as of `35f4b5a6` — plots are wired and the stale-result regression is fixed (diagnostics now read from `result.spec`). **Action required: Franco must browser-validate** that all three plots render correctly after pinning, sidebar edits, and model restore. If Y~Yhat is still missing, check that the OLS `result` carries a `fitted` array — the `_duckdb` large-n path may not pre-compute fitted values.
**Invariants:** None violated.

---

### [medium] · UI — Browser validation pending · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete as of `5e105529` — Leaflet heatmap layer restored with KDE cells, palette controls, opacity, legend, HTML export, and a deterministic 5k-point cap. **Action required: Franco must browser-validate** at various zoom levels and confirm the HTML export button produces a standalone file. If the 5k-point cap causes under-rendering, expose it as a configurable slider in `SpatialLayerEditor.jsx`.
**Invariants:** None violated.

---

### [high] · UI — Browser validation pending · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Code-complete as of `35f4b5a6` — OLS/WLS display is now frozen to `result.spec`. **Action required: Franco must browser-validate** that adding/removing a variable after estimation does NOT change the displayed coefficient plot or diagnostics. If it still mutates, look for any `useMemo` or `useEffect` in `ModelPlots.jsx` that reads `xVars` from live sidebar state instead of from `result.spec.xVars`.
**Invariants:** None violated.

---

## Features

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Add a "✎ Edit" button to each step card in `History.jsx`. When clicked, call a callback from `WranglingModule` that switches the active tab to the tab owning that step type and pre-populates the subsection form with the step's current parameters. A `STEP_TAB_MAP` lookup (`stepType → tabName`) in `WranglingModule.jsx` is the cleanest anchor point.
**Invariants:** None violated. History.jsx and WranglingModule are UI components — no math imports.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `country_code` panel to `FeatureTab.jsx` in the Strings/Formatting section. Embed a minimal static lookup table (~250 rows: name, iso2, iso3, numeric) as a JS object — no network call. The step takes an input column and a target format and emits a new column. Wire it to a new `country_code` case in `runner.js` and a matching entry in `registry.js`.
**Invariants:** New step must be registered in both `runner.js` and `registry.js`. No React imports in `runner.js`.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`
**Suggestion:** Add a `lists` map to `SessionStateProvider` state (key = list name, value = string[]). Lists are session-scoped by default. Expose a lightweight "Lists" panel in `WranglingModule` and add a `column IN list` operator to the `CleanTab.jsx` filter builder. IDB persistence and the cross-module integration points need a design session before implementation — this is a design recommendation, not an implementation spec.
**Invariants:** If persistence is added, it must go through `indexedDB.js`.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" section to `CalculateTab.jsx`. Use the existing CAS layer to compute `lim_{x→a} f(x)` symbolically if the library supports it; otherwise implement numerical limits via left/right approach sequences for teaching value. Display the result with a one-sided vs. two-sided explanation.
**Invariants:** None violated. Pure UI + math — no pipeline steps needed.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When a user's expression evaluates to a number (no variable dependency), render it as a dashed horizontal line rather than an error. Detect this in the parser: if the parsed expression has no free variable, branch to `hline` rendering. Two-line change in the expression evaluator.
**Invariants:** None violated.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text input filter above the variable chip list in `VariableSelector.jsx`. On each keystroke, filter displayed chips to those whose names contain the search string (case-insensitive). No backend changes — the full variable list is already in component state. Clear the filter when a variable is selected.
**Invariants:** None violated.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, add an optional "Show value labels" toggle in the Labels panel that renders Observable Plot's `text` mark near each point/bar showing the color-group name (or a user-specified column). Limit to the top N visible categories to avoid overplotting.
**Invariants:** None violated.

---

## UX / Design

### UX · 2026-05-19
**Feedback:** Add filter(`regionname %in% c("Spain", "Basque Country")`) for filter option
**File:** `src/components/wrangling/CleanTab.jsx`
**Suggestion:** Add a pre-processing step in the filter expression parser that rewrites `column %in% c(v1, v2)` to `["v1","v2"].includes(column)` before JS evaluation. Alternatively, expose a dedicated "Value is one of" operator in the filter builder UI dropdown (safer than raw expression rewriting and more discoverable).
**Invariants:** None violated.

---

### UX · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)`
**File:** `src/components/modeling/ModelPlots.jsx`, `src/math/CausalEngine.js`
**Suggestion:** Add a collapsible `RDDSummaryTable` section below the RDD result card in `ModelPlots.jsx` that formats `result` fields into an `rdrobust`-style table: rows for bandwidth, kernel, polynomial order, N left/right of cutoff, conventional/robust LATE, SE, and CI. All values already exist in `result` — no additional computation needed in `CausalEngine.js`.
**Invariants:** Model output presentation is UI-only — no changes to `CausalEngine.js` math required.

---

### UX · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer row-filter control in `GeoLayerConfig.jsx`: a column selector and a value/range input that filters the layer's data before rendering. Implement as a client-side `.filter()` on the layer's `rows` array at render time — do not emit a pipeline step or mutate data.
**Invariants:** Display-only filter — must not mutate `rawData`. No pipeline step emitted.

---

### UX · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c" regardless of actual column names
**File:** `src/ReportingModule.jsx`, `src/services/export/latexTable.js`
**Suggestion:** In `latexTable.js`, expose a `labelOverrides` parameter (`Map<varName, displayLabel>`). In `ReportingModule.jsx`, render an editable label next to each row in the LaTeX preview — a small text input that updates `labelOverrides` and re-renders the table. For RDD, pre-populate overrides from `result.spec.runningVar` and `result.spec.treatCol` instead of the hardcoded generic strings.
**Invariants:** None violated.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`
**Suggestion:** Fase X4 is code-complete per the X5 reconciliation table — large datasets are routed through DuckDB with a 500-row UI preview and OPFS Parquet caching. **Action required: Franco must run the browser benchmark** (`plans/2026-05-22-fase-x4-performance.md`) to confirm the crash no longer occurs. If tab transitions still lag, profile via Chrome DevTools — the likely cause is a `map()`/`filter()` over full `rawData` in a React render path outside the DuckDB fast path.
**Invariants:** Any computation on large data must stay in DuckDB — never materialize the full Arrow result into JS objects. Display limit is 200–500 rows; computation limit is full dataset.

---

## Other

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...
**File:** `src/DataStudio.jsx` (CSV parse entry point), `src/services/data/parsers/`
**Suggestion:** This is the same class of failure as BugTriage 2026-06-14 "malformed CSV row aborts import" (status: fix-later). When DuckDB's CSV loader throws `Invalid Input Error` on a specific line, catch it at the `DataStudio.jsx` boundary and surface a recovery dialog: show the offending line number and raw content, and offer "Skip this row and continue" or "Abort". For DuckDB-backed loads, use DuckDB's `ignore_errors=true` scan option to collect parse errors as metadata rather than aborting.
**Invariants:** Must not bypass `DataStudio.jsx` as the single parse entry point.

---

## Suggested Action Order

1. **[high] CS DiD replication code broken** — users who estimated a Callaway-Sant'Anna model cannot replicate results in R; confirm spec field extraction in `rScript.js` case `"CallawayCS"` and that `CallawayEngine.js` produces a `type: "CallawayCS"` discriminant.
2. **[high] Stata workbench incomplete** — audit all estimator branches in `stataScript.js` against Stata syntax rules; the 2026-07-17 HC2 pass likely left coverage gaps in panel/IV paths.
3. **[high] Plot replication code omits load lines** — add `buildRLoadLine`/`buildPyLoadLine` calls to `PlotBuilder`'s copy-code path; affects every user who copies a plot replication script.
4. **[high] Browser-validate Y~Y_hat/Residual/Q-Q plots** — code-complete since June 2026; Franco needs to confirm `35f4b5a6` renders all three diagnostic plots correctly including the DuckDB large-n path.
5. **[high] Browser-validate diagnostics freeze** — same commit; confirm sidebar edits after estimation no longer mutate the coefficient plot or diagnostics panel.
6. **[medium] group_summarize cross-group math** — add a `group_diff` step or document the two-step `group_summarize → mutate` workaround; blocks common ATE calculations for PhD students.
7. **[medium] Chi-square for linear hypothesis** — straightforward math extension: `χ² = q × F`; add to `ModelHypothesis.js` and the result display.
8. **[medium] Unfinished work lost in FeatureTab** — lift in-progress `case_when`/`if_else` draft state to `WranglingModule`; high user friction, low implementation risk.
9. **[medium] LaTeX table sizing for many columns** — add `resizebox`/`tabularx` wrapping in `latexTable.js`; affects every multi-model comparison table export.
10. **[medium] Elasticity interpretation in AI narrative** — add ELASTICITY RULE to `INTERPRET_REGRESSION_PROMPT`; high teaching value for the LMU PhD target audience.
