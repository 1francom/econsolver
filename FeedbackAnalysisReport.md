# Feedback Analysis Report — 2026-09-05

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 7 open bugs · 16 open features · 1 open UX · 1 open performance · 1 open other

> **Note on "fixed but un-strikethrough" items:** Many entries in ClaudeFB.md are confirmed fixed in BugTriage.md (or the X5 reconciliation table) but were never struck through in ClaudeFB.md. Those are excluded from this report. Only items that are genuinely unresolved — BugTriage status `open`/`fix-later`, or no BugTriage entry with no ✓ in ClaudeFB.md — are included below.

---

## Bugs

### [high] · Explore / Time Series · 2026-07-28
**Feedback:** "References of the group in time series doesn't work"  
**File:** `src/ExplorerModule.jsx` (lines 1293–1490, `TimeSeriesTab`) and `src/ExplorerModule.jsx:178`, `SvgMiniTimeSeries`  
**Suggestion:** `SvgMiniTimeSeries` renders all groups in the `series` array using `s.grp` as the SVG key, but never draws a legend matching group name to colour. When a `groupCol` is set in `TimeSeriesTab`, each aggregated series has a `.grp` label but it is invisible in the pin-Compare preview. Add a compact text legend strip below the SVG (group label beside a colour swatch), mirroring the `COLORS` array at `ExplorerModule.jsx:180`. The full `TimeSeriesTab` view should check whether the SQL aggregate path at `ExplorerModule.jsx:148` preserves the `grp` field through the DuckDB result; if not, `fetchAggregateTimeSeriesSQL` needs a `GROUP BY ${grpCol}, ${tCol}` clause and an explicit `AS grp` alias.  
**Invariants:** No React in math files. All styles via the `C` object from `shared.jsx` — no CSS files.

---

### [high] · Simulate · 2026-07-28
**Feedback:** "Update the replication script, currently it replicates only the DGP"  
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (script generators)  
**Suggestion:** `SimulateTab` exposes a DGP draw plus downstream panels (StatWorkspace, SampleTestPanel, Monte Carlo). The replication script currently calls only the DGP draw functions from `src/math/dgpScript.js`. Extend `buildSessionSnapshot` (or a new `buildSimulateSnapshot`) to capture the active StatWorkspace computations and SampleTest configuration, then extend the unified-script generators to emit post-DGP code — e.g. the R equivalent of a `t.test`, `var.test`, or summary stat over the simulated vector. A minimal MVP is to append the statistical-test code block (already generated per-item in `SampleTestPanel`) after the DGP draw in the copied/downloaded script.  
**Invariants:** All Anthropic calls through `AIService.js`. Snapshot data goes through `sessionSnapshot.js` seam.

---

### [high] · Calculate / Stata Workbench · 2026-07-17
**Feedback:** "Stata workbench doesn't work completely"  
**File:** `src/components/calculate/workbench/exportScript.js` (lines 79–116, `buildStata`)  
**Suggestion:** The exported Stata `.do` file wraps expressions in `mata:`/`end` blocks and uses `twoway function` for plots. Known gap: the Mata output calls are bare `("label", value)` print statements that are valid Mata syntax but produce no visible output in the Stata Results window (the correct form is `printf("label %g\n", value)` or `display`). Constraints are silently dropped (only a comment is emitted, line 120). Fix: replace bare tuple prints with `printf` in `buildStata`; surface unsupported constraint equations as commented-out `nlcom`/`testnl` stubs with a TODO note rather than a blanket "not translated" footer. Update the `HintBox` copy in `CalculateTab.jsx` to note that Stata has no symbolic engine (only numeric Mata), so symbolic differentiation results are absent.  
**Invariants:** No external UI libs. No CSS files.

---

### [medium] · Data · 2026-05-10
**Feedback:** "Parse error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47..."  
**File:** `src/DataStudio.jsx` (`parseFile` CSV path), `src/services/data/parsers/` (CSV via Papa/DuckDB)  
**Suggestion:** The error suggests a row where a numeric field exceeds the column's inferred type or a mismatched quote/delimiter. The current CSV path aborts on the first bad row. Add a row-level `try/catch` (or use PapaParse's `skipEmptyLines: true` + `error` callback to collect bad rows) and surface a non-blocking warning: "Skipped N rows — see details." A "download problematic rows as CSV" option would let the user inspect and fix before re-importing. This is the same "resilient-import UX" item in BugTriage 2026-06-14.  
**Invariants:** Persistence via IndexedDB, not localStorage.

---

### [medium] · Model / FE · 2026-07-09 *(needs browser confirmation)*
**Feedback:** "Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable"  
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`  
**Suggestion:** BugTriage marks this as "likely fixed — N-way FE shipped in #104 (`045f5cda`)". If the UI still shows only Entity/Time pickers and does not expose an "Additional FE columns" multi-select, the math is done but the wiring is incomplete. `ModelConfiguration.jsx` should expose the extra-FE column picker (already exists in the codebase per CLAUDE.md) to all users, not just those who know to look for it. Franco should browser-verify the column picker is reachable from the standard Model tab path.  
**Invariants:** SE type must be passed explicitly — `runFEMulti` already accepts `seOpts`.

---

### [medium] · Model / FE Interactions · 2026-07-09 *(needs browser confirmation)*
**Feedback:** "Add also FE with interactions"  
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions` step)  
**Suggestion:** BugTriage marks this "likely fixed — FE interactions shipped in #104". Verify that `factor_interactions` pipeline steps are correctly passed through the `ModelConfiguration` → `estimate()` path for panel estimators, and that the expanded interaction columns appear in the FE design matrix. If the pipeline step works but the FE engine doesn't consume it, wire the interaction-expanded headers from `runner.js` into `runFEMulti`'s `xCols` before demeaning.  
**Invariants:** Zero React in `src/math/`. SE type passed explicitly.

---

### [low] · Clean / Filter · 2026-06-14
**Feedback:** "R-style `%in% c(...)` membership syntax is not accepted by the filter expression UI"  
**File:** `src/components/wrangling/CleanTab.jsx` (`FilterBuilder`), `src/pipeline/predicate.js` (canonical condition language)  
**Suggestion:** `predicate.js` is the single owner of all filter operators (`OPERATORS`, `normalizeOp`, `evalPredicate`, `predicateToSQL`). The `in` operator already exists there. The gap is the UI `FilterBuilder` which provides a value input but no free-text expression parser. Rather than adding R syntax parsing (a large scope change), surface the `in` operator's multi-value chip input (comma-separated list) more prominently in `CleanTab.jsx` with a label "Value is one of…" — that covers the `%in% c(...)` use case without any new predicate logic.  
**Invariants:** `predicate.js` is the single owner of condition logic — never re-implement a filter operator elsewhere.

---

## Features

### Model · 2026-07-06
**Feedback:** "Add vector for running more than 1 hypothesis and save them to replicate in R"  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `CoefficientTestPanel` currently runs one hypothesis at a time. Add a "saved hypotheses" list (array in component state, persisted to IndexedDB via the model buffer or a new `hypothesis_buffer` store) that accumulates `{label, constraints, result}` entries. The existing `generateJointHypothesisScript()` (shipped 2026-07-30 per BugTriage) already emits a single `car::linearHypothesis` call — extend it to emit a `for`-loop or repeated calls for each saved hypothesis, matching the R pattern `lapply(hypotheses, function(h) linearHypothesis(fit, h))`. Display saved results as a collapsible table below the single-test panel.  
**Invariants:** Persistence via `services/Persistence/indexedDB.js`, not localStorage. `src/math/ModelHypothesis.js` must remain React-free.

---

### Model / AI Interpretation · 2026-07-09
**Feedback:** "Add direct elasticity interpretation"  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`), `src/services/AI/AIService.js`  
**Suggestion:** Add a new rule to `INTERPRET_REGRESSION_PROMPT`: when a log-transformed Y or X is detected (column name starts with `log_` or the spec contains a `log` pipeline step for that variable), report the elasticity interpretation — log-log: "a 1% increase in X is associated with a β% change in Y"; log-level: β × 100 as a semi-elasticity; level-log: β/100 per 1% X increase. The spec already carries `pipeline` steps and column names — extract the log-transform footprint from `spec.pipeline` inside the `callClaude` user block and inject the detected log spec into the user prompt before the AI call, so the model doesn't have to infer it.  
**Invariants:** All Anthropic calls through `AIService.js`. Prompt caching — keep `SHARED_CONTEXT` as the cached block; the new rule goes in `INTERPRET_REGRESSION_PROMPT`, not in `SHARED_CONTEXT`.

---

### Export · 2026-07-05
**Feedback:** "Adjust the size of LaTeX tables when the number of columns increases"  
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`  
**Suggestion:** `buildStargazer` (in `latexTable.js`, imported at `ReportingModule.jsx:32`) generates a `tabular` environment. When the number of pinned models exceeds ~4, the table is wider than `\textwidth`. Wrap the `tabular` in `\resizebox{\textwidth}{!}{...}` when the column count exceeds a threshold (e.g. 4 models), or emit `\adjustbox{max width=\textwidth}{}` (requires `adjustbox` package). Add the package declaration to the emitted preamble when the wrap is active. Alternatively, emit `\tiny` or `\footnotesize` size modifiers as a simpler fallback. Add a comment in the emitted LaTeX noting the package requirement.  
**Invariants:** None specific — pure export formatting.

---

### Calculate · 2026-07-04
**Feedback:** "Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)"  
**File:** `src/components/calculate/workbench/Workbench.jsx`, `src/components/calculate/workbench/WorkbenchCanvas.jsx`, `src/components/calculate/workbench/exportScript.js`  
**Suggestion:** Allow an equation card to have a constant expression (no variable axis). When the axis selector is empty and the expression evaluates to a finite scalar, draw a horizontal `y = k` line across the full x-range in `WorkbenchCanvas`. In `exportScript.js`, emit `abline(h = k)` (R), `axhline(k)` (Python), and a `twoway (function y = k, range(...))` (Stata) for constant cards. The `operations.js` `runCard` call already returns a numeric result for scalar expressions — it just needs a rendering branch in the canvas and a constant-detection check (`isFinite(result)` with no sweep).  
**Invariants:** Inline styles via `C` object. No external UI libs.

---

### Model · 2026-06-14 *(fix-later)*
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"  
**File:** BugTriage points to `src/components/modeling/resultDisplay.jsx` (does not exist by that name); actual location is `src/ReportingModule.jsx` (`buildStargazer` → `latexTable.js`) and `src/services/export/latexTable.js`  
**Suggestion:** Add a `varLabels` map (column name → display label) to the model's export metadata, editable via a small "Edit labels" popover in `CodeEditor.jsx` or `ReportingModule.jsx`. `buildStargazer` should accept an optional `varLabels` argument and substitute labels for raw column names in the coefficient rows. Persist `varLabels` in the model spec alongside `seType`/`clusterVar` — they need the same three-whitelist-edits treatment (`CodeEditor.jsx`, `ReportingModule.jsx`, and each transpiler call site).  
**Invariants:** Spec → export needs THREE whitelist edits (per CLAUDE.md invariant already documented).

---

### Model · 2026-05-21 *(fix-later)*
**Feedback:** "Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression"  
**File:** `src/math/CausalEngine.js` (`runSharpRDD`), `src/components/modeling/ModelPlots.jsx` or a new `RDDSummary` component  
**Suggestion:** `EstimationResult` already carries `bandwidth`, `kernel`, `polyOrder`, `nLeft`, `nRight`, `eff.n`, `tau` (LATE), `se`, `pval`, and `ci`. Render a compact table in the model result panel that mirrors `rdrobust`'s printed summary: "Cutoff c=0 | Left | Right", then rows for number of observations, eff. obs., order polynomial, kernel, bandwidth, and the sharp-RD point estimate + CI. This is a pure display addition — no new math.  
**Invariants:** None specific. Component lives in `src/components/modeling/`.

---

### Spatial · 2026-05-30 *(fix-later)*
**Feedback:** "Allow spatial filters for plots"  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`  
**Suggestion:** Add a bounding-box or attribute filter control to `SpatialGeoPlot.jsx` — a row with column picker + operator + value (reusing `FilterBuilder` from `CleanTab.jsx`), applied to `rows` before `GeoPlotCanvas` renders. For the Leaflet map tab, a drawn-rectangle region filter (using Leaflet Draw or a manual lat/lon bounds form) would filter `rows` to points within the bounds. The spatial plot currently receives all rows; the filter should be a view-level memo transformation, not a pipeline step.  
**Invariants:** Spatial steps in the pipeline use `sp_*` step types and go through `runner.js`. A view-level filter does NOT create a pipeline step.

---

### Model / Variable Selector · 2026-07-09
**Feedback:** "Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster"  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a debounced text `<input>` at the top of the variable list in `VariableSelector` that filters the displayed headers by substring match (case-insensitive). The filter is view-only and does not affect which variables are selected. Use `C.bg` / `C.border` inline styles, `mono` font. The existing chip list already renders from `headers.filter(...)` — wrap that filter to additionally apply the search string.  
**Invariants:** Inline styles via `C`. No external UI libs.

---

### Explore / Plot Builder · 2026-07-11
**Feedback:** "Add more labels in tables or boxes when plotting by color"  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set and the colour scheme is categorical (`observable10`), the existing legend renders colour swatches. The feedback asks for count or percentage labels next to each group. Extend the legend rendering (in PlotBuilder's SVG legend section or Observable Plot's `Plot.legend`) to append `(n=X, Y%)` computed from `rows.filter(r => r[aes.color] === g).length`. This is a pure display addition to the existing legend component.  
**Invariants:** No external UI libs. CDN-loaded Observable Plot 0.6 only.

---

### Clean / Pipeline Navigation · 2026-07-17
**Feedback:** "Add an option to click on one pipeline step and go to the corresponding subsection automatically"  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** Each step in `History.jsx` already knows its `step.type`. Map step types to their tab and sub-panel: e.g. `rename/drop/filter/set_where` → Clean tab → FilterBuilder section; `log/sq/std/lag/lead` → Features tab → Transform section; `group_summarize` → Reshape tab. On step click (in addition to the existing select behaviour), emit an `onNavigate(tabId, sectionId)` callback that `WranglingModule` handles by setting `activeTab` and scrolling the target panel into view (`element.scrollIntoView({ behavior: 'smooth' })`). The step-type-to-tab map can be a simple `const STEP_TAB_MAP = {...}` object in `History.jsx`.  
**Invariants:** Pipeline steps must remain non-destructive and always replay on `rawData`.

---

### Clean / Transformation State · 2026-07-17
**Feedback:** "Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing"  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** Use `sessionStorage` (keyed by `pid + tab + section`) to persist in-progress form state for `case_when`, `if_else`, `mutate`, and `grouped_mutate` panels. On mount, restore from storage; clear on successful "Add to pipeline". This is a view-level convenience that does NOT interact with the pipeline or IndexedDB — `sessionStorage` is the right tier (per-tab, per-session, clears automatically). The same pattern should apply to `CleanTab.jsx`'s `FilterBuilder` in-progress state.  
**Invariants:** Use `sessionStorage` (not `localStorage`) for per-session UI convenience state. IndexedDB is for pipeline/data persistence only.

---

### General / Artifact Groups · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria"  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** Add a `group` field (string label) to each artifact's metadata in `artifactOrder.js`. In `ModelBufferBar.jsx`, render a "New Group" button that prompts for a label; a drag-or-assign UI then moves pinned model chips into group lanes. Groups are collapsed/expanded independently. For an MVP: a simple dropdown on each pinned model chip ("Move to group: [None, A, B, C + New…]") with group sections rendered as sub-rows in the buffer bar. Persist groups in `model_buffer` IndexedDB store alongside `order`.  
**Invariants:** Persistence via `services/Persistence/indexedDB.js`.

---

### Clean / Country Codes · 2026-07-19
**Feedback:** "Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform"  
**File:** `src/components/wrangling/FeatureTab.jsx` (new transform entry), `src/pipeline/runner.js` (new step or reuse `recode`)  
**Suggestion:** Ship a `src/math/countryLookup.js` with a static 249-row ISO 3166-1 table (iso2, iso3, name — about 20KB minified). Add a "Country Code" transform in `FeatureTab.jsx` that: picks an input column, a from-format (iso2/iso3/name), and a to-format; emits a `recode` pipeline step with the full crosswalk map. `runner.js` already handles `recode` — no new step type is needed. For fuzzy name matching, call the existing `fuzzyGroups` in `wrangling/utils.js` with a high similarity threshold (≥ 0.9) as a pre-normalisation pass before the exact lookup.  
**Invariants:** `src/math/` is pure JS — no React imports. The `recode` step type already exists and must replay on `rawData`.

---

## UX / Design

### General · 2026-07-17
**Feedback:** "Guide tour must be updated"  
**File:** `src/components/HelpSystem.jsx` (line 11, `TOUR_STEPS` array)  
**Suggestion:** Audit `TOUR_STEPS` against the current tab list in `WorkspaceBar.jsx` — the workspace now has 7 tabs (Data/Clean/Explore/Model/Simulate/Calculate/Report) and several new sub-features (Spatial, PlotBuilder, Callaway-Sant'Anna, N-way FE, Synthetic Control). Add or update entries that reference modules added since the tour was last edited. Per CLAUDE.md convention: `HintBox` trigger always reads "Help", title is the module name (not "How to X"), and never write counts ("14 estimators") — name the things. Also remove any reference to WLS as a sidebar estimator (it's an OLS toggle since the strategy × family split).  
**Invariants:** `TOUR_STEPS` in `HelpSystem.jsx` — no external UI libs. All styles via `C`.

---

## Performance

### Data · 2026-05-08
**Feedback:** "Big datasets generate crashes (21MB) over 900k rows"  
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`  
**Suggestion:** BugTriage notes this is "code-complete Fase X4; browser benchmark pending Franco." The DuckDB-Wasm path, OPFS Parquet cache, and 500-row UI preview were all implemented. The open action is Franco's browser benchmark: load the 21MB/900k-row fixture in the deployed Vercel app, verify the ⚡ DuckDB badge appears, confirm tab-transition times are under 3 seconds, and mark the BugTriage row fixed. If the benchmark reveals a remaining bottleneck, the most likely culprits are (a) the OPFS restore failing silently (see CLAUDE.md `opfsCacheKey` bug), or (b) `getTablePage` issuing a `SELECT *` instead of column-subset queries. Check whether `extensions.duckdb.org` is in `vercel.json`'s `connect-src` (added 2026-07-25 per CLAUDE.md — confirm it is present).  
**Invariants:** Data never leaves the browser (privacy-first). No server-side computation.

---

## Other

### Data · 2026-05-10
**Feedback:** "Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47..."  
**File:** `src/DataStudio.jsx` (CSV parse path), `src/services/data/parsers/` (PapaParse / DuckDB CSV ingestion)  
**Suggestion:** This CSV contains a truncated/malformed row (the description cuts off). The error was never added to BugTriage. Before implementing resilient import, verify whether DuckDB's native CSV reader (`READ_CSV_AUTO`) would handle this file — DuckDB is far more tolerant of trailing truncation than PapaParse. If the file is small enough for the JS path, try `PapaParse` with `skipEmptyLines: true` and the `error` callback to collect bad rows without aborting. Add this file to a test fixture collection if Franco still has it.  
**Invariants:** None specific.

---

## Suggested action order

1. **Fix time series group reference display** (Explore, 2026-07-28) — users see no legend for grouped series; straightforward SVG addition to `SvgMiniTimeSeries` and `TimeSeriesTab` in `ExplorerModule.jsx`.
2. **Fix Stata workbench `printf` output** (Calculate, 2026-07-17) — bare Mata tuple prints produce no visible output; a one-line change in `exportScript.js:buildStata` to replace `(label, value)` with `printf("…", value)` unblocks users who rely on the Stata export.
3. **Extend Simulate replication script** (Simulate, 2026-07-28) — the script stops at the DGP draw; add post-DGP test code blocks from `SampleTestPanel` to make the script actually reproducible.
4. **Multi-hypothesis save and R export** (Model, 2026-07-06) — the underlying `generateJointHypothesisScript()` already works; adding a saved-list UI and a looped export is a contained addition to `CoefficientTestPanel.jsx`.
5. **LaTeX table width scaling** (Export, 2026-07-05) — a one-line `\resizebox` wrap in `latexTable.js` fixes wide tables for multi-model comparisons.
6. **Variable searcher in model selector** (Model, 2026-07-09) — high-frequency UX pain for large datasets; a debounced filter input in `VariableSelector.jsx` is a small, isolated change.
7. **Confirm N-way FE and FE interactions with browser validation** (Model, 2026-07-09) — math is done per BugTriage; Franco should verify the UI column picker is reachable and mark these closed or raise a follow-up UI ticket.
8. **Guide tour update** (UX, 2026-07-17) — low risk, high visibility; audit `TOUR_STEPS` in `HelpSystem.jsx` against current module list and remove stale references.
