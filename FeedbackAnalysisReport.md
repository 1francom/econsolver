# Feedback Analysis Report — 2026-09-14

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 8 open bugs · 10 open features · 3 open UX/Design · 1 open performance · 1 open other

> **Cross-reference note:** Items marked ~~strikethrough~~ or `✓` in ClaudeFB.md, or `fixed`/`wontfix` in BugTriage.md, are omitted. Items marked "code-complete / browser validation pending" are also omitted (the fix is merged). "Likely fixed — needs confirmation" items are flagged at the bottom of each section. No new feedback batches have arrived since 2026-07-31; item counts are unchanged from 2026-09-13.

---

## Bugs

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx` (no dedicated simulate exporter exists — `src/services/export/` has no `simulateScript.js`)
**Suggestion:** Add a `simulateScript.js` exporter that serializes the full Simulate session in order: (1) DGP draw params via the existing `distExprR/Py/Stata` in `src/math/dgpScript.js`, (2) any Monte Carlo loop (sample size, n_reps, statistic type), (3) any downstream `SampleTestPanel` tests or estimation steps run on the simulated frame. Wire a "Copy replication code" button in `SimulateTab.jsx` that calls this exporter for all three languages, following the `generateCleanScript` preamble pattern already used by PlotBuilder. The DGP draw block already exports correctly (lines 13–14 import `distExprR/Py/Stata`); the gap is stitching it to the Monte Carlo loop state (`mcVar`, `mcStat`, `mcY`, `mcX`, `mcReps`) and downstream analysis steps.
**Invariants:** All script generation must go through per-language files in `src/services/export/`. No React in `src/math/`. The `dgpDraw.js` / `dgpScript.js` split (pure math vs. expression strings) must be preserved.

---

### [high] · Calculate/Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx` (Stata tab), `src/services/export/stataScript.js`
**Suggestion:** `stataScript.js` has at least three explicitly-UNVERIFIED paths: (1) many-to-many `joinby` merge (~line 519, commented "UNVERIFIED against a real Stata run"), (2) `ibn.` factor coding for through-the-origin no-intercept models (~lines 704, 881, labelled UNVERIFIED in CLAUDE.md), and (3) `ib#.` reference level for numeric factors (~line 741). Additionally, `xtreg y x, fe vce(cluster a b)` is a Stata syntax error (documented in the BugTriage 2026-07-17 row) — two-way cluster routing to `reghdfe` is already in the code, but this path also needs live verification. Surface a per-language "⚠ UNVERIFIED" warning chip in `CodeEditor.jsx` whenever the emitted snippet carries a `NOTE:` or `UNVERIFIED` marker (scan the emitted string before display). Separately, audit `joinby`, `ibn.`, and `ib#.` against a live Stata session and promote or replace.
**Invariants:** Inline styles via `C` only. No AI calls outside `AIService.js`.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` — `TimeSeriesTab` component (~line 1442); specifically the legend rendering below the line chart SVG and the `colorFor` / `seriesColors` map (~lines 1523–1524)
**Suggestion:** The `COLORS` array and `colorFor(grp, si)` function are defined, and `grpCol` state drives separate series. The likely fault is in how the legend or group labels are rendered in the SVG — look for the legend block below ~line 1630 and check that each group's `grp` key is resolved correctly when `grpCol` changes after the chart has already been drawn. A stale `seriesColors` map (keyed by old group names) would cause "references" to point to the wrong group after the selector changes. Suggestion: reset `seriesColors` to `{}` in a `useEffect` that watches `grpCol`, and add a visible legend strip below the SVG using the same `colorFor` mapping with one swatch + label per group.
**Invariants:** No external UI libraries. Inline styles only via `C`.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` — `TimeSeriesTab` (~line 1442); `src/services/export/exploreStatScript.js`
**Suggestion:** The SVG chart uses hardcoded axis ticks (`xTicks`/`yTicks`, ~lines 1547–1548) with no title or axis-label inputs. Add three text inputs to the controls row: "Chart title", "X axis label", "Y axis label". Render them as `<text>` elements inside the SVG (title centered at top, X-label centered below bottom axis, Y-label rotated at left margin). These should also be emitted in `exploreStatScript.js`'s R export as `labs(title=, x=, y=)`. Wire pin/compare params with `title`/`xLabel`/`yLabel` fields so saved pins reproduce the labels.
**Invariants:** SVG only (no external charting library for this component). Inline styles. No React in math files.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` emits a fixed-width `tabular` environment. For wide tables (more than ~5 models), the table overflows the page margin. Switch to `adjustbox{max width=\linewidth}` wrapping the `tabular`, or emit a `resizebox{\textwidth}{!}{...}` when column count exceeds a threshold (≥5). Alternatively, auto-rotate to `\begin{landscape}` via the `pdflscape` package when more than 7 models are present. Add a note in the `latexTable.js` header comment naming the threshold so it does not silently change.
**Invariants:** No external UI libraries. No AI calls outside `AIService.js`. LaTeX output only — no changes to the JS math layer.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line 1583 — Original Line: `China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (CSV parse branch), `src/services/data/parsers/` (no dedicated CSV parser — uses DuckDB or Papa Parse directly)
**Suggestion:** BugTriage marks this as "fix-later — post-launch resilient-import pass." The specific symptom is that a malformed row (e.g., a quoted field containing an embedded comma or newline) aborts the entire import rather than skipping the bad row. When the parse error is DuckDB-originated, wrap it in `describeLoadError()` and surface a recovery dialog: show the first bad line number, offer "Skip bad rows (import rest)" vs "Abort". For Papa Parse (small CSV), set `skipEmptyLines: true` and `error` callback to collect malformed rows and continue. BugTriage also notes: "retain original row number and offer skip/repair preview."
**Invariants:** Privacy-first — no rows leave the browser. Non-destructive: raw import errors must not corrupt an in-progress pipeline.

---

### [low] · General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` — `TOUR_STEPS` array
**Suggestion:** The existing `TOUR_STEPS` registry (~9 steps per CLAUDE.md) was written when the tab structure was simpler. Audit each step's `selector` and copy against the current 7-tab nav bar (Data/Clean/Explore/Model/Simulate/Calculate/Report) and update any selector that no longer matches a live element (stale selectors cause the tour overlay to detach or skip). At minimum: the Pipeline panel step should note its collapsible ⟩ rail, the Model tab should mention the N-way FE and Sun–Abraham/CS estimators, and the Simulate tab should reference the SampleTestPanel. CLAUDE.md rule: never write a count into tour steps; describe the feature, not the number of features.
**Invariants:** Tour steps are pure React (`TourOverlay` in `HelpSystem.jsx`). Inline styles only.

---

### Likely Fixed — Needs Franco's In-App Confirmation

- **p-value wrong for near-zero t-statistics** (reported 2026-05-21, 2026-07-22 ×2, 2026-07-28 ×2) — BugTriage: "likely fixed — `216b1ecb`"; fix is `betainc`/`betacf` Lentz continued-fraction in `src/math/LinearEngine.js`. **File:** `src/math/LinearEngine.js` (`tCDF`, `fCDF`, `betainc`). Confirm by running a regression with an obviously insignificant coefficient (e.g., β≈0.0001, SE≈0.002) and checking p > 0.9.
- **FE accepts any categorical variable (country, state, sector)** (2026-07-09) — BugTriage: "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation". **File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`.
- **FE with interactions not supported** (2026-07-09) — BugTriage: "likely fixed — FE interactions shipped in `045f5cda` (#104); needs confirmation". Same files as above.

---

## Features

### Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` currently supports one hypothesis at a time (Single tab). Add a "Save hypothesis" button that appends the current constraint string to a local list (`savedHypotheses` state in `CoefficientTestPanel`). Render the saved list as chips with a ✕ to remove. On "Copy all", emit a batch R block: `car::linearHypothesis(fit, c("...", "..."))` for each saved constraint. Wire the list into `generateJointHypothesisScript()` (already exists per BugTriage 2026-07-17 fix) so R/Python/Stata replication covers the full set. Persist per-model via `spec.savedHypotheses` threaded into `estimationDispatch.js` (same pattern as other spec fields).
**Invariants:** `ModelHypothesis.js` is in `src/math/` — no React imports there. Persistence goes through `indexedDB.js`, not localStorage.

---

### Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add an "ELASTICITY INTERPRETATION" rule to `INTERPRET_REGRESSION_PROMPT` (after the existing estimator-type rules J–R): when the dependent variable and a regressor are BOTH log-transformed (column names starting with `log_` or `ln_`), interpret β as a constant elasticity; when only Y is log-transformed (log-lin model), interpret as a semi-elasticity (`(e^β − 1) × 100%` percent change); when only X is log-transformed (lin-log), interpret as a one-unit proportional change in X shifting Y by `β/100`. Check column names from `result.varNames` passed in the user prompt. Additionally surface a compact "Elasticity hint" chip in `ModelingTab.jsx` below the coefficient table (not AI-gated) showing the formula when the model satisfies log-lin/log-log/lin-log patterns — this gives students instant feedback without an AI credit spend.
**Invariants:** All AI calls through `AIService.js` only. No React in `src/math/`. `SHARED_CONTEXT` cached block must remain intact.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text input at the top of each variable panel (Y, X, W) that filters the displayed column list by substring match. Debounce at ~150ms. The filtered list should highlight the matching substring in the chip label. Reset the filter when a column is added or the dataset changes. Keep the existing drag-to-reorder behavior on the unfiltered list; the filter is a search overlay, not a replacement for the full list. No external library needed — a simple `.filter(h => h.toLowerCase().includes(query))` over `availableVars`.
**Invariants:** Inline styles only (`C` object). No external UI libraries.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** The existing binned scatter plot (`BinnedScatterPlot` or equivalent in `ModelPlots.jsx`) exposes no bin-count control. Add UI controls modeled on `rdplot()`/`binsreg()` arguments: number of bins (slider, default 20), polynomial degree for the fit line within bins (0–3 selector), and a `ci_level` toggle (95%/90%/none). These should map directly to the emitted R code (`rdplot(y, x, nbins=N, p=P, ci=CI_level)` or `ggplot + stat_summary_bin`). Show the controls in a collapsible "Scatter options" row below the plot.
**Invariants:** `ModelPlots.jsx` is a UI component — computations (binning, fit) go in `src/math/` or `src/core/`. Inline styles only.

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Add a "Max points" slider (100–full n, log scale) to the collapsible plot controls panel in `ModelingTab.jsx`. Pass it as a `maxPoints` prop to `ModelPlots.jsx`. Inside each plot component that draws raw scatter points (partial plots, residual plots, RDD scatter), subsample to `maxPoints` via a deterministic stride (`rows.filter((_, i) => i % stride === 0)`). Add a "showing N of M observations" label so users know subsampling is active. Do NOT subsample for aggregated geoms (binned scatter, fitted line, event study CIs) — only point-level draws.
**Invariants:** Subsampling is display-only; it must never affect estimation (`rawData` and `cleanedData` are untouched). Inline styles only.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set in PlotBuilder and the number of distinct levels exceeds ~8, the observable10 legend chips become unreadable. Add a "Legend labels" toggle in the Labels panel that switches between chip-strip (current) and a vertical legend table with a color swatch, level name, and optional point count. For the Explore tab's categorical bar chart, add a hover tooltip on each bar showing the exact count and percentage. These are display-only changes with no impact on the underlying data or exports.
**Invariants:** Inline styles only. Observable Plot 0.6 CDN — no new CDN libs.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limit" mode button to the Calculate workbench alongside the existing Derivative/Integral/Optimize buttons. Use the existing CAS/symbolic engine (or the `nerdamer` / `algebrite` library already wired if present) to compute `lim_{x→a} f(x)` symbolically. Render input fields for: expression `f(x)`, variable, approach value `a`, and direction (±∞, left, right, two-sided). Output the limit value with a step-by-step expansion matching the "Show steps" pattern already implemented for integrals. Emit LaTeX and export a `limit()` call.
**Invariants:** No React in `src/math/`. If a symbolic engine is imported, it must come from the existing CDN allowlist (cdnjs, jsdelivr). All UI as inline styles with `C`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The function graph in `CalculateTab` currently only plots expressions evaluated as functions of `x`. Add a "Constants" sub-panel that lets users add named constants (e.g., `MC = 4`, `AVC_min = 2.5`) and toggle each one as a horizontal `hline` geom on the same axes. Constants should be labelled at the right edge of the plot. This matches the `hline`/`vline` geoms already implemented in `PlotBuilder.jsx` — reuse that rendering logic for the constant lines. No new math needed; render as a `<line y1={sy(val)} y2={sy(val)} />` SVG element in the existing chart.
**Invariants:** Inline styles only. No external plotting library for this component (uses custom SVG, not Observable Plot).

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** BugTriage flags this as "fix-later — route to V4 plots after launch." The minimum viable version: add a row-filter control above the map/geo-plot that accepts a column + operator + value (reuse the `FilterBuilder` predicate UI from `CleanTab.jsx` as a controlled component). Apply the filter using `evalPredicate` from `src/pipeline/predicate.js` before passing rows to the layer renderer. This avoids duplicating filter logic — `predicate.js` is the single owner of condition evaluation per CLAUDE.md.
**Invariants:** `predicate.js` is the ONLY owner of filter logic — never re-implement operators elsewhere. No React in `src/math/`.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/modeling/results/` (no dedicated RDD result component confirmed — likely `src/components/ModelingTab.jsx`), `src/math/CausalEngine.js`
**Suggestion:** BugTriage marks this "fix-later — presentation enhancement." `CausalEngine.runSharpRDD` already returns bandwidth, kernel, polynomial order, LATE estimate, SE, and bias-corrected CI. Add a collapsible "rdrobust summary" panel below the main coefficient table in the RDD result view, formatted identically to R's `summary(rdrobust(...))`: method/kernel/BW, N (left/right), Coef, SE (conv/robust), CI (conv/robust), and the bias/variance decomposition. This is purely display — no new math needed.
**Invariants:** Inline styles only. No external UI libraries. No changes to engine math.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", etc.
**File:** `src/services/export/latexTable.js`, `src/components/modeling/resultDisplay.jsx`
**Suggestion:** BugTriage marks this "fix-later — reporting metadata feature." Add a `labelOverrides` map (variable name → display label) to the model `spec`. Surface a small "Edit labels" pencil icon next to each row in the coefficient table that opens an inline text input for the display name. Pass `labelOverrides` to `latexTable.js`'s `buildStargazer()` function (already exported per CLAUDE.md) to substitute labels in the `\multicolumn` row. Persist `labelOverrides` in the model buffer alongside `spec` (it's already an IDB-persisted object).
**Invariants:** `labelOverrides` is a display-only map — it must never affect coefficient values or SE. Persistence goes through `indexedDB.js`. Inline styles only.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/components/modeling/ModelComparison.jsx`
**Suggestion:** BugTriage marks this open. `artifactOrder.js` already manages a project-scoped global order across saved plots/maps/models. Extend it with a `groups` field: `[{ id, label, artifactIds[] }]`. Add a "New group" button in `ModelBufferBar.jsx` that creates a named group and lets users drag pinned models into it. The comparison table (`ModelComparison.jsx`) should default to showing only the active group, with a "Show all" toggle. Groups should persist in IDB alongside `artifactOrder`.
**Invariants:** Persistence goes through `indexedDB.js`, never localStorage. `artifactOrder.js` is the single owner of cross-artifact ordering — do not add a second ordering store.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** BugTriage marks this open. `FeatureTab.jsx` mounts/unmounts its sub-panels on tab switch, discarding draft state. Solution: lift the draft state for the currently active transform type (e.g., `caseWhenDraft`) into `WranglingModule.jsx`'s component state (or a `useRef`) so it survives tab switches within the same dataset session. A simpler alternative: use `sessionStorage` keyed by `(pid, stepType)` to persist draft JSON — write on every change, read on mount. This mirrors how `History.jsx` already uses sessionStorage for the pipeline-column collapsed state.
**Invariants:** Persistence for pipeline/data goes through `indexedDB.js`. Draft UI state (not data) is fine in sessionStorage. No mutations to `rawData`.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** Design-only — no implementation started. Likely `src/services/session/sessionState.jsx` (for session-scoped state) and `src/components/wrangling/CleanTab.jsx` (for filter consumption)
**Suggestion:** This is a design question. The natural home is a `namedLists` map in the `SessionStateProvider` context (analogous to `datasets`) — a named list is an array of scalar values, scoped to the session. Expose a "Lists" panel in the Data tab or sidebar where users can create, name, and populate lists (paste from clipboard or select distinct values of a column). In `FilterBuilder`, add an `%in% list("ListName")` operator that resolves the list at evaluation time from `sessionState`. In `FeatureTab`, allow `case_when` to reference `%in% list("ListName")`. Export as `c(...)` literals in R and Python since they have no session-list concept.
**Invariants:** `sessionState.jsx` is the correct home for session-scoped, cross-module shared state. `predicate.js` must remain the single owner of filter operator evaluation — add the list resolution there, not in a local `filter()` call.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (CSV parse branch), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** BugTriage indicates Fase X4 is "code-complete; browser benchmark pending Franco." The fix (DuckDB-WASM route for large files, OPFS Parquet cache, 500-row preview) is merged. **This item needs Franco's browser confirmation** — specifically test: (1) load a 900k-row CSV (>10 MB) and confirm the tab does not crash, (2) close and reopen the project and confirm the full dataset restores from OPFS without re-importing, (3) tab-switch latency is under 2s. If crashes persist, the likely remaining gap is the `connect-src` CSP for `extensions.duckdb.org` (see CLAUDE.md CSP bug history — this has bitten three times) or the OPFS Parquet write being silently blocked.
**Invariants:** Data never leaves the browser. DuckDB as the data boundary — never pull the full Arrow result into JS objects for display.

---

## Other

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: `China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (CSV parse entry point), DuckDB-WASM CSV parser
**Suggestion:** BugTriage (2026-06-14) tracks this as "fix-later — post-launch resilient-import pass." When DuckDB's CSV parser encounters a malformed row (truncated value, embedded unescaped comma, or over-long field), it raises a `duckdb.RuntimeError`. Catch this at the `parseFile` boundary in `DataStudio.jsx` and surface: (1) the exact line number, (2) the raw line content (truncated at 200 chars), (3) two action buttons — "Skip bad rows and import rest" (retry with DuckDB's `IGNORE_ERRORS=TRUE` option) and "Abort". The skip path should report how many rows were skipped via `appendLog`. Wrap in `describeLoadError()` so the error message is user-readable.
**Invariants:** Privacy-first — raw row content shown only in the in-app dialog, never sent to any server. Non-destructive: an import error must not affect the active pipeline.

---

## Suggested action order

1. **Confirm p-value fix in-browser** — multiple critical reports; BugTriage marks "likely fixed" since `216b1ecb` but no Franco confirmation yet. Run one insignificant OLS coefficient and verify p > 0.9.
2. **Simulate replication script** (`src/components/tabs/SimulateTab.jsx` + new `src/services/export/simulateScript.js`) — high-priority UX gap; the DGP draw already exports, only the MC loop + downstream analysis stitching is missing.
3. **Stata workbench UNVERIFIED paths** (`src/services/export/stataScript.js`) — surface a warning chip in `CodeEditor.jsx` for any emitted snippet with a `NOTE:` or `UNVERIFIED` marker so users know before copying; separately audit `joinby` and `ibn.` against live Stata.
4. **Time series group references bug** (`src/ExplorerModule.jsx` `TimeSeriesTab`) — reset `seriesColors` on `grpCol` change and add a visible legend strip below the SVG.
5. **Confirm big-dataset performance** (`src/DataStudio.jsx` + DuckDB) — browser-test 900k-row CSV load, OPFS restore, and tab-switch latency; if still broken, check `connect-src` CSP for `extensions.duckdb.org`.
6. **LaTeX table width** (`src/services/export/latexTable.js`) — wrap wide tables in `adjustbox` or `resizebox`; low-effort, high academic-user impact.
7. **Variable searcher in VariableSelector** (`src/components/modeling/VariableSelector.jsx`) — quick win: filter chips by text input, no new libraries needed.
8. **Save case_when draft state** (`src/components/wrangling/FeatureTab.jsx`) — sessionStorage keyed by `(pid, stepType)` is a minimal intervention with high workflow value for the thesis-student use case.
