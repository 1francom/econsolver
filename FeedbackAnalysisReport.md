# Feedback Analysis Report — 2026-09-04

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

After cross-referencing ClaudeFB.md with BugTriage.md, the following open counts reflect items **not yet marked fixed** in either document, plus items marked "code-complete/pending browser validation" (still requiring a user-visible confirmation pass):

- **9 open bugs** (BugTriage status = open or code-complete-pending-validation)
- **4 bugs needing confirmation** (BugTriage status = "likely fixed" or "code-complete — browser validation pending Franco")
- **14 open features**
- **3 open UX / Design items**
- **1 open performance item**
- **1 open other item**

> **Note on many ClaudeFB items with no strikethrough:** Dozens of pre-June 2026 bullets lack strikethrough/✓ in ClaudeFB.md but are confirmed fixed in BugTriage.md (e.g. p-value bug, .dta parsing, plot color mapping, ACF/PACF theme). Those are excluded from this report — the ClaudeFB log was simply not updated with strikethroughs. Only items with BugTriage `status = open` or no BugTriage entry are treated as genuinely open.

---

## Bugs

### [high] · Export / Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** Identify which Stata tab flows are broken (model code tab vs. pipeline script tab vs. copy button). The most likely cause is `transpileStep` falling through to an unhandled step type and silently emitting a blank block, or the multi-subset/allDatasets path wiring being incomplete for Stata (see the HC-SE fix in 2026-07-18 which found ~25 hardcoded sites). Audit `stataScript.js` against the full `STEP_REGISTRY` list to find any step with a missing `toStata` branch.
**Invariants:** None — export files are pure JS with no React. Any new step translator must also be added to `pipelineReliabilityValidation.mjs` (T5 reverse check).

---

### [high] · Model / Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** The Single-hypothesis tab already emits a R snippet; the Joint tab (added 2026-07-30) gained `generateJointHypothesisScript()`. The gap is that users can't queue multiple hypotheses and export them all at once. Add a hypothesis history array in `CoefficientTestPanel` state (or persisted in IndexedDB under the model's result entry) and a "Copy all" button that emits an R block with one `linearHypothesis` call per saved constraint. Keep `ModelHypothesis.js` pure — the array lives in the component.
**Invariants:** Persistence goes to `src/services/Persistence/indexedDB.js`, never localStorage.

---

### [high] · Export / Simulate · 2026-07-28
**Feedback:** Update the replication script — currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx` (line ~1280, "Replication script" section), `src/math/dgpScript.js`
**Suggestion:** The Simulate replication script emits only `distExprR`/`distExprPy`/`distExprStata` (the DGP draw). Post-DGP analysis — resampling/bootstrap draws, Monte Carlo statistic distributions, and any model estimated on simulated data — is discarded. Extend the script generator to walk the user's current SimulateTab state (selected analysis mode, statistic, test, n_replications) and emit the corresponding code blocks after the DGP draw. Use `dgpScript.js` param schema as the single source for reproducible seed/param encoding.
**Invariants:** `dgpScript.js` is pure JS (no React). Script generation may live in a new `src/services/export/simulateScript.js` following the `rScript.js` pattern.

---

### [high] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The time-series plot's group/color mapping likely breaks when the reference group column contains null or non-string values — the same `Number(null) === 0` / `String(null)` coercion class that has bitten four other spots in this codebase. Check the group lookup in the TS plot path of `ExplorerModule.jsx` and ensure null group values are filtered out before the Observable Plot mark is built, matching the null-handling pattern in `SampleTests.js:cleanNumeric`.
**Invariants:** None — UI component only.

---

### [high] · Export / Model · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` generates a fixed `tabular` environment. For wide tables (5+ models), switch to `\resizebox{\textwidth}{!}{\begin{tabular}…}` or use the `adjustbox` package wrapper. Alternatively, detect column count and emit `\small` or `\footnotesize` before the environment, then restore. Either approach should be gated on column count so narrow tables are unaffected. Keep the logic in `latexTable.js` (pure JS) — `ReportingModule.jsx` just calls it.
**Invariants:** `latexTable.js` must remain React-free (pure JS export helper).

---

### [medium] · Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation of coefficients
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add an `ELASTICITY INTERPRETATION` rule block to `INTERPRET_REGRESSION_PROMPT` (analogous to the existing `ESTIMATOR TYPE RULES`). The rule should trigger when: (a) both Y and X are log-transformed (coefficient ≈ elasticity), (b) only X is log (semi-elasticity: 1% ↑ X → `β/100` unit ↑ Y), or (c) only Y is log (semi-elasticity: 1 unit ↑ X → `100·β%` ↑ Y). The detection hint should come from `spec.yVar`/`spec.xVars` names (presence of `log_`, `ln_`, or the pipeline `log` step). Thread the pipeline steps through the AI call (already in `snapshot`) so the model knows which transforms were applied.
**Invariants:** All Anthropic calls go through `AIService.js:callClaude`. No raw `fetch` to Anthropic API elsewhere.

---

### [medium] · UX / Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The time-series path in `ExplorerModule.jsx` renders through `PlotBuilder`. Check whether the existing labels panel (title, x/y axis label, caption) in `PlotBuilder.jsx` propagates to the Observable Plot `Plot.plot({...})` call for the TS geom — if not, wire `plotSpec.title`, `plotSpec.xLabel`, and `plotSpec.yLabel` into the TS mark/frame options. If the TS plot bypasses `PlotBuilder` and renders directly in `ExplorerModule`, add the same label options there.
**Invariants:** None.

---

### [medium] · General / UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Add a `groupLabel` field to the artifact entry schema in `artifactOrder.js`. In `ModelBufferBar.jsx` expose a rename/group chip on each pinned card that sets `groupLabel`; the buffer bar can then render grouped sections (collapsible) rather than a flat list. Store groups in IndexedDB under the existing `model_buffer` + `spatial_maps` stores — no new store needed. Start with free-text labels before building a drag-and-drop group UI.
**Invariants:** Persistence via `src/services/Persistence/indexedDB.js`. No localStorage.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean, and coming back to continue writing
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a per-subsection draft object stored in `sessionStorage` (keyed by `pid + subsection name`) that captures the in-progress form fields (column name, cases list, etc.) on every edit. On mount, restore from draft if one exists. Draft is cleared when the step is committed or explicitly cancelled. Use `sessionStorage` — not IndexedDB — because draft state is tab-local and transient; it need not survive a hard reload.
**Invariants:** Pipeline/data storage uses IndexedDB. `sessionStorage` is acceptable for ephemeral UI draft state (same pattern as the pipeline-column collapse state added 2026-08-01).

---

### [low] · General / UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array)
**Suggestion:** Audit each of the 9 `TOUR_STEPS` entries against the current tab structure (7-tab nav: Data/Clean/Explore/Model/Simulate/Calculate/Report). Update selectors (`.data-tour-*` attributes) and copy to reflect: the Strategy × Family estimator split, N-way FE, the collapsible Pipeline rail, and the new Spatial and Simulate subtabs. CLAUDE.md convention: never embed a count ("14 estimators") — name the thing instead.
**Invariants:** None.

---

## Bugs Needing Browser Confirmation

The following are code-complete per BugTriage but have never received Franco's in-browser sign-off. They should be tested in the next browser validation pass before being closed.

### [high] · Model · 2026-06-10 / 2026-06-14
**Feedback:** Plot Builder in ModelingTab is not resizable; PNG export has size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**BugTriage status:** code-complete `35f4b5a6` — browser validation pending Franco
**Suggestion:** Browser-test: (1) drag the plot viewport resize handle, (2) confirm PNG export captures the user-selected dimensions, (3) check that the export does not clip or over-scale the canvas on non-standard aspect ratios.
**Invariants:** None.

---

### [high] · Model · 2026-06-11 / 2026-06-14
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**BugTriage status:** code-complete `35f4b5a6` — browser validation pending Franco
**Suggestion:** Browser-test: (1) run OLS, confirm all three plots appear; (2) pin a model, switch the sidebar to a new spec, confirm the plots remain frozen on the pinned result; (3) restore a pinned model and confirm the plots reappear correctly.
**Invariants:** None.

---

### [high] · Spatial · 2026-06-13 / 2026-06-14
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**BugTriage status:** code-complete `5e105529` — browser validation pending Franco
**Suggestion:** Browser-test: (1) load a lat/lon dataset, switch to Map tab, add a Heatmap layer; (2) confirm KDE cells render with palette/opacity controls; (3) export as HTML; (4) check the 5k-point cap behaviour on large datasets.
**Invariants:** None.

---

### [high] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**BugTriage status:** code-complete `35f4b5a6` — browser validation pending Franco
**Suggestion:** Browser-test: (1) estimate OLS, note displayed diagnostics; (2) remove a regressor from the sidebar WITHOUT re-estimating; (3) confirm diagnostics remain frozen on the prior result; (4) pin the result, change the spec, confirm restoring the pin reverts the display correctly.
**Invariants:** None.

---

## Features

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — "Call: rdrobust Sharp RD estimates using local polynomial regression"
**File:** `src/math/CausalEngine.js`, `src/components/ModelingTab.jsx` (RDD result block)
**Suggestion:** In `runSharpRDD`'s returned `EstimationResult`, add a `rdrobustSummary` field that mirrors the key rows from `rdrobust`'s console output: Bandwidth (h), kernel, polynomial order, effective N left/right of cutoff, conventional/robust estimate and CI. Render this as a collapsible `<pre>` block just below the main coefficient table, using `mono` font and the `C` color scheme. Keep the math engine pure — formatting goes in the UI result block.
**Invariants:** `src/math/CausalEngine.js` must have zero React imports.

---

### Explore / Clean · 2026-05-19
**Feedback:** Add filter option for R-style membership syntax: `filter(regionname %in% c("Spain", "Basque Country"))`
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** The canonical predicate language already supports `in`/`nin` operators. The gap is the UI: the `FilterBuilder` in `CleanTab.jsx` should allow a multi-value list input (comma-separated or tag-input) for `in`/`nin`, not just a single value field. `predicate.js` (`evalPredicate`, `predicateToSQL`) already handles `in` natively — this is purely a UI affordance change. The `IN` chip should show a tag-input that produces `{op:"in", val:["Spain","Basque Country"]}`.
**Invariants:** `predicate.js` is the single owner of filter logic — never re-implement `op:"in"` elsewhere. Inline styles only, via `C` from `shared.jsx`.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** Add a `labelOverrides: { [internalName]: string }` param to `buildStargazer()` in `latexTable.js`. In `ReportingModule.jsx` render an editable label table (one row per coefficient, using the actual column names) that feeds into `labelOverrides`. Store overrides in IndexedDB under the model's result ID so they persist across sessions. The math files never see the display labels.
**Invariants:** `latexTable.js` must remain React-free. Persistence via IndexedDB.

---

### Calculate · 2026-06-19
**Feedback:** Add limits (calculus limits: `lim_{x→a} f(x)`)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" block to the Calculate workbench alongside Derivatives/Integrals/Optimization. Implement as a symbolic approach using the existing CAS (if the CAS supports substitution + L'Hôpital) or a numerical approach (evaluate at `a ± ε` for decreasing ε, extrapolate). Display left-limit, right-limit, and two-sided limit; flag indeterminate forms (0/0, ∞/∞). Pure math in `src/math/` (no React), UI only in `CalculateTab.jsx`.
**Invariants:** `src/math/` and `src/core/` must have zero React imports.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** In the equation workbench, detect expressions of the form `y = k` (no variable dependency) or allow a special `const k = …` syntax. For such expressions, render a `Plt.ruleY([k])` horizontal line in the Observable Plot canvas rather than a function curve. Add a label at the right margin with the constant's value. Extend the R/Python/Stata export to emit `geom_hline(yintercept=k)` / `plt.axhline(k)` / `yline k` alongside the existing function curves.
**Invariants:** Inline styles via `C` object. No external UI libraries.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the static spatial plot (SVG/geo-plot tab), not just the Leaflet map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a `grid` layer type to `SpatialGeoPlot`'s layer system (alongside the existing `points`/`polygons`/`choropleth`). If the dataset has a `grid_id` column (produced by the `sp_grid_assign` pipeline step), group rows by `grid_id`, compute per-cell aggregates (e.g. mean of a value column), and draw each cell as an SVG `<rect>` projected through the current CRS. Reuse `buildColorScale` from `src/components/tabs/spatial/shared/color.js` for fill. The grid geometry data (lat/lon bounds per cell) is already produced by `SpatialEngine.js`.
**Invariants:** Inline styles via `C`. No external UI libraries in SVG render.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots — filter map/plot to a spatial region
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** Add a "Spatial filter" accordion in each spatial plot tab's layer config: let users pick a WKT geometry column and a comparison geometry (either an existing dataset row or a drawn bounding box), then pass a `bbox`/`within` predicate that clips the rendered rows. Keep this as a VIEW filter only (like the Data Viewer's sort) — it never writes a pipeline step. For the Leaflet tab, this maps to `L.LatLngBounds` intersection; for the SVG tab, to a simple lat/lon range check before the `<rect>`/`<path>` is emitted.
**Invariants:** Never add a pipeline step here — the spatial filter is display-only. If users want a persistent filter, they should use `sp_spatial_join` as a pipeline step.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider so users can type and find variables faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a search text input above the variable list in `VariableSelector.jsx`. Filter the displayed column list to names containing the search string (case-insensitive). Use a `<input placeholder="Search variables…">` with `mono` font and `C.teal` focus border. Clear the search when the panel closes. No state needs to leave the component.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color (show what each color represents)
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set, Observable Plot already draws a color legend. The gap is likely that the legend chip/swatch labels are truncated or invisible on the dark theme. Check `Plot.legend({color: …})` output in the dark-theme path of `PlotBuilder.jsx` and ensure the legend container has sufficient width and that text color follows `C.fg` not a hardcoded value. Also add an optional `showLegend: true/false` toggle in the labels panel so users can show/hide it.
**Invariants:** Inline styles via `C`.

---

### Clean · 2026-07-17
**Feedback:** Click on a pipeline step in History and jump to the corresponding subsection in FeatureTab/CleanTab automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Add an `onStepClick(step)` callback from `History.jsx` up to `WranglingModule.jsx`. In `WranglingModule`, map the step's `type` to the corresponding tab (`log`/`sq`/`std`/`lag`/`lead` → "Feature" → "Transforms"; `filter` → "Clean"; `rename` → "Clean"; `group_summarize` → "Reshape"; etc.) and call `setActiveTab`/`setActiveSubTab` to navigate there. Populate the subsection's form from `step.params` so users see the step's current settings ready to edit.
**Invariants:** None — pure UI state navigation.

---

### Clean · 2026-07-19
**Feedback:** Add a Country Codes function — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step with params `{col, from, to}` where `from`/`to` ∈ `{iso2, iso3, name}`. Embed a static ~250-row lookup table (ISO 3166-1) directly in the step handler in `runner.js` — no external fetch. The step replaces each cell with the mapped value, leaving unrecognized codes as-is (or `null` with a warning count). Wire a "Country Codes" control into `FeatureTab.jsx` under a new "Reference" section. Register in `registry.js` and add R/Python/Stata translators.
**Invariants:** Pipeline step must replay on `rawData`, never mutate in place. Registry and runner must stay in sync (`pipelineReliabilityValidation.mjs`).

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them, and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** Add a `lists` concept to `sessionState.jsx`: a list is `{id, name, values: string[]}`. Store lists in IndexedDB under a new `session_lists` object store (pid-scoped). Expose lists in the `FilterBuilder` `in`/`nin` operator via a "Pick a list" option, and in `FeatureTab.jsx`'s `case_when` condition builder. This design keeps lists as session-level data, not global — one list created in a Clean step is automatically available in Explore's filter and vice versa, because they share the same pid-scoped session context.
**Invariants:** Persistence via `indexedDB.js`. No localStorage.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot binned scatter plots after running models using R/Python-style arguments for all estimators
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** The binned scatter panel already exists for RDD/OLS. Expose its binning params (`n_bins`, `bin_width`, `x_range`, `polynomial_order_overlay`) as user-editable inputs in a collapsible "Binscatter options" accordion below the plot. Add a "Copy to R" button that emits `binsreg::binsreg(y, x, w, nbins=n, ...)` (the `binsreg` package) rather than a manual `cut`/`aggregate` approach. Keep the rendering in `ModelPlots.jsx`, the R emission in `rScript.js`.
**Invariants:** `src/math/` files must remain React-free. Inline styles via `C`.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps so not everything is compared at the same time
*(Also listed above under Bugs as a UI enhancement — included here for UX completeness)*
**File:** `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/artifactOrder.js`
**Suggestion:** See the Bugs section entry above.
**Invariants:** Persistence via IndexedDB.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections
*(Also listed above under Bugs — included here for UX completeness)*
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** See the Bugs section entry above.
**Invariants:** Use `sessionStorage` for draft state, not IndexedDB.

---

### Clean · 2026-07-17
**Feedback:** Click on one pipeline step and go to the corresponding subsection automatically
*(Also listed under Features — flagged as UX because it is primarily a navigation convenience, not a new capability)*
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** See the Features section entry above.
**Invariants:** None.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (~21MB, 900k rows)
**File:** `src/DataStudio.jsx` (parseFile branches for rds/xlsx/shp), `src/services/data/duckdb.js`
**Suggestion:** The DuckDB-backed path (auto-convert CSV/TSV >10MB to Parquet + OPFS cache) is complete and browser-validated for CSV and `.dta`. The remaining gap is `.rds`, `.xlsx`, and `.shp` — these are still parsed fully into a JS object array and then (if >10MB) handed to DuckDB only after the full parse. For `.xlsx`, the `XLSX.utils.sheet_to_json` call materialises the whole workbook. For `.rds`, `parseRDS` returns a full array. Extend the post-parse bridge: after parsing any file format, if `rows.length > 50_000`, register as a DuckDB Arrow table (same `duckdb.registerArrow()` path as the DuckDB CSV loader) and set `_duckdb` on the result so the existing OPFS cache and SQL pipeline paths kick in. No new architectural concept — the DuckDB path already exists; only the entry check in `DataStudio.parseFile` needs the non-CSV formats added to the `>10MB` branch.
**Invariants:** Non-destructive: post-parse rows are never mutated. DuckDB is always the data boundary for large datasets.

---

## Other

### Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line: 1583 — "China,CHN,2010,2425.47..." (malformed row aborts import)
**File:** `src/services/data/parsers/` (CSV path in `DataStudio.jsx`)
**Suggestion:** BugTriage classifies this as "fix-later — resilient-import UX after launch." The concrete change: catch per-row parse errors in the CSV parse loop, accumulate them as `{ lineNumber, rawLine, reason }` entries, and after parsing show a "N rows skipped" banner with a toggleable detail panel listing the bad lines. This matches R's `problems()` / pandas' `error_bad_lines=False` UX. The remaining rows should still load. Do not silently drop rows without surfacing the count.
**Invariants:** Non-destructive pipeline: skipped rows must never affect the stored `rawData` silently.

---

## Suggested action order

1. **Browser-validate the four code-complete items** (PlotBuilder resize, Y~Yhat/Q-Q plots, Spatial heatmap, Diagnostics freeze) — they are already implemented and just need Franco's sign-off; closing them removes 4 high-severity items with zero code cost.
2. **Fix Stata workbench** (`stataScript.js` audit against full `STEP_REGISTRY`) — user-reported as broken; affects all Stata replication users.
3. **Implement Simulate replication script** (`SimulateTab.jsx` script generator extension) — currently the exported script reproduces none of the downstream analysis; high research-reproducibility impact.
4. **Fix time series group references** (`ExplorerModule.jsx`) — broken UI, null-coercion class previously seen in four other locations; likely a 5-line fix.
5. **Add multiple hypothesis test save + export** (`CoefficientTestPanel.jsx`) — expressly requested for R replication and directly blocks research workflow.
6. **Implement variable searcher** (`VariableSelector.jsx`) — low-effort UI add with high usability gain for wide datasets.
7. **Fix LaTeX table width** (`latexTable.js`) — one-line `\resizebox` wrapper; high visibility to thesis/paper users.
8. **Add time series plot labels** (`PlotBuilder.jsx`) — medium effort; time-series without axis labels is not publication-ready.
9. **Add `in`/`nin` tag-input UI** (`CleanTab.jsx`) — predicate engine already supports it; purely a FilterBuilder UI gap.
10. **Extend DuckDB-backed path to rds/xlsx/shp** (`DataStudio.jsx`) — completes the performance work already done for CSV/.dta and eliminates the remaining large-file crash class.
