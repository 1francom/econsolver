# Feedback Analysis Report — 2026-09-24

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **13 open bugs** (confirmed unresolved) · **5 pending browser validation** · **6 deferred (fix-later)** · **14 open features** · **4 open UX** · **1 open performance**
- Total actionable items: **43**

---

## Bugs

### [critical] · Model · 2026-07-22 / 2026-07-28
**Feedback:** P-values with small numbers — t=0.49 but p<0.001; beta=0.0001, se=0.002 cannot give p<0.001 with ***; P-values of OLS wrong for insignificant estimates (three duplicate reports)
**File:** `src/math/LinearEngine.js` (`tCDF`/`betainc`)
**Suggestion:** Commit `216b1ecb` is flagged as the fix (Numerical Recipes continued-fraction `betainc`). Status in BugTriage is "likely fixed — needs confirmation." Franco must confirm in-app by running a regression where t ≈ 0 and verifying p ≈ 1, and a case with t≈2 to verify p≈0.05. If fix is confirmed, mark all three rows as resolved in BugTriage.
**Invariants:** none — this is pure math; `src/math/` is zero-React.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx` (lines 84+), `src/services/export/stataScript.js`
**Suggestion:** The BugTriage entry for this item (row 2026-07-17) is "open" with no investigation notes. `CodeEditor.jsx`'s `buildScript()` constructs a `config` object from a field whitelist; the CS DiD fix (2026-07-30) already exposed that this whitelist was the root cause of a similar broken-export bug. Inspect which estimators produce malformed or empty Stata output — likely the whitelist is missing spec fields for Panel FE, CS DiD, or the new `noIntercept`/HC2/HC3 modes. Run the existing `exportValidation.js` harness with `--lang=stata` to identify which estimator/SE combinations fail, then patch `stataScript.js` and the whitelist together.
**Invariants:** Stata export must agree with the spec fields established by `estimationDispatch.js`; the "THREE whitelist edits" rule from CLAUDE.md applies.

---

### [high] · Export · 2026-07-28
**Feedback:** Update the replication script — currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/statInferenceScript.js`, `src/services/export/statInferenceGroupScript.js`
**Suggestion:** `SimulateTab` exports a DGP-only script. The downstream analysis (Monte Carlo loop, sampling tests via `SampleTestPanel`, OLS distribution on simulated data) never reaches the script. Add a `buildSimulateReplicationScript()` function in a new `src/services/export/simulateScript.js` that chains: (1) DGP draw (`distExprR`/`distExprPy`/`distExprStata` from `dgpScript.js`), (2) any MC OLS loop if `mcY`/`mcX` are set, (3) any sample test that ran. The Simulate tab already appends to the session log (`appendLog`) with `opType: "mc_ols"` / `"mc_single"` — use those log entries as the source of truth for which analyses to emit, mirroring how `CodeEditor.jsx` uses `result.spec`.
**Invariants:** `src/math/` and `src/core/` must remain zero-React; DGP logic already lives in `dgpDraw.js` / `dgpScript.js` and can be called directly.

---

### [high] · Export · 2026-07-05
**Feedback:** LaTeX table size must adjust when the number of columns increases
**File:** `src/services/export/latexTable.js` (line 39)
**Suggestion:** `colFmt = "l" + " r".repeat(colsN)` emits fixed-width columns regardless of column count; wide multi-model tables overflow the text block. When `colsN >= 4`, wrap `\begin{tabular}` in `\begin{adjustbox}{max width=\textwidth}...\end{adjustbox}` (requires `\usepackage{adjustbox}` in the preamble), or switch to `p{1.5cm}` column specifiers and emit `\tiny` in the preamble. Add the `adjustbox` package to the emitted preamble comment. The CS DiD ATT(g,t) table at line 231 uses a fixed 8-column spec and should receive the same treatment.
**Invariants:** `latexTable.js` is pure JS with no React imports — safe to modify freely.

---

### [high] · Pipeline · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/components/modeling/CoefficientTestPanel.jsx`, `src/math/ModelHypothesis.js`
**Suggestion:** `CoefficientTestPanel` runs one hypothesis at a time; results are shown but not accumulated. Add a `savedTests` state array in `CoefficientTestPanel.jsx` with an "Add to saved" button per result; render saved tests as a collapsible list; expose a "Copy all" button that calls the new `generateJointHypothesisScript()` (already shipped 2026-07-30) once per saved test and concatenates the snippets. SE type and model spec are already available in the panel props.
**Invariants:** none for this UI change; `ModelHypothesis.js` is already pure JS.

---

### [medium] · UI · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` (around line 1455–1617, `grpCol` state)
**Suggestion:** The time-series component uses `grpCol` to split series by group. The bug description is vague — most likely the reference line or legend for a specific group renders incorrectly or not at all. Inspect the `aggregateTimeSeries()` / `fetchAggregateTimeSeriesSQL()` path (lines 182–227): verify the `groups` array matches what `setGrpCol` stores, and that the SVG/Plot line layer iterates over all groups. Also check whether a group selected as a "reference" (for a before/after comparison) correctly resets when the column changes. Add a minimal repro: one numeric column, one two-level categorical group column, switch group selector, confirm both lines render and the legend labels match.
**Invariants:** none.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis labels, etc.
**File:** `src/ExplorerModule.jsx` (time-series section, ~line 1580+), `src/components/PlotBuilder.jsx`
**Suggestion:** The time series chart in Explore is an SVG rendered inline in `ExplorerModule.jsx`, not via `PlotBuilder`. Add a collapsible "Labels" panel below the chart controls (mirrors the Pattern already in PlotBuilder's labels panel): title, x-axis label, y-axis label. Store in local state; pass to the SVG renderer as props. The pin export already includes `yCol`/`timeCol` — extend the pin kind to include labels so `renderPinnedPlot` can use them.
**Invariants:** none; inline styles via `C`, no CSS files.

---

### [medium] · Export · 2026-07-09
**Feedback:** Add direct elasticity interpretation (displayed alongside coefficient output)
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT, line 364+), `src/components/ModelingTab.jsx`
**Suggestion:** The AI prompt already has log-log elasticity and log-level semi-elasticity rules (lines 364–365). The feedback likely means a deterministic, non-AI elasticity badge computed directly from the result — similar to R's `margins` package. After estimation, inspect `result.varNames` and `result.yVar`: if `yVar` or an `xVar` was log-transformed in the pipeline (check `result.spec.pipeline` for `log` steps), display a small inline badge "elasticity ≈ β" or "semi-elasticity ≈ β×100%" beside each coefficient row. The log-detection logic can reuse the `FeatureTab.jsx` `log` step signature.
**Invariants:** Coefficient display is in `src/components/modeling/` — no `src/math/` changes needed; no new Anthropic calls.

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** `artifactOrder.js` owns the global order list for saved plots/maps/models. Extend `makeArtifactId`/`parseArtifactId` with an optional `group` tag. In `ModelBufferBar`, add a "+ Group" button that creates a named group and a group selector chip per pinned model. Store group assignments in the existing IDB `model_buffer` store (add a `group` field to `trimResult`). For comparison, filter `compRows` to the active group only. This is a pure UI/persistence change with no math impact.
**Invariants:** persist via `indexedDB.js`; never localStorage.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, coming back to case_when to continue writing
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` manages `case_when`, `mutate`, `if_else`, and other expression builders in local component state that is lost on navigation. Lift the draft state to the parent `WranglingModule.jsx` (or to `sessionStorage` keyed by `pid + stepType`) so that switching tabs preserves the in-progress expression. This mirrors how `History.jsx` already persists pipeline collapse state in sessionStorage. No IDB or IndexedDB write needed — sessionStorage is fine for ephemeral draft data.
**Invariants:** none.

---

### [medium] · Math · 2026-07-09
**Feedback:** FE must accept any categorical variable (country, state, sector), not just entity and time (+ FE with interactions not supported)
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage status is "likely fixed — N-way FE shipped in `045f5cda` (#104), needs confirmation." Franco should confirm in-app that arbitrary categorical columns (e.g. `sector`, `country`) can be selected as FE dimensions beyond the standard entity/time pair, and that interaction FE terms (e.g. `state×year`) work. No code change suggested until confirmed broken — this is a verification task.
**Invariants:** `runFEMulti` is in `src/math/` — zero React. Any UI changes go in `ModelConfiguration.jsx`.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array)
**Suggestion:** CLAUDE.md's working conventions note that `TOUR_STEPS` and `HintBox` copy rot because nothing enforces them. A targeted pass is needed: compare each of the 9 tour steps against the current UI — estimator list, tab names, FE options, geom count (now 12), SE types (now includes CR2/CR3). Update step descriptions and screenshot references. Also ensure `APP_CAPABILITY_MAP` in `appCapabilityMap.js` matches the current 7 tabs.
**Invariants:** inline styles via `C`; no CSS.

---

### Bugs Pending Browser Validation (code-complete, action = Franco confirms in-app)

| Date | Module | Description | File | Status |
|------|--------|-------------|------|--------|
| 2026-06-10 | Model | Plot Builder resize and PNG export size/aesthetics | `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` |
| 2026-06-11 | Model | OLS Y~Ŷ, Residual vs Fitted, Q-Q plots missing | `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` |
| 2026-06-13 | Spatial | Heatmap display broken | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` | code-complete `5e105529` |
| 2026-06-14 | Model | Diagnostics change when variables are added/removed | `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` |
| 2026-07-17 | Model | HC2 SE not replicated in R/Stata replication code | `src/services/export/rScript.js`, `src/services/export/stataScript.js` | code-complete 2026-07-18 |

**Action:** Franco should open the app and verify each of these five items. Mark them fixed or reopen with specific failure description.

---

### Deferred Bugs (fix-later — post-launch)

| Date | Module | Description | File |
|------|--------|-------------|------|
| 2026-05-10 | Data | CSV parse error on malformed row (China, 2010) — should surface recovery options | `src/DataStudio.jsx`, `src/services/data/parsers/` |
| 2026-06-14 | Data | Malformed CSV row aborts import instead of offering skip/repair | `src/DataStudio.jsx` |
| 2026-06-14 | Clean | R-style `%in% c(...)` not accepted by filter expression UI | `src/components/wrangling/CleanTab.jsx` |
| 2026-06-14 | Model | RDD result lacks `summary(rdrobust)`-style compact table | `src/components/modeling/` |
| 2026-06-14 | Spatial | No spatial row-filter control scoped to a plot layer | `src/components/tabs/spatial/plot/` |
| 2026-06-14 | Model | LaTeX coefficient table display names not independently editable | `src/components/modeling/resultDisplay.jsx` |

---

## Features

### · Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limit" expression type in the expression workbench alongside derivatives and integrals. Internally the `calcEngine.js` (or a new `src/math/limitsEngine.js`) can use L'Hôpital's rule or Richardson extrapolation for numeric limits; a symbolic CAS call handles formal limits. Expose a `lim(f(x), x, a)` syntax in the expression parser consistent with current derivative notation.
**Invariants:** `src/math/` is zero-React; `CalculateTab.jsx` is the only UI surface.

---

### · Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. horizontal line for a constant alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Already partially done in `PlotBuilder.jsx` (the `hline`/`vline` geoms added 2026-07-29). In `CalculateTab`, detect when an expression is a numeric constant (no `x` variable) and render it as a dashed horizontal line on the existing function canvas using the same Observable Plot `ruleY` primitive used by `hline`. No new UI surface needed — the expression input already handles it; just branch on `isConstant(expr)` before the function-plotter path.
**Invariants:** none.

---

### · Model · 2026-07-22
**Feedback:** Add more options for binned scatter plots after running models — using R and Python command arguments in the UI, for all estimators
**File:** `src/components/modeling/ModelPlots.jsx`, `src/math/CausalEngine.js`
**Suggestion:** Add a "Binned scatter" panel (collapsed by default) under model results for OLS, FE, 2SLS, and RDD. Use `rdplot`-style fixed-width bins or quantile bins; expose `nbins`, `x_range`, and `ci=TRUE/FALSE` controls. The underlying scatter is already available in `result.rows`. Map to `rdplot()` (R), `binscatter()` (Python `binsreg`), and a manual `xtile`+`collapse`+`scatter` idiom (Stata). For RDD specifically, the bandwidth window and polynomial order are already in `result.spec` and can pre-fill the controls.
**Invariants:** `src/math/` files are zero-React; plot components belong in `src/components/modeling/ModelPlots.jsx`.

---

### · General · 2026-07-05
**Feedback:** Add plots with 3 axes
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 does not natively support a secondary y-axis (`scale_y_sec_axis` in ggplot2). A pragmatic approach is to add a "dual-axis" mode that renders two overlapping Plot instances with a shared x-axis — the second y-axis ticks are drawn on the right via a `Plot.axisY({anchor:"right"})` mark. This is a significant UI addition; scope carefully. Alternatively, expose a "two-series, right axis" shortcut as a layer-level toggle (e.g. any `line` layer can opt into the secondary y-axis).
**Invariants:** no external UI libraries; all styling via `C`.

---

### · Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a filter input above the variable chip list in `VariableSelector.jsx`. Filter is local state — `useState("")` — that narrows the displayed chips via `.filter(h => h.toLowerCase().includes(q))`. No IDB or pipeline impact. This is a small, self-contained UI change.
**Invariants:** inline styles via `C`.

---

### · Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** Add a "Display n" slider or input in the plot toolbar that limits the number of rows rendered (e.g. random sample of 500–5000 points). This is a VIEW-only setting — it never touches `result` or `rawData`. For scatter plots (RDD, DiD, PartialPlot), pass `slicedRows = rows.slice(0, displayN)` to the mark. Flag in the plot caption: "Showing N of M obs." This mirrors the Data Viewer's existing preview cap.
**Invariants:** display limit is never applied to estimation; `runner.js` always sees the full dataset.

---

### · Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` already knows its `type`. Add a click handler on the step badge that calls a new `onNavigateToStep(step)` callback in `WranglingModule.jsx`. The callback maps `step.type` → tab index (e.g. `normalize`, `filter` → Clean; `log`, `add_column` → Feature; `group_summarize` → Reshape; `join` → Merge) and calls `setTab(tabIndex)`. No pipeline or data mutation.
**Invariants:** none.

---

### · Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` step type: input column (country name or iso2), output column, direction (name→iso2, name→iso3, iso2→iso3, etc.). The lookup table (~250 rows, ~8KB) can be embedded as a static JSON in `src/math/countryLookup.json` or inlined as a const in a helper. Wire through the registry and runner. The `FeatureTab.jsx` UI panel can reuse the existing `add_column` skeleton. For fuzzy matching (name→code) reuse `fuzzyGroups` from `wrangling/utils.js` with a strict similarity threshold.
**Invariants:** step must replay on `rawData` (non-destructive); persist step params via `registry.js`; lookup table in `src/math/` only (no React there).

---

### · Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set and the geom is `point` or `line`, Observable Plot renders a color legend via `Plot.legend`. Add a toggle to also draw inline labels at the end of each line (for `line` geom, the last data point) or as `Plot.text` marks placed at the centroid of each color group (for `point` scatter). This is a common ggplot2 `ggrepel`-style enhancement. Keep it opt-in via a "Show labels" toggle in the Labels panel to avoid clutter in dense plots.
**Invariants:** inline styles via `C`.

---

### · Explore / Model · Various
**Feedback (grouped):** (a) Filter with `%in% c(...)` R-style syntax (2026-05-19); (b) RDD `summary(rdrobust)`-style table (2026-05-21); (c) Spatial filters for plots (2026-05-30); (d) Editable LaTeX table variable names (2026-06-06); (e) Cross-module session list management (2026-07-19); (f) 3D plots (2026-07-05)
**File:** Various — see Deferred Bugs table above + `src/components/PlotBuilder.jsx` for 3D
**Suggestion:** (a), (b), (c), (d) are already tracked as "fix-later" in BugTriage. (e) needs a design decision before implementation — session-scoped context in `sessionState.jsx` is the natural home. (f) 3D plots require a third-party library (Plotly or Three.js) or Observable Plot 0.7+ — scope as a post-MVP enhancement.
**Invariants:** (f) would require adding a new CDN dependency — add to `vercel.json` `connect-src` AND `script-src` per the CSP bug documented in CLAUDE.md.

---

## UX / Design

### · UX · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** (See Bug section above — duplicate entry; handled there.)
**Invariants:** sessionStorage keyed by `pid + stepType`; not IDB.

---

### · UX · General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`
**Suggestion:** A session-scoped list store is a design-first decision. The natural home is `SessionStateProvider` (already in `sessionState.jsx`) — add a `lists` map `{name → string[]}`. Expose a "Save as list" button next to the distinct-values panel (already shipped 2026-07-30). In `FilterBuilder`, add a `$in list` operator that pulls from the stored lists. No IDB write needed for session-only lists; an IDB `session_lists` store would be needed for persistence across reloads.
**Invariants:** persistence via `indexedDB.js` if cross-session; `sessionState.jsx` is React context, never used in `src/math/`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (~21 MB, 900k rows)
**File:** `src/DataStudio.jsx` (parseFile branches), `src/services/data/duckdb.js`
**Suggestion:** BugTriage routes this to "Fase X4 — code-complete; browser benchmark pending Franco." The DuckDB performance strategy in CLAUDE.md lists: (1) OPFS Parquet cache for large CSV, (2) `SELECT` only needed columns, (3) 200–500 row display limit, (4) precompute column stats in SQL. Browser benchmarking is the outstanding task — Franco should open a 21MB CSV, navigate all 7 tabs, and confirm no crash. If a crash persists, the likely culprits are: (a) a tab that still calls `SELECT *` materialising 900k rows in JS (check `duckdbRunner.js` step handlers), or (b) React re-rendering with the full `rawData` array in state (check DataStudio's `useEffect` dependencies).
**Invariants:** full dataset is always available to `runner.js` / `duckdbRunner.js`; display layer caps at 500 rows; never truncate `rawData`.

---

## Suggested Action Order

1. **[CRITICAL, confirm] P-value fix** — Franco confirms `216b1ecb` in browser: run an OLS with a near-zero t-stat and verify p ≈ 1. Mark resolved in BugTriage if confirmed; otherwise open a hotfix.
2. **[HIGH, confirm] Five code-complete validation items** — Franco opens the app and verifies: Plot Builder resize/PNG, OLS residual plots, spatial heatmap, frozen diagnostics, HC2 SE replication code (see Pending Browser Validation table). Each is code-complete; only browser sign-off is missing.
3. **[HIGH] Fix Stata workbench** — run `exportValidation.js` in Stata mode to identify broken estimator/SE combos; patch whitelist + `stataScript.js` together.
4. **[HIGH] Fix Simulate replication script** — add `buildSimulateReplicationScript()` that chains DGP + MC + sample test blocks, using session log entries as the source of truth.
5. **[HIGH] LaTeX table auto-width** — wrap `\begin{tabular}` in `adjustbox` when `colsN >= 4`; one-line change in `latexTable.js:39`.
6. **[MEDIUM] Time series group references** — reproduce in-app with a two-group time series, trace `grpCol` through `aggregateTimeSeries`/SQL path, fix reference line rendering.
7. **[MEDIUM] Multiple hypothesis tests save/export** — add `savedTests` state + "Add to saved" button + "Copy all" in `CoefficientTestPanel.jsx`.
8. **[MEDIUM] Variable searcher in model sidebar** — `VariableSelector.jsx` filter input, ~20 lines.
9. **[MEDIUM] Pipeline step → subsection navigation** — `History.jsx` click handler + `WranglingModule.jsx` tab dispatcher, ~30 lines.
10. **[MEDIUM] Country codes transform step** — new `country_code` step type with static lookup; unblocks a common data cleaning workflow.
