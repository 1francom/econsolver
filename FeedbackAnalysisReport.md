# Feedback Analysis Report — 2026-09-10

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **10 open bugs** · **9 open features** · **2 open UX / Design** · **1 open performance** · **1 open other**
- **4 items marked "likely fixed — needs Franco's in-browser confirmation"** (p-values ×3, N-way FE ×2)
- **3 items code-complete but browser validation still pending** (PlotBuilder resize, residual plots, spatial heatmap)

---

## Bugs

### [high] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`  
**Suggestion:** Audit the Stata tab of `CodeEditor.jsx` — specifically the interactive REPL/workbench section — for missing `do`-file directives (e.g. `version`, `clear`, delimiter) that Stata requires to execute a pasted block. Cross-check `stataScript.js`'s `transpileModel` output against each estimator family using the 168-cell SE-type matrix test already in the codebase (`seTolerances.js`). Known gap: `xtreg` two-way cluster routes to `reghdfe` with a note — verify that the `reghdfe` install call is present in the emitted preamble.  
**Invariants:** All Anthropic calls must go through `AIService.js` only — do not add an AI-assisted Stata eval path elsewhere.

---

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP  
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (script generators)  
**Suggestion:** `SimulateTab.jsx` currently hands only the DGP draw config to the script generator. The downstream steps (resampling draws, Monte Carlo loop, model estimation on simulated data, sample tests) live in component state but are never serialised. Add a `buildSimulateScript({ dgpConfig, mcConfig, resamplingConfig, testConfig })` function in `src/services/export/` (mirroring `generateUnifiedScript`), and wire it to a "Copy replication script" button in `SimulateTab`. R output should wrap the DGP in a `replicate(B, { ... })` loop; Python should use a list comprehension or `pd.DataFrame([... for _ in range(B)])`. The Stata path should emit an honest note that Stata's simulation loop differs from R/Python's functional style.  
**Invariants:** `src/math/dgpDraw.js` is pure JS — the script generator must not import it. Read `dgpScript.js` for the R/Python/Stata expression translators, which are already the single source for this.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work  
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`  
**Suggestion:** In `ExplorerModule.jsx`, find the time-series plot section and trace how the "group" / "color by" column is mapped to Observable Plot's `stroke` channel. The likely defect is that the group accessor is built once on the initial `rows` array but Plot receives a memoised `plotRows` slice — so the accessor's closure captures a stale reference. Verify that `group` / `colorBy` is passed as a column name string into `PlotCanvas`, not as a pre-extracted array, so Plot can re-resolve it per row. The `ExplorerModule` time-series section predates the PlotBuilder's `aes.color` refactor and may still use an older direct-array binding.  
**Invariants:** None — pure UI change.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`  
**Suggestion:** The Explorer's time-series view renders directly in `ExplorerModule.jsx` rather than delegating to `PlotBuilder.jsx`. `PlotCanvas` already accepts `xLabel`, `yLabel`, and `title` props (confirmed in `PlotBuilder.jsx:858-874`). The fix is to expose three text inputs in the time-series sub-tab of `ExplorerModule` and thread them through to `PlotCanvas`. The pin/compare params object should carry `{xLabel, yLabel, title}` so pinned time-series plots restore their labels. The R/Python/Stata export counterparts (`labs(x=..., y=..., title=...)`, `plt.xlabel/ylabel/title`, `title/ytitle/xtitle`) are already in `plotScript.js` for PlotBuilder geoms — reuse the same emitter.  
**Invariants:** None — pure UI and export change.

---

### [medium] · General · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`  
**Suggestion:** `latexTable.js:173` currently emits a plain `\begin{tabular}{...}` with no width control. When the number of models exceeds ~5 the table overflows `\textwidth`. Wrap the `tabular` in `\resizebox{\textwidth}{!}{ ... }` (requires `graphicx`, already common in econometrics templates) — or emit `\begin{adjustbox}{max width=\textwidth}` (requires `adjustbox`). Add a `\usepackage{graphicx}` comment at the top of the emitted LaTeX block. Prefer `resizebox` as it needs only `graphicx`, which most APA/AER templates already load. Expose a `wrapMode` param so callers can override if needed.  
**Invariants:** None — export-only change.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `CoefficientTestPanel.jsx` currently runs one test at a time with no persistence. Add an `savedHypotheses` array in the component (or in `IndexedDB` via `indexedDB.js` under a `hypotheses` store keyed by model id) so tests accumulate per model. Each saved entry stores `{ label, type, constraints, nullVals, stat, p, preferred }`. The "Copy code" button should emit ALL saved tests in one block — `car::linearHypothesis(fit, list(c1, c2))` in R runs multiple in one call; Python can loop `fit.wald_test(constraint)`. The generation logic already lives in `generateJointHypothesisScript()` (added 2026-07-30) — extend it to accept an array.  
**Invariants:** Use `indexedDB.js` for persistence, not `localStorage`.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** `FeatureTab.jsx` has no draft state for in-progress `case_when`, `if_else`, or `mutate` builders. Add a `draftState` object keyed by `[pid, datasetId, subTab]` — stored in `sessionStorage` (not `localStorage`; not `indexedDB` — this is per-session transient state, not pipeline state) — that the component reads on mount and writes on every change. The key should include the dataset id (accessible from `useSessionState`) so drafts don't leak across datasets. Scope to the current session by NOT persisting to IndexedDB: a draft is intentionally lost on reload (it is not a committed step), but must survive intra-session tab-switches. Guard the `sessionStorage` access in a try/catch per the architectural note on storage availability.  
**Invariants:** Do not store pipeline steps in `sessionStorage` — that is `indexedDB.js`'s domain. Draft state is ephemeral UI state only.

---

### [medium] · Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)  
**Suggestion:** The prompt already handles log-log and log-level functional forms (lines 99, 364-365 confirmed). The gap is that the AI does not automatically detect which specification the user is running and trigger the elasticity rule without the user knowing to use log transforms. Add a rule in `INTERPRET_REGRESSION_PROMPT` under the functional-form section: if `result.yVar.startsWith("ln_")` or `result.yVar.startsWith("log_")` AND an X similarly starts with `ln_`/`log_`, the AI should lead with "This is a log-log specification; β is an elasticity." The AI context already receives `result.varNames` and `result.yVar` — no engine changes needed.  
**Invariants:** All Anthropic calls must route through `AIService.js` only. Prompt changes only go in `src/services/AI/Prompts/index.js`.

---

### [critical — needs confirmation] · Model · 2026-07-22 / 2026-07-28 (×3 duplicates)
**Feedback:** P-values with small numbers — beta=0.0001 and se=0.002 can never give p<0.001 with ***  
**File:** `src/math/LinearEngine.js` (`tCDF`/`betainc`)  
**Suggestion:** The fix was committed as `216b1ecb` (2026-07-28) — Numerical Recipes continued-fraction `betainc` replacing the Riemann-sum approximation. The fix also landed in `calcEngine.js`'s `_incompleteBeta` (2026-07-30). **Action needed: Franco must confirm in-browser on a regression with a truly insignificant coefficient** (t ≈ 0.5, expected p ≈ 0.62). If still wrong, check whether `EstimationResult.js` caches the pre-fix p-value and the cache is not invalidated on the commit. The DuckDB suff-stats path uses the same `tCDF` — also needs confirmation under large-n (n > 50k).  
**Invariants:** None — math-only fix. No React in `src/math/`.

---

### [medium — needs confirmation] · Model · 2026-07-09
**Feedback:** Add options for fixed effects — not just entity and time, but also variables like country, state, sector  
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`  
**Suggestion:** N-way FE shipped in `#104` (`045f5cda`) and is validated vs R fixest for 2-way and 3-way FE (CLAUDE.md confirms 18/18 checks). **Action needed: Franco should confirm in-browser by declaring a non-standard FE dimension (e.g. `sector`) in `PanelTab` and verifying that the FE picker in `ModelConfiguration.jsx` accepts it and that the result table shows the right df.** FE interactions were included in the same PR. Mark both BugTriage rows fixed once confirmed.  
**Invariants:** None.

---

## Features

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/components/ModelingTab.jsx`, `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** After `runSharpRDD` returns, construct a summary block styled after `rdrobust`'s printed output: bandwidth (h_l, h_r), effective obs. left/right, order, kernel, and the four conventional/bias-corrected/robust CI rows. This is a pure display component using existing `result` fields (`bw`, `nLeft`, `nRight`, `coef`, `se`, `ci`). Render it as a collapsible `<Section>` above the coefficient table in the RDD results panel. No new engine work needed — all values are already computed.  
**Invariants:** None — UI-only.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/geo.js`  
**Suggestion:** The SVG geo-plot tab (`SpatialGeoPlot.jsx`) already renders polygon/point layers via Observable Plot. Add a `grid` layer type in `geo.js` (`mkGeoLayer`) that reads the `gridId` / H3 column (already produced by `GridSection.jsx`'s pipeline step) and renders filled rectangles or hex cells using `Plt.geo` with a GeoJSON FeatureCollection assembled from the WKT rings. Reuse `buildColorScale` from `color.js` for choropleth fills. The `GeoLayerConfig.jsx` layer picker should expose a "Grid" option alongside Point/Polygon.  
**Invariants:** `src/math/SpatialEngine.js` is pure JS — no React imports. Grid geometry helpers belong there.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a text `<input>` at the top of `VariableSelector`'s column list that filters the displayed variable names by substring match (case-insensitive). The filter should be local component state (no persistence needed). Apply the filter to the rendered `<Chip>` list only — do not mutate the underlying `headers` array. Clear the filter when the dataset changes. For accessibility, give the input a `placeholder="Search variables…"` and `aria-label`.  
**Invariants:** None — pure UI.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`  
**Suggestion:** When `aes.color` is set, the legend currently shows color swatches with the raw value string. Add a "Legend title" text input in the Labels panel (already has `xLabel`/`yLabel`/`title`) that maps to Observable Plot's `color: { legend: true, label: legendTitle }` option. Also expose a "Show labels on marks" toggle that, for point/line geoms, adds `Plot.text(rows, {x, y, text: colorCol, fill: colorAccessor, fontSize: 9})` — matching ggplot's `geom_label`. Wire `legendTitle` into the R export as `labs(color = legendTitle)` and into Python as `legend_title=`.  
**Invariants:** None.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators  
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** Extend the `ModelPlots` binscatter section (currently rendered for OLS/RDD) to accept user-controlled parameters matching `rdplot`/`binsreg`: number of bins (`J`), polynomial order for the mean line, confidence-band toggle, and x-axis range override. Store these in a `binscatterOpts` state object in `ModelingTab` (same pattern as `rddOpts`). The replication code should emit `binsreg::binsreg(y, x, data=df, nbins=J, polyreg=p)` in R and `pystata`-style equivalents. Keep the DGP rendering correct: use the full `rawData` via the DuckDB path for large datasets, not the 500-row preview.  
**Invariants:** Engines in `src/math/` must remain pure JS with no React imports.

---

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The calculator already uses a CAS (Computer Algebra System) for derivatives, integrals, and optimisation. Add a `limit(expr, var, val, direction?)` entry to the workbench (direction: `+`, `-`, or two-sided). The CAS likely already supports this through its `derivative` path (L'Hôpital is a fallback) — check whether the existing library exposes a `limit()` call before adding a new one. Emit LaTeX as `\lim_{x \to val} expr` using the existing `renderToString` path in `CalculateTab`.  
**Invariants:** None.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The function plotter in `CalculateTab` already renders multiple equations. A constant `c = 5` is the degenerate case of a function `f(x) = 5`. Check whether the parser already handles expressions without `x` (they would return the same value for every sample point and render as a horizontal line). If not, add a type check: if the parsed expression has no `x` dependency, generate `Array(N).fill(value)` as the y-array instead of mapping over x. Label the constant line with its value in the legend (e.g. "y = 5").  
**Invariants:** None.

---

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` currently maintains a flat ordered list. Extend its schema to include an optional `groupId` field per artifact entry. Add a `groups` array to the project-scoped order document stored in IndexedDB. `ModelBufferBar.jsx` would render a group-selector row (small chips: "All", "Group 1", "Group 2", "+ New Group") and filter `pinnedModels` by the selected group before passing them to the comparison table. Group membership assignment can be a drag-target or a right-click context menu on each pinned model chip. Persist groups via `saveArtifactOrder`.  
**Invariants:** Persistence goes through `indexedDB.js`, not `localStorage`.

---

## UX / Design

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** Each pipeline step in `History.jsx` already renders its step type. Map step types to their UI tab using a lookup table (e.g. `filter → "Clean"`, `log → "Feature"`, `group_summarize → "Reshape"`). When the user clicks a step, dispatch a `NAVIGATE_TO_STEP` event (or call a `onNavigateToStep(step)` prop) that `WranglingModule` handles by setting `activeTab` to the correct tab and, where possible, scrolling the matching section into view. For compound tabs (Feature has multiple sub-sections), set a `highlightSection` state that the relevant sub-panel reads to show a brief highlight border.  
**Invariants:** None — pure UI navigation.

---

### General · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`)  
**Suggestion:** `TOUR_STEPS` in `HelpSystem.jsx` documents the 9-step onboarding tour. Recent additions since the last tour update include: N-way FE picker, Sun & Abraham / Callaway & Sant'Anna estimators, Bacon decomposition panel, pipeline collapse toggle, Distinct Values panel, Data Viewer sort, and the 12th PlotBuilder geom (tile). Walk through `TOUR_STEPS` and update any step that references an element that has moved or been renamed. Also check that the 8 `HintBox` panels (Data, Clean, Explore, Model, Report, Calculate, Simulate, Spatial) match the current UI — CLAUDE.md notes they were audited as of 2026-07-30 but several new features have shipped since.  
**Invariants:** The `HintBox` trigger button must always read "Help" and the `title` prop must be the MODULE name (not "How to X").

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`  
**Suggestion:** The DuckDB-Wasm fast path is code-complete for CSV/TSV > 10MB (Phase X4). Confirmed by `CLAUDE.md`: `.dta` files over 10MB also route through DuckDB since 2026-06-10. The outstanding gap is the tab-transition lag (~15s for 900k-row tables) caused by JS object allocation when switching modules. Per the agreed DuckDB performance strategy in `CLAUDE.md`, the next action is OPFS Parquet persistence on first CSV load (auto-convert on import, not just on `.parquet` upload). The `DataStudio.jsx` OPFS path for `.parquet` uploads is already implemented — extend `parseFile`'s CSV branch to call `convertToParquetOPFS()` after the initial DuckDB load and before the PREVIEW_ROWS sample is returned. This eliminates re-parse on every reload.  
**Invariants:** Never pull the full Arrow result into JS objects (DuckDB performance rule #1). No server-side computation (privacy-first constraint).

---

## Other

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables — cross-module feature  
**File:** `src/services/session/sessionState.jsx` (new `LIST_REGISTRY` reducer action), `src/components/wrangling/CleanTab.jsx`, `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** This is a design item requiring a session before implementation. The natural owner is `sessionState.jsx`: a `listRegistry` map from `listName → string[]` accessed via a `useSessionState()` hook. Lists could be populated in the Filter builder ("Save selection as list") or the Distinct Values panel ("Save values as list"). Consuming them in `FilterBuilder` as an `in` / `not in` operator against a named list would unlock `%in% c(...)` style filtering without embedding the literal vector. The R/Python/Stata export should emit the list as a named vector / Python list / local macro at the top of the script. No implementation decision yet — the logged ask is to pick scope with Franco.  
**Invariants:** Session-scoped lists should live in React context (`sessionState.jsx`), not `localStorage` or IndexedDB, so they are naturally scoped to the session and do not appear in the pipeline export.

---

## Suggested action order

1. **Confirm p-value fix (CRITICAL)** — Ask Franco to run a clearly insignificant OLS (t ≈ 0.5) in-browser and verify p ≈ 0.62; also confirm on a DuckDB large-n dataset. Mark BugTriage rows `216b1ecb` as "confirmed" once done.
2. **Stata workbench (HIGH)** — Audit `CodeEditor.jsx` Stata tab and `stataScript.js` for the broken interactive path; likely a `do`-file syntax issue in the pasted block or a missing `reghdfe` preamble.
3. **Simulate replication script (HIGH)** — Add `buildSimulateScript()` exporter covering the Monte Carlo loop, resampling draws, and sample tests — the DGP expression translators in `dgpScript.js` already exist.
4. **Confirm N-way FE / FE interactions (HIGH)** — Franco browser-tests with a non-standard FE dimension (e.g. sector variable); mark BugTriage rows confirmed once done.
5. **Time-series group references bug (MEDIUM)** — Trace the group accessor in `ExplorerModule.jsx`'s time-series section; likely a stale closure on the pre-PlotBuilder code path.
6. **Time-series label controls (MEDIUM)** — Expose `xLabel`/`yLabel`/`title` inputs in the Explore time-series sub-tab and thread to `PlotCanvas`; PlotBuilder already accepts these props.
7. **LaTeX table width overflow (MEDIUM)** — Wrap `tabular` in `\resizebox{\textwidth}{!}{}` in `latexTable.js` when `modelCount > 4`.
8. **FeatureTab draft persistence (MEDIUM)** — Add `sessionStorage`-backed draft state in `FeatureTab.jsx` keyed by `[pid, datasetId, subTab]` so `case_when` edits survive intra-session tab switches.
9. **Guide tour update (LOW)** — Walk through `TOUR_STEPS` and the 8 `HintBox` panels against current UI; update any step referencing a moved or renamed element.
10. **OPFS Parquet auto-conversion for large CSV (PERFORMANCE)** — Extend the CSV `parseFile` branch in `DataStudio.jsx` to call the existing OPFS Parquet cache write path after first DuckDB load, eliminating the re-parse lag on reload.
