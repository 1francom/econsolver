# Feedback Analysis Report — 2026-06-15

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.
> Previous report (2026-06-14) is superseded. The X5 reconciliation audit in ClaudeFB.md (appended 2026-06-14) resolved or explicitly deferred the majority of prior open items. This report covers only what remains open after X5.

## Summary
- **6 open bugs** (code-complete, browser validation pending) · **1 deferred bug** · **5 deferred features** · **0 open UX/Design** · **1 open performance** · **0 open other**
- Total open items analysed: **13**

Cross-reference methodology: items marked "verified fixed" or "wontfix" in the X5 reconciliation table (ClaudeFB.md bottom section) are excluded. Items marked "fix-now" are code-complete but still unconfirmed in the browser — they remain open from the user's perspective. Items marked "fix-later" and "routed" are included.

---

## Bugs

### [high] · Model · 2026-06-11 · awaiting browser validation
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6`. The fix freezes OLS/WLS diagnostic display to the estimation's frozen `result.spec` so that sidebar edits cannot mutate the already-displayed plots. Browser validation checklist: (a) run an OLS estimation, confirm Y~Yhat scatter, Residual vs Fitted, and Q-Q plot all render; (b) add a new regressor and confirm the displayed diagnostics do not change; (c) pin a model to the buffer and restore it — confirm the plots reappear correctly. If any of these fail, root-cause will be in `ModelingTab.jsx` at the point where `ResidualPlots` receives its `result` prop.
**Invariants:** Diagnostic plots must read only from the frozen estimation result (`result.spec`), not from live sidebar state. `src/math/LinearEngine.js` must remain React-free — any sampling of residuals for large-n DuckDB results must happen in `ModelingTab.jsx`, not the engine.

---

### [high] · Model · 2026-06-14 · awaiting browser validation
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Code-complete in the same commit `35f4b5a6` as the OLS diagnostic fix — same root cause (live sidebar state leaking into displayed result). Validate together with the Y~Yhat browser session above. Specific check: after estimation, toggle a variable checkbox in the variable selector, and verify the coefficient bar chart does not redraw. Also restore a pinned model from ModelBufferBar and confirm it re-renders correctly without mixing in the current sidebar variables.
**Invariants:** Same as the OLS residual fix — read from `result.spec` (immutable), not from live `xVars`/`yVar` state.

---

### [high] · Model · 2026-06-14 · awaiting browser validation
**Feedback:** 2SLS Poisson interpretation wrong — AI narrative interprets IV-Poisson coefficients as linear 2SLS effects instead of incidence-rate ratios.
**File:** `src/services/AI/Prompts/index.js`
**Suggestion:** Code-complete in commit `bc2d7b9d`. `IVPoisson` now follows the Poisson IRR rule in `INTERPRET_REGRESSION_PROMPT` and separately discusses instrument relevance/exclusion. Browser validation: estimate an IV-Poisson model (treatment instrument + count outcome), click "Interpret results," and confirm the narrative says something like "a one-unit increase in X is associated with a factor of exp(β) change in the expected count" (incidence rate ratio) rather than "β additional units of Y per unit of X." Also confirm instrument discussion is present.
**Invariants:** All Anthropic API calls must go through `src/services/AI/AIService.js` only — `Prompts/index.js` holds prompt text without API calls; this invariant is not at risk here.

---

### [medium] · Model · 2026-06-10 · awaiting browser validation
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` — the plot viewport now has native vertical resize with bounded responsive canvas sizing so PNG captures the user-selected dimensions. Browser validation: drag the resize handle to a non-default height; export PNG; confirm the exported image matches the in-browser view (correct aspect ratio, no clipped axes, no dark/light theme bleed). Also test the SVG export path.
**Invariants:** Inline styles only via the `C` object from `shared.jsx` — no external CSS files. Verify the resize handle uses inline style updates and not a CSS class.

---

### [medium] · Explore · 2026-05-11 · awaiting browser validation
**Feedback:** The graph does not fit the screen.
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** Grouped with the Plot Builder resize fix in commit `35f4b5a6` — the same responsive-canvas change covers both the Explore and Modeling contexts. Browser validation: open a dataset in the Explore tab, build a scatter or histogram, and confirm the chart fills the available container width without horizontal overflow or whitespace at narrow viewport widths. Test at 1280 px and 1440 px screen widths.
**Invariants:** Layout fix only — no math changes.

---

### [medium] · Spatial · 2026-06-13 · awaiting browser validation
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete in commit `5e105529` — Heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5 k-point cap. Browser validation checklist: (a) load a point dataset (lat/lon columns); (b) open the Map sub-tab in Spatial, add a Heatmap layer via SpatialLayerEditor; (c) confirm KDE heat overlay renders on the Leaflet map; (d) adjust palette and opacity sliders — confirm live update; (e) click HTML export and confirm the heatmap is captured; (f) test with a dataset larger than 5 k rows and confirm it does not crash (5 k cap applied).
**Invariants:** The Map sub-tab (`src/components/tabs/spatial/map/`) is self-contained with Leaflet — inline styles via `C` only.

---

### [medium] · Data · 2026-05-10 · deferred (fix-later)
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47… — import aborts on the first malformed row.
**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/duckdb.js`
**Suggestion:** DuckDB's strict CSV parser aborts on the first error. In `DataStudio.jsx`, wrap the DuckDB `read_csv_auto` call in a try/catch; on failure, retry with `IGNORE_ERRORS=TRUE` to load the clean rows. Surface a recoverable-import warning banner: "N rows could not be parsed — [show details]" listing the first few offending line numbers and raw text. Never display raw DuckDB error strings to the user. Store the skip report in the pipeline audit trail via `auditor.js`. As a pre-DuckDB alternative, PapaParse already handles thousands-separator commas and BOM artifacts that confuse DuckDB — consider routing CSV ≤ 10 MB through PapaParse first and DuckDB only for larger files.
**Invariants:** Retry logic stays in `DataStudio.jsx` (UI/parse boundary) — not inside `duckdb.js`. Full recovered dataset must still reach `runner.js`/`duckdbRunner.js`.

---

## Features

### Model · 2026-05-25 · deferred (fix-later)
**Feedback:** Add an option to estimate without intercept.
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`, `src/math/CausalEngine.js`, `src/math/NonLinearEngine.js`
**Suggestion:** Add an optional `noIntercept: boolean` field to the model `spec`. In `LinearEngine.js` `runOLS` / `runWLS`, gate the prepended ones column on `spec.noIntercept !== true`. Adjust df = n − k (not n − k − 1) and switch R² to the raw (uncentered) formula when the intercept is absent — matching `lm(y ~ x - 1)` in R. Expose as a "Suppress constant (−1)" checkbox in `ModelConfiguration.jsx`, threaded through `ModelingTab.estimate()` into the engine call. Add a footnote in the result display noting that uncentered R² is not comparable to standard R². All math engines that support the intercept (`PanelEngine`, `CausalEngine`, `NonLinearEngine`) should receive and respect the same flag.
**Invariants:** `seType` must still be passed explicitly to engines — the no-intercept flag is a separate spec field. All math files in `src/math/` stay React-free.

---

### Clean · 2026-05-19 · deferred (fix-later)
**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option.
**File:** `src/components/wrangling/CleanTab.jsx`, `src/services/exprEvalService.js`, `src/workers/exprEval.worker.js`, `src/pipeline/runner.js`
**Suggestion:** Extend the existing `filter` step with an `in` operator. Step payload: `{ type: "filter", col: "regionname", op: "in", values: ["Spain (Espana)", "Basque Country (Pais Vasco)"] }`. In `runner.js` filter branch, add `case "in": return values.map(String).includes(String(row[col]))` (and `case "not_in"` for the inverse). In `CleanTab.jsx`'s FilterBuilder, add an "is one of" operator with a tag-input chip control (comma-separated, add-on-Enter). This avoids requiring users to type `%in%` syntax at all, while generating equivalent JS. No new step type — only `runner.js` filter case and `CleanTab.jsx` UI need updating; `registry.js` inline comment should document the new operators.
**Invariants:** Pipeline steps must replay on `rawData` — never mutate in place. Registry must stay in sync with runner.js at all times.

---

### Model (RDD) · 2026-05-21 · deferred (fix-later)
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — showing kernel, bandwidth, effective N, LATE, SE, CI.
**File:** `src/math/CausalEngine.js` (lines 577–579, hardcoded `varNames`), `src/components/modeling/results/` (alongside existing `FuzzyRDDResults.jsx`, `IVResults.jsx`)
**Suggestion:** Create `src/components/modeling/results/RDDResults.jsx` modelled on `FuzzyRDDResults.jsx`. The table should surface: method, polynomial order, kernel, bandwidth h, N left / N right of cutoff, effective N, LATE (point estimate), bias-corrected estimate (if available), conventional SE, robust SE, t-stat, p-value, and 95% CI. All fields are already returned by `CausalEngine.runSharpRDD` — this is purely a display addition. Simultaneously fix `CausalEngine.js` lines 577–579 to use the actual `runningVar` and `treatCol` arguments (pass them into `runSharpRDD` as parameters) instead of the hardcoded "D (treatment)" / "running − c" strings, so the table displays real column names.
**Invariants:** `src/math/CausalEngine.js` must remain React-free — column names are passed as function arguments, not pulled from React state. All display logic goes in the new results component. Inline styles via `C` from `src/components/modeling/shared.jsx`.

---

### Spatial · 2026-05-30 · deferred (fix-later, V4)
**Feedback:** Allow spatial filters for plots.
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`
**Suggestion:** Add a collapsible "Spatial filter" section in `GeoLayerConfig.jsx` per layer with: (a) bounding-box min/max lat/lon inputs; (b) an optional WKT polygon field for arbitrary region filters. In `GeoPlotCanvas.jsx`, before rendering a layer, filter its rows to those whose coordinates fall inside the bbox/polygon using `SpatialEngine.js`'s existing point-in-polygon utilities. This is a display-time filter only — it does not emit a pipeline step and does not modify `rawData`. The filter state lives entirely in the layer config object within `SpatialGeoPlot`'s local state.
**Invariants:** The non-destructive pipeline invariant must not be violated — spatial plot filters must never call `applyStep` on `rawData`. `SpatialEngine.js` stays React-free. Inline styles via `C`.

---

### Model · 2026-06-06 · deferred (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D × (running − c)" regardless of actual column names.
**File:** `src/math/CausalEngine.js` (lines 577–579), `src/components/modeling/resultDisplay.jsx`, `src/services/export/rScript.js`, `src/ReportingModule.jsx`
**Suggestion:** Two-part fix. (1) In `CausalEngine.js` lines 577–579, replace hardcoded `varNames` with the actual column names passed as function arguments to `runSharpRDD` (e.g. `polyNames[0] = runningVar + " − cutoff"`). (2) In `resultDisplay.jsx`, add an editable label map (`{ [originalName]: displayName }`) to local state, pre-filled from `result.coefs` keys. Render each coefficient row label as a click-to-edit inline `<input>` (blur-to-save). Pass the label map into `buildStargazer` in `ReportingModule.jsx` and the R/Python/Stata export scripts in `rScript.js` etc. to use display names in output. Persist the label map per model in the `model_buffer` IDB store alongside the trimmed result.
**Invariants:** Label edits are presentation metadata only — never mutate `EstimationResult.coefs` keys or any field in `src/math/`. `CausalEngine.js` stays React-free. Persist via `indexedDB.js`, never localStorage.

---

## Performance

### [high] · Data · 2026-05-08 · routed to Fase X4 (browser benchmark pending)
**Feedback:** Big datasets generate crashes (21 MB) over 900 k rows.
**File:** `src/DataStudio.jsx` (parseFile, all format branches), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** The 2026-06-10 fix routed large `.dta` files through DuckDB + OPFS Parquet, but large CSV/TSV uploads — the primary large-data format — still materialise the full JS object array, causing OOM crashes. Immediate action: apply the same 10 MB threshold check to CSV/TSV in `DataStudio.jsx` parseFile: register in DuckDB, emit Arrow-backed preview (500 rows), cache as Parquet to OPFS. No browser benchmark with a 900 k-row CSV has been confirmed. Once done, audit `duckdbRunner.js` step coverage: of the 53 step types in `runner.js`, only ~11 have SQL fast paths; step types `add_column`, `filter`, `normalize_cats`, `group_transform`, and `pivot_longer` are the highest-volume fallbacks still materialising full JS arrays. Also verify `vercel.json` has `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` — without these headers, `SharedArrayBuffer` is unavailable and DuckDB-Wasm runs single-threaded.
**Invariants:** Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js` — the display-limit (500 rows) is enforced at the rendering layer only (CLAUDE.md DuckDB rule 5). No server-side computation — privacy-first. OPFS caching per CLAUDE.md rule 3.

---

## Suggested action order

1. **Browser-validate commit `35f4b5a6` (one session covers 4 items)** — OLS residual/Q-Q plots, diagnostics freeze on variable-selector change, Plot Builder resize + PNG export, and Explore graph fit. Mark each validated or file a targeted regression against the specific check that fails.
2. **Browser-validate IV-Poisson AI interpretation fix (`bc2d7b9d`)** — estimate an IV-Poisson model and confirm the narrative uses incidence rate ratio framing, not linear marginal effects.
3. **Browser-validate spatial heatmap (`5e105529`)** — load a point dataset, add Heatmap layer, confirm KDE overlay + palette/opacity + HTML export all function; test the 5 k-point cap with a large dataset.
4. **900 k-row CSV browser benchmark for Fase X4** — apply the `.dta` DuckDB path to CSV in `DataStudio.jsx`; measure cold-start and OPFS-warm tab-transition times; confirm no full-table JS materialisation in the wrangling preview path.
5. **Resilient CSV import (malformed-row recovery)** — `DataStudio.jsx`: retry `read_csv_auto` with `IGNORE_ERRORS=TRUE` on failure; surface a row-skip warning with line numbers; post-launch priority.
6. **No-intercept estimation toggle** — `LinearEngine.js` + `ModelConfiguration.jsx`; adjust df and R² conventions; thread `noIntercept` spec flag through all engine entry points. Requires engine-wide change — schedule as a focused sprint.
7. **R `%in% c(...)` filter in CleanTab** — extend `runner.js` filter with `in`/`not_in` operators; add tag-input chip control to `CleanTab.jsx` FilterBuilder; low complexity, high UX value for panel-data workflows.
