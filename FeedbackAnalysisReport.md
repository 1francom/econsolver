# Feedback Analysis Report — 2026-09-11

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **11 open bugs** · **10 open features** · **1 open UX / Design** · **1 open performance** · **1 open other**
- **2 items marked "likely fixed — needs Franco's in-browser confirmation"** (p-values ×3, N-way FE ×2)
- **2 feature requests likely already implemented** (Limits in CalculateTab, plot constants via workbench refLines) — verify with Franco before building
- **3 items code-complete but browser validation still pending** (PlotBuilder resize, residual plots, spatial heatmap — carried from previous report; add to Franco's next browser-test pass)

---

## Bugs

### [critical — needs confirmation] · Model · 2026-07-22 / 2026-07-28 (×3 duplicates)
**Feedback:** P-values with small numbers — beta=0.0001 and se=0.002 can never give p<0.001 with ***  
**File:** `src/math/LinearEngine.js` (`tCDF`/`betainc`, lines ~120–180); also `src/math/calcEngine.js` (`_incompleteBeta`)  
**Suggestion:** Fix was committed as `216b1ecb` (2026-07-28) replacing the Riemann-sum with Numerical Recipes' continued-fraction `betainc`; a second independent copy in `calcEngine.js` was fixed 2026-07-30. **Action needed:** Franco must confirm in-browser on a clearly insignificant OLS coefficient (t ≈ 0.5, expect p ≈ 0.62) AND on a DuckDB large-n dataset (n > 50k, where the old Riemann sum broke most badly). If still wrong after `216b1ecb`, check whether `EstimationResult.js` caches a stale pre-fix p-value and the cache is not invalidated on the commit.  
**Invariants:** No React in `src/math/`. All three p-value paths (`LinearEngine.tCDF`, `calcEngine.pt`, `duckdbDispatch` suff-stats result) must agree.

---

### [high] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx` (line 84 dispatches `generateStataScript`); `src/services/export/stataScript.js`  
**Suggestion:** The Stata tab in `CodeEditor.jsx` calls `generateStataScript(config)`. Likely causes: (1) emitted `do`-file block lacks required Stata preamble directives (`version 17`, `clear all`, `set more off`) that Stata needs to execute a pasted snippet without hanging; (2) `reghdfe` is referenced for two-way cluster SE but no `ssc install reghdfe` guard or package-check comment is emitted; (3) `xtreg y x, fe vce(cluster a b)` is a Stata syntax error (two-cluster-var form) — the `pVceHdfe` mapper was fixed to route this to `reghdfe` but confirm the output block assembles cleanly. Audit using the 168-cell SE-type matrix (`seTolerances.js`) and run each generated block through `stata -b do <file>.do` in the CI-equivalent test path.  
**Invariants:** All Anthropic calls must go through `AIService.js` only — do not add an AI-assisted Stata eval path elsewhere.

---

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP  
**File:** `src/components/tabs/SimulateTab.jsx` (line 760: `scriptOpen` state, line 1294: `generateSimScript` call); `src/math/dgpScript.js`  
**Suggestion:** `SimulateTab.jsx`'s `generateSimScript` (line 1294) receives only `(lang, n, seed, variables)` — it has no awareness of the Monte Carlo loop count `B`, the resampling config, or any sample test selections. The downstream analysis lives in component state but is never passed to the exporter. Add a `buildSimulateScript({ dgpConfig, mcConfig, resamplingConfig, testConfig })` function in `src/services/export/simulateScript.js`, following the existing `dgpScript.js` expression translators as the source. R output should wrap the DGP in a `replicate(B, { ... })` loop; Python should use a list comprehension or `pd.DataFrame([... for _ in range(B)])`. Stata output can emit a `forvalues` loop with a note on the stylistic difference. Wire it to the "Replication script" collapsible at line ~1280.  
**Invariants:** `src/math/dgpDraw.js` is pure JS with no React — the script generator must not import it. `dgpScript.js` exports the R/Python/Stata expression translators — use those, not a new copy.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work  
**File:** `src/ExplorerModule.jsx` (lines 1480–1502: `jsSeries`/`sqlSeries` aggregation; lines 1524: `colorFor` function)  
**Suggestion:** `TimeSeriesTab` uses `grpCol` state (line 1455) to aggregate, and `colorFor` maps each group to a color (line 1524). The bug is almost certainly that the group SQL aggregation (`fetchAggregateTimeSeriesSQL`, line 1498) passes `grpCol` to the SQL query but the resulting series objects' `.grp` key may not match the actual column values when `grpCol` changes after the initial render — the `colorFor` index is built from `series` which is memoised from the prior SQL result. Force a `sqlSeries` reset (`setSqlSeries(null)`) synchronously when `grpCol` changes before the async `fetchAggregateTimeSeriesSQL` resolves, so the legend never shows stale group labels. Also check that the JS fallback `jsSeries` (line 1480) and SQL result (line 1499) use the same group key format — type coercion between a SQL string and a JS number label can cause `colorFor(s.grp, si)` to miss its map entry.  
**Invariants:** None — pure UI/data change.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/ExplorerModule.jsx` (lines 1739–1746: axis label rendering, currently hardcoded to `tCol` and `agg(yCol)`)  
**Suggestion:** The time-series SVG at lines 1739–1746 already draws axis labels — they are hardcoded column names. Add three optional text inputs (`chartTitle`, `xAxisLabel`, `yAxisLabel`) in local state above the graph render and thread them as overrides: `{xAxisLabel || tCol}` and `{yAxisLabel || \`${agg}(${yCol})\`}`. Add `<text>` for `chartTitle` at `(W/2, 14)` matching the pattern in `SvgHistogram` (line 305). The pin params object (`onPin(...)`, line 1578) should carry `{chartTitle, xAxisLabel, yAxisLabel}` for restore. The R/Python/Stata export preamble in `plotScript.js` already handles `labs(x=, y=, title=)` — reuse it via the existing `scriptPreamble` seam added 2026-07-30.  
**Invariants:** None — pure UI and export change.

---

### [medium] · General · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js` (line 173: `\begin{tabular}{colFmt}`)  
**Suggestion:** Line 173 emits a plain `\begin{tabular}` with no width guard. When the number of models exceeds ~5 the table overflows `\textwidth`. Wrap the `tabular` in `\resizebox{\textwidth}{!}{ ... }` (requires `graphicx`) when `modelCount > 4`. The `graphicx` package is standard in APA/AER/NBER templates — add a `% requires \\usepackage{graphicx}` comment at the top of the emitted block. Alternatively emit `\begin{adjustbox}{max width=\textwidth}` which adapts without distorting fonts. Expose a `wrapMode: "resizebox" | "adjustbox" | "none"` param (default `"resizebox"`) so callers can override for journal templates that forbid shrinkage.  
**Invariants:** None — export-only change.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`; `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `CoefficientTestPanel.jsx` runs one test at a time with no persistence. Add a `savedHypotheses` array — stored in IndexedDB via `indexedDB.js` under key `hypotheses_<modelId>` in the existing `pipelines` store — so tests accumulate per pinned model. Each entry stores `{ label, type, constraints, nullVals, stat, p, preferred }`. The "Copy code" button should emit all saved tests in one block: `car::linearHypothesis(fit, list(c1, c2))` in R (runs multiple in one call), a loop over `fit.wald_test(constraint)` in Python. The generation logic already lives in `generateJointHypothesisScript()` added 2026-07-30 — extend it to accept an array of entries and emit them sequentially.  
**Invariants:** Use `indexedDB.js` for persistence, not `localStorage`.

---

### [medium] · Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`, lines ~99, 337, 364–365)  
**Suggestion:** The prompt already handles log-log and log-level forms but requires the user to know the convention. Add a detection heuristic in `INTERPRET_REGRESSION_PROMPT`: if `result.yVar` starts with `ln_`/`log_`/`l_` AND at least one X var also starts with `ln_`/`log_`/`l_`, the model is log-log and the AI should **lead** with "This is a log-log specification; β is an elasticity — a 1% increase in X implies β% change in Y." If only Y is log-transformed, flag it as log-level (semi-elasticity). The AI context already receives `result.varNames` and `result.yVar` via the serialized snapshot — no engine change needed. Also add a rule for `result.estimatorType === "Poisson"` to explicitly call out IRR alongside percent-change interpretation since users are misreading Poisson output as linear.  
**Invariants:** All Anthropic calls route through `AIService.js` only. Prompt changes go in `src/services/AI/Prompts/index.js` only.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean, coming back to continue  
**File:** `src/components/wrangling/FeatureTab.jsx` (no draft persistence found — confirmed no `sessionStorage` / `localStorage` usage beyond the security guard at line 97)  
**Suggestion:** Add a `draftState` object keyed by `[pid]_[datasetId]_[subTab]` in `sessionStorage` (not IndexedDB — this is ephemeral UI state, not pipeline state). Read it on component mount and write on every `case_when` / `if_else` / `mutate` field change. Use the `pid` and current dataset id from `useSessionState()` to scope the key so drafts never bleed across datasets. Wrap every `sessionStorage` access in `try/catch` per the platform's storage-availability rule. A draft is intentionally NOT restored on full page reload (IndexedDB is not used), but it survives intra-session tab switches, which is the reported failure mode.  
**Invariants:** Pipeline step commits go to IndexedDB only. `sessionStorage` is allowed for per-session UI state per CLAUDE.md's working conventions.

---

### [medium] · General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js`; `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` maintains a flat ordered list under key `artifactOrder_<pid>` in IndexedDB. Extend the stored document schema with an optional `groups: [{ id, name }]` array and add an optional `groupId` field on each entry in `order`. In `ModelBufferBar.jsx`, render a group-filter chip row ("All", group names, "+ New Group") and filter `pinnedModels` by the selected group before passing to the comparison table. `saveArtifactOrder` already takes the full order object — no new IndexedDB API needed, just a schema extension. Group assignment can be a context menu or drag-target on each pinned model chip. Default: all artifacts have `groupId: null` → appear in "All".  
**Invariants:** Persistence goes through `indexedDB.js`, not `localStorage`.

---

### [low] · General · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` starting at line 11)  
**Suggestion:** `TOUR_STEPS` is a 9-step array. Features shipped since the last documented audit (2026-07-30) include: N-way FE picker, Sun & Abraham / Callaway & Sant'Anna estimators in the sidebar, Bacon decomposition panel under TWFE, pipeline collapse toggle (⟩ rail), Distinct Values panel in Clean → Formatting, Data Viewer column sort (⇅ headers), and the 12th PlotBuilder geom (`tile`). Walk through each step, check that referenced element names and UI locations still match the current DOM, and update any step that describes a removed or renamed control. Also audit the 8 `HintBox` copy panels (Data in `App.jsx`, Clean in `WranglingModule.jsx`, Explore in `ExplorerModule.jsx`, Model in `ModelingTab.jsx`, Report in `ReportingModule.jsx`, plus `CalculateTab`/`SimulateTab`/`SpatialTab`) — CLAUDE.md records they were audited as of 2026-07-30 but multiple features have shipped since.  
**Invariants:** `HintBox` trigger always reads "Help"; `title` prop is the MODULE name (not "How to X"). Never write a count into help copy.

---

### [medium — needs confirmation] · Model · 2026-07-09
**Feedback:** Add options for fixed effects — not just entity and time, but also variables like country, state, sector  
**File:** `src/math/PanelEngine.js` (`runFEMulti`); `src/components/modeling/ModelConfiguration.jsx`  
**Suggestion:** N-way FE shipped in `#104` (`045f5cda`) — `CLAUDE.md` confirms 18/18 checks pass vs R fixest for 2-way and 3-way FE. **Action needed:** Franco must confirm in-browser by declaring a non-standard FE dimension (e.g. a `sector` column) in PanelTab and verifying (1) the FE column picker in `ModelConfiguration.jsx` accepts it, (2) the result table shows the right df, and (3) the replication code emits `feols(y ~ x | fe1 + fe2)` with the actual column names. FE interactions shipped in the same PR — same confirmation needed. Mark BugTriage rows as "confirmed" once done.  
**Invariants:** None.

---

## Features

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/components/ModelingTab.jsx`; `src/math/CausalEngine.js` (`runSharpRDD`); `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** `runSharpRDD` already returns `bw` (bandwidth), `nLeft`, `nRight`, `coef`, `se`, and CI bounds. Render a compact `rdrobust`-style summary table as a collapsible `<Section>` **above** the coefficient table in the RDD results panel — columns: "Kernel", "BW", "Eff. Obs. (L)", "Eff. Obs. (R)", "Estimate", "SE", "95% CI". No engine change needed — all values exist. Reuse the `C`-object styling from `shared.jsx`; match the `mono` typeface already used in the results table.  
**Invariants:** None — UI-only component. No React in `src/math/`.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names  
**File:** `src/services/export/latexTable.js`; `src/ReportingModule.jsx` (lines ~340–360: `buildStargazer` call)  
**Suggestion:** `buildStargazer` emits row labels from `result.varNames`. Add an optional `varLabels` map (column → display label) to the `buildStargazer` signature. In `ReportingModule.jsx`, expose a small editable table: one row per coefficient, a text input for the display label, prefilled with the column name. Store `varLabels` in component state; pass it into `buildStargazer`. RDD-specific labels ("D (treatment)", "running − c") are currently hardcoded in the RDD result's `varNames` — consider whether they should remain as defaults that the user overrides rather than removing them outright, since they carry semantic meaning.  
**Invariants:** None — export and UI-only change.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/plot/geo.js`; `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**Suggestion:** The SVG geo-plot tab (`SpatialGeoPlot.jsx`) already renders polygon and point layers via Observable Plot. Add a `"grid"` layer type in `geo.js`'s `mkGeoLayer` that reads the `gridId` / H3 column produced by `GridSection.jsx`'s pipeline step and renders filled rectangles/hex cells using `Plt.geo` with a GeoJSON FeatureCollection assembled from the WKT ring strings (reuse `wkt.js`'s `leafletPolygonLatLngs` for the coordinate array). Extend `GeoLayerConfig.jsx`'s layer type picker with a "Grid" option alongside Point/Polygon. Use `buildColorScale` from `color.js` for choropleth fills; wire to the existing `aes.color` field.  
**Invariants:** `src/math/SpatialEngine.js` is pure JS — no React. Grid geometry helpers stay there or in `wkt.js`.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx` (line 55: component definition; the column list is rendered as `<Chip>` buttons without a filter)  
**Suggestion:** Add a local-state `filter` string (`useState("")`) and a text `<input>` above the variable chip list with `placeholder="Search variables…"`. Filter the rendered chips with `headers.filter(h => h.toLowerCase().includes(filter.toLowerCase()))`. Clear `filter` via a `useEffect` when the dataset changes (the `headers` prop reference changes). This requires zero prop changes — it is entirely internal. For accessibility, give the input `aria-label="Filter variables"` and auto-focus it when the panel opens (using `useEffect` + `inputRef.current?.focus()`).  
**Invariants:** None — pure UI, no persistence needed.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`; `src/services/export/plotScript.js`  
**Suggestion:** When `aes.color` is set, the Observable Plot legend shows raw value strings with no user-controlled title. Add a "Legend title" text input in the Labels panel (which already has `xLabel`/`yLabel`/`title`) that passes to `Plot.plot({ color: { legend: true, label: legendTitle } })`. Also add a "Show value labels on marks" toggle for point/line geoms that appends `Plot.text(rows, { x, y, text: aes.color, fill: colorAccessor, fontSize: 9 })` — equivalent to ggplot's `geom_label`. Wire `legendTitle` into the R export as `labs(color = legendTitle)` and into Python as `legend_title=` on the seaborn/matplotlib call. `plotScript.js` already has the `labs()` mechanism for `xLabel`/`yLabel` — extend it.  
**Invariants:** None.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI  
**File:** `src/components/ModelingTab.jsx`; `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** Extend the `ModelPlots` binscatter panel (currently rendered for OLS/RDD) with user-controlled parameters: number of bins (`J`), polynomial order for the mean line (`polyreg`), confidence-band toggle, and x-axis range override (min/max). Store these in a `binscatterOpts` state object in `ModelingTab` (same pattern as `rddOpts`). The replication script should emit `binsreg::binsreg(y, x, data=df, nbins=J, polyreg=p)` in R and the equivalent `pystata`-style call. When the dataset is DuckDB-backed, ensure binscatter counts are computed via SQL aggregation in `duckdb.js`, not over the 500-row preview.  
**Invariants:** Engines in `src/math/` must remain pure JS with no React imports.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx`; `src/pipeline/runner.js`; `src/pipeline/registry.js`  
**Suggestion:** Add a new pipeline step type `country_code` in `runner.js`/`registry.js` (following the `pipeline-step-adder` checklist). The step accepts `{ col, from: "name"|"iso2"|"iso3", to: "name"|"iso2"|"iso3", outputCol }`. The lookup table is a static ~250-entry JSON array embedded in `runner.js` (avoiding an async fetch) covering ISO 3166-1 alpha-2, alpha-3, and the standard English name. The UI is a new card in `FeatureTab.jsx` under the Formatting section. The R export emits `countrycode::countrycode(df$col, origin="country.name", destination="iso3c")`; Python emits `pycountry` lookups; Stata emits `kountry` with a note. **Note from ClaudeFB.md:** scope must be confirmed with Franco before building — fuzzy country-name matching is a larger scope than a crosswalk.  
**Invariants:** Step must replay on `rawData`, never mutate in place. Registry must stay in sync with `runner.js`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/calculate/workbench/ViewControls.jsx` (lines 30–39: `refLines` array); `src/components/calculate/workbench/WorkbenchCanvas.jsx`  
**Suggestion:** ⚠️ **This may already be implemented.** `workbenchStore.js` (line 47) defines `refLines: []` in the session view, and `ViewControls.jsx` (lines 30–39) already manages horizontal (`kind: "h"`) and vertical (`kind: "v"`) reference lines with a `value` and `label` field. `WorkbenchCanvas.jsx` likely draws them. **Action needed:** Franco should confirm whether the "Add reference line" UI is accessible in the current workbench and whether horizontal constants appear on the canvas. If the UI panel is hidden or collapsed by default, add a visible affordance (e.g. a "+ H-line" / "+ V-line" button in the graph controls).  
**Invariants:** None.

---

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx` (line 1595: `"limit","lim Limit"` is already listed as a tool tab)  
**Suggestion:** ⚠️ **This is already implemented.** `CalculateTab.jsx` line 1595 shows `["limit","lim Limit"]` in the tool selector, and line 2170 renders the limit UI. Lines 1420–1423 call `limit(wrappedFn, a)` from the CAS import. **Action needed:** Franco should confirm in-browser that the Limit tab renders and computes correctly (it may have been added but is not discoverable, or the CAS import at line 18 may not export `limit`). If it works, update the `HintBox` copy in `CalculateTab.jsx` to mention it (line 1574 only lists 5 tools by name but not `limit`).  
**Invariants:** None.

---

### General · 2026-07-17
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
**File:** `src/services/session/sessionState.jsx`; `src/components/wrangling/CleanTab.jsx`; `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** This is a design question — logged per ClaudeFB.md. The natural owner is `sessionState.jsx`: a `listRegistry` map from `listName → string[]` in the `SessionStateProvider`. Lists could be populated from the Distinct Values panel ("Save as list") or the Filter builder ("Save selection as list"). Consuming them in `FilterBuilder` as an `in`/`not in` operator against a named list (`filter(country %in% lists$eu_countries)`) would unlock the `%in% c(...)` use case. R export emits a named vector at the top of the script; Python emits a Python list; Stata emits a local macro. **No implementation yet — pick scope with Franco before starting.**  
**Invariants:** Session-scoped lists should live in React context (`sessionState.jsx`), not `localStorage` or IndexedDB, so they are naturally scoped to the session.

---

## UX / Design

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step  
**File:** `src/components/wrangling/History.jsx`; `src/WranglingModule.jsx`  
**Suggestion:** Add a lookup table in `History.jsx` mapping step types to their UI tab: `filter → "clean"`, `log/sq/std/dummy/lag/... → "feature"`, `group_summarize/pivot_longer/... → "reshape"`, `join/append → "merge"`. When a user clicks a step in the history rail, call a `onNavigateToStep(step)` prop that `WranglingModule` handles by setting `activeTab` to the correct tab. For compound tabs (Feature has multiple sub-sections), also set a `highlightSection` state — the target sub-section reads it and renders a brief gold highlight border for 2–3 seconds (`useEffect` timeout). This is pure navigation wiring with no pipeline mutation.  
**Invariants:** None.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` (`parseFile` function, CSV branch); `src/services/data/duckdb.js` (OPFS Parquet write path)  
**Suggestion:** The DuckDB fast path is code-complete for CSV/TSV > 10MB (Phase X4, confirmed in CLAUDE.md). The residual crash/lag (tab transition ~15s) is caused by JS object allocation when `rawData` rows are materialised for display. The next step is OPFS Parquet auto-conversion: after the first DuckDB CSV load, call the existing `convertToParquetOPFS()` path (already wired for `.parquet` uploads) so subsequent reloads skip re-parse entirely. In `DataStudio.jsx`'s CSV branch of `parseFile`, call the OPFS write AFTER returning the 500-row PREVIEW_ROWS sample to the UI — this keeps the first-load instant while the background write completes. Also check whether the 2026-07-25 CSP fix (`extensions.duckdb.org` in `connect-src`) is deployed on Vercel; a blocked Parquet extension silently prevents OPFS writes.  
**Invariants:** Never pull the full Arrow result into JS objects (DuckDB performance rule #1). No server-side computation (privacy-first).

---

## Other

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
*(See the Features section above for this item — it is a design-first ask, not yet scoped for implementation.)*

---

## Suggested action order

1. **Confirm p-value fix (CRITICAL)** — Franco runs an insignificant OLS (t ≈ 0.5) in-browser and verifies p ≈ 0.62; repeat on a DuckDB large-n dataset. Mark BugTriage `216b1ecb` rows "confirmed" once done.
2. **Stata workbench (HIGH)** — Audit `CodeEditor.jsx` Stata output and `stataScript.js`: check for missing `version`/`clear all` preamble, missing `reghdfe` install note, and confirm the two-cluster-variable routing fix assembles valid Stata syntax.
3. **Simulate replication script (HIGH)** — Add `buildSimulateScript()` in `src/services/export/simulateScript.js` covering the Monte Carlo loop, resampling, and sample tests; use `dgpScript.js`'s existing expression translators.
4. **Confirm N-way FE / FE interactions (HIGH)** — Franco browser-tests with a non-standard FE dimension (e.g. `sector`); marks BugTriage rows confirmed.
5. **Time-series group references bug (MEDIUM)** — In `ExplorerModule.jsx`, force `setSqlSeries(null)` synchronously on `grpCol` change; check group key type coercion between SQL and JS in the `colorFor` fallback.
6. **Time-series label controls (MEDIUM)** — Add `chartTitle`/`xAxisLabel`/`yAxisLabel` text inputs in the Explore time-series sub-tab; thread through to the SVG renderer at lines 1739–1746.
7. **LaTeX table width overflow (MEDIUM)** — Wrap `\begin{tabular}` in `\resizebox{\textwidth}{!}{}` in `latexTable.js:173` when model count > 4.
8. **FeatureTab draft persistence (MEDIUM)** — `sessionStorage`-backed draft state in `FeatureTab.jsx` keyed by `[pid]_[datasetId]_[subTab]`; confirmed no existing persistence found.
9. **Verify "Limits" and "Plot constants" are already live (QUICK CHECK)** — Both features appear to already be implemented (CalculateTab line 1595 and workbench `refLines`). Franco confirms in-browser; close both BugTriage items if confirmed.
10. **OPFS Parquet auto-conversion for large CSV (PERFORMANCE)** — Extend the CSV `parseFile` branch to call the existing OPFS Parquet write path after first DuckDB load; also verify the `extensions.duckdb.org` CSP fix is deployed.
