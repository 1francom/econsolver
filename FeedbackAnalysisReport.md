# Feedback Analysis Report — 2026-06-27

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **6 open bugs** · **1 open performance** · **5 open UX / design** · **1 open feature** · **0 open other**
- 4 of the 6 bugs are code-complete pending Franco's browser validation (commits `35f4b5a6` / `5e105529`).
- 1 new critical bug added today (`.rds` gzip pre-validation).
- Pipeline-only-if-dataset-selected bug confirmed fixed 2026-06-25 — removed from open list.

---

## Bugs

### [high] · Data · 2026-06-26 (×2 files)
**Feedback:** `Parse error: crime_panel_final_r_300.rds — not a valid R data file (.rds), unrecognised serialisation header.` (same error for `grid_enriched_500.rds`)
**File:** `src/DataStudio.jsx:371`
**Suggestion:** The `validateFileMagic` function at line 371 checks `.rds` magic bytes against only `0x58 0x0A` (XDR), `0x41 0x0A` (ASCII), and `0x42 0x0A` (native binary). R's default `saveRDS()` call uses `compress=TRUE`, producing gzip output whose magic is `0x1F 0x8B`. This guard fires before `parseRDS` is even called, so gzip-compressed .rds files are rejected unconditionally — even though `rds.js:456–458` already handles gzip decompression correctly. Fix: add `|| (b[0] === 0x1F && b[1] === 0x8B)` to the condition at line 371 so gzip files pass pre-validation and reach `parseRDS`. Additionally, R supports bzip2 (`0x42 0x5A`) and xz (`0xFD 0x37`) compression; since `DecompressionStream` cannot handle those in the browser, detect them at this boundary and throw a user-friendly error explaining how to re-save with `compress="gzip"` or `compress=FALSE`.
**Invariants:** None — fix is entirely within `DataStudio.jsx`; no math or pipeline files touched.

**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`

**Status:** Code-complete in commit `35f4b5a6` (OLS/WLS display frozen to `result.spec` so sidebar edits cannot mutate it). **Browser validation by Franco is pending** — this item cannot be closed until confirmed in the live app.

### [high] · Model · 2026-06-14
**Feedback:** `Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.`
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** Code-complete as of commit `35f4b5a6` — OLS/WLS display is now frozen to `result.spec` so sidebar edits cannot mutate the displayed model. Needs Franco's browser validation only. If regression is found, look for any remaining places in `ModelPlots.jsx` or `DiagnosticsPanel.jsx` that still read from live `xVars`/`yVar` state instead of `activeResult.spec.xCols` / `activeResult.spec.yCol`.
**Invariants:** Frozen result state must read from `result.spec` (immutable post-estimation), never from live sidebar state — consistent with the non-destructive pipeline invariant.

---

### [high] · Model · 2026-06-11
**Feedback:** `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS`
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete as of commit `35f4b5a6` — plots were already wired; the frozen-result-state regression was the root cause. Needs Franco's browser validation. If any plot tab is blank after validation, confirm that `result.spec.residuals` and `result.spec.fitted` arrays are populated in the estimation result before being passed to `ResidualPlots.jsx`.
**Invariants:** Plot data must derive from the frozen estimation result — never recomputed from live `rawData` or sidebar variable state.

---

### [medium] · Model · 2026-06-10
**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete as of commit `35f4b5a6` — native vertical resize with bounded responsive canvas sizing added so PNG captures user-selected dimensions. Needs Franco's browser validation. If PNG export still clips, verify that the canvas `width`/`height` attributes (not just CSS dimensions) are updated after resize before the export `toBlob()` call fires.
**Invariants:** Inline styles only via the `C` object — no CSS resize classes or external UI libraries.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** `Heatmap must be fixed`
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete as of commit `5e105529` — Leaflet KDE heatmap layer restored with palette/opacity controls, legend, HTML export, and deterministic 5k-point cap. Needs Franco's browser validation. If the layer fails to render, check whether the `loadLeaflet` CDN singleton (`src/components/tabs/spatial/shared/leaflet.js`) resolves before the heatmap layer is attached — a timing race would silently fail without throwing.
**Invariants:** `SpatialEngine.js` and any file under `src/math/` must remain free of React imports.

---

### [medium] · Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 — a malformed row aborts the entire import`
**File:** `src/DataStudio.jsx` (CSV parse branch), `src/services/data/parsers/`
**Suggestion:** Triaged as fix-later (post-launch resilient-import pass). When implemented, wrap the CSV row-parsing loop in a try/catch per row, collect `{ lineNumber, rawLine, error }` into a `parseErrors` array, skip bad rows, and surface a recovery modal after import. For the DuckDB large-CSV path, use DuckDB's `IGNORE_ERRORS` pragma so the SQL path also skips bad rows rather than aborting. The recovery modal should offer Skip / Inspect / Abort options.
**Invariants:** Parsed data must flow through `rawData` unchanged; row skipping is a parse-time decision, not a pipeline step.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`
**Suggestion:** Fase X4 code-complete; browser benchmark still pending Franco. Franco should load the 21 MB / 900k-row CSV fixture in the deployed Vercel build and confirm: (1) file imports without crash, (2) the DuckDB ⚡ badge appears in the Wrangling header, (3) tab-transition lag is ≤2 s. If crashes persist, the likely culprit is JS object allocation during preview materialisation — ensure the 500-row preview query uses `USING SAMPLE 500 ROWS` rather than pulling the full Arrow table into JS objects.
**Invariants:** The full 900k rows must remain in DuckDB. Pipeline steps (filter, log, z-score, lag) must execute on the full table via `duckdbRunner.js`. Never truncate `rawData` before passing to `runner.js`.

**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/services/exprEvalService.js`

**Status:** Triaged as fix-later (post-launch expression-language compatibility).

**Suggestion:** In `exprEvalService.js`, add a pre-processing step that rewrites `col %in% c("a", "b")` to `["a","b"].includes(col)` before passing the expression to the evaluator. Also update `FilterBuilder`'s condition-row UI to include a new `is in` operator that emits this pattern from a multi-select input, so users who do not know R syntax can still build set-membership filters.

## UX / Design

### Model · 2026-05-25 (fix-later)
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`, `src/math/NonLinearEngine.js`
**Suggestion:** Add a `noIntercept: boolean` field to the estimation spec. In `ModelConfiguration.jsx`, expose a checkbox (under existing inference options). In each engine, guard the constant-column insertion with `if (!spec.noIntercept)` and decrement `k` by 1 for df calculations. Update `rScript.js`, `pythonScript.js`, `stataScript.js` to emit `lm(y ~ x - 1, ...)` / `sm.OLS(y, X)` / `regress y x, noconstant` when the flag is set.
**Invariants:** `seType` must remain explicitly passed — `noIntercept` is an additional spec field, not an internal engine default. No React in `src/math/`. SE type is never hardcoded inside engine functions.

---

### Clean · 2026-05-19 (fix-later)
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/services/exprEvalService.js`, `src/components/wrangling/CleanTab.jsx`
**Suggestion:** In `exprEvalService.js`, add a pre-processing step that rewrites `col %in% c("a","b")` → `["a","b"].includes(col)` using a simple regex before evaluation. Also add an `is in` operator to `FilterBuilder`'s condition-row UI that emits this pattern from a multi-select input so users unfamiliar with R syntax can still build set-membership filters.
**Invariants:** The rewrite must happen at evaluation time; the stored pipeline `filter` step should record the normalized JS expression so replays are deterministic.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/math/CausalEngine.js` (result object), `src/components/ModelingTab.jsx` or new `src/components/modeling/results/RDDResults.jsx`
**Suggestion:** `runSharpRDD` already returns bandwidth, kernel, polynomial order, LATE, SE, CI, and `nLeft`/`nRight`. Add an `rddSummary` plain-object field to the returned `EstimationResult` containing `{ call, kernel, bandwidth, effN_left, effN_right, estimate, robustSE, z, p }`, substituting the actual `runCol` and `treatCol` names (already on the result) for the generic display labels. Create `RDDResults.jsx` (mirroring the `IVResults.jsx` pattern) to render this as a compact monospaced table when `result.type === "SharpRDD"`.
**Invariants:** No React imports in `src/math/CausalEngine.js`. The `rddSummary` is a plain JS object assembled in the engine; rendering is the UI layer's responsibility only.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/components/modeling/resultDisplay.jsx`, `src/math/LinearEngine.js` (`buildLatex`)
**Suggestion:** Add a `labelOverrides: Record<string, string>` field to local display state in `resultDisplay.jsx` (never stored on the result object). Render an edit icon next to each coefficient row; on click, open an inline input writing to `labelOverrides`. Pass `labelOverrides` as an optional argument to `buildLatex` in `src/math/index.js` so it substitutes `labelOverrides[varName] ?? varName` for every row label. For persistence, store per-model label maps in `indexedDB.js` keyed by model ID.
**Invariants:** Never mutate the `EstimationResult` object. Persistence must go through `indexedDB.js`, not `localStorage`. `buildLatex` must remain a pure JS function with zero React imports. Inline styles only via `C`.

---

### Spatial · 2026-05-30 (fix-later)
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Triaged as fix-later (route to V4 after launch). When implemented, add a collapsible "Filter rows" panel per layer in `GeoLayerConfig.jsx` (similar to CleanTab's `FilterBuilder`). On change, filter `layer.rows` via `exprEvalService.js` before passing to the layer renderer. This keeps the filter ephemeral and display-only — it never writes a pipeline step or mutates `rawData`.
**Invariants:** Display-only filter; must NOT write a pipeline step or touch `rawData`.

**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))` — showing Call, Kernel, Bandwidth, Effective N, RD Estimate, Robust SE, z-value, p-value.

## Features

### Calculate · 2026-06-19
**Feedback:** `Add limits`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** New item — no BugTriage entry yet. Extend the Calculate workbench with a `limit(expr, var, point)` operation card alongside existing Derivative/Integral/Optimize cards. Accept the expression, variable, and approach point (including `±Infinity`). Implement numerically using bilateral ε-sampling (`f(a−ε)` and `f(a+ε)` for small ε ≈ 1e-9), check left/right convergence, and expose a one-sided mode toggle. If the current CAS library supports symbolic limits natively, prefer that path and show intermediate steps when the "Show steps" toggle is active.
**Invariants:** Computation must be client-side only (privacy-first, no API calls). Any symbolic/numeric helper logic must live in a pure JS utility file under `src/math/` or `src/components/tabs/` with zero React imports.

---

## Suggested action order

1. **Patch `src/DataStudio.jsx:371`** to allow gzip magic `0x1F 0x8B` in the `.rds` pre-validation — one-line fix, two users blocked today on fresh data files.
2. **Franco: browser-validate commit `35f4b5a6`** — covers OLS residual plots, frozen diagnostics/coefficient plots, and Plot Builder resize/PNG. Three high/medium bugs close on a single sign-off.
3. **Franco: browser-validate commit `5e105529`** — covers spatial heatmap. One medium bug closes.
4. **Franco: Fase X4 browser benchmark** (900k-row CSV) — confirms or reopens the most-reported performance crash; result determines whether a follow-up sprint is needed.
5. **Implement estimate-without-intercept** (`ModelConfiguration.jsx` checkbox + engine-wide `noIntercept` spec field) — medium impact, engine-wide change, spec carefully before starting.
6. **Add R `%in%` rewrite in `exprEvalService.js`** — small, self-contained, high value for users migrating from R.
7. **Create `RDDResults.jsx`** compact rdrobust-style summary — pure UI work, data already in result object.
8. **Add `labelOverrides` to `resultDisplay.jsx` + `buildLatex`** — post-launch reporting polish.
9. **Resilient CSV import** (malformed-row recovery modal) — post-launch; affects any user with dirty data.
10. **Calculate limits** and **spatial plot filters** — V4 / post-launch.
