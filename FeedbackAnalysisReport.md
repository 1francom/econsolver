# Feedback Analysis Report — 2026-09-03

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 13 open bugs · 12 open features · 0 open UX · 1 open performance · 5 fix-later
- 3 bugs marked "code-complete, browser validation pending" (treated as open)
- 3 critical bugs marked "likely fixed — confirmation pending" (included in bugs count)

---

## Bugs

### critical · Model · p-value wrong for insignificant coefficients · 2026-07-22 / 2026-07-28
**Feedback (3 reports):**
- `[2026-07-22]` "P-values with small numbers!!!!!!!" — t-stat near zero yields p < 0.001
- `[2026-07-28]` "URGENT THE p-values!!!!!! beta=0.0001 and se=0.002 can never give a p-value < 0.001 with ***"
- `[2026-07-28]` "P-Values of OLS have a problem when the estimations are insignificant"

**File:** `src/math/LinearEngine.js` (`tCDF` / `betainc`), commit `216b1ecb`
**Status:** BugTriage says "likely fixed — `216b1ecb` (2026-07-28); needs confirmation." The fix replaced the Riemann-sum `betainc` with Lentz's continued-fraction method (Numerical Recipes), which is stable for large `a` (i.e. large-df regressions). A parallel instance of the same bug was found in `calcEngine.js` (`_incompleteBeta` missing the symmetry branch) and fixed 2026-07-30.
**Suggestion:** Request Franco to run a single OLS on any large dataset (n > 10 000) with a near-zero β and confirm the displayed p-value is > 0.9, not < 0.001. If the fix is confirmed, update all three BugTriage rows to "fixed." If not confirmed, diff `LinearEngine.js` post-`216b1ecb` against the Numerical Recipes reference for the symmetry branch `x < (a+1)/(a+b+2)`.
**Invariants:** `src/math/LinearEngine.js` must have zero React imports. Math changes validated against R `pt()` to 4dp on p-values.

---

### high · Calculate · Stata workbench doesn't work completely · 2026-07-17
**Feedback:** "Stata workbench doesn't work completely"
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** Open the Stata tab in CodeEditor and identify which estimators emit no usable Stata code or emit code that fails on `do`. The most likely causes are: (1) estimators that were added after the HC2/HC3 replication pass (2026-07-18) but whose Stata `vce(hc2)` / `vce(hc3)` / `pVceHdfe` branches are missing; (2) CallawayCS's `csdid` block which uses `compGroup` and may not be installed in a vanilla Stata; (3) connected_components (known gap in `replicationIntegrityValidation`). Start by running `exportValidation.js` and looking for any Stata-specific assertion failures. Each gap should emit a `/* NOTE: ... */` comment rather than a silent empty string.
**Invariants:** All Anthropic/AI calls go through `AIService.js` only. SE type must be passed explicitly — never hardcoded inside `stataScript.js`.

---

### high · Model · Simulate replication script only reproduces DGP · 2026-07-28
**Feedback:** "Update the replication script, currently it replicates only the DGP"
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (script generators)
**Suggestion:** `SimulateTab.jsx` already has a "Copy script" path that calls the DGP exporters in `dgpScript.js`. The downstream analysis (Monte Carlo loop, resampling draws, sample tests, and any estimation on simulated data) is not serialised. Add a `buildSimulateScript(dgpParams, mcConfig, sampleTestConfig, estimationSpec)` export function in `src/services/export/simulateScript.js` that emits: (1) the DGP draw (reuse `dgpScript.js`), (2) a for-loop over `nSim` replications collecting the chosen statistic, (3) summary statistics/histogram code for the sampling distribution. Wire the result into `SimulateTab`'s existing copy-button path. The function must be pure JS with no React imports.
**Invariants:** Pure JS only — no React in `src/services/export/`. SE type passed explicitly if estimation is included.

---

### high · Model · FE must accept any categorical variable; FE with interactions · 2026-07-09
**Feedback:**
- "Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable"
- "Add also FE with interactions"

**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Status:** BugTriage says "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation."
**Suggestion:** Browser confirmation by Franco is the only remaining step. If confirmed working, update both BugTriage rows to "fixed." If not, check that `ModelConfiguration.jsx`'s FE column picker is not locked to the `panel.entityCol`/`panel.timeCol` values and that `runFEMulti` is called with the user-picked additional FE columns passed as the `feCols` array — not only the declared panel dimensions.
**Invariants:** `src/math/PanelEngine.js` must have zero React imports. SE type (`seType`) passed explicitly from ModelingTab to `runFEMulti`.

---

### high · Model · Plot Builder resize + PNG export broken · 2026-06-10
**Feedback:** "Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems"
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Status:** Code-complete (`35f4b5a6`, 2026-06-14). Browser validation pending Franco.
**Suggestion:** Franco should open a model result, expand the ◈ Plot Builder panel, drag the resize handle, and export a PNG. Confirm canvas dimensions match the resized viewport and that SVG text is not clipped. If the resize handle is not discoverable, add a `cursor: ns-resize` CSS property to the resize bar's inline style (the `C` object does not include cursor values; set it directly).
**Invariants:** Inline styles only via `C` object or direct style props — no external CSS.

---

### high · Model · Y~Yhat / Residual-vs-Fitted / Q-Q plots missing · 2026-06-11
**Feedback:** "Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS"
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Status:** Code-complete (`35f4b5a6`, 2026-06-14). Browser validation pending Franco.
**Suggestion:** Confirm all three diagnostic plots render after running an OLS and that they do not change when the user adds/removes X variables before re-estimating (the fix froze display to `result.spec`). If residuals still mutate, verify that `ResidualPlots.jsx` reads from the frozen `result` prop rather than from shared sidebar state.
**Invariants:** All styles inline via `C`. `src/math/` has zero React imports.

---

### high · Explore · Time series group references don't work · 2026-07-28
**Feedback:** "References of the group in time series doesn't work"
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** In ExplorerModule's time series view, selecting a "group by" column should colour each series by group. Check that `aes.color` is being set when the group column is selected, and that `PlotBuilder.jsx` is rendering separate `line` marks per group value (using Observable Plot's `stroke` channel). Also verify `plotRows` is not being sampled before the grouping step, which would silently drop group identity.
**Invariants:** Inline styles only. No external UI libraries.

---

### medium · Model · Multiple hypothesis tests — no vector + no R export · 2026-07-06
**Feedback:** "Add vector for running more than 1 hypothesis and save them to replicate in R"
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` currently runs one hypothesis at a time. Add a "Add hypothesis" button that appends rows to a local state array; each row shows the constraint string, statistic, and p-value. The "Copy R code" button should emit a `car::linearHypothesis(fit, c("...", "..."))` call with all constraints in one vector. `generateJointHypothesisScript()` in `ModelHypothesis.js` (fixed 2026-07-30 for the single case) already emits the right R shape — extend it to accept an array of constraint strings.
**Invariants:** `src/math/ModelHypothesis.js` must be pure JS (no React). Test with the existing `modelHypothesisValidation.mjs` harness.

---

### medium · Explore · Time series plot labels (title, axes) · 2026-07-09
**Feedback:** "Add labels for the Time series plot, title, axis, etc."
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** The Labels panel in `PlotBuilder.jsx` already exists for other geoms (title, x-label, y-label text inputs). Check whether `line` geom suppresses the Labels panel or whether ExplorerModule's time series mode bypasses the full `PlotBuilder` component. If the time series view renders its own SVG directly, port it to use `PlotBuilder` with a `line` geom preset so Labels become available for free. If it already uses PlotBuilder, verify the Labels panel is not conditionally hidden when `geom === "line"`.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

### medium · Model · Direct elasticity interpretation · 2026-07-09
**Feedback:** "Add direct elasticity interpretation"
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add an estimator rule to `INTERPRET_REGRESSION_PROMPT` (after existing rules J–R): when `spec.yLog === true` and `spec.xLog === true` for a given regressor, state that the coefficient is an elasticity and interpret it as "a 1% increase in X is associated with a β% change in Y." When only `spec.yLog === true`, interpret as a semi-elasticity (multiply by 100 for percentage). The log indicator is already in `spec` (set by FeatureTab's `log` step). Thread `spec.yTransform`/`spec.xTransforms` into the AI call payload if not already present.
**Invariants:** All AI calls go through `AIService.js`. The prompt addition must preserve the existing `cache_control: {type:"ephemeral"}` block — add to `INTERPRET_REGRESSION_PROMPT`, not `SHARED_CONTEXT`.

---

### medium · Spatial · Heatmap display broken · 2026-06-13
**Feedback:** "Heatmap must be fixed"
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Status:** Code-complete (`5e105529`, 2026-06-14). Browser validation pending Franco.
**Suggestion:** Franco should open the Map tab, add a Heatmap layer, and confirm the KDE cells render with the chosen palette. Also verify the 5k-point cap banner appears for large datasets and that the HTML export produces a self-contained file.
**Invariants:** Inline styles via `C`. No external map libraries beyond Leaflet (CDN singleton already in `leaflet.js`).

---

### medium · General · LaTeX table too narrow for many columns · 2026-07-05
**Feedback:** "Adjust the size of LaTeX tables when the number of columns increases"
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx` (BugTriage line 62)
**Suggestion:** In `latexTable.js`, detect when the number of model columns exceeds a threshold (e.g. > 5) and switch from a fixed-width `tabular` to `tabularx{\textwidth}` with `X` column specs, or prepend `\resizebox{\textwidth}{!}{…}`. For Stargazer output in `ReportingModule.jsx`, pass `column.sep.width` or use `longtable=TRUE` for wide tables. Keep the fix in the export layer only — no UI change needed.
**Invariants:** Export utilities are pure JS. No React imports in `src/services/export/`.

---

### medium · Clean · Unfinished case_when / mutate work lost on navigation · 2026-07-17
**Feedback:** "Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases"
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Persist the in-progress form state for each sub-panel (case_when rows, mutate expression, etc.) to `sessionStorage` keyed by `pid + stepType`. On mount, rehydrate from `sessionStorage` if the key exists. Clear the key when the step is committed to the pipeline or explicitly cancelled. This is a view-layer convenience — it must not write to IndexedDB or the pipeline (non-destructive invariant applies to the pipeline, not to UI draft state). Wrap every `sessionStorage` access in try/catch to avoid breaking in private-browsing contexts.
**Invariants:** Use `sessionStorage` for UI draft state only. Pipeline persistence uses IndexedDB (`indexedDB.js`). localStorage is deprecated for pipeline/data storage.

---

### medium · General · Groups for pinned models, plots, and maps · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria"
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend `artifactOrder.js`'s `ArtifactOrder` data shape with an optional `groupId` string on each artifact entry. Add a "New group" button in `ModelBufferBar.jsx` that prompts for a group name and assigns a UUID. Render pinned models grouped by `groupId` with a collapsible header per group; ungrouped items form a default group. Persist `groupId` alongside the artifact order in IndexedDB. Keep comparison (G13 `compRows`) scoped to a single group by default, with an "Compare across groups" toggle.
**Invariants:** IndexedDB for persistence. Inline styles via `C` object. No external UI libraries.

---

### medium · Model · Plot Builder in ModelingTab — binned scatter options · 2026-07-22
**Feedback:** "Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators"
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Add a "Binned scatter" geom to `PlotBuilder.jsx` that bins the x-axis into `nBins` equal-width buckets and plots the mean of y in each bin (equivalent to `binscatter` in R / `binsreg` in Python). Expose `nBins` and an option to partial out controls (using FWL residualisation via `runOLS`) in the geom options panel. For the G10 templates in ModelingTab, add a "Binned scatter (raw)" and "Binned scatter (residualised)" template.
**Invariants:** `src/math/` is pure JS. The FWL residualisation helper must not import React.

---

### low · General · Guide tour must be updated · 2026-07-17
**Feedback:** "Guide tour must be updated"
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`)
**Suggestion:** Audit `TOUR_STEPS` against the current UI: verify step targets exist in the DOM (e.g. correct `data-tour` attribute names), update any descriptions that mention old UI text, and add steps for features added since the tour was last written (Spatial tab, Simulate tab, Report tab, model buffer bar). Also update each module's `HintBox` copy per the CLAUDE.md convention: never write a count ("14 estimators") — name the things instead.
**Invariants:** Inline styles via `C`. `HintBox` `title` prop is the module name, trigger button always reads "Help."

---

## Features

### Clean · Country Codes transform · 2026-07-19
**Feedback:** "Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform (new `FeatureTab.jsx` case, small static lookup table) vs. fuzzy country-name matching"
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js` (new step type `country_code`)
**Suggestion:** Add a `country_code` pipeline step: user picks the input column (any text column) and the target format (iso2 / iso3 / country name). The step maps values through a small static lookup table (< 300 entries) bundled as a JSON import. Fuzzy matching (for misspelled names) can reuse `fuzzyGroups` from `wrangling/utils.js`. Wire through `runner.js`, `registry.js`, and `stepTranslators.js` (R: `countrycode::countrycode()`; Python: `pycountry` or a manual dict; Stata: a `merge` against a crosswalk). Use `pipeline-step-adder` agent for the full checklist.
**Invariants:** Non-destructive pipeline — replays on `rawData`. Registry must stay in sync with `runner.js`. No React in `src/math/` or `src/core/`.

---

### Calculate · Limits (lim x→a f(x)) · 2026-06-19
**Feedback:** "Add limits"
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Extend the symbolic workbench in `CalculateTab.jsx` with a `Limit` operation: user inputs an expression, a variable, and a target value (including ±∞). Evaluate numerically by computing f at `a ± ε` for decreasing ε (squeeze theorem approach) and display left/right limits. For polynomial/rational expressions, the existing CAS can simplify first. Emit LaTeX via `\lim_{x \to a}` and R/Python snippets using `pracma::Limit` / `sympy.limit`.
**Invariants:** Inline styles. No external math libraries beyond those already CDN-loaded for the workbench.

---

### Calculate · Plot constants alongside function curves · 2026-07-04
**Feedback:** "Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)"
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** In the equation canvas, allow a row to define a constant (e.g. `y = 3.5`) and render it as a horizontal line using Observable Plot's `ruleY([3.5])`. Display it with a dashed style, distinguished from function curves. The constant's label should appear at the right end of the line. This is a display-only change in `CalculateTab.jsx` — no new pipeline step needed.
**Invariants:** Inline styles only. Observable Plot already loaded via CDN singleton.

---

### Explore · More labels / tooltips when plotting by color · 2026-07-11
**Feedback:** "Add more labels in tables or boxes when plotting by color"
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, add an Observable Plot `tip()` mark (added in Plot 0.6.9) so hovering a point/bar shows its exact `aes.color` value alongside x and y. Also render a legend (Observable Plot `legend()`) that maps each colour swatch to the group label. Both are supported by the CDN version (0.6.x) already loaded.
**Invariants:** CDN-only for Plot (already loaded). Inline styles via `C` object. The `tip` mark must not add external CSS.

---

### Model · Variable searcher in variable selector · 2026-07-09
**Feedback:** "Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster"
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text `<input>` above the variable list in `VariableSelector.jsx` that filters the displayed column names via `toLowerCase().includes(query)`. Keep the input's inline style consistent with `C.teal` / `mono`. Clear the filter when the user selects a variable. This is a pure UI filtering change — no backend or pipeline involvement.
**Invariants:** Inline styles via `C`. IBM Plex Mono typography.

---

### Model · Adjust zoom of plots by changing observations · 2026-07-22
**Feedback:** "Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations"
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Add an "N cap" slider (100 – all) in the PlotBuilder options panel that subsamples `plotRows` to the first N rows (or a random N-row sample via `rows.sort(() => Math.random() - 0.5).slice(0, N)`). The cap is a display convenience only — it must not affect the pipeline or estimation. A "Random sample" toggle randomises the order before slicing for visual deduplication. The existing `splitPoints` (for scatter plots with too many points) can be replaced or complemented by this.
**Invariants:** This is a VIEW-layer control. Estimation always runs on the full dataset. No mutation of `rawData`.

---

### General · Cross-session lists for filters and variable construction · 2026-07-19
**Feedback:** "Think about how to construct lists, save them and use them during the session when filtering or constructing variables"
**File:** `src/services/session/sessionState.jsx` (new list context), `src/components/wrangling/CleanTab.jsx`
**Suggestion:** Extend `sessionState.jsx` with a `lists` map (`listName → string[]`). Add a "Lists" drawer in the sidebar where users define named lists (e.g. `eu_countries = ["DEU", "FRA", ...]`). In `FilterBuilder`, add an `in list` operator that resolves against `sessionState.lists` rather than inlining the values. Persist lists to IndexedDB under a `lists` key in the project record. Export lists to R as `eu_countries <- c(...)` preamble in the replication scripts.
**Invariants:** IndexedDB for persistence. Lists are session-scoped React context — not pipeline state. The `in list` operator must be registered in `predicate.js` (single owner — never re-implement filter operators elsewhere).

---

### Explore / Model · Spatial filter for plots (fix-later) · 2026-06-14
**Feedback:** "Plot builder has no spatial row-filter control scoped to a layer"
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a bounding-box filter in `GeoLayerConfig.jsx`: user draws or enters lat/lon min/max and the layer filters its rows before passing them to `mkGeoLayer`. Implement as a view-layer filter (slice `rows` before render, not a pipeline step), consistent with how the Data Viewer's filter works. Use `predicate.js`'s `evalPredicate` for consistency — do not re-implement a filter operator.
**Invariants:** `predicate.js` is the single owner of filter operators. View-layer filter — never mutates `rawData`.

---

### Model · Editable LaTeX table variable names (fix-later) · 2026-06-14
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** Add a `variableLabels` map (column name → display label) to `EstimationResult`. Expose a rename table in the result display panel (inline edit on click). Pass `variableLabels` into `buildStargazer()` and `buildLatexTable()` so the emitted LaTeX uses the user's labels. For RDD, default the labels to the actual running variable name and treatment column name from `result.spec` instead of the hardcoded strings.
**Invariants:** IndexedDB for persistence. `EstimationResult` is in `src/math/` — no React imports. The rename UI lives in a component in `src/components/modeling/`.

---

### Simulate · Studentization for permutation tests (feature) · 2026-06-08
**Feedback:** "Add studentization for permutation tests"
**File:** `src/components/tabs/SimulateTab.jsx` (or `src/math/dgpDraw.js` / resampling logic)
**Status:** Marked "verified fixed" in the X5 reconciliation table. Confirm with Franco — if not accessible in the UI, add a "Studentize" toggle next to the permutation test statistic selector.
**File:** `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** Add a `studentized` checkbox in the permutation/bootstrap panel. When enabled, divide each resampled statistic by its resampled SE (std of the bootstrap distribution) before comparing to the observed statistic — following `Resampling.js`'s existing `studentized` flag.
**Invariants:** `src/math/dgpDraw.js` is pure JS (shared by Simulate and the expr worker).

---

## Performance

### high · Data · Big datasets generate crashes (21MB, 900k rows) · 2026-05-08
**Feedback:** "Big datasets generate crashes (21MB) over 900k rows"
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Status:** DuckDB Fase X4 is code-complete per the X5 reconciliation table. Browser benchmark pending Franco (`plans/2026-05-22-fase-x4-performance.md`). Specifically: (1) CSV/TSV > 10 MB → auto-convert to Parquet in DuckDB on first load, (2) OPFS cache so re-opening skips re-import, (3) tab transitions read from the cached Parquet rather than materialising the full JS array.
**Suggestion:** Franco should test with a 900k-row CSV: open, switch tabs, run a filter, run an OLS, export a script. Tab-transition lag should drop below 3 s. If the OPFS write is still failing (the "extensions.duckdb.org blocked" class of bug documented in CLAUDE.md), check `connect-src` in `vercel.json` includes `extensions.duckdb.org` and that DuckDB's parquet extension loads without a `RuntimeError: function signature mismatch`.
**Invariants:** Never pull full Arrow result into JS objects. No server-side computation (privacy-first: data stays in browser).

---

## Fix-later (deferred open items)

| Date | Module | Issue | File | Notes |
|------|--------|-------|------|-------|
| 2026-06-14 | Data | Malformed CSV row aborts import instead of surfacing recovery | `src/services/data/parsers/`, `src/DataStudio.jsx` | Post-launch pass: retain original row number, offer skip/repair preview |
| 2026-06-14 | Clean | R `%in% c(...)` filter syntax not accepted by FilterBuilder | `src/components/wrangling/CleanTab.jsx` | Low priority; `in` operator already exists for the canonical predicate language |
| 2026-06-14 | Model | RDD result lacks `summary(rdrobust)` compact table | `src/math/CausalEngine.js`, `src/components/modeling/` | Presentation enhancement; estimates already correct |
| 2026-06-14 | Spatial | Plot builder has no spatial row-filter control | `src/components/tabs/spatial/plot/` | Route to V4 plots after launch |
| 2026-06-14 | Model | LaTeX coefficient table display names cannot be edited | `src/services/export/latexTable.js`, `src/ReportingModule.jsx` | Post-launch reporting metadata feature |

---

## Suggested action order

1. **Confirm p-value fix (critical)** — Request Franco browser-verify the `216b1ecb` fix on a large OLS. Three critical BugTriage rows can be closed immediately if confirmed, or the continued-fraction `betainc` needs a targeted bug hunt.
2. **Stata workbench root-cause** (high) — Run `exportValidation.js`; identify which estimators emit broken or empty Stata; fix those `transpileModel` branches in `stataScript.js`.
3. **Simulate replication script** (high) — Create `src/services/export/simulateScript.js`; wire into `SimulateTab.jsx`'s copy button to emit the full Monte Carlo loop, not just the DGP draw.
4. **Browser-validate three code-complete items** (high) — Plot Builder resize, Y~Yhat/residuals/QQ, and Spatial heatmap are all code-complete since June 2026 and only need Franco's confirmation to close.
5. **FE categorical variable confirmation** (high) — Confirm N-way FE PR (#104) works end-to-end with any categorical column; close two BugTriage rows.
6. **Stata workbench + HC2 replication pass** (high) — The 2026-07-18 HC2/HC3 fix addressed many sites but BugTriage still flags the Stata workbench as "open"; verify the ~25 hardcoded-SE sites include all Stata-specific branches.
7. **Time series group references** (medium) — Locate where `ExplorerModule` renders the time series grouping and verify `aes.color` is threaded to Plot's `stroke` channel.
8. **Guide tour update** (low) — Audit `TOUR_STEPS` targets and update HintBox copy for all eight modules to remove counts and add new features.
