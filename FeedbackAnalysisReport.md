# Feedback Analysis Report — 2026-08-22

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **10 open bugs** (3 critical/high, 5 medium, 2 low) · **12 open features** · **2 open UX** · **1 open performance** · **2 open other**

Items marked ⏳ are code-complete but awaiting Franco's browser validation.  
Items marked ❓ are "likely fixed" per BugTriage but require confirmation.

---

## Bugs

### high · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work  
**File:** `src/services/export/rScript.js`, `src/math/CallawayEngine.js`  
**Suggestion:** The Callaway & Sant'Anna export path in `rScript.js` likely emits a generic TWFE snippet rather than the `did::att_gt` / `aggte()` pattern that `CallawayEngine.js` implements. Audit the `transpileModel` branch for `type === "CallawayDiD"` (or equivalent) and emit a proper R script: `library(did); out <- att_gt(yname, tname, idname, gname, data=df); aggte(out, type="dynamic")`. Validate the emitted output against the existing `callawayRValidation.R` fixture.  
**Invariants:** All Anthropic API calls go through `AIService.js` only; export scripts are pure string generation — no React or math imports.

---

### high · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/services/export/plotScript.js`, `src/components/PlotBuilder.jsx`  
**Suggestion:** `plotScript.js` generates R/Python/Stata code for PlotBuilder plots but the existing `loadLine.js` helpers (`buildRLoadLine`, `buildPyLoadLine`, `buildStataLoadLine`) are not called from the plot script path — only from model scripts. Thread the active dataset's `loadOpts` (available on `datasetMeta`) through PlotBuilder's export action into `plotScript.js`, and prepend a `buildRLoadLine(loadOpts)` / equivalent call at the top of the emitted script for each language. Mirror the pattern in `rScript.js` which already does this for model scripts.  
**Invariants:** Export scripts are pure string generation; `loadLine.js` already exists and must not be duplicated.

---

### high · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group  
**File:** `src/pipeline/runner.js` (`group_summarize` step), `src/components/wrangling/ReshapeTab.jsx`  
**Suggestion:** Per the 2026-08-07 bug fix documented in CLAUDE.md, the fix for the analogous `SampleTestPanel` gap was to add a "long-format" (outcome col + group col) input mode rather than building a new verb. Apply the same approach to `group_summarize` in `ReshapeTab.jsx`: add an optional "Compare groups" mode where the user picks a numeric outcome column and a binary/categorical group column; the runner computes `mean(Y|group=1) − mean(Y|group=0)` within the SQL/JS path and appends it as a `diff_means` summary row. Reuse `SampleTests.twoSampleMeanTest` for the calculation; guard against the `Number(null) === 0` trap (documented in CLAUDE.md) by calling `cleanNumeric` first.  
**Invariants:** `src/math/` and `src/core/` must have zero React imports; pipeline steps replay on `rawData`, never mutating it.

---

### high · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`  
**Suggestion:** The scope is unclear from the report alone; the most likely causes are (1) `stataScript.js` has untranslated step types that fall through silently (the 2026-08-06 "Every Clean filter silently dropped" class of bug), (2) certain estimators (e.g. Callaway-Sant'Anna, Sun-Abraham) have no Stata translation and emit a comment stub that looks like broken code, or (3) N-way FE renders `reghdfe` syntax that requires an `ssc install` that users don't know to run. Audit `stataScript.js` by running `node src/services/export/__validation__/replicationIntegrityValidation.mjs` and reviewing which step types fall to their default. Add `StataInstallNote` banners in `CodeEditor.jsx` for steps that require non-base Stata packages (`reghdfe`, `csdid`, `bacondecomp`).  
**Invariants:** Export scripts are pure JS string generation; never add raw `fetch` calls outside `AIService.js`.

---

### medium · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `CoefficientTestPanel.jsx` currently holds a single hypothesis at a time. Add a `savedTests` array in local component state (or IndexedDB-persisted under the project's `pid`), a "Save this test" button that appends the current `{contrast, nullValue, testType}` to it, a list display with delete controls, and a bulk R-export button that calls `statInferenceScript.js`'s existing `toRSnippet()` for each saved test and concatenates the results. No math changes needed.  
**Invariants:** Use IndexedDB for persistence, never localStorage.

---

### medium · Math · 2026-07-17
**Feedback:** Add chi-square for linearHypothesis  
**File:** `src/math/ModelHypothesis.js`  
**Suggestion:** `ModelHypothesis.js` already implements a Wald F-test for linear hypotheses. Adding chi-square is straightforward: `χ² = F × q` where `q` is the number of restrictions (already computed as `q` in the current Wald path). Expose a `testType: "chi2"` option alongside the existing `"F"` option, compute `pValue = 1 − chiCDF(chi2, q)` using the existing `chi2CDF` helper (or add one via the regularized incomplete gamma, same pattern as `betainc` for the F/t fix). Surface the choice as a toggle chip in `CoefficientTestPanel.jsx`.  
**Invariants:** Zero React imports in `src/math/`.

---

### medium · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** `FeatureTab.jsx` already has draft state in component state for the active transform panel; the draft is lost when the user navigates to another tab. Lift the draft state for each transform section into `WranglingModule.jsx` (already owns pipeline state) under a `featureDrafts` object keyed by section name (e.g. `case_when`, `mutate`). Pass the draft down as a prop and update it via an `onDraftChange` callback. This requires no persistence layer change — the draft only needs to survive within-session tab switches.  
**Invariants:** Use IndexedDB for persistence if the draft should survive page reload; for within-session only, React state in `WranglingModule.jsx` is sufficient.

---

### medium · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`  
**Suggestion:** `latexTable.js` generates the `\begin{tabular}` environment with fixed column widths. When column count exceeds ~6, the table overflows a standard A4 text width. Add a check in `buildStargazer()`: if `colCount > 5`, wrap the output in `\resizebox{\textwidth}{!}{...}` (scales the table to text width) or switch to `\adjustbox{max width=\textwidth}` from the `adjustbox` package. Emit a `\usepackage{adjustbox}` line in the preamble block when this threshold is crossed.  
**Invariants:** None — this is pure string generation.

---

### medium · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`  
**Suggestion:** PlotBuilder's labels panel (`Labels` section in the sidebar) already handles `title`, `xLabel`, `yLabel` for most geoms but is likely not wired for the time-series (`line` geom over a date x-axis) path in `ExplorerModule.jsx`'s dedicated time-series tab. Confirm whether `ExplorerModule.jsx` renders its time-series via PlotBuilder or a separate SVG path; if the latter, add a title/axis label overlay using the same inline-style pattern as the existing `PlotBuilder` labels block.  
**Invariants:** Inline styles only, via the `C` object from `shared.jsx`; no external UI libraries.

---

### medium · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)  
**Suggestion:** Add a rule in `INTERPRET_REGRESSION_PROMPT` under the ESTIMATOR TYPE RULES section: when the dependent variable is log-transformed and an independent variable is also log-transformed, interpret the coefficient as a constant elasticity (% change in Y per 1% change in X). When only X is log-transformed, the coefficient is a semi-elasticity (% change in Y per unit change in X / 100). Thread a `logVars` array (already derivable from the pipeline's `log` steps) into the `interpretRegression` call's user payload via `sessionSnapshot.js`.  
**Invariants:** All Anthropic calls go through `AIService.js`; the `SHARED_CONTEXT` cache block must be preserved (add the rule inside the existing rules section, not as a new cached block).

---

### low · UI · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array)  
**Suggestion:** `TOUR_STEPS` is a hand-written array of 9 steps in `HelpSystem.jsx`. Many features shipped since its last update: N-way FE, Sun-Abraham, Callaway-Sant'Anna, PlotBuilder tile/facet geoms, Bacon decomposition, CR2/CR3, no-intercept OLS, OPFS Parquet caching. A full audit should compare `TOUR_STEPS` descriptions against current UI selectors; update each step's `selector` if the target element has been renamed or removed, and add a step for the spatial workflow. Per CLAUDE.md working convention: never write a count into help copy — name things instead.  
**Invariants:** None — this is UI copy only.

---

### low · UI · 2026-07-17
**Feedback:** No arrange (sort) control in the data viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** The Data Viewer already uses `getTablePage` with SQL ORDER BY for DuckDB datasets. Add a sort control to the column headers (click header to toggle ASC/DESC) that sets `sortCol` / `sortDir` state and passes them into `getTablePage`'s SQL as `ORDER BY "${col}" ${dir}`. For non-DuckDB datasets the JS runner can call `[...rows].sort(...)` inline. Mirror the existing `getFilteredRowCount` pattern so the sort applies to the full table, not just the preview page.  
**Invariants:** Display limit ≠ computation limit; never truncate `rawData` before passing to runner or duckdbRunner.

---

### ❓ needs confirmation · Math/UI · 2026-07-09
**Feedback:** FE must accept any categorical variable (country, state, sector), not just entity and time  
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`  
**BugTriage:** "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation"  
**Suggestion:** N-way FE (`runFEMulti` / `demeanByFE`) is implemented and validated (CLAUDE.md "DONE 2026-07-11"). The BugTriage gap is confirmation that the UI in `ModelConfiguration.jsx` exposes arbitrary categorical columns in the FE picker, not just the hardcoded `entityCol`/`timeCol` pair. Browser-confirm that users can select `country`, `sector`, etc. from a dropdown and the picker populates from the loaded dataset's categorical columns. If the picker is still hardcoded, update it to read column names from `sessionState`.  
**Invariants:** None.

---

### ❓ needs confirmation · Math · 2026-07-09
**Feedback:** Add also FE with interactions  
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions`)  
**BugTriage:** "likely fixed — FE interactions shipped in `045f5cda` (#104); needs confirmation"  
**Suggestion:** `factor_interactions` step exists in `runner.js`. Browser-confirm that a user can add `factor_interactions` columns to an N-way FE model and the interaction dummies are absorbed correctly (i.e., they appear in the X matrix alongside the FE demeaning). No math change expected; this is a UI confirmation task.  
**Invariants:** None.

---

⏳ **Items code-complete, awaiting Franco's browser validation (no new suggestion needed — validate and strike through):**

| Item | BugTriage status | File |
|------|-----------------|------|
| Plot Builder resize / PNG export aesthetics [2026-06-10] | code-complete `35f4b5a6` | `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx` |
| Y~Yhat / Residual vs Fitted / Q-Q plot [2026-06-11] | code-complete `35f4b5a6` | `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx` |
| Spatial heatmap display broken [2026-06-13] | code-complete `5e105529` | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` |
| Diagnostics mutate with sidebar / pinned model restore [2026-06-14] | code-complete `35f4b5a6` | `src/components/ModelingTab.jsx` |

---

## Features

### Model · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option  
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`  
**Suggestion:** The filter builder uses the canonical `in`/`nin` operators from `predicate.js`. Expose a multi-value text input in the Filter Builder UI that maps to the existing `in` operator (which already handles arrays). The R-style `%in% c(...)` syntax doesn't need to be parsed; users just need a way to paste/type a comma-separated list that the UI converts to an array value for the `in` operator. Add a "Paste list" input mode to the `in`/`nin` operator row in `CleanTab.jsx`.  
**Invariants:** `predicate.js` is the single owner of all filter operators — never re-implement elsewhere.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/components/modeling/results/` (RDD results component), `src/math/CausalEngine.js`  
**Suggestion:** `CausalEngine.js` already computes all necessary fields (bandwidth, kernel, polynomial order, LATE, SE, p-value, n_left/n_right). Add a compact summary table component in the RDD results panel that mirrors `rdrobust`'s console output: Method | Coef | S.E. | z | P>|z| | [CI] | BW | N_left | N_right. This is a pure display change — no math changes.  
**Invariants:** Inline styles via `C` object; no external UI libraries.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names  
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/export/latexTable.js`  
**Suggestion:** Add an optional `displayNames` map in `EstimationResult` that overrides the column name displayed in coefficient tables. The Model results panel should show a small ✎ icon next to each variable name that opens an inline edit field; saving writes to `displayNames` on the result object (or on the pinned buffer entry). `latexTable.js` and `buildStargazer` should check `displayNames[col]` before falling back to the raw column name.  
**Invariants:** Use IndexedDB for persistence if display names should survive reloads; inline styles only.

---

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The Calculate workbench already has a CAS layer for derivatives, integrals, and optimization. Limits (`lim x→c f(x)`) can be added as a new operation type in `CalculateTab.jsx` using the same `nerdamer`/`mathjs` CAS call pattern already in use. For most practical limits, substitution + L'Hôpital (already available as the derivative operation) covers the cases students need. Add a "Limit" button in the operations bar, a cutoff-point input, and render the result using the existing KaTeX output path.  
**Invariants:** `src/math/` must have zero React imports — CAS helpers belong in `CalculateTab.jsx` itself or in a separate pure-JS utility, not in `src/math/`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The existing equation plotter renders function curves via a range loop. Constants are simply degenerate functions `f(x) = c`. The simplest approach is to detect whether a parsed expression has no `x` dependency and plot it as a horizontal line (using the existing `hline` geom logic from PlotBuilder as inspiration). Alternatively, expose a "constant" checkbox next to each equation input that renders the expression as a flat line at `y = eval(expr)`.  
**Invariants:** Inline styles only.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/geo.js`  
**Suggestion:** `SpatialGeoPlot.jsx` (the static SVG geo-plot tab) renders point/polygon layers but has no grid overlay capability. The `SpatialEngine.js` already has `buildRectGrid` and the `GridSection.jsx` analyze op produces a grid dataset. Add a "Grid layer" option in `GeoLayerConfig.jsx` that accepts a grid dataset output (WKT polygon column) and renders the grid cells as an SVG fill layer using the existing `wktToLeaflet` / `parseWktRings` helpers. The color scale can reuse `buildColorScale` from `src/components/tabs/spatial/shared/color.js`.  
**Invariants:** Spatial plot uses inline SVG — no external mapping libraries; respect the `wkt.js` WKT parsing contract.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**Suggestion:** Add a per-layer bounding-box or attribute filter in `GeoLayerConfig.jsx`. The simplest useful form is a column-value filter (pick column, pick value(s)) that filters the layer's row array before passing it to the draw loop — reusing `evalPredicate` from `src/pipeline/predicate.js`. For spatial bounding-box filtering, derive the envelope from the map's current viewport (min/max lat/lon already computed in `geoBbox`) and add a "Clip to viewport" toggle.  
**Invariants:** `predicate.js` is the single owner of filter operators.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a text input at the top of each variable panel (Y, X, W, Z) in `VariableSelector.jsx` that filters the visible column chips via `col.toLowerCase().includes(query)`. This is a pure UI change — the underlying variable arrays are unchanged.  
**Invariants:** Inline styles only.

---

### Model · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` already maintains a project-scoped global order for saved artifacts. Extend it with an optional `groupName` property per artifact entry. Add a "New group" button in `ModelBufferBar.jsx` that prompts for a group name (inline input, no modal), assigns the current model to it, and filters the comparison view to the selected group. Persist group assignments via the existing `saveArtifactOrder` path. Render group labels as collapsible section headers in the buffer bar.  
**Invariants:** Use IndexedDB via `indexedDB.js` for persistence; inline styles only.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** `History.jsx` renders the pipeline step list. Each step has a `type` that maps to a tab/sub-tab (e.g., `rename` → Clean tab, `log` → Feature tab, `group_summarize` → Reshape tab). Add a small "✎ edit" icon on each step card in `History.jsx` that, when clicked, calls an `onJumpToStep(step)` callback up to `WranglingModule.jsx`, which then sets the active tab and sub-tab to the one responsible for that step type. Maintain a static `STEP_TYPE_TO_TAB` map for the routing.  
**Invariants:** None — pure UI state change.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`  
**Suggestion:** Add a `country_code` pipeline step: given an input column (containing ISO2/ISO3 codes or country names), and a target format (ISO2 / ISO3 / display name), it maps each value through a static lookup table bundled into the runner (the UN M49 / ISO 3166-1 table is ~250 rows and fits comfortably inline as a JS object). For fuzzy matching of non-standard names, reuse `fuzzyGroups` from `src/components/wrangling/utils.js` with a low distance threshold. Register the step in `registry.js` and add a case in `runner.js`.  
**Invariants:** Pipeline steps must replay on `rawData`, never mutating in place; registry and runner must stay in sync.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
**File:** `src/services/session/sessionState.jsx` (design-first)  
**Suggestion:** This needs a design decision before implementation. Recommended scope: add a `namedLists` map to `sessionState.jsx` (session-scoped React context) keyed by user-chosen name, each entry an array of strings/numbers. Expose a "Lists" panel in the sidebar or a mini-modal from the filter builder ("Choose from saved list"). For persistence across page reloads, save to IndexedDB under the project `pid`. The `in` operator in `predicate.js` already accepts arrays, so filter integration is a matter of passing `namedLists[name]` as the filter value.  
**Invariants:** Use IndexedDB for persistence; `predicate.js` is the single owner of filter logic.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot (the library PlotBuilder is built on) does not natively support 3D plots. This would require either a second y-axis (dual-axis chart, achievable by layering two marks with `fy` scale overrides) or a true 3D scatter (requires a separate library). Recommendation: implement dual-y-axis first (two overlapping line/bar layers with independent y scales using Plot's `y` vs `y2` scale channels) before committing to a full 3D library. Flag as medium complexity.  
**Invariants:** No external JS libraries may be added without also adding them to the CSP `script-src` and, if they use `fetch` at runtime, to `connect-src` in `vercel.json`.

---

## UX / Design

### medium · UX · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set, PlotBuilder renders a legend but may not show value labels directly on data points (e.g., category names on a scatter or bar). Add a `showColorLabels` toggle in the Labels panel that, when enabled, appends a `Plot.text(data, {x, y, text: d => d[aes.color], fill: d => colorScale(d[aes.color])})` mark layer. Apply a white stroke `paintOrder: "stroke"` for legibility (same pattern as tile value labels added 2026-07-28).  
**Invariants:** Inline styles only; Observable Plot 0.6 CDN — no npm package import.

---

### low · UX · General
**Feedback:** Data viewer has no arrange (sort) control (also in Bugs above)  
> See Bugs section for the suggestion.

---

## Performance

### high · Performance · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`  
**BugTriage:** "Fase X4 code-complete; browser benchmark pending Franco"  
**Suggestion:** The DuckDB-WASM path for CSV >10MB and `.dta`/`.rds` already routes through the Parquet OPFS cache (per `duckdb.js` and `DataStudio.jsx`). The reported crashes are likely JS heap exhaustion during the **initial parse** before the DuckDB hand-off (e.g., `rds.js`'s full in-memory tree for a 900k-row file, or `stata.js`'s row-by-row loop). Profile the crash path: if it's in the parser, consider streaming row emission in batches directly to DuckDB's Arrow interface instead of building a full `{headers, rows}` object. This requires no architectural change — the DuckDB hand-off point already exists in `DataStudio.jsx`.  
**Invariants:** Computation must run entirely client-side (privacy-first); never pull full Arrow result into JS objects.

---

## Other

### other · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 · Original Line: China,CHN,2010,2425.47...  
**File:** `src/DataStudio.jsx` (CSV parser path), `src/services/data/parsers/`  
**Suggestion:** BugTriage marks this "fix-later — post-launch resilient-import pass." The suggested approach is to catch per-row parse errors in the CSV parser, log the offending row number and content to a recoverable error list, and surface a "Rows with parse errors" modal after load that lets the user skip or repair them. Retaining original row numbers is essential for reproducibility. Do not abort the full import on a single bad row.  
**Invariants:** Non-destructive pipeline: any per-row fix must be expressed as a pipeline `patch` step, never mutating `rawData` in place.

---

### other · Design needed · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session  
> See Features section for the design suggestion.

---

## Suggested action order

1. **CS DiD replication code** (`src/services/export/rScript.js`) — users can't reproduce their core results; breaks the product's value proposition for Callaway-Sant'Anna users.
2. **group_summarize cross-group math** (`src/pipeline/runner.js`, `ReshapeTab.jsx`) — basic ATE by difference in means is a core econometrics operation and the workaround (pivot_wider → mutate) has a silent trap that drops columns.
3. **Plot replication code missing load lines** (`src/services/export/plotScript.js`) — any exported plot script fails to run without the data load; affects every Explorer/PlotBuilder user who exports code.
4. **Stata workbench audit** (`src/services/export/stataScript.js`, `CodeEditor.jsx`) — investigate which step types silently fall through; add `ssc install` notices for non-base packages.
5. **Chi-square for linear hypothesis** (`src/math/ModelHypothesis.js`) — low effort, high-frequency teaching need; straightforward `χ² = F × q` addition.
6. **Variable searcher in ModelingTab** (`src/components/modeling/VariableSelector.jsx`) — trivial text-filter UI change; high impact when datasets have many columns (common in the LMU panel datasets).
7. **Plot Builder label controls for time series** (`src/components/PlotBuilder.jsx`, `ExplorerModule.jsx`) — missing labels on the most commonly used Explore chart type.
8. **Transformation draft state** (`src/components/wrangling/FeatureTab.jsx`, `WranglingModule.jsx`) — navigation away from `case_when` mid-edit causes full loss; happens repeatedly in teaching workflows.
9. **Browser-validate code-complete items** — confirm `35f4b5a6` (PlotBuilder resize, Q-Q / residual plots, frozen diagnostics) and `5e105529` (spatial heatmap) in Franco's browser; strike through in ClaudeFB.md once confirmed.
10. **Confirm N-way FE + interactions UI** — BugTriage marks these "likely fixed"; a browser check takes minutes and closes 2 open items.
