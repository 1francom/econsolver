# Feedback Analysis Report — 2026-06-01

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 11 open bugs · 9 open features · 0 open UX/Design · 1 open performance · 1 open other

---

## Bugs

### [high] · Data · 2026-05-31
**Feedback:** Parse error: schools_with_coordinates.xlsx — Failed to fetch dynamically imported module: https://cdn.sheetjs.com/xlsx-0.20.3/package/xlsx.mjs (SheetJS CDN dynamic import failing at runtime)
**File:** `src/DataStudio.jsx:96` and `index.html:15–19`
**Suggestion:** The `index.html` importmap maps the SheetJS CDN URL to itself. When `Cross-Origin-Embedder-Policy: require-corp` is active (set in `vercel.json` for DuckDB-Wasm), the CDN module fetch is blocked because `cdn.sheetjs.com` does not send a `Cross-Origin-Resource-Policy` header. Fix: remove the two CDN importmap entries for SheetJS from `index.html` (lines 15 and 19). `DataStudio.jsx:96` already uses the bare `import("xlsx")` specifier which Vite resolves from `node_modules` (v0.18.5 in `package.json`) and bundles at build time — no CDN fetch occurs in the Vite production artifact. The importmap entry is therefore both unnecessary and harmful under COEP.
**Invariants:** COOP/COEP headers in `vercel.json` must be preserved for DuckDB-Wasm SharedArrayBuffer support.

---

### [high] · Data · 2026-05-13
**Feedback:** .dta files are wrong loaded and readed
**File:** `src/services/data/parsers/stata.js` (main parser entry at line 221; old-format path at line 61)
**Suggestion:** The old-format path (`stata.js:61–217`) handles formats 113–116; the new-format path (`stata.js:221+`) only accepts 117–119. If a format-116 file's first bytes partially match the XML `<stata_dta>` tag check, it may fall into the new path and fail with a misleading error. Add a `console.warn` at the format-dispatch boundary (lines 230–238) logging the raw `releaseStr` to aid reproduction. Also verify that variable-type `65526` (64-bit float, Stata 14) is handled correctly in the data-read loop, and that the `le` (little-endian) flag is correctly derived for both old and new format code paths.
**Invariants:** Parser lives in `src/services/data/parsers/` — no React imports.

---

### [high] · Data · 2026-05-31
**Feedback:** OECD proxy error 406 for OECD.SDD.NAD,DSD_NAAG@DF_NAAG_I — server responded with 406 Not Acceptable; Accept header sent does not match any SDMX format the endpoint supports
**File:** `src/services/data/fetchers/oecd.js:182–184`
**Suggestion:** `fetchOECDIndicator` sends only `"Content-Type": "application/json"` with no `Accept` header. The OECD SDMX-JSON API requires `Accept: application/vnd.sdmx.data+json; version=1.0`. Add `"Accept": "application/vnd.sdmx.data+json; version=1.0"` to the fetch headers at line 183. If the Supabase `oecd-proxy` Edge Function strips or overrides the `Accept` header, it must be updated to forward it; alternatively, pass it as a JSON body field that the proxy appends on the server side.
**Invariants:** All data fetching goes through the Supabase proxy — the fix must be confirmed to work end-to-end through the proxy, not just in the client-side fetch call.

---

### [high] · Spatial · 2026-05-20
**Feedback:** Grids do not work
**File:** `src/components/tabs/SpatialTab.jsx`, `src/math/SpatialEngine.js:366–401` (assignRectGrid / assignH3Grid), `src/math/SpatialEngine.js:794–807` (makeGrid)
**Suggestion:** `assignRectGrid` and `assignH3Grid` compute string cell IDs and `makeGrid` produces WKT POLYGON cells, but the downstream rendering path in `SpatialTab` may not treat a grid-row dataset as a renderable layer type. Trace the "Generate Grid" button handler in `SpatialTab` to confirm (1) `makeGrid` is actually called and its return value is non-empty, (2) the resulting rows are stored in component state, and (3) the Leaflet/PlotBuilder render path accepts a `grid_id + geometry` dataset as a polygon layer. Add a defensive `console.error` if `makeGrid` returns an empty array.
**Invariants:** `src/math/SpatialEngine.js` must have zero React imports.

---

### [critical → needs-repro] · Model · 2026-05-21
**Feedback:** **URGENT** There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible
**File:** `src/math/LinearEngine.js:80–86` (pValue function), `src/math/EstimationResult.js:208–295` (testStats → pVals)
**Suggestion:** Prior audit (BugTriage 2026-05-26) found no code-level desync — `pValue(0.49, 100) = 0.625` matches R. The CoeffTable column layout is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig`, so the "0.49" the user saw was most likely the β̂ column, not the t column. Highest-value action: ask Franco for a screenshot with column headers visible and the estimator name. If a genuine bug is confirmed, the investigation should focus on the `testStats` clean step in `EstimationResult.js:251` and whether `pVals` from the engine is being overwritten at any normalisation step.
**Invariants:** SE type must be passed explicitly. `pValue()` in `LinearEngine.js` is the single source of two-tailed p values and should not be duplicated.

---

### [high] · Model · 2026-05-28
**Feedback:** Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators (currently only generic TWFE event study, which is biased under staggered treatment)
**File:** `src/math/PanelEngine.js` (event study entry at line 222), `src/components/modeling/EstimatorSidebar.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** This is a substantial math engine addition. Sun-Abraham (2021) uses interaction-weighted estimation: interact cohort dummies × relative-time dummies, then aggregate using cohort-share weights. Callaway–Sant'Anna (2021) computes group-time ATTs via doubly-robust or outcome-regression/IPW, then aggregates. Suggested plan: (1) add `runSunAbraham(rows, yCol, unitCol, timeCol, treatCol, kPre, kPost, seType)` and `runCallawaySantanna(rows, yCol, unitCol, timeCol, firstTreatCol, kPre, kPost, seType)` to `PanelEngine.js` as pure-JS functions; (2) add two new grouped estimator entries in `EstimatorSidebar.jsx`; (3) wire dispatch and config in `ModelingTab.jsx`; (4) validate against R `did` and `sunab` packages before shipping.
**Invariants:** `src/math/PanelEngine.js` must have zero React imports. `seType` must be passed explicitly — never hardcoded.

---

### [high] · Report · 2026-05-25
**Feedback:** AI does not recognize the pipeline steps in the Script generation
**File:** `src/services/AI/sessionSnapshot.js:68–115`, `src/ReportingModule.jsx:959–962`
**Suggestion:** The snapshot built at `ReportingModule.jsx:961` calls `buildSessionSnapshot({ cleanedData, result: rawResult })`. `buildSessionSnapshot` reads `cleanedData?.pipeline` — but `cleanedData` may not carry a populated `pipeline` array if `WranglingModule.onComplete` fires before any pipeline steps are added, or if `DataStudio.jsx` does not thread the pipeline through. Verify `WranglingModule.jsx:398` `onComplete` payload includes the live `pipeline` state, and confirm `DataStudio.jsx:945` passes it through. This is still listed as VALIDATION PENDING in CLAUDE.md (item 5b) — Franco should validate on Vercel with a `;`-delimited CSV to confirm the unified R/Python/Stata scripts emit the correct load call and pipeline steps.
**Invariants:** All Anthropic API calls go through `AIService.js` only.

---

### [medium] · Model · 2026-05-21
**Feedback:** When selecting and eliminating variables, the model reestimates automatically — fix that
**File:** `src/components/ModelingTab.jsx` (useEffect blocks at lines 589, 1529, 1549, 1554, 1692, 1695)
**Suggestion:** Audit the `useEffect` blocks in `ModelingTab.jsx` for any that call `estimate()` or a derived estimation trigger in response to `xVars` or `yVar` changes. Remove `xVars`/`yVar` from any auto-run effect's dependency array and require an explicit "Estimate" button press. If some auto-run is intentionally kept (e.g. for live model preview), gate it behind an `autoRun` boolean state that defaults to `false` and is only set by user action.
**Invariants:** None specific — pure UI change.

---

### [medium] · General · 2026-05-20
**Feedback:** When a dataset is deleted, all variables should disappear from all modules without having to select another dataset
**File:** `src/components/workspace/DatasetManager.jsx:62–122`, `src/services/session/sessionState.jsx:113`, `src/components/ModelingTab.jsx`
**Suggestion:** `removeDataset` in `sessionState.jsx:113` updates the dataset registry but `ModelingTab` holds its own `yVar`/`xVars` state independently. Add a `useEffect` in `ModelingTab.jsx` that watches the active dataset ID from `useSessionState()` and, when it changes to `null` or to a dataset whose column list no longer contains the selected variables, resets `yVar`, `xVars`, `wVar`, and `result` to their initial empty values. Similar guards may be needed in `ExplorerModule.jsx` for column selector state.
**Invariants:** Use `sessionState` context — do not introduce a separate event bus or localStorage.

---

### [low] · Model · 2026-05-21
**Feedback:** Add an option to clean the current model
**File:** `src/components/ModelingTab.jsx:1529–1534`, `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** A "Clear" button in the estimator header should call `setYVar([])`, `setXVars([])`, `setResult(null)` — the state reset sequence already exists at lines 1533–1534. Add a small `✕ Clear` button in `EstimatorSidebar.jsx` (or in the variable selector header row) that fires these setters. No new state or logic is required.
**Invariants:** Inline styles only via the `C` object from `src/components/modeling/shared.jsx`.

---

## Features

### Model · 2026-05-11
**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength
**File:** `src/math/CausalEngine.js:14–28` (math complete), `src/components/ModelingTab.jsx:907–923` (partial display)
**Suggestion:** The Anderson-Rubin math is already implemented (`andersonRubinCI` at `CausalEngine.js:28`) and is partially surfaced in `ModelingTab.jsx:907` — but gated on `xVars.length === 1`. Improve discoverability: (1) always render the AR CI row when the estimator is 2SLS, with a tooltip explaining it is only computable for the single-endogenous-regressor case; (2) add a `[Weak-IV robust]` label badge to the CI row to distinguish it from the standard Wald CI.
**Invariants:** Math in `src/math/CausalEngine.js` — no React. UI display changes only in `ModelingTab.jsx`.

---

### Model · 2026-05-11
**Feedback:** Add multiple statistics tests
**File:** `src/components/modeling/DiagnosticsPanel.jsx`, `src/math/GMMEngine.js` (J-stat already computed), `src/core/diagnostics/`
**Suggestion:** The request is underspecified — likely refers to hypothesis tests beyond existing diagnostics. Recommended scope: (1) surface the J-stat (already in GMM/2SLS result objects) prominently in `DiagnosticsPanel.jsx`; (2) add a Hausman endogeneity test comparing FE vs pooled OLS using existing `PanelEngine.runFE` and `LinearEngine.runOLS` outputs (can be computed from already-available result objects); (3) add weak-instrument Cragg-Donald F to the 2SLS first-stage display. New test math goes in `src/core/diagnostics/` — pure JS, no React.
**Invariants:** `src/core/diagnostics/` — zero React imports.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/SpatialTab.jsx`, `src/math/SpatialEngine.js:794–807` (makeGrid exists), `src/components/PlotBuilder.jsx`
**Suggestion:** `makeGrid` already generates rectangular WKT POLYGON cells. The gap is rendering: grids are only displayed in the Leaflet OMS map, not in `PlotBuilder`. Add an "Export grid as layer" action in `SpatialTab` that saves the grid rows to session state and passes them to `PlotBuilder.jsx` as a `geom: "polygon"` layer (PlotBuilder already renders WKT POLYGON via `Plot.geo()`).
**Invariants:** `src/math/SpatialEngine.js` — zero React imports.

---

### Simulate · 2026-05-18
**Feedback:** Add resampling, bootstrapping, sampling to the mean
**File:** `src/components/tabs/SimulateTab.jsx`
**Suggestion:** Add a "Bootstrap" mode tab alongside the existing Monte Carlo and Single Variable modes. The user picks a variable and a statistic (mean / median / regression coefficient), sets B (default 1 000) and optional block size for time series. The loop resamples rows with replacement B times, computes the statistic, and plots the bootstrap distribution using the existing `MCHistogram` component. No new math file needed — the resampling loop is simple enough for `SimulateTab.jsx` directly.
**Invariants:** Inline styles via `C` from `src/components/wrangling/shared.jsx`.

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/pipeline/runner.js` (filter step), `src/pipeline/duckdbRunner.js`
**Suggestion:** Add an `in` operator to `FilterBuilder`'s operator dropdown accepting a comma-separated list of values. In the JS `runner.js` filter step, evaluate as `Set(values).has(String(row[col]))`. In `duckdbRunner.js`, emit `WHERE col IN ('v1','v2',...)`. Also add `not in` as a symmetric complement. Register no new step type — `in` is a new operator on the existing `filter` step.
**Invariants:** Pipeline steps replay on `rawData` — never mutate. `registry.js` does not need updating since no new step type is added.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/ModelingTab.jsx` (RDD results section), `src/math/CausalEngine.js` (runSharpRDD return)
**Suggestion:** The `runSharpRDD` result already contains bandwidth, SE, CI, and polynomial order. Add two extra fields to its return: `n_left` (count of rows where `running < cutoff` within bandwidth) and `n_right` (rows where `running >= cutoff` within bandwidth). Then render an `RDDSummaryTable` inline block in `ModelingTab.jsx` that mirrors the `rdrobust` CLI output: Cutoff, h (bandwidth), Poly. Order, n_left, n_right, Estimate, SE, z, P>|z|, 95% CI. This is mostly a display improvement using existing result fields.
**Invariants:** Math addition in `src/math/CausalEngine.js` — no React. Display in `ModelingTab.jsx`.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept
**File:** `src/math/LinearEngine.js:120` (OLS design matrix), `src/math/LinearEngine.js:199` (WLS), `src/components/ModelingTab.jsx`, `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** At `LinearEngine.js:120`, `X = valid.map(r => [1, ...xCols.map(c => r[c])])` always prepends an intercept. Add `noIntercept = false` to `runOLS`, `runWLS`, and the DuckDB suff-stats OLS/WLS path. When `true`: omit the leading `1`; adjust `k` to `xCols.length`; use raw (uncentred) TSS for R² and flag it via `_r2Note: "uncentred"`; keep the same `df = n - k` formula. Expose as a checkbox in `ModelConfiguration.jsx` and thread through `ModelingTab.estimate()`. Apply consistently to FE/FD/2SLS paths.
**Invariants:** `src/math/` — zero React imports. `seType` still passed explicitly — the no-intercept flag is a separate, independent parameter.

---

### Spatial · 2026-05-30
**Feedback:** Geometry transformations and show EPSG
**File:** `src/math/SpatialEngine.js:24–34` (normalizeCrs), `src/components/tabs/SpatialTab.jsx`
**Suggestion:** `normalizeCrs` already resolves CRS strings to `EPSG:XXXX`. Surface the result in `SpatialTab` as a small `CRS: EPSG:4326` badge next to the geometry column selector. For reprojection, add a `reproject(rows, geomCol, fromEPSG, toEPSG)` function in `SpatialEngine.js` using `proj4` (available via `unpkg.com` — already in `vercel.json` CSP's `script-src`). Start with the common `EPSG:4326 ↔ EPSG:3857` pair; add a UI dropdown in `SpatialTab` to select the target CRS.
**Invariants:** `src/math/SpatialEngine.js` — zero React imports. Any new CDN script must be added to `vercel.json` CSP.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/SpatialTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `spatial_filter` pipeline step to `runner.js` that accepts a WKT bounding polygon and filters rows whose lat/lon fall inside it using `pointInPolygon`. In `SpatialTab`, add a "Use selection as pipeline filter" button on drawn bounding boxes or selected polygon features that emits the step to the pipeline with the drawn geometry serialised as WKT. Register in `registry.js` and add a display card in `CleanTab.jsx` or a new spatial-step section.
**Invariants:** Pipeline steps replay on `rawData` — never mutate. Both `runner.js` and `registry.js` must stay in sync. New step math goes in `src/math/SpatialEngine.js` — no React.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx:286–344` (large-file routing), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Per CLAUDE.md DuckDB performance strategy (rules 2–3): (1) Implement auto-convert-to-Parquet on first large-CSV load: after `loadLargeCSV` succeeds, run `COPY (SELECT * FROM tbl) TO '/opfs/${pid}_${name}.parquet' (FORMAT PARQUET)` and cache the OPFS path in `parquetCache`; on subsequent opens, load directly from OPFS. (2) Ensure Explorer and wrangling tabs never materialise more than 500 rows in JS state — confirm the `rawData._duckdb` flag check in `WranglingModule.jsx` is active and that the DuckDB runner is used for all pipeline step types in the large-data path. (3) Confirm `vercel.json` `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` headers are present for SharedArrayBuffer / threaded DuckDB-Wasm.
**Invariants:** Data never leaves the browser. Display limit ≠ computation limit — pipeline steps always run on the full dataset.

---

## Other

### Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...
**File:** `src/DataStudio.jsx:37–91` (parseCSV), `src/services/data/duckdb.js` (loadLargeCSV — DuckDB path for large files)
**Suggestion:** This error originates in DuckDB's CSV reader (file is large enough to route through DuckDB). Line 1583 likely contains an unescaped quote or a numeric value that DuckDB's type inference misclassifies. Fix in `loadLargeCSV`: add `IGNORE_ERRORS=TRUE` as a fallback retry option, or pass `QUOTE '"', ESCAPE '"'` explicitly to `read_csv_auto`. For the JS fallback `parseCSV` at `DataStudio.jsx:37`, the current implementation has no RFC 4180 quote handling — add proper quoted-field parsing to handle `"value, with, commas"` fields.
**Invariants:** None.

---

## Suggested action order

1. **Fix SheetJS CDN import (high · Data · 2026-05-31)** — Excel uploads are completely broken; remove two importmap lines in `index.html:15,19` — two-line delete.
2. **Fix OECD 406 Accept header (high · Data · 2026-05-31)** — add `"Accept": "application/vnd.sdmx.data+json; version=1.0"` in `oecd.js:183`; one-line fix restores all OECD fetching.
3. **Validate and close AI script pipeline recognition (high · Report · 2026-05-25)** — CLAUDE.md 5b is VALIDATION PENDING; trace `cleanedData.pipeline` through `WranglingModule.onComplete` → `DataStudio` → `ReportingModule` snapshot; run Franco's validation scenario on Vercel.
4. **Reproduce p-value URGENT report (critical → needs-repro · 2026-05-21)** — request screenshot from Franco; almost certainly a column-label confusion but must be ruled out formally.
5. **Fix auto-reestimate on variable toggle (medium · Model · 2026-05-21)** — remove `xVars`/`yVar` from auto-run `useEffect` dependency arrays in `ModelingTab.jsx`.
6. **Fix dataset-delete variable bleed (medium · General · 2026-05-20)** — add dataset-change `useEffect` in `ModelingTab.jsx` to reset variable selectors on deletion.
7. **Fix Spatial grids (high · Spatial · 2026-05-20)** — trace `makeGrid` → `SpatialTab` render path and confirm grid rows reach the Leaflet layer renderer.
8. **Fix .dta loading regression (high · Data · 2026-05-13)** — add format-dispatch logging, reproduce with a format-116 file, verify type-65526 float handling.
9. **Sun-Abraham / Callaway-Santana event study (high · Model · 2026-05-28)** — large math sprint; requires R validation fixtures against `did` and `sunab` packages.
10. **Add no-intercept option (feature · Model · 2026-05-25)** — thread `noIntercept` flag through all engines and DuckDB suff-stats path.
