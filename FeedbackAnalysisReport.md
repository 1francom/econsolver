# Feedback Analysis Report — 2026-06-30

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- 2 confirmed open bugs (BugTriage status = open) · 5 code-complete bugs awaiting browser validation · 1 open performance item (pending validation) · 1 genuinely new feature (not triaged) · 6 explicitly deferred (fix-later) items · ~35 stale ClaudeFB.md records that are resolved in BugTriage but not marked done

---

## Bugs

### critical · Data · 2026-06-26
**Feedback:** Parse error: crime_panel_final_r_300.rds — not a valid R data file (.rds), unrecognised serialisation header.
**File:** `src/services/data/parsers/rds.js`
**Suggestion:** The parser handles gzip (`0x1F 0x8B`) and plain XDR (`0x58 0x0A`) but not bzip2 (`0x42 0x5A 0x68`) or zstd (`0x28 0xB5 0x2F 0xFD`), both of which R uses with `saveRDS(..., compress="bzip2")` or `compress="xz"`. Add a `decompressBzip2` branch (using a wasm shim such as `fflate` loaded via CDN, or `DecompressionStream` once browsers support it) and a zstd/xz path, before the final "Unrecognized magic" throw. Also detect lzma/xz magic `0xFD 0x37 0x7A 0x58 0x5A 0x00`. Improve the error message to show detected magic bytes so users can self-diagnose.
**Invariants:** `rds.js` is in `src/services/data/parsers/` — zero React imports. Decompression must stay entirely client-side; no server round-trip.

---

### critical · Data · 2026-06-26
**Feedback:** Parse error: grid_enriched_500.rds — not a valid R data file (.rds), unrecognised serialisation header.
**File:** `src/services/data/parsers/rds.js`
**Suggestion:** Same root cause as the item above — bzip2/xz compression not handled by the current parser. The fix is identical: add the missing compression branches inside `parseRDS`. Both .rds failures should be addressed in a single pass.
**Invariants:** Same as above.

---

### high · Model · 2026-06-10 (code-complete, pending browser validation)
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx` (line 1626: resize handle `<div>`), `src/components/ModelingTab.jsx` (lines 3432–3438: PlotBuilder mount)
**Suggestion:** Code in commit `35f4b5a6` is reportedly complete. Validation blocked on a browser smoke-test by Franco: (1) drag the resize handle and confirm the plot redraws at the new dimensions via the `ResizeObserver`; (2) export PNG and confirm it captures the user-set viewport without clipping. If PNG still shows the pre-resize size, the PNG export path likely reads a cached constant height instead of the wrapper's live `getBoundingClientRect()`.
**Invariants:** No hard-coded pixel heights in inline styles — height must be driven by React state / ResizeObserver.

---

### high · Model · 2026-06-11 (code-complete, pending browser validation)
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ResidualPlots.jsx` (exports `ResidualVsFitted`, `QQPlot`), `src/components/ModelingTab.jsx` (lines 2529–2531: OLS/WLS wiring)
**Suggestion:** Components exist and are wired. Commit `35f4b5a6` fixed the root regression: OLS/WLS display now reads from the frozen `result.spec`, so sidebar edits no longer wipe the plots. Browser smoke-test: run an OLS, confirm all three plots appear, then add a variable to the selector and confirm the plots remain frozen until the user explicitly re-estimates.
**Invariants:** `ResidualPlots.jsx` has no React imports from `src/math/`. The `resid` and `Yhat` arrays must come from the frozen estimation result, not recomputed on render.

---

### high · Model · 2026-06-14 (code-complete, pending browser validation)
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx` (estimate useCallback, result state, pinned-model restore path)
**Suggestion:** Commit `35f4b5a6` addresses this. Validate: (1) after estimation, toggle a variable and confirm displayed coefficients and diagnostics do not refresh; (2) pin the model, clear the form, restore the pin, and confirm all plots reappear identically. If re-estimation fires on variable-selector change, the `estimate` callback is likely wired to `setXVars` — those two must be decoupled.
**Invariants:** Variable selection state must be fully decoupled from displayed-result state. `estimate()` must only run on an explicit user action.

---

### medium · Spatial · 2026-06-13 (code-complete, pending browser validation)
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` (lines 20–25, 224–226, 801–803), `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Commit `5e105529` restored the heatmap as a Leaflet KDE layer. Browser check: add a point dataset with lat/lon columns, add a Heatmap layer, verify rendering, toggle palette/opacity, export HTML. If cells are blank, check whether `buildHeatmapCells` receives the full DuckDB table or only the 500-row UI preview — sparse datasets will appear empty if truncated. Pass the full table reference, not the preview slice.
**Invariants:** Heatmap computation must be client-side only. No server egress for data.

---

## Performance

### high · Data · 2026-05-08 (code-complete, browser benchmark pending)
**Feedback:** Big datasets generate crashes (21MB) over 900k rows.
**File:** `src/services/data/duckdb.js`, `src/services/data/suffStatsCache.js`, `src/pipeline/duckdbRunner.js`, `vercel.json`
**Suggestion:** Fase X4 implementation is code-complete per `docs/superpowers/plans/2026-05-22-fase-x4-performance.md`. Remaining step is Franco's browser benchmark using `src/services/data/__validation__/faseX4BrowserChecklist.md`. Key checks: (a) COOP/COEP headers active in `vercel.json` so `SharedArrayBuffer` is available for threaded DuckDB-Wasm; (b) tab transitions under 5 s; (c) `suffStatsCache` LRU stays under its memory ceiling over a 100-estimation loop; (d) OPFS Parquet cache loads on project reopen without re-parsing the CSV. This item remains open until browser-confirmed.
**Invariants:** Privacy-first — data never leaves the browser. No server-side DuckDB.

---

## Features

### low · Calculate · 2026-06-19 (genuinely new — not yet triaged)
**Feedback:** Add limits.
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** CalculateTab already hosts derivatives, integrals, and a CAS workbench. Add a "Limit" operation type: inputs are the expression, the variable, and the approach point (supporting ±∞). Use the existing CAS library to evaluate `lim_{x→a} f(x)` symbolically; fall back to a two-sided numerical estimate (`f(a ± ε)` for small ε). The `\\lim` toolbar shortcut (line 309) already exists — it just needs a corresponding evaluation path. Display the result in the existing result panel with the usual KaTeX rendering.
**Invariants:** Style via the `C` object from `src/components/wrangling/shared.jsx`. No new external libraries unless loaded via CDN in the existing pattern.

---

## Fix-Later Items (explicitly deferred — log for roadmap)

### medium · Data · fix-later
**Feedback:** Parse error on line 1583 (China,CHN,2010,...) — malformed CSV row aborts the entire import instead of surfacing recovery options. (2026-05-10 · BugTriage 2026-06-14 Data)
**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/`
**Suggestion:** When the DuckDB CSV reader throws on a malformed row, catch the error, re-read the file while skipping the offending row, and surface a recovery UI: "Row 1583 could not be parsed. Skip and continue? Preview the row?" Retain the original row number in the user message. The skip decision should be captured as an explicit `drop` pipeline step, not a silent `rawData` mutation.
**Invariants:** Non-destructive pipeline — skip decisions flow into the pipeline, never mutate `rawData` in place.

---

### medium · Model · fix-later
**Feedback:** Add an option to estimate without intercept. (2026-05-25)
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`
**Suggestion:** Add a "No intercept" checkbox in ModelConfiguration.jsx; pass `suppressIntercept: true` through the spec object to `runOLS` / `runWLS`; in LinearEngine.js, conditionally omit the leading ones column when building the design matrix. Update R/Python/Stata replication scripts to emit `lm(y ~ x - 1, ...)` equivalents. All engines that prepend a constant column need the same gate, so scope the change engine-wide.
**Invariants:** `seType` must still be passed explicitly. No intercept-related hardcoding inside engine functions. `EstimationResult` shape and spec export must remain compatible.

---

### low · Model · fix-later
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — Call, bandwidth, N obs, bias-corrected LATE, CI. (2026-05-21)
**File:** `src/components/ModelingTab.jsx` (RDD result display section), `src/math/CausalEngine.js`
**Suggestion:** Pure presentational addition — no math change needed. After `runSharpRDD` returns, render a compact table modelled on `rdrobust`'s console output: bandwidth (left/right), polynomial order, kernel, N obs left/right, bias-corrected LATE, robust SE, t-stat, p-value, CI bounds. All these values are already present in the `EstimationResult` from `CausalEngine`.
**Invariants:** None beyond the standard inline-style convention (no CSS files).

---

### low · Model · fix-later
**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows "D (treatment)", "running − c", etc. regardless of actual column names. (2026-06-06)
**File:** `src/components/modeling/ModelComparison.jsx` (buildStargazer path), `src/ReportingModule.jsx`
**Suggestion:** Store a `latexLabels: { [colName]: string }` map in the model spec. In the LaTeX export path, substitute column names with their overrides. Expose an editable label column in the coefficient table (pencil icon → inline text input), persisted per-model in the model buffer in IndexedDB via `indexedDB.js`.
**Invariants:** Labels are display-only metadata — must never feed back into estimation logic. Persist via `indexedDB.js`, never `localStorage`.

---

### low · Clean · fix-later
**Feedback:** Add support for R-style `%in% c("Spain", "Basque Country")` membership syntax in the filter UI. (2026-05-19)
**File:** `src/services/exprEvalService.js`, `src/workers/exprEval.worker.js`
**Suggestion:** In the worker's expression evaluator, pre-process filter expressions before passing to `new Function(...)`: rewrite `x %in% c("a", "b")` → `["a","b"].includes(x)` using a simple regex substitution. Inject an `isin(x, ...vals)` helper into the eval scope as a JS-idiomatic alternative. The rewrite must happen inside the Worker to preserve sandboxed eval isolation.
**Invariants:** The runner.js `filter` step must continue to replay non-destructively on `rawData`. No changes to runner.js itself — this is purely a worker-side expression pre-processing step.

---

### low · Spatial · fix-later
**Feedback:** Allow spatial filters for plots. (2026-05-30)
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`
**Suggestion:** Add a per-layer filter row in `SpatialGeoPlot.jsx`'s layer config panel (column selector + operator + value), producing a JS predicate applied to `lyRows(ly)` before the draw pass. The filter atom pattern from CleanTab's FilterBuilder is reusable — consider extracting a shared filter-row component to `src/components/tabs/spatial/shared/atoms.jsx`.
**Invariants:** Filter applied to in-memory rows only — must not mutate the IndexedDB dataset. Styles via the `C` object.

---

## Suggested action order

1. **Fix .rds bzip2/xz/zstd compression** (`src/services/data/parsers/rds.js`) — two confirmed-open critical bugs blocking users from loading standard R panel datasets; single-file fix, high impact.
2. **Browser-validate commit `35f4b5a6`** (PlotBuilder resize, OLS diagnostic plots, diagnostics-freeze fix) — four high-severity items blocked on Franco's smoke-test; no code change needed, just confirmation.
3. **Browser-validate spatial heatmap (`5e105529`) and Fase X4 performance** — clears the remaining two code-complete items and closes the 900k-row performance complaint.
4. **Add calculator limits** (`src/components/tabs/CalculateTab.jsx`) — only genuinely new unplanned feature; small and self-contained.
5. **No-intercept estimation** (`ModelConfiguration.jsx` + `LinearEngine.js`) — medium-effort fix-later with engine-wide scope; schedule for the next feature sprint.
6. **Resilient CSV import** (`src/DataStudio.jsx`) — fix-later; prevents the malformed-row abort from silently discarding a user's dataset.
7. **ClaudeFB.md records cleanup** — ~35 items are resolved per BugTriage but remain syntactically open in ClaudeFB.md; add ~~strikethrough~~ ✓ markers to prevent them from polluting future analysis runs.
