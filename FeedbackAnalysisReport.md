# Feedback Analysis Report — 2026-06-11

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **4 open bugs** (1 critical · 1 high · 2 medium)
- **12 open features**
- **1 open performance issue** (overlaps with bugs — DuckDB bypass)
- **1 open other** (CSV parse error, no BugTriage entry)
- **6 stale items** — appear open in ClaudeFB.md but confirmed fixed in BugTriage or by code inspection (see table at end)

---

## Bugs

### [critical] · Data · 2026-05-13 (REOPENED 2026-06-10)

**Feedback:** `.dta files are wrong loaded and readed` — dates not converted; some numeric columns show garbage values (unemp=3e−34, female=−123, edyrs=−10) for a 420k×16 `.dta` file.

**File:** `src/services/data/parsers/stata.js` lines 285–385 (format 117–119 parser path)

**Suggestion:** The format 117–119 parser (lines 221–385) reads `<variable_types>`, `<varnames>`, and `<data>` but skips the `<formats>` XML section entirely. The legacy format 113–116 parser (lines 61–213) correctly reads `fmtlist` at line 110 and builds a `dateConv` map using the Stata epoch `1960-01-01` (line 112) — the same pattern must be added to the new parser. After the `<varnames>` block, call `tagPos(bytes, '<formats>')`, read K null-terminated format strings (57 bytes each for formats 117/118/119), then build the `dateConv` map before the data loop at line 334. For the garbage numeric values, verify that `strL` type (32768) rows advance `pos` by exactly 8 bytes — the current `typeWidth` returns 8 for strL (line 322) which should be correct, but the `val = ''` branch does not advance `pos` correctly; confirm by checking that `pos += widths[c]` at line 375 fires even for the `strL` else branch. Obtain R ground-truth (`storage type` + spot-check values for `unemp`, `year`, `female`, `birthdate`) before patching the value-parsing branch.

**Invariants:** Parser lives in `src/services/data/parsers/stata.js`; zero React imports. No `rawData` mutation — fix is parse-time only.

---

### [high] · Performance / Data · 2026-06-10

**Feedback:** Large `.dta` (and `.rds`, `.xlsx`, `.shp`) files bypass DuckDB, causing ~15 s tab-transition lag (900 k × 16 columns ≈ 6.7 M JS cells re-rendered per switch).

**File:** `src/DataStudio.jsx` lines 403–406 (`.dta` branch calls `parseStata` directly with no size check and no DuckDB path); `src/services/data/duckdb.js` (`loadLargeCSV` is the only large-file path, CSV/TSV only)

**Suggestion:** After `parseStata(buf)` succeeds, check `result.rows.length >= 50_000` and register the JS rows into DuckDB by writing them as Parquet (via DuckDB's `INSERT INTO ... SELECT` from a JS-provided arrow batch, or by calling `db.registerFileText` with the rows serialised as NDJSON). Return a `_duckdb`-flagged dataset object matching the shape produced by `loadLargeCSV`. Apply the same pattern to `.rds` and `.xlsx`. This is consistent with CLAUDE.md DuckDB performance rule 1 ("DuckDB as the data boundary") and rule 2 ("Parquet as primary format for large data").

**Invariants:** Privacy-first — stays entirely client-side; no server egress. Non-destructive pipeline: `runner.js` is not touched.

---

### [medium] · Data · 2026-05-10

**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...` (truncated — likely a World Bank CSV)

**File:** `src/DataStudio.jsx` (small-file CSV path, `parseCSV` function); `src/services/data/duckdb.js` (`loadLargeCSV` — if file is >10 MB this path would be used instead)

**Suggestion:** If the file is <10 MB, the JS parser is used. A truncated numeric string on line 1583 suggests a non-UTF-8 byte, an unescaped quote inside a field, or a CRLF inside a quoted cell. Add two defensive measures: (1) strip BOM and normalise line endings (`\r\n` → `\n`, bare `\r` → `\n`) before parsing; (2) on parse failure after ≥100 rows successfully parsed, retry with `TextDecoder("latin-1")` and surface a warning banner. If the file is >10 MB and hits DuckDB, add `IGNORE_ERRORS=TRUE` as a retry option and surface a "N rows skipped" warning.

**Invariants:** Never mutate `rawData` in place. Warning banner must be visible to the user before any downstream pipeline step runs.

---

### [medium] · Model / UI · 2026-06-10

**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.

**File:** `src/components/PlotBuilder.jsx` lines 406, 1624–1627 (`PlotCanvas` receives hardcoded `height={320}` from parent); `src/components/ModelingTab.jsx` lines 2025 and 3167 (the two `<PlotBuilder>` mount sites)

**Suggestion:** In `ModelingTab.jsx`, wrap each `<PlotBuilder>` in a container `<div>` with `style={{ resize: "vertical", overflow: "auto", minHeight: 360 }}` (all inline, no CSS files). Add a `ResizeObserver` on that container — matching the pattern already used in `PlotBuilder.jsx` at line 1152 — to measure the current height and pass it down as a `containerHeight` prop. Inside `PlotBuilder`, use `containerHeight` instead of the hardcoded 320 when rendering `<PlotCanvas>`. For PNG export, `downloadCombinedPNG` in `plotExporter.js` should receive the container's `clientWidth`/`clientHeight` instead of the hardcoded values passed at line 1624.

**Invariants:** Inline styles only via the `C` object from `shared.jsx`; no CSS files or Tailwind.

---

## Features

### Model · 2026-05-11

**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength

**File:** `src/math/CausalEngine.js` lines 14–28 (`andersonRubinCI` already implemented), line 201 (already called and attached to result); `src/components/ModelingTab.jsx` 2SLS result display (~line 2987)

**Suggestion:** The math is already done — `andersonRubinCI` is called at line 201 and the `arCI` field is present on the 2SLS result object. The gap is display-only: add a row in the ModelingTab 2SLS result section that shows `AR 95% CI: [lo, hi]` when `result.arCI` is present, with a short tooltip noting it is robust to weak instruments. No engine changes needed.

**Invariants:** Zero React in `src/math/` — this is a UI-only addition.

---

### Model · 2026-05-11

**Feedback:** Add multiple statistics tests

**File:** `src/math/SampleTests.js`; `src/components/tabs/statsim/SampleTestPanel.jsx`

**Suggestion:** The existing `SampleTests.js` covers one-sample mean t, variance χ², and generic parameter t/z. Add: two-sample Welch t-test, Mann-Whitney U, one- and two-sample Kolmogorov-Smirnov, and Levene / Bartlett equal-variance tests. Wire each into `SampleTestPanel.jsx` as additional collapsible cards with column selectors. The math functions must be pure JS in `SampleTests.js` with no React imports.

**Invariants:** Zero React in `src/math/` — all new test functions in `SampleTests.js` only.

---

### Model · 2026-05-21

**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression

**File:** `src/components/ModelingTab.jsx` RDD result display (~lines 2987–3040)

**Suggestion:** Add a `<pre>`-styled block in the RDD result section that mirrors rdrobust's console output: `Call:`, cutoff, left/right effective N, BW selection method, kernel, VCE method, then a coefficient row with `Coef / SE / z / P>|z| / CI (95%)`. All data is already in the result object (`result.late`, `result.lateSE`, `result.lateP`, `result.h`, `result.nLeft`, `result.nRight`, `result.kernelType`). No math changes required — this is pure display.

**Invariants:** Inline styles only.

---

### Model · 2026-05-25

**Feedback:** Add an option to estimate without intercept

**File:** `src/math/LinearEngine.js` (~line 128, ones-column prepend); `src/components/modeling/ModelConfiguration.jsx`; `src/components/ModelingTab.jsx`; `src/pipeline/duckdbOLS.js` (SQL fast path)

**Suggestion:** Add `noIntercept?: boolean` to `runOLS` and `runWLS`. When `true`, skip prepending the column of ones and set df = `n − k` (not `n − k − 1`). Add a "Suppress constant" checkbox in `ModelConfiguration.jsx`. Thread the flag through `ModelingTab.estimate()` to both the JS and DuckDB paths. In `duckdbOLS.buildOLSSuffStats`, omit the `1.0` literal from the column expansion when `noIntercept` is set. R² without intercept uses `1 − SSR / Σyᵢ²` — flag this note in the displayed diagnostics.

**Invariants:** `seType` must still be passed explicitly; never hardcoded inside engine functions. `noIntercept` is a plain boolean parameter, not React state inside the engine.

---

### Model · 2026-06-06

**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows "D (treatment)", "running − c", "D × (running − c)" regardless of actual column names

**File:** `src/math/CausalEngine.js` lines 577–579 (`runSharpRDD` varNames array) and lines 844–847 (`runFuzzyRDD`); `src/math/EstimationResult.js` line 553

**Suggestion:** Replace the hardcoded strings with the actual column names already in scope: `"${treatVar} (treatment)"`, `"${runCol} − ${cutoff}"`, `"${treatVar} × (${runCol} − ${cutoff})"`. Apply the same substitution in `runFuzzyRDD` (lines 844–847). This single-line change propagates automatically to the coefficient table and LaTeX export without further UI work. For user-level renaming in the LaTeX output, additionally expose a small `coefLabels` state map in `ReportingModule.jsx` (similar to Stargazer's `covariate.labels` argument) — a list of text inputs, one per coefficient, pre-filled with the engine-produced names and editable before copy.

**Invariants:** Label substitution only — no matrix algebra touched. Zero React in `src/math/`.

---

### Data · 2026-06-01

**Feedback:** Add an option to edit/assign column names directly in the Data Viewer

**File:** `src/ExplorerModule.jsx` (data viewer column header rendering)

**Suggestion:** Make column header cells double-click-editable: on `dblclick`, replace the `<span>` with a controlled `<input>` pre-filled with the current header name; on `blur` or `Enter`, emit a `rename` pipeline step (`{ type: "rename", from: old, to: new }`) via the `onStep` callback. The `rename` step already exists in `runner.js` and `registry.js`, so no new pipeline step is needed.

**Invariants:** Must dispatch a `rename` pipeline step — never mutate `rawData` directly. Inline styles only.

---

### Explore · 2026-05-19

**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option

**File:** `src/components/wrangling/CleanTab.jsx` (`FilterBuilder`, line 606: `"in list"` operator already exists); `src/ExplorerModule.jsx` (no filter bar present)

**Suggestion:** The `in` / `nin` operators already work in `CleanTab.jsx`'s `FilterBuilder` (line 606), with category chips for multi-selection (lines 693–702). The UX issue: this feature lives in the Clean tab, not the Explore tab where users expect it. Add a lightweight `ExplorerFilter` bar at the top of `ExplorerModule.jsx`: a column selector + the same `in` / `nin` chip-picker pattern from `CleanTab.jsx`. Apply as a local display filter (a derived `rows.filter(...)` copy) — it does not add a pipeline step.

**Invariants:** Non-destructive view-only filter; never mutates `rawData`. Inline styles via `C`.

---

### Spatial · 2026-05-30

**Feedback:** Geometry transformations and show EPSG

**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx` lines 13–14 (only two hardcoded EPSG options); `src/components/tabs/spatial/shared/crs.js` (`PRESET_CRS`)

**Suggestion:** Extend the CRS source/target selectors from a closed two-option dropdown to a free-text + searchable-preset combo. Expose `PRESET_CRS` from `crs.js` as a labelled dropdown (e.g. "EPSG:4326 — WGS84", "EPSG:32632 — UTM 32N"). Display a CRS badge next to geometry column headers in the Analyze tab when `rawData._crs` is set. `crs.js` must remain zero-React; badge logic lives in the component layer only.

**Invariants:** `crs.js` must remain zero-React.

---

### Spatial · 2026-05-30

**Feedback:** Allow spatial filters for plots

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/map/SpatialPlotTab.jsx`

**Suggestion:** Add a bounding-box filter panel to both the Geo Plot and Map tabs: min/max lat/lon inputs that produce a `rows.filter(...)` derived array passed to `mkGeoLayer`/`mkSLayer`. A more powerful variant uses a reference WKT polygon as a clip mask checked with a `pointInRing` function (leverageable from `wkt.js`). Filtering must use a local derived copy — never mutate `rawData`.

**Invariants:** View-only filter; no pipeline mutation. Inline styles.

---

### Spatial · 2026-05-20

**Feedback:** Add grids to the plot, not only in the OMS map

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/plot/geo.js`; `src/math/SpatialEngine.js` (`makeRectGrid` already implemented)

**Suggestion:** Add a "Grid overlay" layer type in `GeoLayerConfig.jsx`. When enabled, call `makeRectGrid` from `SpatialEngine.js` on the current bounding box and render the resulting rectangles as thin semi-transparent SVG `<polygon>` elements beneath the data-point layer in `SpatialGeoPlot.jsx`. The existing WKT-to-SVG path (`parseWktRings` in `wkt.js`) can render the grid cells without new geometry code.

**Invariants:** Zero React in `src/math/` — `makeRectGrid` stays pure JS. SVG rendering stays in the component.

---

### Calculate · 2026-06-03

**Feedback:** Add a "show steps button" for integrals, derivatives and optimizations

**File:** `src/components/tabs/CalculateTab.jsx`

**Suggestion:** Extend the derivative and optimization utility functions to optionally return `{ result, steps: string[] }` alongside the numeric/symbolic result. In `CalculateTab.jsx`, add a collapsible "Show working" panel that renders the steps array as a monospace list. For numerical integration, show the quadrature intervals and partial sums. The math utilities must remain zero-React; `CalculateTab.jsx` consumes the `steps` array for display only.

**Invariants:** Math utility files must remain zero-React.

---

### Calculate · 2026-06-06

**Feedback:** Add derivatives with endogenous functions (e.g. `f(x) = 2x² − c(x)`, compute `dc/dx` symbolically)

**File:** `src/components/tabs/CalculateTab.jsx` (symbolic differentiation imports)

**Suggestion:** First test `derivative("2*x^2 - c(x)", "x")` with the existing CAS engine — the header comment (line 9: "unknown functions, product/chain rule") implies it may already work, producing `4*x - c'(x)`. If it does, close the request and update ClaudeFB.md. If not, extend the differentiation engine to treat `f(arg)` patterns as opaque symbols and emit `f'(arg) * arg'` via chain rule. All changes in the math utility file; zero React.

**Invariants:** Zero React in math utility files.

---

### Model · 2026-06-04

**Feedback:** Persistence across sessions of pinned models

**File:** `src/services/modelBuffer.js` lines 27–36; `src/components/ModelingTab.jsx` line 395 (`setProject` call)

**Suggestion:** The infrastructure is complete: `saveModelBuffer` / `loadModelBuffer` in `indexedDB.js:654` and `modelBuffer.setProject(pid)` in `ModelingTab.jsx:395`. If models are not restoring, the likely cause is a race condition where `bufferVersion` increments before `setProject` resolves, or `trimResult` stripping fields needed for the comparison-table labels. Debug: log the resolved buffer length after `setProject`; check `trimResult` does not drop `label`, `type`, or `varNames`. This may already be fixed; verify in the browser and close if so.

**Invariants:** Persistence must use `indexedDB.js`; never localStorage.

---

## Performance

### [high] · Data · 2026-05-08

**Feedback:** Big datasets generate crashes (21 MB) over 900 k rows

**File:** `src/DataStudio.jsx` line 379 (10 MB DuckDB routing threshold); `vercel.json` (COOP/COEP headers); `src/pipeline/duckdbRunner.js`

**Suggestion:** A 21 MB CSV should already route to `loadLargeCSV` (DuckDB path). The crash most likely has two compounding causes: (1) `vercel.json` missing `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` headers, which disables `SharedArrayBuffer` and forces DuckDB-Wasm into single-threaded mode (OOMs on 900 k rows); (2) a downstream `SELECT *` in `duckdbRunner.js` materialising all rows as JS objects. Audit `vercel.json` first; then grep `duckdbRunner.js` for un-paginated `SELECT *` queries and replace with column-selective queries.

**Invariants:** Never materialise the full 900 k rows as JS objects (CLAUDE.md DuckDB rule 1). All computation on the full dataset must stay inside DuckDB.

---

## Other

### Data · 2026-05-10

**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`

*(Cross-listed under Bugs above for triage. Filed as "Other" in ClaudeFB.md.)*

**File:** `src/DataStudio.jsx` (small-file CSV path)

**Suggestion:** See Bugs section for full suggestion. Short form: strip BOM, normalise line endings, retry with `latin-1` on parse failure, emit a "N rows skipped" warning.

---

## Stale entries — ClaudeFB.md cleanup recommended

The following items appear open in ClaudeFB.md (no strikethrough, no ✓) but are confirmed resolved in BugTriage.md or by direct code inspection. Recommend updating ClaudeFB.md in the next collector run:

| Item | ClaudeFB.md date | Evidence |
|------|-----------------|----------|
| The graph does not fit the screen | 2026-05-11 | BugTriage: `fixed` |
| Grids: adjust size of border grids by cutting | 2026-05-13 | BugTriage: `fixed (Sutherland-Hodgman clipping, 2026-05-18)` |
| Grids do not work | 2026-05-20 | BugTriage: `fixed (2026-06-01)` |
| Add resampling, bootstrapping, sampling to mean | 2026-05-18 | `StatWorkspace.jsx:694` — bootstrap/permutation fully implemented |
| Add studentization for permutation tests | 2026-06-08 | `StatWorkspace.jsx:1159` — `studDiffMeans` + `permutationCompare` implemented |
| Persistence across sessions of pinned models | 2026-06-04 | `modelBuffer.js:36` + `indexedDB.js:654` — per-project buffer load/save implemented |

---

## Suggested action order

1. **[critical] Fix `.dta` date conversion in format 117–119 parser** — add `<formats>` section read + `dateConv` map in `src/services/data/parsers/stata.js` (mirrors the old-parser pattern already at lines 110–131; minimal, well-bounded change)
2. **[high] Route large `.dta`/`.rds`/`.xlsx` through DuckDB after parse** — `src/DataStudio.jsx:403-406`; register JS rows into DuckDB when `rows.length >= 50_000`; eliminates 15 s tab-transition lag on the most common research datasets
3. **[medium] Fix hardcoded RDD variable labels** — one-line interpolation in `CausalEngine.js:577-579` and `844-847`; zero math risk, immediate user-visible improvement across coefficient table and LaTeX export
4. **[medium] Add Anderson-Rubin CI display** — math is already computed (`result.arCI`); add 2–3 display rows in `ModelingTab.jsx` around line 2987 to surface it
5. **[medium] Plot Builder resize + PNG export fix** — `ResizeObserver` container in `ModelingTab.jsx` + pass measured dimensions to `PlotCanvas`; fixes both usability and export fidelity
