# Feedback Analysis Report — 2026-06-29

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 6 open bugs · 6 open features · 0 open UX · 1 open performance · 1 open other
- 4 of the 6 bugs are **code-complete** (commits `35f4b5a6` / `5e105529`) but lack Franco's browser confirmation; they are included here as validation asks, not as unpatched defects.
- 2 bugs are **genuinely open** (no code fix yet): the .rds bzip2/xz decompression gap.

---

## Bugs

### [high] · Data · 2026-06-26
**Feedback:** Parse error: crime_panel_final_r_300.rds — not a valid R data file (.rds), unrecognised serialisation header. *(Same root cause: grid_enriched_500.rds.)*
**File:** `src/DataStudio.jsx:369-374`, `src/services/data/parsers/rds.js:440-460`
**Suggestion:** The pre-check in `DataStudio.jsx:371-373` only recognises gzip (`0x1F 0x8B`) and raw XDR/ASCII/binary (`X\n` / `A\n` / `B\n`) magic bytes. Both failing files almost certainly use **bzip2** (magic `0x42 0x5A`) or **xz** (magic `0xFD 0x37`) compression, which R's `saveRDS()` supports via `compress="bzip2"/"xz"`. Shortest fix: (1) extend the `isRDSMagic` guard in `DataStudio.jsx:371` to also admit `0x42 0x5A` (bzip2) and `0xFD 0x37` (xz) so the file reaches the parser; (2) in `parseRDS` (`rds.js:456` block), detect those bytes and throw an actionable message: *"This .rds file uses bzip2/xz compression. Re-save in R with `saveRDS(df, 'file.rds', compress='gzip')`."* Long-term: integrate a JS bzip2 decoder (`compressing` npm package or `bzip2-wasm`) to decompress transparently, matching the existing `decompressGzip` path at `rds.js:414-431`.
**Invariants:** No React may be imported into `rds.js` (pure-JS parser). The browser `DecompressionStream` API only supports gzip — a JS/WASM library is required for bzip2/xz transparent decompression.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line 1583 · Original Line: China,CHN,2010,2425.47... (truncated) — import aborts on a malformed row.
**File:** `src/DataStudio.jsx:381-389` (CSV branch in `parseFile`)
**Suggestion:** Wrap the CSV parse call in a try/catch; on failure, surface a recovery modal with: the row number and a truncated preview of the offending line, a "Skip malformed rows and continue" button (re-parse with a `skipErrors` option), and an "Abort" fallback. For DuckDB-backed large CSVs (`>10 MB`), DuckDB's `IGNORE_ERRORS=TRUE` pragma can silently skip bad rows — expose that as a checkbox in the large-file import flow. This is *fix-later* in BugTriage; the suggestion here documents the intended UX shape.
**Invariants:** Non-destructive: the pipeline must replay on the cleaned `rawData`, never a mutation of a partially-imported table.

---

### [high] · Model · 2026-06-11 *(code-complete — browser validation pending)*
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage records this as code-complete in commit `35f4b5a6` — OLS/WLS diagnostics now render from the frozen `result.spec`. **Action needed: Franco runs an OLS, confirms all three plots appear, then toggles a sidebar variable and confirms the plots remain frozen.** If plots are still absent, check that `ModelingTab` passes a non-null `result` prop to `ResidualPlots` and that the component is conditionally rendered for `estimator === "OLS"` and `"WLS"`.
**Invariants:** No hooks inside conditionals (prior regression that caused the 2SLS black screen).

---

### [medium] · Model · 2026-06-10 *(code-complete — browser validation pending)*
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage records this as code-complete in commit `35f4b5a6` — native vertical resize with bounded responsive canvas sizing was added so PNG captures user-selected dimensions. **Action needed: Franco resizes the embedded Plot Builder and exports a PNG, confirming correct dimensions and no clipping.** If the resize handle is missing, check that the wrapper `<div>` in `ModelingTab` has `resize: "vertical"` in its inline style and that `PlotBuilder` reads the container's `offsetHeight` via `ResizeObserver`.
**Invariants:** Inline styles only via the `C` object — no new CSS classes.

---

### [medium] · Spatial · 2026-06-13 *(code-complete — browser validation pending)*
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** BugTriage records this as code-complete in commit `5e105529` — heatmap restored as a Leaflet layer with KDE cells, palette/opacity controls, legend, and a deterministic 5k-point cap. **Action needed: Franco loads a point dataset, activates the Heatmap layer type in the Map tab, and confirms the gradient, legend, and HTML export all work.** If the layer type is missing from the dropdown, check `SpatialLayerEditor` for a `"heatmap"` case in its type list.
**Invariants:** Leaflet must be loaded via the existing CDN singleton (`src/components/tabs/spatial/shared/leaflet.js`) — do not bundle it.

---

### [medium] · Model · 2026-06-14 *(code-complete — browser validation pending)*
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** BugTriage records this as code-complete in commit `35f4b5a6` — display now reads from the frozen `result.spec` snapshot. **Action needed: Franco runs an OLS, toggles a sidebar variable, and confirms coefficients/diagnostics do not change; then restores a pinned model and confirms it renders correctly.** If mutation still occurs, grep `ModelPlots.jsx` for props derived from live `xVars`/`yVar` state rather than `result.spec.*`.
**Invariants:** No hooks inside conditionals.

---

## Features

### · Calculate · 2026-06-19
**Feedback:** Add limits.
**File:** `src/components/tabs/CalculateTab.jsx`, `src/math/calcEngine.js`
**Suggestion:** The Calculate workbench already imports `evalExpression`, `derivative`, `integrate`, and `solveRootAuto` from `calcEngine.js` (`CalculateTab.jsx:18`). Add a `limit(expr, varName, target, direction?)` function to `calcEngine.js` — numeric approach: evaluate `expr` at `target ± ε` for decreasing `ε ∈ {1e-4, 1e-6, 1e-8}` and check convergence; symbolic: use the existing CAS backend for polynomial/rational expressions. In `CalculateTab.jsx`, add a "Limit" section below Derivative with inputs for variable, approach value, and side (left / right / two-sided). The `lim` LaTeX macro is already in the keyboard shortcut list at `CalculateTab.jsx:309`.
**Invariants:** `calcEngine.js` lives under `src/math/` — zero React imports allowed.

---

### · Explore · 2026-05-19 *(fix-later)*
**Feedback:** Add filter option supporting R-style `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` membership syntax.
**File:** `src/components/wrangling/CleanTab.jsx:878`, `src/pipeline/runner.js:198-250`
**Suggestion:** The pipeline `filter` step already handles `op:"in"` predicate nodes (`runner.js:208`). The missing piece is a parser in `CleanTab` that detects `col %in% c(v1, v2, ...)` in the expression box and converts it to `{ type:"condition", col, op:"in", values:[...] }`. Add a `parseRlikeMembership(expr)` helper that matches `/(\w+)\s*%in%\s*c\((.+)\)/` and splits the quoted-string list on commas. If the pattern matches, push the structured predicate directly; otherwise fall through to the existing formula expression path at `CleanTab.jsx:878`.
**Invariants:** Runner.js replays on `rawData` — no mutation. The `in` operator is already implemented in `evalPredicate`; only the UI parse layer needs extending.

---

### · Model · 2026-05-21 *(fix-later)*
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))` — Call: rdrobust Sharp RD estimates using local polynomial regression.
**File:** `src/math/CausalEngine.js:579` (varNames), `src/components/ModelingTab.jsx` (result render for `result.type === "RDD"`)
**Suggestion:** `CausalEngine` already computes all required statistics: LATE, SE (conventional + robust), p-values, IK bandwidth, polynomial order, kernel, and `varNames` at line 579. Create a `RDDSummaryTable` component that renders: a header block (method, kernel, bandwidth, N left/right of cutoff, poly order) and a two-row table (conventional vs robust) showing coef / SE / z / p / 95% CI. Wire it into the RDD result section in `ModelingTab.jsx` where `result.type === "RDD"` is detected.
**Invariants:** The display component must not import from `src/math/` — use the result object passed as a prop.

---

### · Model · 2026-05-25 *(fix-later)*
**Feedback:** Add an option to estimate without intercept.
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js:128`
**Suggestion:** Add a `noIntercept: boolean` checkbox to `ModelConfiguration.jsx` (visible for OLS/WLS families). In `LinearEngine.js:runOLS` (around line 128), when `spec.noIntercept` is true, build the design matrix from `xCols` only (no prepended ones column) and set `k = xCols.length`. The DuckDB OLS suff-stats path (`duckdbOLS.buildOLSSuffStats`) must also receive this flag and exclude the constant column from its SQL `SELECT` list — or short-circuit to the JS path until SQL support is added.
**Invariants:** `noIntercept` is a spec-level flag — never hardcoded inside any engine function. `seType` continues to be passed explicitly. For FE/FD/TWFE estimators the intercept is already absorbed by demeaning, so the toggle should be hidden for those estimator types.

---

### · Spatial · 2026-05-30 *(fix-later)*
**Feedback:** Allow spatial filters for plots.
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add an optional `filterExpr` text field to each layer's config in `GeoLayerConfig.jsx`. Before passing a layer's rows to the SVG renderer in `SpatialGeoPlot.jsx`, evaluate the expression against each row using the `evalPredicate` logic from `runner.js:211-250`. Extract that function into a shared `src/pipeline/evalPredicate.js` util to avoid duplicating it across files. The filter is ephemeral (render-time only) and does not mutate the pipeline.
**Invariants:** `SpatialEngine.js` is pure JS — filter evaluation must live in the component layer or the shared util, not inside `src/math/`.

---

### · Model · 2026-06-06 *(fix-later)*
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names.
**File:** `src/math/CausalEngine.js:579`, `src/services/data/duckdbRDD.js:44-46`, `src/EconometricsEngine.js:373`, `src/ReportingModule.jsx`
**Suggestion:** Add a `coefLabels: Record<string,string>` field to the model spec. In `ModelConfiguration.jsx` (RDD config section), render an editable label input for each coefficient name after estimation. In `ReportingModule.jsx` and `buildStargazer`, replace technical names ("D (treatment)", "running − c") with `coefLabels[name] ?? name` when building the LaTeX table. The `varNames` are set in `CausalEngine.js:579` (sharp), `CausalEngine.js:844-847` (fuzzy), `duckdbRDD.js:44-46` (SQL path), and `EconometricsEngine.js:373` (legacy shim) — label remapping must happen exclusively in the reporting layer.
**Invariants:** `src/math/` files must never read `coefLabels` — all substitution happens in `ReportingModule.jsx` and the export scripts, keeping the math layer free of UI concerns.

---

## Performance

### · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21 MB) over 900k rows.
**File:** `src/DataStudio.jsx` (large-file branch, `parseFile`), `src/services/data/duckdb.js`
**Suggestion:** Fase X4 is code-complete per X5 reconciliation (OPFS Parquet caching, DuckDB large-file routing, 500-row preview). **Action needed: Franco loads the 21 MB / 900k-row CSV, navigates between all tabs, and confirms no crash and sub-3s transitions.** If crashes persist, check that the DuckDB singleton initialises before React renders the data viewer, and that the display slice is capped at ≤500 rows throughout the component tree.
**Invariants:** Pipeline steps must run on the **full** DuckDB dataset — never on the truncated preview. Passing the preview slice to `runner.js` or `duckdbRunner.js` would silently produce wrong results on operations like `lag`, `group_summarize`, and model estimation.

---

## Other

### · Security · 2026-06-13 (follow-up)
**Feedback (implicit from BugTriage):** Client-side feedback sanitization in `feedbackService.submitFeedback` is bypassable by a direct POST to the Supabase REST endpoint.
**File:** Supabase `feedback` table (add a `BEFORE INSERT` trigger)
**Suggestion:** Add a Postgres `BEFORE INSERT OR UPDATE` trigger on the feedback table that applies entity-escaping for `<>&"'` and caps `description` at 5000 characters — mirroring the client-side `sanitizeFeedback` logic. A trigger cannot be bypassed by direct REST or Supabase SDK calls. Example body: `NEW.description = left(regexp_replace(NEW.description, E'[<>&"'']', '?', 'g'), 5000); RETURN NEW;`. Apply via a Supabase migration (`supabase/migrations/`) and confirm the feedback-collector agent's INSERT path still succeeds after deployment.
**Invariants:** The trigger must not break existing RLS policies on the feedback table. Client-side sanitization should be kept as a first layer — the trigger is the durable server-side boundary.

---

## Suggested action order

1. **Fix RDS bzip2/xz decompression** (`rds.js:456`, `DataStudio.jsx:371`) — two researcher files fail to load at all; the highest-impact unpatched defect as of this run.
2. **Browser-validate the four code-complete Model/Spatial bugs** (OLS residual plots, Plot Builder resize, Heatmap, diagnostics freeze) — all fixed in code; only Franco's click-through is missing before ClaudeFB can be updated.
3. **Add Calculate limits** (`calcEngine.js` + `CalculateTab.jsx`) — genuinely open feature with no BugTriage entry; low complexity given existing CAS/numeric infrastructure.
4. **Server-side feedback sanitization** (Supabase trigger) — small change that closes the XSS bypass window before public launch.
5. **%in% filter syntax** (`CleanTab.jsx:878`) — low-effort parse layer change; unblocks a common R-to-EconStudio workflow.
6. **Editable LaTeX coefficient labels** (`ReportingModule.jsx`, `ModelConfiguration.jsx`) — confined to the reporting layer; affects publication-ready output quality.
