# Feedback Analysis Report — 2026-08-16

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary

- **13 open bugs** · **10 open features** · **3 open UX/design** · **1 open performance** · **1 open other**

**Note — 2 items flagged as "open" in BugTriage.md are likely already fixed per CLAUDE.md:**
- `group_summarize` cross-group math → CLAUDE.md says "DONE 2026-08-07" (BugTriage not updated).
- N-way FE + interactions → CLAUDE.md confirms shipped in `045f5cda` (#104); BugTriage marked "likely fixed — needs confirmation".

**5 items code-complete but pending Franco's browser validation:**
Plot Builder resize/PNG, OLS residual plots, spatial heatmap, coefficient plot freeze, HC2/HC3 export scripts.
These are not counted in the open totals above; they need browser sign-off to be closed.

---

## Bugs

### high · Export · 2026-07-05
**Feedback:** `Replication code for CS DiD 2021 does not work`
**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`) · `src/math/CallawayEngine.js`
**Suggestion:** The `CallawayCS` export stub in `rScript.js` starts the block (line 1432) but R validation is flagged as pending in CLAUDE.md. Check whether the emitted `did::att_gt()` + `did::aggte()` calls pass the correct `control_group`, `anticipation`, and `base_period` args that CallawayEngine actually uses. Run the emitted script verbatim against a staggered-DiD fixture and diff against `callawayBenchmarks.json`. Fix any mismatched argument names first, then check the bootstrap SE call.
**Invariants:** All Anthropic calls stay in `AIService.js`. Export is pure JS — no React imports allowed in the generator.

---

### high · Export · 2026-07-05
**Feedback:** `Replication codes of the plots must have the lines to load datasets as well`
**File:** `src/components/PlotBuilder.jsx` (line 2631 JSX call in `src/ExplorerModule.jsx`) · `src/services/export/loadLine.js`
**Suggestion:** `ExplorerModule.jsx` mounts `<PlotBuilder>` without a `scriptPreamble` prop (line 2631 — the prop is absent). `PlotBuilder.copyPlotScript` already prepends a `preamble` when one is provided (line 1528–1538). Wire the fix in `ExplorerModule.jsx`: build a preamble via `buildRLoadLine(filename, loadOpts)` / `buildPyLoadLine` / `buildStataLoadLine` from `src/services/export/loadLine.js` and pass it as `scriptPreamble`. Note that `ModelingTab` already does this for its embedded PlotBuilder (line 3559) — replicate that pattern in ExplorerModule.
**Invariants:** No direct Anthropic fetch outside `AIService.js`. Load lines must use the existing `loadLine.js` helpers, not new hardcoded `read_csv` strings.

---

### high · Export · 2026-07-17
**Feedback:** `Stata workbench doesn't work completely`
**File:** `src/components/modeling/CodeEditor.jsx` · `src/services/export/stataScript.js`
**Suggestion:** `CodeEditor.jsx` routes `tab === "stata"` to `generateStataScript(config)` (line 74). The most likely failures are: (a) Stata-incompatible syntax in newly-added SE type emitters (`vce()` vs `robust`, `reghdfe` vs `xtreg` for panel + two-way cluster — both changed in the 2026-07-17 HC2/HC3 pass); (b) `seType`/`clusterVar` not threaded through to the Stata generator for all estimator branches; (c) N-way FE using `reghdfe` with an absorb syntax the user's Stata installation can't find (`ssc install reghdfe`). Test by running `stataScript.js`'s output verbatim in a real Stata session with the relevant fixture. Check that the `pVceHdfe` helper emits `reghdfe` syntax correctly for all SE variants.
**Invariants:** Stata export is pure JS, no React. Inline styles use the `C` object in shared.jsx — not relevant here.

---

### high · Pipeline · 2026-07-17 *(check CLAUDE.md — may be done)*
**Feedback:** `Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group`
**File:** `src/pipeline/runner.js` (line 528, `case "group_summarize"`) · `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** CLAUDE.md reports this as "DONE 2026-08-07" (splitByGroup + SampleTestPanel long-format input-mode toggle), but BugTriage.md still reads "open". Confirm that BugTriage is stale: run the two-sample long-format mean-difference test from the UI with a real treatment/control column and verify it computes the right ATE. If it works, mark BugTriage as fixed. If it does not work in the browser, the CLAUDE.md entry may describe a partial fix.
**Invariants:** `src/math/` and `src/core/` must remain React-free. Pipeline steps must replay on `rawData`, never mutate in place.

---

### medium · Export · 2026-07-05
**Feedback:** `Adjust the size of LaTeX tables when the number of columns increases`
**File:** `src/services/export/latexTable.js` (line 36 `colFmt`, line 168 `\begin{tabular}`)
**Suggestion:** `colFmt` is currently `"l" + " r".repeat(colsN)` with a plain `\begin{tabular}` — no width control. Wrap the `tabular` in `\resizebox{\textwidth}{!}{…}` when `colsN > 6` (a reasonable threshold), or unconditionally use `tabularx` with `X` columns instead of `r` for equal-width layout. Add `\usepackage{tabularx}` or `\usepackage{graphicx}` to the preamble comment block accordingly. Test with a 10-column ModelComparison table.
**Invariants:** Pure JS export function — no React, no IndexedDB calls. Match the existing `booktabs`/`\hline` style convention already in the file.

---

### medium · Export / Math · 2026-07-06
**Feedback:** `Add vector for running more than 1 hypothesis and save them to replicate in R`
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` supports a single hypothesis vector at a time. Extend `CoefficientTestPanel` to maintain a saved list of hypothesis results (an array in component state, serialized to `sessionStorage` keyed by `pid + modelId`). Expose an "Add to saved tests" button that appends the current result. Add a "Copy as R" button that serialises each saved test as `car::linearHypothesis(fit, c("β1 = 0", "β2 = 0"))`. The math engine (`waldTest`) already returns `h0s` — use them as the R string argument.
**Invariants:** Persistence must use IndexedDB (via `indexedDB.js`) if persisting across sessions. `sessionStorage` is acceptable for within-session use. No `localStorage` for pipeline/data storage.

---

### medium · UI · 2026-07-09
**Feedback:** `Add labels for the Time series plot, title, axis, etc.`
**File:** `src/ExplorerModule.jsx` (time-series sub-tab around line 413) · `src/components/PlotBuilder.jsx`
**Suggestion:** The ExplorerModule time-series view (`const times=[...new Set(rows.map(r=>r[timeCol]))]...`) draws a minimal SVG/PlotBuilder chart without title or axis label controls. Add a small panel with text inputs for title, x-axis label, and y-axis label, storing them in the existing plot-spec state. Wire these into the Observable Plot `Plot.plot({…, title, x:{label}, y:{label}})` options. Also propagate these labels into the R/Python replication code via `xlab`/`ylab`/`main`.
**Invariants:** Inline styles only using the `C` object from `shared.jsx`. No CSS files.

---

### medium · AI / Export · 2026-07-09
**Feedback:** `Add direct elasticity interpretation`
**File:** `src/services/AI/Prompts/index.js` (lines 99, 326, 353–354 — elasticity rules already present)
**Suggestion:** The prompt already handles log-log (elasticity) and log-level (semi-elasticity). The gap is **direct elasticity from a level-level regression**: $\hat{\varepsilon} = \hat{\beta} \cdot \bar{x}/\bar{y}$ evaluated at sample means. Add a rule to `INTERPRET_REGRESSION_PROMPT` instructing Claude to compute and report this when (a) neither Y nor X is log-transformed, (b) the user or result metadata hints at elasticity interest (e.g., variable names containing "price", "quantity", "income"), and (c) sample means are available in the result. Thread sample means (already computed during estimation) through `EstimationResult.js` if not already present there. Flag as "approximate point-elasticity at sample means" to distinguish from structural elasticity.
**Invariants:** All AI calls through `AIService.js`. `src/math/` must remain React-free.

---

### medium · Math / UI · 2026-07-17
**Feedback:** `Add chi-square for linearhypothesis`
**File:** `src/math/ModelHypothesis.js` (line 203 — `chiSq` already computed) · `src/components/modeling/CoefficientTestPanel.jsx` (line 211 — `chiSq` displayed)
**Suggestion:** The Wald chi-square is already computed and displayed in the joint test panel. The likely gap is that individual-coefficient tests only show t-statistics, and there is no F-statistic (chi-sq/q) alongside the chi-sq for the joint test. Confirm what the user expects by checking R's `car::linearHypothesis` output format (it shows both F and chi-sq). If F is the missing variant, add `fStat: chiSq / q` and its p-value (`1 - fCDF(fStat, q, df_resid)`) to the `waldTest` return and display it alongside `chiSq` in `CoefficientTestPanel`.
**Invariants:** `src/math/` must remain React-free. Use the existing `fCDF` (Lentz's continued-fraction) from `LinearEngine.js` — the fixed version, not the old Riemann-sum implementation.

---

### medium · UX · 2026-07-17
**Feedback:** `Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases`
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `useRef`-based or `useState`-based draft object that saves each subsection's in-progress form state (e.g., `case_when` rows, `mutate` expression, `lag` config) to `sessionStorage` keyed by `pid + "featureDraft"`. Restore from this draft on mount and clear it only when the step is committed to the pipeline via `onAdd`. The simplest approach: serialize the active subsection's state to `sessionStorage` in a `useEffect` that fires on every change, and read it on mount. No IndexedDB needed since this is transient within a session.
**Invariants:** `localStorage` is deprecated for pipeline/data storage. `sessionStorage` is acceptable for transient UI drafts. No React in `src/math/` or `src/core/`.

---

### medium · UI · 2026-07-17
**Feedback:** `Add arrange functions in the data viewer`
**File:** `src/ExplorerModule.jsx` (DataViewer area — no `sortCol`/`sortDir` state found in search)
**Suggestion:** Add `sortCol` and `sortDir` state to the DataViewer section of `ExplorerModule`. Render a clickable column header that cycles through `asc` → `desc` → `none`. For DuckDB-backed datasets, push `ORDER BY ${sortCol} ${sortDir}` into `getTablePage`'s SQL (alongside the existing `WHERE`); for JS-backed datasets, sort the in-memory `rows` slice. Respect the existing `PREVIEW_ROWS` cap — sorting must operate on the full DuckDB table, not the preview. Show a small sort indicator (▲/▼) in the header cell.
**Invariants:** DuckDB is the data boundary for large datasets — sort must be done inside SQL for DuckDB-backed data, not by pulling all rows into JS. Inline styles only.

---

### low · UI · 2026-07-17
**Feedback:** `Guide tour must be updated`
**File:** `src/components/HelpSystem.jsx` (line 11 — `TOUR_STEPS` array)
**Suggestion:** Audit each `TOUR_STEPS` entry against current module state: check that module names, tab labels, and feature descriptions match the current UI (e.g., WLS is no longer a sidebar estimator; the strategy × family split; 12 geoms not 11; CR2/CR3 SE types now present; N-way FE available). Remove or update any step referencing removed UI elements. Add steps for N-way FE, CS DiD / Sun-Abraham estimators, spatial pipeline integration, and the Bacon decomposition panel. Per CLAUDE.md: never write counts ("14 estimators") — name the things.
**Invariants:** `HelpSystem.jsx` is a React component — inline styles via `C` object. No CSS files.

---

### medium · Data · 2026-07-17
**Feedback:** `Create groups for pinned models, plots, and maps, so not everything is compared at the same time`
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** This is primarily a UX/organization feature (see UX section below for the design suggestion). From the persistence side, `artifactOrder.js` currently stores a flat global order; it would need a `groupId` field per artifact and a `groups` map (id → label). Defer implementation until the UX design is agreed.
**Invariants:** Persistence via IndexedDB only (`indexedDB.js`). No `localStorage`.

---

## UX / Design

### medium · UX · 2026-07-17
**Feedback:** `Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria`
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx` · `src/components/workspace/DatasetManager.jsx`
**Suggestion:** Add a `groups` concept to the model buffer: a lightweight group is a string label the user assigns. Store `{artifactId, groupId}` pairs in a new `artifact_groups` IndexedDB store (added to `indexedDB.js`'s schema, bumping the schema version). In `ModelBufferBar`, render a group filter dropdown that hides artifacts not in the active group. A `+` button creates a group; dragging a pinned card into a group assigns it. Keep groups optional — ungrouped artifacts appear under "All" by default.
**Invariants:** IndexedDB only for persistence. Inline styles via `C` object. No external UI libraries.

---

### medium · UX · 2026-07-17
**Feedback:** `Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step`
**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`
**Suggestion:** Each step card in `History.jsx` knows its `type`. Add a click handler (or a small "✏ Edit" chip) that maps step type to the tab/subsection that created it (e.g., `filter` → Clean tab / Filter section; `log` → Feature / Transform; `group_summarize` → Reshape). Emit an event upward via a prop callback `onNavigate(tab, subsection)` to `WranglingModule`, which already controls the active tab state. A simple static map `{filter: ["clean", "filter"], log: ["feature", "transform"], ...}` covering the 54 step types is sufficient.
**Invariants:** No `localStorage`. The navigation callback must not mutate pipeline state — it only changes the active tab, which is transient React state.

---

### low · UX / Export · 2026-06-14
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/services/export/latexTable.js` · `src/components/modeling/` (result display components)
**Suggestion:** Add a `labelOverrides: {[colName]: string}` prop to the LaTeX table generator. Expose an inline edit affordance in the model result panel — a small pencil icon next to each coefficient row label that opens a text input. Store overrides in `sessionStorage` keyed by `pid + modelId`. Thread the overrides into `latexTable.js`'s row-label lookup. This is a post-estimation display feature with no math impact.
**Invariants:** `localStorage` deprecated; `sessionStorage` is acceptable for transient label overrides within a session. No React in `src/math/`.

---

## Features

### medium · Feature / Export · 2026-06-19
**Feedback:** `Add limits`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" section to CalculateTab alongside derivatives and integrals. Use the existing CAS / symbolic math infrastructure (or a lightweight numerical approach: evaluate at 1000 points approaching the limit point from left and right, compare). For one-sided and two-sided limits, expose `x → a⁻`, `x → a⁺`, and `x → ∞` / `x → −∞` variants. Display the result with LaTeX rendering (already present in CalculateTab). Export as `limit(expr, x, a)` for R (via the `Ryacas` CAS package) or Python (`sympy.limit`).
**Invariants:** `CalculateTab.jsx` is a React component — no pure-JS math files needed unless computationally heavy. Inline styles via `C` object.

---

### low · Feature · 2026-07-04
**Feedback:** `Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** A constant `k` is already plottable as `f(x) = k` (a flat function). However, the graph likely requires at least one `x`-dependent expression. The simplest fix: allow pure numeric inputs in the equation list, rendering them as horizontal rules via `hline` (which PlotBuilder already supports). Alternatively, detect a numeric-only expression and add a `Plt.ruleY([k])` mark to the Observable Plot spec instead of evaluating it as a function.
**Invariants:** No external UI libraries. Inline styles via `C` object.

---

### medium · Feature · 2026-06-14
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option` / R-style `%in% c(...)` membership syntax not accepted
**File:** `src/components/wrangling/CleanTab.jsx` · `src/pipeline/predicate.js`
**Suggestion:** The canonical condition language in `predicate.js` already has `in` / `nin` operators (`OPERATORS` table). The gap is UI: `FilterBuilder` likely does not expose a multi-value chip input for the `in` operator, so users attempt to type R's `%in%` syntax and it fails. Add a tag-input widget to `FilterBuilder` for the `in`/`nin` case (paste-split on comma; values displayed as removable chips). `predicateToSQL` already handles `in` correctly. Also update `INFER_UNITS_PROMPT` examples to show the app's native UI instead of R syntax so users learn the interface idiom.
**Invariants:** `predicate.js` is the single owner of the canonical condition language — never re-implement a filter operator elsewhere.

---

### medium · Feature · 2026-07-09
**Feedback:** `Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster`
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text input at the top of each variable list (Y, X, W, Z) that filters the displayed header chips by substring match. Store the filter text in local component state — no persistence needed. Clear the filter when the dataset changes. This is a pure UI affordance with no math impact; 10–20 lines of code.
**Invariants:** Inline styles via `C` object from `src/components/modeling/shared.jsx`. No external UI libraries.

---

### low · Feature · 2026-07-11
**Feedback:** `Add more labels in tables or boxes when plotting by color`
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is mapped, the legend currently shows color swatches but may omit group-level statistics (count, mean, etc.). Add an optional "annotate groups" toggle: when enabled, PlotBuilder appends a `Plot.text` mark that draws `n = k` or `mean = x.xx` next to each group's centroid or at the legend. The annotation values are computed from the data in a `useMemo` keyed on `aes.color + rows`. Export this as `geom_label`/`annotate` in R and `ax.annotate` in Python.
**Invariants:** No external UI libraries. Observable Plot 0.6 CDN — no npm install.

---

### low · Feature · 2026-07-19
**Feedback:** `Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk`
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a "Country Codes" sub-section to FeatureTab. Include a small static lookup table (~250 rows: iso2, iso3, country name, UN region) bundled as a JS constant (< 20 KB). Expose three operations: (a) detect and add iso2 from country name (fuzzy via the existing `fuzzyGroups` in `wrangling/utils.js`); (b) add iso3 from iso2; (c) add UN region from iso2. Emit as a `vector_assign` pipeline step with the lookup embedded (so it replays correctly). Replication code: `countrycode::countrycode(col, "country.name", "iso2c")` in R.
**Invariants:** No external UI libraries. The lookup table must be a static JS constant — no fetch calls (privacy-first). Pipeline step must be a valid `vector_assign` (or new step type with a registry entry) so it replays on `rawData`.

---

### low · Feature · 2026-07-05
**Feedback:** `Add plots with 3 axis`
**File:** File not located — no existing 3D plot module found in `src/components/`.
**Suggestion:** 3D scatter plots are outside Observable Plot's scope (it is 2D). A lightweight option: use `Plotly.js` (CDN, similar pattern to the existing Leaflet/proj4 CDN loaders) for a 3D scatter mode in PlotBuilder. This is a significant scope expansion — recommend as a post-MVP feature. For the near term, a workaround guide in the HelpSystem (use `color` or `size` as the third aesthetic dimension) may address most use cases.
**Invariants:** Any CDN origin fetched at runtime must be added to both `script-src` AND `connect-src` in `vercel.json` (see CLAUDE.md CSP bug history). No external UI libraries imported from npm.

---

### low · Feature · 2026-07-19
**Feedback:** `Think about how to construct lists, save them and use them during the session when filtering or constructing variables — cross-module feature`
**File:** `src/services/session/sessionState.jsx` (design: lists would live here as session context)
**Suggestion:** This is a design-first item — agreed in ClaudeFB.md to be deferred until Franco approves a scope. Proposed shape: a `lists` map `{id: {name, values[]}}` in `SessionStateProvider`, with a Lists panel in `WorkspaceBar` or `DatasetManager`. Lists are referenced in `FilterBuilder` (as a named `in` operand) and in `FeatureTab`'s `case_when`/`mutate` (as a literal array). Serialize to IndexedDB keyed by `pid`. Not started — flag for a design session.
**Invariants:** Persistence via IndexedDB only. Lists are session-scoped, never sent to the server (privacy-first).

---

## Performance

### high · Performance · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js` · `src/pipeline/duckdbRunner.js`
**Suggestion:** Per the X5 reconciliation table, "Fase X4 code-complete; browser benchmark pending Franco." The DuckDB performance strategy in CLAUDE.md (OPFS persistence, threaded Wasm, preview limit ≠ computation limit, SQL-only aggregation) is architecturally complete. The blocking item is browser validation of the 900k-row benchmark on the Vercel deployment with COOP/COEP headers. **Franco: run the benchmark and confirm the tab-transition lag is resolved.** If the lag persists on a specific tab transition, profile with Chrome DevTools to identify which JS-path operation is materializing full Arrow results. Until browser-confirmed, do not mark this as closed.
**Invariants:** Full-dataset operations (model estimation, group_summarize, pipeline steps) must run on the full DuckDB table. Display limit of 200–500 rows does not cap computation.

---

## Other

### medium · Other · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...` (truncated)
**File:** `src/services/data/parsers/` (CSV auto-detection path) · `src/DataStudio.jsx`
**Suggestion:** This is the same class of bug as BugTriage's 2026-06-14 "Malformed CSV row can abort import" (status: fix-later). The error on line 1583 suggests a row that has a different field count than the header (e.g., a trailing comma in a value, an unescaped comma in a quoted field, or an encoding anomaly). Implement resilient CSV import: (a) detect the first N rows' field count as the expected schema; (b) on mismatch, skip the row, log it, and continue rather than aborting; (c) after parsing, display a "Skipped N malformed rows (lines: …)" banner. DuckDB's native CSV reader supports `IGNORE_ERRORS = TRUE` — use it for large files routed through DuckDB instead of the JS parser.
**Invariants:** Non-destructive pipeline: only report skipped rows in a banner — never silently mutate them. DuckDB is the boundary for large-file parsing.

---

## Items to verify / confirm closed

The following ClaudeFB.md items have no strikethrough/✓ yet are marked fixed in BugTriage or CLAUDE.md:

| Item | ClaudeFB status | BugTriage / CLAUDE.md status |
|------|----------------|-------------------------------|
| `.dta` wrong loaded [2026-05-13] | no ✓ | BugTriage fixed 2026-06-10 |
| Spatial grids border clipping [2026-05-13] | no ✓ | BugTriage fixed 2026-05-18 |
| Spatial grids do not work [2026-05-20] | no ✓ | BugTriage fixed 2026-06-01 |
| Dataset delete leaves stale vars [2026-05-20] | no ✓ | BugTriage fixed |
| p-value calculation urgent [2026-05-21] | no ✓ | BugTriage fixed 2026-06-05 |
| Auto-reestimate on variable change [2026-05-21] | no ✓ | BugTriage fixed 2026-06-01 |
| Clear current model [2026-05-21] | no ✓ | BugTriage fixed |
| Calculate graph from zero [2026-05-22] | no ✓ | BugTriage fixed |
| Only 1 equation plotted [2026-05-25] | no ✓ | BugTriage fixed |
| Color references in PlotBuilder [2026-05-19] | no ✓ | BugTriage fixed 2026-05-30 |
| Resampling/bootstrap [2026-05-18] | no ✓ | X5: verified fixed |
| Montecarlo single variable [2026-05-19] | no ✓ | BugTriage fixed |
| Spatial auto-fit [2026-05-30] | no ✓ | BugTriage fixed |
| group_summarize cross-group math [2026-07-17] | no ✓ | CLAUDE.md: DONE 2026-08-07 |
| FE any categorical variable [2026-07-09] | no ✓ | CLAUDE.md: shipped #104 |
| FE with interactions [2026-07-09] | no ✓ | CLAUDE.md: shipped #104 |
| No intercept estimation [2026-05-25] | no ✓ | CLAUDE.md: DONE 2026-07-18 |
| Share project RLS error [2026-07-05] | no ✓ | BugTriage fixed 2026-07-14 |
| Shared projects can't open [2026-07-13] | no ✓ | BugTriage fixed 2026-07-14 |
| Pipeline applies only when Clean dataset selected [2026-06-17] | no ✓ | BugTriage fixed 2026-06-25 |

Recommend a housekeeping pass on ClaudeFB.md to add ✓ markers and strikethroughs for these.

---

## Suggested action order

1. **Fix CS DiD 2021 replication code** (`rScript.js` line 1432) — replication is the core academic value proposition; a broken export undermines all Callaway-Sant'Anna results.
2. **Wire `scriptPreamble` in ExplorerModule's PlotBuilder mount** — one-line fix to pass `buildRLoadLine(filename, loadOpts)`, closes a high-frequency usability gap for all plot script exports.
3. **Confirm and close group_summarize cross-group math** — CLAUDE.md marks done 2026-08-07 but BugTriage still open; a 5-minute browser test and BugTriage update closes this.
4. **Fix Stata workbench completeness** (`stataScript.js`, `CodeEditor.jsx`) — run the emitted Stata do-file against a fixture to find the specific broken cases; this affects every Stata user.
5. **Add data viewer sort control** (`ExplorerModule.jsx`) — no math involved, straightforward UI addition, high daily-use impact for dataset inspection.
6. **Add variable search to Model sidebar** (`VariableSelector.jsx`) — trivial to implement, directly unblocks users with wide datasets.
7. **Wrap LaTeX table in `\resizebox`** (`latexTable.js`) — 5-line change, directly impacts publication output quality for multi-model comparisons.
8. **Update guide tour** (`HelpSystem.jsx`) — low-code task, high first-impression impact for new LMU users.

---

*Report generated: 2026-08-16 by feedback-analyst agent.*
*Open item count: 13 bugs · 10 features · 3 UX · 1 performance · 1 other = 28 total.*
*Pending browser validation (code-complete): 5 items — Plot Builder resize/PNG, OLS residual plots, spatial heatmap, coefficient plot freeze, HC2/HC3 export scripts.*
