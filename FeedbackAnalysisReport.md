# Feedback Analysis Report — 2026-08-07

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 11 open bugs · 10 open features · 4 open UX/design (deferred) · 1 open performance · 1 open other
- 4 items code-complete with browser validation pending (flagged separately)

---

## Code-Complete / Browser Validation Pending

These items were fixed in code but not yet confirmed working by Franco. They are NOT listed as open bugs, but are flagged here to prompt browser testing.

| Item | Commit / Date | Validation needed |
|------|---------------|-------------------|
| HC2/HC3 replication fix (R/Stata/Python) | code-complete 2026-07-18 | All three language tabs for HC2, HC3 |
| Plot Builder resize + PNG export aesthetics | `35f4b5a6`, 2026-06-14 | Drag resize; PNG pixel dimensions correct |
| OLS Y~Ŷ / Residual-vs-Fitted / Q-Q plots | `35f4b5a6`, 2026-06-14 | All three diagnostic plots visible after estimation |
| Spatial heatmap layer | `5e105529`, 2026-06-14 | Heatmap renders with palette/opacity/legend |

---

## Bugs

### critical · Model · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js:1432`, `src/math/CallawayEngine.js:291`
**Suggestion:** The R export case `"CallawayCS"` (rScript.js:1432) destructures `yVar`, `treatCol`, `entityCol`, `timeCol`, `xVars`, `compGroup`, `csEstMethod`, and `csAnticipation` directly from the pinned-model object. But `CallawayEngine.js`'s return block (line 291) does not include these spec fields — it returns only `type`, `attgt`, `aggregations`, `cohorts`, etc. Every destructured variable lands as `undefined`, so the emitted R script calls `att_gt(yname = undefined, ...)` and is unrunnable. Fix: add `spec: { yCol, entityCol, timeCol, treatCol, xVars, compGroup, csEstMethod: estMethod, csAnticipation: anticipation }` to the `CallawayEngine` return object (mirroring the pattern used by every other engine), then read `model.spec` in the R/Stata/Python export cases instead of `model` directly. The Stata export (`stataScript.js:1127`) has the same destructuring bug and must be fixed in the same pass.
**Invariants:** `src/math/CallawayEngine.js` is a pure-JS math file — the fix adds only data fields, no React imports. Matches the SE `seType`-always-explicit invariant (pass `csEstMethod` as a spec field).

---

### high · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/components/PlotBuilder.jsx` (R/Python code-copy panel), `src/services/export/loadLine.js`
**Suggestion:** The "Copy R" / "Copy Python" buttons in PlotBuilder emit ggplot/matplotlib code but skip the data-load preamble. `src/services/export/loadLine.js` already exposes `buildRLoadLine` / `buildPyLoadLine` — thread the active dataset's `loadOpts` (available via `allDatasets` prop already passed to `CodeEditor`) into PlotBuilder's code-emit path and prepend the load line before the ggplot block. The same omission exists in `ExplorerModule.jsx`'s time-series and ACF replication snippets; fix both in the same pass.
**Invariants:** All Anthropic calls go through `AIService.js` — this is a pure string-generation change, no API calls involved. Inline styles only.

---

### high · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js:528` (`group_summarize` case), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** The `group_summarize` step supports per-column aggregations (`mean`, `sum`, `count`, etc.) but has no cross-group arithmetic (e.g. `mean(Y | D=1) − mean(Y | D=0)`). The cleanest approach that fits the existing step model: add an optional `derived` array to the step spec, where each entry is `{ name, op: "diff" | "ratio" | "custom", a, b }` — `a` and `b` reference columns produced by the aggregation. `runner.js` computes the aggregated table first, then evaluates derived columns by matching group-value keys. The ReshapeTab UI can expose this as a secondary "Compute across groups" mini-form under the aggregation builder. This is a pure pipeline extension — `runner.js` and `ReshapeTab.jsx` only, no math files.
**Invariants:** Non-destructive pipeline: derived step always replays on `rawData`. Never mutate in place. New step variant must be added to `registry.js` in the same commit and the registry/runner sync test must stay green.

---

### high · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx:74`, `src/services/export/stataScript.js`
**Suggestion:** The BugTriage entry is unspecific ("doesn't work completely"). `CodeEditor.jsx:74` routes to `generateStataScript(config)` for the Stata tab. Most likely causes: (a) recent SE-type routing changes (`stataScript.js` was substantially rewritten 2026-07-17 to support HC2/HC3/cluster/HAC per estimator) introduced a branch that throws or returns an empty string for certain estimator types; (b) the `seType` / `clusterVar` fields are not always present on `config` when the Stata tab is opened from a non-OLS pinned model. Diagnostic: open the Stata tab for OLS/HC1, TWFE, RDD, and CS DiD in turn and log what `config` contains at `CodeEditor.jsx:74`. If the issue is a throw in `stataScript.js`, wrap the call in try/catch and surface the error text in the code panel. Fix whatever the specific branch is; do not add a silent empty-string fallback.
**Invariants:** All styles inline via `C` — no new CSS. SE type must never be hardcoded inside a generated script; it must come from `config.seType`.

---

### high · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js:36,168`
**Suggestion:** `latexTable.js:36` builds `colFmt = "l" + " r".repeat(colsN)` and the `tabular` environment uses this directly, with no width constraint. When a comparison table has 6+ models the columns overflow `\textwidth`. Fix: wrap the `tabular` in `\resizebox{\textwidth}{!}{...}` (requires `\usepackage{graphicx}`) or use `adjustbox` (`\begin{adjustbox}{max width=\textwidth}`). Add the required package to the preamble comment block. For very wide tables also consider emitting `\small` before the table. The fix is entirely within `latexTable.js` — no UI changes.
**Invariants:** None — this is pure LaTeX string generation.

---

### medium · Model/AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js:353`
**Suggestion:** The `INTERPRET_REGRESSION_PROMPT` already handles log-log and log-level elasticity rules (lines 99, 353). However the AI cannot compute a point elasticity at the mean from a level-level regression because it doesn't receive `mean(X)` and `mean(Y)`. To expose this: in `AIService.js`'s `interpretRegression` call, augment the user payload with column means computed from `metadataExtractor.js` (which already extracts variable metadata). Then add a prompt rule: "If the model is level-level, report the arc-elasticity at the mean: η = β × mean(X)/mean(Y). Label it 'implied elasticity at the mean'." This keeps all Anthropic calls inside `AIService.js` and does not require a math-file change.
**Invariants:** Single API egress choke point — only `callClaude()` in `AIService.js` may call Anthropic. `src/math/` is not touched.

---

### medium · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/components/PlotBuilder.jsx` (time-series geom layer), `src/ExplorerModule.jsx`
**Suggestion:** The PlotBuilder already has a Labels panel (title, x-axis, y-axis label fields) wired for point/line/bar geoms via `Plot.plot({ ...spec, ... })`. The time-series rendering path in ExplorerModule bypasses PlotBuilder and uses its own SVG/Observable Plot call without passing through the labels panel. Either route the time-series plot through PlotBuilder's existing spec (add a `"timeseries"` geom entry) or expose title/axis label text inputs in ExplorerModule's time-series sub-tab and thread them into the Observable Plot config.
**Invariants:** Inline styles only via `C`.

---

### medium · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js:206`
**Suggestion:** `ModelHypothesis.js` already imports `pchisq` (line 1) and uses it at line 206. The current `testLinearHypothesis` function computes an F-statistic and its p-value. Add a `stat: "chi2"` option that returns `chi2 = q * F` (where `q` is the number of restrictions) with a chi-squared(q) p-value — this is the Wald form that `car::linearHypothesis(test="Chisq")` produces. Expose it as a toggle in `CoefficientTestPanel.jsx`. The chi-square version is asymptotically equivalent to the F and is often preferred when df is large (DuckDB path). Output both F and χ² in the results row when the toggle is on.
**Invariants:** `src/math/` has zero React imports. SE type must be passed explicitly — the existing `seType` already flows through; chi-square is only a different test distribution, not a different SE calculation.

---

### medium · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently runs a single hypothesis at a time and does not persist results. Add a "Save test" button that pushes the current hypothesis spec (`{R, r, label}`) into an array in component state (or a per-model list in IndexedDB via `indexedDB.js`). Render saved tests as a collapsible list with delete buttons. In the R replication export, emit one `car::linearHypothesis(fit, hypothesis.matrix = ...)` call per saved test. This is a UI and export change only — the math in `ModelHypothesis.js` is unchanged.
**Invariants:** Persistence goes through `indexedDB.js` if tests need to survive page reload — never `localStorage`. All Anthropic calls through `AIService.js` only.

---

### medium · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The Data Viewer table headers are static. Add clickable sort indicators (↑↓) to column headers. On click, derive a sorted slice from the DuckDB backend for large datasets (`getTablePage` already accepts `ORDER BY` — add an `orderCol`/`orderDir` parameter) and from the in-memory preview array for small datasets. Show at most one active sort column at a time (toggle asc/desc/none). This does not add a pipeline step — it is a display-only sort scoped to the viewer session. For pipeline-level sorts, direct users to the existing `arrange` step in the Clean tab.
**Invariants:** This is a display feature — do not mutate `rawData` or the pipeline. `getTablePage` already accepts SQL fragments; extend it, don't bypass it.

---

### low · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx:11` (`TOUR_STEPS`)
**Suggestion:** `TOUR_STEPS` is hand-written prose with 9 registered steps and is not tested. CLAUDE.md's working conventions require that every new tab/estimator/geom also gets a `TOUR_STEPS` update (same pass). Do an audit pass: compare `TOUR_STEPS` content against current WorkspaceBar tabs (Data, Clean, Explore, Model, Simulate, Calculate, Report — 7 tabs) and the current estimator list in `EstimatorSidebar.jsx`. Remove stale references to removed surfaces, add entries for any tab or major feature that has no step. Do not write counts ("14 estimators") — name the surfaces.
**Invariants:** No architectural constraints — this is pure prose in a JS array. The CLAUDE.md convention "never write a count into help copy" must be respected.

---

### other · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 (truncated)
**File:** `src/DataStudio.jsx`, `src/services/data/parsers/` (CSV path via DuckDB or Papa Parse)
**Suggestion:** This is a malformed-row resilience issue (BugTriage 2026-06-14 "fix-later"). The parser aborts on the first bad line instead of surfacing a recovery dialog. The recommended fix: catch the parse error in `DataStudio.jsx`'s `parseFile` CSV branch, extract the failing row number, and display a modal offering "Skip bad rows and continue" vs. "Abort". If the user chooses skip, re-parse with `skipBadLines: true` (Papa Parse) or `IGNORE_ERRORS=TRUE` (DuckDB `read_csv`). Show a banner in the data viewer listing skipped row numbers.
**Invariants:** Non-destructive — the raw file is never modified. The skip-rows flag must be stored in `loadOpts` so the load line in exported scripts includes the equivalent flag (`error="skip"` in R's `read_csv`, `on_error="skip"` in DuckDB).

---

## Features

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `country_code` pipeline step. The step takes an input column (country name or ISO-2/3 code) and a `targetFormat` (`"iso2"`, `"iso3"`, `"name"`) and outputs a new column with the converted value. Embed a compact static lookup table (~250 rows, iso2/iso3/name triplets) directly in the step or in a small `src/data/countryCodes.js` file — no external API. Runner.js applies the lookup row-by-row; unmatched values emit `null` with a warning count in the step result. Add a registry entry and a replication export that applies the same table using `countrycode::countrycode()` in R and `pycountry` in Python.
**Invariants:** Non-destructive pipeline — step replays on `rawData`. Must be added to `registry.js` in the same commit.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx:25`
**Suggestion:** `VariableSelector.jsx` renders a list of variable chips from `cols`. Add a text input at the top of each panel (Y, X, W) that filters the displayed chips using a case-insensitive substring match. The filter is purely local state in the component — no pipeline step, no API call. When a column name matches the filter, highlight the match substring. Clear the filter when a chip is selected (UX convention consistent with dropdown menus).
**Invariants:** Inline styles via `C` only. No external UI libraries.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue
**File:** `src/components/wrangling/FeatureTab.jsx:453` (`mode` state, `case_when` state)
**Suggestion:** `FeatureTab.jsx` already holds `mode` (line 453) and `case_when` state (line 461) in React `useState`. These reset on unmount when the user navigates to another tab. Lift the draft state for the active sub-form (case_when rows, if_else fields, mutate expression) into a session-scoped ref or `sessionStorage` key (keyed by `pid`), so that tab-switching and even page refresh preserves the draft. Restore draft state on mount. Discard draft state when the user applies a step (the step is now committed to the pipeline) or explicitly clicks a "Reset" button. This uses `sessionStorage` only for ephemeral draft UI state, not for pipeline data (which belongs in IndexedDB) — the architectural invariant is preserved.
**Invariants:** Pipeline data and raw data must never go to `sessionStorage` or `localStorage` — only the transient form draft (strings, arrays of `{condition, value}` pairs). `indexedDB.js` is unchanged.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** `History.jsx` renders the pipeline step list. Add an edit icon (✏) to each step row. On click, emit an `onEditStep(step)` callback to `WranglingModule`. `WranglingModule` handles this by (a) switching to the tab that owns the step's type (e.g. `"filter"` → Clean tab, `"log"` → Feature tab, `"group_summarize"` → Reshape tab) and (b) populating that tab's form fields from the step spec. Each sub-tab already receives its pipeline steps as props; add a `editingStep` prop and a `useEffect` that applies the step's spec to local form state on change. This is a read-from-spec operation — it does not write to the pipeline until the user re-applies.
**Invariants:** Non-destructive — editing a step from History is a form prefill, not a pipeline mutation. The pipeline only changes when the user clicks Apply.

---

### Model · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend the artifact metadata schema in `artifactOrder.js` to add an optional `group` string field. In `ModelBufferBar.jsx`, render a group-label badge on each pinned model card. Expose a right-click or ⋯ menu to assign/rename/remove a group. A "Compare" button scoped to a group runs the existing `ModelComparison` logic but filters `pinnedModels` to the selected group. Persisting group assignments: store them alongside the artifact order in IndexedDB (`saveArtifactOrder`), which already manages per-project global ordering.
**Invariants:** Persistence via `indexedDB.js` — never `localStorage`. Group metadata is display-only; it does not affect math or pipeline steps.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" mode to the Calculate workbench (alongside Derivatives and Integrals). Use the existing `math.js` (already bundled for symbolic computation in CalculateTab) to evaluate `math.limit(expr, variable, value, direction)`. Expose: expression input, variable picker, limit point, and side (left / right / both). Display the evaluated limit and the symbolic LaTeX form via the existing KaTeX renderer. Export to LaTeX as `\lim_{x \to c} f(x) = L`.
**Invariants:** Inline styles via `C` only. No external UI libraries. KaTeX render uses `trust: false` (already set).

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The calculate graph already plots function curves; add support for constant expressions (e.g. entering `c = 5` draws a horizontal line at y = 5). When the parser detects that an expression is constant (no free variable), render it as a dashed `hline` at that value using Observable Plot's `ruleY` (the same mark already used in PlotBuilder for `hline` geoms). Label the line with the constant name/value. No new dependencies required.
**Invariants:** Inline styles only. The graph rendering stays within CalculateTab — no pipeline step created.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis (3D plots)
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** Observable Plot (the current renderer) is 2D-only. True 3D requires a different library (e.g. Plotly.js). Given the privacy-first / no-CDN-for-compute constraint, Plotly would need to be bundled or loaded from a CSP-allowlisted CDN. This is a high-cost feature: new library, new render path, new export story. Recommend deferring to a post-MVP phase. For the near term, an "Add Z axis" option that produces a 2D heatmap (`tile` geom with `x`, `y`, `fill = z`) or a bubble chart (`point` geom with `size = z`) covers most econometric use cases (e.g. 3-variable relationships). These are already implemented and should be highlighted in the Help panel.
**Invariants:** Any new CDN script must be added to both `script-src` AND `connect-src` in `vercel.json` (this has caused three prior outages — see CLAUDE.md CSP bug section).

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, PlotBuilder already applies a color scheme and renders a legend. The feedback asks for richer in-plot labeling — specifically, category labels drawn directly on the plot marks (e.g. text annotations at the group centroid for scatter plots, or bar labels for grouped bars). Add an optional `showGroupLabels` toggle in the Labels panel; when on, add a `Plot.text(data, { x, y, text: colorCol, ... })` mark that uses the same color scale as the point/line marks. For line geoms, anchor the label at the last point per group (standard sparkline convention).
**Invariants:** Inline styles via `C`. No external libraries beyond the already-loaded Observable Plot CDN.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`
**Suggestion:** This is a cross-module design question. The suggested approach: extend `SessionStateProvider` with a `namedLists` map (`{[listName]: string[]}`). Expose `saveList(name, values)` and `getList(name)` from `useSessionState`. In CleanTab's filter builder, add a "From saved list" option that populates the `in`/`nin` predicate with a named list. In FeatureTab's `case_when` builder, allow referencing a list by name in condition values. Persist named lists to IndexedDB alongside the project pipeline so they survive reload. This is a greenfield feature — the design session with Franco should finalize the UX before implementation begins.
**Invariants:** Persistence goes through `indexedDB.js`. Lists are session-scoped metadata, not pipeline steps — they do not appear in `runner.js`. The filter builder's predicate must use `predicate.js`'s canonical operators (`in`, `nin`) when using a named list; no new operator.

---

## UX / Design (Deferred — fix-later)

### low · Clean · (deferred from 2026-06-14)
**Feedback:** R-style `%in% c(...)` membership syntax is not accepted by the filter expression UI
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** `predicate.js` owns the canonical condition language and already has `in`/`nin` operators. The gap is that the UI's free-text expression parser (used by the NL command bar and any typed filter field) does not recognise R's `x %in% c(a, b, c)` syntax. Add a pre-parse normaliser in `predicate.js` that converts `x %in% c(...)` → `{col: "x", op: "in", value: ["a","b","c"]}` before the main evaluator. This keeps the canonical language as the single owner of all operator semantics.
**Invariants:** `predicate.js` is the single owner of the condition language — never re-implement a filter operator elsewhere. Any new syntax sugar must translate to existing canonical operators, not add new ones.

---

### low · Model · (deferred from 2026-06-14)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c" regardless of actual column names
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** Add an editable label map stored in component state in `ReportingModule.jsx`: `{ [colName]: string }`. Render a small inline editor in the Stargazer preview panel where each row label can be overridden. Pass the label map into `buildStargazer` and use it to replace variable display names before emitting the `tabular` rows. The map is ephemeral (session-scoped state, not persisted) unless a future phase adds LaTeX metadata to IndexedDB.
**Invariants:** Inline styles only. `buildStargazer` in `latexTable.js` is pure — no React inside it.

---

### low · Spatial · (deferred from 2026-05-30)
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** The static geo-plot tab (Observable Plot / SVG) has no row-filter UI. Add a layer-scoped filter configuration to `GeoLayerConfig.jsx`: a simple predicate builder (column, operator, value) using `predicate.js`'s `evalPredicate`. The filter runs client-side on the layer's data slice before the SVG is rendered — no pipeline step is created. For datasets backed by DuckDB, route through `predicateToSQL` to filter at the SQL layer before materialising the layer data.
**Invariants:** `predicate.js` owns all filter operators. Spatial filters at this layer are display-only — they must not modify `rawData` or the pipeline.

---

### low · Model · (deferred from 2026-05-21)
**Feedback:** Add for RDD a summary table as in R with summary(rdrobust) — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/modeling/ModelPlots.jsx`, `src/math/CausalEngine.js`
**Suggestion:** The `runSharpRDD` result already includes `bw` (bandwidth), `h` (left/right half-widths), `n_left`/`n_right`, `beta`, `se`, `tStat`, `pVal`, and `fStat`. Add a collapsible `<RDDSummaryTable>` component in `ModelPlots.jsx` (below the RDD scatter plot) that displays these in the style of R's `rdrobust` console output: bandwidth, effective N on each side, polynomial order, kernel, and the coefficient table with `Coef / Std.Err / z / P>|z| / [CI_lo, CI_hi]`. No new math — only presentation of already-computed fields.
**Invariants:** The component must live in `src/components/modeling/` — zero React in `src/math/`. Inline styles via `C`.

---

## Performance

### high · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`
**Suggestion:** BugTriage marks Fase X4 as "code-complete; browser benchmark pending Franco." The CLAUDE.md DuckDB strategy addresses exactly this: CSV >10MB → parse to DuckDB Parquet → 500-row UI preview → OPFS persistence. The remaining risk is tab-transition lag (JS object allocation on module switch). Before declaring this fixed, Franco should browser-benchmark: load a 900k-row CSV, switch Data → Clean → Model → Explore rapidly, and confirm tab transitions complete in under 2s. If lag persists, the likely culprit is `WranglingModule` passing the full `cleanedData` array to child components; profile and add `useMemo`/`useCallback` guards on the pipeline result before the next heavy component mounts.
**Invariants:** Never truncate `rawData` before passing it to `runner.js` or `duckdbRunner.js`. Display limit (200–500 rows) ≠ computation limit.

---

## Suggested action order

1. **Fix CS DiD replication code** (`CallawayEngine.js` return block + R/Stata export destructuring) — critical correctness bug affecting a shipped estimator; the engine result simply never included the spec fields the exporter needs.
2. **Fix group_summarize cross-group math** (`runner.js` + `ReshapeTab.jsx`) — high-impact pipeline gap; ATE as difference-in-means is the most basic DiD diagnostic and the tab offers no way to compute it.
3. **Fix plot replication code to include load lines** (`PlotBuilder.jsx` + `ExplorerModule.jsx` + `loadLine.js`) — high-severity replication fidelity bug; a script that omits `read_csv(...)` is not runnable and undermines the core value prop of reproducibility.
4. Diagnose and fix Stata workbench breakage (`CodeEditor.jsx:74` → `stataScript.js`) — high; one of three replication languages is broken.
5. Browser-validate the four code-complete items (HC2, Plot Builder resize, diagnostics, heatmap) to close out open validation debt.
