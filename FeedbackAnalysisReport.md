# Feedback Analysis Report — 2026-06-03

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 11 open bugs · 10 open features · 0 open UX · 1 open performance · 1 open other

---

## Bugs

### [critical] · Calculate/Export · 2026-06-02
**Feedback:** `crash latex export`
**File:** `src/ReportingModule.jsx` (lines 261–335, `LatexPanel` component) + `src/services/export/latexTable.js`
**Suggestion:** `LatexPanel` calls `buildStargazer` inside a `useMemo` without guarding against `result.__error`, a null `result.varNames`, or an unwrapped FE/FD bundle `{ type:"FE/FD", fe, fd }`. Add the same `normaliseResult` unwrap pattern (already used in the main `ReportingModule` for SigCallout) before passing `result` to `buildStargazer`, and add `?? []` fallback on `varNames` in `latexTable.js:44`.
**Invariants:** `latexTable.js` is pure JS — keep it React-free.

---

### [critical→needs-repro] · Model · 2026-05-21
**Feedback:** `**URGENT** There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible`
**File:** `src/math/EstimationResult.js` (lines 22–23, result shape), `src/components/ModelingTab.jsx` (CoeffTable rendering)
**Suggestion:** BugTriage audit found no t/p desync in any of 11 engines. The coefficient-table column order is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig`, so the user most likely read `β̂ = 0.49` as the t-statistic. Add a prominent bold column-header label `t-stat` and consider colouring the `β̂` column header gold (`C.gold`) to visually distinguish it from the test-statistic column. Request a screenshot from the user confirming estimator + SE type before any math changes.
**Invariants:** Coefficient computation in `src/math/` must remain React-free; UI label changes go in `ModelingTab.jsx` only.

---

### [high] · Data · 2026-06-01
**Feedback:** `Not all columns of .dta are recognized — columns loaded twice appear as replicated entries without names`
**File:** `src/services/data/parsers/stata.js`
**Suggestion:** The parser likely appends a second copy of the variable-name list when a `.dta` file's variable-label block is misread as a second header row; or a `fval` / string-continuation branch doubles the column allocation. Audit the column-collection loop: ensure variable names are read from the header block exactly once, deduplicated by position index (not by name string), and that the missing-name fallback emits `col_N` rather than an empty string.
**Invariants:** Parser is pure JS in `src/services/data/`; no React imports.

---

### [high] · Data · 2026-05-13
**Feedback:** `.dta files are wrong loaded and readed`
**File:** `src/services/data/parsers/stata.js`
**Suggestion:** BugTriage status is "pending" — this predates and likely overlaps the 2026-06-01 duplicate-column report. Both complaints point to the same root: the parser mishandles multi-pass variable metadata. Fix in one combined pass: verify that the Stata 117/118 value-label dictionary loop, the variable-type array, and the variable-name array are all indexed from the same zero-based variable index and never re-iterated.
**Invariants:** Same as above.

---

### [high] · Data · 2026-05-31
**Feedback:** `OECD proxy error 406 for OECD.SDD.NAD,DSD_NAAG@DF_NAAG_I — acceptable content types: application/vnd.sdmx.genericdata+xml; version=2.1 and application/vnd.sdmx.data+json; version=2`
**File:** `src/services/data/fetchers/oecd.js:184`
**Suggestion:** Line 184 sends `Content-Type: application/json`; the OECD SDMX endpoint expects `Accept: application/vnd.sdmx.data+json; version=2` (not `Content-Type`). Change the header key from `Content-Type` to `Accept` and set the value to `application/vnd.sdmx.data+json; version=2` for data requests. Add `application/vnd.sdmx.genericdata+xml; version=2.1` as a fallback retry if JSON parsing fails.
**Invariants:** All external fetch calls go through `src/services/data/fetchers/` — do not route via `AIService.js`.

---

### [high] · Report/AI · 2026-05-25
**Feedback:** `AI does not recognize the pipeline steps in the Script generation`
**File:** `src/ReportingModule.jsx:968`, `src/services/AI/sessionSnapshot.js:77`, `src/App.jsx:2277`
**Suggestion:** `buildSessionSnapshot` reads `cleanedData?.pipeline` (line 77 of `sessionSnapshot.js`) but `cleanedData` is `tabOutput("report")` from `App.jsx`, which is `null` if the user runs a model without first visiting the Clean tab. When `cleanedData` is null the serialized pipeline is an empty array, so Claude receives no step context. Fix: pass the live `pipeline` state from `WranglingModule` to `ReportingModule` as a separate prop (not tied to the `cleanedData` output artifact), and prefer that prop in `buildSessionSnapshot`. CLAUDE.md marks this as pending item 5b (validation pending 2026-05-30).
**Invariants:** All Anthropic calls go through `AIService.js`; `sessionSnapshot.js` is pure JS.

---

### [high] · Model · 2026-05-28
**Feedback:** `Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators (currently only generic TWFE event study, which is biased under staggered treatment)`
**File:** `src/math/PanelEngine.js`, `src/components/ModelingTab.jsx`, `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** The current `runEventStudy` in `PanelEngine.js:222` implements generic TWFE event study, which yields contaminated estimates under staggered adoption. Add `runSunAbraham` (interaction-weighted: interact cohort-dummies × calendar-time dummies, aggregate to ATT with cohort-share weights) and `runCallawaySantana` (doubly-robust with never-treated / not-yet-treated comparison groups) as new exported functions in `PanelEngine.js`. Wire two new estimator dropdown entries in `EstimatorSidebar.jsx` and route them from `ModelingTab.jsx`. Validate against `R::sunab` and `R::did` before shipping (6dp coefficients, 4dp SE).
**Invariants:** Zero React in `PanelEngine.js`; SE type passed as argument; validate to 6dp/4dp against R.

---

### [high] · Model/AI · 2026-06-02
**Feedback:** `Change the interpretation according to the specification (Poisson e.g)`
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`), `src/ReportingModule.jsx:533`
**Suggestion:** The current paragraph-2 guidance covers OLS/FE/DiD/IV/RDD/Logit/Probit but is silent on Poisson and count models. Add a Poisson rule to the model-specific guidance block: "Poisson / Poisson FE — coefficients are log-incidence-rate-ratios; always report `exp(β) − 1` as the percentage change in the expected count alongside the raw log-coefficient; comment on overdispersion and whether Negative Binomial should be considered." The estimator name is already available via `result.spec.estimator` and should be included in the `interpretRegression` user payload.
**Invariants:** All prompt edits in `src/services/AI/Prompts/index.js`; `callClaude` must keep `SHARED_CONTEXT` as the cached block with `cache_control: {type:"ephemeral"}`.

---

### [medium] · General · 2026-05-20
**Feedback:** `When a dataset is deleted, all variables should disappear from all modules without having to select another dataset`
**File:** `src/components/workspace/DatasetManager.jsx:59` (`REMOVE_DATASET` dispatch), `src/services/session/sessionState.jsx:59–114`
**Suggestion:** `REMOVE_DATASET` fires correctly but `ModelingTab`, `ExplorerModule`, and `WranglingModule` only re-read the dataset list on the next user interaction rather than reacting to the removal immediately. Add a `useEffect` in each consumer that watches `sessionState.datasets`: when the currently selected dataset id is no longer in the list, reset local `xVars`, `yVar`, `activeDatasetId`, and `result` state to their initial values.
**Invariants:** State lives in React Context (`sessionState.jsx`) — do not move it to localStorage or IndexedDB.

---

### [medium] · Spatial · 2026-05-30
**Feedback:** `Adjust size respectively to the spatial unit(s) — geometry/polygon display sizing not scaling with unit type`
**File:** `src/components/tabs/spatial/map/layers.js` (`mkSLayer`), `src/components/tabs/spatial/plot/geo.js` (`mkGeoLayer`)
**Suggestion:** Stroke-width and marker radius are likely hardcoded in `mkSLayer` and `mkGeoLayer` rather than derived from the bounding-box extent of the loaded geometry. Compute the bbox diagonal using `geoBbox` (already in `src/components/tabs/spatial/plot/geo.js`) and scale the default stroke/radius by a `log(diag + 1)` factor so small communes render with proportionally thicker borders and national polygons render thinner. Apply the same logic to the Leaflet layer builder.
**Invariants:** Styling is inline; no CSS files; `SpatialEngine.js` in `src/math/` must remain React-free.

---

### [low] · Model · 2026-05-21
**Feedback:** `Add an option to clean the current model`
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** Add a "Clear model" button (trash icon, `C.red` colour) in the `EstimatorSidebar` header area that resets `yVar`, `xVars`, `result`, `pinnedModels`, and SE state back to their initial empty values. No math changes needed — this is a UI-only reset.
**Invariants:** Inline styles via `C` object only.

---

## Performance

### Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (CSV loader path), `src/pipeline/duckdbRunner.js`, `src/WranglingModule.jsx`
**Suggestion:** CLAUDE.md documents this as the agreed DuckDB performance track (Rules 1–7). Immediate mitigations: (1) lower the large-file DuckDB routing threshold in `DataStudio.parseFile` from 10MB to 5MB; (2) virtualise the data-viewer table (render only 200 visible rows, never the full 900k array); (3) debounce pipeline re-runs in `WranglingModule` by 300ms to prevent cascading re-renders on column-selector changes. The permanent fix is the Parquet+OPFS pipeline described in CLAUDE.md.
**Invariants:** DuckDB path in `duckdbRunner.js` must replay on `rawData` non-destructively; display truncation must never affect estimation or export.

---

## Other

### Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (CSV auto-delimiter detection) or `src/pipeline/duckdbRunner.js` (DuckDB CSV scanner)
**Suggestion:** The error suggests a stray embedded quote or a locale-formatted number (`2,425.47`) tripping the comma delimiter. Add a `try/catch` around the DuckDB `COPY` command and, on failure, retry with `quote=''` (disable quoting) and `decimal_separator=','` options in the DuckDB CSV scanner. Surface the offending line number and column name in the error message so the user can fix the source file.
**Invariants:** Parsing happens entirely client-side; do not send data to any server.

---

## Features

### Model · 2026-05-11
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js:28` (`andersonRubinCI`), `src/ReportingModule.jsx:908`
**Suggestion:** This feature is already implemented. `andersonRubinCI` exists at `CausalEngine.js:28` and is surfaced at `ReportingModule.jsx:908` as "Weak-IV robust CI (Anderson-Rubin, 95%)" for the single-endogenous-variable case. Verify in the browser that the row appears in the 2SLS output table when `second.arCI` is set. If invisible, the issue is a conditional rendering bug (`xVars.length === 1` guard) rather than a missing feature.
**Invariants:** None — math already exists.

---

### Model · 2026-05-11
**Feedback:** `Add multiple statistics tests`
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`, `src/math/LinearEngine.js`
**Suggestion:** This request is underspecified — clarify scope with the user. Most likely candidates: joint F-test / Wald χ² on user-selected regressor subsets, Hausman endogeneity test (FE vs RE), or Sargan/Hansen J overidentification (already in `GMMEngine.js`). A reasonable first step is a "Hypothesis tests" collapsible panel in `DiagnosticsPanel.jsx` that shows a Wald χ² for a user-entered linear restriction `Rβ = r`, implemented as pure JS in `LinearEngine.js`.
**Invariants:** New test math goes in `src/math/LinearEngine.js` or a new `src/core/inference/` file — zero React; SE type passed explicitly.

---

### Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/modeling/ModelPlots.jsx` or `src/components/ModelingTab.jsx`
**Suggestion:** Add a `<RDDSummaryTable>` component in `ModelPlots.jsx` that renders the rdrobust-style block: bandwidth (h), kernel type, polynomial order, effective N left/right, conventional LATE with SE, bias-corrected LATE, robust CI, and first-stage F (fuzzy only). All values are already present in the `EstimationResult` returned by `runSharpRDD` / `runFuzzyRDD`; this is a pure display task requiring no new math.
**Invariants:** Display component only; inline styles via `C`; no new math functions.

---

### Model · 2026-05-25
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/math/LinearEngine.js:128` (intercept added unconditionally), `src/components/ModelingTab.jsx`, `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add a `noIntercept?: boolean` option to `runOLS` / `runFE`. When true, skip the `addOnes` column and adjust `k` and `df` accordingly (`df = n − k_reg` not `n − k_reg − 1`). Wire a "No intercept" checkbox in `ModelConfiguration.jsx` and pass the flag down to the engine. Add a visible warning in the output that R² lacks its standard interpretation without an intercept.
**Invariants:** Zero React in `LinearEngine.js`; SE type still passed explicitly; do not hardcode any intercept assumption inside the engine.

---

### Simulate · 2026-05-18
**Feedback:** `Add resampling, bootstrapping, sampling to the mean`
**File:** `src/components/tabs/SimulateTab.jsx`
**Suggestion:** Add a "Bootstrap" mode alongside the existing Monte Carlo mode. In bootstrap mode the user selects a dataset column and a statistic (mean/median/sd); the engine draws B=1000 samples with replacement in pure JS (no new engine file) and plots the bootstrap distribution using the existing `MCHistogram` component. The mode toggle and column-picker can follow the same pattern as the existing OLS/Single toggle added in Phase 2026-05-30.
**Invariants:** Pure JS sampling logic; no localStorage; no new engine file required.

---

### Explore · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/pipeline/runner.js` (filter step), `src/pipeline/registry.js`
**Suggestion:** Add an `in` operator to the filter step in `runner.js` that accepts `{ op: "in", values: string[] }` and evaluates as `values.includes(row[col])`. In `CleanTab.jsx` FilterBuilder, add an "In list" operator that renders a tag/multi-value input (comma-separated). Register the new operator in `registry.js` under the existing `filter` step type — no new step type is needed.
**Invariants:** Pipeline step must replay on `rawData` non-destructively; registry.js must stay in sync with runner.js; no localStorage.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`
**Suggestion:** Add a "Show grid" toggle to the SVG geo-plot tab. When enabled, render a `<g>` layer of equally spaced latitude/longitude lines using the `lonToTx` / `latToTy` helpers already in `src/components/tabs/spatial/shared/leaflet.js`, projected to SVG canvas coordinates. Annotate tick values at the margins using the existing `GEO_MARGIN` constants from `legend.js`.
**Invariants:** SVG rendering uses inline styles; no CSS files; `SpatialEngine.js` stays React-free.

---

### Spatial · 2026-05-30
**Feedback:** `Geometry transformations and show EPSG`
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`, `src/components/tabs/spatial/shared/crs.js`
**Suggestion:** `CRSTransformSection.jsx` already handles CRS re-projection via `loadProj4` and `PRESET_CRS`. Extend the UI to display the detected EPSG code of the loaded geometry (parse it from the `AUTHORITY["EPSG","XXXX"]` substring in the WKT string, or from the user's CRS selection) as a read-only teal badge (`C.teal`). For additional geometry transforms (centroid, area, convex hull), add thin wrapper calls to the existing `SpatialEngine.js` helper functions and expose them as buttons in the section.
**Invariants:** `SpatialEngine.js` stays React-free; inline styles only.

---

### Spatial · 2026-05-30
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer filter panel in `GeoLayerConfig.jsx` that lets the user specify a column, operator, and value. Pre-filter `rows` with that condition before passing to `mkGeoLayer` in `geo.js`. Reuse the same `eq/ne/gt/lt/in` operator vocabulary as the pipeline filter step, applied at render time only — this does not modify `rawData` or the pipeline.
**Invariants:** Display-only filter; does not modify `rawData`; inline styles via `C`.

---

### Simulate · 2026-06-02
**Feedback:** `You could add different types of quantiles like in R, but it might not be necessary`
**File:** `src/components/tabs/SimulateTab.jsx`
**Suggestion:** Low priority per the user's own hedging. If implemented: add a "Quantile type" dropdown (Type 5 = default R midpoint, Type 7 = Excel-compatible linear interpolation, Type 1 = empirical/inverse CDF) to the Monte Carlo output summary and apply the selected interpolation formula when computing percentile bands on the simulated distribution. A pure JS helper (no new engine file) is sufficient.
**Invariants:** Pure JS; no localStorage.

---

## Suggested action order

1. **Fix LaTeX export crash** (`src/ReportingModule.jsx` `LatexPanel` + `src/services/export/latexTable.js`) — critical, breaks a primary output feature; add `normaliseResult` unwrap and `varNames ?? []` guard before `buildStargazer`.
2. **Fix OECD 406 Accept-header error** (`src/services/data/fetchers/oecd.js:184`) — one-line change (`Content-Type` → `Accept: application/vnd.sdmx.data+json; version=2`); unblocks live macro data fetching immediately.
3. **Fix .dta duplicate/unnamed-column parser bug** (`src/services/data/parsers/stata.js`) — two overlapping high-severity reports (2026-05-13 + 2026-06-01) point to the same root; fix once with a single indexed column-allocation pass.
4. **Add Poisson IRR interpretation rule to AI prompt** (`src/services/AI/Prompts/index.js`) — one-paragraph addition prevents confidently wrong linear-effect narratives in Poisson model outputs.
5. **Fix pipeline steps not reaching AI script generator** (`src/services/AI/sessionSnapshot.js` + `src/ReportingModule.jsx`) — pass live `pipeline` prop to `ReportingModule` independent of `cleanedData`; resolves CLAUDE.md pending item 5b.
6. **Fix dataset-deletion variable bleed across modules** (`DatasetManager.jsx` + `sessionState.jsx`) — add `useEffect` guards in `ModelingTab`, `ExplorerModule`, and `WranglingModule` that reset variable selectors when the active dataset id is removed.
7. **Sun-Abraham / Callaway-Santana event study estimators** (`PanelEngine.js`) — significant math-correctness issue for staggered DiD; larger effort, validate against R `::sunab` and `::did` before shipping.
