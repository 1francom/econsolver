# Feedback Analysis Report — 2026-06-08

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **7 open bugs** · **17 open features** · **0 open UX** · **1 open performance** · **1 open other**
- Total open items analysed: **26**
- Cross-reference note: Several ClaudeFB.md items lack strikethrough/✓ yet BugTriage marks them `fixed` — those are treated as resolved. Only items with BugTriage status `open`, `pending`, or `needs-repro` (or absent from BugTriage entirely) are included below.

---

## Bugs

### [critical] · Calculate · 2026-06-02
**Feedback:** `crash latex export`
**File:** `src/components/tabs/CalculateTab.jsx` (line 289, `download(...)` call), `src/components/calculate/workbench/ResultsPanel.jsx` (line 21, `renderInto` call)
**Suggestion:** BugTriage status is `needs-repro`. Two candidate crash paths: (1) `ResultsPanel.jsx:21` — `loadKatex().then(k => renderInto(k, ref.current, latex))` will throw for malformed LaTeX output from the symbolic backend; the promise rejection is unhandled; wrap in `.catch(()=>{})` and degrade to plain-text. (2) `CalculateTab.jsx:289` — the `download()` call with a template string containing `${latex}` can receive `undefined` if no equation has been evaluated; guard with a null check before calling. Request a browser console screenshot from Franco to confirm which path triggers first.
**Invariants:** Fix lives in `CalculateTab.jsx` and `ResultsPanel.jsx` only. `src/math/` files are pure JS — no changes there.

---

### [critical] · Model / Inference · 2026-05-21
**Feedback:** `**URGENT** There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible`
**File:** `src/math/LinearEngine.js` (`pValue(t, df)` lines 80–82), `src/math/EstimationResult.js`
**Suggestion:** BugTriage status is `needs-repro`. A 2026-05-26 audit found no desync — `pValue(0.49, 100) ≈ 0.625` matches R. Most likely the user read the `β̂` column as the `t` column (table layout: Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig). Before any code change: request a screenshot and estimator name from Franco. If confirmed real, verify that `tStats` and `pVals` remain index-aligned after any slice on `varNames` (e.g. when FE unit fixed-effects are dropped from display). As a prevention measure, add a `data-col="t"` / `data-col="p"` attribute (or tooltip) on each coefficient-table column header.
**Invariants:** `src/math/` is pure JS — zero React. Any display fix goes in `resultDisplay.jsx`.

---

### [high] · Data / Stata · 2026-05-13
**Feedback:** `.dta files are wrong loaded and readed`
**File:** `src/services/data/parsers/stata.js` (`parseStata()` ~line 223)
**Suggestion:** BugTriage status is `pending`. The parser handles formats 117–119 (Stata 13–15). A format-119 column-name stride bug was fixed 2026-06-03, but this earlier entry remains open, suggesting a different failure mode. Most probable causes: (1) `strL` long-string values (formats 118/119) whose `v`/`o` address pairs are not resolved, leaving columns as `null`; (2) value-label decoding applying the wrong label map index. Request the user's `.dta` file or a synthetic fixture and trace against Stata's `describe` / `list` output.
**Invariants:** `src/services/data/parsers/stata.js` is pure JS — no React imports.

---

### [high] · Report / AI · 2026-05-25
**Feedback:** `AI does not recognize the pipeline steps in the Script generation`
**File:** `src/ReportingModule.jsx` (~line 764, `AIUnifiedScript._buildModelScript`), `src/services/AI/sessionSnapshot.js` (`buildSessionSnapshot`, `serializeSnapshot`), `src/services/AI/Prompts/index.js` (`UNIFIED_SCRIPT_PROMPT` rule 8)
**Suggestion:** Matches CLAUDE.md pending item 5b (marked `VALIDATION PENDING 2026-05-30`). Infrastructure exists but is unvalidated. Two likely failure modes: (1) `cleanedData.pipeline` is empty when the user lands directly on the Report tab without visiting Wrangling — guard with a non-null check in `buildSessionSnapshot`; (2) the serialized snapshot is not attached to the AI user prompt — add a temporary `console.log(snapshot)` gate in `AIUnifiedScript._buildModelScript` and confirm the block is non-empty. Franco should validate by loading a `;`-delimited CSV then checking that the generated R/Python/Stata script contains the correct load call and all pipeline steps.
**Invariants:** All Anthropic API calls must go through `AIService.js` — no raw `fetch` elsewhere. Cached block must be present on every `callClaude` invocation.

---

### [high] · Model / Panel · 2026-05-28
**Feedback:** `Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators`
**File:** `src/components/modeling/EstimatorSidebar.jsx` (estimator list ~line 28), `src/components/modeling/runners/estimationDispatch.js`, `src/math/NonLinearEngine.js` (`runSunAbraham`), `src/math/CallawayEngine.js`
**Suggestion:** `CallawayCS` already has a sidebar entry ("CS DiD", id `CallawayCS`). `runSunAbraham` is implemented in `NonLinearEngine.js` and validated against R. The gap for SA: no standalone sidebar entry — it is only reachable as a combo path. Add `{ id: "SunAbraham", label: "SA DiD", group: "Panel", desc: "Sun & Abraham (2021) heterogeneity-robust event study — panel required", color: "#6ec8b4" }` to `EstimatorSidebar.jsx` and confirm the `estimationDispatch.js` branch `effModel === "SunAbraham"` routes correctly. Also update `APP_CAPABILITY_MAP` in `src/services/AI/appCapabilityMap.js` per CLAUDE.md convention. Relabel the existing generic "Event Study" estimator to clarify it is plain TWFE (biased under staggered treatment).
**Invariants:** `APP_CAPABILITY_MAP` must be updated when a new estimator is added to the sidebar.

---

### [medium] · Data / Explorer · 2026-06-05
**Feedback:** `Don't round the numbers when inserting, deleting or overwriting`
**File:** `src/ExplorerModule.jsx` (line 1259: `const fmt = v => ... v.toFixed(3)`)
**Suggestion:** The cell-edit commit path likely coerces values through `v.toFixed(3)` before writing back to the data object, causing irreversible precision loss. Fix: separate the display formatter from the commit path — apply `.toFixed` only inside the `<td>` renderer, never on write. On cell commit, store the raw `parseFloat(inputValue)` (full precision) in the backing row. Audit lines 86, 334, 489, and 1259 in `ExplorerModule.jsx` for any `.toFixed` / `.round` calls on the write path.
**Invariants:** `rawData` is the immutable source; pipeline replays on it. Edits must write full-precision values through `saveRawData` in `indexedDB.js`.

---

### [low] · Spatial / Geocoding · 2026-06-06
**Feedback:** `Geocoding directions might be upgraded — geocoder address resolution quality needs improvement`
**File:** `src/components/tabs/spatial/analyze/GeocodeSection.jsx`, `src/services/data/geocoding.js`
**Suggestion:** The Photon/Komoot default applies a München bounding-box preset that silently constrains all queries to Bavaria. Expose the bbox preset as a user-configurable dropdown (or allow clearing it). Add a result-confidence display: Photon returns `properties.type` (`city`, `street`, `house`) and an `importance` score — surface these alongside the resolved coordinates so users can audit match quality. Optionally add a fallback chain (Photon → custom Nominatim endpoint already in settings) when Photon returns a low-importance result.
**Invariants:** All geocoding remains client-side initiated; API keys supplied by the user, never hardcoded.

---

## Features

### Model / IV · 2026-05-11
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js` (alongside `run2SLS`), `src/components/modeling/results/IVResults.jsx`
**Suggestion:** AR CI inverts the AR test: grid-search `β₀` values, regress `(y − X·β₀)` on `Z`, collect the F-statistic, and invert the rejection region. Add `runAndersonRubin(rows, yCol, xCols, zCols, seOpts)` to `CausalEngine.js` returning `{ lo, hi, fStat, gridBeta }`. Surface in `IVResults.jsx` as an additional CI row labelled "AR CI (weak-IV robust)" — triggered when first-stage F < 10 (already flagged by `coachingTriggers.js`).
**Invariants:** `src/math/CausalEngine.js` must have zero React imports. `seType` passed explicitly.

---

### Model / Tests · 2026-05-11
**Feedback:** `Add multiple statistics tests`
**File:** `src/math/SampleTests.js`, `src/components/tabs/statsim/SampleTestPanel.jsx`
**Suggestion:** `SampleTests.js` already implements one-sample t, variance χ², and generic parameter t/z. Extend with: two-sample t (pooled and Welch), paired t, two-proportion z, Pearson/Spearman correlation test, and chi-square goodness-of-fit (for categorical frequencies). Add the new test types to the `SampleTestPanel` dropdown. Each function must accept explicit `seType`/`alpha` arguments for consistency.
**Invariants:** `src/math/SampleTests.js` is pure JS — no React.

---

### Simulate · 2026-05-18
**Feedback:** `Add resampling, bootstrapping, sampling to the mean`
**File:** `src/components/tabs/SimulateTab.jsx`, `src/math/Resampling.js`
**Suggestion:** `Resampling.js` already exports `bootstrapMean`, `subsampleMean`, `permutationTwoSampleMean`, and `bootstrapStatistic`. These are imported in `StatWorkspace.jsx` but may not be exposed in `SimulateTab.jsx`'s UI. Add a "Resampling" sub-mode to `SimulateTab.jsx` with a column picker, method selector (bootstrap / subsample / permutation), iteration count, run button, and a histogram of the resampled statistic distribution. Reuse the existing `MCHistogram` component.
**Invariants:** `src/math/Resampling.js` is pure JS; `SimulateTab.jsx` may import freely.

---

### Explore / Clean · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder, lines 606–633), `src/pipeline/runner.js` (lines 238–242), `src/pipeline/registry.js`
**Suggestion:** The `"in"` operator is already implemented in both `CleanTab.jsx` and `runner.js`. The UX gap is that entering a list of values requires manual comma-separated typing — add a multi-select dropdown populated from the unique values of the selected column (already computed for column stats) so users can tick items rather than type them. No runner.js or registry changes are needed.
**Invariants:** Inline styles via `C` only. Pipeline steps replay on `rawData` without mutation.

---

### Spatial / Plot · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/math/SpatialEngine.js`
**Suggestion:** The Map tab (Leaflet) already supports grid overlays; the SVG geo-plot tab does not. Add a "Grid" layer type in `GeoLayerConfig.jsx` that renders rectangular or H3 grid cell WKT polygons as SVG `<polygon>` elements using the existing `parseWktRings` helper from `wkt.js`. The grid data comes from a user-selected WKT column (output of the Spatial → Grid analyze step). No new math is needed — `buildRectGrid` and `buildH3Grid` already exist in `SpatialEngine.js`.
**Invariants:** `src/math/SpatialEngine.js` is pure JS. No React in math files.

---

### Model / RDD · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/modeling/ModelPlots.jsx` or new `src/components/modeling/results/RDDResults.jsx`, `src/math/CausalEngine.js`
**Suggestion:** All fields for an rdrobust-style summary (cutoff, bandwidth `h`, effective N left/right, polynomial order, kernel type, LATE, SE, CI, p-value) already exist in the `runSharpRDD` result object. Add a `RDDSummaryTable` component mounted above the generic coefficient table when `result.type === "RDD"` or `"FuzzyRDD"`. Follow the `IVResults.jsx` pattern for layout. No math changes needed.
**Invariants:** New display component uses inline styles via `C` from `shared.jsx`.

---

### Model / Estimation · 2026-05-25
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/math/LinearEngine.js` (`runOLS`, `runWLS`), `src/components/ModelingTab.jsx`, `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add `noIntercept: boolean` to the options/seOpts object passed to `runOLS` and `runWLS`. When true, omit the leading column of ones from X and set `df = n − k` (not `n − k − 1`) to match R's `lm(y ~ x + 0)`. Propagate the flag through `run2SLS`, `runFE`, `runFD`, and all DuckDB SQL paths that prepend `1.0 AS _const`. Expose as a checkbox in `ModelConfiguration.jsx`. Display a warning if the model has no constant and the running variable is not centred.
**Invariants:** `seType` is still passed explicitly and is independent of `noIntercept`. The SQL `duckdbOLS.buildOLSSuffStats` path must also omit `_const` when `noIntercept` is true.

---

### Spatial / CRS · 2026-05-30
**Feedback:** `Geometry transformations and show EPSG`
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`, `src/components/tabs/spatial/shared/crs.js`
**Suggestion:** Two improvements: (1) Expand `PRESET_CRS` in `crs.js` with a searchable EPSG lookup (common codes: 4326, 3857, 25832–25836 for European users, 32618–32638 for UTM) and display the EPSG code alongside the transform result. (2) Add a separate `GeometryTransformSection.jsx` in `src/components/tabs/spatial/analyze/` for affine geometry operations (scale, translate, rotate) applied to WKT columns via proj4 (CDN singleton already loaded). Both sections must follow the `onResult(rows, newCols)` interface contract.
**Invariants:** All analyze sections communicate via `onResult(rows, newCols)` — no direct state mutations.

---

### Spatial / Plot · 2026-05-30
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer "Filter rows" control in `GeoLayerConfig` — a column picker plus value/range input that subsets rows before rendering. Store as `colFilter: { col, op, val }` in component state and apply at render time (display-only, non-destructive). A simple eq / range / in-list operator is sufficient for the first iteration.
**Invariants:** Must not mutate `rawData` or emit a pipeline step — display-layer filter only.

---

### Data / Explorer · 2026-06-01
**Feedback:** `Add an option to edit/assign column names directly in the Data Viewer`
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Make column header cells double-click editable. On commit, emit a `rename` pipeline step (already in `runner.js` and `duckdbRunner.js`) so the rename is non-destructive, undoable, and appears in the pipeline history sidebar. Do not mutate `rawData.headers` directly.
**Invariants:** Use `rename` step in `runner.js` — never mutate `rawData` directly. Persistence via `indexedDB.js`.

---

### Simulate · 2026-06-02
**Feedback:** `You could add different types of quantiles like in R, but it might not be necessary`
**File:** `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** Low priority per the user's own qualifier. If implemented, add a quantile-type selector (types 6 and 7 cover SAS/SPSS and R defaults) to the distribution summary section. Implement `quantileTyped(sorted, p, type)` as a pure helper in `src/math/SampleTests.js`. Default to type 7 to match R's `quantile()`.
**Invariants:** Pure JS math helpers — no React in `src/math/`.

---

### Spatial / Plot · 2026-06-03
**Feedback:** `Add geometry to plot points, not just lat and lon`
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/shared/wkt.js`
**Suggestion:** Extend `GeoLayerConfig` with a "position from" toggle: `"Lat/Lon columns"` vs `"WKT geometry column"`. For POINT WKT, extract coordinates via `wktToLeaflet` from `wkt.js`. For POLYGON / MULTIPOLYGON, render SVG polygons via `parseWktRings` (already used in the geo plot canvas). The `guessWktCol` helper in `guess.js` can pre-select the WKT column automatically.
**Invariants:** Changes to `wkt.js` must not break existing callers in `SpatialPlotTab.jsx`.

---

### Calculate · 2026-06-03
**Feedback:** `Add a "show steps button" for integrals, derivatives and optimizations`
**File:** `src/components/tabs/CalculateTab.jsx`, `src/math/symbolicDiff.js`, `src/components/calculate/workbench/ResultsPanel.jsx`
**Suggestion:** For derivatives: add a `showSteps` mode to `symbolicDiff.js` that annotates the chain/product/quotient rule decomposition in the AST, then render the steps as a KaTeX list in `ResultsPanel.jsx`. For integrals (currently numeric quadrature): surface the Riemann sum breakdown or Newton iteration table in a collapsible panel. A full symbolic step-by-step system for integrals requires SymPy escalation. All step rendering goes in the UI layer — math files stay pure.
**Invariants:** `src/math/symbolicDiff.js` is pure JS — step rendering must stay in `ResultsPanel.jsx`.

---

### Model / Buffer · 2026-06-04
**Feedback:** `Persistence across sessions of pinned models`
**File:** `src/services/Persistence/indexedDB.js` (`saveModelBuffer` line 654, `loadModelBuffer`), `src/services/modelBuffer.js` (line 32), `src/components/ModelingTab.jsx`
**Suggestion:** `saveModelBuffer(pid, models)` is already called on every buffer change (line 32 of `modelBuffer.js`). Verify that `ModelingTab.jsx` (or `DataStudio.jsx`) calls `loadModelBuffer(pid)` on mount and restores the pinned models state. If missing, add a `useEffect` on mount that loads the buffer and calls `setPinnedModels`. Use `trimResult` from `services/Persistence/trimResult.js` to keep payloads small. This is likely a one-line mount hook.
**Invariants:** IndexedDB only — never `localStorage`. Scoped to `pid` to avoid project state bleed.

---

### Spatial / Map · 2026-06-05
**Feedback:** `Allow for colour selection with the "color by" option, useful for scales also`
**File:** `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`, `src/components/tabs/spatial/map/ColorRow.jsx`, `src/components/tabs/spatial/shared/color.js`
**Suggestion:** `buildColorScale` in `color.js` already supports categorical and sequential scales. Extend the map layer editor with a palette picker: categorical options (Observable 10, Tableau 10) for string columns, and sequential/diverging scales (viridis, RdBu) with domain min/max inputs for numeric columns. Pass the chosen palette name as a prop to `buildColorScale`.
**Invariants:** Inline styles via `C`. No external UI component libraries.

---

### Calculate / Symbolic · 2026-06-06
**Feedback:** `Add derivatives with endogenous functions (e.g. f(x) = 2x² − c(x), compute dc/dx symbolically)`
**File:** `src/math/symbolicDiff.js`, `src/math/cas/casAdapter.js`
**Suggestion:** This requires treating user-defined function calls (`c(x)`) as opaque symbols during differentiation and emitting `c′(x)` via the chain rule. Extend `symbolicDiff.js` to accept a `userFunctions: Map<name, exprAST>` argument; when the differentiator encounters a call to a known user function, expand it inline before applying chain-rule differentiation. Expressions with unresolvable function calls should fall through to the SymPy escalation path in `casAdapter.js`.
**Invariants:** `src/math/symbolicDiff.js` and `src/math/cas/` are pure JS — no React. Route SymPy escalation through `casAdapter.js`; do not call SymPy directly from UI components.

---

### Model / RDD · 2026-06-06
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/math/CausalEngine.js` (lines 577–579, `varNames` construction in `runSharpRDD`)
**Suggestion:** Replace the hardcoded strings with names derived from the actual column names already in scope: `"D (treatment)"` → `treatCol`; `"running − c"` → `` `${runCol} − ${cutoff}` ``; `"D × (running − c)"` → `` `${treatCol} × (${runCol} − ${cutoff})` ``. These column names are already passed into the function. The `varNames` array propagates automatically through `wrapResult` to the UI and to `buildLatex` / `buildStargazer` — no downstream changes needed.
**Invariants:** Change is in `src/math/CausalEngine.js` only — pure JS, zero React. Must also update the Fuzzy RDD equivalent at lines 844–848.

---

## Performance

### [high] · Data / DuckDB · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (file load handler), `src/services/data/duckdb.js` (DuckDB singleton), `src/WranglingModule.jsx` (dual-path runner)
**Suggestion:** Per CLAUDE.md DuckDB performance rules: (1) confirm CSV files >10 MB are auto-converted to Parquet on first DuckDB load (`COPY ... TO ... (FORMAT PARQUET)`) and cached in OPFS — eliminates re-import cost on every session open; (2) verify `vercel.json` includes `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` for `SharedArrayBuffer` / threaded WASM; (3) confirm the React data-table is virtualised to ≤500 rows and never materialises the full 900k-row JS array; (4) add a progress indicator during DuckDB import so users do not interpret a busy tab as a crash.
**Invariants:** Computation always runs on the full dataset in `runner.js` / `duckdbRunner.js` — display truncation must never affect pipeline output. Data never leaves the browser.

---

## Other

### Data / CSV · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (DuckDB CSV loader), `src/services/data/duckdb.js`
**Suggestion:** This is a DuckDB sniffer error on what appears to be a well-formed World Bank panel CSV. Most likely cause: BOM character, trailing comma, or CRLF/LF inconsistency at line 1583. Mitigation: (1) add a pre-flight BOM strip before DuckDB ingestion; (2) retry the `read_csv` call with `IGNORE_ERRORS=TRUE` + `SAMPLE_SIZE=0` (full scan) and surface a banner showing the count and first few skipped rows. Surface the raw offending line in the error UI. Note: no BugTriage entry exists for this item — create one.
**Invariants:** DuckDB is the correct loader for large CSVs. Do not silently drop rows without user notification.

---

## Suggested action order

1. **Add Sun-Abraham as a standalone sidebar estimator** (`EstimatorSidebar.jsx` one-line insertion + `estimationDispatch.js` guard + `APP_CAPABILITY_MAP` update) — engine is fully validated, dispatch branch already exists; lowest-effort fix for a critical causal-inference bias concern that currently misleads users running TWFE on staggered treatments.
2. **Browser-validate Report AI pipeline script generation (CLAUDE.md §5b)** — shipped but unvalidated since 2026-05-25; blocks users from replicating any workflow. Load a `;`-delimited CSV and verify R/Python/Stata unified scripts contain the correct load call and pipeline steps.
3. **Fix `.dta` loading regression in `stata.js`** — pending since 2026-05-13 despite the June stride fix; likely affects format-118 users with value labels or `strL` columns.
4. **Fix cell-editor rounding in `ExplorerModule.jsx:1259`** — medium severity but high data-integrity trust impact; move all `toFixed`/`round` calls from the write path to the display path only.
5. **Wire pinned-model `loadModelBuffer` on mount in `ModelingTab.jsx`** — `saveModelBuffer` is already called on every change; adding the reciprocal load on mount is likely a one-line `useEffect`.
