# Feedback Analysis Report — 2026-06-16

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.
> Supersedes 2026-06-15 report. The X5 reconciliation audit in ClaudeFB.md (appended 2026-06-14) resolved or explicitly deferred the majority of prior open items. This report covers what remains open after X5 plus new items from the 2026-06-14 ClaudeFB batch.

## Summary
- **7 open bugs** (5 code-complete pending browser validation · 1 fix-later · 1 effectively fixed, ClaudeFB not updated)
- **5 open features** (all marked fix-later in BugTriage)
- **0 open UX / Design**
- **1 open performance** (cross-listed with bugs — large-dataset crash)
- **1 open other** (server-side XSS hardening follow-up)
- **Total distinct open items: 13**

> **Exclusion methodology:** Items recorded as "verified fixed" or "wontfix" in the X5 reconciliation table (ClaudeFB.md bottom section) are excluded even if the ClaudeFB bullet is not struck through or marked ✓. Those are housekeeping for ClaudeFB.md only.

---

## Bugs

### [high] · Model · 2026-06-11 · code-complete, browser validation pending
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6`. The fix freezes OLS/WLS diagnostic display to the estimation's frozen `result.spec` so that sidebar edits cannot mutate already-displayed plots. Franco should: (a) run an OLS estimation and confirm Y~Ŷ scatter, Residual vs Fitted, and Q-Q plots all render; (b) add a new regressor without re-estimating and confirm the displayed diagnostics do not change; (c) restore a pinned model from ModelBufferBar and confirm all three plots reappear correctly. If freeze fails, trace whether `result.spec` is passed by reference through the `ModelingTab → ResidualPlots` prop chain — a deep copy at estimation time is required.
**Invariants:** Diagnostic plots must read only from the frozen `result.spec`, not from live sidebar state. `src/math/LinearEngine.js` must remain React-free.

---

### [high] · Model · 2026-06-14 · code-complete, browser validation pending
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` (same root cause as the OLS residual bug above — live sidebar state leaking into the displayed result). Validate in the same browser session: after estimation, toggle a variable checkbox in VariableSelector and verify the coefficient bar chart does not redraw. Restore a pinned model and confirm it renders without mixing in the current sidebar variables. Both items share a single validation pass.
**Invariants:** Result display components must read from `result.spec` (immutable at estimation time), never from live `xVars`/`yVar` React state.

---

### [high] · Model · AI · 2026-06-14 · code-complete, pending ClaudeFB ✓
**Feedback:** 2SLS Poisson interpretation wrong — AI narrative interprets IV-Poisson coefficients as linear 2SLS effects instead of incidence-rate ratios.
**File:** `src/services/AI/Prompts/index.js`
**Suggestion:** Fixed in commit `bc2d7b9d` — `IVPoisson` now follows the Poisson IRR rule in `INTERPRET_REGRESSION_PROMPT` and separately discusses instrument relevance/exclusion. No code action needed. Franco should estimate an IV-Poisson model, click "Interpret results," and confirm the narrative says something like "a one-unit increase in X is associated with a factor of exp(β) change in expected count" rather than "β additional units of Y." Also confirm instrument relevance/exclusion discussion is present. After validation, mark ClaudeFB.md `[2026-06-14 11:13]` as `✓ fixed 2026-06-14 (bc2d7b9d)`.
**Invariants:** All Anthropic API calls go through `src/services/AI/AIService.js` only — `Prompts/index.js` holds prompt text, not API calls.

---

### [medium] · Model · 2026-06-10 · code-complete, browser validation pending
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` — the plot viewport now has native vertical resize with bounded responsive canvas sizing so PNG captures user-selected dimensions. Franco should: drag the resize handle to a non-default height; export PNG; confirm dimensions match the in-browser view (correct aspect ratio, no clipped axes, no theme bleed). Also test SVG export. If the resize handle is missing, verify `ResizeObserver` is wired in `PlotBuilder.jsx` and that canvas `width`/`height` attributes are set from the observed container size before `context.drawImage`.
**Invariants:** Inline styles only via the `C` object from `shared.jsx` — no CSS files.

---

### [medium] · Spatial · 2026-06-13 · code-complete, browser validation pending
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` (buildHeatmapCells at line ~20), `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete in commit `5e105529` — Leaflet KDE heatmap layer restored with palette/opacity controls, legend, HTML export, and a 5 k-point cap. Browser checklist: (a) load a point dataset with lat/lon columns; (b) open Map sub-tab in Spatial, add a Heatmap layer; (c) confirm KDE overlay renders; (d) adjust palette and opacity sliders and confirm live update; (e) click HTML export and confirm heatmap is captured; (f) test with > 5 k rows and confirm no crash. If layer doesn't render, check that `buildHeatmapCells` at `SpatialPlotTab.jsx:20` receives valid rows (not undefined) — the `lyRows(ly)` helper must resolve the layer data source before passing to the builder.
**Invariants:** Map sub-tab is self-contained with Leaflet CDN. Inline styles via `C` only.

---

### [medium] · Data · 2026-05-10 · deferred (fix-later)
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47… — import aborts on the first malformed row.
**File:** `src/DataStudio.jsx` (function `parseCSV` line ~41, and DuckDB `read_csv_auto` call in `parseFile` line ~376)
**Suggestion:** Two-tier fix. (1) For the DuckDB large-CSV path: wrap `read_csv_auto` in a `try/catch`; on failure, retry with `IGNORE_ERRORS=TRUE` to recover clean rows; surface a dismissible warning banner listing the first N skipped rows with their original line numbers and raw text. (2) For the JS small-CSV path (`parseCSV`): wrap per-row parsing in `try/catch`; accumulate skipped rows; surface the same warning UI. Store the skip report in the pipeline audit trail via `auditor.js`. Do not display raw DuckDB error strings to the user — the current "Invalid Input Error" message is cryptic and blocks the entire import for one bad row in a 1 500-row file.
**Invariants:** Pipeline must replay on `rawData` — the recovered array must be a clean JS/Arrow table; skipped rows must not be injected via a mutating step. Full recovered dataset reaches `runner.js`/`duckdbRunner.js`.

---

### [high] · Data · Performance · 2026-05-08 · routed (Fase X4), browser benchmark pending
**Feedback:** Big datasets generate crashes (21 MB) over 900 k rows.
**File:** `src/DataStudio.jsx` (parseFile CSV/TSV branch, line ~387–464), `src/services/data/duckdb.js`
**Suggestion:** The Fase X4 DuckDB routing for `.dta` > 10 MB (fixed 2026-06-10) did not extend to CSV/TSV, which are the primary large-data format. Mirror the `.dta` branch at `DataStudio.jsx:403` for CSV/TSV: if `file.size > 10_000_000`, register the parsed text in DuckDB, return a `_duckdb`-tagged result with a 500-row preview, and persist as Parquet to OPFS. No browser benchmark with a 900 k-row CSV has been confirmed. Also audit `duckdbRunner.js` for high-volume step types that still fall back to JS object materialisation (`add_column`, `normalize_cats`, `group_transform`, `pivot_longer`). Separately, confirm `vercel.json` has `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` — without these, `SharedArrayBuffer` is absent and DuckDB-Wasm runs single-threaded.
**Invariants:** Never truncate `rawData` before passing to `runner.js`/`duckdbRunner.js` — the 500-row display limit is enforced only at the rendering layer (CLAUDE.md DuckDB rule 5). No server-side computation — all processing stays in-browser.

---

## Features

### Model · 2026-06-06 · deferred (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D × (running − c)" regardless of actual column names.
**File:** `src/math/CausalEngine.js` (line 579 — hardcoded `varNames`), `src/components/modeling/resultDisplay.jsx`, `src/components/modeling/ModelComparison.jsx` (line 331 — existing `varLabels` pattern)
**Suggestion:** Two-part fix. (1) In `CausalEngine.js:579`, replace `"D (treatment)"` with the actual `treatCol` argument and substitute `runningVar + " − cutoff"` for the running variable name — these are already function arguments, no new data needed. (2) In `resultDisplay.jsx` (single-model result panel), add an editable label map pre-filled from `result.coefs` keys, rendered as click-to-edit inline inputs (blur-to-save). Mirror the existing `varLabels` state pattern already working in `ModelComparison.jsx:331`. Pass the label map into `buildStargazer` in `ReportingModule.jsx` and the export scripts in `rScript.js`/`pythonScript.js`/`stataScript.js`. Persist per-model label maps in the `model_buffer` IDB store.
**Invariants:** Label edits are presentation metadata only — never mutate `EstimationResult.coefs` keys or any field in `src/math/`. `CausalEngine.js` stays React-free. Persist via `indexedDB.js`, never localStorage.

---

### Model · 2026-05-25 · deferred (fix-later)
**Feedback:** Add an option to estimate without intercept.
**File:** `src/math/LinearEngine.js` (line ~128: `const k = xCols.length + 1`), `src/components/modeling/ModelConfiguration.jsx`, `src/math/PanelEngine.js`, `src/math/NonLinearEngine.js`
**Suggestion:** Add `noIntercept: boolean` to the engine spec. In `LinearEngine.js runOLS`/`runWLS`, gate the prepended ones column on `!spec.noIntercept` and adjust df = n − k (not n − k − 1); switch R² to uncentered formula when no intercept (matching R's `lm(y ~ x - 1)`). Expose as a "Suppress constant (−1)" checkbox in `ModelConfiguration.jsx`, threaded through `ModelingTab.estimate()`. Add a result footnote: "Note: uncentered R² is not comparable to standard R²." Thread the flag through `PanelEngine`, `NonLinearEngine`, and the DuckDB suff-stats dispatch. Also update export scripts: R → `lm(..., data=df)` becomes `lm(y ~ x - 1, data=df)`; Python → `fit_intercept=False`; Stata → `, noconstant`.
**Invariants:** `seType` must continue to be passed explicitly — `noIntercept` is a separate spec field. All `src/math/` files stay React-free.

---

### Model · 2026-05-21 · deferred (fix-later)
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — showing kernel, bandwidth, effective N, LATE, SE, CI.
**File:** `src/math/CausalEngine.js` (lines 577–579), a new `src/components/modeling/results/RDDResults.jsx`
**Suggestion:** Create `RDDResults.jsx` modelled on the existing `FuzzyRDDResults.jsx`. The table should surface: method, polynomial order, kernel, bandwidth h, N left / N right of cutoff, effective N, LATE point estimate, conventional SE, robust SE (if available), t-stat, p-value, and 95% CI. All fields are already returned by `runSharpRDD` — this is purely a display addition. Simultaneously fix `CausalEngine.js:579` to substitute real column names (see the editable-labels feature above) so the table shows meaningful variable labels. Render the table above or below the existing `RDDPlot` in `ModelPlots.jsx`.
**Invariants:** `src/math/CausalEngine.js` must remain React-free — column names are passed as function arguments. All display logic in the new results component. Inline styles via `C` from `src/components/modeling/shared.jsx`.

---

### Clean · 2026-05-19 · deferred (fix-later)
**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option — R-style membership syntax.
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder, line ~807), `src/pipeline/runner.js` (filter step), `src/services/exprEvalService.js`
**Suggestion:** The FilterBuilder already has "in list" / "not in list" operators with a chip UI (`CleanTab.jsx:606`), but free-text `%in% c(...)` is not parsed. Two options: (a) add a pre-processor in `exprEvalService.js` that rewrites `col %in% c("a", "b")` to the existing `{col, op:"in", values:[...]}` step schema using a regex (`/(\w+)\s+%in%\s+c\(([^)]+)\)/`); or (b) add a "Paste R expression" input above the chip list in FilterBuilder that auto-populates the chip list from the parsed values. Option (a) is lower surface-area. Runner.js filter branch already needs an `"in"` operator case if not present: `case "in": return values.includes(String(row[col]))`.
**Invariants:** Pipeline steps must replay on `rawData` — never mutate in place. Registry must stay in sync with runner.js.

---

### Spatial · 2026-05-30 · deferred (fix-later, V4)
**Feedback:** Allow spatial filters for plots.
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`
**Suggestion:** Add a collapsible "Spatial filter" section to `GeoLayerConfig.jsx` per layer: (a) bounding-box min/max lat/lon inputs; (b) optional WKT polygon field. In `GeoPlotCanvas.jsx`, filter layer rows to those whose coordinates fall inside the bbox/polygon before passing to `mkGeoLayer`, using `SpatialEngine.js`'s existing point-in-polygon utilities. This is a display-time filter only — it does not emit a pipeline step and does not modify `rawData`. Filter state lives in the layer config object within `SpatialGeoPlot` local state. Deferred to V4 post-launch.
**Invariants:** Must not call `applyStep` on `rawData` — the non-destructive pipeline invariant. `SpatialEngine.js` stays React-free. Inline styles via `C`.

---

## UX / Design

No genuinely open UX/Design items. All prior UX reports are resolved, subsumed into the feature items above, or marked "verified fixed" in the X5 reconciliation table.

---

## Other

### Security · 2026-06-13 / 2026-06-14 · hardening follow-up
**Feedback:** XSS injection attempts submitted via feedback form (two separate payloads). Client-side `sanitizeFeedback` fix landed 2026-06-14 — `&<>"` escaped, 5 000-char cap. BugTriage notes: "Hard server-side boundary (Supabase trigger/validation) recommended as follow-up since client sanitization is bypassable by direct POST."
**File:** `supabase/functions/` (new or existing feedback ingestion Edge Function), client feedback submit path (likely `src/services/` — file not located; manual investigation needed to confirm exact path)
**Suggestion:** Add a Supabase Edge Function (or Postgres `BEFORE INSERT` trigger on the `feedback` table) that re-sanitizes `description` on write: strip `<script>`, `<img onerror>`, and `javascript:` patterns; truncate to 5 000 chars; reject payloads with unescaped `<` or `>` with a `400` response. A Postgres `CHECK` constraint (`length(description) <= 5000`) provides an additional durable backstop. This makes the sanitization invariant regardless of the submission client. Note: the app itself has no live render path for feedback descriptions (FeedbackModal is write-only per BugTriage analysis), so the immediate injection risk is low — but a direct `POST` to the Supabase endpoint bypasses the client JS entirely.
**Invariants:** Supabase egress must go through the established sync client path. Do not introduce a second Anthropic API egress point in the Edge Function.

---

## Suggested action order

1. **Browser-validate 4 items from commit `35f4b5a6` in one session** — OLS residual/Q-Q plots, diagnostics freeze on variable toggle, Plot Builder resize, and Explore graph fit. Mark each item ✓ or file a targeted regression report.
2. **Mark IV-Poisson fix ✓ in ClaudeFB.md (`bc2d7b9d`)** — zero-code housekeeping; prevents duplicate investigation. Then browser-validate by running an IV-Poisson model.
3. **Browser-validate spatial heatmap (`5e105529`)** — load a point dataset, add Heatmap layer, confirm KDE + palette + HTML export; test the 5 k-point cap.
4. **Extend DuckDB CSV/TSV routing (`DataStudio.jsx:403`)** — mirror the `.dta` > 10 MB branch for CSV/TSV; run a 900 k-row benchmark; check `vercel.json` COOP/COEP headers.
5. **Resilient CSV import** (`DataStudio.jsx:41`) — `IGNORE_ERRORS=TRUE` retry + skip-row warning UI; post-launch priority but blocks large real-world datasets.
6. **Server-side feedback sanitization** (Supabase Edge Function / Postgres trigger) — low urgency given write-only render path, but closes the direct-POST bypass.
7. **RDD hardcoded variable names** (`CausalEngine.js:579`) — low effort: substitute `treatCol` / `runningVar` from function arguments; unblocks both the editable-labels feature and the RDD summary table.
8. **R `%in% c(...)` filter** (`exprEvalService.js` + `CleanTab.jsx`) — low complexity; high UX value for panel-data filtering workflows.
9. **No-intercept estimation toggle** (`LinearEngine.js:128`, `ModelConfiguration.jsx`) — requires engine-wide df/R² convention changes and export script updates; scope as a focused sprint.
10. **RDD summary table** (new `RDDResults.jsx`) — pure display, all data already in `EstimationResult`; straightforward after #7.
11. **Spatial plot filters** (`GeoLayerConfig.jsx`) — deferred to V4; no pre-launch action.
