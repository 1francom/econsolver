# Feedback Analysis Report — 2026-05-30

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 10 open bugs · 8 open features · 0 open UX · 1 open performance · 1 open other

---

## Bugs

### [high] · Data · 2026-05-13
**Feedback:** `.dta files are wrong loaded and readed`
**File:** `src/services/data/parsers/stata.js`
**Suggestion:** The parser covers formats 117–119 (Stata 13–15) with a legacy shim for 113–116 (Stata 8–12). Re-check the `parseStataOld` branch (line 77): value-label decoding, date-format mapping (`%td`/`%tm`), and strLS blob handling for format 117 may produce garbled strings or missing rows for certain `.dta` files exported with non-ASCII labels. Add a round-trip test fixture by exporting a known dataset from Stata 14 and diffing the parsed output row-by-row. Pay close attention to `strLS` (long strings) segment parsing introduced in format 117, which may not be reached in the legacy path.
**Invariants:** Parser must live in `src/services/data/parsers/` with no React imports.

---

### [high] · Spatial · 2026-05-20
**Feedback:** `Grids do not work`
**File:** `src/components/tabs/SpatialTab.jsx` (GridSection, lines 554–612), `src/math/SpatialEngine.js` (`assignRectGrid` line 253, `assignH3Grid` line 274)
**Suggestion:** `GridSection` calls `assignRectGrid` / `assignH3Grid` and passes the result directly to `onResult`. Verify that `assignRectGrid` returns a valid array — it depends on numeric lat/lon values that may be `null` after upstream pipeline transforms. Add a guard: check `rows.some(r => r[latCol] == null || r[lonCol] == null)` before calling and surface a user-facing error rather than silently producing a broken result. Also verify the H3 resolution input is cast to `Number` — it currently is on line 572, but the select element may still pass a string in edge cases.
**Invariants:** `SpatialEngine.js` is in `src/math/` — must have zero React imports. Engine changes must not break the `assignH3Grid` contract used in `runner.js`.

---

### [medium] · General · 2026-05-20
**Feedback:** `When a dataset is deleted, all variables should disappear from all modules without having to select another dataset`
**File:** `src/components/workspace/DatasetManager.jsx` (onDeleteAll handler, line 407), `src/services/session/sessionState.jsx` (removeDataset, line 113), `src/components/ModelingTab.jsx`
**Suggestion:** `removeDataset` removes the dataset entry from session state, but `ModelingTab`, `WranglingModule`, and `ExplorerModule` hold their own local state copies of `yVar`, `xVars`, and `headers`. When the active dataset is deleted, none of these are reset. Fix: add a `useEffect` in each module watching the session datasets list — if the currently active dataset id is no longer present, reset all local variable selections to empty arrays/strings.
**Invariants:** Persistence uses `indexedDB.js`, not localStorage. No direct localStorage access.

---

### [critical → needs-repro] · Model · 2026-05-21
**Feedback:** `URGENT There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible`
**File:** `src/math/LinearEngine.js` (`pValue` function line 80; `tStats`/`pVals` lines 141–142), `src/math/EstimationResult.js` (result normalisation line 251)
**Suggestion:** BugTriage notes this is most likely a column-misread in the result table — the layout is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig` and the "0.49" may have been the β̂ column, not the t-column. Code audit of 11 engines found no t/p desync. However, if a DuckDB-path engine (TWFE, FE, WLS, 2SLS) returns `testStats` from a pre-sandwich SE and then `se` is overwritten by the robust SE, a mismatch is possible. Verify that every DuckDB suff-stats path derives `testStats` from the final post-sandwich `se`. Request a screenshot + estimator name from Franco before spending more time.
**Invariants:** `pValue()` must use a two-tailed t-distribution. `testStats` must always derive from final `se`. SE type must be passed explicitly — never hardcoded.

---

### [medium] · Model · 2026-05-21
**Feedback:** `When selecting and eliminating variables, the model reestimates automatically — fix that`
**File:** `src/components/ModelingTab.jsx` (lines 2963–2967 — B2 block fires `estimate()` after `xVars` state update)
**Suggestion:** The B2 `useEffect` fires `estimate()` whenever `xVars` changes. Remove this auto-estimate side-effect and require the user to click "Estimate" explicitly. This is consistent with a research tool workflow where changing the regressor list is exploratory and must not silently overwrite a valid result.
**Invariants:** None specific — UI-only change.

---

### [medium] · Calculate · 2026-05-22
**Feedback:** `Fix the function graph from 0`
**File:** `src/components/tabs/CalculateTab.jsx` (`FunctionGrapher`, line 511 — `useState([-8, 8])`)
**Suggestion:** The default x-range `[-8, 8]` starts in negative territory. For economic functions defined only on x ≥ 0, add a "Start from 0" checkbox that clamps `xMin` to `Math.max(0, xMin)`. The `positiveQuad` flag already exists at line 552 but only affects the render, not the range controls. Alternatively change the default to `[0, 10]`, which is safer for the typical economics use case.
**Invariants:** None.

---

### [high] · Report · 2026-05-25
**Feedback:** `AI does not recognize the pipeline steps in the Script generation`
**File:** `src/ReportingModule.jsx` (AIUnifiedScript, line 748), `src/services/AI/sessionSnapshot.js` (`buildSessionSnapshot`), `src/services/AI/AIService.js` (`generateUnifiedScript`)
**Suggestion:** This matches CLAUDE.md pending item 5b (marked "VALIDATION PENDING 2026-05-30"). The snapshot integration is implemented — `buildSessionSnapshot` is called at line 961 of `ReportingModule` and threaded through `AIUnifiedScript`. The most likely failure is that `cleanedData.pipeline` is empty when the Report tab is opened without a full wrangling session. Debug by logging `serializeSnapshot(snapshot)` before the AI call. If `pipeline` arrives as `[]`, trace back through `DataStudio → WranglingModule.onComplete` to confirm the pipeline payload is propagated to `ReportingModule` via props. Also verify `UNIFIED_SCRIPT_PROMPT` rule 8 exists in `src/services/AI/Prompts/index.js`.
**Invariants:** All Anthropic API calls must go through `AIService.js`. No raw `fetch` to the Anthropic API anywhere else.

---

### [medium] · Explore · 2026-05-19
**Feedback:** `Color references in the plot builder for "color = X"`
**File:** `src/components/PlotBuilder.jsx` (mark builder, line 182 — `const colorVal = aes.color || fill`)
**Suggestion:** When `aes.color` is set to a categorical column, Observable Plot should auto-create an ordinal scale. However, the `Plot.plot()` call (around line 526) passes only `color: { scheme }` — it does not set `color: { legend: true }` or an explicit `domain`. For categorical columns, add `color: { ...colorOpts, legend: true }` to the plot options when `aes.color` is a non-empty column name and the column is categorical (detectable via existing `colInfo.isCat`). Also ensure `scheme` defaults to `"tableau10"` for categorical variables so color assignments are stable.
**Invariants:** Inline styles only via `C` object. Observable Plot 0.6 CDN.

---

### [medium] · Simulate · 2026-05-19
**Feedback:** `Montecarlo for single variables`
**File:** `src/components/tabs/SimulateTab.jsx` (runMonteCarlo guard, line 550)
**Suggestion:** The MC simulation requires both `mcY` and `mcX` (regression β̂ sampling). A single-variable mode would show the sampling distribution of the mean (or any scalar statistic) for one DGP variable. Add a second MC mode — "Single variable" — that bootstrap-resamples one DGP variable and plots the sampling distribution of its mean. The DGP scope is already built; the sampling loop is a few lines. Expose as a radio toggle: "OLS β̂" vs "Variable mean".
**Invariants:** None specific.

---

### [high] · Model · 2026-05-28
**Feedback:** `Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators (currently only generic TWFE event study, which is biased under staggered treatment)`
**File:** `src/math/PanelEngine.js` (`runEventStudy` line 236), `src/components/ModelingTab.jsx`, `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** The current `runEventStudy` is a generic TWFE OLS horizon regression — biased under staggered adoption with heterogeneous effects (Callaway & Sant'Anna 2021, Sun & Abraham 2021). Add `runSunAbraham` (cohort × event-time interaction OLS with aggregation weights) and `runCallawaySantanna` (nonparametric group-time ATT estimation with never/not-yet-treated comparison group) to `PanelEngine.js`. Expose them as separate estimator entries in `EstimatorSidebar.jsx` and wire through `ModelingTab`. Both are pure JS — no React — and must accept `seType` explicitly. Plan 2–3 days of math work with R validation against `sunab` and `did` packages.
**Invariants:** `src/math/PanelEngine.js` must have zero React imports. `seType` passed explicitly to both engines. Results must conform to the `EstimationResult` normalised shape.

---

## Features

### Model · 2026-05-11
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js` (andersonRubinCI line 28, called at line 204), `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Anderson-Rubin CIs are **already implemented** in `CausalEngine.js` (function `andersonRubinCI`, inverted via grid search) and attached to 2SLS results. The gap is UI: the `arCI` field is never rendered. Add a collapsible "Weak-instrument robust CI (Anderson-Rubin)" row below the main coefficient table for 2SLS results. Under 1 hour of work — the math already exists.
**Invariants:** UI-only addition. No changes to `src/math/`.

---

### Model · 2026-05-11
**Feedback:** `Add multiple statistics tests`
**File:** `src/components/modeling/DiagnosticsPanel.jsx`, `src/core/diagnostics/` (heteroskedasticity.js, autocorrelation.js, normality.js, multicollinearity.js)
**Suggestion:** Existing diagnostics cover BP, White, DW, BG, JB, Shapiro-Wilk, VIF. Likely requested additions: Hausman endogeneity test (for IV), Ramsey RESET (functional form), Wooldridge serial-correlation test for panels. Add these as new files in `src/core/diagnostics/` (zero React) and surface them conditionally in `DiagnosticsPanel.jsx` — RESET for OLS/WLS only, Hausman when IV available, Wooldridge for FE/FD only.
**Invariants:** New diagnostic modules in `src/core/diagnostics/` must have zero React imports.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/SpatialTab.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** `makeGrid` in `SpatialEngine.js` (line 430) already generates rectangular grid-cell WKT polygons. Add a "Show grid overlay" toggle in the SpatialTab plot panel that calls `makeGrid` with the current boundary + user-supplied cell size and injects the resulting polygon rows as a geometry layer in `PlotBuilder`. No new math needed — primarily a UI wiring task.
**Invariants:** None specific.

---

### Simulate · 2026-05-18
**Feedback:** `Add resampling, bootstrapping, sampling to the mean`
**File:** `src/components/tabs/SimulateTab.jsx`
**Suggestion:** Add a "Resampling" panel below the DGP Builder. Accept a DGP variable (or a column from the active session dataset) and offer bootstrap resampling of the mean with configurable n and reps, plus percentile CI bounds. Output: histogram of bootstrap statistic with CI overlay. The core logic is a `for` loop over draws with replacement — no new engine file needed.
**Invariants:** No math logic in `src/math/`. Keep calculation as a local pure helper inside the component file.

---

### Explore · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder chip UI, line 683), `src/ExplorerModule.jsx`
**Suggestion:** The `in` operator is already implemented in `runner.js` and the FilterBuilder has a chip-select UI for categorical columns. The user may be requesting in-list filtering in the **Explorer** tab's data view, where no filter builder currently exists. Add a lightweight column filter bar to `ExplorerModule.jsx` that uses the existing `op === "in"` pipeline step. Also add a tooltip or code hint in the Clean tab FilterBuilder showing the equivalent R syntax (`filter(col %in% c(...))`) to improve discoverability.
**Invariants:** Any new filter step must go through `runner.js` and replay on `rawData` — never mutate in place.

---

### Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/modeling/ModelPlots.jsx` (RDD section), `src/components/ModelingTab.jsx`
**Suggestion:** `runSharpRDD` already returns bandwidth (`h`), kernel type, polynomial order, n, LATE, SE, CI, and t/p stats. Add an `RDDSummaryTable` component in `ModelPlots.jsx` that formats these in the style of `rdrobust`'s `summary()` output: cutoff, n left/right, effective bandwidth, kernel, polynomial order, and a three-row table for Conventional / Bias-corrected / Robust estimates. Data is all available — purely a display addition.
**Invariants:** None.

---

### Model · 2026-05-21
**Feedback:** `Add an option to clean the current model`
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** Add a "Clear model" button (or × icon) in the model header that resets `yVar`, `xVars`, `zVars`, `result`, and all estimator-specific config state to initial values. Show a confirmation prompt if a result is currently displayed to prevent accidental loss.
**Invariants:** None.

---

### Model · 2026-05-25
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/math/LinearEngine.js` (`runOLS` line 128 — `const k = xCols.length + 1`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** `runOLS` always prepends a ones column (implicit intercept). Add a `suppressIntercept` boolean option: when `true`, skip the ones column and use `k = xCols.length`. Propagate the flag through `runWLS`, the DuckDB suff-stats OLS path (`buildOLSSuffStats` — check the intercept column injection), and the result export helpers. Add a checkbox in `ModelConfiguration.jsx` under "Advanced options" with a warning that R² is not conventionally interpretable without an intercept.
**Invariants:** `seType` must still be passed explicitly. DuckDB SQL path must also respect the flag.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (CSV loader lines 289–302), `src/services/data/duckdb.js`
**Suggestion:** DuckDB-WASM routing already exists for CSV/TSV >10MB. The remaining crash vector is that `detectDelimiter` + `parseCSV` still runs in JS for files that narrowly miss the DuckDB threshold. Consider lowering the JS-path size ceiling to 5MB. Additionally, per CLAUDE.md performance rules: (1) add OPFS persistence to cache the DuckDB Parquet file after first import, avoiding re-parse on project re-open; (2) add `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` to `vercel.json` to unlock `SharedArrayBuffer` and full parallel DuckDB execution.
**Invariants:** Dataset must never leave the browser. DuckDB stays client-side. Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`.

---

## Other

### Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (`parseCSV` line 37)
**Suggestion:** Line 1583 contains a value causing a quoting or field-count error — likely an embedded comma or unescaped quote in an unquoted string field. The hand-rolled `parseCSV` (line 37) does not handle all RFC 4180 edge cases. Replace or supplement it with a robust quoted-field parser that handles embedded commas and escaped double-quotes. For DuckDB-routed files this is handled natively. Also improve the error message to show the expected vs. actual field count per row rather than the raw DuckDB error string.
**Invariants:** None. Parser lives in `src/DataStudio.jsx`.

---

## Suggested action order

1. **Verify or close p-value bug** — request screenshot + estimator name from Franco; if real, audit DuckDB SE paths for stale `testStats` (critical, low effort to confirm).
2. **Remove auto-reestimate on variable toggle** — delete or gate the `useEffect` B2 block at `ModelingTab.jsx` lines 2963–2967 (high impact, ~30 min).
3. **Expose Anderson-Rubin CIs in the 2SLS results UI** — math already exists in `CausalEngine.js`; add a collapsible AR CI display row in `ModelPlots.jsx` (<1 hour).
4. **Validate Report AI pipeline recognition (CLAUDE.md 5b)** — load a `;`-delimited CSV and check the unified script emits the correct load call; trace `snapshot.pipeline` if empty.
5. **Fix Spatial grid null-guard** — add lat/lon null-check in `GridSection` before calling `assignRectGrid`/`assignH3Grid`.
6. **Fix dataset-deletion variable bleed** — `useEffect` in `ModelingTab`, `WranglingModule`, `ExplorerModule` to clear variable state when active dataset is removed.
7. **Plan Sun-Abraham / Callaway-Sant'Anna event study** — schedule as a dedicated math track (2–3 days); methodology correctness issue for staggered DiD users.
8. **Fix CSV quoting parse error** — improve `parseCSV` RFC 4180 handling for embedded commas and unescaped quotes.
- 11 open bugs · 7 open features · 0 open UX · 1 open performance · 1 open other

---

## Bugs

### [critical / needs-repro] · Model · 2026-05-21
**Feedback:** URGENT There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible
**File:** `src/math/EstimationResult.js` · `src/components/ModelingTab.jsx` (CoeffTable display, line 865 area)
**Suggestion:** Investigation (BugTriage 2026-05-26) found no desync in 11 engines: `pValue(0.49, 100) = 0.625` matches R exactly. The CoeffTable column order is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig`; the reported "0.49" is almost certainly the β̂ column, not the t column. Add a column header tooltip or a fixed-width label like **"t-stat"** to reduce column confusion. Ask Franco for a screenshot + estimator name before marking closed.
**Invariants:** none

---

### [high] · Model · 2026-05-28
**Feedback:** Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators
**File:** `src/math/PanelEngine.js` (`runEventStudy`, line 236) · `src/components/ModelingTab.jsx` · `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** The current `runEventStudy` is a vanilla TWFE dynamic regression, which is biased under staggered/heterogeneous treatment timing (Callaway & Sant'Anna 2021; Sun & Abraham 2021). Add two new estimators: (1) **Sun-Abraham** — interaction-weighted (IW) estimator, decomposes TWFE into cohort-specific ATTs weighted by relative cohort size; implementable in pure JS with a group × relative-time dummy interaction matrix. (2) **Callaway-Sant'Anna** — doubly-robust DiD using outcome regression or IPW within each (cohort, calendar-time) cell, aggregated to ATT. Both require a `cohort` column (first treatment period). Add them as distinct entries in `EstimatorSidebar.jsx` under a new "Staggered DiD" group. These are new math engines — keep zero React imports in `src/math/`.
**Invariants:** `src/math/` must have zero React imports. `seType` must be passed explicitly to new engines.

---

### [high] · Data · 2026-05-13
**Feedback:** .dta files are wrong loaded and readed
**File:** `src/services/data/parsers/stata.js` (main parser line 221, old-format parser line 61)
**Suggestion:** The parser supports formats 117–119 (Stata 13–15) and formats 113–116 (Stata 8–12) via the old-format path. Likely causes: (a) Stata 16+ files use format 119 with `uint32` K — check the `format > 119` guard at line 237 (currently throws for 119+, but format 119 IS handled); (b) malformed value-label blocks in format 118/119 can crash the tag-walking loop — wrap the `strls` and `value_labels` sections in try/catch and surface readable errors; (c) encoding issues with non-Latin variable labels — normalize to UTF-8 before assigning. Add a `try/catch` around the entire parser body that throws `"Stata format N could not be parsed: <detail>"` with the section name so users can report a clearer error.
**Invariants:** none (`src/services/data/` is pure JS)

---

### [high] · Spatial · 2026-05-20
**Feedback:** Grids do not work
**File:** `src/components/tabs/SpatialTab.jsx` (`GridSection` component, line 555; `generatedGrid` useMemo, line 1942) · `src/math/SpatialEngine.js` (`makeGrid`, line 430)
**Suggestion:** Two separate grid paths exist: (a) `GridSection` in the Spatial analytics pipeline (point-to-cell assignment via `assignRectGrid`/`assignH3Grid`) and (b) the map layer grid via `makeGrid` + `generatedGrid`. The "latlon" mode of `generatedGrid` builds a bounding-box WKT from point extent — if `gl.latCol`/`gl.lonCol` are empty string (default), `parseFloat("")` is `NaN`, so `lats` is empty and the memo returns `null`, silently producing no cells. Add a validation guard: if `latCol`/`lonCol` are empty, show a UI error "Select latitude and longitude columns to generate a grid." Also check that `gl.mode` is correctly initialized — the default `ly.mode` value for newly added grid layers may be missing or stale.
**Invariants:** none

---

### [high] · Report · 2026-05-25
**Feedback:** AI does not recognize the pipeline steps in the Script generation
**File:** `src/ReportingModule.jsx` (snapshot build, line 961; `AIUnifiedScript`, line 748) · `src/services/AI/sessionSnapshot.js` (`buildSessionSnapshot`, line 59) · `src/services/AI/Prompts/index.js` (`UNIFIED_SCRIPT_PROMPT`)
**Suggestion:** Per CLAUDE.md pending item 5b, `buildSessionSnapshot` and `serializeSnapshot` are implemented but **not validated end-to-end**. The most likely failure: `buildSessionSnapshot` receives `cleanedData` from `ReportingModule` but `cleanedData.pipeline` may be `undefined` if `WranglingModule` hasn't fired `onComplete` with the pipeline payload yet (race condition on tab switch). Add a fallback: if `snapshot.pipeline` is empty, log a console warning and surface "No pipeline steps detected — run a wrangling step first" in the AI output panel. Also verify that `serializeSnapshot` actually injects the pipeline block: look for the `PIPELINE (N steps, …)` section in the user message sent to Claude (`UNIFIED_SCRIPT_PROMPT` rule 8 instructs Claude to walk the pipeline, but only if the serialized block is present).
**Invariants:** All Anthropic API calls must go through `AIService.js`; never add raw `fetch` to the Anthropic API elsewhere.

---

### [medium] · Model · 2026-05-21
**Feedback:** When selecting and eliminating variables, the model reestimates automatically — fix that
**File:** `src/components/ModelingTab.jsx` (no auto-estimate `useEffect` found in the dep scan — the issue is more likely in `VariableSelector.jsx` or the `estimate()` callback trigger)
**Suggestion:** Review whether `VariableSelector.jsx` calls an `onChange` prop that directly invokes `estimate()`. If so, decouple variable selection from estimation: variable changes should only update local state (`xVars`, `yVar`), and estimation should only fire when the user clicks the "Estimate" button. An "auto-estimate" toggle could be offered in `InferenceOptions.jsx` as an opt-in for power users who want live updates. Ensure no `useEffect` watching `[xVars, yVar, model]` calls `estimate()` — the current dep scan shows none, but the trigger may come from a child component callback.
**Invariants:** none

---

### [medium] · General · 2026-05-20
**Feedback:** When a dataset is deleted, all variables should disappear from all modules without having to select another dataset
**File:** `src/services/session/sessionState.jsx` (`removeDataset`, line 113) · `src/components/workspace/DatasetManager.jsx` · `src/components/ModelingTab.jsx`
**Suggestion:** `removeDataset(dispatch, id)` fires `REMOVE_DATASET` to the session reducer, but `ModelingTab`, `ExplorerModule`, and `WranglingModule` each hold local state (`yVar`, `xVars`, active dataset id) that is not reset in response. Add a `useEffect` in each tab that watches `sessionState.datasets`: when the currently active dataset id no longer exists in the registry, reset all variable selectors to empty. Alternatively, dispatch a `DATASET_DELETED` event that session consumers subscribe to via a shared effect.
**Invariants:** Use IndexedDB (`indexedDB.js`) for persistence — no localStorage; never mutate `rawData` in place.

---

### [medium] · Explore · 2026-05-19
**Feedback:** Color references in the plot builder for "color = X"
**File:** `src/components/PlotBuilder.jsx` (color channel, lines 182–414; color legend, line ~670)
**Suggestion:** When `aes.color` is set to a column name, `colorVal` is passed directly as the Observable Plot `fill`/`stroke` value. Observable Plot treats a string column name as a channel and auto-generates a color legend when the channel is declared via the `Plot.plot({ color: { legend: true } })` option. Currently no `color` option is passed to `Plot.plot(...)`, so no legend renders. Add `color: { legend: true, scheme: "tableau10" }` (or the existing palette) to the `Plot.plot()` options object when `aes.color` is a non-empty string. For the non-map path, also render a `<div>` swatch list below the chart mirroring the map's `buildColorScale` legend.
**Invariants:** Inline styles via the `C` object from `shared.jsx` only — no CSS files.

---

### [medium] · Simulate · 2026-05-19
**Feedback:** Montecarlo for single variables
**File:** `src/components/tabs/SimulateTab.jsx` (`runMonteCarlo`, line 549)
**Suggestion:** `runMonteCarlo` currently requires both `mcY` and `mcX` (bivariate OLS). To support single-variable MC (e.g. distribution of a sample mean, power of a t-test, proportion estimation): add a "MC mode" toggle — "Regression (Y ~ X)" vs "Single variable (Y only)". In single-variable mode, remove the `mcX` selector, and instead compute descriptive statistics per simulation draw: mean, sd, median, and optionally a one-sample t-statistic. This requires only adding a conditional branch in `runMonteCarlo` — no new math engine needed.
**Invariants:** `src/math/` must have zero React imports; keep new MC logic in the component.

---

### [medium] · Calculate · 2026-05-22
**Feedback:** Fix the function graph from 0
**File:** `src/components/tabs/CalculateTab.jsx` (graph canvas `drawCanvas`, line 541; `xRange` default, line 511)
**Suggestion:** The default `xRange` is `[-8, 8]` and `positiveQuad` defaults to `false`. The "Q⁺" button toggles `positiveQuad`, which clamps `xMin = Math.max(0, xMin)` and `yMin = 0`. This mechanism works correctly, but the button label "Q⁺" is cryptic — many users won't find it. Rename it to "Start from 0" or "x ≥ 0" and add a short label. Additionally, when a new function is first entered, auto-detect if all parameter defaults would produce a non-negative domain (e.g. `sqrt(x)`, `ln(x)`) and offer to enable Q⁺ automatically.
**Invariants:** Inline styles via `C` only.

---

### [low] · Model · 2026-05-21
**Feedback:** Add an option to clean the current model
**File:** `src/components/ModelingTab.jsx` · `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** Add a "Clear model" button (trash icon, low-emphasis style) in the model spec header row. On click, reset `yVar`, `xVars`, `zVars`, `wVars`, `result`, and all estimator-specific state to their initial values. The button should sit next to the estimator dropdown in `EstimatorSidebar.jsx` or as a secondary action in the `ModelingTab` header row. Keep the estimator selection intact — only clear the variable assignment and output.
**Invariants:** Inline styles via `C` only.

---

## Features

### Model · 2026-05-11
**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength
**File:** `src/math/CausalEngine.js` (`andersonRubinCI`, line 28) · `src/components/ModelingTab.jsx` (display, line 865)
**Suggestion:** Anderson-Rubin CI is **already fully implemented** in the math engine and displayed in ModelingTab (lines 865–881) for 2SLS with a single endogenous regressor. This feature request may be stale. Verify the AR CI panel renders correctly in the UI with a weak-instrument dataset. If the user wants AR CI extended to the multi-endogenous case or to Fuzzy RDD, that is a new task. No code change needed unless the display is broken.
**Invariants:** none

---

### Model · 2026-05-11
**Feedback:** Add multiple statistics tests
**File:** `src/core/diagnostics/` (`heteroskedasticity.js`, `autocorrelation.js`, `normality.js`, `multicollinearity.js`) · `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** The request is under-specified. Likely candidates based on context: Hausman test for FE vs RE, Sargan/Hansen overidentification test (already in GMMEngine), Wu-Hausman endogeneity test for 2SLS, and Ramsey RESET test for functional form. The `DiagnosticsPanel.jsx` already shows BP, DW, JB, and VIF. Add a "Specification tests" section: (1) Ramsey RESET — regress residuals on `Ŷ²` and `Ŷ³`; (2) Wu-Hausman — compare OLS vs 2SLS coefficients via Hausman statistic. Both can be pure-JS additions to `src/core/diagnostics/` without React imports.
**Invariants:** `src/core/` must have zero React imports.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/PlotBuilder.jsx` · `src/ExplorerModule.jsx`
**Suggestion:** The request is for reference grid lines (graticule) on non-map plots in PlotBuilder, not spatial grid cells. Observable Plot supports grid lines via `Plot.plot({ grid: true })` or via explicit `Plot.gridX()` / `Plot.gridY()` marks. Add a "Show grid" checkbox in the PlotBuilder labels/style panel that toggles `grid: true` in the `Plot.plot()` options object. This is a one-line change to the plot options builder.
**Invariants:** Inline styles via `C` only.

---

### Simulate · 2026-05-18
**Feedback:** Add resampling, bootstrapping, sampling to the mean
**File:** `src/components/tabs/SimulateTab.jsx`
**Suggestion:** Add a second tab or collapsible section "Resampling" in `SimulateTab.jsx` with three modes: (1) **Bootstrap** — draw B samples with replacement from a loaded dataset column, compute statistic (mean/median/OLS slope), show empirical CI; (2) **Permutation test** — shuffle treatment/outcome, recompute test statistic B times, show null distribution; (3) **Sampling distribution of mean** — draw samples of size n from an uploaded or simulated column, plot distribution of sample means. All three only need the dataset rows already available through session state — no new data source needed. The bootstrap and permutation loops can use the existing `evalScopeInWorker` pattern or operate directly on row arrays.
**Invariants:** none

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder) · `src/pipeline/runner.js` (`filter` step type)
**Suggestion:** The current FilterBuilder likely supports single `==` comparisons. Add a new operator `%in%` (multi-value membership) to the operator dropdown in `FilterBuilder`. In the UI, show a tag-input for multiple values when this operator is selected. In `runner.js`, handle the `filter` step when `op === "in"` by checking `allowedValues.includes(row[col])`. The pipeline step schema would be `{ type: "filter", col, op: "in", values: ["Spain (Espana)", "Basque Country (Pais Vasco)"] }`. This is self-contained in `CleanTab.jsx` + `runner.js`.
**Invariants:** Pipeline steps must replay on `rawData` — never mutate in place.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/ModelingTab.jsx` (RDD result display, around line 865) · `src/components/modeling/ModelPlots.jsx` (`RDDPlot`)
**Suggestion:** After `runSharpRDD` returns, render a collapsible "RDD Summary" panel styled after `rdrobust`'s R output: show bandwidth (chosen vs user), kernel, polynomial order, N (left/right of cutoff), conventional + bias-corrected + robust estimates with SE/CI, and the effective sample size at the boundary. The result object already contains `bandwidth`, `kernel`, `polyOrder`, `late`, `se`, `ci95`. Add `nLeft`/`nRight` (count of obs on each side of cutoff) to `runSharpRDD`'s return value and display them in this panel. No engine changes required beyond adding the two row counts.
**Invariants:** `src/math/` must have zero React imports.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept
**File:** `src/math/LinearEngine.js` (`runOLS`, line 128) · `src/components/ModelingTab.jsx` · `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** `runOLS` hard-codes `k = xCols.length + 1` (line 128) and prepends a `1` intercept column to X. Add an optional `noIntercept: boolean` parameter to `runOLS`, `runWLS`, and the panel engines. When `noIntercept = true`, skip prepending the ones column and set `k = xCols.length`. F-stat changes to test all coefficients simultaneously (not against intercept-only null). In the UI, add a small "No intercept" checkbox in `ModelConfiguration.jsx` or `InferenceOptions.jsx`. Must be threaded through `ModelingTab.estimate()` → engine call and through the DuckDB suff-stats path (which builds the X matrix in SQL).
**Invariants:** `seType` must still be passed explicitly. The DuckDB suff-stats SQL path adds a `1 AS _cons` column by default — this column must be omitted when `noIntercept` is true.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (`parseFile`, line 289) · `src/services/data/duckdb.js` · `src/pipeline/duckdbRunner.js`
**Suggestion:** DuckDB-WASM is implemented and large CSV (>10MB) routes to `loadLargeCSV` via DuckDB. Per CLAUDE.md performance rules, two items are still pending: (1) **OPFS persistence** — cache the DuckDB Parquet file in OPFS so re-opening the same project skips re-import; (2) **Auto-convert CSV to Parquet** — after first load into DuckDB, run `COPY table TO 'data.parquet' (FORMAT PARQUET)` and query Parquet on subsequent loads. The crash at 900k rows is likely happening because the non-DuckDB JS path still allocates full arrays on tab switch (Explorer re-renders all rows). Enforce the 200–500 row display limit in Explorer and ensure `rawData` is never pulled fully into JS objects from the DuckDB table (`SELECT *` on a 900k-row Arrow result is the most likely crash vector).
**Invariants:** Dataset must never leave the browser (no server-side computation). Use IndexedDB/OPFS for persistence.

---

## Other

### Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...
**File:** `src/DataStudio.jsx` (`parseCSV`, line 37; `loadLargeCSV` via DuckDB, line 289)
**Suggestion:** This error most likely originates from DuckDB's CSV reader, which is stricter than the custom `parseCSV` JS parser. The World Bank CSV format has a multi-line header with metadata rows before the data (e.g. `"Data Source",...` or blank rows), which can push the actual column count off by the time DuckDB reaches line 1583. Fix: in `loadLargeCSV`, add `SKIP 4` (or detect via `DESCRIBE`) when loading World Bank-format files, or pre-strip metadata rows before passing to DuckDB. A quick heuristic: if the first row doesn't start with a plausible column name pattern, try `SKIP` 1, 2, 4, and use whichever produces the most columns.
**Invariants:** none

---

## Suggested action order

1. **[high · blocker] Report AI script gen** — validate `serializeSnapshot` pipeline injection end-to-end (CLAUDE.md pending 5b); likely a race on `cleanedData.pipeline` being `undefined` at snapshot build time (`src/ReportingModule.jsx:961`, `src/services/AI/sessionSnapshot.js:59`).
2. **[high · methodological] Event study Sun-Abraham / Callaway-Santana** — TWFE event study is known-biased under staggered treatment; add IW and CS estimators to `PanelEngine.js` as distinct entries in `EstimatorSidebar.jsx` before the tool is used for real policy papers.
3. **[high · data] .dta parser robustness** — wrap `stata.js` value-label / strls sections in try/catch with detailed error messages; recurring user complaint across two batches (`src/services/data/parsers/stata.js`).
4. **[medium · UX] Auto-reestimate on variable toggle** — decouple `setXVars` from `estimate()` so model only runs on explicit button click (`src/components/ModelingTab.jsx`).
5. **[medium · data] Spatial grids latlon mode** — guard against empty `latCol`/`lonCol` in `generatedGrid` useMemo and show UI validation error (`src/components/tabs/SpatialTab.jsx:1942`).
