# Feedback Analysis Report — 2026-09-18

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 11 open bugs · 11 open features · 0 open UX · 1 open performance · 1 open other

---

## Bugs

### [high] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` (function `aggregateTimeSeries` ~L182; time series render block ~L1585)
**Suggestion:** `aggregateTimeSeries` groups by `grpCol` and returns a `Map<string, {x, y}[]>` keyed by group value. The SVG line renderer likely fails to iterate the map or renders only the first key. Inspect how the multi-group result is consumed after `useMemo` at L1481 and ensure all group keys produce distinct `<polyline>` elements with a legend entry. A colour-per-group scale already exists in the codebase (`color.js`); wire it to the group key map.
**Invariants:** Inline styles only via `C`; no external UI library; grouping logic is pure JS (no React in math files).

---

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx` (~L519 `generateSimScript`); `src/services/export/` (script generators)
**Suggestion:** `generateSimScript` currently emits only the DGP draw section. Extend it to append the Monte Carlo loop, the chosen test statistic computation, and any estimation (OLS/mean/resampling) the user configured in `StatWorkspace`/`SampleTestPanel`. The state for those sub-tasks already lives in `SimulateTab`'s React state; pass it into `generateSimScript(lang, n, seed, variables, mcConfig, testConfig)` and branch per config. Emit a NOTE when a sub-task has no equivalent in the target language.
**Invariants:** Script generators must stay in `src/services/export/`; no React imports in export helpers; all three languages (R/Python/Stata) must be extended together.

---

### [high] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/components/modeling/CoefficientTestPanel.jsx`; `src/math/ModelHypothesis.js`
**Suggestion:** The panel already runs a single hypothesis per call. Add a `savedTests` array in local state, a "Save test" button that appends `{label, restriction, null_value, stat, p, snippet}`, and a scrollable list of saved tests with copy-all. `generateJointHypothesisScript()` (added 2026-07-30) already produces per-test snippets; wrap them in a loop for the multi-test export. No engine changes needed — the math is already there.
**Invariants:** None additional; state stays in the component (no persistence needed for a session-scoped feature).

---

### [high] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` (time series block ~L1585–1720)
**Suggestion:** The numeric/categorical panels already expose `plotTitle`, `xLab`, `yLab` inputs via `labInput()` (~L981). The time series block at L1585 has none of these — add three `labInput` calls matching the existing pattern and thread them into the `SvgLineChart` render and into the `onPin` payload so pinned exports carry the labels. The `SvgLineChart` component should accept `title`/`xLabel`/`yLabel` props and render them as SVG `<text>` elements above and beside the chart, matching the histogram/bar implementation.
**Invariants:** Inline styles only; no external charting library. SVG text must work in both light and dark themes using `C.text`/`C.textMuted`.

---

### [high] · Model · 2026-07-09
**Feedback:** No direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT); `src/services/AI/AIService.js` (`interpretRegression`)
**Suggestion:** The prompt already has log-log and log-level elasticity rules (~L99, ~L364). What's missing is a **direct** elasticity calculation: for OLS with no log transformation on Y or X, `ε = β · (mean(X) / mean(Y))` evaluated at the sample means. `interpretRegression` receives `result` (which has `meanX`/`meanY` only if surfaced). Extend `EstimationResult` to carry column means (already computed during OLS), and add a rule to INTERPRET_REGRESSION_PROMPT instructing Claude to compute and display the point elasticity at the mean for continuous regressors when the model is level-level. No new AI call — add the means to the existing user-payload JSON.
**Invariants:** All Anthropic calls go through `AIService.js`; math for computing column means must stay in `src/math/LinearEngine.js` or `src/core/`; no React in math files.

---

### [medium] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/calculate/workbench/exportScript.js` (`buildStata` ~L79)
**Suggestion:** `buildStata` outputs numeric Mata code but leaves several gaps: (1) the `mata: … end` block wraps each equation separately with `display` calls inside it — these are valid Mata `display()` statements but `("text", value)` is not valid Mata syntax; it should be `printf("%g\n", value)` or `display "text: " value`; (2) constraints section is intentionally skipped with a NOTE, which is acceptable but should mention `constraint define` as the Stata equivalent; (3) the optimizer uses a simple grid search with no convergence check. Fix the `display` syntax issue first (highest impact — crashes any Mata block) by replacing `("label", val)` with `st_local("_res", strofreal(val)); display "label: " _res`. The derivative and integral blocks have the same issue.
**Invariants:** No React imports in export helpers; Stata output must not claim to support features it doesn't (e.g. symbolic differentiation).

---

### [medium] · General · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`; `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer()` in `latexTable.js` currently emits a fixed `tabular` environment. For wide tables (>5 model columns), switch to `adjustbox{max width=\linewidth}` wrapping the tabular, or emit `\setlength{\tabcolsep}{4pt}` with a `\footnotesize` scope before the table. For very wide tables (>8 columns), consider `\rotatebox{90}{...}` (landscape). Detect column count from the `models` array length and branch accordingly. The user-facing rendering (HTML Stargazer preview in `ReportingModule`) should also respect the same width logic via `overflow-x: auto`.
**Invariants:** `latexTable.js` must remain pure JS; no React imports.

---

### [medium] · General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array ~L11)
**Suggestion:** TOUR_STEPS (9 steps as of the last audit) no longer covers: the Spatial tab, the Simulate tab's Monte Carlo mode, N-way FE in the Model tab, the Callaway-Sant'Anna and Sun-Abraham estimators, the Pipeline collapse toggle, and the new Dataset Manager. Add a step for each missing module/feature, following the existing `{ target, title, content, tab }` shape. Keep prose to 1–2 sentences per step (per CLAUDE.md: "never write a count"). The tour button lives in `WorkspaceBar.jsx`; no wiring change needed, just append to the array.
**Invariants:** No count strings ("9 steps", "14 estimators") — name things, don't count them.

---

### [medium] · General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`; `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` stores a flat ordered list of artifact IDs per project. Extend the schema to `{ order: string[], groups: { id, label, artifactIds }[] }` and add `createGroup/renameGroup/deleteGroup/moveToGroup` helpers. In `ModelBufferBar`, render group headers as collapsible sections; in the plot/map gallery (wherever artifact pins are displayed), add a group picker dropdown. Because the schema change is backward-compatible (old format has no `groups` key → defaults to a single "Default" group), no migration is needed.
**Invariants:** Persist via `indexedDB.js` (the existing `artifactOrder_<pid>` key in the `pipelines` store); never use localStorage.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab` holds `cwCases`, `cwName`, `mutateExpr`, `ifElseCond`, etc. in local `useState`; navigating away unmounts the component and drops all of it. Lift these draft states into `sessionStorage` keyed by `${pid}_${datasetId}_feature_draft`, reading on mount and writing on every change (debounced). Use try/catch around every access (private window / blocked storage). Only the DRAFT state needs persisting — applied steps are already in the pipeline. Clear the draft when the user clicks "Add step →".
**Invariants:** Use `sessionStorage` here (per-session draft, not project-level data); `indexedDB.js` is for pipeline/data storage; never use localStorage.

---

### [low] · Model · 2026-06-14
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/services/export/latexTable.js`; `src/components/modeling/` (result display)
**Suggestion:** Add an optional `labelMap: { [colName]: string }` field to `EstimationResult` (or the `spec`). In `buildStargazer()`, look up each coefficient's display name in `labelMap` before falling back to the raw column name. Expose a simple editable name field in the result panel (pencil icon → inline text input per variable row), writing to a `labelMap` in the model buffer entry. The RDD default labels ("D (treatment)" etc.) become the initial values in `labelMap`, making them editable instead of hardcoded.
**Invariants:** `latexTable.js` must remain pure JS; spec → export requires THREE whitelist edits per CLAUDE.md: `CodeEditor.jsx`, `ReportingModule.jsx`, and every `transpileModel()` call site.

---

## Features

### [high] · Clean · 2026-06-14
**Feedback:** Add filter(regionname %in% c("Spain", "Basque Country")) for filter option
**File:** `src/pipeline/predicate.js` (`OPERATORS`, `evalPredicate`, `predicateToSQL`); `src/components/wrangling/CleanTab.jsx` (FilterBuilder)
**Suggestion:** Add `in` and `nin` (not-in) operators to `OPERATORS` in `predicate.js` with `arity: "list"` (value is a comma-separated string or a JSON array). `evalPredicate` already has partial support (`"in"` is in `OPERATORS` — verify it reaches `evalPredicate`'s switch); `predicateToSQL` needs `col IN (${placeholders})` emission. The FilterBuilder UI needs a "Is one of" / "Is not one of" option with a tag-style multi-value input. Keep `predicate.js` as the single owner of the condition language — do not re-implement in `CleanTab.jsx`.
**Invariants:** `predicate.js` is the canonical single owner of the filter operator vocabulary — per CLAUDE.md "never re-implement a filter operator anywhere else". A permissive SQL default (`TRUE`) is explicitly banned — throw on unknown operators.

---

### [medium] · Model · 2026-06-14
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js` (`runSharpRDD`); `src/components/modeling/` (RDD result display)
**Suggestion:** `runSharpRDD` returns an `EstimationResult` with `bw`, `n_eff`, `se`, `pval`, `ci`. Extend `EstimationResult` (or add an `rddMeta` field) to carry: effective N left/right of cutoff, the IK bandwidth, the kernel name, the polynomial order, and the 95% CI. Render a compact summary panel above the coefficient table, modelled on `rdrobust::summary.rdrobust` output: "Method: RD Sharp, kernel: Triangular, bw: X, Obs (left/right): N₁/N₂, Coef: β, SE: s, z: t, CI: [lo, hi]". No new math needed — all values already exist in the engine's output.
**Invariants:** Pure JS in `src/math/`; no React imports in engine files. The display component lives in `src/components/modeling/`.

---

### [medium] · Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a controlled text input above the variable list, filtering `headers` by substring match (case-insensitive). The filtered list replaces the full chip list while the query is non-empty; the input clears on variable selection or on Escape. Use `useMemo` to derive `filteredHeaders = headers.filter(h => h.toLowerCase().includes(query))`. No external search library needed. The input should have `placeholder="Search variables…"` and a clear × button.
**Invariants:** Inline styles only via `C`; no external UI library.

---

### [medium] · Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators
**File:** `src/components/modeling/ModelPlots.jsx`; `src/math/LinearEngine.js` (or a new `binScatter.js`)
**Suggestion:** Add a "Binned Scatter" collapsible plot section to the model result panel. Controls: number of bins (default 20), residual-vs-fitted or raw Y-vs-X mode, optional overlay of the fitted line, and an option to bin the RDD running variable separately left/right of the cutoff (for RDD models). The binning logic is pure JS: sort by X, cut into N equal-count groups, compute mean(X) and mean(Y) within each bin. Expose the same controls in the replication exporters via `rdplot`-style R code.
**Invariants:** Any new math helper goes in `src/math/` with zero React imports; the UI component goes in `src/components/modeling/`.

---

### [medium] · Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`; `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Add an integer "Max observations" input to `PlotBuilder`'s labels panel (default: unlimited). When set, slice `plotRows` to the first N rows **after** any existing sort, so the user can combine "sort by X → show first N" to get a meaningful subsample. For model result plots in `ModelPlots.jsx`, expose the same slider. Emit a NOTE in replication code (`head(df, N)` in R, `df.head(N)` in Python) so the output is reproducible.
**Invariants:** Slicing is view-only — it must never mutate `rawData` or write to the pipeline; the pipeline always replays on the full dataset.

---

### [medium] · Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/ExplorerModule.jsx` (scatter/bar/histogram with color-by group); `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, the current legend shows one swatch per group with the group value. Add an option to overlay the group label directly on the plot (e.g. as a text mark at the group centroid for scatter, or inside each bar for bar charts). In Observable Plot, use a `Plot.text()` mark with the same color scale. Expose this as a "Show labels on plot" toggle in the labels panel, defaulting to off to avoid clutter.
**Invariants:** Inline styles and Observable Plot only; no external UI library. Color scale must use `CAT_PALETTE` from `src/components/tabs/spatial/shared/color.js` or Observable's `observable10`.

---

### [low] · Spatial · 2026-06-14
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** Add a bbox or attribute filter panel to the geo-plot root. Attribute filters should compile through `predicate.js`'s `predicateToSQL` / `evalPredicate` (the same engine used by the Clean tab) so no new filter logic is needed. Bbox filter can be a simple min/max lat/lon input that pre-filters the rows before they reach the layer builder. The filter is view-only — it does not add a pipeline step.
**Invariants:** Reuse `predicate.js` as the single owner of filter operators. Do not re-implement condition logic in the spatial components.

---

### [low] · Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/calculate/workbench/EquationsPanel.jsx`; `src/components/calculate/workbench/WorkbenchCanvas.jsx`
**Suggestion:** Add a "Add constant" button that appends a `{ type: "constant", value: expr, label }` entry to the session's equation list. The canvas renders it as a horizontal line `y = value` at the computed scalar. In `exportScript.js`, R emits `abline(h = value)`, Python `plt.axhline(value)`, Stata `yline(value)` on the existing twoway graph. This avoids overloading the existing equation type and keeps the canvas renderer simple.
**Invariants:** No React in math files. The workbench's `cas` adapter is already available for evaluating the constant expression symbolically.

---

### [low] · General · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`; `src/ExplorerModule.jsx`
**Suggestion:** True 3-axis plots (x, y, z in 3D) require a 3D rendering library (e.g. Plotly) which is not in the current stack. A pragmatic alternative: a dual-Y-axis plot (left Y axis for one variable, right Y axis for a second, same X). Observable Plot supports this via two `y`-scale layers. Add a "Secondary Y" aesthetic mapping when the geom is `line` or `point`. Emit `sec_axis()` in R / `twinx()` in Python. Document the 3D limitation in the HintBox.
**Invariants:** If Plotly is added, it must be loaded from `cdn.jsdelivr.net` (allowed in CSP); its fetch must be covered by `connect-src` — per CLAUDE.md, a new CDN dependency needs both `script-src` AND `connect-src` entries in `vercel.json`.

---

### [low] · Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`; `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` renders a row with a step type (e.g. `case_when`, `filter`, `log`). Add a "Jump to editor" icon button that calls a new `onJumpToStep(stepType)` prop. `WranglingModule` receives this and maps `stepType` to the correct tab and sub-section (`case_when` → Feature tab → case_when collapsible; `filter` → Clean tab → FilterBuilder), then calls `setActiveTab` and optionally `setSubSection`. A lookup table `STEP_TYPE_TO_TAB_SECTION` with ~20 entries covers the common types; unknown types show a toast "No editor for this step type."
**Invariants:** The mapping table must stay in the UI layer (`WranglingModule.jsx` or a sibling helper file), not in `runner.js` or `registry.js`. Do not import UI constants into pipeline files.

---

### [low] · General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx` (new `lists` slice); `src/components/wrangling/CleanTab.jsx` (filter UI); `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `lists: Map<string, string[]>` slice to the `SessionStateProvider` context (alongside the existing `datasets` registry). A user names a list and populates it with values (from a distinct-values panel or manual entry). Lists are referenced by name in filter expressions: `$myList` expands to an `in` predicate. Display a "My Lists" panel in the sidebar. Persist in `indexedDB.js` under a new `session_lists` key (scoped per `pid`). This is a design-heavy feature; suggest a short design session before implementation to agree on the name-reference syntax and the cross-module UX.
**Invariants:** Persist via `indexedDB.js`; never localStorage. The `$name` reference syntax must be resolved through `predicate.js` so it remains the single owner of the filter language.

---

## Suggested action order

1. **Fix time series group reference rendering** [2026-07-28, Explore, `ExplorerModule.jsx`] — correctness bug, multi-line plots broken for any grouped time series, high user-visible impact.
2. **Add time series label controls** [2026-07-09, Explore, `ExplorerModule.jsx`] — low-effort (mirrors existing numeric plot label inputs), unblocks pinned time series exports.
3. **Extend Simulate replication script** [2026-07-28, Simulate, `SimulateTab.jsx`] — the script currently lies by omission (DGP only, no tests or estimation), making the replication promise false.
4. **Fix Stata workbench `display` syntax** [2026-07-17, Calculate, `exportScript.js`] — crashes any Mata block; one-line fix per occurrence.
5. **Multiple hypothesis tests + save** [2026-07-06, Model, `CoefficientTestPanel.jsx`] — existing math is ready; only UI accumulation and snippet batching needed.
6. **Variable search in selector** [2026-07-09, Model, `VariableSelector.jsx`] — low-effort UX win for sessions with many columns.
7. **Draft state persistence in FeatureTab** [2026-07-17, Clean, `FeatureTab.jsx`] — common workflow pain point; straightforward sessionStorage solution.
8. **LaTeX table width scaling** [2026-07-05, General, `latexTable.js`] — publication-critical for multi-model comparison tables.
9. **Groups for pinned artifacts** [2026-07-17, General, `artifactOrder.js` + `ModelBufferBar.jsx`] — schema extension only; no migration needed.
10. **Update guide tour** [2026-07-17, General, `HelpSystem.jsx`] — copy-only change; unblocks new user onboarding for features shipped since the last tour edit.
