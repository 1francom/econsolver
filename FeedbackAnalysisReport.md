# Feedback Analysis Report — 2026-09-13

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 10 open bugs · 10 open features · 4 open UX/Design · 1 open performance · 0 open other

> **Cross-reference note:** Items marked ~~strikethrough~~ or `✓` in ClaudeFB.md, or `fixed`/`wontfix` in BugTriage.md, are omitted. Items marked "code-complete / browser validation pending" are also omitted (the fix is merged). "Likely fixed — needs confirmation" items are flagged inline and listed last in their section. No new feedback batches have arrived since 2026-07-31; status is unchanged from 2026-09-12 for all items except those noted.

---

## Bugs

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx` (no dedicated simulate exporter exists — `src/services/export/` has no `simulateScript.js`)
**Suggestion:** Add a `simulateScript.js` exporter that serializes the full Simulate session in order: (1) DGP draw params via the existing `distExprR/Py/Stata` in `src/math/dgpScript.js`, (2) any Monte Carlo loop (sample size, n_reps), (3) any downstream `SampleTestPanel` tests or estimation run on the simulated frame. Wire a "Copy replication code" button in `SimulateTab.jsx` that calls this exporter for all three languages, following the `generateCleanScript` preamble pattern already used by PlotBuilder. The DGP draw already exports correctly; the gap is stitching it to the downstream analysis steps.
**Invariants:** All script generation must go through per-language files in `src/services/export/`. No React in `src/math/`. The `dgpDraw.js` / `dgpScript.js` split (pure math vs. expression strings) must be preserved.

---

### [high] · Calculate/Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx` (Stata tab, `id: "stata"`), `src/services/export/stataScript.js`
**Suggestion:** `stataScript.js` has at least three explicitly-UNVERIFIED paths: (1) many-to-many `joinby` merge (line ~519 — "UNVERIFIED against a real Stata run"), (2) `ibn.` factor coding for through-the-origin no-intercept models (lines ~704, ~881 — labelled as UNVERIFIED in CLAUDE.md pending section), and (3) `ib#.` reference level for numeric factors (line ~741). Additionally, multiple estimators fall back silently to unsupported SE types with NOTE comments but no UI signal. Surface a per-language "⚠ UNVERIFIED" warning chip in `CodeEditor.jsx` whenever the generated snippet carries a `NOTE:` or `UNVERIFIED` marker (scan the emitted string before display), so users know before copying. Separately, audit the `joinby`, `ibn.`, and `ib#.` code paths against a live Stata session and promote them from UNVERIFIED to tested or replace with a documented no-op NOTE.
**Invariants:** Inline styles via `C` only. No AI calls to Anthropic outside `AIService.js`.

---

### [high] · Data/Performance · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (`parseFile` switch branches for `.rds`, `.xlsx`, `.shp`), `src/services/data/duckdb.js`
**Suggestion:** The `.dta` branch (fixed 2026-06-10) is the template: after in-browser parse, call `conn.insertArrowTable` or batch-insert into a DuckDB table, register it with `_duckdb`, reduce to a 500-row preview, and persist via the existing OPFS Parquet cache. Apply the same pattern to `.rds` (already parsed to a JS object array in `rds.js`), `.xlsx` (SheetJS gives rows), and `.shp` (DBF rows from `shapefile.js`). Gate behind the existing `size > 10_000_000` threshold. Add a "Converting to columnar format…" spinner in the Data header during the initial DuckDB write. Ensure `_duckdb` is forwarded through `onComplete` (the 2026-07-02 DuckDB-observation-count bug was caused by omitting it in `proceed()`).
**Invariants:** Data must never leave the browser. `Cross-Origin-Embedder-Policy: require-corp` is required for `SharedArrayBuffer`; `extensions.duckdb.org` must stay in `connect-src` in `vercel.json` (triple-bite pattern).

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` (TimeSeriesTab component, `grpCol` state block ~lines 1455–1620, `aggregateTimeSeries`/`fetchAggregateTimeSeriesSQL` ~lines 182–232)
**Suggestion:** The time series section groups by `grpCol` and renders one coloured line per group, but there is no "reference group" selector and no reference line rendered — confirmed by grep (no `refGroup`/`refLine`/`referenceGroup` variable exists anywhere in `ExplorerModule.jsx`). Add a `refGrp` state variable (default: `""` = none) with a chip selector below the group-by picker. When set, draw that group's line in `C.textMuted` (dashed) regardless of its position in the palette, and label it "Reference" in the legend. The chip should list the same group levels as the groupBy picker, with a "None" option. The pin payload should carry `refGrp` so the pinned time series snapshot preserves it.
**Invariants:** No external UI libraries; inline styles via `C` only. The Observable Plot spec must remain inside `ExplorerModule.jsx` — no React in `src/math/`.

---

### [medium] · Model/Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` runs one hypothesis at a time with no persistence. Add a `savedTests` array to component state (and persist per model uuid in `indexedDB.js` under the `model_buffer` store alongside the model). A "Save test" button appends `{label, Rmat, r, testType, statistic, pVal}` to the list; a scrollable table below the test panel shows saved results; a "Copy all to R" button calls `generateJointHypothesisScript` once per saved entry and concatenates. The `generateJointHypothesisScript` function added in 2026-07-30 already handles the per-test export — this is purely a multi-test accumulator UI.
**Invariants:** Persistence via `src/services/Persistence/indexedDB.js` only — never `localStorage`. No React in `src/math/`.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.
**File:** `src/ExplorerModule.jsx` (TimeSeriesTab render block, Observable Plot spec construction ~lines 1540–1570)
**Suggestion:** The time series Observable Plot spec has no `caption`, no `x.label`, and no `y.label` option exposed. Add a collapsible "Labels" panel (matching the one already in `PlotBuilder.jsx` for point/line geoms) with title, x-label, y-label, and caption text inputs. Wire them into the Plot spec's `caption` field and axis `label` options. The R replication script should emit `labs(title = …, x = …, y = …)` when any label is set.
**Invariants:** No external UI libraries; inline styles via `C` only.

---

### [medium] · General/Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` emits a fixed `{tabular}` environment with no width adaptation. For tables with more than ~5 regressor columns, wrap the `tabular` in `\resizebox{\textwidth}{!}{…}` (requires `graphicx`, which is almost universally loaded) or emit an `adjustbox` wrapper. Alternatively, auto-detect when the estimated column width × n_columns exceeds `\linewidth` and switch to a `longtable` or landscape-oriented `sidewaystable` (comment advising `\usepackage{rotating}`). Prepend a `% Required packages:` comment listing what to add to the preamble.
**Invariants:** Pure export/text-generation change — no engine or pipeline changes needed.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 — malformed CSV row aborts import
**File:** `src/DataStudio.jsx` (CSV parse path), `src/services/data/parsers/` (PapaParse config)
**Suggestion:** BugTriage marks this "fix-later — resilient import." Configure PapaParse with `skipEmptyLines: true` and a per-row `step` callback that captures malformed rows (those PapaParse flags with `errors`) without aborting. After parse, if any rows were skipped, render a banner in the Data tab header: "N rows skipped — malformed (see console)". Offer a "Download skipped rows" button (a `data:text/csv` download of those rows with original line numbers as a first column). For DuckDB-backed large files that use the DuckDB CSV reader, catch the `Invalid Input Error` and retry with `IGNORE_ERRORS = TRUE`, then surface a "K rows were ignored" count from `sniff_csv`.
**Invariants:** Non-destructive: malformed rows must never silently disappear — the user must be told explicitly. Never pass malformed rows to the pipeline as valid data.

---

### [low · fix-later] · Clean · 2026-06-14
**Feedback:** R-style `%in% c(...)` membership syntax is not accepted by the filter expression UI
**File:** `src/components/wrangling/CleanTab.jsx` (filter builder operator list), `src/pipeline/predicate.js` (`OPERATORS`)
**Suggestion:** BugTriage marks this "fix-later — post-launch expression-language compatibility." The `predicate.js` canonical operator set already includes `in` / `nin` operators that implement set-membership — the gap is that the Clean filter UI exposes them as an `IN (values)` chip, not as R-style `%in% c(...)` free-text input. A simpler short-term fix: add an NL-command-bar shortcut that parses `col %in% c("a","b")` to a `{op:"in", values:["a","b"]}` predicate object. This reuses the existing `in` operator without changing the canonical predicate language.
**Invariants:** `predicate.js` is the single owner of the canonical condition language — never re-implement a filter operator elsewhere.

---

### [critical · needs confirmation] · Model · 2026-07-22/28 (3 reports)
**Feedback:** P-values wrong — small t-stat reported as p<0.001; beta=0.0001 se=0.002 gives p<0.001 with *** (three separate reports)
**File:** `src/math/LinearEngine.js` (`tCDF`, `betainc`, `betacf` functions)
**Suggestion:** Commit `216b1ecb` (2026-07-28) replaced the fixed-2000-step Riemann-sum `betainc` with Numerical Recipes' Lentz continued-fraction method, which is stable for any `a, b`. This fix is marked "likely fixed — needs Franco confirmation." **Action required: Franco should test a regression with a near-zero t-stat (e.g. t ≈ 0.1, n ≈ 5000) and verify the p-value is close to 1, not close to 0.** Once confirmed, the three BugTriage rows (2026-07-22, 2026-07-28 ×2) should be updated to `fixed`. `calcEngine.js`'s `_incompleteBeta` symmetry-switch fix (2026-07-30, same class of bug) is in the same confirmation batch.
**Invariants:** `src/math/LinearEngine.js` must have zero React imports. The fix is math-only; no UI or pipeline changes.

---

## Features

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators
**File:** `src/components/PlotBuilder.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Expose `nbins`, `line_type` (linear/quadratic/none), `by` (conditional binscatters by group), and a raw-scatter toggle as model-plot config options in `ModelPlots.jsx`. Mirror `rdplot`'s `nbins`/`p` params and `binsreg::binsreg()`'s auto-selection for reference. Emit the corresponding R argument names in the replication script via the `specExtras` whitelist in `CodeEditor.jsx`.
**Invariants:** `specExtras` → export requires THREE whitelist edits (CodeEditor, ReportingModule, each `transpileModel` call site). No React in `src/math/`.

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Add a `sampleN` slider (1k … full n, log scale) to the plot config panel that randomly downsamples rows passed to the Observable Plot spec. For model plots (residual vs fitted, partial plots) this makes dense clouds legible without affecting the fitted line, which is always computed on the full dataset. The slider lives in PlotBuilder's existing "Labels/Axis" collapsible — NOT in the pipeline.
**Invariants:** Downsampling is view-only; it must never affect estimation results or the pipeline. `src/math/` must not be touched.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small `<input type="search">` above the variable chip list in `VariableSelector.jsx`. Filter displayed chips in real time by matching the typed string against column name and label. Keep the filtered list sorted alphabetically; highlight the matching substring with a `background: C.teal + "33"` span. This is a pure UI addition — no engine, pipeline, or math change needed.
**Invariants:** Inline styles only via `C`; no external UI libraries.

---

### Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT), `src/components/modeling/` (result display)
**Suggestion:** The AI prompt (lines 364–367 in `Prompts/index.js`) already emits log-log/log-level/level-log functional-form rules for the AI narrative. The "direct elasticity" request is about a POINT-elasticity panel shown alongside the coefficient table — at the sample mean, `ε = β × (x̄/ȳ)` for a level-level regression (the only case where the reported coefficient is not already an elasticity). Add a collapsible "Elasticities at the mean" section in the OLS result display that computes and shows `ε_i` for each regressor where both x̄ and ȳ are available from `result.means` (already populated by `LinearEngine.js`). This is a UI-only addition that reads from the existing `EstimationResult`.
**Invariants:** Computation must happen in the result display component, not in `src/math/LinearEngine.js` (adding UI concerns to a math file violates the React-free invariant). Zero React imports in `src/math/` and `src/core/`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When the user defines a numeric constant (e.g. `c = 5`), detect it in the expression evaluator and render a "Plot as line" toggle beside the constant's result row. When toggled on, draw a `Plt.ruleY([5])` mark (horizontal) or `Plt.ruleX([5])` mark (vertical, for domain constants) in the same graph panel alongside the function curves. This reuses Observable Plot's `Plt.ruleY` — the identical mechanism as PlotBuilder's `hline` geom.
**Invariants:** No external UI libraries; inline styles via `C`. `src/math/` and `src/core/` untouched.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set, the current legend shows colour swatches but no count or mean per group. Add an optional "Data labels" chip (off by default) in the PlotBuilder legend section that overlays each series' final data-point label, or appends a count annotation to each legend swatch row. R export should emit `ggrepel::geom_label_repel` when the option is on; Python emits `ax.annotate`; Stata emits `mlabel()`.
**Invariants:** Inline styles only; no external UI libraries.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Extend the Calculate workbench to evaluate limits: `lim(f(x), x → a)` from both sides. Use the existing expression evaluator to compute `f(a - ε)` and `f(a + ε)` at geometrically decreasing ε values (1e-1 … 1e-8), and report convergence. For symbolic results (e.g. l'Hôpital-style `0/0`) fall back to a NOTE recommending the CAS. Show the one-sided limits side-by-side. Export as `limit(f(x), x, a)` in R (requires `Ryacas`) and `sympy.limit` in Python.
**Invariants:** Computation in `CalculateTab.jsx`; no engine changes in `src/math/` needed for the numerical path. No external UI libraries.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** BugTriage marks this "fix-later — route to V4 plots." Implement a bounding-box drag filter on the Leaflet map that writes a `filter` pipeline step using `predicate.js` `gte/lte` operators on lat/lon columns. For DuckDB-backed datasets, compile through `predicateToSQL`. This reuses existing pipeline infrastructure and keeps the operation reproducible via `sp_*` step translators already in `stepTranslators.js`.
**Invariants:** Any new pipeline step must be added to `runner.js`, `registry.js`, AND all three script translators simultaneously. Never re-implement filter operators outside `predicate.js` — the 2026-08-06 "rogue operators" fix must not be undone.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a new pipeline step type `country_code` with params `{inCol, fromFormat, toFormat}` (formats: `iso2 | iso3 | name | numeric`). Ship a static lookup table (< 50 KB) in `src/math/countryLookup.js` (pure JS, zero React). The FeatureTab gets a "Country Code" sub-panel with two format dropdowns and a column picker. Export to R (`countrycode` package), Python (`pycountry`), Stata (`kountry` SSC). Update `registry.js` and `runner.js` in the same commit (pipelineReliabilityValidation.mjs T5 will catch a registry-runner mismatch).
**Invariants:** Static lookup table lives in `src/math/` — ZERO React imports. Step must be added to `STEP_REGISTRY` and handled in `runner.js` simultaneously.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis (3D plots)
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** Observable Plot 0.6 does not support 3D. Short-term: offer a colour-mapped 2D heatmap (`tile` geom with `aes.color = z`) as the "3D substitute" — already supported. True 3D would require Plotly.js from `cdnjs.cloudflare.com` as a new optional geom chip that lazy-loads only when selected. Add `cdnjs.cloudflare.com` to BOTH `script-src` AND `connect-src` in `vercel.json` if not already present (the triple-bite pattern — check before adding).
**Invariants:** External scripts may only load from the CSP allowlist. Any new CDN origin must be added to BOTH `script-src` AND `connect-src` in `vercel.json`.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** Extend the `artifactOrder` store schema (v9 `model_buffer` + spatial maps) to include a `groups: [{id, label, artifactIds[]}]` array alongside the flat `order` array. Add a "New group" button to `ModelBufferBar` that prompts for a label, then lets users drag model chips into named groups. The comparison mode (`ModelComparison.jsx`) should scope to the active group by default, with an "All groups" toggle. `getArtifactOrder`/`saveArtifactOrder` in `artifactOrder.js` carry the group structure transparently.
**Invariants:** Persistence via `indexedDB.js` only — never `localStorage`. The existing v9 `model_buffer` store is the right owner; schema change requires either a migration or graceful fallback when `groups` is absent.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when
**File:** `src/components/wrangling/FeatureTab.jsx` (case_when, mutate, lag sub-panels)
**Suggestion:** Each transformation sub-panel has controlled-input state that resets on unmount. Add a `useEffect` in each sub-panel that writes the current draft to `sessionStorage` under a key scoped to `pid + stepType` (e.g. `draft_<pid>_case_when`). On mount, restore from that key. Clear the draft when the step is successfully applied or explicitly cancelled. This is view-state persistence, not pipeline mutation, safe under the non-destructive pipeline invariant.
**Invariants:** `sessionStorage` is correct here (view draft, not pipeline data — `indexedDB.js` is required only for pipeline/data storage). Never write partial/invalid steps to the pipeline.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` already has a `type` field from `STEP_REGISTRY`. Map step types to their tab/section: `filter|drop_na|fill_na|recode` → Clean tab, `log|sq|std|dummy|lag|mutate|case_when` → Feature tab, `group_summarize|pivot_longer|pivot_wider` → Reshape tab, `join|append` → Merge tab. A dedicated ✏️ icon on each step row dispatches a `setActiveTab` + `setActiveSection` event to `WranglingModule`. Pre-populate the relevant sub-panel with the step's existing params so the user lands in an edit-ready state.
**Invariants:** Never mutate a step directly on click. Open a copy in the sub-panel editor and only commit on "Apply" (non-destructive pipeline invariant). `STEP_REGISTRY` in `registry.js` is the canonical step-type list — don't hardcode the mapping in `History.jsx`.

---

### General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array, starts at line 11)
**Suggestion:** `TOUR_STEPS` mentions Callaway-Sant'Anna and Sun-Abraham in one step (confirmed by grep — line 40 of HelpSystem.jsx) but the tour was written before N-way FE, Bacon decomposition, Synthetic Control validation, `connected_components`, facet_wrap in PlotBuilder, and several new Spatial operations shipped. Audit each step's `target` selector against the current DOM, update `content` to reflect current features (line 40 is already partially updated). Add or update steps for the Spatial and Simulate tabs (lines 51, 63 are stubs — expand them). Follow the CLAUDE.md convention: `title` = module name only, never "How to…"; never embed feature counts in copy.
**Invariants:** None — UI copy only. Do NOT re-introduce "How to" titles.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile branches for `.rds`, `.xlsx`, `.shp`), `src/services/data/duckdb.js`
**Suggestion:** *(Cross-listed in Bugs — see the Bugs section for the full suggestion.)* After in-browser parse of `.rds`, `.xlsx`, and `.shp`, write the result to DuckDB via `conn.insertArrowTable` or batch-insert, register `_duckdb`, persist via OPFS Parquet cache — the same pattern the `.dta` branch uses since 2026-06-10. Add a "Converting to columnar format…" progress indicator in the Data tab header during the write.
**Invariants:** DuckDB-Wasm requires `Cross-Origin-Embedder-Policy: require-corp` in `vercel.json`. `extensions.duckdb.org` must remain in `connect-src`. Data must never leave the browser.

---

## Suggested action order

1. **P-value confirmation [critical]** — Request Franco to browser-test a regression with a near-zero t-stat on a large dataset to confirm `216b1ecb` is live and correct. Three separate reports; resolves them all.
2. **Stata workbench [high]** — High-visibility gap that blocks replication for Stata users; surface a `NOTE:`/`UNVERIFIED` chip in `CodeEditor.jsx` as a minimal fix, then close each UNVERIFIED branch against a live Stata session.
3. **Simulate replication script [high]** — Replication fidelity is a core value proposition; extend the DGP-only exporter to cover the full Simulate session (tests, MC loop, estimation).
4. **Time series group references [medium]** — Broken rendering feature in the heavily-used Explore time-series panel; reference-group line is a small Observable Plot + `refGrp` state addition.
5. **Variable searcher in modeling sidebar [medium]** — Low-effort, high-impact UX: a single `<input type="search">` filter in `VariableSelector.jsx` directly unblocks sessions with many columns.
6. **Draft persistence in transformation subsections [medium]** — Recurring pain point (`case_when`, `mutate`); a `useEffect` ↔ `sessionStorage` round-trip per sub-panel requires minimal code and has no pipeline risk.
