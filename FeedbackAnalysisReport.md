# Feedback Analysis Report — 2026-08-23

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 15 open bugs · 13 open features · 3 open performance/pending-validation · 1 open other

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js`, `src/math/CallawayEngine.js`
**Suggestion:** `CallawayEngine.js` exports `runCallawayCS` but the R replication emitter in `rScript.js` likely falls through to a generic DiD case instead of emitting `did::att_gt` + `aggte` blocks. Add a dedicated `case "CallawayCS":` branch that emits the standard `did::att_gt(yname, tname, idname, gname, data=df, ...)` call with the correct control group, estimator, and aggregation options matching the engine's spec. Validate the emitted script runs against a fixture.
**Invariants:** Export must honour `seType` passed from the engine — do not hardcode classical SE.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`
**Suggestion:** `PlotBuilder` in `ExplorerModule.jsx` (line ~2631) is mounted without a `scriptPreamble` prop, so `buildGgplot` / `buildMatplotlibPlot` / `buildStataPlot` receive no dataset load context and emit no `read.csv` / `pd.read_csv` / `import delimited` header. Pass `scriptPreamble={(lang) => buildLoadPreamble(lang, filename, loadOpts)}` (reusing the `buildRLoadLine`/`buildPyLoadLine`/`buildStataLoadLine` helpers already imported in `plotScript.js`) to the `PlotBuilder` mounted in the Explorer's "Plot" tab. The ModelingTab's `PlotBuilder` already has a `scriptPreamble`; this is a gap in the Explore mount only.
**Invariants:** `loadLine.js` already exists — do not add raw `read.csv` strings inline; route through the shared helpers so format/delimiter/sheet-name hints are inherited.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (`group_summarize` case ~line 528), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` collapses one column's rows per group but cannot emit the difference of two groups' means as a single row — that requires a post-summarise `pivot_wider + mutate` chain, or a new `group_diff` aggregation function. The shorter fix: add a `"diff_means"` aggregation function that takes `{col, treatCol, treatVal, controlVal}` — internally groups by `treatCol`, computes each group's mean, then subtracts. Expose it in `ReshapeTab.jsx` as a new aggregate type "Diff in means (two groups)". A longer fix would surface `SampleTestPanel`'s existing `splitByGroup` path as a pipeline step rather than just a UI widget. Either must avoid touching `rawData` in place (replay-on-rawData invariant).
**Invariants:** Steps must replay on `rawData` — no in-place mutation. New agg types must be registered in `src/pipeline/registry.js` and translated in the three export scripts.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/services/export/stataScript.js`, `src/components/modeling/CodeEditor.jsx`
**Suggestion:** BugTriage points to `stataScript.js` as the probable file. Known gaps in this pass: two-way clustering emits `reghdfe` with a note but the required `ssc install reghdfe` line is not prepended; `xtreg` / `areg` branches that used to hardcode `vce(cluster entity)` were fixed in 2026-07-17 but the fix may not cover all panel estimators (EventStudy, LSDV, PoissonFE). Audit `generateStataScript` for any estimator family that still has a hardcoded or missing `vce()` option, and prepend `ssc install reghdfe, replace` and `ssc install ftools, replace` before any `reghdfe` call so the script is copy-pasteable without manual setup.
**Invariants:** Never add raw `fetch` to the Anthropic API in export files. Styles are irrelevant here.

---

### [high] · Pipeline / UI · 2026-07-09
**Feedback:** Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage status is "likely fixed" by the N-way FE commit (`045f5cda`). Needs Franco's browser confirmation on the UI — specifically that `ModelConfiguration` exposes an arbitrary-column FE picker (not just hardcoded entity/time dropdowns) when N-way FE is selected. If the picker is missing, add a multi-select column chip list in `ModelConfiguration.jsx` alongside the existing entity/time pickers, gated on estimator type being FE/TWFE.
**Invariants:** `src/math/PanelEngine.js` must have zero React imports.

---

### [high] · Pipeline / Math · 2026-07-09
**Feedback:** Add also FE with interactions
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions` step)
**Suggestion:** BugTriage status is "likely fixed" by `045f5cda`. Needs Franco's browser confirmation that interaction terms (e.g., `country × year`) can be included in an FE model's regressor list alongside the absorbed FE dimensions. If still broken, verify that `factor_interactions` expanded columns are correctly passed to `runFEMulti` and that the demeaning step does not collapse interaction columns.
**Invariants:** `src/math/` has zero React imports.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` emits a plain `\begin{tabular}{...}` with no width-adaptive wrapper. For tables wider than ~5 columns, wrap the tabular in `\resizebox{\textwidth}{!}{...}` (scales down, keeps aspect ratio) or switch to a `\begin{adjustbox}{max width=\textwidth}` (requires `adjustbox` package). Add the required `\usepackage{adjustbox}` to the preamble. Alternatively, switch wide tables to `\begin{landscape}` + `longtable`. The threshold for "wide" should be configurable (e.g., column count > 6 triggers auto-resize).
**Invariants:** None relevant.

---

### [medium] · Export / Math · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` runs one hypothesis at a time. To batch: add a `runHypothesesBatch(hypotheses, result)` that maps each `{R, r}` pair through the existing `testLinearHypothesis` and returns an array of results. In `CoefficientTestPanel.jsx`, expose an "Add hypothesis" button that appends to a local list; display results as a scrollable table. The R export should emit one `car::linearHypothesis(fit, ...)` call per row — already available from the snippet generator, just loop it. Save the hypothesis list in component state; optionally persist to IndexedDB alongside the model.
**Invariants:** Math changes stay in `src/math/`. No React imports in `ModelHypothesis.js`.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearHypothesis
**File:** `src/math/ModelHypothesis.js`
**Suggestion:** `ModelHypothesis.js` already computes a Wald chi-square (`chiSq`, line ~203) via `R'β − r` times the sandwich inverse. The result object already carries `chiSqPval`. The issue is that the UI in `CoefficientTestPanel.jsx` likely only displays the F-statistic variant. Expose `result.chiSq` and `result.chiSqPval` alongside F in the panel, and add a chi-square row to the R snippet (`car::linearHypothesis(fit, ..., test="Chisq")`). No math change needed — only UI wiring.
**Invariants:** None relevant.

---

### [medium] · AI / Export · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)
**Suggestion:** Add a new rule block to `INTERPRET_REGRESSION_PROMPT` for elasticity interpretation: if both Y and the regressor are log-transformed (detectable from variable names containing `log_`, `ln_`, or from the dictionary), the coefficient is a direct elasticity (1% change in X → β% change in Y) — state this explicitly rather than calling it a "marginal effect". Also handle semi-elasticity cases (log Y, linear X: 100·β% change; linear Y, log X: β/100 unit change per 1% change in X). The estimator-specific rules block already follows this pattern.
**Invariants:** All Anthropic calls must route through `AIService.js` only.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** PlotBuilder's Labels panel (already exists for other geoms) should be extended to time-series line/area plots. Confirm that `aes.title`, `aes.xLabel`, `aes.yLabel` are passed through to `buildGgplot`/`buildMatplotlibPlot` for `line` geom with a time x-axis. If ExplorerModule's ACF/PACF chart is the affected surface, add title+axis label inputs to the Time Series sub-tab of ExplorerModule and thread them into the SVG rendering and the export script preamble.
**Invariants:** Inline styles via `C` object from `shared.jsx` only.

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` maintains a flat ordered list of artifact IDs. Extend the schema with an optional `groupId` field per artifact and a parallel `groups` map (id → label). In `ModelBufferBar.jsx`, render groups as collapsible sections; within a group, only those artifacts are compared. Persist groups per project in IndexedDB alongside the artifact order. A minimal first pass: a simple "Group" tag chip on each pinned model card, with a filter dropdown at the top of the buffer bar.
**Invariants:** Persistence via `indexedDB.js` — never localStorage.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` holds in-flight `case_when` / `mutate` / `if_else` builder state in local React state that resets on tab navigation. Lift that state to a component-level ref or to a session-scoped context (e.g., a `draftSteps` map in `sessionState.jsx` keyed by step type) so it survives tab changes within the same session. Alternatively, persist the draft to `sessionStorage` (acceptable for transient within-session state, since the invariant only bars `localStorage` for pipeline/data storage). Clear the draft on explicit cancel or on successful step application.
**Invariants:** Pipeline/data storage must use IndexedDB. Transient within-session draft UI state can use `sessionStorage`.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The Data Viewer tab in `ExplorerModule.jsx` already supports pagination via `getTablePage`. Add column-header click handlers that set a `{sortCol, sortDir}` state and pass `ORDER BY sortCol [DESC]` to `getTablePage`'s SQL query (for DuckDB-backed datasets) or sort the JS row array before slicing (for small datasets). Display a sort indicator (▲/▼) in the header. This does not add a pipeline step — it is a view-only sort like a spreadsheet, leaving `rawData` unmodified.
**Invariants:** Sort must not mutate `rawData`; it is display-only, not a pipeline mutation.

---

### [medium / pending browser validation] · UI · 2026-06-10 → 2026-06-14
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage status is "code-complete (`35f4b5a6`), browser validation pending Franco." If browser testing surfaces remaining issues, check that the ResizeObserver in `PlotBuilder` is correctly re-triggering the Observable Plot render on height changes, and that `downloadCombinedPNG` captures the full resized canvas rather than a fixed internal size.
**Invariants:** Inline styles only via `C`.

---

### [high / pending browser validation] · UI · 2026-06-11 → 2026-06-14
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage status is "code-complete (`35f4b5a6`), browser validation pending Franco." Diagnostics render from the frozen `result.spec` so sidebar edits cannot mutate them. If still missing after validation, check that `ResidualPlots` receives a non-null `result` when OLS/WLS is selected and that `result.spec.fitted`/`result.spec.residuals` arrays are populated by `LinearEngine`.
**Invariants:** None.

---

### [medium / pending browser validation] · UI · 2026-06-13 → 2026-06-14
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** BugTriage status is "code-complete (`5e105529`), browser validation pending Franco." If still broken after testing, check that the KDE calculation receives valid lat/lon column names and that the Leaflet heatmap layer plugin is loaded before rendering.
**Invariants:** None.

---

### [other] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 (China,CHN,2010,2425.47...)
**File:** `src/services/data/parsers/` (CSV path), `src/DataStudio.jsx`
**Suggestion:** This is likely a stray quote, encoding issue, or a value with an embedded delimiter in the China row. The parser aborts on the first parse error rather than offering a skip/repair flow. Add a `resilientParse` mode (BugTriage 2026-06-14 "fix-later") that collects up to N malformed rows with their line numbers, presents a preview banner ("3 rows could not be parsed — skip?"), and continues loading the rest. Short-term fix: expose the raw error line and column to the user so they can fix the source file.
**Invariants:** None.

---

## Features

### Model · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** `predicate.js` already has an `in` operator. The gap is the UI: `FilterBuilder` in `CleanTab.jsx` needs a multi-value input for `in` / `nin` operators (comma-separated or a tag-input). Expose a textarea or tag input when `op === "in"` that parses a comma-separated or `c("a","b")` literal. The parser can strip R-style `c(...)` wrapping to extract the values array. The NL command bar also supports `in` via the AI step translator — no change needed there.
**Invariants:** `predicate.js` is the single owner of `in`/`nin` semantics — do not re-implement them in `CleanTab.jsx`.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** The Map tab (Leaflet) already shows grids; the Plot tab (Observable Plot / SVG) does not. Add a "Grid overlay" layer type to `GeoLayerConfig.jsx` that accepts a grid dataset (produced by a `sp_grid_assign` pipeline step) and renders it as `Plt.cell` (BAND scales on both axes) in `SpatialGeoPlot.jsx`. Reuse the existing `TILE_SCHEMES` from `PlotBuilder.jsx` for fill colour.
**Invariants:** No React in `src/math/`.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/modeling/results/` (RDD result display), `src/math/CausalEngine.js`
**Suggestion:** `CausalEngine.runSharpRDD` already returns LATE, SE, confidence intervals, bandwidth, kernel type, and polynomial order. Render them in an `rdrobust`-style summary block: a header row showing the cutoff and bandwidth, a coefficient block (Conventional / Bias-Corrected / Robust rows with N-left / N-right counts), and an effective-n block. This is a display change only — no math change required. Model it on the existing `BaconPanel.jsx` pattern (collapsible section below the main result table).
**Invariants:** Display-only change; no math in the UI component.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add an optional `filter` field to each layer config in `GeoLayerConfig.jsx`. The filter should use `evalPredicate` from `src/pipeline/predicate.js` (already the single predicate owner) to subset the layer's rows before rendering. Expose a simple "where column op value" UI row per layer — reuse `FilterBuilder`'s compact single-condition variant if extractable, or implement inline. Routed to V4 per BugTriage; medium effort.
**Invariants:** `predicate.js` is the single owner — never re-implement filter operators in spatial components.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", etc.
**File:** `src/components/modeling/` (result display / LaTeX table builder), `src/services/export/latexTable.js`
**Suggestion:** Add an editable `displayNames` map (column → label) stored per model result in component state. In the LaTeX export and the on-screen table, substitute `displayNames[col] ?? col`. Expose an "Edit labels" pencil icon that opens an inline edit grid. Persist `displayNames` in IndexedDB alongside the model buffer entry so they survive reload.
**Invariants:** Persistence via IndexedDB.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" mode to the Calculate workbench alongside Derivatives, Integrals, and Optimise. Use the existing CAS engine (or a lightweight symbolic limit evaluator) to compute `lim_{x→a} f(x)`. Expose a `limit(f, x, a)` function in the expression evaluator. For one-sided limits, add a direction selector (→ a⁺, → a⁻). Show steps analogously to the existing derivative step display. The function graph (already multi-equation capable) can annotate the limit point with a vertical asymptote marker.
**Invariants:** Inline styles via `C` only.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When the Calculate graph evaluates an expression for a range of x-values, a constant expression (no x) produces a flat line — this already works if the evaluator returns a scalar. If it doesn't, add a check: if `eval(expr, {x: 0})` returns the same value as `eval(expr, {x: 1})`, treat it as a constant and draw a dashed `hline` at that value (reuse the PlotBuilder `hline` geom pattern: `Plt.ruleY([c])` in Observable Plot). Label it with the expression string.
**Invariants:** Inline styles via `C`.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`
**Suggestion:** Observable Plot 0.6 does not support dual y-axes natively (neither does ggplot's base layer system without `sec.axis`). A pragmatic approach: allow one "secondary Y" layer with an independent Y domain that is rendered on the right-hand axis using two overlapping SVG marks with separate scales, then merged in the SVG. In R, emit `ggplot` with `scale_y_continuous(sec.axis = sec_axis(~.*factor, name="Y2"))`. Flag this as medium-complexity and note the known rendering limitation that the secondary axis is always a linear rescaling of the primary.
**Invariants:** Inline styles via `C`.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small text input at the top of each variable panel (Y, X, Controls / W) in `VariableSelector.jsx` that filters the rendered column list to those whose name contains the typed string (case-insensitive). Clear the filter on blur or when a variable is selected. This is a UI-only filter — it does not affect the actual variable set, only what is visible for clicking.
**Invariants:** Inline styles via `C`.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set to a categorical column, the legend already uses `observable10` swatches. "More labels" likely means: (a) showing the category value in a tooltip or data label directly on the mark (add a `showLabels` toggle in the Labels panel that emits `Plt.text(data, {x, y, text: d => d[colorCol], fill: colorScale(d[colorCol])})` for point marks); or (b) showing value labels on the legend entries for counts. Clarify with Franco which surface is affected; implement (a) first as the more common need.
**Invariants:** Inline styles via `C`.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each pipeline step has a `type` that maps to a tab/subsection (e.g., `filter` → Clean tab > Filter, `log` → Feature > Transforms). Add an "Edit" icon to each step row in `History.jsx` that calls a `onEditStep(step)` callback. In `WranglingModule.jsx`, handle `onEditStep` by switching to the correct tab and, within that tab, scrolling to or pre-populating the relevant builder with the step's current parameters. This requires each sub-panel to accept an `initialState` prop for pre-population.
**Invariants:** Pipeline must stay non-destructive — editing opens the builder pre-filled; applying creates a new step or replaces the old one atomically.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`
**Suggestion:** Add a `country_code` pipeline step with a small static lookup table (ISO 3166-1 — ~250 rows, ~15 KB inline) embedded in a new `src/pipeline/countryCodes.js` module. The step accepts a source column (containing iso2, iso3, or full name) and an output format, and adds a new column with the mapped value. In `FeatureTab.jsx`, expose it as a new "Country Code" card in the Feature tab. Register it in `registry.js` and add R/Python/Stata translators (`countrycode` package in R, `pycountry` in Python, manual label logic in Stata).
**Invariants:** Steps replay on `rawData`; lookup table is pure JS, no React.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`
**Suggestion:** Add a session-scoped "Lists" store to `sessionState.jsx` (a `Map<name, string[]>` in the context) and an IndexedDB persistence slot for them. In `CleanTab.jsx`'s filter UI, add a "Use list…" button that opens a picker showing saved lists and injects the values into an `in` predicate. In `FeatureTab.jsx`, expose lists in the `case_when` value picker. Lists should be editable from a dedicated "Lists" panel (a new collapsible section in the sidebar or a small modal). This is a design-session item per the feedback note — scope with Franco before implementing.
**Invariants:** Persistence via IndexedDB. Lists feed into `predicate.js`'s `in` operator — never re-implement membership elsewhere.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** BugTriage routes this to "Fase X4 code-complete; browser benchmark pending Franco." The plan (`docs/superpowers/plans/2026-05-22-fase-x4-performance.md`) covers OPFS Parquet caching, DuckDB query boundary enforcement, and 200–500 row display cap. If browser testing shows crashes still occur, check: (1) whether non-CSV large formats (`.dta`, `.rds`, `.xlsx`) are routed through DuckDB after parse (`.dta` was fixed 2026-06-10; `.rds`/`.xlsx` were listed as "future work"); (2) whether the 200-row UI preview is correctly capped on every tab transition. The COOP/COEP headers enabling `SharedArrayBuffer` for threaded Wasm must be present on Vercel (`vercel.json`).
**Invariants:** Data never leaves the browser; no server-side compute. DuckDB is the data boundary — never pull the full Arrow result into JS objects.

---

## Suggested action order

1. **Plot replication code missing load lines** (high · export · 2026-07-05) — one-line fix in `ExplorerModule.jsx`: pass `scriptPreamble` to `PlotBuilder`. Highest impact-per-effort ratio; affects every exported plot from the Explore tab.
2. **group_summarize ATE/difference-in-means** (high · pipeline · 2026-07-17) — core econometric workflow gap reported by multiple users; blocks basic treatment-effect calculation without a manual multi-step workaround.
3. **CS DiD replication code does not work** (high · export · 2026-07-05) — the Callaway-Sant'Anna estimator is fully implemented in `CallawayEngine.js` but the R export is broken; researchers who used this estimator cannot replicate their work.
4. **Stata workbench incomplete** (high · export · 2026-07-17) — affects the Stata-primary user segment (LMU target audience skews Stata); audit and fix `generateStataScript` missing `ssc install` preambles and uncovered estimator branches.
5. **Browser-validate pending code-complete items** (Plot Builder resize, Y~Yhat/Residual/Q-Q, Spatial heatmap) — three features are code-complete since 2026-06-14 with no Franco confirmation; unblock them or surface regressions before adding new work.
6. **Chi-square for linear hypothesis** (medium · math · 2026-07-17) — math already computed in `ModelHypothesis.js`; only UI wiring needed (expose `chiSq`/`chiSqPval` in `CoefficientTestPanel`).
7. **Multiple hypothesis batch & R replication** (medium · export · 2026-07-06) — frequently requested; `ModelHypothesis.js` is already loopable; main work is UI state and export looping.
8. **LaTeX table width adaptation** (medium · export · 2026-07-05) — single `\resizebox` wrapper in `latexTable.js`; low effort, high polish for publication-ready output.
9. **Variable searcher in variable slider** (medium · ui · 2026-07-09) — purely cosmetic filter in `VariableSelector.jsx`; unblocks wide datasets where the column list becomes unmanageable.
10. **Arrange (sort) control in data viewer** (medium · ui · 2026-07-17) — view-only sort; passes `ORDER BY` to `getTablePage`; no `rawData` mutation.
