# Feedback Analysis Report — 2026-09-09

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **6 open bugs** · **16 open features** · **4 open UX/Design** · **1 open performance** · **0 open other**
- **27 total open items** analysed from ClaudeFB.md + BugTriage.md

Note: Many ClaudeFB.md bullets appear un-struck but are confirmed resolved in BugTriage.md (X5 reconciliation 2026-06-14). Those items are treated as resolved and excluded. Only items with BugTriage status `open` or `fix-later`, or items absent from BugTriage with no ✓ marker, are included below.

---

## Bugs

### [high] · Export (Calculate/Code) · 2026-07-17
**Feedback:** "Stata workbench doesn't work completely"
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** Audit the Stata tab in CodeEditor against the list of estimators in EstimatorSidebar — identify which emit incomplete or syntactically broken do-file blocks (LSDV, FD, and interaction-based FE are the likeliest culprits based on past SE-type bugs). Add a per-estimator smoke test to `src/services/export/__validation__/` asserting the Stata snippet compiles (no `invalid syntax` in a `do` file head). Fix each broken emitter in `stataScript.js`.
**Invariants:** Stata emitter must never fabricate commands — emit a `* NOTE: no base-Stata equivalent` comment rather than a silently wrong snippet.

---

### [high] · Export (Simulate) · 2026-07-28
**Feedback:** "Update the replication script, currently it replicates only the DGP"
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/statInferenceScript.js`, `src/services/export/statInferenceGroupScript.js`
**Suggestion:** `SimulateTab.jsx` has a "Copy script" path for the DGP only. Extend it to capture the full downstream analysis: resampling config (→ `statInferenceGroupScript.js` conventions), sample test selections (→ `statInferenceScript.js`), and any estimation run on the simulated data (→ `rScript.js` model block). The script should re-draw from the DGP seed first, then replay the analysis. Use the same `scriptPreamble` / `resolvePlotPreamble` seam already in `plotScript.js` so nothing is hard-coded.
**Invariants:** SE type must be passed explicitly to any script-emitted engine call; math files (`src/math/`) must not be called from export logic.

---

### [high] · UI (Explore) · 2026-07-28
**Feedback:** "References of the group in time series doesn't work"
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** In the time series view, the "group by" / color reference line is likely reading the wrong column after a `color = X` aesthetic mapping (the same class as the 2026-05-19 PlotBuilder color bug). Reproduce with a grouped time-series dataset; if the reference line uses the FULL dataset's mean instead of the per-group mean, fix by scoping the reference-line computation to each group's rows before passing to Observable Plot — mirror how the faceted boxplot already builds per-panel stats.
**Invariants:** No React imports in `src/math/`; plot computations live in the component, not in engine files.

---

### [medium] · Export (Report) · 2026-07-05
**Feedback:** "Adjust the size of LaTeX tables when the number of columns increases"
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` likely emits a fixed-width `tabular` environment. Switch to `tabularx` (fill available width) or compute per-column width as `\textwidth / k` when `k > 4`. Also add `\resizebox{\textwidth}{!}{…}` as a fallback for very wide tables. Expose a "table width" toggle (auto / full-width / compact) in the LaTeX export panel in `ReportingModule.jsx`.
**Invariants:** None specific — this is a pure export concern.

---

### [medium] · Export (Model) · 2026-07-06
**Feedback:** "Add vector for running more than 1 hypothesis and save them to replicate in R"
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` currently runs one hypothesis at a time and discards the result. Add a "hypothesis stack" (local state array) where each tested hypothesis is appended with its Rβ, null value, result (F/χ², p), and the emitted R snippet. Expose a "Copy all" button that calls `generateJointHypothesisScript()` for each entry and concatenates into one R script block. Persist the stack to the model's `spec` (via the same whitelist in `CodeEditor.jsx`) so it survives pinning.
**Invariants:** `ModelHypothesis.js` is in `src/math/` — no React imports; all UI state lives in the component.

---

### [medium] · Data · 2026-05-10
**Feedback:** "Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47..."
**File:** `src/services/data/parsers/` (CSV path in `src/DataStudio.jsx`), `src/DataStudio.jsx`
**Suggestion:** The CSV parser aborts the entire import on a malformed row. Wrap the per-row parse step in a try/catch that accumulates `{line, rawText, reason}` into a `skipped` array, then surface a post-import banner: "Loaded N rows · K rows skipped — download error log". This matches the existing `skipped` pattern in `rdata.js` and `parseExcel`. For DuckDB-backed loads (>10MB), use DuckDB's `COPY … (IGNORE_ERRORS TRUE)` or the `read_csv` `ignore_errors` option.
**Invariants:** Non-destructive pipeline invariant: the raw data must reflect what actually loaded, not an assumed full set; skip count must be visible to the user.

---

## Features

### Model · 2026-05-21
**Feedback:** "Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression"
**File:** `src/math/CausalEngine.js`, `src/components/ModelingTab.jsx` (RDD results section)
**Suggestion:** `runSharpRDD` already returns bandwidth, kernel, polynomial order, and effect estimates. Add a `formatRDDSummaryTable()` helper in `CausalEngine.js` that structures these into a `rdrobust`-style block (conventional / bias-corrected / robust columns, bandwidth h/b, effective n left/right). Render it as a pre-formatted mono block in the results panel above the existing coefficient table.
**Invariants:** Zero React imports in `src/math/`; formatting logic that depends on JSX must live in `ModelingTab.jsx` or a results component.

---

### Model · 2026-06-06
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/ReportingModule.jsx`, `src/services/export/latexTable.js`
**Suggestion:** Add a `varLabels` map (`{colName: displayLabel}`) to the model's `spec`. In `latexTable.js`, substitute labels before emitting row names. Expose an inline-edit UI in ReportingModule's LaTeX preview (click on a row label → text input → Enter commits). Persist via `CodeEditor.jsx`'s spec whitelist. RDD hard-coded labels (`"D (treatment)"` etc.) should be seeded from the actual column names as defaults.
**Invariants:** The `spec → export` whitelist in `CodeEditor.jsx` AND the three exporter call sites (single-model/multi-model/subset) must all be extended — see CLAUDE.md gotcha "THREE whitelist edits".

---

### Explore · 2026-06-14 (fix-later)
**Feedback:** "R-style `filter(regionname %in% c(...))` syntax is not accepted by the filter expression UI"
**File:** `src/pipeline/predicate.js`, `src/components/wrangling/CleanTab.jsx`
**Suggestion:** The canonical `in` / `nin` operators in `predicate.js` already implement the membership semantics. Add a syntax-sugar parser in `CleanTab.jsx`'s NL command bar or a dedicated "values list" chip in `FilterBuilder` that auto-generates an `in` predicate from a comma-separated paste. Do NOT add a new operator to `predicate.js` — `in` covers it; this is a UX affordance, not a language gap.
**Invariants:** `predicate.js` is the single owner of the condition language (CLAUDE.md: "never re-implement a filter operator anywhere else").

---

### Spatial · 2026-05-30 (fix-later)
**Feedback:** "Allow spatial filters for plots"
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer filter row in `GeoLayerConfig.jsx` (column picker + operator + value, reusing `FilterBuilder` or the predicate chip pattern from `CleanTab`). Apply the filter via `evalPredicate` from `src/pipeline/predicate.js` to the layer's rows before passing to the geo renderer. Keep it view-only (no pipeline step created) — same convention as the Data Viewer's filter.
**Invariants:** Use `predicate.js` exclusively for filter evaluation; never re-implement operator logic in a spatial component.

---

### Model · 2026-07-09
**Feedback:** "Add direct elasticity interpretation"
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)
**Suggestion:** Add an "ELASTICITY RULE" to `INTERPRET_REGRESSION_PROMPT`: when both Y and X are log-transformed (detectable from column names via `log_`, `ln_` prefix or dict unit `log`) the coefficient IS the elasticity and must be stated as "a 1% increase in X is associated with a β% change in Y". When only X is log (semi-elasticity), state "a 1-unit increase in log(X), i.e. ~1% increase in X, changes Y by β units". Thread the variable dictionary (`metadataExtractor.js`) into the prompt context so the model can detect the transformation.
**Invariants:** All Anthropic calls go through `AIService.js` — no raw fetch elsewhere.

---

### Calculate · 2026-06-19
**Feedback:** "Add limits"
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" panel to the Calculate workbench (alongside Derivatives/Integrals). Use the existing CAS / symbolic math library already powering derivatives to evaluate `lim_{x→a} f(x)`. For one-sided limits, expose a direction toggle (left / right / two-sided). Show the result and the L'Hôpital-applied intermediate step in the "show steps" panel.
**Invariants:** No external UI library; all styling via the `C` object from `shared.jsx`.

---

### Calculate · 2026-07-04
**Feedback:** "Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)"
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When the user defines a variable as a constant (no `x` in the expression), add it as a horizontal `hline` mark in the existing equation canvas rather than trying to plot it as a curve. The `hline` geom already exists in `PlotBuilder.jsx` — reuse its Observable Plot `ruleY` pattern. Label the line with the constant's name.
**Invariants:** None specific.

---

### Model · 2026-07-09
**Feedback:** "Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster"
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small text input at the top of `VariableSelector.jsx`'s column list that filters visible rows to those matching the typed string (case-insensitive, substring match). Keep all selected variables visible regardless of the filter so selections aren't hidden. This is a pure UI filter — no change to the underlying headers array or pipeline.
**Invariants:** None specific — pure UI.

---

### Explore · 2026-07-11
**Feedback:** "Add more labels in tables or boxes when plotting by color"
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, the Observable Plot legend shows color swatches but no value-count or summary. Add an optional legend table (toggle in the Labels panel) that lists each color group, its count, and optionally its mean Y, rendered as a `<table>` overlay positioned top-right of the canvas. Use the existing `buildColorScale` helper from `src/components/tabs/spatial/shared/color.js`.
**Invariants:** None specific — UI concern only.

---

### Clean · 2026-07-17
**Feedback:** "Add an option to click on one pipeline step and go to the corresponding subsection automatically"
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` already renders its `type`. Map step types to their owning tab (e.g. `filter` → Clean, `log/sq/std` → Feature, `pivot_longer` → Reshape, `join` → Merge) and emit a click handler that calls the existing `setActiveTab(tabName)` in `WranglingModule.jsx`. Scroll the target tab panel to the relevant section using a `useRef` / `scrollIntoView` on the section header.
**Invariants:** None specific.

---

### Clean · 2026-07-19
**Feedback:** "Add a function for Country Codes — iso2/iso3/country-name crosswalk transform"
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step with a compact static lookup table (~250 rows, < 20 KB) mapping iso2 ↔ iso3 ↔ country name. Expose it in FeatureTab under a new "Lookup" section (alongside `lag`, `lead`, etc.). The step takes `{col, from: "iso2"|"iso3"|"name", to: "iso2"|"iso3"|"name"}` and uses a VLOOKUP-style match. For fuzzy name matching, fall back to `fuzzyGroups` from `wrangling/utils.js` with a high threshold (>0.9). Keep the lookup table as a pure JS module in `src/math/` or `src/pipeline/`.
**Invariants:** Registry must stay in sync with runner.js. The `src/math/` lookup module must have zero React imports.

---

### General · 2026-07-19
**Feedback:** "Think about how to construct lists, save them and use them during the session when filtering or constructing variables"
**File:** `src/services/session/sessionState.jsx`, `src/pipeline/predicate.js`, `src/components/wrangling/CleanTab.jsx`
**Suggestion:** This needs a design pass before implementation. Sketch: a "Named Lists" store in `sessionState.jsx` (`{name, values[], type}` entries). In FilterBuilder, the `in` operator gains a "…from list" option that references a list by name instead of inlining values. In Feature builder, `case_when` / `if_else` can reference a list in a membership test. Persistence: `indexedDB.js` — new store `session_lists`. Export: R emits `list_x <- c(...)` preamble, Python a `dict`, Stata a `local`. **Do not implement without a design session with Franco to agree on scope.**
**Invariants:** IndexedDB for persistence (not localStorage). Predicate language: reuse `in`/`nin` from `predicate.js`.

---

### Model · 2026-07-22
**Feedback:** "Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators"
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** The binscatter (partial plot / AVP) currently has no controls. Add a collapsible options panel: number of bins (default 20), equal-frequency vs equal-width binning, a CI band toggle (bootstrap or analytical), and a fit-line polynomial order. Mirror `rdplot`'s argument surface. Emit the equivalent `rdplot` / `binsreg` call in R and `binscatter` in Stata.
**Invariants:** None specific — UI + export concern.

---

### Model · 2026-07-22
**Feedback:** "Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations"
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Add an `x-range` / `y-range` clamp control in the Labels panel of `PlotBuilder.jsx` (min/max input pairs). Pass these as `domain` overrides to Observable Plot's x/y scales. In ModelingTab's G10 templates, wire the same control through `plotSpec`. This is a VIEW clamp — it must not filter the underlying data.
**Invariants:** None specific — view-only clamp.

---

### General · 2026-07-05
**Feedback:** "Add plots with 3 axis"
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot does not natively support a second y-axis. A pragmatic approach: allow a second y-axis by adding a `y2` aesthetic mapping and rendering a secondary Observable Plot layer with its own scale, then overlaying the two SVGs with aligned x-axes via CSS `position: absolute`. Complex but achievable without forking the library. **Scope this narrowly** — only `line` geom on `y2` initially. Export in R via `ggplot2::sec_axis`, Python via `twinx()`, Stata via `twoway` overlay.
**Invariants:** No external UI library; all styling inline via the `C` object.

---

## UX / Design

### Explore · 2026-07-09
**Feedback:** "Add labels for the Time series plot, title, axis, etc."
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The time series view in Explore appears to render its own chart (not PlotBuilder) and lacks the Labels panel that `PlotBuilder.jsx` provides. Either (a) migrate the time series chart to reuse `PlotBuilder.jsx` with a `line` geom preset, or (b) add a small inline label editor (title, x-label, y-label) above the chart in `ExplorerModule.jsx` and pass them as Observable Plot `caption` / `label` options. Option (a) is preferable for long-term consistency.
**Invariants:** None specific.

---

### Clean · 2026-07-17
**Feedback:** "Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue"
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `draftState` object persisted to `sessionStorage` (keyed by `pid`) that captures the in-progress form state for each Feature sub-section (case_when rows, if_else condition, formula text, etc.). On remount, rehydrate from `draftState`. Clear the draft when the step is successfully applied. Keep the draft for the SAME `pid` only — never bleed across projects.
**Invariants:** IndexedDB for pipeline/data persistence; `sessionStorage` is acceptable here (transient per-session draft, not user data).

---

### General · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time"
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/modelBuffer.js`
**Suggestion:** Add a `groupLabel` string to each artifact entry in `artifactOrder.js`. In `ModelBufferBar.jsx`, render pinned models grouped by label with a section divider. The comparison table (`ModelComparison.jsx`) should only compare items within the same group by default, with a "compare across groups" toggle. Persist group assignments in IndexedDB alongside the artifact order.
**Invariants:** IndexedDB for persistence (not localStorage). All styling via the `C` object.

---

### General · 2026-07-17
**Feedback:** "Guide tour must be updated"
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS registry)
**Suggestion:** Audit `TOUR_STEPS` in `HelpSystem.jsx` against the current WorkspaceBar tab names, module entry points, and feature set (N-way FE, Callaway-Sant'Anna, Bacon decomposition, Synthetic Control, Spatial tab). Update step text and selectors. Per CLAUDE.md: never write a count into help copy ("9 steps") — the count is a short-lived fact. Run the tour in a browser after updating to confirm all CSS selectors still resolve.
**Invariants:** No external UI library; tour overlay is custom (`TourOverlay` in `HelpSystem.jsx`).

---

## Performance

### Data · 2026-05-08 (pending browser validation)
**Feedback:** "Big datasets generate crashes (21MB) over 900k rows"
**File:** `src/DataStudio.jsx` (parseFile path), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** DuckDB Fase X4 (code-complete per X5 reconciliation) routes large CSV/TSV to DuckDB and caches via OPFS Parquet. The crash report predates this fix and may already be resolved. **Action: Franco should browser-benchmark the 21MB / 900k-row CSV file** and confirm no crash. If the crash persists, profile via Chrome DevTools: likely the issue is the initial `parseCSV` call materialising the full JS array before DuckDB registration. Fix: stream-parse directly into DuckDB via `db.registerFileBuffer` or `insertCSVFromPath` without the intermediate full-array allocation.
**Invariants:** Full dataset must be queryable by runner.js and duckdbRunner.js; never truncate rawData before pipeline steps; display limit (200–500 rows) is view-only.

---

## Suggested action order

1. **Fix Stata workbench (CodeEditor)** — users copy scripts to run in Stata and a broken output directly undermines the replication-fidelity value proposition; `stataScript.js` has known gaps (xtreg two-way cluster, `ibn.` factor, IV poly fallback).
2. **Fix Simulate replication script** — incomplete script export from Simulate is a research-workflow blocker; extend `SimulateTab.jsx` to emit DGP + full downstream analysis using existing script-generation infrastructure.
3. **Fix time series group references** — broken reference lines give wrong visual output; likely a scoping bug in the `color = X` grouped series path in `ExplorerModule.jsx`.
4. **LaTeX table width scaling** — expand `latexTable.js` to use `tabularx` / `resizebox` for wide multi-model tables; quick win with clear R precedent.
5. **Variable searcher in VariableSelector** — pure UI addition with high usability payoff for large datasets; no math or pipeline changes.
6. **Pipeline step → subsection jump** — click-to-navigate in `History.jsx` is a type-to-tab mapping exercise; removes one of the most common navigation pain points.
7. **Multiple hypothesis tests stack** — extend `CoefficientTestPanel.jsx` to accumulate and export multiple tests; most of the math already exists in `ModelHypothesis.js`.
8. **Elasticity interpretation in AI narratives** — add elasticity rule to `INTERPRET_REGRESSION_PROMPT`; low implementation cost, high research-communication value.
9. **Country Codes transform step** — high demand from LMU students working with cross-country panel data; self-contained pipeline step with a small static lookup table.
10. **RDD rdrobust-style summary table** — data already computed by `runSharpRDD`; formatting-only addition to the results panel.
