# Feedback Analysis Report — 2026-09-20

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 10 open bugs · 13 open features · 0 open UX · 1 open performance · 3 needs-confirmation

---

## Bugs

### high · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** Audit the Stata tab in `CodeEditor.jsx` end-to-end against `stataScript.js`: confirm the Stata output path is wired for every estimator type, that `transpileModel` branches reach all estimators (not just OLS/FE), and that any recently added spec fields (e.g. `noIntercept`, CallawayCS fields) flow through. Cross-check the `ibn.`/`ib#.` interaction-factor emitter noted in CLAUDE.md as "UNVERIFIED against a live Stata". Known gap: `ibn.` under interactions still uses a NOTE rather than verified Stata syntax.
**Invariants:** `seType` must be passed explicitly and not hardcoded inside the exporter. Never add React imports to math/export files.

---

### high · Export · 2026-07-28
**Feedback:** Replication script for Simulate only reproduces the DGP draw, not the downstream analysis (resampling/tests/estimation on the simulated data)
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/rScript.js`, `src/services/export/pythonScript.js`, `src/services/export/stataScript.js`
**Suggestion:** `SimulateTab.jsx`'s "Copy Script" path currently feeds only the DGP parameters to the export layer. Extend it to also serialise the active StatWorkspace state (resampling config, sample test settings) and any Monte Carlo results, and add a `simulateExportSection` function in each script generator (parallel to `generateCleanScript`) that emits the corresponding R/Python/Stata code (e.g. `set.seed()` + DGP draw + `t.test()` / bootstrap loop). Guard against emitting literal vector dumps for large simulations — use parametric form only.
**Invariants:** Export functions must be pure JS with no React imports. All script generation must go through the existing per-language generators; do not add a raw `fetch` to the Anthropic API.

---

### medium · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** In `latexTable.js`, detect when column count exceeds a threshold (e.g. > 6) and wrap the `tabular` environment in `\resizebox{\linewidth}{!}{…}` or `\adjustbox{max width=\linewidth}{…}` (the latter requires `adjustbox` package, which should be added to the emitted `\usepackage` list). Alternatively, switch automatically to `\small` font inside the table body. Surface a toggle in `ReportingModule.jsx` for users who prefer landscape (`\begin{landscape}`) over scaling.
**Invariants:** none specific beyond inline-style conventions in the UI layer.

---

### medium · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` currently runs one hypothesis at a time and shows the result inline. Add a "Save test" button that appends the current hypothesis (restriction string + null value + test type + result) to an array in component state (or a lightweight IndexedDB store keyed by model uuid). Render the saved list as a compact accordion. In `ModelHypothesis.js`'s `generateJointHypothesisScript()`, accept an array of constraints and emit a single `car::linearHypothesis` call per saved test. The "Copy" button in `CoefficientTestPanel` should include all saved tests in sequence.
**Invariants:** Persistence must use IndexedDB (`services/persistence/indexedDB.js`), never `localStorage`. `ModelHypothesis.js` is in `src/math/` — must remain React-free.

---

### medium · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis labels, etc.
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The Explore time-series sub-tab renders its own SVG (not PlotBuilder). Add a Labels section below the chart controls (matching PlotBuilder's existing Labels panel structure) with inputs for chart title, x-axis label, and y-axis label. These values should be passed as props into the time-series chart render function and included in any pinned/exported script. Mirror the PlotBuilder `labels` state shape so pin output is consistent.
**Invariants:** Styling must use inline styles via the `C` object from `shared.jsx`. No external UI libraries.

---

### medium · UI · 2026-07-28
**Feedback:** References of the group in time series plot don't work
**File:** `src/ExplorerModule.jsx`
**Suggestion:** In the Explore time-series chart, the "group by color" path uses a `groupCol` to split lines by category. The reference line / reference group selector likely reads from component state that is initialised before `groupCol` is set, producing an empty or stale reference. Audit the `useEffect`/`useMemo` that builds the group-colour mapping in `ExplorerModule.jsx` and ensure the reference group dropdown's options are re-derived reactively whenever `groupCol` or `filteredRows` changes. Also verify the reference line is drawn AFTER all other lines, not before, since Observable Plot renders in paint order.
**Invariants:** No React in `src/math/` or `src/core/`. Inline styles only.

---

### medium · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation of coefficients
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** In `INTERPRET_REGRESSION_PROMPT` (in `Prompts/index.js`), add a rule under the ESTIMATOR TYPE RULES section (after the existing Poisson/IRR rules) for log-linear, log-log, and semi-log functional forms: detect when `y` or any `x` variable has been log-transformed (the pipeline's `log` step should be reflected in the snapshot), and instruct the model to report the coefficient as a percentage change / elasticity accordingly. Use the `SHARED_CONTEXT` prompt-caching channel — do not add a new `fetch` call; route through `callClaude()` in `AIService.js`.
**Invariants:** All Anthropic calls must go through `AIService.js`. `SHARED_CONTEXT` must remain the cached block. No new `fetch` to the Anthropic API elsewhere.

---

### medium · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** In `FeatureTab.jsx`, detect the `case_when` (and similar multi-input panels like `if_else`, `mutate`) forms and persist their in-progress state to `sessionStorage` keyed by `<pid>:<stepId>:<panelName>`. Read back the draft on mount. This is intentionally `sessionStorage` (not IndexedDB) because it is a transient per-browser-session convenience — the user's draft should not survive a reload, only a within-session tab navigation. Clear the draft when the step is committed to the pipeline.
**Invariants:** Use `sessionStorage` here (not IndexedDB) per the "per-viewer conveniences" rule. Never persist raw data or pipeline steps to `localStorage`/`sessionStorage` — only UI draft state.

---

### medium · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/components/workspace/DatasetManager.jsx`
**Suggestion:** Extend `artifactOrder.js` with a `group` field on each artifact entry (string label, default `"default"`). In `ModelBufferBar.jsx`, render artifacts grouped by label with a collapsible header per group and a small rename affordance. Store group assignments in the existing `project_artifact_order` IndexedDB key (extend the JSON schema, no migration needed for missing field → falls back to `"default"`). Keep the comparison table filtered to the active group only; add an "All groups" toggle.
**Invariants:** Persistence in IndexedDB (`indexedDB.js`) only. Inline styles via `C` object.

---

### low · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`)
**Suggestion:** Audit the 9 `TOUR_STEPS` entries in `HelpSystem.jsx` against the current tab structure (7 tabs: Data/Clean/Explore/Model/Simulate/Calculate/Report per `WorkspaceBar.jsx`) and the current estimator list. Remove references to estimator names or tab labels that have changed (e.g. WLS as a separate estimator, old OLS-only framing). Per CLAUDE.md convention: never write a count ("14 estimators") — name the things instead. The `HintBox` title must be the MODULE name, not "How to X".
**Invariants:** none beyond style conventions. Never write a count in help copy.

---

## Features

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** The static SVG geo-plot (`SpatialGeoPlot.jsx`) already has grid-layer support per the code structure; check whether the `GridSection` output (H3 or rect grid cells as WKT) can be passed in as an additional layer using the existing `GeoLayerConfig`/`mkGeoLayer` pattern. If the grid WKT is already in the active dataset after a `sp_grid_assign` step, it should be selectable as a geometry column in the Plot tab's layer picker without new engine code.
**Invariants:** `src/math/SpatialEngine.js` must remain React-free. Styling inline via `C`.

---

### Calculate · 2026-06-19
**Feedback:** Add limits (e.g. lim x→a f(x))
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" section to `CalculateTab.jsx` alongside the existing derivatives/integrals panels. Use the CAS library already imported (verify which symbolic engine is in use — `nerdamer`, `math.js`, or a custom implementation) to evaluate `limit(expr, x, a)`. Display the result with KaTeX rendering (matching the existing equation output pattern). One-sided limits (left/right) should be offered as a sub-option. Keep the math fully client-side — do not route limit evaluation through `AIService.js`.
**Invariants:** No Anthropic API calls for pure math computation. No React imports in `src/math/` or `src/core/`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** In `CalculateTab.jsx`, allow the user to define a constant expression (e.g. `k = 2.5`) and render it as a horizontal reference line on the function plot canvas. The constant can be entered alongside the function expressions list; the plot renderer (already handling multi-equation) simply adds a `y = const` series. This mirrors PlotBuilder's existing `hline`/`vline` geom functionality — consider extracting a shared `ReferenceLineLayer` utility if the pattern is needed in both places.
**Invariants:** Pure client-side math rendering. Inline styles via `C`.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axes (3D plots)
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** Observable Plot 0.6 (already on CDN) does not support 3D natively. The lowest-friction approach is to add a `scatter3d` geom type that loads Plotly.js (`cdnjs.cloudflare.com/ajax/libs/plotly.js/`) lazily (same CDN-singleton pattern as Leaflet/proj4) and renders a `Scatter3d` trace. Keep it gated behind a clear "3D (beta)" label and only offer it when all three of x, y, and z/color mappings are set. CSP `connect-src` in `vercel.json` must include `cdn.cloudflare.com` if the Plotly bundle fetches its own sub-resources at runtime — check this before deploying (same connect-src class of bug documented in CLAUDE.md).
**Invariants:** External scripts must be loaded only from `cdnjs.cloudflare.com` or `cdn.jsdelivr.net/npm/`. Any new CDN dependency needs both `script-src` AND `connect-src` entries in `vercel.json`.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so users can type and find variables faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** In `VariableSelector.jsx`, add a text input above the variable chip list that filters `availableVars` by substring match (case-insensitive). The filtered list should update on every keystroke via `useMemo`. Keep the full list visible when the input is empty. This is a purely presentational change — no data layer or pipeline involvement needed.
**Invariants:** Inline styles via `C` object. No external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set on a point/line/bar layer, the current legend shows color swatches with raw column values. Extend the Labels panel to include a "Legend title" input (passed to Observable Plot's `color.label` option) and a "Show value labels" toggle (for bar/point layers, emit a `text` mark with the color-group name). This matches ggplot's `labs(color = "Group")` and `geom_text` conventions, which are already emitted in the R exporter.
**Invariants:** Inline styles via `C`. Observable Plot CDN already loaded — do not add a second copy.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** In `History.jsx`, each step already has a `type` field. Add an `onClick` handler on the step row that dispatches a `NAVIGATE_TO_TAB` action (or calls a `setActiveTab` callback passed from `WranglingModule.jsx`) to jump the user to the correct tab (e.g. `filter` → Clean, `log`/`std` → Features, `group_summarize` → Reshape, `join` → Merge). Map step type → tab name in a small lookup table in `History.jsx`. This is view-only navigation — no pipeline mutation.
**Invariants:** Navigation state via React callbacks/context only. Inline styles via `C`.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step with a static ISO 3166-1 crosswalk table (bundled as a small JS object — ~250 entries, ~6 KB, no network call). The step accepts `col` (source column), `fromType` (name|iso2|iso3|numeric), and `toType` (any of the four), and applies a lookup via `runner.js`'s `applyStep`. Register in `registry.js` and wire a UI panel in `FeatureTab.jsx`'s Formatting tab. Add R (`countrycode::countrycode`), Python (`pycountry`), and Stata (`kountry`) translators in `stepTranslators.js`.
**Invariants:** The static lookup table must live in `src/math/` or `src/pipeline/` as pure JS (no React). New step types must be registered in BOTH `runner.js` AND `registry.js`. Translator must be added so `pipelineReliabilityValidation.mjs` T5 keeps passing.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models — using arguments of R/Python as commands in the UI, for all estimators
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** The current binned scatter is generated with default bins. Expose a collapsible "Scatter options" panel inside `ModelPlots.jsx` with inputs for: number of bins (default 20), x-axis range (min/max), and whether to overlay the fitted line. Pass these as `binnedOpts` props into the plot render function. Emit the equivalent `binscatter` (R `binsreg`), Python `binscatter`, and Stata `binscatter` arguments in the replication exporter. Scope to OLS/FE estimators first — RDD and DiD have their own dedicated plot functions.
**Invariants:** `ModelPlots.jsx` is a UI component — any binned-scatter math must stay in `src/math/LinearEngine.js` or a new pure-JS helper. Inline styles via `C`.

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** In the ModelingTab Plot Builder section, add a "Max points" slider (e.g. 100–10 000, default = actual row count) that samples the result-augmented rows before passing them to `PlotBuilder`. Use a deterministic sample (e.g. `rows.filter((_, i) => i % step === 0)`) rather than random to keep the plot reproducible. Note clearly in the UI that this is a display sample — it does not affect estimation. Mirror the approach used in `duckdbResiduals.sampleResiduals` (5k-row sample via `USING SAMPLE`) for DuckDB-backed datasets.
**Invariants:** Sampling is display-only — never pass a reduced row set to any estimation function. No mutation of `rawData`.

---

## Performance

### critical · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows — DuckDB-WASM Fase X4 routed but not browser-validated for all formats
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** DuckDB integration for CSV/TSV >10MB and `.parquet` is complete; `.dta` DuckDB routing landed 2026-06-10. Remaining formats (`.rds`, `.RData`, `.xlsx`, `.shp`) still parse fully in the JS main thread and can cause tab freezes on large files. The fix pattern is already established in `DataStudio.jsx`'s DTA branch: parse once → register Arrow table → reduce to 500-row preview → persist OPFS Parquet cache. Apply the same route to `.xlsx` files above a threshold (e.g. 5 MB), since SheetJS is synchronous and blocks the main thread. `.rds`/`.RData` and `.shp` are more complex (custom parsers) — consider an inline Worker approach or a size cap with a user warning for those.
**Invariants:** Data never leaves the browser (privacy-first). Must use OPFS Parquet cache via `indexedDB.js` path, not localStorage. `connect-src` in `vercel.json` must already cover all CDN origins used by DuckDB at runtime — double-check after any DuckDB version bump.

---

## Needs confirmation (likely fixed — browser validation pending)

### critical · Math · 2026-07-22 / 2026-07-28
**Feedback:** P-values wrong for near-zero t-stats and for insignificant OLS estimates (multiple duplicate reports)
**File:** `src/math/LinearEngine.js` (`tCDF`, `betainc`, `betacf`)
**Suggestion:** Fix committed in `216b1ecb` (Lentz's continued-fraction `betainc`). No code change needed — confirm with Franco in-browser that a regression with a t-stat near 0 (e.g. 0.49) no longer shows `p < 0.001`. If the bug persists after confirmation, the calcEngine.js `_incompleteBeta` fix from 2026-07-30 (CoefficientTestPanel path) may not have been backported to the main OLS display path in `LinearEngine.js`.
**Invariants:** `src/math/LinearEngine.js` must be React-free. tCDF/fCDF must use the Lentz continued-fraction form, not a Riemann sum.

---

### high · Math · 2026-07-09
**Feedback:** FE must accept any categorical variable (country, state, sector); FE with interactions not supported
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** N-way FE shipped in `045f5cda`. Verify in-browser that: (1) a non-time, non-entity categorical column (e.g. `country`) can be added as an FE dimension in the FE column picker and produces the same coefficients as `fixest::feols(y ~ x | country)`, and (2) a factor interaction term (e.g. `country × sector`) can be added without crashing. The harness `panelNwayFeRValidation.R` covers the math; the pending check is the UI affordance in `ModelConfiguration.jsx`.
**Invariants:** SE type must be passed explicitly to `runFEMulti`. Math logic must stay in `src/math/PanelEngine.js`, not in UI files.

---

## Post-launch fix-later items (not blocking, prioritised for next sprint)

| # | Item | File | Notes |
|---|------|------|-------|
| 1 | Malformed CSV row aborts import | `src/DataStudio.jsx`, `src/services/data/parsers/` | Offer skip-row / repair preview UX |
| 2 | R `%in% c(...)` filter syntax in Clean | `src/components/wrangling/CleanTab.jsx` | Expression-language compatibility |
| 3 | RDD `summary(rdrobust)`-style table | `src/components/modeling/results/`, `src/math/CausalEngine.js` | Presentation enhancement |
| 4 | Spatial filters for plots | `src/components/tabs/spatial/plot/` | Route to V4 plots |
| 5 | Editable LaTeX variable labels in tables | `src/components/modeling/ResultDisplay.jsx` (check path) | Post-launch metadata feature |

---

## Suggested action order

1. **Stata workbench broken** (`src/services/export/stataScript.js`, `CodeEditor.jsx`) — high-severity, regularly reported, directly affects replication reproducibility for the target LMU user base.
2. **Simulate replication script** (`SimulateTab.jsx` + script generators) — high-severity export gap; users running Monte Carlo cannot reproduce their analysis in R/Python/Stata at all.
3. **P-value fix browser confirmation** (`LinearEngine.js` `tCDF`) — critical math correctness; `216b1ecb` likely fixed this but has not been confirmed in-browser; block no other work on it but confirm immediately.
4. **Time series group references broken** (`ExplorerModule.jsx`) — medium bug, breaks a visible plot feature; likely a reactive state ordering issue, low implementation cost.
5. **LaTeX table size** (`latexTable.js`) — medium, purely cosmetic but repeatedly reported; `\resizebox` wrap is a 5-line change.
6. **Save draft in case_when / transformation panels** (`FeatureTab.jsx`) — medium UX; prevents wasted user work and is straightforwardly `sessionStorage`.
7. **Multiple hypothesis tests save + replicate** (`CoefficientTestPanel.jsx`, `ModelHypothesis.js`) — medium; needed for full replication workflow.
8. **Time series plot labels** (`ExplorerModule.jsx`) — medium UI gap, low implementation cost.
9. **Elasticity interpretation** (`Prompts/index.js`) — medium AI quality; prompt-only change with no code risk.
10. **Country codes step** (`FeatureTab.jsx`, `runner.js`, `registry.js`) — medium feature, high practical value for international panel data users; requires REGISTRY sync so run `pipelineReliabilityValidation.mjs` after.
