# Feedback Analysis Report — 2026-06-17

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.
> Supersedes 2026-06-16 report.

## Summary

- **6 open bugs** (4 code-complete, browser validation pending; 1 code-complete, benchmark pending; 1 fix-later) · **5 open features** (all explicitly deferred post-launch) · **0 open UX** · **0 open performance** (tracked under Bugs below as Fase X4 item) · **0 open other**

Cross-reference methodology: ClaudeFB.md items without `~~strikethrough~~` and without ✓ were collected, then reconciled against BugTriage.md status column and the 2026-06-14 X5 reconciliation table. Items marked "verified fixed" in X5 are treated as closed (ClaudeFB strikethrough not yet applied — doc-sync lag only). Items marked "fix-now" with "browser validation pending" and items marked "fix-later" are treated as genuinely open.

---

## Bugs

### [high] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code landed in commit `35f4b5a6` — OLS/WLS diagnostic plots now render from the frozen `result.spec` rather than live sidebar state. The outstanding action is browser validation by Franco: load an OLS model, run estimation, then verify all three plots (Y~Ŷ, Residuals vs Fitted, Q-Q) render and remain frozen when sidebar variables are changed. If they still fail, check that `result.spec` is correctly populated before `setResult` is called in `ModelingTab.jsx`.
**Invariants:** `src/math/` must remain React-free. Residual calculations must happen inside engine files, not in the rendering component.

---

### [high] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** Fix landed in `35f4b5a6` (OLS/WLS display frozen to `result.spec`). Browser validation is the remaining gate: add a variable after estimation and confirm plots do not re-render; pin a model, switch to another, restore the pin, and confirm the original plots reappear. If the pinned-restore path still shows stale state, verify `modelBuffer.loadModel()` correctly sets both `result` and `result.spec` in `ModelingTab` state.
**Invariants:** SE type is always passed explicitly to engines — never hardcode inside engine functions. This constraint must not be accidentally dropped during any future refactor of the frozen-result path.

---

### [high] · Explore · 2026-05-11
**Feedback:** The graph does not fit the screen
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** Code-complete in `35f4b5a6`. Browser-validate that the PlotBuilder canvas fills its parent container in both the Explore tab (free mode via `ExplorerModule.jsx`) and the ModelingTab collapsible panel. If the graph is still clipped, check that the `ResizeObserver` in `PlotBuilder.jsx` fires on initial mount and that the parent container has an explicit height (not just `height: auto`).
**Invariants:** Inline styles only via the `C` object from `shared.jsx`. No external UI component libraries.

---

### [medium] · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in `35f4b5a6` — native vertical resize added, PNG captures user-selected dimensions. Browser-validate by dragging the resize handle and triggering PNG export; confirm the exported image matches the visible canvas size. If the PNG is still cropped or blurry, check that the Observable Plot `svg` element has explicit `width`/`height` attributes set from the post-resize container dimensions before export.
**Invariants:** No external UI component libraries. Styling via the `C` object and inline styles only.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Heatmap restored as a Leaflet layer in commit `5e105529` with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5 k-point cap. Remaining action: browser-validate by loading a point dataset, switching to the Map tab, enabling the Heatmap layer type, and confirming it renders and responds to opacity/palette controls. If the layer is blank, check that the Leaflet heatmap plugin CDN singleton is loaded before the layer is constructed.
**Invariants:** Leaflet is loaded as a CDN singleton (`src/components/tabs/spatial/shared/leaflet.js`). Never bundle Leaflet directly — this would break the singleton pattern and duplicate tile listeners.

---

### [medium] · Data · 2026-06-14 (fix-later)
**Feedback:** Malformed CSV row can abort import instead of surfacing recovery options (CSV Error on Line: 1583)
**File:** `src/DataStudio.jsx` (parseFile, CSV branch ~lines 390–430), `src/services/data/parsers/`
**Suggestion:** When implementing, wrap the CSV parse loop in a try-catch that collects failing row indices rather than throwing immediately. After the pass, present a recovery modal showing: (a) total rows successfully parsed, (b) list of failing line numbers with a raw snippet, and (c) options "Skip bad rows / Abort / Repair manually". Preserve the full original line text in the error payload so the user can inspect the bad data.
**Invariants:** Pipeline is non-destructive — steps replay on `rawData`. Partially-parsed rows must never reach `rawData` silently. Use IndexedDB for any persistence, not localStorage.

---

### [high] · Performance · Data · 2026-05-08 (Fase X4 — benchmark pending)
**Feedback:** Big datasets generate crashes (21 MB) over 900 k rows
**File:** `src/services/data/duckdb.js`, `src/DataStudio.jsx`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Fase X4 is code-complete for `.dta` (Arrow-backed DuckDB table, 500-row UI preview, OPFS Parquet cache). Browser benchmark is the remaining gate. Franco should load the 900 k-row fixture, measure tab-transition time before and after, and confirm no crash. If performance is still inadequate for large CSV, the next step is auto-converting large CSV to Parquet on first load inside DuckDB (CLAUDE.md DuckDB rule 2).
**Invariants:** Data never leaves the browser. Display is limited to 200–500 rows (virtualized table) but pipeline steps always run on the full DuckDB table, never on the truncated preview. Never pull the full Arrow result into JS objects.

---

## Features

### · Clean · 2026-05-19 (fix-later)
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx`
**Suggestion:** Add a "membership" operator to `FilterBuilder`'s operator dropdown, rendered as a textarea for comma-separated values. In `runner.js`, implement the step as `Array.includes()` against the parsed value list. Validate the step in `registry.js` and `stepValidator.js` to keep STEP_REGISTRY in sync. Optionally parse R-style `c("a", "b")` input syntax and normalise to a JSON array in the step spec.
**Invariants:** Pipeline is non-destructive — the new filter step must produce a new row set from `rawData`, never mutate in place. Registry must stay in sync with runner.js (enforced by `pipelineReliabilityValidation.mjs` T5).

---

### · Model · 2026-05-21 (fix-later)
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js`, `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** `CausalEngine.js` already returns the full RDD result object including bandwidth, kernel, polynomial order, local estimates, and SE. Add a `RDDSummaryTable` component alongside the existing `RDDPlot` in `ModelPlots.jsx` that renders the canonical rdrobust-style header block: estimator type, bandwidth (h), kernel, N (left/right), coefficient, SE, z, p-value, CI. Wire it into the ModelingTab result display section below the plot. No engine changes are needed.
**Invariants:** `src/math/` and `src/core/` must have zero React imports. The new component must live entirely in `src/components/`.

---

### · Model · 2026-05-25 (fix-later)
**Feedback:** Add an option to estimate without intercept
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`, `src/math/CausalEngine.js`, `src/math/NonLinearEngine.js`
**Suggestion:** Add a boolean `noIntercept` field to the model spec (checkbox in `ModelConfiguration.jsx`). Each engine must conditionally skip prepending the column of ones to the design matrix. Replication script generators (`rScript.js`, `stataScript.js`, `pythonScript.js`) must emit `-1` / `, cons(0)` / `fit_intercept=False` accordingly. SE and diagnostics must use the reduced degree-of-freedom count (`df = n - k`, not `n - k - 1`). This is an engine-wide contract change — validate each engine against R (6 dp coef, 4 dp SE) before merging.
**Invariants:** SE type must still be passed explicitly; never hardcoded inside engine functions. No React in `src/math/`. R validation to 6 dp on coefficients, 4 dp on SE is mandatory.

---

### · Spatial · 2026-05-30 (fix-later)
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a `filterExpr` text input in `GeoLayerConfig.jsx` per layer. Parse the expression using the same filter evaluator used in `CleanTab.jsx` and apply it before passing rows to the renderer in `SpatialGeoPlot.jsx`. Pre-filter the rows before computing the geo-bbox so the map auto-fits to the visible subset. This keeps filtering client-side and avoids any pipeline mutation.
**Invariants:** No pipeline mutation — spatial filtering is view-only and must not alter `rawData`. Inline styles via `C`, no CSS files.

---

### · Model · 2026-06-06 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/Persistence/indexedDB.js`, `src/ReportingModule.jsx`
**Suggestion:** Add an editable label map (per-model state, keyed by coefficient name) stored alongside pinned models in the IDB `model_buffer` store via `modelBuffer.js`. Render each coefficient row in `resultDisplay.jsx` with an inline `<input>` that populates the map on blur. When building the LaTeX Stargazer table in `ReportingModule.jsx`, substitute mapped labels before emission. The label map should survive session reload via the IDB-persisted model buffer.
**Invariants:** IndexedDB for persistence — not localStorage. The label map is metadata only; it must never alter `result.coef` or `result.se` arrays.

---

## Suggested action order

1. **Browser-validate commit `35f4b5a6`** — OLS residual/diagnostic plots frozen, frozen-result state (diagnostics must not mutate on sidebar edits), Plot Builder resize, and Explore graph sizing: all code-complete, Franco's browser confirmation is the only gate.
2. **Browser-validate Spatial heatmap (`5e105529`)** — Map tab → Heatmap layer → confirm KDE render, opacity/palette controls, and HTML export work end-to-end.
3. **Browser-benchmark large-dataset performance (Fase X4)** — Load 900 k-row fixture, confirm <3 s tab transitions and no crash; document timing for the performance log.
4. **Implement malformed CSV resilient-import** — Medium-severity UX gap; one bad row currently aborts the entire import with no recovery path.
5. **Implement estimate-without-intercept** — Medium severity, requires engine-wide change + full R validation pass across all engines.
6. **Implement `%in% c(...)` filter expression syntax** — Low priority but repeatedly requested; scope limited to `CleanTab.jsx` + `runner.js` + `registry.js`.
7. **Add RDD `summary(rdrobust)` table** — Low priority; no engine work needed, purely a display component.
8. **Add editable LaTeX variable labels** — Low priority; IDB-persisted label map + substitution in `ReportingModule.jsx`.
9. **Add spatial plot layer filter** — Lowest priority; routed to V4 post-launch track.
10. **Mark verified-fixed items in ClaudeFB.md with strikethrough** — Doc-sync cleanup: ~20 items are confirmed resolved in X5/BugTriage but still appear open in ClaudeFB because the strikethrough was never applied. Reduces noise for the next analysis run.
