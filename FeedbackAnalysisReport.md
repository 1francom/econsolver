# Feedback Analysis Report — 2026-06-25

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 5 open bugs · 6 open features · 0 open UX · 1 open performance · 1 open other

**Cross-reference note:** Many ClaudeFB.md entries that still lack ~~strikethrough~~ were confirmed
resolved in BugTriage or the 2026-06-14 X5 reconciliation table and are excluded here
(covers: .dta parsing, p-value bug, grid sizing, deleted-dataset stale vars, auto-reestimate,
equation plotting, color mapping, Monte Carlo single-variable, resampling, LaTeX crash, Poisson
interpretation, XLSX CDN, OECD 406, XSS payloads, Callaway-Santana, Anderson-Rubin, stats tests,
geometry-in-point-plots, show-steps for calculus, pinned-model persistence, endogenous derivatives,
studentized permutations, spatial auto-fit, EPSG/CRS controls, IV-Poisson interpretation).

Items marked **code-complete / browser validation pending** in BugTriage are included because they
remain un-struck in ClaudeFB.md and may contain unreported regressions.

---

## Bugs

### [high] · Model / App routing · 2026-06-17
**Feedback:** Pipeline only applies if dataset is also selected in Clean Module
**File:** `src/App.jsx` lines 2845–2848 (`handleComplete`), 2810–2811 (`tabDsId`/`tabOutput`), 2838 (`modelCleanedData`)
**Suggestion:** `handleComplete` keys the pipeline output under `tabDsId("clean")` = `activeDatasetIds["clean"] ?? activeDatasetId` (App.jsx:2846). `modelCleanedData` looks it up under `tabDsId("model")` = `activeDatasetIds["model"] ?? activeDatasetId` (App.jsx:2838). These resolve to different IDs whenever the user has a different per-tab dataset selection for "clean" vs "model". Fix: inside `handleComplete`, after calling `setOutputs`, also call `setActiveDatasetIds(prev => ({ ...prev, model: id, explore: id }))` so downstream tabs are synced to the same dataset. A more complete fix also replays the saved pipeline from IndexedDB via `loadPipeline(pid, dsId)` + `runPipeline` when `tabOutput("model")` is null on first load of the Model tab, so the model always sees the pipelined data without requiring a round-trip through Clean.
**Invariants:** Pipeline must replay on `rawData` — never mutate in place (runner.js). Use indexedDB.js for persistence, never localStorage.

---

### [high] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/ModelingTab.jsx` lines 2319–2325; `src/components/modeling/ResidualPlots.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` (OLS/WLS diagnostics now render from the frozen `result.spec`). Browser validation is the remaining action. Confirm by: (1) running a fresh OLS with 2+ regressors, (2) verifying all three plots render — `YFittedPlot` at ModelingTab:2319, `ResidualVsFitted` at :2323, `QQPlot` at :2325. If plots are absent, verify that `result.resid` and `result.Yhat` are populated by `LinearEngine.runOLS` and that `r` in the plot-construction block is the frozen snapshot, not live component state.
**Invariants:** None beyond standard React state hygiene.

---

### [high] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.
**File:** `src/components/ModelingTab.jsx`; `src/components/modeling/ResidualPlots.jsx`; `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6` (frozen `result.spec` pattern). Browser-validate: (1) run a model, observe a coefficient plot, (2) add a variable without re-running, (3) confirm plots do not change, (4) pin a second model, switch to it, confirm the first model's plots restore exactly. Root cause if it regresses: a stale `result` ref in the plot-construction callback — ensure all diagnostic/coefficient-plot components read from `result.spec`, not live state.
**Invariants:** None.

---

### [medium] · Model / PlotBuilder · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx` line 1626; `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete in commit `35f4b5a6`; the plot viewport now uses `resize: "vertical"` with `minHeight: 220` and `maxHeight: "70vh"` (PlotBuilder.jsx:1626). Browser-validate: (1) drag the lower-right corner, (2) confirm the Observable Plot canvas re-renders at the new dimensions via the `ResizeObserver` at PlotBuilder:1171, (3) export PNG and verify it captures the user-selected dimensions rather than the 400 px default. If the ResizeObserver fires but `<PlotCanvas>` does not re-render, check that `width`/`height` props are wired to measured container dimensions rather than constants.
**Invariants:** Inline styles via the `C` object only. No CSS files.

---

### [medium] · Spatial / Map · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` lines 224 and 801; `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete in commit `5e105529` (Leaflet heatmap layer with KDE cells, palette/opacity controls, legend, HTML export, 5k-point cap). Browser-validate: (1) load a point dataset with lat/lon columns, (2) add a Heatmap layer in the Map tab, (3) confirm gradient renders over the basemap with correct palette and opacity. If `buildHeatmapCells` at SpatialPlotTab:224 throws, check that `kde2d` from `src/math/SpatialEngine.js` returns `rows` with a `density` column that `buildColorScale` can consume.
**Invariants:** `src/math/SpatialEngine.js` must have zero React imports.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile branch, DuckDB dispatch); `src/services/data/duckdb.js`; `src/pipeline/duckdbRunner.js`
**Suggestion:** Fase X4 is code-complete for `.dta` files (Arrow-backed DuckDB table, 500-row UI preview, OPFS Parquet cache). Browser benchmark with a 900k-row CSV is still pending. Validate by: loading a 21 MB CSV, confirming the DuckDB badge appears in the wrangling header, and verifying tab transitions stay under 3 seconds. If CSV files still bypass DuckDB, extend the `>10 MB` size check in `DataStudio.parseFile` to cover CSV/TSV in addition to the existing `.dta` branch, converting to Parquet on first load. Grep for `getTable`/`getArrow` calls in duckdbRunner that lack a `LIMIT` clause — any `SELECT *` that materialises the full Arrow result will reproduce the crash.
**Invariants:** Full dataset always used for computations (CLAUDE.md rule 5); display truncated to 500 rows (rule 7); no server-side computation (privacy-first constraint).

---

## Other

### · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...
**File:** `src/DataStudio.jsx` (CSV parse path); `src/services/data/parsers/`
**Suggestion:** BugTriage status is fix-later (resilient-import pass after launch). When implementing: add a `try/catch` around individual row parsing in the CSV reader; collect malformed row indices and raw text; surface a recovery dialog offering "skip N rows and continue" or "abort". For datasets routed to DuckDB, `read_csv` already accepts `ignore_errors = true` — a one-line change. For the JS parse path, add a row-level try/catch that collects `{ lineNumber, rawText }` and surfaces them post-parse.
**Invariants:** Never silently drop rows without user acknowledgement.

---

## Features

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js` lines 577–579 (hardcoded varNames); `src/components/ModelingTab.jsx` (RDD results section ~3071–3088)
**Suggestion:** BugTriage status is fix-later (presentation enhancement). Add a collapsible `rdrobust`-style summary panel to the RDD results section of ModelingTab displaying: estimator, kernel, bandwidth (h), effective N left/right, polynomial order, LATE, SE, t-stat, p-value, and CI bounds. All values are already present in the `runSharpRDD` result object — this is pure UI. As a companion improvement, replace the hardcoded `"D (treatment)"` / `"running − c"` labels in `CausalEngine.js:577–579` with the actual `dCol`/`runCol` arguments so the table and LaTeX export show meaningful variable names.
**Invariants:** No changes to engine output shape. `src/math/CausalEngine.js` must have zero React imports — pass column names as plain string arguments.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept
**File:** `src/math/LinearEngine.js` line 128 (`k = xCols.length + 1`); `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage status is fix-later (engine-wide contract change). Add a `noIntercept: boolean` field to the estimation spec. In `LinearEngine.runOLS`, when `noIntercept = true`, omit the leading ones column in the design matrix and set `k = xCols.length`. Propagate to `runWLS`, and to the DuckDB suff-stats SQL path (the design-matrix CTE in `duckdbOLS.buildOLSSuffStats` must conditionally omit the constant column). Expose a checkbox in `ModelConfiguration.jsx`. Update R/Stata/Python script exports: emit `lm(y ~ x - 1)` / `reg y x, noconstant` / `sm.OLS(y, x)` without `add_constant`. Implement OLS+WLS first; defer Panel/IV/RDD.
**Invariants:** SE type must remain a caller-supplied argument — never hardcode inside the no-intercept branch. Zero-React rule applies to `src/math/`.

---

### Explore / Clean · 2026-05-19
**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option
**File:** `src/components/wrangling/CleanTab.jsx`; `src/pipeline/runner.js` (filter step); `src/math/calcEngine.js` (`evalExpression`)
**Suggestion:** BugTriage status is fix-later. Add a pre-processing rewrite in the `filter` step of `runner.js` (or at the top of `evalExpression` in `calcEngine.js`) that converts R-style `col %in% c(v1, v2, ...)` to a JS equivalent `[v1, v2].includes(col)` before evaluation. A single regex substitution covers the common case. No engine changes needed.
**Invariants:** The rewrite must not alter the `rawData` reference. Filter steps must replay deterministically on `rawData`.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** BugTriage routes this to V4. When implementing, add a per-layer filter row in `GeoLayerConfig` (reusing the filter-expression infrastructure from `runner.js`). Apply the filter to the layer's row set in `SpatialGeoPlot` before passing to `mkGeoLayer`. Display-only — must not add a pipeline step or touch `rawData`.
**Invariants:** No mutations to `rawData`. No React imports in `src/math/SpatialEngine.js`.

---

### Model / Report · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/math/CausalEngine.js` lines 577–579 and 844–848; `src/services/export/latexTable.js` line 126; `src/components/ReportingModule.jsx` lines 280 and 266
**Suggestion:** BugTriage status is fix-later. Two-part fix: (1) Replace hardcoded labels in `CausalEngine.js:577–579` with interpolated column names (`dCol`, `${runCol} − c`, `${dCol} × (${runCol} − c)`) so the engine emits meaningful names by default. (2) Thread the `varLabels` override (already implemented in `ModelComparison.jsx:331–358`) through to the single-model LaTeX export in `ReportingModule:280` by exposing an editable label-map in the Report tab. `buildStargazer` at `latexTable.js:126` already accepts `varLabels` — it just needs to be supplied from the Report tab UI.
**Invariants:** `src/math/CausalEngine.js` must have zero React imports. Label edits must be persisted in IndexedDB.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx` lines 1169 (`activeTool` state) and 1556 (tool-selector chips); `src/math/calcEngine.js`
**Suggestion:** No BugTriage entry yet — untriaged. Add a `"limit"` mode to the `activeTool` state (line 1169) and add it to the tool-picker at line 1556 alongside Solver/Derivative/Symbolic/Algebra/Integral. UI needs: function input, variable selector, approach value (`x → a` including `±∞`), and direction (two-sided / left / right). Implement `computeLimit(fn, a, direction, opts)` in `calcEngine.js` using Richardson extrapolation for numerical limits. Flag clearly when the limit does not converge or is ±∞. Wire KaTeX rendering for the result via `trust: false` (matching the existing pattern in CalculateTab).
**Invariants:** Math implementation must live in `src/math/calcEngine.js` with zero React imports. Never use raw HTML injection — KaTeX `trust: false` only.

---

## Suggested action order

1. **Fix pipeline/dataset routing bug** (`src/App.jsx:2845`) — only truly open unpatched bug; blocks the core Clean→Model workflow for anyone who doesn't explicitly select the dataset in the Clean tab. One-line sync of `activeDatasetIds["model"]` inside `handleComplete` resolves the immediate symptom.
2. **Browser-validate commit `35f4b5a6`** — covers three high/medium items in one session: OLS residual plots, frozen coefficient plots, and Plot Builder resize + PNG export. Ask Franco to confirm each and add ✓ to ClaudeFB.md.
3. **Browser-validate spatial heatmap (`5e105529`)** — separate session from (2); load a point dataset, add a Heatmap layer, confirm KDE gradient renders.
4. **Confirm Fase X4 performance on 900k-row CSV** — code is done for `.dta`; extend the `>10 MB` dispatch in `DataStudio.parseFile` to CSV/TSV if the benchmark shows CSV files still materialise in JS.
5. **RDD summary table + un-hardcode variable labels** (`CausalEngine.js:577–579`) — 4-line engine change + additive UI panel; high research value.
6. **`%in%` filter syntax rewrite** (`runner.js` filter step) — low effort, high daily-use impact for World Bank / OECD panel datasets.
7. **No-intercept estimation** — requires engine-wide spec change; implement OLS + WLS first.
8. **Editable LaTeX variable labels in single-model Report** — `buildStargazer` already accepts `varLabels`; thread from ReportingModule UI.
9. **Calculate: add limits** — new feature, untriaged; numerical path via `calcEngine.js` Richardson extrapolation is straightforward.
10. **Spatial filters for plots** — routed to V4; low pre-launch priority.
11. **CSV malformed-row recovery** — post-launch; DuckDB path is a one-liner (`ignore_errors = true`).
