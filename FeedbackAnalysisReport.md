# Feedback Analysis Report — 2026-09-02

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.
>
> **Note on methodology:** ClaudeFB.md is treated as the authoritative open/closed signal (any item not ~~strikethrough~~ and not ending with ✓ is open). BugTriage.md is cross-referenced to catch items that are resolved in code but not yet marked in ClaudeFB, and to distinguish "code-complete pending Franco browser validation" from genuinely unresolved work. Items that are code-complete but lack Franco's browser confirmation are flagged with ⚠️ **PENDING VALIDATION** rather than treated as open bugs.

## Summary

- **10 open bugs** (3 high · 6 medium · 1 low)
- **14 open features** (0 critical · 4 medium · 10 low/enhancement)
- **0 open UX/Design** (all prior UX items resolved or closed)
- **1 open performance** (partially mitigated by DuckDB; non-DuckDB formats remain)
- **1 open other** (malformed CSV resilience — fix-later tracked)
- **4 items code-complete, pending Franco browser validation** (not counted as bugs above)

---

## ⚠️ Code-Complete Items Pending Browser Validation

These are NOT counted as open bugs — implementation is done — but they remain unconfirmed and should be validated by Franco before being closed in ClaudeFB.

| Item | Module | Commit / Note |
|------|--------|---------------|
| Plot Builder resize + PNG export aesthetics | Model | `35f4b5a6` |
| Y~Ŷ · Residual-vs-Fitted · Q-Q plots for OLS | Model | `35f4b5a6` |
| Diagnostics panel frozen to `result.spec` (does not mutate on sidebar changes) | Model | `35f4b5a6` |
| Spatial heatmap layer restored | Spatial | `5e105529` |

---

## Bugs

---

### [high] · Calculate · 2026-07-17

**Feedback:** Stata workbench doesn't work completely

**File:** `src/components/modeling/CodeEditor.jsx` · `src/services/export/stataScript.js`

**Suggestion:** The Calculate tab's Stata replication panel routes through `CodeEditor.jsx`'s `buildScript()` and ultimately `stataScript.js`. Likely failure modes are: (a) a Stata keyword or command emitted by `stataScript.js` is syntactically wrong for the estimator/SE combination, (b) `CodeEditor.jsx`'s field whitelist is missing keys needed for certain estimators so the config object is incomplete when reaching the Stata translator, or (c) a `transpileStep` case in `stataScript.js` hits its permissive `default:` branch and emits a no-op. Reproduce by copy-pasting the Stata output from `CodeEditor` into a fresh Stata session and recording the first error line, then trace back to the emitter in `stataScript.js`. The 2026-07-18 fix pass added many SE-type branches; check that every estimator path also handles the case where `seType` is `"classical"` and `clusterVar` is undefined (null-dereference risk on the new mappers).

**Invariants:** All inline styles must use the `C` object — no changes to the UI needed here. No Anthropic API calls involved.

---

### [high] · Simulate · 2026-07-28

**Feedback:** Update the replication script, currently it replicates only the DGP

**File:** `src/components/tabs/SimulateTab.jsx` · `src/services/export/rScript.js` · `src/services/export/pythonScript.js` · `src/services/export/stataScript.js`

**Suggestion:** `SimulateTab.jsx` has a "Copy script" or export action that calls into the script generators. Currently only the DGP draw (random-number generation using `dgpDraw.js`/`dgpScript.js`) is serialised. The fix requires identifying what downstream analyses the user has run on the simulated data (resampling, bootstrap, sample tests, Monte Carlo estimation) and threading those into the script generators. The cleanest approach: capture the Simulate tab's current analysis state (which tests ran, with what parameters) and pass it to `generateUnifiedScript` via `buildSessionSnapshot` — the same mechanism used for pipeline steps. `dgpScript.js` already has R/Python/Stata expression emitters for the DGP itself; the resampling and inference emitters in `SampleTestPanel.jsx` / `StatWorkspace.jsx` should be extended to produce a script fragment the same way. Check that the two-sample group-split mode added in the 2026-08-07 fix emits `t.test(y ~ treat, data = df)` (dataset reference form) rather than inlining literal vectors.

**Invariants:** No React imports in `src/math/` or `src/core/`. All Anthropic calls through `AIService.js`.

---

### [medium] · Explore · 2026-07-28

**Feedback:** References of the group in time series doesn't work

**File:** `src/ExplorerModule.jsx` · `src/components/PlotBuilder.jsx`

**Suggestion:** The time-series plot in `ExplorerModule.jsx` renders group/color groupings using Observable Plot's `group` transform or a `color` channel. If the group reference is a column that changes names after a pipeline step (e.g. after a `rename`), the color channel will resolve to `undefined` silently — Observable Plot renders a single grey series instead of raising an error. Reproduce with a dataset that has a string grouping column, rename it in the pipeline, then try to plot with the old name. Also check whether the `filteredRows` slice passed to `PlotBuilder` preserves the grouping column after a `group_summarize` step: if the summary only emits aggregate columns, the group key must be listed in the `by` fields. If the bug is in `PlotBuilder.jsx` itself, look at whether the `aes.color` accessor is built from the raw column name (safe) or from a derived index (fragile after updates).

**Invariants:** None specific beyond inline-styles.

---

### [medium] · General (Report) · 2026-07-05

**Feedback:** Adjust the size of LaTeX tables when the number of columns increases

**File:** `src/services/export/latexTable.js` · `src/ReportingModule.jsx`

**Suggestion:** When a model comparison table or Stargazer-style output has many regressors, the generated `tabular` environment overflows the page width. The fix is to emit `\resizebox{\textwidth}{!}{ ... }` around the `tabular` body, or switch to `tabularx` with `X` columns and a `\textwidth` width argument when the column count exceeds a threshold (e.g. > 5 columns). Alternatively, add `\tiny` or `\small` font scaling. The column count is known at generation time from the number of model specs in `buildStargazer()` inside `latexTable.js` — add a conditional wrapper there rather than hard-coding a size. `ReportingModule.jsx` just calls through to `latexTable.js`, so the fix is entirely in the export layer.

**Invariants:** None — `latexTable.js` is a pure export utility with no React or AI dependencies.

---

### [medium] · Model · 2026-07-06

**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R

**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`

**Suggestion:** `CoefficientTestPanel.jsx` currently supports testing a single hypothesis at a time. The request is to allow a list of hypotheses (e.g. "β₁ = 0, β₂ = 1, β₁ + β₂ = 0") and export all of them together as a `car::linearHypothesis` call with a character vector of constraints. `ModelHypothesis.js` already has `generateJointHypothesisScript()` (added 2026-07-30) which emits `car::linearHypothesis(fit, c(...))`. The extension needed is: (a) a "Add row" button in `CoefficientTestPanel.jsx` to grow a list of constraints, (b) passing the full list to `waldTest()`/`generateJointHypothesisScript()`, and (c) persisting the list in the model spec (via the `specExtras` mechanism already used for `noIntercept`) so the Code Editor picks it up. Note the existing joint-test panel already exports a multi-constraint R call — this item may be asking for the UI affordance to ADD multiple separate single-hypothesis rows, not just one joint matrix test.

**Invariants:** `ModelHypothesis.js` is in `src/math/` — must remain zero React imports. All UI changes go in `CoefficientTestPanel.jsx`.

---

### [medium] · Explore · 2026-07-09

**Feedback:** Add labels for the Time series plot, title, axis, etc.

**File:** `src/components/PlotBuilder.jsx` · `src/ExplorerModule.jsx`

**Suggestion:** The Plot Builder's Labels panel already has a Title field and X/Y label fields for most geom types (see the Labels section in `PlotBuilder.jsx`). The issue is that the time-series plot may be rendered by a separate code path in `ExplorerModule.jsx` (the ACF/PACF / time-series specific view) that bypasses `PlotBuilder`'s labels controls entirely. If so, the fix is either to route the time-series explorer through `PlotBuilder` (with a `line` geom + x-axis set to the time column), or to add standalone title/axis-label inputs to the time-series section in `ExplorerModule.jsx`. The latter is simpler and avoids a larger refactor. Confirm which path is used by checking how `ExplorerModule.jsx` renders the Time Series tab.

**Invariants:** None.

---

### [medium] · Model · 2026-07-09

**Feedback:** Add direct elasticity interpretation

**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)

**Suggestion:** The AI narrative in `interpretRegression()` already has estimator-specific rules (e.g. Poisson IRR, logit AME). Elasticity interpretation applies to OLS/WLS/FE models where both Y and X are in log form (log-log elasticity = β directly) or where Y is log and X is linear (semi-elasticity = 100·β % change per unit X). The fix requires detecting from the column names (or from pipeline step metadata on whether a `log` step was applied) whether variables are in log form, and adding a rule to `INTERPRET_REGRESSION_PROMPT` that surfaces the elasticity reading when the condition is met. A pragmatic first step: add a rule that checks if column names contain "log", "ln", or "l_" prefix — this covers most real-world cases. The prompt already carries pipeline metadata via `buildSessionSnapshot`, so log-transform steps are visible to the AI.

**Invariants:** All Anthropic calls must go through `AIService.js`. No changes to `src/math/` or `src/core/`.

---

### [medium] · General · 2026-07-17

**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria

**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`

**Suggestion:** `artifactOrder.js` manages the global artifact order via `getArtifactOrder` / `saveArtifactOrder` with a flat list. The grouping extension needs: (a) a `group` field on each artifact entry in IndexedDB (nullable string), (b) a group picker/creator UI in `ModelBufferBar.jsx` (a small "Add to group" dropdown or label chip), and (c) filtering/collapsing by group in the comparison view. Since `artifactOrder.js` is a pure persistence utility with no React, keep all state management in `ModelBufferBar.jsx` and write group assignments back through the existing `saveArtifactOrder` interface. Default group = `null` (no group) for backward compatibility with existing pinned models.

**Invariants:** Persistence must use IndexedDB (`indexedDB.js`), not localStorage. All styles via the `C` object.

---

### [medium] · Clean · 2026-07-17

**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases

**File:** `src/components/wrangling/FeatureTab.jsx`

**Suggestion:** `FeatureTab.jsx` manages its form state locally in React (`useState`). When the user navigates to another tab and returns, the component re-mounts and that state is lost. The fix is to persist draft form state to `sessionStorage` (keyed by `pid` + form type, e.g. `"pid_casewhen_draft"`), restoring it on mount and clearing it when a step is committed or the user explicitly cancels. `sessionStorage` (not IndexedDB) is appropriate here because this is ephemeral per-session UI state, not pipeline data. The `lsGet`/`lsSave` in `wrangling/utils.js` are noted as deprecated for pipeline storage — but `sessionStorage` for temporary drafts is a separate concern and is fine here. Scope to the feature forms that have the most user-typing: `case_when`, `if_else`, `mutate`, and `str_splice`.

**Invariants:** Pipeline state stays in IndexedDB. `sessionStorage` for draft UI state is acceptable here — it is not pipeline or data storage.

---

### [low] · General · 2026-07-17

**Feedback:** Guide tour must be updated

**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array)

**Suggestion:** `HelpSystem.jsx` contains a `TOUR_STEPS` registry of 9 steps that narrate the interface. After recent UI changes (pipeline hide toggle, N-way FE, Sun-Abraham/Callaway-Sant'Anna estimators, new Clean subsections, etc.) several step descriptions are stale. Audit each step against the current UI: step target selectors (data-tour-id attributes) should still exist, and text should reflect the current module names/features. The CLAUDE.md instruction "Never write a count into help copy" applies — replace any count-based phrasing (e.g. "14 estimators") with naming the categories instead. Also update the `HintBox` copy in each module simultaneously (8 panels: Data, Clean, Explore, Model, Report, Calculate, Simulate, Spatial).

**Invariants:** None — pure UI text changes.

---

## Features

---

### Explore · 2026-05-19

**Feedback:** Add filter(regionname %in% c("Spain", "Basque Country")) for filter option (R-style `%in%` membership syntax)

**File:** `src/components/wrangling/CleanTab.jsx` · `src/pipeline/predicate.js`

**Suggestion:** The canonical filter predicate language in `predicate.js` already has `in` / `nin` operators (mapped from `%in%` / `!%in%`). The gap is the `FilterBuilder` UI in `CleanTab.jsx` — it accepts a structured builder form but has no text-mode where users can type R-style syntax. The minimal fix: in the `in` / `nin` operator rows of `FilterBuilder`, add a textarea input that accepts a comma-separated list (optionally surrounded by `c(...)` which is stripped). `predicateToSQL` already handles `in` via `col IN (...)`, so no engine change is needed. **Invariant note:** The canonical predicate language is the single owner of filter semantics — do not add a parallel parser. Strip `c(...)` wrapper before handing the value list to the existing `in` operator path.

**Invariants:** `predicate.js` is the single owner of filter operators — never re-implement filter logic elsewhere.

---

### Model · 2026-05-21

**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — Call: rdrobust Sharp RD estimates using local polynomial regression

**File:** `src/components/ModelingTab.jsx` · `src/math/CausalEngine.js`

**Suggestion:** `runSharpRDD` in `CausalEngine.js` already returns LATE, SE, CI, bandwidth, and kernel — all the data `summary(rdrobust)` displays. The gap is purely in rendering: add a collapsible "RDD Summary Table" section to the RDD result display in `ModelingTab.jsx` (analogous to the existing BaconPanel for TWFE) that formats the output in `rdrobust`'s style: effective N, order, bandwidth, rho, conventional/robust CI rows. No new computation required.

**Invariants:** `CausalEngine.js` is in `src/math/` — any new formatting logic belongs in the UI component, not the engine.

---

### Spatial · 2026-05-30

**Feedback:** Allow spatial filters for plots

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` · `src/components/tabs/spatial/map/SpatialPlotTab.jsx`

**Suggestion:** Add a geometry-based row-filter control to the spatial Plot and Map tabs. The simplest approach: a bounding-box filter (min/max lat-lon inputs or a "draw bbox on map" interaction) that pre-filters rows before they are passed to the layer renderers. More powerful: a "filter to selected geometries" toggle, where clicking a polygon on the map filters the data to rows whose geometry intersects it. Start with the bbox approach since `SpatialEngine.js` already has haversine/spatial-join functions that can express the filter. Route the result through a pipeline `filter` step (using `evalPredicate`) so the filter is non-destructive and undoable.

**Invariants:** Non-destructive pipeline — spatial filter must replay on `rawData` or be a view-layer filter only, never mutate in place.

---

### Model · 2026-06-06

**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names

**File:** `src/services/export/latexTable.js` · `src/ReportingModule.jsx`

**Suggestion:** `buildStargazer()` in `latexTable.js` sources coefficient names from `result.varNames`. For RDD these are hardcoded strings in `CausalEngine.js` (`"D (treatment)"` etc.) rather than the actual column names. Two complementary fixes: (1) have `runSharpRDD` set `varNames` from the actual `treatCol` and `runningCol` names passed in; (2) add a small name-mapping editor in `ReportingModule.jsx` (a two-column table: auto-name → display name) whose output is passed to `buildStargazer`. The second gives the user control over ALL estimators, not just RDD.

**Invariants:** `CausalEngine.js` is in `src/math/` — no React imports there. The name-mapping state lives in `ReportingModule.jsx`.

---

### Calculate · 2026-06-19

**Feedback:** Add limits (e.g. lim_{x→a} f(x))

**File:** `src/components/tabs/CalculateTab.jsx`

**Suggestion:** The Calculate tab uses a CAS (Computer Algebra System) for derivatives, integrals, and optimisation. If the CAS library loaded (e.g. via CDN in `CalculateTab.jsx`) supports symbolic limits, add a "Limits" entry to the workbench. Typical API: `nerdamer.limit(expr, variable, point, direction)` or similar. If the current CAS lacks limit support, this requires either swapping to a more capable library or adding a numeric fallback (Richardson extrapolation). Before implementing, confirm which CAS is in use by reading the CDN `<script>` tag in `CalculateTab.jsx`.

**Invariants:** All styles via the `C` object. External libraries must be loaded from the CSP-allowed CDN allowlist (cdnjs.cloudflare.com, cdn.jsdelivr.net).

---

### Calculate · 2026-07-04

**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)

**File:** `src/components/tabs/CalculateTab.jsx`

**Suggestion:** The Calculate tab's equation plotter already handles `f(x) = ...` curves. Add support for constants by detecting when a defined expression has no variable dependence (i.e. evaluates to the same value for all x) and plotting it as a horizontal line (`y = k`). In the UI this can be implicit: entering `k = 5` and toggling it "on" would draw `y = 5` as a dashed line. Internally, evaluate the expression at two distinct x-values — if the results are equal, treat as constant and emit a horizontal `hline` at that value using the existing Observable Plot `ruleY` primitive (already used for `hline` geoms in `PlotBuilder.jsx`).

**Invariants:** None beyond inline styles.

---

### General · 2026-07-05

**Feedback:** Add plots with 3 axes (3D plots)

**File:** `src/components/PlotBuilder.jsx` · `src/ExplorerModule.jsx`

**Suggestion:** Observable Plot (the 2D charting library in use) does not support 3D rendering natively. Adding true 3D plots requires either a separate library (e.g. Plotly.js or three.js, both on the CDN allowlist via cdnjs) or a 2D approximation (contour plot + colour as the third axis, which Observable Plot does support via `contourf`). The contour/heatmap approach covers the most common econometric use case (e.g. loss surface over two parameters) without a new heavy dependency. If the user is asking for a 3D scatter, Plotly.js is the practical choice. This is a significant scope item — recommend a design session with Franco to pick the approach before implementing.

**Invariants:** External scripts must be loaded from the CSP-approved CDN list. No UI component library imports. All styles via the `C` object.

---

### Model · 2026-07-09

**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster

**File:** `src/components/modeling/VariableSelector.jsx`

**Suggestion:** `VariableSelector.jsx` renders Y, X, and W (control) column pickers. When the dataset has many columns (50+), the list is unscrolled. Add a text-input filter at the top of each picker that filters the displayed column list by substring match (case-insensitive). State lives in a local `useState` within `VariableSelector.jsx`, reset to empty string whenever the dataset changes. This is a pure UI change — no engine or pipeline impact.

**Invariants:** Inline styles only via `C`. No external UI libraries.

---

### Explore · 2026-07-11

**Feedback:** Add more labels in tables or boxes when plotting by color

**File:** `src/components/PlotBuilder.jsx`

**Suggestion:** When `aes.color` is set in Plot Builder and the geom is `point` or `line`, Observable Plot renders a legend automatically. If the request is for data-label overlays (text annotations per series), add a `showLabels` toggle to the Labels panel in `PlotBuilder.jsx` that, when on, emits a `Plt.text(data, { x, y, text: aes.color, ... })` mark — labelling each series at its last data point (for lines) or per point (for scatter, with a jitter offset to avoid overlap). The R export already has a `geom_text` / `geom_label` precedent from the bar value-labels fix (2026-07-29); reuse that pattern here.

**Invariants:** None.

---

### Clean · 2026-07-17

**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically

**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`

**Suggestion:** `History.jsx` renders the pipeline sidebar. Each step chip could get an "Edit" or "↗" icon that, when clicked, fires an event (via a callback prop to `WranglingModule.jsx`) to switch the active tab and scroll to / open the relevant panel for that step type. The mapping of step type → tab + sub-section needs a lookup table (e.g. `rename` → Clean tab / DictionaryTab, `log` → Feature tab / transforms section, `filter` → Clean tab / FilterBuilder). Start with the most-used step types: `rename`, `filter`, `log`, `sq`, `std`, `dummy`, `case_when`, `if_else`, `mutate`. Wire the tab switch through `WranglingModule.jsx`'s existing tab-state setter.

**Invariants:** Non-destructive pipeline — clicking to edit should open the form pre-filled from the step's parameters, not apply changes until the user saves. The step is not re-applied until the user explicitly saves the edit.

---

### Clean · 2026-07-19

**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform

**File:** `src/components/wrangling/FeatureTab.jsx` · `src/pipeline/runner.js`

**Suggestion:** Add a `country_code` pipeline step (new step type in `runner.js` + `registry.js`) with a small static lookup table (ISO 3166-1 alpha-2, alpha-3, and full name, ~250 rows, embedded as a JSON literal). The step takes: `col` (the input column), `from` (one of `iso2 | iso3 | name`), `to` (one of `iso2 | iso3 | name`). The UI in `FeatureTab.jsx` exposes the column picker and from/to dropdowns. Fuzzy name matching (`fuzzyGroups` in `wrangling/utils.js`) can handle common misspellings. The static table must live in `src/pipeline/` or `src/math/` (not in a UI file) so the runner can import it without React. Export translators: R uses `countrycode::countrycode(col, "iso2c", "iso3c")` etc.; Python uses `pycountry`; Stata uses a documented `kountry` package note.

**Invariants:** Registry must stay in sync with `runner.js`. The lookup table must not be in `src/math/` or `src/core/` if it has no mathematical content — put it in `src/pipeline/data/` or inline in the runner case.

---

### General · 2026-07-19

**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables

**File:** `src/services/session/sessionState.jsx` · `src/components/wrangling/CleanTab.jsx`

**Suggestion:** This is a design-level feature request with no clear implementation home yet. The most natural fit is a session-scoped "named lists" store in `sessionState.jsx` (alongside the existing dataset registry): `{ name, values: string[] }[]`, persisted to IndexedDB per `pid`. The Clean tab's `FilterBuilder` and Feature tab's `case_when` would get an "Insert list" picker that resolves a saved list name to its values inline (e.g. `["Spain", "Germany"]` for an `in` operator). This needs a design session with Franco to decide: (a) where lists appear in the UI (a dedicated Lists panel? a popover?), (b) whether lists are pipeline-step parameters or referenced by name (the latter requires a list-resolution pass before pipeline replay), and (c) how list edits propagate to steps that reference them.

**Invariants:** Persistence via IndexedDB, not localStorage. Non-destructive pipeline — a named list referenced in a step must be serialised into the step's parameters at commit time (value snapshot), not by reference, so the pipeline can replay without the session's list store.

---

### Model · 2026-07-22

**Feedback:** Add more options to plot the binned scatter plots after running models, using arguments of R and Python as commands in the UI, for all estimators

**File:** `src/components/ModelingTab.jsx` · `src/components/modeling/ModelPlots.jsx`

**Suggestion:** `ModelPlots.jsx` renders binned scatter plots (BinScatter) post-estimation. The request is to expose more configuration (number of bins, polynomial overlay, residual-on/off, conditional on controls vs raw). Add a collapsible "BinScatter Options" panel below the plot in `ModelPlots.jsx` with: bin count slider (10–100), polynomial degree selector (0/1/2), and a "Partial out controls" toggle. These parameters are passed to the existing binscatter computation function — check what arguments it already accepts vs. what needs to be added. Match the R `binsreg` / `binscatter` package options in nomenclature.

**Invariants:** None.

---

### Model · 2026-07-22

**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations

**File:** `src/components/PlotBuilder.jsx` · `src/components/ModelingTab.jsx`

**Suggestion:** The request is for a data-density slider — show only the top N% of rows (by some ordering) to reduce overplotting on large scatter plots. The cleanest implementation: add a "Max points" slider to `PlotBuilder.jsx`'s options panel that randomly samples the display rows (NOT the underlying data) to the selected count before passing them to Observable Plot. This is a VIEW-LAYER operation only — it never modifies `rawData` or the pipeline. On DuckDB-backed datasets, sample in SQL (`USING SAMPLE N ROWS`) to avoid pulling 900k rows into JS just to discard most of them. Couple this with a visual indicator ("showing N of M rows") so users understand the viewport is sampled.

**Invariants:** Non-destructive — sampling is view-only, never applied to pipeline or `rawData`. DuckDB remains the data boundary for large datasets.

---

## Performance

---

### [medium] · Data · 2026-05-08

**Feedback:** Big datasets generate crashes (21MB) over 900k rows

**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js` · `src/services/data/parsers/`

**Suggestion:** DuckDB-Wasm integration is code-complete and validated for `.csv` and `.dta` formats, reducing tab-transition lag and preventing JS-heap crashes on large CSV/DTA files. However, `.rds`, `.xlsx`, and `.shp` parsers still materialise full JS objects before DuckDB registration, so a 21MB `.xlsx` or a large `.rds` workspace will still crash. The remaining work: after parsing `.rds`/`.xlsx`/`.shp` to a row array in the browser, hand it off to `duckdb.js`'s `registerTable()` / `copyToParquet()` path (the same path used for CSV today) and reduce to a 500-row preview. This is the "Other browser-only formats remain future work" noted in BugTriage. The OPFS Parquet cache should be the target for all formats once registered — not just `.dta`.

**Invariants:** Data never leaves the browser (privacy-first constraint). No server-side computation. DuckDB as the data boundary — no full JS materialisation on large tables.

---

## Other

---

### [medium] · Data · 2026-05-10

**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 — Original Line: China,CHN,2010,2425.47... (abrupt truncation in long value field)

**File:** `src/DataStudio.jsx` · `src/services/data/parsers/`

**Suggestion:** BugTriage tracks this as `fix-later` under "Malformed CSV row can abort import instead of surfacing recovery options". The current parser (`Papa Parse` or similar) likely throws on the first malformed row and aborts the entire import. The fix is a resilient-parse mode: catch per-row errors, collect the affected row numbers, and surface a non-blocking warning banner ("3 rows could not be parsed — they have been skipped") with an option to download the rejected rows as a separate CSV for inspection. The original row number (from the error message) should be preserved in the error object so the user knows where to look. This matches R's `readr::read_csv()` `problems()` pattern.

**Invariants:** Non-destructive — skipped rows are surfaced to the user; they are not silently dropped.

---

## Suggested action order

1. **Stata workbench broken [high · Calculate]** — Reproduction is cheap (copy output to Stata, read first error line). Most likely a stale command in `stataScript.js` after the 2026-07-18 SE-type pass; surgical fix once the failing line is identified.
2. **Simulate replication script [high · Simulate]** — Users running Monte Carlo experiments have no way to reproduce them outside the app; high research-reproducibility impact. Extend `buildSessionSnapshot` + script generators to include inference/resampling steps.
3. **Time series group references broken [medium · Explore]** — The `aes.color` channel resolution in `PlotBuilder.jsx` / `ExplorerModule.jsx` is a narrow fix; once the failing accessor is found, likely a one-line change.
4. **LaTeX table column overflow [medium · Report]** — A `\resizebox` or `tabularx` wrapper in `latexTable.js`; no UI changes needed; trivially testable.
5. **Validate all 4 code-complete items** — Ask Franco to browser-validate `35f4b5a6` (Plot Builder resize, Q-Q/residual plots, frozen diagnostics) and `5e105529` (spatial heatmap) so those can be closed in ClaudeFB and BugTriage.
6. **Multiple hypothesis tests + R export [medium · Model]** — Builds on the 2026-07-30 joint-test work; the UI extension (add-row affordance) is low risk since the math is already there.
7. **FeatureTab draft persistence [medium · Clean]** — `sessionStorage` draft per form type; no engine or pipeline impact; targeted fix in `FeatureTab.jsx`.
8. **Elasticity interpretation rule [medium · Model]** — Prompt-only change in `INTERPRET_REGRESSION_PROMPT`; no engine work; test by running a log-log OLS and reading the narrative.
