# Feedback Analysis Report — 2026-07-01

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 6 open bugs · 6 open features · 0 open UX · 1 open performance · 1 open other
- 5 of the 6 bugs are **code-complete** (commits `35f4b5a6` / `5e105529`) but browser validation is still pending Franco — they are included here until confirmed working.
- 1 discrepancy: `[2026-06-17] Pipeline-only-if-dataset-selected` is marked **fixed** in BugTriage (2026-06-25) but the ClaudeFB.md entry has no strikethrough/✓. Recommend marking it closed in ClaudeFB.

---

## Bugs

---

### [high] · Data · .rds parser · 2026-06-26
**Feedback (×2):**
- `Parse error: crime_panel_final_r_300.rds — not a valid R data file (.rds), unrecognised serialisation header.`
- `Parse error: grid_enriched_500.rds — not a valid R data file (.rds), unrecognised serialisation header.`

**File:** `src/services/data/parsers/rds.js` — lines 444–460 (magic-byte detection) and line 413 (gzip decompressor).

**Suggestion:** The parser already handles gzip-compressed XDR (R's default `compress=TRUE`). The two failing files are almost certainly saved with `compress="bzip2"` (magic bytes `0x42 0x5A` = "BZ") or `compress="xz"` (magic `0xFD 0x37`), which the current magic-byte block falls through to the generic error. The Web Streams `DecompressionStream` API only supports `"gzip"` and `"deflate"` — bzip2 and xz require a WASM library or a user-facing hint. Short-term fix: detect `0x42 0x5A` ("BZ") and `0xFD 0x37` (xz) in the magic-byte block at line 456 and throw a descriptive error (`"RDS: bzip2-compressed .rds detected — please re-save with saveRDS(df, 'file.rds', compress='gzip')"`) rather than the generic "unrecognised header." Longer-term: bundle a tiny bzip2-js or seek a CDN WASM decoder so both formats parse transparently.

**Invariants:** `rds.js` is a pure-JS parser with no React imports — no invariant concerns here. ✓

---

### [high] · Model · OLS diagnostic plots · 2026-06-11
**Feedback:** `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS`

**File:** `src/components/modeling/ResidualPlots.jsx` (components), `src/components/ModelingTab.jsx` — lines 2529–2531 (wiring to `ResidualVsFitted` / `QQPlot`).

**Status:** Code-complete in commit `35f4b5a6`. The fix renders from the frozen `result.spec` rather than live sidebar state. Browser validation still pending.

**Suggestion:** Franco should load a dataset, run an OLS, then add/remove a variable in the sidebar *without* re-estimating, and confirm `ResidualVsFitted` and `QQPlot` still show the originally estimated result and do not go blank or shift. If they still misbehave, check that the `r` object passed to `ResidualVsFitted` (`resid`, `Yhat`) is read from the pinned estimation result rather than recomputed on each render.

**Invariants:** None beyond React component scope. ✓

---

### [high] · Model · Diagnostics mutate on variable change · 2026-06-14
**Feedback:** `Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.`

**File:** `src/components/ModelingTab.jsx` (estimation state management), `src/components/modeling/ModelPlots.jsx`.

**Status:** Code-complete in commit `35f4b5a6` (OLS/WLS display frozen to `result.spec`). Browser validation pending.

**Suggestion:** After browser validation confirms the freeze works for OLS/WLS, check that the same invariant holds for PanelFE/FD and 2SLS results — those result types are stored in separate state variables (`panelFE`, `panelFD`, `twoSLS`) that may not yet be frozen to their spec at render time. The fix pattern (reading from `result.spec?.xVars` rather than live selector state) should be applied uniformly to all result branches in `ModelPlots.jsx`.

**Invariants:** None. ✓

---

### [medium] · Model · Plot Builder resize + PNG export · 2026-06-10
**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`

**File:** `src/components/PlotBuilder.jsx` — line 1626 (`resize: "vertical"` wrapper div), line 1087 (`downloadCombinedPNG`), `src/components/ModelingTab.jsx`.

**Status:** Code-complete in commit `35f4b5a6` (native `resize: "vertical"` CSS, bounded `maxHeight: "70vh"`). Browser validation pending.

**Suggestion:** Franco should drag the resize handle in both the standalone PlotBuilder (ExplorerModule) and the embedded one in ModelingTab, then download a PNG at a non-default height and verify that the exported image matches the visible canvas — no extra whitespace or cropping. If the PNG still has sizing artifacts, check that `downloadCombinedPNG` in `src/services/export/plotExporter.js` derives dimensions from `getBoundingClientRect()` rather than a hardcoded constant.

**Invariants:** None. ✓

---

### [medium] · Spatial · Heatmap · 2026-06-13
**Feedback:** `Heatmap must be fixed`

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` — lines 20–228 (`buildHeatmapCells`), `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`.

**Status:** Code-complete in commit `5e105529` (restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, 5k-point cap). Browser validation pending.

**Suggestion:** Franco should add a heatmap layer to a point dataset, adjust opacity and palette, then export HTML and verify the legend is included. If cells do not render, check that `buildHeatmapCells` receives valid lat/lon columns (non-null, numeric) — the 5k-point cap is deterministic but the guard at line 224 may silently fall through if `ly.latCol` is undefined.

**Invariants:** None. ✓

---

### [medium] · Explore · Graph does not fit screen · 2026-05-11
**Feedback:** `The graph does not fit the screen`

**File:** `src/components/PlotBuilder.jsx` — ResizeObserver at line 1171, `src/components/ModelingTab.jsx`.

**Status:** Code-complete in commit `35f4b5a6`. Browser validation pending.

**Suggestion:** Confirm the embedded PlotBuilder in ExplorerModule auto-fits its canvas to the available panel width on first render and on window resize, including when the sidebar is collapsed/expanded. If overflow persists, check whether the `ResizeObserver` at line 1171 correctly throttles to avoid the feedback loop noted in the comment at line 598 (Leaflet fills container → ResizeObserver fires → infinite resize).

**Invariants:** None. ✓

---

## Features

---

### Model · Estimate without intercept · 2026-05-25 (fix-later)
**Feedback:** `Add an option to estimate without intercept`

**File:** `src/components/modeling/ModelConfiguration.jsx` (spec assembly), `src/math/LinearEngine.js` — line 128 (`k = xCols.length + 1` always prepends a constant column).

**Suggestion:** Add a `noIntercept: boolean` toggle in `ModelConfiguration` and thread it through `ModelingTab.estimate()` into the engine call. In `LinearEngine.runOLS`, when `noIntercept` is true, skip prepending the constant column and set `k = xCols.length`. R² must switch to uncentered SST (`Σ yᵢ²`) when there is no intercept. Apply the same `noIntercept` path to `runWLS`, and update the DuckDB suff-stats dispatch to include a `noIntercept` flag in the SQL `SELECT 1 AS _c` column guard. The replication scripts (`rScript.js`, `pythonScript.js`, `stataScript.js`) must emit `-1` / `noconstant` / `nocons` when the flag is set. This is a cross-cutting engine change — plan carefully before implementing.

**Invariants:** `seType` must still be passed explicitly to the no-intercept engine variant; never hardcode SE inside the engine. The existing `runOLS` signature already threads `seType` correctly — the no-intercept variant must keep that pattern. ✓

---

### Model · RDD summary table (rdrobust style) · 2026-05-21 (fix-later)
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`

**File:** `src/components/ModelingTab.jsx` (SharpRDDResults display region, ~line 2646 neighbourhood), `src/math/CausalEngine.js` (result object already contains `h`, `kernel`, `polyOrder`, LATE, SE).

**Suggestion:** `CausalEngine.runSharpRDD` already returns all needed fields. Add a `SharpRDDSummaryTable` sub-component (can be inlined in `ModelingTab.jsx` or extracted to `src/components/modeling/`) that renders a compact table matching the `rdrobust` console output layout: method, kernel, BW type, bandwidth (left/right h), effective n left/right of cutoff, LATE estimate, SE, z-stat, p-value, and 95% CI. No new math is needed. Render the table above the existing coefficient row in `FuzzyRDDResults` / `SharpRDDResults`.

**Invariants:** No math changes — UI-only addition. `CausalEngine.js` must not be touched. ✓

---

### Clean · R `%in% c(...)` filter syntax · 2026-05-19 (fix-later)
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`

**File:** `src/components/wrangling/CleanTab.jsx` (filter expression UI), `src/services/exprEvalService.js` (expression evaluator bridge to the worker).

**Suggestion:** The filter worker evaluates JS expressions, so `%in%` (R syntax) is never valid there. Pre-process the filter string before passing it to the worker: detect the pattern `<col> %in% c(...)` with a regex and rewrite it to `[...values].includes(<col>)` in JS. The rewrite regex is: `/(\w+)\s*%in%\s*c\(([^)]+)\)/g` → replace with `[$2].includes($1)` (the `c()` argument list is already comma-separated and quote-compatible with a JS array literal). Apply the rewrite in `CleanTab.jsx` before the expression reaches `exprEvalService`. Add a test case to the pipeline reliability harness.

**Invariants:** The filter step replays on `rawData` in `runner.js` — the rewrite must happen before the expression reaches the worker, not inside the engine. Never mutate `rawData`. ✓

---

### Spatial · Spatial filters for plots · 2026-05-30 (fix-later)
**Feedback:** `Allow spatial filters for plots`

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`.

**Suggestion:** Add a per-layer filter row in `GeoLayerConfig` that accepts a column name and a predicate (value match for categorical, min/max for numeric). Filter `layerRows` in `SpatialGeoPlot.jsx` before passing to `mkGeoLayer` in `geo.js`. Keep the filter purely in UI state — do not mutate `rawData` or add a pipeline step. Route to V4 as noted in BugTriage.

**Invariants:** Client-side filtering only; no Anthropic API calls involved. ✓

---

### Model · Editable LaTeX variable names in coefficient table · 2026-06-06 (fix-later)
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`

**File:** `src/ReportingModule.jsx` (LaTeX table builder / `buildStargazer`), and the coefficient label generation in `ModelingTab.jsx` result display.

**Suggestion:** Add an optional `labelOverrides: { [coefName]: string }` field to each pinned model's metadata in the `model_buffer` IDB record. Expose an inline ✎ button next to each coefficient row in the results panel; clicking it opens an in-place text input that populates this map. In the LaTeX export path, substitute `labelOverrides[coefName]` when present. For RDD specifically, derive default label strings from `runningVar` and `cutoff` values rather than hardcoding "D (treatment)", so the initial labels are already meaningful before any manual override.

**Invariants:** Persist overrides via `indexedDB.js` inside the `model_buffer` store — never `localStorage`. No Anthropic API calls. ✓

---

### Calculate · Add limits (calculus) · 2026-06-19
**Feedback:** `Add limits`

**File:** `src/components/tabs/CalculateTab.jsx` — line 1169 (`activeTool` state; current options: `"solver" | "derivative" | "symbolic" | "algebra" | "integral"`).

**Suggestion:** Add a `"limit"` tool mode alongside the existing calculus tools. The UI panel needs: expression input, variable, limit point (with `Inf` / `-Inf` / numeric text field), and direction toggle (left `x → a⁻`, right `x → a⁺`, both). For evaluation, `mathjs` (already imported via `evalExpression`) does not compute symbolic limits natively, but numerical limit estimation via successive evaluation at a geometric sequence converging to the limit point is feasible for most student use cases. For `x → ∞`, evaluate at 1e6, 1e9, 1e12 and check convergence; show the estimated limit and a convergence note. Add the `\lim_{x \to a}` LaTeX snippet to the existing toolbar array at line 309 (it is already listed there as `["lim","\\lim_{x\\to 0}"]`, so just verify the display label is shown in the toolbar).

**Invariants:** Any numerical limit helper must live in `CalculateTab.jsx` or a sibling utility file — not in `src/math/` (zero React in math files). ✓

---

## Performance

---

### [high] · Data · Large dataset crashes (21 MB, 900 k rows) · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`

**File:** `src/DataStudio.jsx` (parseFile + DuckDB routing), `src/services/data/duckdb.js`.

**Status:** Fase X4 code-complete; browser benchmark pending Franco. Plan: `plans/2026-05-22-fase-x4-performance.md`.

**Suggestion:** Franco should load the 21 MB CSV, confirm the DuckDB badge appears in the wrangling header, navigate across all tabs (Clean → Explore → Model → Simulate), and time each transition. If any tab still takes >3 s, the likely culprit is a `SELECT *` materializing the full Arrow result into JS objects. Ensure all preview paths use `LIMIT 500` and that `runner.js`/`duckdbRunner.js` never calls `getRows()` without a row-count guard when `rawData._duckdb` is set. The agreed rule: display-layer = 200–500 rows; computation-layer = full DuckDB table, never truncated before `runner.js`.

**Invariants:** Computation must always run on the full dataset — never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`. Privacy-first: no server-side computation. ✓

---

## Other

---

### Data · Malformed CSV row aborts import · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`

**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/`.

**Status:** BugTriage: fix-later — post-launch resilient-import pass.

**Suggestion:** When the CSV parser (or DuckDB's CSV reader) throws on a specific row, catch the error and surface a recovery UI: show the offending line number and content, then offer — (1) skip the bad row and continue, (2) import up to the bad row, (3) abort. For DuckDB-backed large files, `COPY ... (IGNORE_ERRORS true)` can skip bad rows at the SQL layer; expose a `_skippedRows` count in the result and show a warning badge in the data viewer header. For the JS parser path, wrap the per-row loop in a try/catch.

**Invariants:** Skipped-row recovery must not mutate `rawData` — the row is simply absent from the initial parse and the pipeline replays cleanly from whatever was loaded. ✓

---

## ClaudeFB.md discrepancies (doc hygiene)

The following items are confirmed fixed in BugTriage but are NOT yet marked as strikethrough/✓ in ClaudeFB.md. Recommend updating in the next collector run:

| ClaudeFB entry | BugTriage verdict |
|---|---|
| `[2026-06-17] Pipeline only applies if dataset is also selected in Clean Module` | fixed 2026-06-25 (`date_parse` missing from `STEP_REGISTRY`) |
| `[2026-06-14] 2SLS Poisson interpretation wrong` | fixed 2026-06-14 (`bc2d7b9d`) |
| `[2026-06-13] XSS injection attempts (×2)` | fixed 2026-06-14 (sanitize-on-write at `feedbackService.submitFeedback`) |
| `[2026-06-11] Y~Y_hat, Residual vs fitted, Q-Q plot missing` | code-complete `35f4b5a6` — pending Franco browser validation |
| `[2026-06-10] Plot Builder resize + PNG` | code-complete `35f4b5a6` — pending Franco browser validation |
| `[2026-06-13] Heatmap` | code-complete `5e105529` — pending Franco browser validation |
| `[2026-05-11] Graph does not fit screen` | code-complete `35f4b5a6` — pending Franco browser validation |
| `[2026-06-14] Diagnostics mutate on variable change` | code-complete `35f4b5a6` — pending Franco browser validation |

---

## Suggested action order

1. **[critical — data blocked] Fix `.rds` bzip2/xz detection in `rds.js` lines 456–460** — two open files fail on load; detecting `BZ` / xz magic bytes and throwing an informative error (or adding bzip2 decompression via WASM) unblocks users immediately.
2. **[high — clears 5 bugs at once] Browser-validate commits `35f4b5a6` + `5e105529`** — five code-complete fixes (OLS diagnostic plots, diagnostics freeze, PlotBuilder resize/PNG, graph fit, heatmap) are blocked only on Franco's sign-off; confirming them closes 5 open entries simultaneously.
3. **[medium — frequent user request, self-contained] Add calculus limits to CalculateTab** — no new math library needed; numerical approximation via the already-imported `mathjs` evaluator is self-contained in `CalculateTab.jsx` and the feature has no scheduled milestone yet.
