# Feedback Analysis Report — 2026-09-22

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **16 open bugs** (4 code-complete pending Franco browser confirmation) · **10 open features** · **2 open UX/design** · **1 open performance** · **0 open other**

> **Note on branch:** The scheduled task targets `Main-`, which does not exist in this repo. All prior reports have been committed to `main`; this report follows that convention.
>
> **Note on items not listed here:** Many ClaudeFB.md bullet points that lack ~~strikethrough~~ / ✓ markers are already resolved per BugTriage.md. Those are treated as fixed. Items listed below are either explicitly "open" or "fix-later" in BugTriage.md, or are unreferenced there and not confirmed resolved.
>
> **Already-done items that ClaudeFB never marked:** `Add limits` (CalculateTab has `"limit"` tool); Anderson-Rubin CIs (X5: "verified fixed"); Multiple statistics tests (X5: "verified fixed"); Estimate without intercept (CLAUDE.md: "DONE 2026-07-18").
>
> **No source changes since 2026-09-21** — only `FeedbackAnalysisReport.md` was committed. Open items are unchanged.

---

## Bugs

### [critical] · Performance / Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Per CLAUDE.md "Fase X4 code-complete; browser benchmark pending Franco." Confirm browser benchmark of the OPFS Parquet cache + SQL push-down path on a real 900k-row dataset. If the crash still reproduces, check whether the OPFS write succeeded (DuckDB parquet-extension CSP issue documented in CLAUDE.md) and whether Arrow materialisation on tab-transition is still happening rather than query-only access.
**Invariants:** DuckDB as data boundary; never pull full Arrow result into JS objects (CLAUDE.md performance rule 1).

---

### [critical] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 (CSV import aborts on malformed row)
**File:** `src/services/data/parsers/`, `src/DataStudio.jsx`
**Suggestion:** BugTriage categorises this "fix-later — post-launch resilient-import UX." Suggested approach: catch per-row parse errors, collect them, allow the import to proceed with bad rows skipped or flagged, and surface a "N rows skipped due to parse errors (lines: 1583, …)" banner. The row number is already available at parse time.
**Invariants:** None specific.

---

### [high] · Export / Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** BugTriage says "open." The July 2026 SE-type fix pass corrected HC2/HC3 and panel SE routing, but a broad "doesn't work completely" suggests remaining gaps (e.g. `ibn.` factor coding in no-intercept specs marked UNVERIFIED, `joinby` for many-to-many joins, interaction-only factor coding under no-intercept, two-way cluster routing to `reghdfe`). Audit `stataScript.js` against R output by running `replicationIntegrityValidation.mjs` and tracing any `UNVERIFIED` or `NOTE:` comments to confirm they produce valid `.do` syntax. A focused round of `reghdfe`-based panel SE plus the `ibn.`/factor edge cases is the likely root cause.
**Invariants:** None — export files are pure JS outside `src/math/` and `src/core/`.

---

### [high] · Export / Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx` (approx. line 519, `generateSimScript`)
**Suggestion:** `generateSimScript` emits only the DGP variable draws (`distExprR`/`distExprPy`/`distExprStata`). It does not emit: (a) the downstream OLS/single-variable Monte Carlo loop, (b) `SampleTestPanel` tests run on simulated data, nor (c) summary stats from `StatWorkspace`. Extend `generateSimScript` to accept the active MC mode and emit a `for`-loop with estimation/test calls matching what the UI ran. The `dgpScript.js` expressions are already the single source for the draw step — only the analysis wrapper is missing.
**Invariants:** `src/math/` and `src/core/` must have zero React imports — keep export logic in `SimulateTab.jsx` or a new `src/services/export/simScript.js`.

---

### [high] · Model · 2026-07-09
**Feedback:** Add options for fixed effects — not just entity and time, but country, state, sector, every categorical variable must be allowed as FE
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/PanelEngine.js` (`runFEMulti`)
**Suggestion:** BugTriage says "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation." The N-way FE math is validated (18/18 R checks in CLAUDE.md). The remaining question is whether the UI allows users to select arbitrary categorical columns as FE dimensions without requiring a panel declaration. If the pickers are still limited to panel-declared columns, add a multi-select for extra FE dimensions that routes to `runFEMulti` directly.
**Invariants:** SE type must be passed explicitly to `runFEMulti` — never hardcoded. No React in `PanelEngine.js`.

---

### [high] · Model · 2026-07-09
**Feedback:** Add also FE with interactions
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions`)
**Suggestion:** BugTriage says "likely fixed — shipped in `045f5cda` (#104); needs confirmation." Verify that FE×covariate interactions (e.g. `country×female`) can be specified in the ModelConfiguration UI and are passed to `runFEMulti` as interaction columns, not just as separate FE dimensions. If the `factor_interactions` pipeline step is required upstream, confirm it is reachable from the Modeling tab without a separate Clean step.
**Invariants:** No React in `PanelEngine.js`.

---

### [high] · Model · 2026-06-11 (code-complete since 2026-06-14)
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage says "code-complete `35f4b5a6` — all three plots already wired; browser validation pending Franco." Over 3 months old. If still failing in-browser, check that `result.spec` is populated for all OLS paths (including DuckDB suff-stats path) and that `ResidualVsFitted` / `QQPlot` receives `resid` and `Yhat` arrays rather than `null`. The suff-stats path materialises residuals via `duckdbResiduals.sampleResiduals` (5k-row sample) — verify it is forwarded to the plot components.
**Invariants:** None.

---

### [medium] · Model · 2026-06-14 (code-complete since 2026-06-14)
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** BugTriage says "code-complete `35f4b5a6` — OLS/WLS display frozen to `result.spec`; browser validation pending Franco." Also over 3 months old. Confirm that `result.spec` is populated correctly for panel, IV, and non-linear estimators too (the fix may only target OLS/WLS). Non-OLS results should derive their display from `result.spec` consistently.
**Invariants:** None.

---

### [medium] · Spatial · 2026-06-13 (code-complete since 2026-06-14)
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** BugTriage says "code-complete `5e105529` — restored Heatmap as a Leaflet map layer with KDE cells; browser validation pending Franco." Over 3 months old. If still broken in-browser, check whether the Leaflet heatmap plugin loads correctly (CSP `connect-src` issue documented in CLAUDE.md could block CDN-loaded plugins), and whether the deterministic 5k-point cap is applied before KDE computation.
**Invariants:** CSP `connect-src` must include every CDN loaded at runtime (CLAUDE.md key bug).

---

### [medium] · Model · 2026-06-10 (code-complete since 2026-06-14)
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage says "code-complete `35f4b5a6` — plot viewport has native vertical resize." If PNG export quality is still wrong, check that SVG-to-Canvas rasterisation reads `contentRect` dimensions rather than the original 400px default.
**Invariants:** None.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` (time-series section, approx. lines 1460–1640)
**Suggestion:** BugTriage says "open." The time-series sub-tab groups by a color/group column and overlays one line per group. If group references (legend labels, color mapping) break, the likely cause is that the group-split path passes the wrong column key when `tCol` and `colorCol` are both set. Inspect the group-aggregation step and verify the `groupBy` value matches what the SVG line renderer expects.
**Invariants:** None.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.
**File:** `src/ExplorerModule.jsx` (time-series section), `src/components/PlotBuilder.jsx`
**Suggestion:** BugTriage says "open." The time-series line chart in ExplorerModule is an SVG custom renderer — it does not use PlotBuilder, so it lacks the labels panel. Add a compact labels row (title, x-axis label, y-axis label) above the chart, matching the pattern in other Explore sub-tabs.
**Invariants:** Inline styles via `C` object only — no CSS files.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`
**Suggestion:** Wrap the table in `\\resizebox{\\linewidth}{!}{...}` or emit `\\usepackage{adjustbox}` + `\\begin{adjustbox}{max width=\\linewidth}`. Alternatively, emit `\\begin{tabular*}{\\linewidth}{@{\\extracolsep{\\fill}}...}`. Check whether `ReportingModule.jsx` needs to thread a `wide` flag to `latexTable.js`.
**Invariants:** None.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** BugTriage says "open." Extend `CoefficientTestPanel` with a saved-tests list: each test is pushed to a `savedTests` array (state in ModelingTab), displayed in a summary table, and exported via `generateJointHypothesisScript()` (already added 2026-07-30) as a batch of `car::linearHypothesis` calls.
**Invariants:** None.

---

### [low] · Clean · 2026-05-19
**Feedback:** Add filter(`regionname %in% c("Spain", "Basque Country")`) — R-style `%in%` membership syntax not accepted
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** BugTriage says "fix-later." The `in`/`nin` operators in `predicate.js` already cover the `%in%` semantic. The gap is that `FilterBuilder` doesn't surface an easy "in list" editor. Adding a multi-value tokeniser chip input for `in`/`nin` operators, with a tooltip showing the equivalent R `%in%`, would close this without changing the predicate language.
**Invariants:** `predicate.js` is the single owner of filter operators — never re-implement a filter operator elsewhere (CLAUDE.md architectural invariant).

---

### [low] · Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** BugTriage says "fix-later — route to V4 plots." A per-layer bounding-box or attribute filter analogous to the row-filter in Clean would let users show only a geographic subset without adding a pipeline step. Minimal implementation: add a `GeoLayerConfig` panel entry that accepts a predicate using `evalPredicate` from `predicate.js`.
**Invariants:** Use `evalPredicate` from `src/pipeline/predicate.js` — never re-implement filter logic.

---

### [low] · Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/components/modeling/resultDisplay.jsx`
**Suggestion:** BugTriage says "fix-later." Add a `displayNames` map to `EstimationResult` (or to the spec's metadata), editable via an inline double-click in the coefficient table or a dedicated "Edit labels" modal. `latexTable.js` already receives the coefficient names — it just needs to accept an override map.
**Invariants:** None.

---

## Features

### Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation of coefficients
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`), `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage says "open." The AI narrative already handles log-log elasticity and log-level semi-elasticity. A non-AI path: add an "Elasticity" chip in the coefficient table showing `β × (X̄/Ȳ)` (point elasticity at the mean) computed from the dataset's column means and the estimated β — no AI call required, matches R's `margins` package default for linear models.
**Invariants:** All Anthropic calls through `AIService.js` only.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Open. Add a small search input at the top of the variable list panel that filters displayed column names client-side. The list already renders from `headers` — a `filteredHeaders = useMemo(() => headers.filter(...), [headers, searchQuery])` with a text input is sufficient.
**Invariants:** Inline styles via `C` object only.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Open. When `aes.color` is set, PlotBuilder shows a legend via Observable Plot's default. Add a "Color labels" checkbox in the labels panel that, when enabled, appends a `Plot.text` mark with the series name at the last data point for line geoms, and uses `showValues` (already supported for bar) for grouped bars.
**Invariants:** None.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — local polynomial regression summary
**File:** `src/components/modeling/ModelPlots.jsx` (RDD results area), `src/math/CausalEngine.js`
**Suggestion:** BugTriage says "fix-later." `runSharpRDD` already returns bandwidth, kernel, polynomial order, N left/right, and the full SE and CI. A dedicated `RDDSummaryTable` component (Method, Kernel, BW, N obs left/right, Coef, SE, CI) would match R's `summary(rdrobust)` output format 1:1 and improve replication confidence.
**Invariants:** No React in `CausalEngine.js`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Open. A constant `y = k` is a degenerate parametric curve. Add support for defining a "constant" entry in the expression list that evaluates to a fixed value and renders as a horizontal line. Alternatively, allow `f(x) = 5` in the existing parser — if the parser already handles it, check whether the y-domain is clipped to exclude the constant value.
**Invariants:** Inline styles only, no external UI libraries.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Open. Expose controls for bin count, x-axis range (useful for RDD), residualisation toggle (raw vs. conditional), and whether to show the regression line. These map directly to `binsreg` / `rdplot` arguments in R. A collapsible "Plot options" panel below `ModelPlots` is the right surface.
**Invariants:** None.

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Open. Add a slider for max-observations (defaulting to ~5000) that subsamples via `plotRows.slice` or a DuckDB `USING SAMPLE` call for display. This is a display-only operation and must never affect model estimation.
**Invariants:** Display limit ≠ computation limit (CLAUDE.md performance rule 5). Do not truncate `rawData` passed to estimation.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Open. Add a click handler in the pipeline sidebar that maps step type → Clean/Feature/Reshape/Merge tab + sub-section, then dispatches `setActiveTab` + `setActiveSection`. The mapping is mechanical — a `STEP_TYPE_TO_SECTION` lookup table (≈30 entries) keyed on `STEP_REGISTRY` is all that is needed.
**Invariants:** Use `STEP_REGISTRY` as the source — never maintain a parallel step-type list.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Open. Add a `country_code` pipeline step in `runner.js` with a bundled static lookup table (~250 rows, iso2/iso3/name). Expose it in FeatureTab as a column-transform with a mode selector. The lookup is entirely client-side.
**Invariants:** New step types must be added to both `runner.js` AND `registry.js` (enforced by `pipelineReliabilityValidation.mjs`).

---

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** BugTriage says "open." Extend `artifactOrder.js` to add a `group` string to each `ArtifactId`. In `ModelBufferBar.jsx`, add a group label row in the buffer bar and a "New group / Move to group" context menu on each pinned model chip. Persist group assignments per project in IndexedDB.
**Invariants:** Persistence in `indexedDB.js`, not localStorage (CLAUDE.md architectural invariant).

---

## UX / Design

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to continue
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** BugTriage says "open." Use `sessionStorage` keyed by dataset `pid` + section name to persist in-progress form state (draft case_when rows, partial mutate expression, etc.) across tab switches within the same session — matching the pattern `History.jsx` uses for pipeline-hide state. Per-session only; never persisted to IndexedDB.
**Invariants:** IndexedDB for pipeline/data storage — `sessionStorage` is acceptable here for transient draft state within a single session only.

---

### General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`, line 11)
**Suggestion:** BugTriage says "open." Features shipped since the last tour update include N-way FE, Sun-Abraham/Callaway-Sant'Anna DiD, Goodman-Bacon decomposition, PlotBuilder tile/facet, Spatial pipeline integration, model buffer, split-pane divider, and the Credits system. Review each `TOUR_STEPS` entry and update step text + target selectors. Per CLAUDE.md: never write counts into help copy — name the things instead.
**Invariants:** Same inline-styles/`C` pattern as the rest of `HelpSystem.jsx`.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** (Also listed under Bugs as the primary open critical item.) The OPFS Parquet + DuckDB SQL push-down path is code-complete but browser-unconfirmed. Priority: Franco browser-validate on a real 900k-row dataset, confirm OPFS write succeeds (CSP `connect-src` includes `extensions.duckdb.org`), and verify tab-transition no longer materialises the full Arrow result in JS.
**Invariants:** Never pull full Arrow result into JS; query/aggregate inside DuckDB (CLAUDE.md performance rules 1–2).

---

### · Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` step type: input column (country name or iso2), output column, direction (name→iso2, name→iso3, iso2→iso3, etc.). The lookup table (~250 rows, ~8KB) can be embedded as a static JSON in `src/math/countryLookup.json` or inlined as a const in a helper. Wire through the registry and runner. The `FeatureTab.jsx` UI panel can reuse the existing `add_column` skeleton. For fuzzy matching (name→code) reuse `fuzzyGroups` from `wrangling/utils.js` with a strict similarity threshold.
**Invariants:** step must replay on `rawData` (non-destructive); persist step params via `registry.js`; lookup table in `src/math/` only (no React there).

---

### · Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set and the geom is `point` or `line`, Observable Plot renders a color legend via `Plot.legend`. Add a toggle to also draw inline labels at the end of each line (for `line` geom, the last data point) or as `Plot.text` marks placed at the centroid of each color group (for `point` scatter). This is a common ggplot2 `ggrepel`-style enhancement. Keep it opt-in via a "Show labels" toggle in the Labels panel to avoid clutter in dense plots.
**Invariants:** inline styles via `C`.

---

### · Explore / Model · Various
**Feedback (grouped):** (a) Filter with `%in% c(...)` R-style syntax (2026-05-19); (b) RDD `summary(rdrobust)`-style table (2026-05-21); (c) Spatial filters for plots (2026-05-30); (d) Editable LaTeX table variable names (2026-06-06); (e) Cross-module session list management (2026-07-19); (f) 3D plots (2026-07-05)
**File:** Various — see Deferred Bugs table above + `src/components/PlotBuilder.jsx` for 3D
**Suggestion:** (a), (b), (c), (d) are already tracked as "fix-later" in BugTriage. (e) needs a design decision before implementation — session-scoped context in `sessionState.jsx` is the natural home. (f) 3D plots require a third-party library (Plotly or Three.js) or Observable Plot 0.7+ — scope as a post-MVP enhancement.
**Invariants:** (f) would require adding a new CDN dependency — add to `vercel.json` `connect-src` AND `script-src` per the CSP bug documented in CLAUDE.md.

---

## UX / Design

### · UX · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** (See Bug section above — duplicate entry; handled there.)
**Invariants:** sessionStorage keyed by `pid + stepType`; not IDB.

---

### · UX · General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`
**Suggestion:** A session-scoped list store is a design-first decision. The natural home is `SessionStateProvider` (already in `sessionState.jsx`) — add a `lists` map `{name → string[]}`. Expose a "Save as list" button next to the distinct-values panel (already shipped 2026-07-30). In `FilterBuilder`, add a `$in list` operator that pulls from the stored lists. No IDB write needed for session-only lists; an IDB `session_lists` store would be needed for persistence across reloads.
**Invariants:** persistence via `indexedDB.js` if cross-session; `sessionState.jsx` is React context, never used in `src/math/`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (~21 MB, 900k rows)
**File:** `src/DataStudio.jsx` (parseFile branches), `src/services/data/duckdb.js`
**Suggestion:** BugTriage routes this to "Fase X4 — code-complete; browser benchmark pending Franco." The DuckDB performance strategy in CLAUDE.md lists: (1) OPFS Parquet cache for large CSV, (2) `SELECT` only needed columns, (3) 200–500 row display limit, (4) precompute column stats in SQL. Browser benchmarking is the outstanding task — Franco should open a 21MB CSV, navigate all 7 tabs, and confirm no crash. If a crash persists, the likely culprits are: (a) a tab that still calls `SELECT *` materialising 900k rows in JS (check `duckdbRunner.js` step handlers), or (b) React re-rendering with the full `rawData` array in state (check DataStudio's `useEffect` dependencies).
**Invariants:** full dataset is always available to `runner.js` / `duckdbRunner.js`; display layer caps at 500 rows; never truncate `rawData`.

---

1. **Browser-validate 4 code-complete items (OLS diagnostics, coef freeze, Heatmap, PlotBuilder resize)** — these are now 3+ months old; either mark them ✓ in ClaudeFB.md/BugTriage.md or re-open with a specific repro. Clearing these unblocks significant backlog tidying.
2. **Fix Stata workbench completeness** (`stataScript.js`) — Stata is the primary replication tool for LMU academic users; a broken workbench directly undermines the institutional licensing pitch.
3. **Extend Simulate replication script** (`SimulateTab.jsx` `generateSimScript`) — a research simulation that can't be reproduced defeats the purpose of the tool; extending it to cover the downstream OLS/test loop closes a workflow gap users have flagged directly.
4. **Confirm FE arbitrary categorical + FE interactions** (`ModelConfiguration.jsx`) — the N-way FE math ships (18/18 R checks), but if the UI still limits pickers to two declared panel columns, the feature is unreachable for the primary academic use case.
5. **Multiple hypothesis test save** (`CoefficientTestPanel.jsx`, `ModelHypothesis.js`) — directly enables replication of Wald tests in PS2–PS7 workflows; `generateJointHypothesisScript` already exists.
