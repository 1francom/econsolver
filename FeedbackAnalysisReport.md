# Feedback Analysis Report — 2026-09-06

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 10 open bugs · 10 open features · 2 open UX/Design · 1 open performance · 1 open other

Items with status "code-complete / pending browser validation" (3) and "fix-later" (5) are tracked separately at the end. P-value bugs (3 entries) are consolidated into one: "likely fixed, awaiting Franco confirmation."

---

## Bugs

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** Open `CodeEditor.jsx` and trace the Stata tab's render path; confirm `stataScript.js` exports a runnable `.do` file for every active estimator (OLS, FE, IV, Poisson, CS DiD, etc.). The 2026-07-17 HC2 pass fixed many estimator-specific SE routes but noted `xtreg … vce(cluster a b)` is a Stata syntax error and that `ibn.` for through-the-origin factors is UNVERIFIED. Start there: run each estimator's generated Stata snippet through `stata -b do <file>.do` (or hand-check syntax) and file a per-estimator failure list.
**Invariants:** All styles inline via `C` object. Export logic stays in `stataScript.js`; no raw Anthropic calls from the exporter.

---

### [high] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** In `latexTable.js` inspect `buildStargazer`: it emits a fixed `tabular` environment. When the number of models (columns) exceeds ~5 the table overflows a standard page width. Wrap the table in a `\resizebox{\textwidth}{!}{…}` or switch to `tabularx` / `adjustbox` and emit `\begin{adjustbox}{max width=\linewidth}`. Add a column-count guard: if `models.length > 4`, emit the resize wrapper automatically.
**Invariants:** None — pure string generation, no React.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already has `waldTest` and `generateJointHypothesisScript`. The gap is state: `CoefficientTestPanel.jsx` runs one hypothesis at a time and does not persist results. Add a `savedHypotheses` array in panel state (or via IndexedDB keyed by `result.uuid`); render each saved test as a collapsible row with its F/χ², p-value, and a combined "Copy all as R" button that emits multiple `linearHypothesis(fit, …)` calls. Store in IndexedDB, not localStorage.
**Invariants:** Persistence must use `src/services/Persistence/indexedDB.js`, not localStorage.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The Explore ACF/PACF / time-series sub-tab in `ExplorerModule.jsx` renders its own SVG chart without going through PlotBuilder's labels panel. Add a `<LabelsPanel>` (title, x-label, y-label) mirroring PlotBuilder's existing labels section — the fields already exist in PlotBuilder's state model, so the pattern is established. Wire title into the SVG `<text>` header and axes into the existing axis-label elements.
**Invariants:** Inline styles via `C`; no external UI libraries.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add an `ELASTICITY RULES` block to `INTERPRET_REGRESSION_PROMPT` (after the existing ESTIMATOR TYPE RULES): when `x` and `y` are both log-transformed, interpret β as the elasticity (% change in y per 1% change in x); when only `x` is log, interpret as semi-elasticity (β/100 ≈ % change in y per 1% change in x); when only `y` is log, interpret as the percentage-change effect of a unit increase in x. The model's `spec` already carries `yVar` and `xVars`; check for `log_` prefix or a `log` pipeline step upstream of the variable to detect log transformation.
**Invariants:** All AI calls route through `src/services/AI/AIService.js`; do not add raw `fetch` to Anthropic API elsewhere.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The Explore time-series view has a `groupBy` dimension that colours series by group level. The "reference" line (mean or baseline for the full sample) is computed once and is not re-scoped by group when a `groupBy` is active — it likely uses the full-sample mean as a reference for each group series, which is wrong when groups have different baseline levels. Fix: when `groupBy` is set, compute one reference statistic per group and draw group-specific reference lines rather than a single global one. Inspect the `groups.map` block around line 665 of `ExplorerModule.jsx`.
**Invariants:** Inline styles; no external UI libraries.

---

### [medium] · Export · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx`, `src/math/dgpScript.js`
**Suggestion:** `SimulateTab.jsx` imports `distExprR/Py/Stata` from `dgpScript.js` and emits DGP draw code only. The panel also runs resampling tests, sample tests (`SampleTestPanel`), and Monte Carlo loop logic — none of which are serialised. Add a `generateSimulateScript(lang)` function (new file `src/services/export/simulateScript.js`) that emits: (1) the DGP draw block (reuse `dgpScript.js`); (2) the MC loop (`for (i in 1:reps) { draw; compute statistic }`); (3) the sample test code (reuse `SampleTests.js`'s existing R/Py/Stata emitters). Wire into `SimulateTab.jsx`'s existing "Copy R" / "Copy Python" button.
**Invariants:** Pure JS in the export file — no React imports; no raw Anthropic fetch. Style via `C`.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array)
**Suggestion:** `TOUR_STEPS` in `HelpSystem.jsx` is hand-written prose. Walk each step against the current UI: the pipeline-column collapse, N-way FE, CS DiD, the Bacon Panel, the Spatial tab sub-tabs (Analyze/Map/Plot), the model buffer bar, and the Report AI. Update text for any step that refers to a UI element that has moved or been renamed. Per CLAUDE.md convention: never write counts ("14 estimators") — name the things; HintBox `title` is always the MODULE name.
**Invariants:** Inline styles via `C`. `HintBox` trigger button always reads "Help".

---

### [low] · UX · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/components/workspace/DatasetManager.jsx`
**Suggestion:** `artifactOrder.js` already manages a global ordered list with `makeArtifactId`/`parseArtifactId`. Extend with a `groups` map (`{ [groupId]: { label, artifactIds[] } }`) stored alongside `order` in the same IndexedDB key. Add a "New group" button in `ModelBufferBar.jsx` that prompts for a name and moves checked items into the group. A group collapses to a single row with a count badge. Requires no engine changes — pure UI + persistence.
**Invariants:** Persist in `indexedDB.js`, not localStorage. Inline styles via `C`.

---

### [low] · UX · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Each transformation sub-section (case_when, mutate, lag, etc.) in `FeatureTab.jsx` holds its draft state in local React state only, so navigating away (to Clean or another tab) resets it. Mirror the pattern used by `History.jsx`'s pipeline sidebar: persist the in-progress form fields in `sessionStorage` keyed by `${pid}:${datasetId}:featureTab:${subSection}`. On mount, restore them; on unmount or successful step-add, clear them. This is view state, not data — sessionStorage is the right store here (not IndexedDB).
**Invariants:** Form draft is view state (sessionStorage OK). No localStorage for pipeline/data storage per CLAUDE.md.

---

## Features

### Model · 2026-05-11
**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength
**File:** `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/results/` (IV results panel)
**Suggestion:** `CausalEngine.js` already has `andersonRubinCI` per BugTriage's 2026-06-14 X5 reconciliation note ("verified fixed: `CausalEngine.andersonRubinCI` + `IVResults` weak-IV panel"). Confirm the function is exposed in the IV results display: open `ModelPlots.jsx` or the IV results component and verify the AR CI appears when the first-stage F < 10. If not wired into the UI, add it to the "Weak instrument diagnostics" section of the IV results panel.
**Invariants:** `src/math/` must have zero React imports.

---

### Model · 2026-05-11
**Feedback:** Add multiple statistics tests
**File:** `src/components/tabs/statsim/SampleTestPanel.jsx`, `src/math/SampleTests.js`, `src/ExplorerModule.jsx`
**Suggestion:** `SampleTests.js` already implements one-sample mean, variance χ², two-sample mean, two-proportion, and variance ratio. The gap from user feedback is that only one test is visible at a time in `SampleTestPanel`. Add a "Add another test" button to stack multiple test configurations, each rendered as a collapsible row with its result. Results should be independent (not pooled), matching R's workflow of calling `t.test()` / `var.test()` separately per question.
**Invariants:** `SampleTests.js` must remain pure JS (no React).

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** The filter UI already supports an `in` operator (from `predicate.js`'s `OPERATORS`). The feature request is multi-value `in` with a comma-separated value entry, similar to R's `%in% c(...)`. Inspect `FilterBuilder` in `CleanTab.jsx`: when `op === "in"`, the value field should accept comma-separated strings and parse them into an array before passing to `evalPredicate`. Verify `predicateToSQL` emits `col IN (...)` correctly for this operator. This is effectively a UX polish on an existing operator.
**Invariants:** `predicate.js` is the single owner of condition language — never re-implement a filter operator elsewhere.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/modeling/ModelPlots.jsx` or a new `src/components/modeling/results/RDDResults.jsx`
**Suggestion:** The BugTriage entry for this is "fix-later". The RDD result already has `bandwidth`, `h`, `kernel`, `polyOrder`, `n_left`, `n_right`, `late`, `se`, `p`. Lay them out in a compact 2-column table mimicking `rdrobust`'s `summary()`: Bandwidth (h), Kernel, Polynomial order, N (left/right), LATE estimate with SE, 95% CI, t-stat, p-value. This is display-only — no engine change. Add as a collapsible "RD Summary" block above the existing RDDPlot.
**Invariants:** Display only. `src/math/CausalEngine.js` stays pure JS.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The tab already has a "Limit" entry in its hint copy (line ~1574 of CalculateTab.jsx). Confirm the CAS engine (nerdamer or similar) is wired up for `limit(f, x, a)` calls. If already wired, the gap may just be UX: add a "Limits" sub-section in CalculateTab with a destination-point input (including `Infinity`, `-Infinity`, one-sided `+`/`-`), call the CAS, and render the result with KaTeX. If the CAS doesn't support limits, evaluate numerically as `f(a ± ε)` with decreasing ε and display the limit estimate.
**Invariants:** No React in math/calc engine code. Styles via `C`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The plot in CalculateTab renders function curves. A "constant" is just a function `y = c` (a horizontal line). Accept numeric constants in the equation list by detecting when the parsed expression has no variables: if so, emit a horizontal line at `y = value` rather than evaluating across x. Alternatively, add an "Add constant line" input that appends an `hline` layer styled consistently with the function curves. The PlotBuilder already has `hline` as a geom — reuse its rendering pattern.
**Invariants:** No external UI libraries. `C` for styles.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 (current CDN version) does not natively support a third Y axis. The practical approach is a dual-Y layout: two overlapping layers with independent scales, with the second Y axis drawn on the right side. Observable Plot supports this via a secondary `fy` or by composing two plots with `Plot.plot({…})` sharing x but using different `y` domains and drawing the second Y scale manually. Scope carefully — this is a significant PlotBuilder change. A phased approach: (1) allow assigning a layer to "right Y axis" in layer config; (2) normalise both Y series to [0, 1] as a simpler fallback with a NOTE that scales are normalised.
**Invariants:** No external UI libraries beyond the existing Observable Plot CDN.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** `VariableSelector.jsx` renders a list of variable chips. Add a text `<input>` filter above the list (like a `useState("")` filter on column names). Chips whose `name.toLowerCase().includes(query)` are shown; others are hidden. No backend needed — purely client-side filtering on the already-loaded headers array. Style consistently with the existing chip style using `C`.
**Invariants:** Inline styles via `C`; no UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set, PlotBuilder renders a legend. The feedback asks for richer legend labels (e.g., group sizes, means per color group). In PlotBuilder's legend rendering: when `aes.color` is a categorical variable, compute group size from `rows` and append it to the legend label as `label (n=N)`. For a continuous color, show the ramp's min/max values. Keep this additive — the existing legend label still shows the level name.
**Invariants:** No UI libraries. `C` for styles.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** `History.jsx` renders pipeline steps. Add a small "↗" icon (or make the step label itself a link) that dispatches a `NAVIGATE_TO_STEP_SOURCE` action. `WranglingModule.jsx` handles it by mapping step `type` to a tab: e.g., `log/sq/std/dummy` → FeatureTab, `filter/fill_na/normalize_cats` → CleanTab, `group_summarize/pivot_longer` → ReshapeTab, `join/append` → MergeTab. Then programmatically set the active tab and (optionally) the sub-section. A lookup table `STEP_TYPE_TO_TAB` keeps this maintainable.
**Invariants:** No UI libraries. Pipeline steps must replay on `rawData` — the navigation is view-only.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step: given an input column (country name or ISO code) and a target format (iso2/iso3/name), look up in a small bundled JSON crosswalk table (~250 rows, ~15KB). Implement as a new case in `runner.js` and registry entry in `registry.js`; expose in `FeatureTab.jsx` under Formatting → "Country Code". Fuzzy matching can reuse the existing `fuzzyGroups` logic from `wrangling/utils.js` for name → ISO lookups.
**Invariants:** New pipeline step must be added to both `runner.js` AND `registry.js` (registry must stay in sync). Pure JS — no React in the runner.

---

## UX / Design

### UX · 2026-07-17
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`, `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Introduce a session-scoped "Named Lists" store in `sessionState.jsx` (`{ [listName]: string[] }`). Users define a list (a set of strings, e.g. country names) from the Clean → Filter panel or a new "Lists" sub-tab. `FilterBuilder` then offers a "from list" option in the `in` operator picker. Lists persist in IndexedDB per `pid`. This design avoids a new tab: lists are authored contextually at the point of use and stored in existing session infrastructure.
**Invariants:** Persistence via `indexedDB.js`. `predicate.js` remains the single owner of the condition language — the list is resolved to an array before being passed to `evalPredicate`.

---

### UX · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models, for all estimators
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** The existing `RDDPlot` already has binned scatter logic. Expose a "Binned scatter" panel under the model-level ◈ Plot Builder for OLS, FE, and IV results. Parameters: bin count (default 20), x variable (defaults to the first regressor), whether to use residuals or raw values, and whether to overlay the fitted line. This reuses the existing PlotBuilder layer system — add a `binnedScatter` geom that pre-bins `rows` in JS before passing to Observable Plot.
**Invariants:** `src/math/` functions must remain pure JS (no React imports). PlotBuilder geom list must stay in `PlotBuilder.jsx`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** The DuckDB-WASM integration (Fase X4) is code-complete per BugTriage but browser validation is pending. Verify the OPFS Parquet cache path end-to-end with a 21MB CSV: (1) first load converts to Parquet via `COPY … TO … (FORMAT PARQUET)` — confirm `extensions.duckdb.org` is in `connect-src` in `vercel.json` (the known CSP trap documented in CLAUDE.md); (2) on reload, `loadFromOPFSKey` restores without re-parsing; (3) all module tabs use DuckDB path (no 500-row cap except UI preview). The CSP entry for `extensions.duckdb.org` was specifically flagged in CLAUDE.md as a known crasher — check this first.
**Invariants:** DuckDB-WASM performance strategy: never pull the full Arrow result into JS objects; display limit ≠ computation limit.

---

## Other

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 — China,CHN,2010,2425.47...
**File:** `src/DataStudio.jsx`, `src/services/data/parsers/` (CSV branch)
**Suggestion:** The error suggests a row that contains a field whose content or quoting violates the parser's expectation (e.g., an embedded newline, an unescaped quote, or a value with commas). BugTriage's "fix-later" entry for malformed CSV rows suggests the approach: catch the parse error, identify the offending line number, and surface a recovery UI offering "Skip this row and continue" or "Show me the raw line." DuckDB's CSV reader has an `IGNORE_ERRORS` mode that skips bad rows while logging them — use it as the fallback reader when the primary parse fails, then surface skipped rows in a banner.
**Invariants:** Non-destructive pipeline — `rawData` must remain the original parsed rows. Never mutate in place.

---

## Pending browser validation (code-complete, not yet confirmed)

| Item | File | Status |
|------|------|--------|
| Plot Builder resize + PNG export | `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` — Franco validation pending |
| Y~Ŷ / Residual-Fitted / Q-Q plot for OLS | `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` — Franco validation pending |
| Spatial heatmap | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` | code-complete `5e105529` — Franco validation pending |
| P-value correctness (`tCDF`/`betainc`) | `src/math/LinearEngine.js` | `216b1ecb` — likely fixed, awaiting Franco confirmation |
| HC2 replication in R/Stata export | `src/services/export/rScript.js`, `stataScript.js` | code-complete 2026-07-18 — Franco validation pending |

## Deferred / fix-later (not blocking, tracked for post-launch)

| Item | BugTriage date | File |
|------|---------------|------|
| Malformed CSV row aborts import (no recovery UI) | 2026-06-14 | `src/DataStudio.jsx`, `src/services/data/parsers/` |
| R-style `%in% c(...)` filter expression syntax | 2026-06-14 | `src/components/wrangling/CleanTab.jsx` |
| RDD `summary(rdrobust)` compact table | 2026-06-14 | `src/components/modeling/ModelPlots.jsx` |
| Spatial plot layer row-filter control | 2026-06-14 | `src/components/tabs/spatial/plot/` |
| Editable LaTeX coefficient label names | 2026-06-14 | `src/ReportingModule.jsx` |

---

## Suggested action order

1. **[CRITICAL] Confirm P-value fix browser-side** — Three P-value bug reports (2026-07-22, 2026-07-28 ×2) are all "likely fixed" via `216b1ecb`; Franco should verify in-app before any other model work. False significance reporting is the highest-trust risk for an econometrics tool. (`src/math/LinearEngine.js`)
2. **[HIGH] Fix Stata workbench end-to-end** — The Stata export is broken for multiple estimators, a blocking issue for the primary research workflow. Start with `CodeEditor.jsx` tab routing and `stataScript.js` per-estimator SE emission. (`src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`)
3. **[HIGH] Simulate replication script** — The Simulate tab is used for teaching (LMU tutorials); a script that only reproduces the DGP draw and ignores the analysis is not replicable. Add `simulateScript.js` covering the MC loop and sample tests. (`src/components/tabs/SimulateTab.jsx`)
4. **[MEDIUM] Variable searcher in VariableSelector** — High-frequency UX friction for datasets with many columns; 1–2 hour fix in `VariableSelector.jsx`. (`src/components/modeling/VariableSelector.jsx`)
5. **[MEDIUM] LaTeX table column overflow** — LaTeX exports are truncated for multi-model comparisons; add `\resizebox` wrapper in `latexTable.js`. (`src/services/export/latexTable.js`)
6. **[MEDIUM] Time series label controls** — Axis/title labels missing from the time series sub-tab; follow the PlotBuilder labels panel pattern in `ExplorerModule.jsx`. (`src/ExplorerModule.jsx`)
7. **[MEDIUM] Unfinished FeatureTab work lost on navigation** — Add sessionStorage draft persistence to `FeatureTab.jsx`. (`src/components/wrangling/FeatureTab.jsx`)
8. **[LOW] Multiple hypothesis tests with R export** — Extend `CoefficientTestPanel.jsx` to stack and save tests, then emit a combined R snippet. (`src/components/modeling/CoefficientTestPanel.jsx`)
9. **[LOW] Groups for pinned models/plots** — Extend `artifactOrder.js` with a groups map; add "New group" in `ModelBufferBar.jsx`. (`src/services/Persistence/artifactOrder.js`)
10. **[LOW] Guide tour update** — Walk `TOUR_STEPS` in `HelpSystem.jsx` against the current UI and update stale steps. (`src/components/HelpSystem.jsx`)
