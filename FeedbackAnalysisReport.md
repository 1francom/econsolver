# Feedback Analysis Report — 2026-06-14

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **5 open bugs** · **17 open features** · **0 open UX/Design** · **1 open performance** · **1 open other**
- Total open items: **24**
- BugTriage cross-reference complete — items recorded as `fixed` in BugTriage are excluded even when ClaudeFB.md has no strikethrough. One discrepancy noted: "The graph does not fit the screen" (2026-05-11) is unstruck in ClaudeFB.md but marked `fixed` in BugTriage; treated as resolved. Full housekeeping list at end of report.

---

## Bugs

### critical · Feedback form (XSS #1) · 2026-06-13
**Feedback:** ⚠️ SECURITY EVENT: XSS injection attempt submitted via feedback form — JavaScript payload in description field (proof-of-concept alert injection). Payload redacted.
**File:** `src/services/feedback/feedbackService.js` (line 19 — `description: description.trim()` stored raw) · `src/components/feedback/FeedbackModal.jsx` (textarea collects unrestricted input)
**Suggestion:** In `feedbackService.js` line 19, sanitize `description` before the Supabase insert: strip HTML tags (e.g. `description.replace(/<[^>]*>/g, "").slice(0, 2000)`). Also add a `maxlength="2000"` attribute to the FeedbackModal textarea to prevent oversized payloads in the browser. Additionally, add a Supabase Postgres `CHECK` constraint on the `feedback` table rejecting rows whose `description` matches `/<script|javascript:|onerror=/i`. React renders text nodes safely by default, but the Supabase admin dashboard and any future log renderer using `innerHTML` would execute the stored payload.
**Invariants:** No React in `src/math/` or `src/core/` — not applicable here. The sanitization call must live in `feedbackService.js` (client-side, before network egress), not inside the modal component.

---

### critical · Feedback form (XSS #2) · 2026-06-13
**Feedback:** ⚠️ SECURITY EVENT: XSS injection attempt submitted via feedback form — HTML `<script>` and `<img onerror>` tags in description field. Payload redacted.
**File:** `src/services/feedback/feedbackService.js` · `src/components/feedback/FeedbackModal.jsx`
**Suggestion:** Same root cause and same fix as XSS #1 — both events share the unsanitized `description` field stored in Supabase. A single `description.replace(/<[^>]*>/g, "")` call in `feedbackService.js` line 19 covers both. Two payloads are already stored in the Supabase `feedback` table; consider a one-off SQL `UPDATE feedback SET description = '' WHERE description ~* '<script|onerror'` to purge them.
**Invariants:** None conflicting.

---

### high · Model (OLS residual plots) · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/math/LinearEngine.js` (line 219 — `resid: null, Yhat: null` explicitly set on DuckDB suff-stats path) · `src/components/modeling/ResidualPlots.jsx` (lines 191, 318, 454 — silently returns `null` when `resid` is falsy) · `src/components/ModelingTab.jsx` (lines 2299–2301 — renders `ResidualVsFitted` / `QQPlot` unconditionally, unaware of null)
**Suggestion:** Root cause: when OLS routes through the DuckDB suff-stats SQL path (n ≥ 50 k), `LinearEngine` explicitly sets `resid: null, Yhat: null` (the comment at line 158 acknowledges this). `ResidualPlots` returns `null` silently. Fix in `ModelingTab.jsx` (UI layer, not math): after `runOLSFromSuffStats` returns and before rendering, call the already-implemented `duckdbResiduals.sampleResiduals` to obtain a ≤ 5 k-row sample, then attach the sample as `r.resid` / `r.Yhat`. Add a small banner inside each plot: "Sampled residuals (n = 5,000 of N)" so the user understands the display is approximate. Do not change `LinearEngine.js` — the null is intentional for the math layer.
**Invariants:** All math must stay in `src/math/` — the sampling call and result attachment must be in `ModelingTab.jsx` only. `src/math/LinearEngine.js` must remain React-free.

---

### medium · Spatial (Plot tab heatmap) · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx` (lines 136, 190 — heatmap KDE branch) · `src/components/tabs/spatial/plot/geo.js` (lines 24, 50 — heatmap layer factory) · `src/components/tabs/spatial/plot/GeoLayerConfig.jsx` (line 104, 256, 262 — heatmap config UI)
**Suggestion:** The heatmap layer uses a `kde2d` call before rendering. Without more detail from the user, investigate in order: (a) does `kde2d` throw or return empty when `latCol`/`lonCol` are not yet set (default empty string in `geo.js` line 50)? — add a guard at `GeoPlotCanvas.jsx` line 136 before `kde2d` that checks `ly.latCol && ly.lonCol && rows.length > 0`; (b) does `buildColorScale` return an empty legend when all densities are 0? — check for degenerate KDE output; (c) is there a CDN load failure for any Observable Plot dependency? — check browser console. Surface a descriptive error message to the user rather than a blank canvas.
**Invariants:** Inline styles via `C` object only — no external CSS.

---

### medium · Model (PlotBuilder in ModelingTab) · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx` · `src/components/ModelingTab.jsx` (lines 2049, 3190–3196 — `<PlotBuilder>` mount sites)
**Suggestion:** `PlotBuilder` already uses a `ResizeObserver` for responsive SVG sizing, but inside `ModelingTab` it is mounted in a fixed-height container. Add a vertical drag handle: a `div` with `cursor: row-resize` style and a `pointerdown → pointermove` listener that updates a `plotHeight` state variable used on the container. For PNG export, pass explicit `width` and `height` options to the Observable Plot `render` call (e.g. 1200 × 800) independent of the visible panel size — and apply the same dark-background-fill stripping that the SVG export path already does.
**Invariants:** Inline styles via `C` only — no external CSS files or libraries.

---

## Features

### Model · 2026-05-11
**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength
**File:** `src/math/IV2SLSEngine.js` · `src/math/CausalEngine.js` · `src/components/modeling/results/IVResults.jsx`
**Suggestion:** No Anderson-Rubin implementation exists (confirmed by grep). The AR CI inverts an F-test: for a grid of candidate β₀ values, regress `Y − β₀·D` on `Z` and controls (reduced form), compute the F-stat, and collect all β₀ where F < F_{α}. Add `runAndersonRubinCI(Y, D, Z, X, alpha, gridSize)` to `IV2SLSEngine.js` (pure JS). Wire the result into `IVResults.jsx` as a collapsible "Weak-instrument robust CI" section toggled by a button, so it runs on demand (not on every estimation). Expose the option in `ModelConfiguration.jsx` next to the instrument list.
**Invariants:** `src/math/` must stay React-free. SE type is not an argument to the AR CI — it is a test-inversion procedure; document this clearly.

---

### Model · 2026-05-11
**Feedback:** Add multiple statistics tests
**File:** `src/math/SampleTests.js` · `src/components/tabs/statsim/SampleTestPanel.jsx`
**Suggestion:** Under-specified — clarify scope with the user before implementing. Likely refers to extending pre-model sample tests. Good candidates: two-sample independent t (pooled and Welch), paired t, Mann-Whitney U, Kolmogorov-Smirnov two-sample, one-way F / ANOVA, and chi-squared goodness-of-fit. Add the math as pure functions in `SampleTests.js`; extend `SampleTestPanel.jsx`'s test-type selector with the new entries.
**Invariants:** Zero React imports in `SampleTests.js`.

---

### Simulate · 2026-05-18
**Feedback:** Add resampling, bootstrapping, sampling to the mean
**File:** `src/math/Resampling.js` · `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** `Resampling.js` already exists (permutation tests implemented). Add `bootstrapCI(data, statFn, B = 2000, alpha = 0.05)` that draws `B` i.i.d. samples with replacement, computes the statistic each time, and returns `{ distribution, ciLow, ciHigh, se }`. Wire it into `StatWorkspace.jsx` as a "Bootstrap" mode alongside the existing permutation section — let the user choose a column, a statistic (mean/median/sd/min/max), and B. Reuse the `MCHistogram` component from `SimulateTab.jsx` for the distribution display.
**Invariants:** Zero React in `Resampling.js`. Persist results to IndexedDB if needed — never localStorage.

---

### Explore · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/pipeline/runner.js` · `src/components/wrangling/CleanTab.jsx` · `src/pipeline/registry.js`
**Suggestion:** Extend the existing `filter` step type with an `in` operator (and its negation `not_in`). Step payload: `{ type: "filter", col: "regionname", op: "in", values: ["Spain (Espana)", "Basque Country (Pais Vasco)"] }`. In `runner.js`, add `case "in": return values.map(String).includes(String(row[col]))`. In `CleanTab.jsx`'s `FilterBuilder`, add an "is one of" operator and render a tag-input (comma-separated editable chips) for the value field. No new step type needed — `registry.js` only needs the operator documented in an inline comment.
**Invariants:** Pipeline steps must replay on `rawData` — never mutate data in place. Registry must stay in sync with runner.js.

---

### Spatial (Plot tab) · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` (line 177 — layer type selector) · `src/components/tabs/spatial/plot/geo.js` (line 50 — layer factory) · `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`
**Suggestion:** The Leaflet Map tab already supports a "grid" layer via `SpatialEngine.assignGrid`; the SVG/Observable Plot tab (`SpatialGeoPlot`) does not. Add `"grid"` to the layer type list in `SpatialGeoPlot.jsx` line 177, add a default grid config in `geo.js` line 50 (cellSize, clipBorder), and in `GeoPlotCanvas.jsx` render grid cell polygons using the existing WKT-to-SVG path (same as the polygon layer branch) — reuse cells already produced by `SpatialEngine`. Expose a cell-size / H3-resolution slider in `GeoLayerConfig.jsx`.
**Invariants:** `src/math/SpatialEngine.js` must remain React-free. Inline styles via `C` only.

---

### Model (RDD) · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js` (lines 577–579 — `varNames` using hardcoded display strings) · `src/components/ModelingTab.jsx` · `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Create `src/components/modeling/results/RDDResults.jsx` rendering an rdrobust-style summary block: method, polynomial order, kernel, bandwidth h, N left/right of cutoff, effective N, LATE estimate, SE, t-stat, p-value, 95% CI. All fields are already returned by `CausalEngine.runSharpRDD` — the work is purely UI. Thread the actual `runningCol` and `treatCol` names from the call site into `CausalEngine.js` so lines 577–579 use real column names instead of the hardcoded "D (treatment)" / "running − c" literals.
**Invariants:** Column name threading must be via function arguments — no React state or imports in `CausalEngine.js`. Inline styles via `C`.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept
**File:** `src/math/LinearEngine.js` · `src/components/ModelingTab.jsx` · `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add an optional `{ noIntercept: boolean }` flag to `runOLS` and `runWLS` in `LinearEngine.js`. When true, omit the leading ones column from the design matrix and adjust df = n − k (not n − k − 1). Expose as a checkbox "Suppress constant (−1)" in `ModelConfiguration.jsx` and thread via `ModelingTab.estimate()` → engine call. Add a footnote in the result display noting that R² without an intercept is not comparable to the standard R².
**Invariants:** `seType` must still be passed explicitly — never hardcoded. `src/math/LinearEngine.js` stays React-free.

---

### Spatial · 2026-05-30
**Feedback:** Geometry transformations and show EPSG
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx` · `src/components/tabs/spatial/shared/crs.js`
**Suggestion:** `CRSTransformSection.jsx` already wraps `loadProj4` and `PRESET_CRS`. Extend the output to include a new column `_epsg` with the target EPSG code appended to each result row. In the section UI, show the EPSG badge alongside the result preview. Also detect the input CRS: when a WKT geometry column is selected, use `isProjectedWKT` from `crs.js` to hint at the source projection and display it to the user before transformation.
**Invariants:** CRS output must go through a pipeline step (`add_column` or `vector_assign`) in `runner.js` — never mutate `rawData`.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` · `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a collapsible "Spatial filter" panel in `GeoLayerConfig.jsx` with: (a) bounding-box min/max lat/lon inputs; (b) a paste-WKT field for polygon filters. Before rendering, filter rows in `GeoPlotCanvas.jsx` whose coordinates fall inside the bounding box / polygon using `SpatialEngine.js`'s existing point-in-polygon logic. This is a display-only filter — it does not emit a pipeline step and does not modify `rawData`.
**Invariants:** No mutation of `rawData`. Inline styles via `C` only.

---

### Data · 2026-06-01
**Feedback:** Add an option to edit/assign column names directly in the Data Viewer
**File:** `src/ExplorerModule.jsx` · `src/pipeline/runner.js` (existing `rename` step type)
**Suggestion:** In the data table column header row in `ExplorerModule.jsx`, add a double-click handler that replaces the `<span>` with a controlled `<input>`. On `Enter`/blur, emit a `rename` pipeline step (`{ type: "rename", from: oldName, to: newName }`) via the existing `onPipelineStep` callback — this makes the rename replayable and undoable. Add a hover tooltip "Double-click to rename". The `rename` step type already exists in `runner.js`; no change to the pipeline is needed.
**Invariants:** Rename must flow through the `rename` pipeline step — never mutate `rawData` column names directly. IndexedDB for persistence — never localStorage.

---

### Simulate · 2026-06-02
**Feedback:** Add different types of quantiles like in R, but it might not be necessary
**File:** `src/math/Resampling.js` · `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** Low priority per the user's own qualifier. R implements 9 quantile types (Hyndman-Fan 1996). Add a `quantile(arr, p, type = 7)` helper to `Resampling.js` covering types 1, 6, 7, 8 (the four most common in applied work). Expose a "Quantile type" dropdown in `StatWorkspace.jsx` in the distribution/percentile sections only. Types differ only by the interpolation formula — 20–30 lines of pure JS.
**Invariants:** Zero React in `Resampling.js`.

---

### Spatial · 2026-06-03
**Feedback:** Add geometry to plot points, not just lat and lon
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx` (line 104 — point/heatmap guard on lat/lon) · `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx` · `src/components/tabs/spatial/shared/wkt.js`
**Suggestion:** Currently a point layer requires `latCol`/`lonCol`. Add a "WKT mode" toggle in `GeoLayerConfig.jsx` for the point layer (cf. existing `ly.mode === "wkt"` check at line 104). When active, auto-detect a WKT column via `guessWktCol` from `guess.js`. Parse centroid coordinates from WKT cells using `wktToLeaflet` (already in `wkt.js`) and render as point markers in `GeoPlotCanvas.jsx`. Also allow rendering the full geometry (polygon/line) as SVG paths using `parseWktRings` — this overlaps with the F12 request above and can be implemented together.
**Invariants:** Inline styles via `C` only.

---

### Calculate · 2026-06-03
**Feedback:** Add a "show steps button" for integrals, derivatives and optimizations
**File:** `src/components/tabs/CalculateTab.jsx` · `src/math/cas/sympyBackend.js`
**Suggestion:** Check whether `sympyBackend.js` exposes intermediate steps from the symbolic engine. If not, add an AI fallback: when the user clicks "Show steps", call `callClaude` in `AIService.js` with a prompt requesting a step-by-step derivation in LaTeX. Display the response in the existing KaTeX renderer as a collapsible "Derivation" panel. For standard integrals/derivatives this is a fast, cheap call (Haiku model).
**Invariants:** All Anthropic API calls must go through `AIService.js` only — never add a raw `fetch` to the Anthropic API inside `CalculateTab.jsx`. `dangerouslySetInnerHTML` is already used for KaTeX in `CalculateTab.jsx` — the steps output must go through the same KaTeX pipeline, not raw HTML injection.

---

### Model (Pinned models) · 2026-06-04
**Feedback:** Persistence across sessions of pinned models
**File:** `src/services/Persistence/indexedDB.js` · `src/services/Persistence/trimResult.js` · `src/services/modelBuffer.js` · `src/components/modeling/ModelBufferBar.jsx` · `src/DataStudio.jsx`
**Suggestion:** `indexedDB.js` already has a `model_buffer` store (introduced in v9 per CLAUDE.md). The missing wiring: (1) add `saveModelBuffer(pid, models)` / `loadModelBuffer(pid)` helpers to `indexedDB.js`; (2) call `saveModelBuffer` from `ModelBufferBar.jsx` on every pin/unpin action (serialize each model via `trimResult` first — strips raw data for privacy + size); (3) call `loadModelBuffer(pid)` on `DataStudio.jsx` or `ModelingTab.jsx` mount and rehydrate `pinnedModels` state. Display a note in the UI: "Estimates persist across sessions — raw data does not."
**Invariants:** IndexedDB only — never localStorage. Always `trimResult` before persisting — never store raw data arrays.

---

### Calculate · 2026-06-06
**Feedback:** Add derivatives with endogenous functions (e.g. f(x) = 2x² − c(x), compute dc/dx symbolically)
**File:** `src/components/tabs/CalculateTab.jsx` · `src/math/cas/sympyBackend.js`
**Suggestion:** Sympy supports user-defined symbolic functions via `Function('c')(x)` — `diff(f, x)` then returns `4*x + Derivative(c(x), x)`. Check whether `sympyBackend.js` exposes this path. If the backend does not support it, route to AI: pass the expression to `callClaude` in `AIService.js` (model: `claude-haiku-4-5-20251001` for speed) with a prompt requesting the symbolic derivative treating single-letter calls as unknown functions. Render via KaTeX.
**Invariants:** All AI calls through `AIService.js` only. KaTeX pipeline for all rendered math — no raw HTML.

---

### Model (LaTeX table / RDD) · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D × (running − c)" regardless of actual column names
**File:** `src/math/CausalEngine.js` (lines 577–579 — hardcoded `varNames`) · `src/services/export/latexTable.js` · `src/ReportingModule.jsx`
**Suggestion:** Two-part fix: (1) In `CausalEngine.js` lines 577–579, use the actual `runningVar` and `treatCol` arguments (pass them into `runSharpRDD`) as the base for `varNames` — e.g. `polyNames[0] = runningVar + " − c"`. (2) In `ReportingModule.jsx` / `latexTable.js`, add a collapsible "Variable labels" section with one editable `<input>` per regressor; pass the label map into `buildStargazer` to override raw column names in the LaTeX output.
**Invariants:** Label map is UI-only metadata — never mutate `EstimationResult.varNames`. `CausalEngine.js` stays React-free (pass column names as function arguments).

---

### Simulate · 2026-06-08
**Feedback:** Add studentization for permutation tests
**File:** `src/math/Resampling.js` (lines 214, 300–303, 378–380 — studentized test is documented in comments but not surfaced in UI) · `src/components/tabs/statsim/StatWorkspace.jsx` (line 1322 — UI comment mentions studentization)
**Suggestion:** `Resampling.js` already contains the mathematical rationale and (based on the comments) a partial implementation of the studentized statistic. The gap is that no UI toggle exposes it. Add a "Studentize" checkbox to the permutation test panel in `StatWorkspace.jsx`, and expose the studentized statistic path in `Resampling.js` as a named export (e.g. `permutationTestStudentized`). Cap at B = 199 permutations for browser performance; consider `setTimeout`-chunked iteration to avoid blocking the main thread.
**Invariants:** Zero React in `Resampling.js`. Long-running loops (B > 500) must not block the main thread — use chunked iteration or a Web Worker.

---

## Performance

### high · Data (Large CSV crash) · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js` · `src/pipeline/duckdbRunner.js`
**Suggestion:** The 2026-06-10 fix routed large `.dta` files through DuckDB + OPFS Parquet, but large CSV/TSV uploads still materialize the full JS object array, causing OOM crashes. In `DataStudio.jsx`, apply the same 10 MB threshold check to CSV/TSV: register them in DuckDB, cache as Parquet in OPFS, and reduce to a 500-row preview for display — same as the `.dta` path. Also audit `duckdbRunner.js` step coverage: only 11 of 35 step types use SQL; the remaining 24 fall back to JS on the full array and will OOM at 900 k rows. Priority step push-downs: `add_column`, `filter`, `normalize_cats`, `group_transform`, `pivot_longer`. Also verify `vercel.json` has `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` to enable `SharedArrayBuffer` for threaded DuckDB-Wasm.
**Invariants:** Never truncate `rawData` before passing to `runner.js`/`duckdbRunner.js` — display limit ≠ computation limit. No server-side computation (privacy-first). OPFS caching per CLAUDE.md rule 3.

---

## Other

### medium · Data (CSV parse error) · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated)
**File:** `src/DataStudio.jsx` (CSV load path — DuckDB `read_csv_auto` call)
**Suggestion:** The "CSV Error on Line: 1583" message is from DuckDB's strict CSV parser. Likely cause: an unquoted embedded comma in a numeric field formatted with a thousands separator (e.g. `2,425.47`), or a BOM/encoding artifact. In `DataStudio.jsx`, wrap the DuckDB CSV import in a try/catch; on failure, retry with `IGNORE_ERRORS=TRUE` and surface a warning banner showing how many rows were skipped and the first offending line. Never expose raw DuckDB error strings directly to the user — wrap them in a friendly message: "Could not parse N rows — see details." Optionally pre-process through PapaParse (which handles embedded commas correctly) before handing off to DuckDB.
**Invariants:** Retry logic stays in `DataStudio.jsx` (UI boundary) — not inside `duckdb.js`. Full dataset must still reach `runner.js`/`duckdbRunner.js` after recovery.

---

## Housekeeping note

The following **19 items** appear open in `ClaudeFB.md` (no strikethrough, no ✓) but are recorded as `fixed` in `BugTriage.md`. A housekeeping pass to add ~~strikethrough~~ + ✓ in `ClaudeFB.md` is recommended to keep the files in sync:

| Date | Module | Item |
|------|--------|------|
| 2026-05-11 | Explore | The graph does not fit the screen |
| 2026-05-13 | Data | .dta files are wrong loaded and readed |
| 2026-05-13 | Spatial | Grids: adjust the size of border grids by cutting them |
| 2026-05-20 | Spatial | Grids do not work |
| 2026-05-20 | General | Dataset deletion — variables should disappear from all modules |
| 2026-05-21 | Model | p-value bug — t=0.49 but p<0.001 (URGENT) |
| 2026-05-21 | Model | Variables auto-reestimate on selection/elimination |
| 2026-05-21 | Model | Add option to clean/reset current model |
| 2026-05-22 | Calculate | Function graph does not start from 0 |
| 2026-05-25 | Calculate | Only 1 equation is plotted |
| 2026-05-19 | Explore | Color references in PlotBuilder for `color = X` |
| 2026-05-19 | Simulate | Monte Carlo for single variables |
| 2026-05-30 | Spatial | Adjust size respectively to the spatial unit(s) |
| 2026-05-31 | Data | Parse error: schools_with_coordinates.xlsx — SheetJS CDN |
| 2026-05-31 | Data | OECD proxy error 406 |
| 2026-06-01 | Data | Not all .dta columns recognized — replicated entries without names |
| 2026-06-02 | Calculate | Crash on LaTeX export |
| 2026-06-02 | Model | AI interpretation does not adapt to estimator (Poisson) |
| 2026-06-06 | Spatial | Geocoding address resolution quality (BugTriage: "works — enhancement only; Franco confirmed 2026-06-10") |

---

## Suggested action order

1. **[critical] Sanitize feedback `description` before Supabase insert** — `src/services/feedback/feedbackService.js` line 19: strip HTML tags. Two XSS payloads are already stored in the database; purge them with a one-off SQL UPDATE.
2. **[high] Fix OLS residual/Q-Q plots on DuckDB suff-stats path** — call `duckdbResiduals.sampleResiduals` after `runOLSFromSuffStats` in `ModelingTab.jsx`; attach as `r.resid`/`r.Yhat`; no change needed in `LinearEngine.js`.
3. **[high] Route large CSV/TSV through DuckDB + OPFS** — `DataStudio.jsx`: apply the same 10 MB threshold that `.dta` already uses; prevents 900k-row OOM crashes for the primary data format.
4. **[medium] Investigate and fix heatmap** — `GeoPlotCanvas.jsx` line 136: add null-guard before `kde2d`; surface descriptive error when lat/lon columns are missing.
5. **[medium] PlotBuilder resize handle + PNG export dimensions** — add drag-resize in `ModelingTab.jsx` container; pass explicit width/height to Observable Plot on PNG export.
6. **[medium] Pinned model IndexedDB persistence** — `indexedDB.js`: add `saveModelBuffer`/`loadModelBuffer`; wire from `ModelBufferBar.jsx` on pin/unpin and from `DataStudio.jsx` on mount; the v9 IDB store already exists.
7. **[medium] Thread real column names into RDD `varNames`** — `CausalEngine.js` lines 577–579: use `runningVar` + `treatCol` as the base; eliminates hardcoded "D (treatment)" / "running − c" in LaTeX exports.
8. **[medium] CSV parse error recovery** — `DataStudio.jsx`: retry DuckDB CSV import with `IGNORE_ERRORS=TRUE` on failure; surface a user-friendly row-skip warning.
9. **[feature] Intercept-free estimation** — `LinearEngine.js`: `noIntercept` flag; `ModelConfiguration.jsx`: checkbox.
10. **[feature] Anderson-Rubin CI for 2SLS** — `IV2SLSEngine.js`: pure-JS grid inversion; `IVResults.jsx`: collapsible on-demand section.
