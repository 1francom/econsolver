# Feedback Analysis Report — 2026-06-21

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **1 critical open bug** · **4 high bugs (code-complete, browser validation pending)** · **2 medium fix-later bugs** · **6 open/new features** · **0 standalone UX/Design** · **0 standalone performance (folded into bugs)**

> **ClaudeFB bookkeeping note:** Several ClaudeFB entries lack strikethrough despite being confirmed fixed in BugTriage or the X5 reconciliation (2026-06-14). Affected resolved items: Explore graph fit, `.dta` loading, grid clipping, dataset-delete stale vars, p-value URGENT, auto-reestimate, clear model, Calculate graph/multi-equation, PlotBuilder color mapping, Montecarlo single-var, geocoding quality, XSS probes, 2SLS Poisson interpretation, Anderson-Rubin CI, multiple stats tests, resampling/bootstrap, geometry/EPSG transforms, spatial auto-fit, show-steps, pinned model persistence, symbolic derivatives, studentized permutation tests. These are **resolved** — the ClaudeFB entries just need striking through.

---

## Bugs

---

### critical · Model / Pipeline · 2026-06-17

**Feedback:** Pipeline only applies if dataset is also selected in Clean Module

**File:** `src/App.jsx` (lines ~2810–2848 — `handleComplete`, `tabOutput`, `activeDatasetIds`), `src/WranglingModule.jsx`

**Suggestion:** `tabOutput(tab)` resolves to `outputs[activeDatasetIds[tab] ?? activeDatasetId]`. `handleComplete` (line 2846) keys on `tabDsId("clean")`, not `tabDsId("model")`. If the user loads a dataset and navigates directly to the Model tab without clicking "→ Analyze" in Clean, `outputs` never has an entry under the model-tab key, so `modelCleanedData` falls back to raw data — pipeline never applied. Fix by making `handleOutputReady` (line 2852) write the output under every per-tab key that resolves to that dataset, OR by having `tabOutput("model")` fall back to the clean-tab output for the same `datasetId` before falling back to `rawToCleaned`.

**Invariants:** Pipeline must always replay on `rawData` via `runner.js` — never mutate in place. The fix must not bypass the pipeline runner or cache stale pipeline output across dataset switches.

---

### high · Model · 2026-06-10

**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems

**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Code-complete as of commit `35f4b5a6` (native vertical resize, PNG captures user-selected dimensions). Browser validation by Franco is the only remaining step. Strike through ClaudeFB entry once confirmed.

**Invariants:** None additional.

---

### high · Model · 2026-06-11

**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS

**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Code-complete as of `35f4b5a6` — plots render from frozen `result.spec` so sidebar edits cannot mutate them. Browser validation pending. Strike through in ClaudeFB once confirmed.

**Invariants:** None additional.

---

### high · Model · 2026-06-14

**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.

**File:** `src/components/ModelingTab.jsx`

**Suggestion:** Code-complete as of `35f4b5a6` — OLS/WLS display frozen to `result.spec`; pinned model restore path included. Browser validation pending. Strike through in ClaudeFB once confirmed.

**Invariants:** None additional.

---

### medium · Spatial · 2026-06-13

**Feedback:** Heatmap must be fixed

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`

**Suggestion:** Code-complete as of `5e105529` — Heatmap restored as a Leaflet layer with KDE cells, palette/opacity controls, legend, HTML export, and 5k-point cap. Browser validation pending Franco. Strike through in ClaudeFB once confirmed.

**Invariants:** None additional.

---

### medium · Data · 2026-05-10 (BugTriage: fix-later 2026-06-14)

**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 · Original Line: China,CHN,2010,2425.47… — malformed CSV row aborts import instead of surfacing recovery options

**File:** `src/DataStudio.jsx` (parseFile CSV path), `src/services/data/parsers/`

**Suggestion:** Wrap the CSV parse step in a per-row try/catch; collect offending line numbers and surface a "Skip N malformed rows and continue?" recovery modal before aborting. Return a `_parseWarnings` array in the loaded dataset metadata so the Data Viewer can display a dismissible warning banner. Never truncate silently — the user must see which rows were dropped and why.

**Invariants:** Full dataset must remain in DuckDB for pipeline and estimation (performance Rule 5). Only skip the offending rows in the UI; do not truncate `rawData` before `runner.js`.

---

### medium · Performance · 2026-05-08

**Feedback:** Big datasets generate crashes (21MB) over 900k rows

**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`

**Suggestion:** Fase X4 is code-complete (`.dta` over 10 MB routed to DuckDB/OPFS Parquet). Browser benchmark with the 21 MB / 900k-row fixture is the only pending step. Other large-format files (`.rds`, `.xlsx`, `.shp`) still bypass DuckDB and are flagged as future work in BugTriage. Once the benchmark passes, strike through the ClaudeFB entry and add the remaining formats to the DuckDB routing plan.

**Invariants:** Full dataset must remain available for pipeline runner and estimation — display is capped at 200–500 rows (DuckDB strategy Rule 5). Never truncate `rawData` before passing to `runner.js`.

---

## Features

---

### Clean · 2026-05-19 (BugTriage: fix-later 2026-06-14)

**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option — R-style membership syntax not accepted

**File:** `src/components/wrangling/CleanTab.jsx` (`FilterBuilder` ~line 807+), `src/services/exprEvalService.js`

**Suggestion:** The FilterBuilder already supports an `"in"` operator internally (line 863 renders `col in [v1, v2]`). The gap is that users who type R-style `col %in% c(v1, v2)` in a free-text expression field get no match. Add a pre-processing normalizer in `exprEvalService.js` that converts `col %in% c(v1, v2)` into the existing `{ op:"in", col, values:[v1, v2] }` predicate node before evaluation. No new UI surface needed.

**Invariants:** The normalized predicate must flow through `runner.js` `filter` step as a tree node — never evaluate free-form JS strings against `rawData`. Do not use `eval()`.

---

### Model · 2026-05-21 (BugTriage: fix-later 2026-06-14)

**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — Call: rdrobust Sharp RD estimates using local polynomial regression

**File:** `src/components/ModelingTab.jsx` (RDD results block ~line 1075+), `src/math/CausalEngine.js` (`runSharpRDD` return value)

**Suggestion:** `runSharpRDD` already returns `bandwidth`, `late`, `se`, `pVal`, `nLeft`, `nRight`, `polyOrder`, and kernel. Add an optional `rdrobustSummary` object to `EstimationResult` with these fields, then render a compact block in the RDD result section matching R's `summary(rdrobust)` layout: effective N by side, bandwidth, polynomial order, kernel, LATE with 95% CI and p-value. Purely presentational — no new math.

**Invariants:** New field on `EstimationResult` must be optional so `trimResult.js` projection and `ModelComparison` continue to work without changes. `src/math/CausalEngine.js` must have zero React imports.

---

### Model · 2026-05-25 (BugTriage: fix-later 2026-06-14)

**Feedback:** Add an option to estimate without intercept

**File:** `src/math/LinearEngine.js` (line 128: `const k = xCols.length + 1`), `src/components/modeling/ModelConfiguration.jsx`

**Suggestion:** Add a `noIntercept` boolean toggle to `ModelConfiguration.jsx` (hidden by default, shown under "Advanced"). Thread it as `spec.noIntercept` through `ModelingTab._runEstimation` into `LinearEngine.runOLS`/`runWLS`. When `true`, skip prepending the ones-column and set `k = xCols.length`. The DuckDB SQL path must also suppress the intercept CTE column. R² without intercept should use the uncentered (TSS from origin) formula or be flagged with a note. Store `noIntercept` in `result.spec` so replication scripts emit `lm(y ~ x - 1)` / `sm.OLS(y, X)` without intercept. Validate against R before shipping.

**Invariants:** `seType` must still be passed explicitly into the engine — never hardcode SE variants inside the engine function. `src/math/LinearEngine.js` must have zero React imports. Persist `noIntercept` via `indexedDB.js` model buffer, not `localStorage`.

---

### Model · 2026-06-06 (BugTriage: fix-later 2026-06-14)

**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names

**File:** `src/components/modeling/resultDisplay.jsx`

**Suggestion:** Add an optional `labelMap` (column → display name) stored alongside the pinned model in `modelBuffer` (via `indexedDB.js`). Render small inline-editable text fields next to each coefficient row in the LaTeX table view in `resultDisplay.jsx`. On edit, update `labelMap` and re-render the LaTeX string. Serialize `labelMap` in `trimResult.js` so it survives session reload.

**Invariants:** Use inline styles and the `C` object — no external UI component libraries, no CSS files. Persist via `indexedDB.js`, not `localStorage`.

---

### Spatial · 2026-05-30 (BugTriage: fix-later 2026-06-14)

**Feedback:** Allow spatial filters for plots

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Add a per-layer row filter UI to `GeoLayerConfig.jsx`: column + operator + value input (can reuse the `ConditionRow` pattern from `CleanTab.jsx`) that filters the layer's row array before passing it to `geo.js` rendering. The filter applies display-only — it must not emit a pipeline step or mutate `rawData`. Routed to V4 per X5 reconciliation.

**Invariants:** Filter is display-only — never mutate `rawData` in place. Do not emit pipeline steps from the Spatial plot tab. No `localStorage`.

---

### Calculate · 2026-06-19 (NEW — not yet in BugTriage)

**Feedback:** Add limits

**File:** `src/components/tabs/CalculateTab.jsx` (tool selector at line 1556)

**Suggestion:** Add a `"lim" → "∞ Limit"` entry to the tool tab selector (alongside the existing Solver/Derive/Symbolic/Algebra/Integrate tabs). Implement numerical one-sided and two-sided limits using a shrinking-ε approach: evaluate `f(x₀ ± ε)` for ε = 1e-4…1e-12 and report convergence. For symbolic indeterminate forms (0/0, ∞/∞), apply L'Hôpital via the existing `symbolicDiff` engine one time and re-evaluate. UI: `f(x)` expression input, `x →` target value, side selector (left / right / two-sided), result display with convergence table. If a pure-JS limit utility is needed beyond `CalculateTab.jsx`, place it in `src/math/timeSeries.js` or a new `src/math/calculus.js` (pure JS, no React).

**Invariants:** Any shared math utility must live in `src/math/` or `src/core/` with zero React imports. UI stays entirely in `CalculateTab.jsx`.

---

## Suggested action order

1. **Fix pipeline-dataset coupling bug** (`src/App.jsx` ~line 2845) — critical, silently strips the pipeline from all estimations when Clean was never visited first.
2. **Browser-validate four code-complete items** — Plot Builder resize/PNG, Y~Ŷ/residual/Q-Q plots, frozen diagnostics, Heatmap; strike through ClaudeFB entries once confirmed.
3. **Add limits to Calculate tab** (`CalculateTab.jsx`) — new, well-scoped, high daily-use value.
4. **%in% filter normalizer** (`exprEvalService.js`) — low implementation cost, high wrangling-workflow value.
5. **RDD summary table** (`ModelingTab.jsx` + `CausalEngine.js`) — presentation-only, no new math.
6. **Editable LaTeX variable labels** (`resultDisplay.jsx`) — post-estimation reporting polish.
7. **Spatial plot layer filter** (`GeoLayerConfig.jsx`) — display-only row filter; routed to V4.
8. **No-intercept option** (`LinearEngine.js` + `ModelConfiguration.jsx`) — engine-wide spec change; validate against R before shipping.
