# Feedback Analysis Report — 2026-07-29

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **13 open bugs** · **7 open features** · **2 open UX** · **0 open performance** · **5 deferred (fix-later)**
- **3 code-complete items pending Franco's browser validation** (flagged separately below)
- Many ClaudeFB entries are not yet marked ✓ despite BugTriage showing them resolved — a housekeeping pass is recommended.

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js` (L1432–1476), `src/math/CallawayEngine.js`
**Suggestion:** The emitted R scaffold calls `att_gt(gname = ...)` but the `gname` column must contain the numeric first-treatment period (or 0 for never-treated). If `treatCol` stores a binary 0/1 indicator rather than a cohort year, the `did` package will error. In `rScript.js` around line 1439, add a comment instructing users to ensure `df[[gname]]` holds the cohort year (or 0). Also confirm that `CallawayEngine.js` passes `treatCol` as the cohort-year variable — not a binary flag — and surface this requirement in the `ModelConfiguration` tooltip. Run `callawayRValidation.R` → `callawayBenchmarks.json` (noted as pending in CLAUDE.md) to isolate whether the engine or the export is at fault.
**Invariants:** Engine math in `src/math/CallawayEngine.js` must remain pure JS with no React imports. Export lives in `src/services/export/rScript.js` (pure JS).

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/components/PlotBuilder.jsx` (L1525–1543 `copyPlotScript`), `src/services/export/plotScript.js` (`buildGgplot` L343, `buildMatplotlibPlot` L798, `buildStataPlot` L935), `src/services/export/loadLine.js`
**Suggestion:** `copyPlotScript` calls `buildGgplot`/`buildMatplotlibPlot`/`buildStataPlot` but never prepends a dataset load line — `scriptPreamble` is not passed from `ExplorerModule.jsx` so the preamble path is always empty. The geo-plot path already has `geoLoadLines()` in `plotScript.js` (L546) that calls `buildRLoadLine`/`buildPyLoadLine` from `loadLine.js`. Mirror this for non-geo plots: accept `{ filename, loadOpts }` (or the full dataset object) into these three builders and prepend the load line at the top of the emitted script. Alternatively, wire `scriptPreamble` from `ExplorerModule.jsx` → `PlotBuilder` using the existing load-line builders from `loadLine.js`.
**Invariants:** Use `buildRLoadLine`/`buildPyLoadLine`/`buildStataLoadLine` from `loadLine.js` — do not duplicate the load-line logic.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** The "Stata" tab in `CodeEditor.jsx` generates output via `generateStataScript`. Likely failure modes: (1) The July HC2/HC3 fix pass rerouted two-way cluster to `reghdfe` for panel — verify this is applied consistently across all estimator families in `stataScript.js`, not just panel; (2) Other estimators may still emit `vce(cluster a b)` (invalid Stata syntax) without routing to `reghdfe`. Add a smoke-test regex scan across all estimator/SE-type combinations in `stataScript.js` looking for known-invalid Stata patterns (e.g. `vce(cluster \w+ \w+)` without `reghdfe`). The 168-cell matrix from the July HC2 pass should be extended to cover non-panel paths.
**Invariants:** All exports are pure JS. No React imports in export files.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (L528–582, `group_summarize` case), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` only supports fixed per-group aggregations (`mean`, `sum`, `count`, `min`, `max`, `sd`, `median`, `quantile`). Computing ATE as the difference in group means requires a cross-group computation in a single step. Two options: (a) Add a UX hint after `group_summarize` suggesting a follow-up `mutate` step (e.g. `treatment_mean - control_mean`); this requires no runner change and aligns with the non-destructive pipeline model. (b) Add a `"diff"` aggregation type to the `group_summarize` spec that takes two group-key values and subtracts their stats — requires runner + registry + UI changes. Option (a) is lower risk and can be shipped quickly; option (b) is more powerful but needs a new step type.
**Invariants:** Any new aggregation type or step type must be added to both `runner.js` and `registry.js` and validated by `pipelineReliabilityValidation.mjs`. Steps must replay on `rawData`, never mutate in place.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` emits a fixed-width `tabular` environment that overflows the page when there are many models. Wrap the `tabular` in `\adjustbox{max width=\linewidth}{...}` (requires `\usepackage{adjustbox}` in the preamble) or switch to `tabularx` with `X` columns. Infer the column count from the header row and apply the wrapping when `modelCount > 4`. Add the `adjustbox` package to the emitted preamble in the same function that builds the table string.
**Invariants:** None. Pure string generation; no React or math imports.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` tests one hypothesis at a time. Add a "Save test" button that appends the current hypothesis spec to a persisted list (IndexedDB via the model buffer store). In the R/Python/Stata export, iterate the saved list and emit one `linearHypothesis(...)` call per saved test — `ModelHypothesis.js` already has `rSnippet`/`stataSnippet`/`pythonSnippet` generators per test; map over the list.
**Invariants:** Persistence must use IndexedDB (`services/Persistence/indexedDB.js`), not localStorage.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The PlotBuilder (used in Explore's Plot Builder tab) already supports title/x-label/y-label. The ExplorerModule's time-series chart is a separate bespoke SVG renderer that doesn't reuse PlotBuilder. Add `title`, `xLabel`, and `yLabel` text input controls to the time-series chart panel in `ExplorerModule.jsx` and wire them into the SVG `<text>` elements of the chart. Find the SVG block rendering the time-series line to locate the exact insertion point.
**Invariants:** Inline styles via `C` object from `shared.jsx`. No external styling libraries.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add a rule for log-log specifications: when both Y and X were log-transformed (detectable if column names start with `log_` or the variable dictionary has `unit: "log"`), instruct Claude to interpret the coefficient as an elasticity (β% change in Y per 1% change in X). Also add a note for semi-log forms (only Y or only X is log). Coordinate with `src/core/validation/metadataExtractor.js` — it already tags variable types — to inject transformation metadata into the user prompt block to give Claude an explicit signal.
**Invariants:** All Anthropic API calls go through `AIService.js` only. Prompt updates go in `services/AI/Prompts/index.js`.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linear hypothesis tests
**File:** `src/math/ModelHypothesis.js`
**Suggestion:** The current implementation reports a Wald F-statistic for multi-coefficient joint tests. Add a χ² variant: χ² = q·F where q is the number of restrictions (already available as `R.length` in the test logic). The p-value follows the chi-square CDF with q degrees of freedom. Expose it as a toggle in `CoefficientTestPanel.jsx` ("F-test" vs "χ²-test") and emit the appropriate snippet: R `linearHypothesis(..., test="Chisq")`, Stata `test` (already χ²), Python `f_stat * q` with chi2 p-value.
**Invariants:** Math logic in `src/math/ModelHypothesis.js` — no React imports in math files.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Each transformation sub-panel (e.g. `case_when` builder) maintains local state that resets when the user navigates away. Add `sessionStorage` persistence keyed by `pid + stepType` for the in-progress draft state (not a committed pipeline step — just the UI draft). Restore it on mount. This is a pure UI concern and doesn't touch the pipeline or IndexedDB. Ephemeral `sessionStorage` is appropriate here since it is draft UI state, not project data.
**Invariants:** Only pipeline/data persistence goes to IndexedDB per CLAUDE.md. Draft UI state in `sessionStorage` is acceptable.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Add column-header click-to-sort to the Data tab of `ExplorerModule.jsx`. Maintain `sortCol` / `sortDir` state and derive a sorted slice for the display. For DuckDB-backed datasets, sort must happen in SQL — pass an `ORDER BY` clause to the DuckDB preview query in `duckdb.js` so the sort reflects the full dataset ordering, not just the 500-row preview. For JS-backed datasets, sort the displayed rows array directly.
**Invariants:** For DuckDB datasets, sort must happen in SQL (not by sorting the 500-row JS preview) to correctly reflect large-dataset ordering.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array)
**Suggestion:** Review `TOUR_STEPS` against the current 7-tab layout (Data/Clean/Explore/Model/Simulate/Calculate/Report), and against features added since the tour was last written: N-way FE, Callaway DiD, Synthetic Control UI, Spatial Analyze pipeline steps, PlotBuilder geoms, and the Bacon decomposition panel. Update step targets (DOM selectors) and descriptions. Content-only change.
**Invariants:** None beyond inline-style convention.

---

## Code-Complete Items Pending Browser Validation

These are marked in BugTriage as code-complete but not yet confirmed by Franco. Prioritise for a browser-validation pass before further development.

| Date | Module | Feedback | Commit | Status |
|------|--------|----------|--------|--------|
| 2026-06-10 | Model | Plot Builder not resizable; PNG export aesthetic problems | `35f4b5a6` | browser validation pending Franco |
| 2026-06-11 | Model | Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS | `35f4b5a6` | browser validation pending Franco |
| 2026-06-13 | Spatial | Heatmap display broken | `5e105529` | browser validation pending Franco |

---

## Features

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The function plotter already renders multiple expressions. Add a special case for constant inputs: detect when the parsed expression has no dependency on the variable (e.g. `evalExpression(expr, { x: 0 }) === evalExpression(expr, { x: 1 })`) and render it as a dashed horizontal line at `y = constant`. Add a short label ("y = 5") near the right edge. No new UI needed — the existing multi-equation input accepts constants.
**Invariants:** None. Pure JS/JSX in `CalculateTab.jsx`.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis (3D plots)
**File:** `src/components/PlotBuilder.jsx` (new layer type), or a dedicated `Plot3DTab` component
**Suggestion:** True WebGL 3D is a significant lift. A scoped first step: add a "3D Scatter" geom backed by `Plotly.js` (CDN) that maps X, Y, Z columns to `scatter3d`. The R export emits `plotly::plot_ly(df, x=~X, y=~Y, z=~Z, type="scatter3d")`; Python emits `px.scatter_3d(df, x=..., y=..., z=...)`. Scope to scatter only initially.
**Invariants:** Any new CDN must be added to BOTH `script-src` AND `connect-src` in `vercel.json` CSP — this pattern has caused three production regressions (see CLAUDE.md). Plotly's `.js` file is script-loaded AND its WASM extensions may fetch from CDN at runtime; check both.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so users can type and find variables faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text filter input above the variable list. Maintain a `search` state string and filter the displayed `headers` array by `header.toLowerCase().includes(search.toLowerCase())`. Use `C.bg2` background, IBM Plex Mono font. Pure UI change — no pipeline or engine impact.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, add an optional `showColorLabels` toggle in the layer panel. When enabled, append a `Plt.text` mark using the `aes.color` column as the text channel, positioned at each point/bar's location with a contrasting colour. For legends: verify that the Observable Plot auto-legend shows the actual category values (not just the column name) — if not, render a custom legend below the chart using the unique values of the color column.
**Invariants:** Inline styles via `C`.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Add an `onNavigate(tabName, subPanel)` callback from `History` to `WranglingModule`. Build a lookup table mapping step types to (tab, sub-panel) pairs — e.g. `log`/`sq`/`std`/`dummy` → Feature tab, `filter`/`fill_na`/`replace` → Clean tab, `group_summarize`/`pivot_longer` → Reshape tab. On step click, call `onNavigate` to switch the active tab and open the relevant sub-panel. The step-type → tab mapping is already implicit in WranglingModule's tab router; make it an explicit exported constant.
**Invariants:** None beyond existing wrangling architecture.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Bundle a static ~250-row JS constant mapping `{ iso2, iso3, name }` (no CDN fetch — privacy-safe). Expose as a new `country_code` pipeline step with `from` (`"iso2" | "iso3" | "name"`) and `to` fields. In `runner.js`, implement case-insensitive, `String.normalize("NFD")`-normalised lookup. Wire a UI card in `FeatureTab.jsx` under "Formatting". R translators emit `countrycode::countrycode(x, from, to)`; Python emits `pycountry`; Stata emits `kountry` with an `ssc install kountry` note.
**Invariants:** New step type must be added to both `runner.js` and `registry.js` (enforced by `pipelineReliabilityValidation.mjs`). No external fetch — static table only.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`, `src/components/wrangling/CleanTab.jsx`, `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `namedLists` slice to `SessionStateProvider` (alongside `datasets`): `{ [listName]: (string | number)[] }`. Expose a `ListManager` popover from the toolbar to define/edit/delete lists. In `CleanTab.jsx` FilterBuilder, add a `%in% list:<name>` option. In `FeatureTab.jsx` `case_when`, add autocomplete for `@listName` that expands to the list values. Lists are session-scoped by default; optionally persist to a new IndexedDB store for cross-session use. Design-heavy — recommend a scoping session with Franco before implementation.
**Invariants:** If persisted, must use IndexedDB (`services/Persistence/indexedDB.js`), not localStorage.

---

## UX / Design

### Model · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` already tracks a global order across saved plots/maps/models. Extend the persisted schema to include an optional `group: string` field per artifact. Add a "Group" text input (or dropdown of existing group names) to each artifact tile in `ModelBufferBar.jsx`. Add a chip-row above the buffer bar to toggle which group is active; filter the comparison table to only the active group. Persist the group field in the same IDB `model_buffer` store.
**Invariants:** Persistence must use IndexedDB (`services/Persistence/indexedDB.js`), not localStorage.

---

## Fix-Later (Deferred)

| Date | Module | Feedback | File | Note |
|------|--------|----------|------|------|
| 2026-05-10 | Data | Malformed CSV row aborts import instead of offering recovery | `src/services/data/parsers/`, `src/DataStudio.jsx` | Post-launch resilient-import UX pass |
| 2026-05-19 | Clean | R-style `%in% c(...)` filter syntax not accepted | `src/components/wrangling/CleanTab.jsx` | Expression-language compatibility pass |
| 2026-05-21 | Model | RDD lacks `summary(rdrobust)` compact table | `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx` | Presentation enhancement |
| 2026-05-30 | Spatial | No spatial row-filter control scoped to a layer in plot builder | `src/components/tabs/spatial/plot/` | Deferred to V4 |
| 2026-06-06 | Model | LaTeX coefficient table display names can't be edited independently | `src/components/modeling/resultDisplay.jsx` | Post-launch reporting metadata feature |

---

## ClaudeFB Housekeeping Note

Many ClaudeFB entries are not yet marked ✓ despite BugTriage confirming resolution (e.g. p-value calculation, auto-reestimate, .dta loading, XLSX CDN, OECD 406, XSS injection, 2SLS Poisson, Share Project, CS DiD engine split, and many others from the X5 reconciliation table). A one-time housekeeping pass adding strikethroughs to all BugTriage-`fixed` entries in ClaudeFB would make the true open-item count immediately visible without cross-referencing two files.

---

## Suggested Action Order

1. **[high] Plot replication load lines** (`PlotBuilder.jsx` / `plotScript.js`) — one-file fix; low risk, high user-visible impact on every exported plot script.
2. **[high] group_summarize cross-group math** — option (a) (UX hint + follow-up mutate step) can be shipped in an afternoon without a runner change.
3. **[high] Stata workbench smoke-test + fix** — run the SE-type/estimator matrix specifically against Stata output to isolate remaining breakage from the July HC2 pass.
4. **[high] CS DiD replication code** — run `callawayRValidation.R` to identify whether engine or export is at fault; targeted fix once root cause is known.
5. **Browser validation pass** — Franco should validate commits `35f4b5a6` and `5e105529` to close out Plot Builder resize, OLS diagnostic plots, and spatial heatmap.
6. **[medium] Variable searcher** (`VariableSelector.jsx`) — tiny self-contained UI change; high usability gain for large datasets.
7. **[medium] LaTeX table auto-scaling** (`latexTable.js`) — one `\adjustbox` wrap; resolves a common publication workflow blocker.
