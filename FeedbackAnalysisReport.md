# Feedback Analysis Report — 2026-08-01

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **17 open bugs** (4 code-complete/browser-validation pending · 5 fix-later · 8 confirmed open) · **10 open features** · **2 open UX/Design** · **1 open performance**

**Delta since 2026-07-31:** No items closed between runs. The BugTriage rows for Plot Builder resize (medium, `35f4b5a6`), OLS diagnostics plots (high, `35f4b5a6`), coefficient-freeze bug (high, `35f4b5a6`), and spatial heatmap (medium, `5e105529`) remain code-complete but unconfirmed by browser — these four are still blocking Franco's browser validation pass.

---

## Bugs

### [high] · Export / CS DiD · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`) · `src/math/CallawayEngine.js`
**Suggestion:** The R emitter at `rScript.js:1432` emits `att_gt()` with `base_period = "varying"` hardcoded and picks column names from `model.spec` fields (`treatCol`, `entityCol`, `timeCol`). Verify that `CallawayEngine.js` stamps all three onto `result.spec` after estimation, and that the R script binds the correct `gname` (first-treated column, not treatment indicator). Run `callawayRValidation.R` → `callawayBenchmarks.json`; if benchmarks are absent that is the root cause — engine output may not round-trip correctly through the export layer.
**Invariants:** All Anthropic calls go through `AIService.js` only. Export functions are pure JS — no React imports.

---

### [high] · Export / Explore · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/services/export/plotScript.js` · `src/services/export/loadLine.js`
**Suggestion:** `plotScript.js` already imports `buildPyLoadLine` and `buildRLoadLine` from `loadLine.js` and uses them for geo-layer plots (lines 559, 607). The gap is the plain PlotBuilder export path and the Explorer statistical plots. Trace the copy-to-clipboard handler in `ExplorerModule.jsx` (around line 2394) and ensure `allDatasets` + `activeFilename` are passed into the R and Python emitters so `buildRLoadLine`/`buildPyLoadLine` can prefix the correct load call. Stata already says "assumes dataset loaded" (`plotScript.js:961`) — add a deterministic `use "<filename>", clear` line instead.
**Invariants:** `loadLine.js` is already the single source for load-line generation — do not duplicate its logic inline.

---

### [high] · Export / Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx` (line 74) · `src/services/export/stataScript.js`
**Suggestion:** `CodeEditor.jsx` calls `generateStataScript(config)`. Likely failure modes: (1) missing/undefined `seType`/`clusterVar` fields in the config object causing the script to emit incomplete or invalid Stata; (2) two-way clustered SE incorrectly emitting `xtreg` with two cluster vars (a Stata syntax error — `xtreg` takes one cluster variable; the fix routes to `reghdfe`, noted in BugTriage 2026-07-17). Audit `generateStataScript` against each estimator type and SE type combination using the 168-cell matrix approach already documented in BugTriage; confirm no pair emits invalid Stata syntax.
**Invariants:** `stataScript.js` is pure JS, no React. SE type must be read from `seOpts`/`seType` passed in config — never hardcoded inside the exporter.

---

### [high] · Pipeline / Explore · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group_summarize, like ATE by taking the difference in means between treatment and control group
**File:** `src/ExplorerModule.jsx` (line 1764, `GroupSummarizeExplorer`) · `src/pipeline/runner.js` (line 528, `case "group_summarize"`)
**Suggestion:** `GroupSummarizeExplorer` currently offers only per-group aggregations (mean, median, sum, count, min, max, sd — `ExplorerModule.jsx:1779`). Computing an ATE as `mean(y | treatment=1) − mean(y | treatment=0)` is a cross-group difference, not a per-group aggregation. Add an optional "Post-summary formula" field in the UI: after `doSummarize` produces its collapsed frame, let the user write e.g. `mean_y_1 - mean_y_0` and append that as a new column, evaluated via the existing `mutate` expression evaluator from `runner.js` on the summarised rows. This is simpler than building a new DSL.
**Invariants:** `runner.js` must remain non-destructive and replay on `rawData`. The post-summary formula operates on the collapsed output — it must never mutate the pipeline's raw data path.

---

### [high] · Model · 2026-07-09 (likely fixed — browser confirmation needed)
**Feedback:** Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable
**File:** `src/math/PanelEngine.js` (`runFEMulti`) · `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage marks this "likely fixed — N-way FE shipped in #104 (`045f5cda`)." CLAUDE.md confirms N-way FE is validated to 18/18 checks. Franco should browser-confirm that any categorical column (not just entity/time) can be added as a fixed-effect dimension in `ModelConfiguration.jsx` and that the FE picker does not restrict to only `entityCol`/`timeCol`. If the picker still filters to panel-declared columns only, widen its column selector to include all string/factor-typed columns from `info`.
**Invariants:** `runFEMulti` is pure JS, no React. SE type must be passed explicitly.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`
**Suggestion:** `buildStargazer()` uses a fixed `l r r ...` column format (`latexTable.js:36`). For wide tables, add `\usepackage{adjustbox}` and wrap the `tabular` in `\begin{adjustbox}{max width=\linewidth}...\end{adjustbox}`, or switch to `tabularx` with `X` columns that auto-compress. Expose an `autoResize` option defaulting to `true` when `models.length > 3`. Pure string-generation change in `latexTable.js` — no UI or React changes needed.
**Invariants:** `latexTable.js` is pure JS, no React imports.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` already shows chi-square for joint tests (line 211). The gap is saving multiple individual hypothesis rows and exporting them. Add a "Save hypothesis" button per test row that pushes to a local `savedHypotheses[]` array in the panel's state, then an "Export to R" button that calls `ModelHypothesis.generateSnippet()` for each saved test and concatenates the output. The R snippet generator already exists in `ModelHypothesis.js` — the only new surface is the accumulation UI and batch-export call.
**Invariants:** `ModelHypothesis.js` is pure JS, no React.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` (time series sub-component around line 413)
**Suggestion:** The time series panel uses a custom SVG renderer. Add a collapsible Labels section (matching PlotBuilder's existing "Labels" tab pattern) with inputs for chart title, x-axis label, and y-axis label. Pass these as props to the SVG generator and inject them as `<text>` elements. Reuse the label input pattern from `PlotBuilder.jsx` for consistency.
**Invariants:** Inline styles only via `C` from `shared.jsx`. No external UI libraries, no CSS files.

---

### [medium] · AI / Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT, around line 99)
**Suggestion:** `INTERPRET_REGRESSION_PROMPT` already handles log-log (elasticity) and log-level (semi-elasticity) via functional-form rules (lines 353–356). The gap is level-level "direct" elasticity — β × (x̄/ȳ) at the mean. Add a rule: when the model is level-level and `result.means` is available in the prompt payload, append: "At the sample mean (x̄ = …, ȳ = …), the implied elasticity is β × x̄/ȳ = …." Requires `result.means` to be populated in `EstimationResult` and threaded into the AI prompt's user block in `AIService.js`. Scope to OLS/WLS initially.
**Invariants:** All Anthropic calls go through `AIService.js`. `SHARED_CONTEXT` is the cached block and must remain unchanged — add to the user payload only.

---

### [medium] · Model · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already imports `pchisq` (line 1) and uses `chiSq` in joint tests (line 206). For single-restriction tests, the Wald chi-square is simply `F × q` (q = 1 for single restriction). Add a `chiSq` / `chiSqPval` field to the single-test result object and expose a "F / χ²" toggle chip in `CoefficientTestPanel.jsx`. No new math needed.
**Invariants:** `ModelHypothesis.js` is pure JS, no React.

---

### [medium] · UX / Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to check variable names, and coming back to find it empty
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` uses local `useState` for in-progress form values (`cwNN`, `cwCases`, etc.) that reset on unmount when the user switches tabs. Serialize the in-progress case_when / if_else draft state to `sessionStorage` (keyed by `pid`) in a `useEffect` on every change, and restore it on mount. This is lighter than lifting state to `WranglingModule`. The `sessionStorage` approach is acceptable for ephemeral draft state — only committed pipeline steps go to IndexedDB.
**Invariants:** Do not use `localStorage` for pipeline or data storage. Only `sessionStorage` for ephemeral draft; actual pipeline steps still write to `indexedDB.js` via the existing path.

---

### [medium] · Data · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Add `sortCol`/`sortDir` state to `ExplorerModule`. Clicking a column header cycles null → asc → desc. For JS-backed tables, sort the displayed 500-row preview slice in-component. For DuckDB tables, issue `SELECT * FROM tbl ORDER BY "${col}" ${dir} LIMIT 500`. This is display-only — it does not add an `arrange` step to the pipeline. Add a tooltip: "Viewing sorted — sort the full dataset via the pipeline's Reshape tab."
**Invariants:** Non-destructive pipeline invariant: this is a display-layer sort, never calls `runner.js`. DuckDB queries go through `src/services/data/duckdb.js` singleton.

---

### [low] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line 1583 — malformed row aborts import
**File:** `src/DataStudio.jsx` (CSV parse path)
**Suggestion:** When PapaParse or DuckDB hits a malformed row, surface a non-fatal warning panel ("Skipped N rows — download error log") rather than aborting the entire import. Store skipped rows with their original line numbers. BugTriage tracks this as "fix-later" (post-launch resilient-import pass).
**Invariants:** OPFS Parquet cache must still be written for valid rows.

---

### [low] · Clean · 2026-06-14
**Feedback:** R-style `%in% c(...)` membership syntax is not accepted by the filter expression UI
**File:** `src/components/wrangling/CleanTab.jsx` · `src/services/exprEvalService.js`
**Suggestion:** BugTriage tracks as "fix-later." Add a pre-processing step in `exprEvalService.js` that regex-replaces `<lhs> %in% c(<args>)` with a JS `["a","b"].includes(row.col)` check before the safe evaluator runs. Low-blast-radius addition.
**Invariants:** Expression evaluation must be sandboxed — no `eval()` of arbitrary JS. Use the existing safe-eval pattern.

---

### [low] · Model · 2026-06-14
**Feedback:** RDD result lacks an R-style `summary(rdrobust)` compact table
**File:** `src/math/CausalEngine.js` · `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage tracks as "fix-later." `runSharpRDD` already populates `h`, `b`, `LATE`, `se`, `pval`, `ci95`, `n_left`, `n_right`, `kernel`, `polyOrder`. Wire these into a compact display table in the RDD result section. No new estimation needed — purely a UI display enhancement.
**Invariants:** Pure UI change — no math changes. Inline styles via `C`.

---

### [low] · Spatial · 2026-06-14
**Feedback:** Plot builder has no spatial row-filter control scoped to a layer
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** BugTriage routes this to V4. When implemented, the filter should operate on the layer's `rows` array before rendering (not on the pipeline) and expose the existing `QuickFilter` expression syntax for consistency.
**Invariants:** Spatial filter is display-only — must not mutate `rawData` or add a pipeline step.

---

### [low] · Model · 2026-06-14
**Feedback:** LaTeX coefficient table display names cannot be edited independently of source columns
**File:** `src/services/export/latexTable.js` · `src/ReportingModule.jsx`
**Suggestion:** BugTriage tracks as "fix-later." `buildStargazer` already accepts a `varLabels` option (`latexTable.js:22`). Expose a `varLabels` editor in the Report tab's model-table panel, persist it per `pid` in IndexedDB, and pass it through to `buildStargazer`. The math and export are ready; only the UI edit surface is missing.
**Invariants:** `latexTable.js` is pure JS, no React. Persist via `indexedDB.js`, not localStorage.

---

### [high] · UI · 2026-06-11 *(code-complete, browser validation pending)*
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx` · `src/components/ModelingTab.jsx`
**Suggestion:** Commit `35f4b5a6` fixed the regression (diagnostics now render from the frozen `result.spec`, so sidebar edits and pinned-model restore cannot mutate the displayed model). Franco needs to browser-validate: (1) all three plots render after a new OLS estimation; (2) adding a variable to the sidebar does not redraw the residual plots; (3) switching to a pinned model shows that model's diagnostics. No code change expected.
**Invariants:** None.

---

### [high] · UI · 2026-06-14 *(code-complete, browser validation pending)*
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables; they must remain frozen until re-estimation
**File:** `src/components/ModelingTab.jsx` · `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Commit `35f4b5a6` freezes OLS/WLS display to `result.spec`. Franco should confirm that coefficient plots do not redraw when the sidebar variable list changes, and that restoring a pinned model shows the correct frozen coefficients/diagnostics.
**Invariants:** None.

---

### [medium] · UI · 2026-06-10 *(code-complete, browser validation pending)*
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx` · `src/components/ModelingTab.jsx`
**Suggestion:** Commit `35f4b5a6` wires native vertical resize with bounded responsive canvas sizing. Franco should validate: (1) the resize handle appears and is draggable; (2) PNG export captures the user-selected dimensions at the correct pixel density.
**Invariants:** None.

---

### [medium] · UI · 2026-06-13 *(code-complete, browser validation pending)*
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` · `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Commit `5e105529` restores Heatmap as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Franco should confirm the heatmap renders on a point-geometry dataset and that the legend/opacity controls respond correctly.
**Invariants:** None.

---

### [high] · Model · 2026-07-09 *(likely fixed — browser confirmation needed)*
**Feedback:** Add also FE with interactions
**File:** `src/math/PanelEngine.js` · `src/pipeline/runner.js` (`factor_interactions`)
**Suggestion:** BugTriage marks this "likely fixed — FE interactions shipped in `045f5cda` (#104)." The `factor_interactions` step in `runner.js` creates interaction dummies. Franco should validate that a model with e.g. `state × industry` absorbs the combined effect and that replication R/Python/Stata scripts emit correct interaction syntax (e.g. `i.state#i.industry` in Stata, `:` interaction in R fixest).
**Invariants:** None.

---

## Features

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` · `src/math/SpatialEngine.js`
**Suggestion:** The Leaflet map tab already renders hex/rect grids as tile layers. Port the same grid-rendering logic to the Observable Plot / SVG geo-plot tab by computing grid cells in JS (reuse `SpatialEngine.js`'s existing grid functions) and rendering them as `<rect>`/`<polygon>` SVG marks alongside the existing point/polygon layers.
**Invariants:** `SpatialEngine.js` is pure JS, no React.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" section to the `CalculateTab` workbench alongside the existing derivative, integral, and optimizer sections. Use the symbolic CAS already wired in to compute one-sided limits (`lim(f(x), x→a+)`, `lim(f(x), x→a−)`) and render the result. Flag indeterminate forms when substitution fails.
**Invariants:** No React in `src/math/`. Any new math helper lives in pure JS under `src/math/` or `src/core/`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Constants" list in the function graph panel. Each entry is a label + numeric value; it renders as a horizontal `hline` mark on the plot canvas. Reuse Observable Plot's `Plot.ruleY([value])` pattern already used in `PlotBuilder.jsx` for the `hline` geom. No CAS integration needed — constants are plain numeric inputs.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 does not natively support dual y-axis or true 3-axis layouts. Recommended scope: add a "Secondary Y axis" toggle that renders a second Observable Plot instance overlaid with a matched x-domain and a right-side y-axis, rather than a true 3-axis plot. Defer a full multi-axis architecture to a future charting upgrade.
**Invariants:** No external charting libraries. Observable Plot is loaded from CDN (already wired).

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small filter `<input>` at the top of the Y/X/W chip panels. On keystroke, filter the displayed column list to names containing the typed substring (case-insensitive). State is local to the component — no persistence needed.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When a categorical color mapping is active, add an optional "Show value labels" toggle in PlotBuilder's Labels tab. When enabled, append `Plot.text(data, { x, y, text: colorCol, ... })` to annotate data points with their category name. Cap at N=50 labelled points to avoid collision.
**Invariants:** Inline styles via `C`. Observable Plot CDN already loaded.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`
**Suggestion:** Each `step.type` maps to a tab+subsection (e.g. `case_when` → FeatureTab → if_else section; `filter` → CleanTab → FilterBuilder). Add a "✎ Edit" icon on each History sidebar step card; clicking it emits an `onEditStep(step)` callback to `WranglingModule`, which sets the active tab and passes the step as a draft to the appropriate sub-component to re-populate its form. A small static step-to-tab lookup table covers this.
**Invariants:** Inline styles via `C`. Draft population must not auto-commit a new pipeline step — user clicks "Apply" as before.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx` · `src/pipeline/runner.js` · `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` step to `runner.js` and `registry.js` backed by a static lookup table (~250 rows, <10 KB) embedded as a JS object in a new `src/math/countryCodes.js` file. The step maps an existing column (via iso2, iso3, or country name) to a new column of the desired format. Expose it as a panel in `FeatureTab.jsx`. Start with exact matching; fuzzy matching can be added in a follow-up using the existing `fuzzyGroups` utility in `wrangling/utils.js`.
**Invariants:** New step must appear in `registry.js` (keep in sync with `runner.js`). The lookup table lives in `src/math/` (pure JS, no React). Must replay on `rawData` non-destructively.

---

### General · 2026-07-19 *(new — first appeared in today's run)*
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables — cross-module feature (Clean filters + Feature builder + maybe Explore)
**File:** `src/services/session/sessionState.jsx` · `src/components/wrangling/CleanTab.jsx` · `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Before implementing, hold a design scope session with Franco to decide: (a) what a "list" is (an ordered set of string/numeric values a user names and stores), (b) where lists live (session-scoped React context alongside `sessionState.jsx` is the natural home — `SessionStateProvider` already manages the dataset registry and could carry a `lists` map), and (c) how they surface (a `%in% listName` shorthand in filter expressions, a lookup-based `case_when`, a chip in the variable selector). Implementation sketch once scoped: add a `lists` key to `sessionState`, expose a "List Manager" panel in the sidebar or a dedicated tab card, and wire `exprEvalService.js` to resolve named lists at eval time. IndexedDB persistence per `pid` via `indexedDB.js` if lists should survive reload.
**Invariants:** Lists are session/project-scoped — must be keyed by `pid`. If stored in IndexedDB, must use `indexedDB.js` (not localStorage). `sessionState.jsx` is a React Context — no imports from it in `src/math/` or `src/core/`.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS, line 11)
**Suggestion:** `TOUR_STEPS` has 9 entries. The Model step (line 40) lists 14 estimators but omits Sun-Abraham, Callaway-Sant'Anna, Poisson FE, LSDV, GMM, LIML, Fuzzy RDD, and N-way FE — all added since the original tour. Update the Model step text to reflect the current estimator roster. The Calculate step (line 56) should mention limits (when added) and matrix algebra (when added). Update the Spatial step if present to reference the Analyze/Map/Plot sub-tab structure. No new tour cards needed.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` already tracks a global ordered list of pinned artifacts. Extend its schema with a `group` string field per artifact (default `"Default"`). Add a group manager in `ModelBufferBar.jsx`: a collapsible panel where users can create named groups and drag artifacts between them. Render each group as a collapsible section header in the buffer bar. Persist group assignments in IndexedDB alongside the artifact order, keyed by `pid`.
**Invariants:** Persistence must use IndexedDB (`indexedDB.js`), not localStorage. Group state is project-scoped (keyed by `pid`).

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB, over 900k rows)
**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js`
**Suggestion:** CLAUDE.md marks Fase X4 as code-complete and BugTriage confirms `.dta` now converts to a DuckDB-backed Parquet file for files over 10 MB. Other formats (`.rds`, `.xlsx`, `.shp`) still parse entirely in JS — extend the DuckDB on-load path to all non-CSV formats so the same OPFS Parquet cache strategy applies. Once Franco browser-validates the `.dta` path, this extension is a straightforward copy of the existing branch in `DataStudio.jsx`.
**Invariants:** Data never leaves the browser (privacy-first). OPFS Parquet cache uses the existing `saveRawData`/`loadFromOPFSKey` path in `indexedDB.js`.

---

## Suggested action order

1. **Fix CS DiD 2021 replication code** — high severity export bug blocking a core econometric workflow; `rScript.js:1432` likely missing bench-validated field plumbing from `CallawayEngine.js`
2. **Add dataset load lines to plot replication exports** — `plotScript.js` already has `buildRLoadLine`/`buildPyLoadLine`; gap is wiring them into the plain PlotBuilder and Explorer pin paths (low effort, high daily-use impact)
3. **Diagnose Stata workbench failures** — run the 168-cell SE × estimator matrix test against `generateStataScript`; fix any pair that emits syntactically invalid Stata
4. **Add cross-group arithmetic to group_summarize** — post-summary formula box is low-scope: evaluate a user expression over the already-computed summary frame; directly unblocks ATE-by-means use case
5. **Persist FeatureTab draft state across tab switches** — serialize `case_when`/`if_else` draft to `sessionStorage` on keystroke, restore on mount; removes a daily friction point
6. **Add arrange/sort to data viewer** — display-only sort (no pipeline step); DuckDB `ORDER BY` path already available
7. **Update Tour step text** — straightforward content update; reduces confusion for new LMU users
8. **LaTeX table auto-resize for wide models** — `adjustbox` wrapper in `buildStargazer`; small change with high polish value for report exports
