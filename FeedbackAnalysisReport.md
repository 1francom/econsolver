# Feedback Analysis Report — 2026-09-16

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 9 open bugs · 10 open features · 2 open UX · 1 open performance · 2 open other

**Notes on cross-referencing:** Several ClaudeFB.md items that lack ~~strikethrough~~ or ✓ markers are confirmed fixed in BugTriage.md or the X5 reconciliation table (e.g. group_summarize ATE, chi-square hypothesis test, data viewer sort, plot load-lines, any() in grouped expressions, join pipeline context). Those are excluded below. Items marked "likely fixed — needs confirmation" in BugTriage.md are retained as **low-confidence open** bugs requiring browser validation by Franco.

---

## Bugs

### [high] · Calculate · 2026-07-17
**Feedback:** `Stata workbench doesn't work completely`
**File:** `src/services/export/stataScript.js`, `src/components/modeling/CodeEditor.jsx`
**Suggestion:** Audit `generateStataScript` for each estimator/SE combination and run each emitted block through a do-file linter or a local Stata session. Known gaps already documented in CLAUDE.md include: `ibn.` factor coding under no-intercept is unverified, `xtreg` two-way cluster routes to `reghdfe` (correct) but `ivregress` HAC emits a NOTE without a working alternative, and `merge 1:m` for multi-to-multi joins was flagged as UNVERIFIED. Start by collecting the exact error message Franco sees — "doesn't work" may point to one of these known gaps or an entirely separate `missing colon` / `option not allowed` syntax issue in a specific estimator path.
**Invariants:** All styles inline via C; no React in math files. The exporter (`stataScript.js`) is pure JS — no React import needed there.

---

### [high] · Simulate · 2026-07-28
**Feedback:** `Update the replication script, currently it replicates only the DGP`
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (no dedicated simulate exporter yet)
**Suggestion:** Add a `generateSimulateScript()` function (R, Python, Stata variants) that emits (1) DGP seed + distribution parameters, (2) resampling / bootstrap loop if those panels were used, (3) any sample test calls (`t.test`, `var.test`, etc.) that the user ran on the simulated data, and (4) any estimation block if a model was run on the simulated draw. The function should read `SimulateTab`'s existing state slices — `dgpParams`, `resamplingState`, and the `sampleTestResults` already passed to `SampleTestPanel`. Wire the "Copy script" button in `SimulateTab` to this function instead of the current DGP-only emitter.
**Invariants:** New exporter must live in `src/services/export/` (pure JS, zero React imports). All Anthropic calls go through `AIService.js` only.

---

### [high] · Model · 2026-07-09 (needs browser confirmation)
**Feedback:** `Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable`
**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/PanelEngine.js`
**Suggestion:** BugTriage marks this "likely fixed — N-way FE shipped in `045f5cda`; needs confirmation." Ask Franco to browser-test: (a) declare entity + time in PanelTab, (b) add a third FE column (e.g. `sector`) via the FE column picker, (c) confirm the model runs and the coefficient table omits the absorbed column. If the FE picker is absent in the UI for the third dimension, `ModelConfiguration.jsx`'s FE column selector needs an `[...remainder]` multi-select for dimensions beyond entity/time.
**Invariants:** `runFEMulti` / `demeanByFE` are in `src/math/PanelEngine.js` — no React imports there.

---

### [high] · Model · 2026-07-09 (needs browser confirmation)
**Feedback:** `Add also FE with interactions`
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js`
**Suggestion:** BugTriage marks this "likely fixed — FE interactions shipped in `045f5cda`." Browser-confirm by running `feols(y ~ x1:x2 | entity + time)` and checking that the interaction term appears in the coefficient table. If interactions are absorbed silently (coefficient missing), the `factor_interactions` step product column may need to be excluded from the within-demean pass, not the other way around.
**Invariants:** none beyond zero-React rule in `src/math/`.

---

### [critical] · Model · 2026-07-22 / 2026-07-28 (needs browser confirmation)
**Feedback:** `P-values with small numbers!!!!!!!! beta=0.0001 and se=0.002 can never give a p-value < 0.001 with ***`
**File:** `src/math/LinearEngine.js` (functions `tCDF`, `betainc`)
**Suggestion:** BugTriage marks this "likely fixed — `216b1ecb` (2026-07-28); needs confirmation." Franco should run a regression where |t| < 1 (e.g. β ≈ 0.0001, SE ≈ 0.002) and confirm the p-value is > 0.5, not < 0.001. The fix replaced the Riemann-sum `betainc` approximation with Lentz's continued-fraction method. If any residual failure appears, check whether `calcEngine.js`'s `_incompleteBeta` (also fixed per BugTriage row 73 for the chi-square panel) is also used in any path that feeds these results — a second copy of the old method was found there.
**Invariants:** `src/math/` — no React imports.

---

### [medium] · Explore · 2026-07-28
**Feedback:** `References of the group in time series doesn't work`
**File:** `src/ExplorerModule.jsx` (functions `aggregateTimeSeries`, `fetchAggregateTimeSeriesSQL`, ~lines 180–270)
**Suggestion:** In the time-series chart, each group/series should receive a distinct colour from the `COLORS` palette and have its label in the legend. The likely bug is that `grpCol` is set but the chart's stroke/colour accessor is not mapping `series[si]` to `COLORS[si % COLORS.length]` — instead all lines may render as the same colour making them indistinguishable (the "reference" is invisible). Check the `series.map((s, si) => ...)` SVG rendering block (around line 268) to confirm each series gets a distinct stroke and is labelled in the legend overlay.
**Invariants:** Inline styles only, using C constants from `shared.jsx`.

---

### [medium] · Explore · 2026-07-09
**Feedback:** `Add labels for the Time series plot, title, axis, etc.`
**File:** `src/ExplorerModule.jsx` (TimeSeriesTab section, around the `tsView` state block)
**Suggestion:** Add a collapsible "Labels" panel beneath the time-series chart (matching the pattern used by `PlotBuilder`'s Labels section) with inputs for chart title, x-axis label, and y-axis label. Pass these strings as SVG `<text>` elements rendered at the chart's top, bottom, and left edge. Wire the same strings into the emitted R (`labs(title=..., x=..., y=...)`) and Python (`ax.set_title/xlabel/ylabel`) replication code.
**Invariants:** Inline styles only; no external chart libraries beyond the existing CDN Observable Plot path.

---

### [medium] · General · 2026-07-05
**Feedback:** `Adjust the size of LaTeX tables when the number of columns increases`
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** In `latexTable.js`, switch the table environment from `tabular` to `tabularx{\linewidth}` when the number of model columns exceeds 4, and emit `\usepackage{tabularx}` in the preamble block. For very wide tables (>7 columns), additionally wrap in `\resizebox{\textwidth}{!}{...}` or switch to `\tiny` font. The threshold and strategy can be a simple `if (nCols > 7) resizebox; else if (nCols > 4) tabularx` branch. No `adjustFontSize` or CTAN macro exists in the current file — this is a new feature, not a fix.
**Invariants:** Pure JS export utility — no React, no UI changes needed.

---

### [medium] · Model · 2026-07-06
**Feedback:** `Add vector for running more than 1 hypothesis and save them to replicate in R`
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently runs one hypothesis at a time and the result is shown inline but not persisted. Add a "Saved tests" list (array in component state, persisted to IndexedDB under the model's uuid key) where each entry is `{ label, restriction, stat, p, script }`. The "Run test" button gains a "Save" option that appends to this list; saved tests render in a compact table below the form; a "Copy all to R" button calls the existing `generateJointHypothesisScript` for each entry and concatenates them. The list should be included in `trimResult` so it survives model-buffer save/restore.
**Invariants:** Persistence goes through `indexedDB.js` (not localStorage). Inline styles only.

---

### [medium] · Model · 2026-07-09
**Feedback:** `Add direct elasticity interpretation`
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT, ~lines 99 and 364)
**Suggestion:** The AI prompt already contains elasticity rules for log–log and semi-log functional forms (lines 99, 364–365). The gap is that these rules fire only when the AI interprets the full regression — the user wants an **inline** elasticity annotation on each coefficient row without running the AI. Add a `getElasticityTag(yVar, xVar, varDict)` utility in `src/math/` (pure, no React) that detects log-transformed names (e.g. `ln_`, `log_`, `_log`) and returns a tag like `"elasticity"` or `"semi-elasticity"` to be displayed beside the coefficient in the results table.
**Invariants:** New utility in `src/math/` must have zero React imports; no new Anthropic API calls (this is pure pattern-matching on variable names, not an AI feature).

---

### [low] · General · 2026-07-17
**Feedback:** `Guide tour must be updated`
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS registry, ~9 steps)
**Suggestion:** Audit `TOUR_STEPS` against the current 7-tab WorkspaceBar (Data / Clean / Explore / Model / Simulate / Calculate / Report). Known stale content: the pipeline panel is on the right (not left), WLS is an OLS toggle (not a sidebar estimator), the PlotBuilder has 12 geoms (not 11), CR2/CR3 exist, and the FE estimator now accepts N-way dimensions. Update the step descriptions to reflect current UI layout and feature set, following the CLAUDE.md rule: **never write a count** — name the things instead.
**Invariants:** Inline styles only (tour overlay is already styled via C); no external libraries.

---

### [low] · Clean · 2026-06-14
**Feedback:** `R-style %in% c(...) filter syntax is not accepted by the filter expression UI`
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** This is a "fix-later" item from the X5 reconciliation. The canonical condition language (`predicate.js`) already has `in`/`nin` operators that map to R's `%in%`. The gap is that the `FilterBuilder` UI only exposes them via a dropdown — it does not parse a typed R expression. A low-cost fix: add a small expression-to-predicate parser that recognises `col %in% c(v1, v2, ...)` and converts it to a `{col, op:"in", value:[v1,v2,...]}` predicate node that the existing `evalPredicate` already handles.
**Invariants:** Any new parser must produce nodes compatible with `predicate.js` — never re-implement a filter operator outside that single owner.

---

### [low] · Model · 2026-06-14
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/ReportingModule.jsx`, `src/services/export/latexTable.js`
**Suggestion:** Add an `aliasMap: { [originalName]: string }` field to the LaTeX table config (passed from `ReportingModule`). `buildStargazer` replaces each coefficient row's label with `aliasMap[name] ?? name`. Expose an editable label column in the table preview UI (a small inline text input per row) that populates `aliasMap`. Persist `aliasMap` in the model's `spec` so it round-trips through model-buffer save/restore.
**Invariants:** Persistence goes through `indexedDB.js`. `latexTable.js` is pure JS — no React import.

---

### [low] · Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/modeling/` (RDD result display), `src/math/CausalEngine.js`
**Suggestion:** This is a "fix-later" item. `CausalEngine.js` already returns all the statistics needed (`h`, `b`, `n_left`, `n_right`, `coef`, `se`, `z`, `p`, `ci_l`, `ci_r`). Add a collapsible `<RDSummaryTable>` component that formats these in the two-column layout `rdrobust` uses (left: conventional, right: bias-corrected, when available). The component sits below the existing result tiles and above the RDD plot, toggled by a "Show rdrobust table" chip.
**Invariants:** Inline styles only, using C object.

---

## Features

### Calculate · 2026-06-19
**Feedback:** `Add limits`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" mode to the `CalculateTab` workbench alongside the existing Derivatives / Integrals / Optimize modes. Evaluate limits numerically by approaching the target value from both sides (a sequence of `x → c⁻` and `x → c⁺` points); for one-sided and infinite limits, use large finite values (`1e15`). Emit R (`limit(expr, x, a)` from the `Ryacas` or `pracma` packages), Python (`sympy.limit`), and Stata (documented no-support note). The mode needs a "point" input and a direction toggle (two-sided / left / right / at ±∞).
**Invariants:** `CalculateTab.jsx` is a UI component — the numeric limit engine should live in `src/math/calcEngine.js` (pure JS, no React import).

---

### Calculate · 2026-07-04
**Feedback:** `Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Allow the user to enter a constant (e.g. `k = 3`) in the equation list alongside function expressions. The plot renderer detects a constant-valued expression (or a bare number) and draws it as a horizontal dashed line at `y = k`, labelled with the constant's name. This reuses the existing `hline` geom pattern from `PlotBuilder.jsx` and follows the `vline`/`hline` approach already wired there.
**Invariants:** Inline styles; no external charting library beyond what `CalculateTab` already uses.

---

### Clean · 2026-07-17
**Feedback:** `Add an option to click on one pipeline step and go to the corresponding subsection automatically`
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Add an `onNavigate(step)` callback to `History.jsx`. Clicking the step name (not the undo/redo arrow) calls `onNavigate(step)`, which `WranglingModule` handles by mapping the step's `type` to a tab index (e.g. `rename/drop/filter/fill_na` → Clean tab, `log/sq/lag` → Feature tab, `group_summarize/pivot_longer` → Reshape tab) and setting `activeTab` accordingly. For step types with a sub-panel (e.g. `case_when` → the Case When section of Feature tab), also scroll to or expand that subsection.
**Invariants:** Inline styles only. Tab routing must go through `WranglingModule`'s existing `activeTab` state — no new global router.

---

### Clean · 2026-07-17
**Feedback:** `Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases`
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Store each subsection's in-progress form state in `sessionStorage`, keyed by `pid + stepType` (e.g. `{pid}_case_when_draft`). Read and restore this draft on mount; clear it on "Add to pipeline" or "Cancel". The draft should store the full form fields (column names, expressions, cases) as a JSON string. Since the user only ever has one in-progress draft per step type per project, a single `sessionStorage` key per type is sufficient without a list.
**Invariants:** `sessionStorage` (not `localStorage` / IndexedDB) is appropriate here because drafts are intentionally ephemeral and session-scoped. IndexedDB is for pipeline/data storage — drafts are pre-step scratch state, not persisted pipeline entries.

---

### Clean · 2026-07-19
**Feedback:** `Add a function for Country Codes — iso2/iso3/country-name crosswalk transform`
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js` (new step type)
**Suggestion:** Add a `country_code` pipeline step that takes a source column (country name or iso2 or iso3) and a target format (iso2 / iso3 / country name) and maps values via a bundled static lookup table (~250 rows, inline JSON). Wire it as a new step type in `registry.js` and `runner.js` following the existing `recode` / `normalize_cats` pattern. Expose the UI as a section in `FeatureTab.jsx`'s Formatting sub-tab. The lookup should be fuzzy-tolerant for country names (reuse `fuzzyGroups` from `wrangling/utils.js`).
**Invariants:** New pipeline step requires entries in both `runner.js` AND `registry.js` (kept in sync per CLAUDE.md invariant, validated by `pipelineReliabilityValidation.mjs`). Static lookup table goes in `src/math/` or `src/pipeline/` as pure JS, no React import.

---

### General · 2026-07-17
**Feedback:** `Create groups for pinned models, plots, and maps, so not everything is compared at the same time`
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Add a `groupId: string | null` field to the artifact order entry structure in `artifactOrder.js`. `ModelBufferBar` renders a group selector header (a small chip row at the top, defaulting to "All") — selecting a named group filters the visible buffer items. Groups are created ad hoc (typing a new name in the chip row creates it), stored as a `Set` of `artifactId`s per `groupId` in IndexedDB alongside the existing artifact order. The "All" group always shows everything.
**Invariants:** Persistence via `indexedDB.js`. Inline styles only.

---

### Model · 2026-07-09
**Feedback:** `Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster`
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small `<input type="text" placeholder="Search variables…">` above each variable list panel (Y, X, W). Filter the rendered list to items whose column name contains the search string (case-insensitive). Keep the search field local state (cleared on dataset change). This is a pure UI filter — it does not change which variables are selected, only which are visible in the picker.
**Invariants:** Inline styles only via C.

---

### Model · 2026-07-22
**Feedback:** `Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators`
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** Extend the existing "Scatter / Binned" plot in `ModelPlots.jsx` with a controls panel: number of bins (slider, 10–100), overlay type (linear fit / LOESS / none), weight column (for WLS), and residual vs raw-Y toggle. These map directly to `rdplot`-style arguments. Pass the config to the Observable Plot render and emit matching R (`rdplot(y,x,nbins=…,kernel="tri")`) / Python (`statsmodels lowess`) / Stata (`cmogram`) code.
**Invariants:** Inline styles only. New controls live in `ModelPlots.jsx` as collapsed options, not a new component.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a "Grid" layer type to `SpatialGeoPlot` that renders a spatial reference grid (lat/lon graticules at a user-chosen interval) as SVG path elements over the geo canvas. The grid lines can be computed from `lonToTx/latToTy` tile math already in `spatial/shared/leaflet.js`. Wire the layer type through `GeoLayerConfig` with an interval input (degrees).
**Invariants:** Inline styles only. Grid computation must be pure JS in `src/components/tabs/spatial/` — no imports from React or math engines.

---

## UX / Design

### General · 2026-07-17
**Feedback:** `Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria`
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** (Covered under Features above — this item was filed in the Bugs section but is a UX enhancement.) The key UX decision: groups should be **additive filters** (a model can belong to multiple groups), not exclusive tags. A chip row at the top of `ModelBufferBar` with "All / Group A / Group B / + New group" satisfies this with minimal surface area.
**Invariants:** Inline styles only; IndexedDB for persistence.

---

### Clean · 2026-07-17
**Feedback:** `Save progress of uncompleted work in transformation subsections`
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** (Covered under Features above.) The critical UX signal: users are losing typed-out `case_when` expressions when they navigate to another tab to check column names. A simple "Draft saved" indicator in the subsection header (a small dot or "● unsaved" label) would surface the persistence promise and reduce anxiety.
**Invariants:** Inline styles only.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (parseFile DuckDB routing), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** BugTriage records this as "Fase X4 code-complete; browser benchmark pending Franco." Priority action is browser validation: load the exact 21MB / 900k-row CSV that triggered the crash, navigate between all 7 tabs, and confirm (a) no tab freezes > 2s, (b) the full row count appears in the model output (not 500), and (c) a DuckDB-backed filter step returns the correct filtered count. If performance remains an issue, the CLAUDE.md strategy doc (`plans/2026-05-22-fase-x4-performance.md`) describes the remaining optimization opportunities (OPFS Parquet cache write, `getTablePage` lazy column fetch).
**Invariants:** Full data never truncated before `runner.js` / `duckdbRunner.js`. localStorage deprecated for pipeline/data storage.

---

## Other

### [medium] · Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/` (DuckDB CSV import)
**Suggestion:** DuckDB's CSV reader aborts on a malformed row by default. Pass `IGNORE_ERRORS TRUE` to the DuckDB `COPY FROM` / `read_csv` call so bad rows are skipped and their line numbers collected, then surface a dismissible banner: "3 rows skipped (lines 1583, …) — download skipped-rows report." The BugTriage entry for this pattern ("Malformed CSV row can abort import instead of surfacing recovery options") is marked "fix-later" but the DuckDB `ignore_errors` flag makes this a low-effort fix.
**Invariants:** Data never leaves the browser. DuckDB is the boundary — parse errors surface in the UI banner, not as a thrown exception that aborts import.

---

## Suggested action order

1. **[critical] Confirm p-value fix (browser validation)** — Franco should run a regression with |t| < 1 to confirm `216b1ecb` resolved the `tCDF`/`betainc` bug; three separate BugTriage entries (`2026-07-22`, `2026-07-28 ×2`) are all gated on this one confirmation.
2. **[high] Fix Stata workbench** (`stataScript.js`) — users cannot replicate their work in Stata at all; investigate the specific error message Franco sees and address the known UNVERIFIED paths (`ibn.`, `ivregress HAC`, `merge 1:m`).
3. **[high] Simulate replication script** (`SimulateTab.jsx` + new `simulateScript.js`) — the DGP-only export is misleading; resampling and test results disappear after the session with no way to reproduce them.
4. **[high] Browser-confirm N-way FE and FE interactions** (code-complete since `045f5cda`) — two BugTriage entries sit at "likely fixed" blocking a commonly requested workflow.
5. **[high] Explore time-series group references** (`ExplorerModule.jsx` ~line 267) — group-coloured lines are a core time-series feature and the bug makes multi-series charts unreadable.
6. **[medium] LaTeX table width for wide models** (`latexTable.js`) — switch to `tabularx` / `\resizebox` when column count > 4; low-effort, high-visibility fix for PhD users publishing in LaTeX.
7. **[medium] Multiple hypothesis tests: save and replicate** (`CoefficientTestPanel.jsx`) — users lose test results on session exit with no replication path.
8. **[medium] Explore time-series labels** (`ExplorerModule.jsx`) — missing title/axis labels prevent publication-ready exports from the Explore tab.
9. **[medium] Feature tab draft persistence** (`FeatureTab.jsx`) — short `sessionStorage` draft saves would eliminate the most-reported UX frustration (lost `case_when` work).
10. **[medium] Direct elasticity annotation on coefficient rows** (`src/math/` utility + `ModelPlots.jsx`) — the AI prompt already handles log-level interpretation; surfacing it inline without an AI call adds value at near-zero cost.
