# Feedback Analysis Report — 2026-08-19

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **15 open bugs** (3 code-complete pending browser validation, 2 "likely fixed" needing confirmation, 1 possibly already fixed per CLAUDE.md)
- **13 open features**
- **3 open UX / Design items**
- **1 open performance item** (code-complete, pending validation)
- **1 open other item** (parse error)

> ⚠️ **BugTriage staleness note:** CLAUDE.md records several fixes from 2026-07-28 through 2026-08-08 that are not reflected in BugTriage.md. Specifically, `group_summarize` cross-group math (BugTriage status "open") appears to have been fixed on 2026-08-07 per CLAUDE.md. BugTriage.md should be reconciled with CLAUDE.md after each feature-land.

---

## Bugs

### high · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 (Callaway & Sant'Anna) does not work
**File:** `src/services/export/rScript.js`, `src/math/CallawayEngine.js`, `src/services/export/latexTable.js`
**Suggestion:** The BugTriage entry notes `CallawayEngine.js` is the orchestrator. The R replication script likely needs a `did` package call (e.g. `callaway_santanna()`) with the correct ATT(g,t) aggregation contract. Inspect `rScript.js`'s CS DiD case: verify it emits `did::att_gt()` + `did::aggte()` and that the `group`, `time`, and `control_group` arguments match what `CallawayEngine.js` passes. Also confirm `src/services/export/stataScript.js` has a corresponding `csdid` translation or an honest not-supported comment.
**Invariants:** All AI calls go through `AIService.js`. No math logic in export files — translators must read from `result` fields set by `CallawayEngine.js`, not re-derive them.

---

### high · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`, `src/services/export/loadLine.js`
**Suggestion:** `loadLine.js` already provides `buildRLoadLine` / `buildPyLoadLine` / `buildStataLoadLine` for the model pipeline scripts (Report tab). The PlotBuilder's export code in `plotScript.js` does not call them. Add a `loadBlock` header to each language's `plotToR` / `plotToPython` / `plotToStata` output, constructed from `loadLine.js` using the active dataset's `loadOpts`. Thread the active dataset metadata from `ExplorerModule.jsx` → `PlotBuilder.jsx` → the export functions (same pattern as `sessionSnapshot.js` passes `dataLoadOpts` to `generateUnifiedScript`).
**Invariants:** Respect the non-destructive pipeline invariant — the load line must load `rawData`, not a transformed slice. Never use localStorage to pass dataset identity between components.

---

### high · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** "Doesn't work completely" is under-specified; the most likely failure modes are: (a) Stata syntax errors in emitted `do`-files for newer estimator families (Sun-Abraham, Callaway-Sant'Anna, CR2/CR3, N-way FE), since several of these were added after the Stata translator was last audited; (b) `stataScript.js` falling through to a `?? "1"` permissive default for unsupported cases (the exact class of bug fixed for filter step on 2026-08-06). Audit every estimator branch in `stataScript.js` against the current estimator list in CLAUDE.md and confirm each either emits valid Stata syntax or an explicit `/* not supported: ... */` comment. Run `stataScriptValidation` if it exists, or write a new harness covering at minimum OLS, FE, 2SLS, Logit, and EventStudy with each SE type — check that no branch returns an empty string or the bare variable name.
**Invariants:** The architectural convention is "emit an honest 'no Stata equivalent + a NOTE' rather than a silent drop or a fabricated statement" (established for spatial and CR2/CR3 translators).

---

### high · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (`group_summarize`), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** ⚠️ CLAUDE.md records a fix on 2026-08-07 ("The gap was the input SHAPE, not the missing capability") that added a `splitByGroup` path and an input-mode toggle. BugTriage.md still shows status "open" — verify whether the fix landed in `Main-` and update BugTriage accordingly. If the fix is confirmed on this branch, no further action is needed. If not landed, the fix path per CLAUDE.md is: add a long-format input mode (one outcome column + one group column, R's `t.test(y ~ treat)` shape) to the two-sample test surface in `ReshapeTab.jsx`/`SampleTestPanel.jsx`, rather than building a new verb.
**Invariants:** Runner steps must replay on `rawData`, not mutate in place. Pure math in `src/math/SampleTests.js` — no React imports.

---

### medium · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** In `latexTable.js`, the tabular column spec is currently likely a fixed string (e.g. `l cc cc`). When the number of regressors or models grows, the table overflows the page width. Add dynamic column-count logic: (1) compute `nCols = 1 + models.length`; (2) if `nCols > 6`, switch from standard tabular to `\resizebox{\textwidth}{!}{...}` wrapper, or emit `\scriptsize` + `\setlength{\tabcolsep}{3pt}`; (3) alternatively, emit `adjustbox` (LaTeX package) as the outer environment. Add a package hint comment when using non-base-LaTeX environments.
**Invariants:** None — pure export/string-building code, no React or math imports needed.

---

### medium · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already has the math for a single hypothesis test plus R/Python/Stata snippet generation. The gap is UX: `CoefficientTestPanel.jsx` only holds one active test at a time and the snippet is ephemeral (not persisted to IndexedDB). Add a `hypotheses[]` array to the panel's state (or lift it to `ModelingTab`'s pinned model entry in IndexedDB `model_buffer`), allow "Add hypothesis" to push additional rows, and in the R snippet emitter emit a `linearHypothesis` call per vector in a block. Persist alongside the model so it survives reload.
**Invariants:** Persist via `indexedDB.js`, not localStorage. The math stays in `ModelHypothesis.js` — no new math in the component.

---

### medium · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The ACF/PACF/time-series chart rendered in `ExplorerModule.jsx` appears to use Observable Plot directly without exposing a labels panel. Add a collapsible labels section (title, x-axis label, y-axis label) in the same style as the existing PlotBuilder labels panel (`PlotBuilder.jsx` already has this component). For the time series chart specifically, the x-axis label should default to the time column name, and the y-axis label should default to the selected variable name. Thread these as props into the Plot spec's `x: {label}` / `y: {label}` / title mark.
**Invariants:** Inline styles only via `C` from `shared.jsx`. No CSS files, no Tailwind.

---

### medium · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)
**Suggestion:** `INTERPRET_REGRESSION_PROMPT` already has estimator-specific rules (J–R section added 2026-06-03). Add a new rule: when the dependent variable and the regressor are both log-transformed (detectable from column names — `log_*` prefix, or `_ln` suffix, or dictionary `units` field containing "log"), Claude must state "this coefficient is an elasticity: a 1% change in X is associated with a β% change in Y." Thread the variable dictionary (`dict` from `DictionaryTab`) into the user block of the `interpretRegression` call so the prompt has access to units metadata. If dict is unavailable, fall back to name-based heuristic. No new API calls — extend the existing `interpretRegression` call in `AIService.js`.
**Invariants:** All Anthropic calls go through `AIService.js` only. Never add raw `fetch` to Anthropic API elsewhere. Prompt caching contract: `SHARED_CONTEXT` stays as the cached block.

---

### medium · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` currently computes an F-test (Wald). A chi-square variant is simply `χ² = F × q` (where q = number of restrictions), which is asymptotically valid and the default in `car::linearHypothesis` when `test="Chisq"`. Add a `testType` parameter (`"F" | "Chisq"`) to `runHypothesisTest()`, return both statistics, and add a toggle chip in `CoefficientTestPanel.jsx`. The R snippet should pass `test = "Chisq"` to `linearHypothesis`. No new math engine file needed — this is a one-liner transform in the existing function.
**Invariants:** Pure math in `src/math/` — no React imports in `ModelHypothesis.js`.

---

### medium · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` currently holds sub-tab state (e.g. `case_when` rows, `mutate` expression draft) in local component state that is lost on unmount. The fix is sessionStorage-backed draft state keyed by `pid + subTab`. Use `lsSave(key, value)` / `lsGet(key)` from `wrangling/utils.js` BUT note that `lsSave`/`lsGet` are deprecated wrappers around `localStorage` — use sessionStorage directly or, better, a React ref that survives within-session tab navigation (the component unmounts when switching to another wrangling tab but can restore from a ref held in the parent `WranglingModule`). Lift draft state for `case_when`, `mutate`, `if_else`, and `grouped_mutate` to `WranglingModule`'s state, passed down as controlled props.
**Invariants:** IndexedDB is for pipeline/data persistence. Session-level UI draft state (not yet a pipeline step) is acceptable in React state or sessionStorage. Never write uncommitted draft data to IndexedDB `pipeline` store.

---

### medium · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The Data Viewer already pushes `ORDER BY` into SQL for DuckDB-backed datasets (per `getTablePage`). Expose this as a column header click: clicking a column header once sorts ASC, twice sorts DESC, third click removes the sort. For non-DuckDB (JS array) datasets, sort with `Array.prototype.sort` on the page-fetched rows. Persist the sort spec in component state so pagination stays consistent. The Data Viewer's BugTriage history (2026-08-04 fix) shows it already has a `getFilteredRowCount` SQL path — the same CTE pattern can carry an `ORDER BY` clause.
**Invariants:** Inline styles only. The sort is display-only — it must not mutate `rawData` or inject an `arrange` pipeline step (that's the pipeline's non-destructive job).

---

### low · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array)
**Suggestion:** `TOUR_STEPS` in `HelpSystem.jsx` was audited as describing "a two-month-old app" (CLAUDE.md 2026-07-30 note). Review each of the 9 steps against the current module list (7-tab nav: Data/Clean/Explore/Model/Simulate/Calculate/Report) and update text to match. Specifically: (1) the Explore step should mention PlotBuilder (12 geoms incl. tile, facet_wrap); (2) the Model step should list the current estimator families (Linear, Panel, Causal, Non-Linear, GMM, Synthetic Control, Spatial); (3) the Calculate step should mention limits and constant plotting if those shipped; (4) remove any mention of features that moved (WLS is an OLS toggle, not a sidebar estimator). Per CLAUDE.md working conventions: never write a count into help copy — name the things instead.
**Invariants:** UI-only change. No math or API changes.

---

### ⏳ code-complete, pending browser validation · UI · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete per commit `35f4b5a6`. Assign Franco to validate: resize handle works, PNG export captures the resized canvas at correct dimensions. If browser validation uncovers regressions, check `ResizeObserver` callback in `PlotBuilder.jsx` and verify the `width`/`height` passed to the Observable Plot spec matches the rendered container, not a hardcoded constant.
**Invariants:** No CSS files — size controls must use inline styles.

---

### ⏳ code-complete, pending browser validation · UI · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete per commit `35f4b5a6`. Assign Franco to validate that all three plots appear for OLS/WLS, that they render correctly for the pinned model (frozen to `result.spec`, not mutated by sidebar variable changes), and that they disappear appropriately for non-OLS families. Watch for the specific regression: "sidebar edits must not mutate the displayed diagnostics until re-estimation."
**Invariants:** The frozen result pattern (`result.spec`) must be preserved — never re-derive diagnostics from live sidebar state after estimation.

---

### ⏳ code-complete, pending browser validation · UI · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete per commit `5e105529`. Assign Franco to validate: Leaflet heatmap layer renders with KDE cells, palette/opacity controls work, and the 5k-point cap is honoured without crashing. Confirm HTML export includes the heatmap.
**Invariants:** Leaflet loaded via `loadLeaflet()` CDN singleton in `src/components/tabs/spatial/shared/leaflet.js`. CSP `connect-src` must include any new CDN origins the heatmap layer fetches at runtime.

---

## Features

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** `SpatialGeoPlot.jsx` is the Observable Plot / SVG geo-plot tab. The `GridSection` spatial op already computes rect and H3 grids — they produce rows with `_grid_id` + geometry WKT. Add a "Grid" layer type to `GeoLayerConfig.jsx` that reads the WKT geometry column (same path as Polygon layers) and renders cells as filled SVG polygons. The existing `mkGeoLayer` in `geo.js` likely handles WKT polygons already — confirm it handles multi-row grid cells, then expose the layer type in the UI.
**Invariants:** No math in UI files. The grid geometry is computed by `SpatialEngine.js`; the plot layer only reads the result rows.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js`, `src/components/modeling/results/` (or `resultDisplay.jsx`)
**Suggestion:** This is a presentation-only feature — `CausalEngine.js` already computes all the needed fields (LATE, SE, bandwidth, kernel, poly order, n_left, n_right). Add a compact display component `RDDSummaryTable.jsx` in `src/components/modeling/results/` that mimics rdrobust's output format: header row (Method / Coef / Std. Err / z / P>|z| / CI), then a bandwidth row. Mount it in the RDD result branch of `resultDisplay.jsx`. No math change needed.
**Invariants:** No React imports in `src/math/`. The summary component reads from `result` fields, never re-runs the estimator.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" verb to the Calculate workbench alongside Derivatives, Integrals, and Optimization. The underlying CAS (likely math.js or nerdamer — confirm which is used in `CalculateTab.jsx`) supports `lim(expr, var, point)` or equivalent. Expose it as a new sub-section with: expression input, variable selector, limit point (including `Infinity` and `-Infinity`), and optional direction (two-sided / left / right). Show steps if the CAS provides them. LaTeX output should use `\lim_{x \to a}` notation.
**Invariants:** UI-only addition, no changes to `src/math/`. All styling inline via `C` from `shared.jsx`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a constant-entry mode in the equation list: when the user inputs a bare number (or a named constant like `c = 3.14`), treat it as a horizontal line at that y-value. In the Observable Plot / canvas render, detect that the expression has no variable dependency and emit an `hline` rule mark at the constant value, with the same label/color controls as function curves. This mirrors PlotBuilder's existing `hline` geom — the Calculate graph can reuse the same Observable Plot `Plot.ruleY([value])` pattern.
**Invariants:** Inline styles only. No new math engine — pure UI/rendering change.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text filter input at the top of each variable panel (Y, X, W). Filter the visible column list with a case-insensitive `includes` match on column name and optionally on the variable label from the dictionary. Debounce the filter (150 ms) to avoid re-renders on every keystroke. Clear the filter when a variable is selected or the panel closes. This is a pure UI change — the existing variable state management and chip selection logic is unchanged.
**Invariants:** Inline styles only. No dictionary or AI call needed for the filter — purely client-side string match.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`
**Suggestion:** When `aes.color` is set, PlotBuilder currently renders a legend but may not show value labels on individual data points or bars. Add a "Labels" toggle to the layer panel that, when on, adds a `Plot.text` mark positioned at each data point showing the color-dimension value (e.g. country name). For bar/histogram geoms with a color group, show the group name inside or above each bar stack. The R export (`plotScript.js`) should emit a matching `geom_label(aes(label = colorCol))` or `geom_text()` call.
**Invariants:** Inline styles only. Export emitters must stay in sync with the canvas render — add the label mark to both `plotSpec` (canvas) and `plotToR/plotToPython` (export) in the same change.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each pipeline step has a `type` (e.g. `case_when`, `filter`, `log`, `join`). Add a mapping from step type to `{tab, subTab}` in `History.jsx` (mirroring the `STEP_REGISTRY`). When a step is clicked, emit a navigation event (lifted callback or context) to `WranglingModule.jsx` that sets `activeTab` and `activeSubTab` accordingly, then pre-populate the sub-tab editor with the step's current parameters (so the user sees the existing step config ready to edit). This requires the sub-tab editors to accept an `editingStep` prop with the step data — a controlled edit mode as opposed to "add new step."
**Invariants:** This is a UI navigation change only — never modify the pipeline step in place; always reconstruct via the non-destructive `replace` step pattern. Persist via IndexedDB, not localStorage.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Scope is small: a static ~250-row `iso_crosswalk.js` lookup table (ISO2 ↔ ISO3 ↔ English country name) bundled in `src/services/data/`. Add a new pipeline step `country_code` to `runner.js` / `registry.js` with parameters `{col, fromFormat: "iso2"|"iso3"|"name", toFormat: "iso2"|"iso3"|"name"}`. Wire a UI picker in `FeatureTab.jsx` under a new "Country Codes" section. R export: `countrycode::countrycode(df$col, "iso2c", "iso3c")`. Since fuzzy name matching is a larger scope, defer it and document the limitation. **Do not bundle a third-party library** — the crosswalk is small enough to inline.
**Invariants:** New pipeline step must be added to both `runner.js` AND `registry.js` (enforced by `pipelineReliabilityValidation.mjs` T5). Pure JS in the runner — no React imports. Replication exporters must be updated alongside the runner.

---

## UX / Design

### UX · Model · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** `artifactOrder.js` manages a project-scoped global order across saved plots/maps/models. Extend the `ArtifactId` schema with an optional `groupId` field. Add a "New Group" button in `ModelBufferBar.jsx` that creates a named group. Persist group membership and group names in IndexedDB (`model_buffer` store, via a new `groups` sub-key). In the model buffer UI, render groups as collapsible sections with drag-to-reorder within groups. Keep the existing un-grouped "All" view for backward compatibility.
**Invariants:** Persist via `indexedDB.js`, not localStorage. `artifactOrder.js` is the single source of truth for artifact ordering — no secondary ordering state elsewhere.

---

### UX · Explore (filter expression) · 2026-06-14
**Feedback:** Add filter with `%in% c("Spain", "Basque Country")` syntax for filter option (R-style membership)
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** `predicate.js` is the canonical condition language owner (CLAUDE.md: "single owner — never re-implement a filter operator anywhere else"). The `in` / `nin` operators already exist in `OPERATORS` and `evalPredicate`. The gap is the UI: `CleanTab.jsx`'s `FilterBuilder` doesn't expose a multi-value input for `in`/`nin`. Add a tag-input UI element for these operators (comma-separated or chip-based) and ensure the operator's `arity` in `predicate.js` is `"multi"` so `predicateToSQL` emits `col IN (v1, v2, ...)`. No new operator needed — only the UI input mode.
**Invariants:** `predicate.js` is the single owner of the condition language. Any new operator input must go through `OPERATORS` / `evalPredicate` / `predicateToSQL` — never a private matcher in a component. Per the 2026-08-04 fix, a SQL translator must never have a permissive default.

---

### UX · Model (reporting) · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/services/export/latexTable.js`, `src/components/modeling/resultDisplay.jsx`
**Suggestion:** Add a `labelOverrides: {[coefficientKey]: string}` map to the model's persisted `spec` in IndexedDB. In the result display, show each coefficient row with an inline ✎ edit trigger (same pattern as the DataViewer rename). On save, write the override to `labelOverrides` in the model buffer entry. `latexTable.js` reads `labelOverrides` before falling back to the raw coefficient name. RDD should default-populate `labelOverrides` with human-readable names matching the rdrobust convention ("Conventional", "Bias-corrected", "Robust") so new RDD results get good names without user action.
**Invariants:** Persist via `indexedDB.js`. No math change — display-only. The override map should not affect the coefficient key used internally for any test/comparison logic.

---

## Performance

### Performance · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** X5 reconciliation notes "Fase X4 code-complete; browser benchmark pending Franco (`plans/2026-05-22-fase-x4-performance.md`)." The code paths for CSV → DuckDB → OPFS Parquet cache are in place. Key remaining risk: the CSP `connect-src` must include `extensions.duckdb.org` (fixed 2026-07-25 for the parquet extension) and any other DuckDB auto-loaded extensions. Validate in browser with a real 900k-row CSV; confirm tab transitions are under 2 s and the OPFS cache key survives a page reload. If crashes persist, profile the Arrow materialization path in `duckdb.js` — `SELECT *` on 900k rows should never be called for display, only paginated queries.
**Invariants:** Privacy-first: no server-side computation. Display limit ≠ computation limit — the pipeline always runs on the full DuckDB table, never a truncated preview.

---

## Other

### Other · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated)
**File:** `src/DataStudio.jsx`, `src/services/data/parsers/` (CSV path), `src/pipeline/duckdbRunner.js`
**Suggestion:** This looks like a mid-file encoding or quoting anomaly (a value with an unescaped comma or embedded newline). DuckDB's CSV reader is strict by default; the BugTriage entry (2026-06-14) calls for a "resilient-import UX" — surface the failing row number and offer a "skip bad rows" option (`IGNORE_ERRORS = true` in DuckDB `read_csv`) with a warning badge showing the count of skipped rows. If the CSV is small enough for the JS path, handle it in `Papa.parse` with `skipEmptyLines: true` and error collection. In neither case should a single malformed row abort the full import silently.
**Invariants:** Never truncate `rawData` silently — if rows are skipped, show a count and row numbers in the import banner. The pipeline must still replay on whatever `rawData` was loaded.

---

## Suggested action order

1. **Fix CS DiD replication code** — blocking users who need R reproducibility for their thesis workflow; affects `CallawayEngine.js` + `rScript.js` (high, export correctness).
2. **Fix plot replication omits load lines** — every PlotBuilder export currently produces a script that can't be run standalone; affects all Explore users who export code (high, usability).
3. **Fix Stata workbench incomplete output** — Stata is the primary econometrics tool for LMU institutional users; broken Stata export undermines the core value proposition (high, institutional GTM risk).
4. **Reconcile BugTriage.md with CLAUDE.md August fixes** — `group_summarize` cross-group math, filter bugs, and several others appear fixed in CLAUDE.md but remain "open" in BugTriage; a stale triage creates false work items (meta, low effort).
5. **Browser-validate the three code-complete items** — Plot Builder resize (2026-06-10), OLS residual plots (2026-06-11), and spatial heatmap (2026-06-13) are all implemented but unvalidated; these are blocking confirmed bugs with working code.
6. **Add variable searcher to VariableSelector** — low-effort, high-impact UX for large-dataset users; pure UI change, no math or API risk (medium, UX).
7. **Add LaTeX table width adjustment** — affects every Report-tab export with >6 regressors; pure string-building fix in `latexTable.js` (medium, export correctness).
8. **Add chi-square to hypothesis tests** — one-liner math in `ModelHypothesis.js`, matching R's `car::linearHypothesis(test="Chisq")` (medium, math completeness).
