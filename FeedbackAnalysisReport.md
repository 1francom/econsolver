# Feedback Analysis Report — 2026-09-23

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **4 open bugs** (1 critical likely-fixed-awaiting-confirmation, 1 high, 1 high, 1 medium)
- **4 code-complete items pending browser validation** (not re-opened here; listed for tracking)
- **13 open features**
- **3 open UX / Design items**
- **1 open performance item**
- **1 open other**

**Note on discrepancy:** `[2026-06-19] Calculate — Add limits` is listed as open in ClaudeFB.md but the `lim Limit` tool is already wired in `src/components/tabs/CalculateTab.jsx:1595` and the help text at line 1574 confirms it. The ClaudeFB.md entry should be marked ✓. Not counted below.

---

## Bugs

### critical · Math · 2026-07-22 / 2026-07-28 (×3 duplicates)
**Feedback:** "P-values with small numbers — t-value = 0.49 but p-value < 0.001, impossible" / "URGENT THE p-values!!!!!! beta=0.0001 and se=0.002 can never give a p-value < 0.001 with ***" / "P-Values of OLS have a problem when the estimations are insignificant"
**File:** `src/math/LinearEngine.js` — `tCDF` / `betainc` / `betacf` (Lentz continued-fraction fix commit `216b1ecb`)
**Suggestion:** Fix is code-complete (commit `216b1ecb`, 2026-07-28 — replaced fixed-step Riemann sum with Lentz continued-fraction `betainc`). BugTriage status is "likely fixed — needs confirmation". Franco should confirm in-browser against a large-n regression where the old code produced false hyper-significance (t ≈ 0, p expected ≈ 1). If confirmed, mark all three ClaudeFB.md entries and the three BugTriage rows as fixed.
**Invariants:** none — pure math file, no React.

---

### high · Export · 2026-07-17
**Feedback:** "Stata workbench doesn't work completely"
**File:** `src/services/export/stataScript.js` — also `src/components/modeling/CodeEditor.jsx`
**Suggestion:** BugTriage status is `open`. The HC2/HC3 fix pass (2026-07-18, same week) addressed SE-type emission for panel/IV/nonlinear models, but the BugTriage row was never closed. Scope the remaining gap: (1) run the existing `seTolerances.js` + `exportValidation.js` harnesses against Stata output specifically and collect any failing cases; (2) the known `ibn.` no-intercept Stata path is documented as UNVERIFIED against live Stata — that is likely part of "doesn't work completely"; (3) the `joinby` vs `merge 1:m` cardinality gap has a NOTE in place but no fallback. Fix approach: verify the `ibn.` through-the-origin Stata case using `noconstant` with `ibn.` vs the no-intercept R/Python which are confirmed correct, then close the BugTriage row.
**Invariants:** Stata emitter lives in `src/services/export/stataScript.js` — pure JS, no React, no math imports.

---

### high · Export · 2026-07-28
**Feedback:** "Update the replication script, currently it replicates only the DGP"
**File:** `src/components/tabs/SimulateTab.jsx` — `generateSimScript()` (line 519)
**Suggestion:** `generateSimScript` stops after the DGP draw (variable definitions from distributions). The downstream pipeline — Monte Carlo loop, resampling, sample tests, and per-replication estimation — is not emitted. Add a second section to the script that (a) wraps the DGP in a `for`-loop for MC mode (same iteration count as `mcReps`), (b) calls the user's chosen estimator on each simulated dataset, and (c) appends the collection of statistics / coefficients. The Simulate tab already drives `runOLS`/`twoSampleMeanTest` etc. in-browser — the script just needs to reproduce that logic in R/Python. Keep the DGP section unchanged; append after it conditionally on whether MC mode is active.
**Invariants:** `src/math/` must stay React-free. `SimulateTab.jsx` is a UI file — any new pure math helpers (distribution draw code) should go in `src/math/dgpDraw.js` or `src/math/dgpScript.js`, not inline in the component.

---

### medium · UI · 2026-07-28
**Feedback:** "References of the group in time series doesn't work"
**File:** `src/ExplorerModule.jsx` — `aggregateTimeSeries` (line 182) / `fetchAggregateTimeSeriesSQL` (line 212) / `TimeSeriesView` component (~line 1455)
**Suggestion:** The time-series component (`ExplorerModule.jsx:~1455`) correctly aggregates by `grpCol` and renders each series with a distinct color from `COLORS`. The most likely failure mode is the DuckDB-backed SQL path (`fetchAggregateTimeSeriesSQL`) not matching the JS path when `grpCol` contains a non-string type (e.g. numeric group IDs): `String(r.grp ?? "")` in the JS path may not agree with how `grpCol` was cast in the SQL `GROUP BY`. Check whether `grpCol` selector correctly passes the raw column name when the column is numeric, and verify `colorFor(s.grp, si)` uses stable keys across re-renders (a `useState`-keyed color map can drift if group ordering changes between the SQL and JS paths). Reproduce with a numeric group column on a DuckDB-backed dataset.
**Invariants:** none specific — UI component.

---

## Code-Complete Items Pending Browser Validation (tracking only)

These items are **not re-opened** — code was merged. Listed here so Franco can confirm and close them:

| Date | Item | File | Commit |
|------|------|------|--------|
| 2026-06-10 | Plot Builder resize + PNG export aesthetics | `src/components/PlotBuilder.jsx` | `35f4b5a6` |
| 2026-06-11 | Y~Ŷ / Residual vs Fitted / Q-Q for OLS | `src/components/modeling/ResidualPlots.jsx` | `35f4b5a6` |
| 2026-06-13 | Spatial heatmap restored as Leaflet layer | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` | `5e105529` |
| 2026-06-14 | Coefficient plots / diagnostics freeze on sidebar edits | `src/components/ModelingTab.jsx` | `35f4b5a6` |

---

## Features

### Data · 2026-06-06
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/services/export/latexTable.js` — `buildStargazer` (line 21); also `src/components/modeling/` result-display layer
**Suggestion:** `buildStargazer` already accepts a `varLabels` option object (line 22) — the infrastructure is there. The gap is that the UI has no way to pass user-edited labels into it. Add a collapsible "Rename for table" section in `CodeEditor.jsx` or the result panel: a key-value editor where each `varName` row gets an optional display name, stored in a `varLabels` map in component state and passed to `buildStargazer`. For RDD specifically, wire the three canonical names (`D`, `running_c`, `D_x_running_c`) as pre-populated defaults that the user can override.
**Invariants:** `buildStargazer` is pure JS; any UI additions must use inline styles from the `C` object, no CSS files.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** "Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression"
**File:** `src/math/CausalEngine.js` — `runSharpRDD` return value; `src/components/modeling/` results panel
**Suggestion:** `runSharpRDD` already returns `{ LATE, se, tStat, pValue, ci, bandwidth, nLeft, nRight, kernel, polyOrder }`. Add a dedicated `RDDSummaryTable` component (alongside `ModelPlots.jsx`) that formats these fields in the `rdrobust`-style layout: estimand, h (IK bandwidth), n_left/n_right, coef, se, CI, p. The component is display-only (no math changes). Wire it above the coefficient table in the RDD result panel.
**Invariants:** The new component lives in `src/components/modeling/` and is React — it must not import from `src/math/` for anything beyond reading the already-computed result object.

---

### Export · 2026-07-05
**Feedback:** "Adjust the size of LaTeX tables when the number of columns increases"
**File:** `src/services/export/latexTable.js` — `buildStargazer` (line 173); column format at line 39
**Suggestion:** The table emits a plain `tabular` environment (`line 173`). For more than 4 models, wrap it in `\resizebox{\textwidth}{!}{…}` or switch to `tabularx` with `X` columns and a `\textwidth` width argument. The column count threshold (e.g. >3 models) can be inferred from `colsN` at line 38. Adding `\usepackage{tabularx}` or `\usepackage{graphicx}` (for `\resizebox`) to the emitted preamble comment would also be helpful.
**Invariants:** `latexTable.js` is pure JS (no React); the `C` style invariant does not apply here.

---

### Clean / Pipeline · 2026-05-19 (fix-later)
**Feedback:** "Add filter(regionname %in% c('Spain (Espana)', 'Basque Country (Pais Vasco)')) for filter option"
**File:** `src/components/wrangling/CleanTab.jsx` — filter operator picker; `src/pipeline/predicate.js` — `OPERATORS`, `evalPredicate`, `predicateToSQL`
**Suggestion:** `predicate.js` already has an `in` operator that the runner honours. The gap is the UI: `FilterBuilder` in `CleanTab.jsx` does not expose a multi-value `%in%` / "is one of" selector. Add an `in` row to the operator dropdown for string and numeric columns, with a comma-separated value input or a chip-based picker. Wire through `predicateToSQL` (which already has an `in` case) and the existing R/Python/Stata translators in `predicateExport.js`. The `nin` (not-in) operator is the symmetric case to add at the same time.
**Invariants:** `predicate.js` is the single owner of the condition language — never re-implement a filter operator elsewhere. `evalPredicate` and `predicateToSQL` are the only modification points.

---

### Model · 2026-07-09
**Feedback:** "Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster"
**File:** `src/components/modeling/VariableSelector.jsx` — `VariableSelector` component (line 55)
**Suggestion:** Add a text `<input>` filter at the top of each variable list column (Y, X, W). Filter `xwCols` by case-insensitive substring match against the typed query. State is local (`useState(""`)) — no persistence needed. The filtered list replaces the current `xwCols.filter(…).map(…)` render in the Y/X/W sections. This is a pure UI change; the underlying variable arrays and setter callbacks are unchanged.
**Invariants:** Inline styles from `C` object only. No external UI libraries.

---

### Clean / Pipeline · 2026-07-17
**Feedback:** "Add an option to click on one pipeline step and go to the corresponding subsection automatically"
**File:** `src/components/wrangling/History.jsx` — step list items; `src/WranglingModule.jsx` — tab state setter
**Suggestion:** Each step card in `History.jsx` already knows its `type` (from `STEP_REGISTRY`). Add a small "jump" icon (→) to each card that calls a new `onJumpToStep(step)` prop. In `WranglingModule.jsx`, wire `onJumpToStep` to read `step.type` against a lookup table (e.g. `rename`/`drop`/`filter` → Clean tab, `lag`/`log`/`mutate` → Feature tab, `group_summarize` → Reshape tab) and set the active tab accordingly, plus scroll to the relevant subsection panel via a `ref` or `id`. Keep the existing History click-to-preview behaviour unchanged.
**Invariants:** Tab routing stays in `WranglingModule.jsx` — do not create a second tab-setter outside it.

---

### Clean · 2026-07-19
**Feedback:** "Add a function for Country Codes — iso2/iso3/country-name crosswalk transform"
**File:** `src/components/wrangling/FeatureTab.jsx` — Formatting subsection; `src/pipeline/runner.js` — `add_column` or a new `country_code` step type
**Suggestion:** The simplest scope is a static lookup-table step (iso2 ↔ iso3 ↔ country name, ~250 rows). Add a `country_code` step type to `runner.js` + `registry.js` with params `{ sourceCol, sourceFormat, targetFormat }`. In `FeatureTab.jsx`, add a "Country Code Crosswalk" panel under Formatting. The static table (JSON) can live in `src/math/` as pure data. Export: R uses `countrycode::countrycode`, Python uses `pycountry`, Stata uses `kountry` (with a `ssc install` note).
**Invariants:** The lookup table in `src/math/` must be pure JSON/JS (zero React). Pipeline step type must be added to both `runner.js` and `registry.js` simultaneously (registry ↔ runner sync is enforced by `pipelineReliabilityValidation.mjs`).

---

### Clean · 2026-07-17
**Feedback:** "Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back"
**File:** `src/components/wrangling/FeatureTab.jsx` — local form state for `case_when`, `if_else`, `mutate` sub-panels
**Suggestion:** Each transformation sub-panel (case_when, if_else, mutate, etc.) has its own `useState` that resets when the component unmounts or the tab changes. Persist draft state to `sessionStorage` keyed by `${pid}_${stepType}_draft` on every change and restore it on mount. This is a per-viewer convenience (appropriate for `sessionStorage` per CLAUDE.md), not persisted data. Keep draft state scoped to `pid` so switching datasets doesn't bleed across.
**Invariants:** `sessionStorage` is the correct choice here per project architecture rules (IndexedDB is for pipeline/data storage; localStorage is deprecated for those purposes; drafts are per-viewer, per-session conveniences).

---

### General · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time"
**File:** `src/services/Persistence/artifactOrder.js` — `getArtifactOrder`/`saveArtifactOrder`; `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Add a `groupId` field to the artifact order entries (default `"default"`). In `ModelBufferBar.jsx`, render group tabs or a group dropdown above the pinned-model list. `artifactOrder.js` already provides stable IDs via `makeArtifactId`/`parseArtifactId` — extend the schema with an optional `group` string and persist via `saveArtifactOrder`. Comparison (`compRows` in G13) should filter to the currently-active group. Group names live in IndexedDB alongside the order list. Do not change `EstimationResult` or engine return shapes.
**Invariants:** Persistence is in IndexedDB via `indexedDB.js` — do not use localStorage for group state.

---

### Model · 2026-07-22
**Feedback:** "Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators"
**File:** `src/components/modeling/ModelPlots.jsx` — scatter / partial plot sections; `src/components/PlotBuilder.jsx`
**Suggestion:** The partial-plot panel already offers scatter with a fitted line. Extend with: (1) a `nbins` slider for binning the x-axis (computing bin means and then plotting averages — the `rdplot`/`binsreg` pattern), (2) a CI band toggle on the scatter. These are display-only additions to the existing `PartialPlot` component. No new math engine work required.
**Invariants:** Any new math (mean-per-bin computation) must live in a pure function in the component file or a helper imported from outside `src/math/` if it is trivial (a `useMemo` groupBy is fine inline).

---

### Spatial · 2026-05-20
**Feedback:** "Add grids to the plot, not only in the OMS map"
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` — geo-plot canvas layer config; `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** The Leaflet map tab already supports grid cells as a layer type (`layers.js`). The Observable Plot / SVG geo-plot tab (`SpatialGeoPlot.jsx`) does not expose a grid layer option. Add a grid layer type to `GeoLayerConfig.jsx` for the Plot tab, rendering the grid cells as SVG rectangles using the existing `wktToLeaflet` / `parseWktRings` utilities from `src/components/tabs/spatial/shared/wkt.js`. Source data: a dataset column containing WKT `POLYGON` grid cells (already produced by the pipeline's `sp_grid_assign` step).
**Invariants:** Zero React in `src/math/` — grid geometry parsing (`wkt.js`) is already in `src/components/tabs/spatial/shared/`, which is correct. Do not duplicate the WKT parser.

---

### General · 2026-07-17
**Feedback:** "Guide tour must be updated"
**File:** `src/components/HelpSystem.jsx` — `TOUR_STEPS` array
**Suggestion:** Review all 9 `TOUR_STEPS` entries against the current tab structure (Data/Clean/Explore/Model/Simulate/Calculate/Report + Spatial). Steps referencing the old two-pane layout, the removed "How to …" HintBox titles, or estimators/geoms that have moved since the last tour update should be rewritten. Per CLAUDE.md working convention, do not write counts ("9 steps", "14 estimators") — name the things instead. The tour button lives in `WorkspaceBar.jsx`; `TOUR_STEPS` is the only file that needs editing.
**Invariants:** `HintBox` title prop must be the MODULE name, never a "How to …" string.

---

## UX / Design

### UX · 2026-07-05
**Feedback:** "Replication codes of the plots must have the lines to load datasets as well" — RESOLVED per BugTriage 2026-07-30 fix (browser validation pending Franco). Not re-opened.

### UX · 2026-05-19 (fix-later)
**Feedback:** "Add filter(regionname %in% c(...)) for filter option in Explore"
**File:** `src/ExplorerModule.jsx` — filter UI; shares `predicate.js` with Clean
**Suggestion:** Covered jointly with the CleanTab `%in%` feature above. Once `predicate.js` gains an `in` operator UI in `CleanTab.jsx`, the same operator should become available in `ExplorerModule.jsx`'s filter panel (they both call `evalPredicate`). No separate work needed beyond making `predicate.js`'s `in` operator accessible in Explore's filter chip builder.
**Invariants:** `predicate.js` is the single owner of the condition language.

---

## Performance

### high · Data · 2026-05-08
**Feedback:** "Big datasets generate crashes (21MB) over 900k rows"
**File:** `src/DataStudio.jsx` — `parseFile` dispatch; `src/services/data/duckdb.js` — DuckDB singleton; `src/pipeline/duckdbRunner.js`
**Suggestion:** DuckDB-Wasm integration is code-complete (Fase 0–8, OPFS Parquet cache, 500-row UI preview). BugTriage `2026-06-10` marks DuckDB for `.dta` as fixed. The original 21 MB crash report predates DuckDB and is now routed through it. The performance spec (`plans/2026-05-22-fase-x4-performance.md`) says browser benchmarking is still pending Franco. **Action required**: Franco should load the 21 MB dataset in-browser and confirm no crash. If it still crashes, the failure is likely in the OPFS Parquet write path (check the CSP `connect-src: extensions.duckdb.org` fix from 2026-07-25 is in the live Vercel deployment's `vercel.json`).
**Invariants:** Data never leaves the browser — no server-side computation. DuckDB is the data boundary; never materialize the full Arrow result in JS objects for large datasets.

---

## Suggested Action Order

1. **Confirm p-value fix in-browser** (critical, 3 open reports) — run a large-n OLS with an insignificant coefficient and verify p ≈ 1 for t ≈ 0; close all three BugTriage rows if confirmed.
2. **Close code-complete items** — Franco browser-validates the 4 items in the "Pending browser validation" tracking table (OLS residual plots, spatial heatmap, plot resize, diagnostics freeze) and marks them fixed.
3. **Fix Simulate replication script** (high) — extend `generateSimScript` in `SimulateTab.jsx` to include the Monte Carlo loop and downstream analysis; this is the single most actionable open bug in the export pipeline.
4. **Diagnose Stata workbench gap** (high) — run `exportValidation.js` against stataScript.js specifically, reproduce the `ibn.` no-intercept issue, and close or split the BugTriage row.
5. **Add `%in%` / "is one of" operator to CleanTab filter** (medium, unblocks Explore filter too) — `predicate.js` + `CleanTab.jsx` + `predicateExport.js`.
6. **LaTeX table `resizebox` for wide model tables** (medium, fast win) — single change in `latexTable.js:buildStargazer`.
7. **Variable searcher in VariableSelector** (medium, frequent user friction) — local `useState` filter on `xwCols` in `VariableSelector.jsx`.
8. **Draft state preservation in FeatureTab** (medium) — `sessionStorage` persist per `${pid}_${stepType}_draft`.
9. **Time series group reference diagnosis** (medium) — investigate `colorFor` / `grpCol` type coercion on DuckDB-backed datasets in `ExplorerModule.jsx`.
10. **Mark `[2026-06-19] Calculate — Add limits` as done in ClaudeFB.md** — limits are fully wired at `CalculateTab.jsx:1595`; entry was never closed.
