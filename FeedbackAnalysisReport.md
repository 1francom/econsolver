# Feedback Analysis Report — 2026-08-25

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 13 open bugs (3 code-complete, browser validation pending) · 10 open features · 1 open UX · 1 open performance · 1 open other

Cross-reference note: Items marked ~~strikethrough~~ or ✓ in `ClaudeFB.md` and `fixed` in `BugTriage.md` are excluded. Items with BugTriage status `code-complete` (browser validation pending Franco) are included as open until Franco confirms. Items reconciled as "verified fixed" in the 2026-06-14 X5 table but still un-struck in `ClaudeFB.md` are omitted (treated as resolved per BugTriage authority).

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 (Callaway & Sant'Anna) does not work
**File:** `src/services/export/rScript.js` (line 1432 — `case "CallawayCS":`), `src/math/CallawayEngine.js`
**Suggestion:** The `CallawayCS` block in `rScript.js` exists but R validation against `callawayBenchmarks.json` is noted as pending in CLAUDE.md. Run `callawayRValidation.R` to generate the benchmarks, then audit whether the emitted `csdid` / `did` R calls match the engine's ATT aggregation convention (simple/group/calendar/dynamic). If the R snippet uses `drdid` internals directly, switch to the published `did` package (`csdid` from `drdid`) with matching `gref`, `control_group`, and `bstrap` arguments.
**Invariants:** All API calls via `AIService.js` only; export scripts must not require Claude — these are static string emitters.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/components/PlotBuilder.jsx` (line 1363 — `scriptPreamble(language)` comment), `src/services/export/loadLine.js`
**Suggestion:** `PlotBuilder.jsx`'s `scriptPreamble` currently only injects model-context code. Thread the active dataset's `loadOpts` (already available in `sessionState`) into `PlotBuilder` props, then call `buildRLoadLine` / `buildPyLoadLine` / `buildStataLoadLine` from `services/export/loadLine.js` at the top of each emitted script, mirroring how `generateWorkspaceScript` handles multi-dataset loads. Grep shows `loadLine.js` is imported in report scripts but not in `plotScript.js` / `PlotBuilder`.
**Invariants:** `buildRLoadLine` honours `loadOpts` (delimiter, encoding, sheet) — do not hardcode `read.csv`.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/services/export/stataScript.js`, `src/components/modeling/CodeEditor.jsx`
**Suggestion:** The 2026-07-17 HC2 pass fixed SE routing but the complaint pre-dates it. Likely candidates: (a) `xtreg y x, fe vce(cluster a b)` syntax error for two-way cluster in panel (already flagged in CLAUDE.md 2026-07-17 BugTriage note — fixed by routing to `reghdfe`, but check if two-way cluster path is exercised in practice); (b) `date_parse` step emitting wrong Stata syntax; (c) CS DiD's Stata snippet emitting an unsupported package call. Run the existing export integrity harness (`npm run lint:undef && node src/pipeline/__validation__/pipelineReliabilityValidation.mjs`) and manually paste several generated Stata scripts into Stata to identify which estimators fail. Then file a focused fix per estimator rather than a bulk rewrite.
**Invariants:** Stata gets an honest "no support" note rather than a silent wrong answer for unsupported SE types.

---

### [high] · Pipeline / Wrangling · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE as difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (line 528 — `case "group_summarize":`), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** Per CLAUDE.md (2026-08-07 bug entry), `splitByGroup` + a long-format input-mode toggle was identified as the right fix. The engine already has `twoSampleMeanTest` in `SampleTests.js`; the gap is exposing it via the `group_summarize` UI. Add an optional "Compare groups" mode in `ReshapeTab.jsx` that accepts one outcome column + one group column, calls `twoSampleMeanTest` in long-format mode, and emits the difference-in-means, SE, t-stat, and p-value as a summary row. Wire this as a new `group_compare` step type in `registry.js` and `runner.js` so it is pipeline-replayable.
**Invariants:** runner.js steps always replay on `rawData` — never mutate in place; `src/math/SampleTests.js` is pure JS, no React imports.

---

### [high] · Math / Model · 2026-07-09
**Feedback:** Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage marks this "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation." Confirm by testing the FE panel UI: open a dataset with a categorical column (e.g. sector), select it as a fixed-effect dimension alongside entity/time, and verify coefficients match `fixest::feols(y ~ x | entity + time + sector)` to 1e-5. If the UI gate still restricts to two dimensions, update `ModelConfiguration.jsx`'s FE dimension picker to allow any categorical column.
**Invariants:** N-way FE validated against R fixest — 18/18 checks at 1e-5/1e-3 tolerance per CLAUDE.md. Do not regress on validated benchmarks.

---

### [high] · Math / Model · 2026-07-09
**Feedback:** Add also FE with interactions
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions` step)
**Suggestion:** BugTriage marks this "likely fixed — shipped in `045f5cda` (#104); needs confirmation." Verify by running `feols(y ~ x | g1^g2)` equivalent: create an interaction of two categorical FE in `ModelConfiguration.jsx`, estimate, and compare against R. If the engine accepts interaction terms but the UI has no way to specify them, add a `+` / `×` toggle to the FE dimension list in `ModelConfiguration.jsx`.
**Invariants:** `factor_interactions` step already in `runner.js`/`registry.js` — reuse rather than duplicate.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js` (`buildStargazer`, line 21), `src/ReportingModule.jsx`
**Suggestion:** Wrap the `tabular` environment in `\resizebox{\textwidth}{!}{...}` when the number of model columns exceeds a threshold (e.g. > 4). Alternatively, emit `\adjustbox{max width=\textwidth}{...}` (requires the `adjustbox` LaTeX package) which scales more gracefully. Add a note at the top of the emitted LaTeX block listing required packages (`booktabs`, `adjustbox`). The column count is known from `models.length` at generation time.
**Invariants:** `buildStargazer` is a pure string emitter — no React, no API calls.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js` (line 179 — `waldTest`), `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` already computes a `jointResult.chiSq`. Extend it with a "Save hypothesis" button that appends the current test spec (terms, null values, joint/individual) to a `savedHypotheses[]` array in component state. Add an "Export all" button that calls `generateModelHypothesisScript` for each saved test and concatenates the snippets per language. Persist `savedHypotheses` in `IndexedDB` via `indexedDB.js` keyed by `pid` so they survive page reload.
**Invariants:** Persist via `indexedDB.js`, not `localStorage`. `ModelHypothesis.js` is pure JS — no React.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` (line ~1309 — time series section), `src/ExplorerModule.jsx` (`SvgHistogram`/`SvgBarChart` already accept `title`/`xLabel`/`yLabel` props at line 219)
**Suggestion:** The `SvgHistogram` and `SvgBarChart` components already accept `title`, `xLabel`, and `yLabel` props. The time series line chart rendered in the `tsView === "line"` block likely uses a different SVG component that lacks them. Add three text inputs (title, x-axis label, y-axis label) beneath the time series controls, mirroring the existing label panel in `PlotBuilder.jsx`, and pass them into the chart component. Wire pinned time series plots to persist the label values in the pin metadata.
**Invariants:** Inline styles via `C` object; no external UI libraries.

---

### [medium] · AI / Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`, line ~99 — log rules already present)
**Suggestion:** The prompt already handles log-log (elasticity) and log-level (semi-elasticity) for single transformations. The gap is likely **cross-equation or partial elasticities** (e.g., dy/dx × x/y computed from coefficients when neither y nor x is logged but the user asks for elasticity at the mean). Add an `ELASTICITY RULE` block that: (1) recognises a user request for elasticity in the result narrative, (2) computes β × x̄/ȳ using the sample means from the result, and (3) emits a one-line elasticity computation. Thread the relevant summary stats from `EstimationResult` into the prompt's user block (they are already in `metadataExtractor.js`).
**Invariants:** All AI calls via `AIService.js` `callClaude()`.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js` (line 179 — `waldTest`), `src/components/modeling/CoefficientTestPanel.jsx` (line 211 — `chiSq` already rendered)
**Suggestion:** `waldTest` already returns `chiSq` and `chiSqPval` (confirmed by grep: `jointResult.chiSq` rendered at line 211 of `CoefficientTestPanel.jsx`). If the user report means the F-test displayed by default should also have a chi-square alternative, add a toggle (F vs χ²) in `CoefficientTestPanel.jsx` that switches the displayed statistic. If the report means individual coefficient tests lack χ², expose `(t²)` as χ²(1) under the existing `coefficientHypothesisTest` result. Clarify with Franco which display is missing.
**Invariants:** No React in `ModelHypothesis.js`.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx` (line 453 — `FeatureBuilder` with `if_else` / `case_when` state)
**Suggestion:** `FeatureTab.jsx` holds `mode`, `cwCases`, and `cwNN` in local `useState` — they reset on unmount. Lift the draft state to `WranglingModule.jsx`'s parent state (or use `sessionState`) so it survives tab switches within the same session. A simpler alternative: `useEffect` to write draft to `sessionStorage` keyed by `pid + "feature_draft"` on every change, and restore on mount. Use `sessionStorage` (session-scoped, appropriate for ephemeral drafts) rather than `IndexedDB` (overkill for this transient state).
**Invariants:** `IndexedDB` for pipeline/data persistence; `sessionStorage` is acceptable for ephemeral UI draft state (not classified as "data storage" in the invariants). Do not use `localStorage`.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The `DataViewer` component (within `ExplorerModule.jsx`) renders a table with no sort controls. Add a click-on-column-header sort toggle (ascending / descending / unsorted) that appends an `arrange` step to the pipeline via `addStep`, mirroring how cell edits emit a `set_where` step. For DuckDB-backed tables, the `arrange` step already has a SQL fast path in `duckdbRunner.js` — it will be efficient. Show a sort indicator (▲/▼) in the header cell while a sort is active.
**Invariants:** Pipeline steps replay on `rawData` — the sort must emit an `arrange` step, never mutate `rawData` directly. Use `predicate.js` canonical operators.

---

### [medium] · UI (code-complete, pending browser validation) · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage marks this "code-complete 2026-06-14 (`35f4b5a6`) — native vertical resize with bounded responsive canvas." Requires Franco to browser-validate: test drag-resize of the plot panel, then click "Export PNG" and confirm the exported image dimensions match the panel's user-selected height without cropping or distortion.
**Invariants:** None specific; ensure `ResizeObserver` cleanup on unmount.

---

### [high] · UI (code-complete, pending browser validation) · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage marks this "code-complete 2026-06-14 (`35f4b5a6`) — OLS/WLS diagnostics now render from frozen `result.spec`." Requires Franco to browser-validate: run an OLS model and confirm all three plots (Y~Ŷ, Residual vs Fitted, Q-Q) appear; then change a variable in the sidebar and confirm the plots do NOT update until "Estimate" is clicked again.
**Invariants:** None specific.

---

### [medium] · UI (code-complete, pending browser validation) · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** BugTriage marks this "code-complete 2026-06-14 (`5e105529`) — Heatmap restored as a Leaflet map layer with KDE, palette/opacity controls, legend, HTML export, 5k-point cap." Requires Franco to browser-validate: load a dataset with lat/lon columns, switch to Spatial → Map tab, add a Heatmap layer, verify the KDE renders correctly, and test the HTML export.
**Invariants:** None specific.

---

## Features

### Model · 2026-05-11
**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength
**File:** `src/math/CausalEngine.js`, `src/components/modeling/results/` (IV results panel)
**Suggestion:** The X5 reconciliation table marks this "verified fixed" (`CausalEngine.andersonRubinCI` + `IVResults` weak-IV panel). If the feature is still not appearing in the UI, confirm that `andersonRubinCI` is called from the 2SLS estimation path in `ModelingTab.jsx` and that the result is passed to the IV results display. Search for `andersonRubin` in `ModelingTab.jsx` to confirm wiring.
**Invariants:** `CausalEngine.js` is pure JS — no React imports.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/services/export/latexTable.js` (`buildStargazer`), `src/components/modeling/ModelComparison.jsx`
**Suggestion:** BugTriage marks this "fix-later — post-launch reporting metadata feature." Add an optional `varLabels` map (column name → display label) to the `buildStargazer` options object. Expose a small label-editor UI in `ModelComparison.jsx` or the `CodeEditor.jsx` replication pane: a mini table of detected variable names with editable text fields. Store the label map in `IndexedDB` keyed by `pid`.
**Invariants:** `IndexedDB` for persistence; `latexTable.js` is a pure string emitter.

---

### Model · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` already manages a global order for saved plots/maps/models via `getArtifactOrder`/`saveArtifactOrder`. Extend the artifact metadata schema with an optional `groupId` / `groupName` field. In `ModelBufferBar.jsx`, add a "New group" button that creates a named group and renders pinned models as collapsible groups. Persist group assignments in `IndexedDB`. The comparison view should filter to one group at a time (or all) via a group selector dropdown.
**Invariants:** `IndexedDB` for persistence; no `localStorage`.

---

### Calculate · 2026-06-19 & 2026-07-04
**Feedback (combined):** (a) Add limits (calculate tab); (b) Allow plotting constants in the graph alongside function curves
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** (a) Limits: Add a `lim(expr, var, value)` function to the CAS/symbolic engine already powering the Calculate tab. A lightweight one-sided numeric limit (substitute `value ± ε` and take the average) would satisfy most use cases. (b) Constants: The multi-equation canvas already plots multiple functions; simply allow a number literal (e.g. `y = 2.5`) as a valid "equation" that renders as a horizontal line — the graph-rendering path likely already handles this if the expression evaluates to a scalar.
**Invariants:** `CalculateTab.jsx` is a UI component; pure math helpers should live outside it in a utils or engine file with no React imports.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` knows its `type`. Add an `onNavigate(type)` callback prop that `WranglingModule.jsx` implements: given a step type, compute the target tab (e.g. `rename`/`drop` → Clean, `log`/`lag` → Feature, `group_summarize` → Reshape) and call `setActiveTab`. Highlight the corresponding subsection/accordion within that tab so the user sees where the step lives. Map the step-to-tab routing from `STEP_REGISTRY`'s `category` field.
**Invariants:** `STEP_REGISTRY` in `registry.js` is the single source of truth for step categories.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` step type with a small (~250-row) static lookup table (ISO 3166-1 alpha-2/alpha-3 / country name) bundled as a JS constant. The step accepts an input column (name or code), a `from` format (`name`|`iso2`|`iso3`), and a `to` format. Expose it in `FeatureTab.jsx` as a new transform card. Register in `registry.js` and implement in `runner.js` (no external API). Export translators in `stepTranslators.js`.
**Invariants:** `runner.js` replays on `rawData`; the lookup table must be a bundled constant, not a fetch (privacy-first).

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large number users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a `<input type="search" placeholder="Filter variables…">` field above each variable panel (Y, X, W). Filter the displayed variable chips by matching the input against column names. A simple case-insensitive `.includes()` match is sufficient. Persist the search query in local state (clears on close). No backend changes needed.
**Invariants:** Inline styles via `C` object; no external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set in `PlotBuilder`, the legend currently shows colour swatches. Extend the legend to include the group count (`n=`) and optionally the group mean of the Y variable beside each label. In `ExplorerModule.jsx`'s categorical bar chart (`SvgBarChart`), show the count as a label at the end of each bar (already done for histograms). The change is purely in the SVG render — no engine changes.
**Invariants:** Inline styles via `C` object.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** This needs a design decision before implementation. Suggested approach: add a `namedLists` map to `sessionState.jsx` (keyed by list name, values are arrays of scalars). Expose a "Named Lists" panel in the Data or Clean tab where users can create, name, and edit lists. In `predicate.js`'s `evalPredicate`, resolve list-name references in `in`/`nin` operators against the session lists. Persist in `IndexedDB` per `pid`. This unblocks both `filter %in% listName` and `case_when` with list membership.
**Invariants:** `IndexedDB` for persistence; `predicate.js` is the single owner of filter operator evaluation — do not re-implement `in`/`nin` elsewhere.

---

## UX / Design

### General · 2026-09-17 (tour)
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (line 11 — `TOUR_STEPS`)
**Suggestion:** Audit `TOUR_STEPS` against the current 7-tab nav (`Data/Clean/Explore/Model/Simulate/Calculate/Report`) and all major features shipped since the tour was written (N-way FE, Callaway-Sant'Anna, Bacon decomposition, Sun & Abraham, PlotBuilder geom_tile + facet_wrap, CR2/CR3, etc.). Per CLAUDE.md working conventions: "Never write a count into help copy — name the things instead." Update each step's `content` string and add steps for any tabs/features that lack coverage. The `HintBox` panels in each module also need a parallel audit (last audited 2026-07-30 per CLAUDE.md — now two months stale again).
**Invariants:** `TOUR_STEPS` entries use `target` CSS selectors — verify each selector still matches a rendered DOM element after changes.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** The X5 reconciliation marks this "Fase X4 code-complete; browser benchmark pending Franco." The DuckDB + OPFS Parquet cache pipeline is implemented. If the crash still occurs, the most likely remaining cause is the initial parse (CSV/Stata → JS array → DuckDB `insertCSV`) materializing the full 21MB in JS memory before registration. Audit `parseFile` in `DataStudio.jsx` for large files: ensure the parse result goes directly to `registerDuckDBTable` and the `rawData` object carries only the 500-row preview plus the `_duckdb` handle — never the full array. Check that DuckDB's OPFS Parquet write path (`COPY … TO … FORMAT PARQUET`) is reached and that `extensions.duckdb.org` is in `connect-src` in `vercel.json` (previously a crash source per CLAUDE.md).
**Invariants:** Never truncate `rawData` before passing to `runner.js`/`duckdbRunner.js` — full computation must run on DuckDB. Display is limited to 500 rows; computation is not.

---

## Other

### Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 — `China,CHN,2010,2425.47...`
**File:** `src/services/data/parsers/`, `src/DataStudio.jsx`
**Suggestion:** BugTriage marks malformed-CSV handling "fix-later — resilient-import pass." The immediate fix is to catch DuckDB's `CSV Error on Line:` exception in `DataStudio.jsx`'s parse path and surface: (1) the offending line number, (2) the raw line content (truncated to 120 chars), and (3) a "Skip row / Abort" choice. For the DuckDB CSV path, pass `ignore_errors=true` as a DuckDB CSV reader option and then re-query with `WHERE` to surface the skipped rows in a diagnostic panel. This avoids aborting the entire import over one bad row.
**Invariants:** All DuckDB calls via `src/services/data/duckdb.js` singleton; do not add raw DuckDB imports in `DataStudio.jsx`.

---

## Suggested action order

1. **Fix Callaway-Sant'Anna R replication output** (`src/services/export/rScript.js` `CallawayCS` block) — the replication script is one of the platform's core value propositions; a broken export for a headline estimator undermines trust.
2. **Browser-validate the three code-complete items** (Plot Builder resize + PNG, OLS residual plots, spatial heatmap) — these were marked done months ago; Franco confirming them closes three open BugTriage rows at zero dev cost.
3. **Add dataset load lines to PlotBuilder replication export** (`PlotBuilder.jsx` → `loadLine.js`) — high-impact, low-effort: `buildRLoadLine` / `buildPyLoadLine` / `buildStataLoadLine` already exist and just need to be called from `scriptPreamble`.
4. **Fix group_summarize for cross-group comparisons (ATE)** (`runner.js` + `ReshapeTab.jsx`) — frequently requested workflow that blocks econometric teaching use cases; engine already has `twoSampleMeanTest`.
5. **Fix Stata workbench** (`stataScript.js`) — identify failing estimators by running existing export harness and pasting scripts in Stata; targeted rather than a full rewrite.
6. **Investigate and confirm N-way FE / FE-interactions UI** (`ModelConfiguration.jsx`) — BugTriage says "likely fixed" but unconfirmed; quick to verify without code changes.
7. **Add direct elasticity interpretation to AI narratives** (`Prompts/index.js`) — low-risk prompt extension with high teaching value for econ users.
8. **Save progress in FeatureTab transformation subsections** (`FeatureTab.jsx`) — prevents data-loss UX frustration; `sessionStorage` draft is a ~10-line fix.
9. **Add sort controls to the data viewer** (`ExplorerModule.jsx`) — emits an `arrange` step via the existing SQL-fast-path; small UI addition.
10. **Update TOUR_STEPS and HintBox copy** (`HelpSystem.jsx`) — low engineering cost; essential for new users arriving from institutional licensing demos.
