# Feedback Analysis Report — 2026-06-02

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **9 open bugs** · **10 open features** · **1 open UX** · **1 open performance** · **0 open other**
- Total open items: **21**
- Cross-referenced against BugTriage.md; items marked fixed/resolved in BugTriage are excluded even if not strikethrough in ClaudeFB.md.

---

## Bugs

### [high] · Data · 2026-05-10

**Feedback:** Parse error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated)

**File:** `src/DataStudio.jsx` — `parseCSV()` at line 38

**Suggestion:** The parser splits the entire text on `/\r?\n/` (line 65) before tokenizing, so any quoted field containing an embedded newline is silently split into two lines, causing the tokenizer to misalign all subsequent columns and throw on line 1583. Replace `text.split(/\r?\n/)` with a streaming RFC-4180 state machine that respects open-quote state before emitting a record boundary. Alternatively, integrate PapaParse (CDN) only for the fallback path when the simple splitter raises a column-count mismatch on any row.

**Invariants:** `DataStudio.jsx` is outside `src/math/` and `src/core/` — no architectural constraints apply to this change.

---

### [high] · Data · 2026-05-13

**Feedback:** .dta files are wrong loaded and read

**File:** `src/services/data/parsers/stata.js` — `parseStata()` at line 223; `parseStataOld()` at line 77

**Suggestion:** BugTriage status has been "pending" since 2026-05-13 with no recorded fix. The most common Stata-13+ failure modes are: (a) the `<strls>` block is present but the parser does not resolve `StrL`-typed columns, leaving them `null`; (b) format 119 (Stata 15, large datasets) uses 8-byte observation counts that the current 4-byte reader at line 228 would silently truncate. Collect a reproducible .dta file from Franco, add it as a fixture in `src/math/__validation__/`, then audit the format-detection branch and add a format-119 reader mirroring the 118 path.

**Invariants:** `src/services/data/parsers/stata.js` must remain pure JS — no React imports.

---

### [high] · Data · 2026-06-01

**Feedback:** Not all columns of .dta are recognized — columns loaded twice appear as replicated entries without names

**File:** `src/services/data/parsers/stata.js` — varnames parsing block near line 302

**Suggestion:** The varname block is read with `tagPos(bytes, '<varnames>')` + a fixed `NAME_LEN` stride. If `NAME_LEN` is miscomputed for format 118/119 (33 bytes per name in all formats, but the offset arithmetic after the tag may be off), the stride overruns into the next varname and duplicates entries. Also, the header deduplication in `DataStudio.parseCSV` (lines 67-73) is not applied to Stata column names. Add a post-parse name-deduplication step identical to the CSV path, and cross-check the `NAME_LEN` constants against the Stata 13/14/15 binary format spec. This likely shares a root cause with the 2026-05-13 .dta bug above — fix both together.

**Invariants:** Pure JS — no React.

---

### [high] · Data · 2026-05-31

**Feedback:** OECD proxy error 406 for OECD.SDD.NAD,DSD_NAAG@DF_NAAG_I — acceptable content types: application/vnd.sdmx.genericdata+xml; version=2.1 and application/vnd.sdmx.data+json; version=2

**File:** `src/services/data/fetchers/oecd.js` — `fetchOECDIndicator()` at line 163; also requires a fix in the Supabase Edge Function `oecd-proxy`.

**Suggestion:** The 406 is returned by the OECD SDMX server, not the proxy itself — the proxy forwards a request with an `Accept` header the OECD server rejects. The edge function must pass `Accept: application/vnd.sdmx.data+json; version=2` (SDMX-JSON v2). Add a retry that falls back to `Accept: application/vnd.sdmx.genericdata+xml; version=2.1` if JSON v2 also fails. On the client side (`oecd.js` line 188-190), surface the acceptable content types from the 406 body in the thrown error message so the user sees an actionable hint.

**Invariants:** The proxy URL is a Supabase Edge Function — the server-side `Accept` header must also be patched there. The client-side `oecd.js` fix alone is insufficient.

---

### [medium] · General · 2026-05-20

**Feedback:** When a dataset is deleted, all variables should disappear from all modules without having to select another dataset

**File:** `src/services/session/sessionState.jsx` — `REMOVE_DATASET` reducer at line 59; `src/components/workspace/DatasetManager.jsx`; `src/components/ModelingTab.jsx`

**Suggestion:** The `REMOVE_DATASET` reducer (lines 59-65) only removes the dataset from the registry and updates `primaryDatasetId`. It does not signal downstream modules to flush their variable state. The simplest fix: in `ModelingTab.jsx`, add a `useEffect` that watches `sessionState.primaryDatasetId` and clears `yVar`, `xVars`, `zVars`, `wVar`, `clusterVar`, `clusterVar2`, `treatedUnit`, and `result` when the active dataset id changes away from its previous value. Also audit `WranglingModule`, `ExplorerModule`, and `SpatialTab` for stale column references after deletion.

**Invariants:** Variable resets must remain in React component layer — do not reach into `src/math/` or `src/core/` from session state.

---

### [medium] · Model · 2026-05-21

**Feedback:** URGENT — p-value calculation broken: t-value = 0.49 but p-value < 0.001, impossible

**File:** `src/math/LinearEngine.js` — `pValue()` at line 80; `src/math/EstimationResult.js` — coefficient table rendering at lines 354, 385

**Suggestion:** A 2026-05-26 audit of all 11 engines found no t/p desync — `pValue(0.49, 100) = 0.625` is correct per R. The most likely explanation is a column layout misread: the coefficient table renders `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig`, so "0.49" is β̂, not t. No code change is recommended until Franco provides a screenshot with the estimator name. If a reproducible case surfaces, audit that every engine aligns `testStats` and `pVals` to the same index ordering as `varNames` before returning.

**Invariants:** None pending reproducer.

---

### [high] · Report · 2026-05-25

**Feedback:** AI does not recognize the pipeline steps in Script generation

**File:** `src/ReportingModule.jsx` — `buildSessionSnapshot` call at line 968; `src/services/AI/sessionSnapshot.js` — `buildSessionSnapshot()` at lines 26-30, 77; `src/services/AI/Prompts/index.js` — `UNIFIED_SCRIPT_PROMPT`

**Suggestion:** `sessionSnapshot.js` reads pipeline from `cleanedData?.pipeline ?? result?.spec?.pipeline` (line 77). If the `cleanedData` prop reaching `ReportingModule` does not carry a populated `.pipeline` array, the snapshot serializes "PIPELINE (0 steps)" and the AI generates a bare load call. Add a `console.log(snapshot)` trace in staging: if `snapshot.pipeline` is empty or undefined, trace back through `WranglingModule.onReady` / `onComplete` payloads to find where the pipeline is being dropped. This is tracked as CLAUDE.md pending item 5b — awaiting Franco's validation run on Vercel with a `;`-delimited CSV.

**Invariants:** All Anthropic calls must route through `src/services/AI/AIService.js` — the snapshot is injected as a user-message block, which is correct.

---

### [critical] · Model · 2026-05-28

**Feedback:** Redefine the event study model separating into Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators (currently only generic TWFE event study, which is biased under staggered treatment)

**File:** `src/math/PanelEngine.js` — `runEventStudy()`; `src/components/ModelingTab.jsx` — event study estimation branch; `src/components/modeling/EstimatorSidebar.jsx`; `src/components/modeling/ModelPlots.jsx` — `EventStudyPlot`

**Suggestion:** The current TWFE event study uses Jacobson-LaLonde-Sullivan (1993) interactions, which produce contaminated estimates under staggered treatment timing with heterogeneous effects (Sun & Abraham 2021, Callaway & Sant'Anna 2021). This is a correctness failure with published-results implications — severity is critical. Recommended path: (1) Add `runSunAbraham(data, opts)` to `PanelEngine.js` using cohort-specific ATTs with never-treated or not-yet-treated controls, weighted by cohort shares; (2) Add it as a new estimator group "Staggered DiD" in `EstimatorSidebar.jsx`; (3) Add a warning banner in the existing TWFE event study UI when staggered treatment timing is detected (i.e., `treatTimeCol` has more than one distinct non-null value across units). Validate against R `fixest::feols` with `sunab()` and `did::att_gt()`.

**Invariants:** `src/math/PanelEngine.js` must have zero React imports. New engine functions must accept explicit `seType` — never hardcode SE variant internally.

---

### [medium] · Spatial · 2026-05-30

**Feedback:** Adjust size respectively to the spatial unit(s) — geometry/polygon display not scaling with unit type

**File:** `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`; `src/components/tabs/spatial/plot/geo.js` — `geoBbox`

**Suggestion:** The Leaflet map auto-fits via `map.fitBounds()` but the SVG geo-plot in `GeoPlotCanvas.jsx` uses a fixed projection scale. When geometries span a few km² vs. a continent, the SVG viewport does not reproject to fill the canvas. Fix: compute the bounding box of all rendered geometries using `geoBbox` from `geo.js` and derive projection `scale` and `translate` so the geometry fills `[width, height]` with a small margin — equivalent to D3's `fitSize([width, height], featureCollection)`. `GeoPlotCanvas.jsx` is the primary file to change.

**Invariants:** Pure UI component — no constraints from `src/math/` or pipeline invariants.

---

## Features

### Model · 2026-05-11

**Feedback:** Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength

**File:** `src/math/CausalEngine.js` — `andersonRubinCI()` at lines 14+, 201+; `src/components/ModelingTab.jsx` — `arCI` display at line 907

**Suggestion:** The math exists in `CausalEngine.js` and `arCI` is already surfaced in `ModelingTab.jsx` (line 907) for the single-endogenous-regressor case. The current condition gates display on `xVars.length === 1`. Confirm the AR CI panel is visible by default (not collapsed). For multi-instrument cases, implement the Kleibergen (2002) `K`-statistic projection CI in `CausalEngine.js` to extend coverage. No UI framework change is needed — extend the existing `arCI` display panel.

**Invariants:** `src/math/CausalEngine.js` must remain React-free.

---

### Model · 2026-05-11

**Feedback:** Add multiple statistics tests

**File:** `src/components/modeling/DiagnosticsPanel.jsx`; `src/core/diagnostics/`; `src/math/LinearEngine.js`

**Suggestion:** The highest-value additions for an econometrics tool are: (1) **RESET test** (Ramsey 1969) — add fitted-values powers to X and test joint significance via F-test in `LinearEngine.js`; (2) **Wald test** for linear restrictions H₀: Rβ = r — add `waldTest(R, r, beta, Vcov)` to `LinearEngine.js`; (3) **Chow test** for structural breaks — split sample and compare SSRs. Surface these in `DiagnosticsPanel.jsx` as new collapsible sections alongside the existing Breusch-Pagan, White, Hausman, Durbin-Watson, Jarque-Bera panels. Test math lives in `src/core/diagnostics/`.

**Invariants:** `src/core/diagnostics/` must be pure JS. Test UI renders in `DiagnosticsPanel.jsx` (React).

---

### Simulate · 2026-05-18

**Feedback:** Add resampling, bootstrapping, sampling to the mean

**File:** `src/components/tabs/SimulateTab.jsx`; `src/math/Resampling.js`

**Suggestion:** `src/math/Resampling.js` already exports `bootstrapMean()` and a subsampling variant but these are not wired to any UI. Add a "Bootstrap / Resampling" panel to `SimulateTab.jsx` that lets the user pick a column, configure B (replications) and α, choose method (bootstrap / subsampling), and renders a histogram of replicates reusing the existing `MCHistogram` component. Add a generic `bootstrapStatistic(values, statFn, B, alpha)` to `Resampling.js` to complement the mean-specific variant and cover median, variance, etc.

**Invariants:** `Resampling.js` is pure JS — all UI code stays in `SimulateTab.jsx`.

---

### Explore · 2026-05-19

**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option

**File:** `src/components/wrangling/CleanTab.jsx` — `FilterBuilder`; `src/pipeline/runner.js` — `filter` step

**Suggestion:** The filter builder supports single-value comparison operators. Add an `in` / `not in` operator that accepts a comma-separated or tag-list input and compiles to `new Set(values).has(row[col])` in the `filter` step handler in `runner.js`. In the UI, render a tag-input component (inline, using `C` styles) when the `in` operator is selected. No parser change needed — this operates on already-loaded string columns.

**Invariants:** Pipeline `filter` step must replay on `rawData` — never mutate in place. The existing `filter` step in `runner.js` follows this pattern; just extend the operator set.

---

### Model · 2026-05-21

**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression

**File:** `src/components/ModelingTab.jsx` — RDD result rendering; `src/components/modeling/ModelPlots.jsx`

**Suggestion:** The `runSharpRDD()` result already returns `bandwidth`, `kernel`, `polyOrder`, `n`, `LATE`, `se`, `pVal`. Add a `RDDSummaryTable` component (inline in `ModelingTab.jsx` or `ModelPlots.jsx`) that renders the rdrobust-style header: `Call:`, `Cutoff`, `Left of c`, `Right of c`, `Number of obs`, `BW type`, `Kernel`, `VCE method`, and the coefficient block with `Conventional` and `Robust` rows. All data is already present in the result object — no math change needed.

**Invariants:** UI only — no changes to `src/math/`.

---

### Model · 2026-05-25

**Feedback:** Add an option to estimate without intercept

**File:** `src/math/LinearEngine.js` — design matrix construction in `runOLS()` at line ~109, `runWLS()` at line ~200; `src/components/ModelingTab.jsx` — model configuration area

**Suggestion:** Add a `noIntercept` boolean parameter to `runOLS()`, `runWLS()`, and the FE/FD paths. When `true`, build `X` without the leading column of ones and omit `"(Intercept)"` from `varNames`. R² without intercept must use the uncentered formula `1 − SSR / Σyᵢ²`; adjust adjR² and F-stat df from `k−1` to `k`. Surface as a checkbox in `ModelingTab.jsx`'s model configuration area. Gate the option out for estimators where it is not applicable (Logit/Probit: link-function intercept is required; FE: within-demeaning already removes the intercept).

**Invariants:** `seType` must still be passed explicitly to all engine functions — do not hardcode SE variant inside. Zero React in `src/math/`.

---

### Spatial · 2026-05-20

**Feedback:** Add grids to the plot, not only in the OMS map

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`; `src/components/tabs/spatial/plot/GeoPlotCanvas.jsx`

**Suggestion:** The Analyze tab already generates H3 and rectangular grid cells as WKT polygons via `SpatialEngine`. The Map tab (Leaflet) renders these as overlays. `GeoPlotCanvas.jsx` draws WKT geometries as SVG paths but has no grid-overlay option. Add a `showGrid` toggle to `GeoLayerConfig.jsx` that, when enabled, calls `SpatialEngine.makeRectGrid()` to generate a grid covering the current bounding box and renders the grid lines as a dedicated low-opacity SVG `<path>` layer behind the data layers. No new math is needed — `makeRectGrid` already exists.

**Invariants:** `src/math/SpatialEngine.js` must remain pure JS. Grid rendering stays in `GeoPlotCanvas.jsx`.

---

### Spatial · 2026-05-30

**Feedback:** Geometry transformations and show EPSG

**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`; `src/components/tabs/spatial/shared/crs.js` — `PRESET_CRS`

**Suggestion:** (1) Extend the `PRESET_CRS` list in `crs.js` with the 10–20 most common EPSG codes for European and global research (UTM zones, national grids, ETRS89, etc.). (2) Display the resolved EPSG label and code next to each CRS dropdown — `PRESET_CRS` entries should carry a `label` and `epsg` string rendered in a `<small>` tag. (3) Add a centroid-extraction geometry transform (Polygon → Point) to the Analyze tab as a new section — centroid math is `mean of ring coordinates`, implementable as a pure-JS helper in `src/components/tabs/spatial/shared/wkt.js`.

**Invariants:** None — purely UI and data layer additions.

---

### Spatial · 2026-05-30

**Feedback:** Allow spatial filters for plots

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Add a filter panel to the Plot tab's layer config that lets the user pick a column and value (or bounding-box extent) and pre-filters the `rows` array passed to `GeoPlotCanvas` before rendering. For bounding-box filter, a `wktBbox` helper reading the coordinate extremes from the WKT string is sufficient — no full spatial index needed for display purposes. Column-value filters reuse the same `in` / equality logic suggested for `CleanTab.FilterBuilder` above.

**Invariants:** Filtering for display only — do not mutate `rawData`. Pass filtered rows as a derived array local to the Plot tab.

---

### Data · 2026-06-01

**Feedback:** Add an option to edit/assign column names directly in the Data Viewer

**File:** `src/ExplorerModule.jsx`; `src/pipeline/runner.js` — `rename` step (already implemented)

**Suggestion:** In the data table header row of `ExplorerModule.jsx`, make each header cell double-click editable (`contentEditable` on double-click, commit on Enter/blur). On commit, dispatch a `rename` pipeline step via `WranglingModule` — do not mutate `rawData` directly. The `rename` step already exists in `runner.js` and `registry.js`. Add a faint pencil icon on header hover to signal editability. This is a UI-only change; the pipeline invariant is fully preserved.

**Invariants:** Rename must go through the pipeline `rename` step — never mutate `rawData` directly. Use `indexedDB.js` for persistence — never `localStorage`.

---

## UX / Design

### Model · 2026-05-21

**Feedback:** Add an option to clean the current model (reset estimator selection and variable state)

**File:** `src/components/ModelingTab.jsx` — variable state at lines 1562-1563; `src/components/modeling/EstimatorSidebar.jsx`

**Suggestion:** Add a "Clear model" button (small, low-emphasis — e.g., a `×` icon or `[Clear]` text link styled with `C.textMuted`) in the ModelingTab header bar or next to the estimator dropdown. On click, reset `yVar`, `xVars`, `zVars`, `wVar`, `clusterVar`, `clusterVar2`, `treatedUnit`, `synthTreatTime`, and `result` to their initial empty/null values via a `useCallback` reset handler. Do not reset the estimator type — the user may want to keep the same estimator and re-select variables. No new state or context is required.

**Invariants:** State resets use React state setters only — not direct DOM manipulation or `rawData` mutation.

---

## Performance

### Data · 2026-05-08

**Feedback:** Big datasets generate crashes (21MB) over 900k rows

**File:** `src/DataStudio.jsx` — file parser dispatch at line ~318; `src/services/data/duckdb.js`; `src/pipeline/duckdbRunner.js`; `vercel.json`

**Suggestion:** Three likely causes in priority order: (1) **Missing COOP/COEP headers** — `vercel.json` may be missing `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp`, preventing `SharedArrayBuffer` and forcing single-threaded Wasm that OOMs on 900k rows. Check `vercel.json` first — this is a zero-code fix. (2) **Full JS string allocation** — the CSV text is fully loaded into a JS string before DuckDB ingestion; switch to `FileReader.readAsArrayBuffer` → `DuckDB.registerFileBuffer` to avoid the ~42MB V8 string copy. (3) **JS fallback for untranslated step types** — `duckdbRunner.js` translates only 11 of 23 step types; if a pipeline uses any untranslated step, the full 900k-row table is materialized to JS objects. Audit which step type triggers the JS fallback and add SQL translation for it. The CLAUDE.md DuckDB rule 4 documents the COOP/COEP requirement.

**Invariants:** Dataset must never leave the browser — no server-side computation. Display limit stays at 200–500 rows (virtualized); pipeline ops run on the full DuckDB table. Persist via `indexedDB.js` (OPFS).

---

## Suggested Action Order

1. **[critical] Event study Sun-Abraham / Callaway-Santana** (`src/math/PanelEngine.js`) — current TWFE event study produces biased estimates under staggered treatment; this is a math-correctness failure with published-results implications.
2. **[high] .dta duplicate columns + wrong load** (`src/services/data/parsers/stata.js`) — two bugs share a likely root cause (NAME_LEN / StrL parsing); fix together to unblock Franco's Stata workflow.
3. **[high] OECD proxy 406** (`src/services/data/fetchers/oecd.js` + Supabase Edge Function) — one-line Accept-header change in the proxy plus a client-side error message improvement.
4. **[high] AI script pipeline steps missing** (`src/ReportingModule.jsx`, `src/services/AI/sessionSnapshot.js`) — a `console.log(snapshot.pipeline)` trace in staging will identify whether the pipeline is dropped before or after `buildSessionSnapshot`; CLAUDE.md pending item 5b.
5. **[high] CSV quoted-newline parse error** (`src/DataStudio.jsx`) — any CSV with embedded newlines in quoted fields hits this; replace the current line-split with a streaming RFC-4180 tokenizer.
6. **[medium] Dataset deletion — stale variables** (`src/components/ModelingTab.jsx`, `src/services/session/sessionState.jsx`) — a `useEffect` on `primaryDatasetId` clears model variable state with minimal risk.
7. **[performance] 21MB crash** (`vercel.json`, `src/DataStudio.jsx`) — check COOP/COEP headers first; if missing, add them to `vercel.json` as the zero-code fix before touching JS.
8. **[feature] Resampling/bootstrap UI** (`src/components/tabs/SimulateTab.jsx`) — math already in `Resampling.js`; wiring to the UI is low-risk and high user value.
9. **[feature] No-intercept option** (`src/math/LinearEngine.js`, `src/components/ModelingTab.jsx`) — small, self-contained design matrix change with a single UI checkbox.
10. **[feature] RDD rdrobust-style summary table** (`src/components/ModelingTab.jsx`) — pure UI; all data already present in the result object.
