# Feedback Analysis Report — 2026-07-20

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **11 open bugs** · **9 open features** · **5 open UX/Design** · **1 open performance** · **0 open other**
- Cross-referenced against BugTriage.md; items marked "fixed", "code-complete", or "verified fixed" in BugTriage are excluded even when no strikethrough appears in ClaudeFB.md.
- **5 items are code-complete but need Franco's browser validation before closing:** OLS residual plots / Plot Builder resize (`35f4b5a6`); Spatial heatmap (`5e105529`); HC2/HC3 replication code (2026-07-18); ConfirmPopover for destructive actions (2026-07-06). Until Franco confirms these in a browser session they should not be closed in BugTriage.
- **2 bugs are "likely fixed" but unconfirmed:** N-way FE categorical variables and FE interactions (shipped `045f5cda` #104; included in Bugs section with a confirmation-needed flag).
- No new feedback rows since the 2026-07-17 batch. No items closed between 2026-07-19 and 2026-07-20.

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`) · `src/math/CallawayEngine.js`
**Suggestion:** Inspect the `CallawayCS` export block at line 1432 of `rScript.js`. The R output calls `did::att_gt` but the argument names and the `aggte()` aggregation call must be verified against the current `did` 2.x API (package arguments changed across versions). Check that `control_group`, `anticipation`, `base_period`, and the `aggte(type="group")` / `aggte(type="dynamic")` calls match the installed package version. The R validation harness (`callawayRValidation.R` → `callawayBenchmarks.json`) is still listed as "pending Franco" in CLAUDE.md — until those benchmarks exist the export is unvalidated. Run the generated script verbatim in R and diff its ATT(g,t) table against the JS engine output.
**Invariants:** Export files (`rScript.js`) are pure JS. All Anthropic API calls stay in `AIService.js` — no impact here.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/ExplorerModule.jsx` (lines 1320, 1376) · `src/components/PlotBuilder.jsx` (line 1290, `scriptPreamble` prop)
**Suggestion:** `ExplorerModule` builds the R/Python preamble with hardcoded `read_csv`/`pd.read_csv` (lines 1320 and 1376). It does not consult `loadOpts` (the per-dataset `_loadOpts` captured by `DataStudio.parseFile`). Fix: thread the active dataset's `loadOpts` into the `scriptPreamble` callback and call `buildRLoadLine`/`buildPyLoadLine` from `src/services/export/loadLine.js` to emit the correct reader (e.g. `haven::read_dta` for `.dta`, `readr::read_delim(..., delim=";")` for semicolon-delimited CSV). The `scriptPreamble` callback already receives `language` as its argument — this is the right place to dispatch `loadLine` builders.
**Invariants:** None — pure export concern; no changes to the pipeline or math layers.

---

### [high] · Export/Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/calculate/workbench/exportScript.js` (`buildStata`, lines 80–116)
**Suggestion:** The `buildStata` function emits Mata code using bare untyped assignments (`n = 1000`, `a = ${x0}`, `sI = ...`) which Mata rejects — all local variables need `real scalar` declarations. Add explicit declarations before each loop/accumulator block: `real scalar n, a, b, h, sI` (integral), `real scalar ng, ag, bg, hg, bestX, bestF, xj, fj` (optimize), `real scalar nr, ar, br, hr, prev, px, xk, fk` (root scan). The derivative display `("label", value)` only works as a bare expression inside a Mata session — wrap it in `printf("%g\n", ...)` or `st_local(...)` for reliable output. The `twoway function` call (line 91) correctly lives outside `mata:...end` — keep that structure.
**Invariants:** `src/components/calculate/workbench/` is UI code, not `src/math/` — no React-in-math constraint applies here.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (line 527, `group_summarize`) · `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` aggregates within groups (mean, sum, sd, etc. per group row) but cannot express cross-group arithmetic like `mean(Y|T=1) − mean(Y|T=0)`. Two complementary fixes: (1) add a UI tooltip in `ReshapeTab` explaining the two-step idiom — `group_summarize → pivot_wider(names_from=treatment, values_from=outcome_mean) → add_column(ATE = treated − control)` — at zero engineering cost; (2) optionally extend the `group_summarize` step spec with a `postAggs` array of `{ nn, expr }` expressions evaluated on the already-collapsed per-group row (e.g. `ATE = T1_mean - T0_mean`). The `postAggs` would be computed after the per-group collapse loop in `runner.js`, using the same safe-eval helper already used by `add_column`.
**Invariants:** Non-destructive pipeline — `group_summarize` must continue to run on `rawData`, never mutate it. Any expression evaluation must go through the existing safe-eval path, not bare `eval()`. No React in `runner.js`.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js` (line 168, `\begin{tabular}`) · `src/ReportingModule.jsx`
**Suggestion:** The table uses a fixed-width `tabular` with no overflow handling. When the model count exceeds ~5 columns the table overflows the page. Add a `\resizebox{\linewidth}{!}{...}` wrapper around the `tabular` environment when the number of model columns exceeds a threshold (suggested: 4). Alternatively, switch the column format from `l r r ...` to `l *{N}{p{1.8cm}}` for fixed-width model columns. Thread a `wideTable` flag into `buildStargazer` in `latexTable.js` that activates the `resizebox` wrapper. `ReportingModule.jsx` can compute the flag as `models.length > 4`.
**Invariants:** None — pure export concern.

---

### [medium] · Export/Math · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx` (lines 155–226)
**Suggestion:** `CoefficientTestPanel` only keeps the most recent single and joint test result in component state — there is no history. Add a `savedTests` array to component state; after each successful test run, push `{ type: "single"|"joint", spec, result }`. Render saved tests as a scrollable list with a "copy all as R block" button that calls the existing `copyScript` path for each test and concatenates them. The `ModelHypothesis.js` R snippet generator already produces a valid `car::linearHypothesis` call per test — no engine changes needed.
**Invariants:** `savedTests` lives in React state only — no IndexedDB persistence needed for this feature. Math files must not be changed.

---

### [medium] · AI/Prompt · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (lines 99, 353–354, `INTERPRET_REGRESSION_PROMPT`) · `src/services/AI/AIService.js` (`interpretRegression`)
**Suggestion:** `INTERPRET_REGRESSION_PROMPT` already documents log-log and log-level rules (lines 353–354) but relies on the narrative model to infer functional form from variable names alone. Augment `interpretRegression` in `AIService.js`: before building the user prompt, inspect `spec.yVar` and each `spec.xVar` against the active variable dictionary for `"ln_"` / `"log_"` prefixes or `"_log"` suffixes; inject an explicit `VARIABLE TYPES` block listing which variables are in logs. This turns an ambiguous hint into a hard constraint. Also add a rule that when `yVar` is in logs and an `xVar` is NOT, the coefficient is a semi-elasticity (`β × 100%` interpretation).
**Invariants:** All Anthropic calls must go through `AIService.js`. The detection logic belongs in `AIService.js`, not in any `src/math/` or `src/core/` engine.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js` (lines 157–172, `runSingleCoefficientTest`) · `src/components/modeling/CoefficientTestPanel.jsx` (single-test result display)
**Suggestion:** The joint Wald test already displays `χ²(q)` (lines 207–221 of `CoefficientTestPanel`). The single-coefficient test returns only a t/z statistic. R's `car::linearHypothesis` reports both F and χ² for single constraints. Add `chiSq: stat * stat` and `chiSqDf: 1` to the return value of `runSingleCoefficientTest` in `ModelHypothesis.js` (since `t²(df₁,df₂) → χ²(1)` asymptotically and exactly for large df). Display `χ²(1) = t²` alongside the t-stat in the single-test result panel. For the joint test panel, also add `F = chiSq / q` with the residual df as the denominator to fully match R output.
**Invariants:** None — this is a display enhancement on values already computed.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 (malformed row aborts import)
**File:** `src/DataStudio.jsx` (CSV parse branch) · `src/services/data/parsers/`
**Suggestion:** The CSV parser currently throws on the first malformed row (mismatched quotes, extra delimiters) and aborts the entire import. Wrap the per-row tokeniser in a try/catch; on failure, record `{ _rowError: true, _rawLine: originalLine, _rowIndex: i }` and continue parsing. After parse completion, if `_rowError` rows exist, surface a dismissible warning banner: "N rows skipped — row numbers: [list]. Click to review." Exclude error rows from the Arrow table passed to DuckDB to maintain column-type consistency. This matches the resilient-import pattern already used in the DTA parser hardening.
**Invariants:** Error rows must NOT propagate into the DuckDB Arrow schema or `rawData`. The skip list is transient state (component useState) — do not persist to IndexedDB. Non-destructive pipeline: `rawData` must reflect what was actually parsed, with `_parseWarnings` metadata for skipped rows.

---

### [medium] · Model · 2026-07-09 — **browser confirmation needed**
**Feedback:** Add options for fixed effects, not just entity and time, but also variables like country, state, sector — every categorical variable
**File:** `src/components/modeling/ModelConfiguration.jsx` (lines 66–124, `FEColumnPicker`) · `src/math/PanelEngine.js` (`runFEMulti`)
**Suggestion:** N-way FE shipped in `045f5cda` (#104) and `runFEMulti` supports arbitrary FE dimensions. `FEColumnPicker` only exposes dimensions declared via "Update panel index" in the Panel tab (line 74: `rawFeCols = panel?.feCols ?? [entityCol, timeCol]`). The gap: users who skip the Panel tab see only entity/time chips. Add an "Add dimension +" button to `FEColumnPicker` that opens a popover listing ALL non-numeric `headers` as selectable FE columns, appending selections to a local `extraFeCols` override array that is merged with `rawFeCols` for the current estimation only (does not mutate the stored panel declaration). This requires no math changes.
**Invariants:** `seType` must continue to be passed explicitly from the sidebar — `FEColumnPicker` must not infer or default SE type from selected columns.

---

### [medium] · Model · 2026-07-09 — **browser confirmation needed**
**Feedback:** Add also FE with interactions
**File:** `src/components/modeling/ModelConfiguration.jsx` (lines 82–124, `FEColumnPicker` interaction chips) · `src/math/PanelEngine.js`
**Suggestion:** FE interactions shipped in `045f5cda`. `FEColumnPicker` already handles `interactionLabel` chips (lines 82–124) with mutual-exclusion logic. BugTriage marks this "likely fixed — needs confirmation". Franco should verify end-to-end: Panel tab → select two columns as an interaction pair → "Update panel index" → Model tab → `FEColumnPicker` shows the `A×B` chip → selecting it greys out A and B individually. If the chip does not appear, check that `panel.interactionCols` is written by `PanelTab.jsx` on "Update panel index" and is persisted through `DataStudio`'s `panelInfo` state.
**Invariants:** None — no math change needed.

---

## Features

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))`
**File:** `src/math/CausalEngine.js` · `src/components/ModelingTab.jsx` (RDD result display)
**Suggestion:** `runSharpRDD` already returns all required values: `bw`, `N_left`, `N_right`, `tau` (LATE), `se`, `pval`, `ci95`, kernel, polynomial order. Add a `RDDSummaryBlock` presentational component that renders a compact two-column block mirroring rdrobust's output: Bandwidth selection, Kernel, Poly. Order, N (Full / Effective Left / Right), and Conventional/Bias-corrected/Robust rows. Wire it into the existing RDD result section in `ModelingTab.jsx` above the coefficient table. No new math; no `EstimationResult.js` changes.
**Invariants:** Display-only component; no imports from `src/math/` inside UI files beyond reading the result object.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows generic names regardless of actual column names
**File:** `src/services/export/latexTable.js` (line 22, `varLabels = {}` default; line 126, `display = varLabels[v] ?? v`) · `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` already accepts a `varLabels: Record<string,string>` parameter and resolves display names at line 126. Only the UI to populate it is missing. In `ReportingModule.jsx`, render each coefficient row as an editable `<span contentEditable>` (or a small `<input>`) that writes into a `labelMap` React state on blur. Pass `labelMap` to `buildStargazer` on every regeneration. For RDD, pre-populate `labelMap` from `result.spec` column names so the default is already correct. No math changes; no `EstimationResult.js` changes.
**Invariants:** `labelMap` is pure display state — never written to IndexedDB, never fed back to engines.

---

### Clean · 2026-05-19
**Feedback:** Add `filter(regionname %in% c("Spain", "Basque Country"))` for filter option (R membership syntax)
**File:** `src/components/wrangling/CleanTab.jsx` (filter expression field) · expression eval path (likely `src/pipeline/runner.js` `filter` case)
**Suggestion:** Add a pre-parse rewrite in the filter expression input handler that transforms `col %in% c("a","b")` → `["a","b"].includes(col)` before the expression is stored as a pipeline step. Use a regex with proper quoted-string tokenisation. Also support the negation form `!x %in% c(...)`. The `filter` step in `runner.js` passes the expression string to an eval — this rewrite happens at the UI layer (when the user types and submits the expression), not inside `runner.js`. Surface `%in%` in the filter UI autocomplete hints alongside the existing operator suggestions.
**Invariants:** The stored `filter` step spec must record the *rewritten* JS-compatible expression so it can replay on `rawData` correctly. No mutation of `rawData`; non-destructive pipeline invariant applies.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx` · `src/components/calculate/workbench/Workbench.jsx`
**Suggestion:** The workbench already supports `refLines` (horizontal/vertical reference lines) per `workbenchStore.js` and `ViewControls.jsx`. Expose this in the equation list as a "constant" type: when a user defines an equation with no axis variable (pure scalar, e.g. `MC = 0.5`), detect it as constant and convert it to a horizontal `refLine` at that value. The `WorkbenchCanvas.jsx` already draws `refLines` (lines 146–150). This requires detecting the no-variable case in the equation parser and routing to `addRef("h", value)` rather than to the function-plot path.
**Invariants:** None — entirely within the Calculate workbench component tree.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axes (3D plots)
**File:** `src/components/PlotBuilder.jsx` · `src/ExplorerModule.jsx`
**Suggestion:** Observable Plot (0.6 CDN) does not support 3D natively. The most pragmatic path is to add a "3D Scatter" geom that renders using a lightweight WebGL library (e.g. `plotly.js` subset, or a pure-canvas projection). Because CSP blocks external CDN at runtime, the library must either be bundled or loaded from the existing allowed CDN list. An alternative is an isometric projection entirely in Canvas/SVG (no new CDN dependency). Mark as `medium` effort; a full 3D engine would be a significant scope addition — consider scoping to 3D scatter (x, y, z axes + optional color) as the MVP.
**Invariants:** Any runtime `fetch()` to a new CDN host must be added to `connect-src` in `vercel.json`. No external UI libraries (CSP restriction). No CSS files.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider so users can find variables faster when there are many
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text filter `<input>` at the top of the variable list in `VariableSelector.jsx`. On keystroke, filter the displayed `headers` array to entries matching the search string (case-insensitive substring match). Hide the filter input when `headers.length ≤ 10` to avoid visual clutter for small datasets. This is a pure display change local to `VariableSelector.jsx` — no state lifted to parent components.
**Invariants:** Inline styles only via the `C` object from `shared.jsx`.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx` (legend rendering)
**Suggestion:** When `aes.color` is set, extend the Observable Plot legend to show per-series row counts (and optionally the mean of the Y aesthetic) alongside the series label. This can be done by post-processing the SVG legend node that Observable Plot renders, or by injecting a custom `legend` option with an enriched `tickFormat` function. A simpler alternative: render a compact stat table below the chart listing each color category, its n-count, and mean Y — styled with `C.bg`/`C.border2` inline styles.
**Invariants:** Inline styles only via `C`; no CSS files. No external UI libraries.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`
**Suggestion:** Add a small "Edit ✎" button to each step card in `History.jsx` that fires a new `onEditStep(step)` callback. `WranglingModule` maps `step.type` to the correct sub-tab using `STEP_REGISTRY`'s category field (categories: "cleaning", "feature", "reshape", "merge"), then calls `setTab(category)`. Pass the selected step as an `editStep` prop to the active sub-tab panel so it can pre-populate its inputs. The non-destructive pipeline invariant means "editing" should stage a replacement step, not mutate the original — the simplest UI is: pre-fill the panel, apply fires a `replace_step_at_index` action via the existing undo/redo stack.
**Invariants:** Non-destructive pipeline — the existing step array must not be mutated in-place. Any replacement must go through the standard `onAdd` → `dispatchStep` path and be undoable. No React imports in `src/math/` or `src/core/`.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` · `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a per-layer attribute filter to `GeoLayerConfig.jsx`: a column selector plus a multi-value chip picker populated from that column's distinct values. Pre-filter the layer's source rows in JS (`rows.filter(r => selected.includes(r[col]))`) before passing to the layer painter in `SpatialGeoPlot.jsx`. This is a view-only filter — no pipeline step, no `rawData` mutation. Geometry-intersection filtering (spatial join) is a separate V4 item.
**Invariants:** Filtered rows must NEVER be written to `rawData` or create a pipeline step. Spatial filter is view-only, scoped to the plot tab. Non-destructive pipeline invariant must hold.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Add a `groupId: string | null` field to the artifact order entry in `artifactOrder.js`. In `ModelBufferBar.jsx`, add a "New group" button that prompts for a name; render artifacts under collapsible group headers; allow drag-and-drop to reassign. Persist `groupId` alongside artifact order in the `v9 model_buffer` IndexedDB store (already has per-project save/load). A simpler MVP: just a manual label chip on each pinned model card without drag-and-drop.
**Invariants:** Persistence must use `indexedDB.js` — never `localStorage`. Inline styles via `C`.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when and navigating away loses all work
**File:** `src/components/wrangling/FeatureTab.jsx` (lines 461–464, `cwNN`, `cwCases`, `cwDefault` useState)
**Suggestion:** `FeatureTab`'s case_when inputs (`cwNN`, `cwCases`, `cwDefault`) are plain `useState` and reset on unmount. Lift this state to `WranglingModule` (which persists across sub-tab changes), or write a draft to `sessionStorage` keyed by `pid` + `"caseWhenDraft"` on every `cwCases` change and read it back on mount. Clear the draft when `onAdd` fires successfully. The same approach should apply to the if_else section and to other multi-field panels (e.g. MergeTab).
**Invariants:** Use `sessionStorage` scoped by `pid` for transient draft state — this is not permanent pipeline data, so IndexedDB is not required. Key format: `${pid}::caseWhenDraft`. Do not use unscoped `localStorage` keys (CLAUDE.md: localStorage deprecated for pipeline/data storage).

---

### Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot (title, axis labels, etc.)
**File:** `src/components/PlotBuilder.jsx` (Labels panel) · `src/ExplorerModule.jsx`
**Suggestion:** PlotBuilder already has a Labels panel for title, x-label, and y-label that feeds into the Observable Plot `marks` config. Confirm that the time series view in `ExplorerModule` is rendered via `PlotBuilder` (not a standalone Observable Plot call). If it IS routed through `PlotBuilder`, labels already work — verify the Labels panel tab is not hidden for this geom type. If the time series is an inline Observable Plot call (not via `PlotBuilder`), add `title`, `x.label`, and `y.label` options to that call and wire them to controlled inputs using the existing `C`-styled input pattern.
**Invariants:** Inline styles via `C`; no external UI libraries.

---

### Data · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx` (data table, `<thead>` around line 1740)
**Suggestion:** The data viewer table has no clickable sort headers. Add `sortCol: string | null` and `sortDir: "asc" | "desc"` to the component's local state. Clicking a `<th>` cycles `asc → desc → none`. Apply a `[...displayRows].sort(...)` on the 200-row display slice before rendering. Add a small ↑↓ glyph in active header cells using `C.teal`. This is a **display-only** sort — it must NOT add an `arrange` pipeline step or modify `rawData`. For explicit sort-and-save, users already have the `arrange` pipeline step in the Clean tab.
**Invariants:** Must not modify `rawData` or create a pipeline step. Inline styles via `C`. This is purely a presentation layer feature.

---

### General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (line 11, `TOUR_STEPS` array of 9 steps)
**Suggestion:** `TOUR_STEPS` was written before several features shipped: Spatial tab, Simulate/DGP, Plot Builder (Explore), N-way FE picker, DiD/Event Study estimators, model buffer bar, and the NL command bar. Audit each step against the current `WorkspaceBar` tab set and `APP_CAPABILITY_MAP` (in `src/services/AI/appCapabilityMap.js`). Add or update entries to reflect the current tab layout. Low engineering effort, high value for the LMU onboarding demo.
**Invariants:** Per CLAUDE.md working convention, any new workspace tab added to the tour must also have an entry in `APP_CAPABILITY_MAP` in `src/services/AI/appCapabilityMap.js`.

---

## Performance

### Data · 2026-05-08 (partially resolved)
**Feedback:** Big datasets generate crashes (21MB, >900k rows)
**File:** `src/DataStudio.jsx` (parseFile branch for `.xlsx`, `.rds`, `.shp`) · `src/services/data/duckdb.js`
**Suggestion:** The DuckDB fast-path (parse → Arrow → DuckDB table → OPFS Parquet cache → 500-row UI preview) was implemented for `.dta` files >10 MB in June 2026. It is NOT yet applied to `.xlsx`, `.rds`, or `.shp` files of similar size. Extend `DataStudio.parseFile`: after SheetJS or the rds/shp parser produces a JS row array, if `rows.length > 20000`, call `duckdb.registerTable(arrowFromRows(rows))`, then reduce `rawData` to the 500-row preview and set `_duckdb: true`. This mirrors the `.dta` path exactly. For `.shp`, WKT geometry columns need to be excluded from numeric aggregation in SQL paths — add a geometry-column guard to `duckdbOLS.buildOLSSuffStats`.
**Invariants:** The full dataset must remain queryable via DuckDB for all pipeline steps and estimation; only the React-rendered preview is trimmed. `SELECT *` on large tables is banned per CLAUDE.md DuckDB strategy. Privacy-first: no server egress.

---

## Suggested action order

1. **Fix Stata workbench Mata declarations** (`exportScript.js` `buildStata`) — one-file fix, no invariant risk; affects every user who clicks "↓Stata" in the Calculate tab.
2. **Add dataset load lines to plot replication code** (`ExplorerModule.jsx` lines 1320/1376 + `loadLine.js`) — high-visibility gap: exported plot scripts are currently unrunnable without manual edits.
3. **Fix CS DiD 2021 replication code** (`rScript.js` `CallawayCS` block) — only estimator confirmed broken; run the generated script in R and diff against `CallawayEngine.js` output to find the argument mismatch.
4. **Persist FeatureTab case_when draft** (`FeatureTab.jsx` → `sessionStorage` keyed by `pid`) — trivial fix (~15 lines), high UX impact; users currently lose multi-line case_when work on every sub-tab switch.
5. **Enable cross-group math in group_summarize** (`runner.js` + `ReshapeTab.jsx`) — at minimum, add a UI tooltip explaining the two-step ATE idiom (zero code); the optional `postAggs` field is a medium lift.
6. **LaTeX table column sizing** (`latexTable.js` `resizebox` wrapper) — one-line change; surfaces on every multi-model comparison table.
7. **Multi-hypothesis save for R** (`CoefficientTestPanel.jsx` savedTests array) — medium effort; completes the inference workflow for IV/panel users.
8. **Chi-square alongside t-stat for single hypothesis** (`ModelHypothesis.js`) — low effort (`t² = χ²(1)`); full parity with `car::linearHypothesis`.
9. **Elasticity auto-detection** (`AIService.js` variable-type injection into prompt) — medium effort; improves AI narrative for all log-transformed models.
10. **Guide tour update** (`HelpSystem.jsx` TOUR_STEPS) — no math risk; high LMU demo value.
