# Feedback Analysis Report — 2026-09-08

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **10 open bugs** (8 unstarted · 2 code-complete pending browser validation)
- **4 open features (selected priority subset)** · several more deferred
- **1 open performance item** (large-dataset crashes)
- **11 items closed since last report** (fixed + browser-validated or marked code-complete)

> Cross-reference note: Several items in ClaudeFB.md lack strikethroughs but are marked "fixed" in BugTriage.md. Those are excluded from this report's open-bug counts. Items marked "likely fixed" or "code-complete, browser validation pending Franco" are listed in a separate **Pending Validation** section.

---

## Bugs

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP  
**File:** `src/components/tabs/SimulateTab.jsx` — `generateSimScript()` at line 519  
**Suggestion:** `generateSimScript` generates only the DGP variable declarations (set.seed, distributions, expressions). It stops there: it never appends the Monte Carlo loop body, the resampling tests, or any post-DGP estimation. Extend the function to emit the full analysis: the MC loop (`for (i in 1:R) { ... }`) containing any estimation steps the user ran in the session, plus the resampling/sample-test blocks from `StatWorkspace`/`SampleTestPanel`. The DGP-only output is the correct starting point; the gap is downstream. Separate the DGP section cleanly (it already works) and append a second section driven by session state (mc reps, which tests were run, which estimators were called on simulated data).  
**Invariants:** No React imports in any `src/math/` file. Script output must not call the Anthropic API; AI calls go through `AIService.js` only.

---

### [high] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/calculate/workbench/exportScript.js` — `buildStata()` at line 80  
**Suggestion:** The Stata path already emits `twoway function` + a numeric Mata block per equation, which is the correct approach since Stata has no symbolic engine. The likely failure is the Mata code block: `mata: … end` must have each statement on its own line, but the current emitter uses `L.push(...)` calls that can produce empty-string separators the Mata interpreter rejects. Also, the `real scalar ${name}(...)` function definition uses `st_numscalar()` inside a function body, which requires the `st_` prefix for scalars defined at the Stata level — verify these calls compile in a real Stata session. Add a simple integration test that confirms each emitted `.do` block is syntactically valid Mata (at minimum: balanced `mata:`/`end`, no bare empty lines inside the block, `st_numscalar` calls match the declared `scalar` names). Track down which specific feature Franco reported as broken and gate a fix on a concrete reproduction.  
**Invariants:** None specific beyond the zero-React, zero-external-API rules for this pure export utility.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js` — `buildStargazer()` (tabular env at line 173)  
**Suggestion:** The current emitter writes a bare `\begin{tabular}` with no width constraint. For wide tables (many regressors or many pinned models), wrap the tabular in `\resizebox{\textwidth}{!}{…}` or emit `\usepackage{adjustbox}` + `\begin{adjustbox}{max width=\textwidth}`. Alternatively, switch wide tables to `\small\setlength\tabcolsep{4pt}` as a lighter touch. A reasonable threshold: if `colFmt.length > 10` characters, emit the `\resizebox` wrapper. The fix is entirely in `latexTable.js`; no engine or React change needed.  
**Invariants:** None.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/components/modeling/CoefficientTestPanel.jsx` · `src/math/ModelHypothesis.js`  
**Suggestion:** `CoefficientTestPanel` currently runs one joint test at a time and has no way to queue or accumulate results. Add a "Save test" button that appends the current hypothesis (`Rβ = r`) + result to a local list (state or IndexedDB under the current model's uuid). Render the saved list below the input with coefficient names, test statistic, df, p-value, and a "Copy R/Stata/Python" button per row. The R snippet for each saved test already exists (`generateJointHypothesisScript` — see BugTriage 2026-07-17); the gap is persistence and multi-test UI.  
**Invariants:** Persistence must use `indexedDB.js`, not localStorage.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.  
**File:** `src/ExplorerModule.jsx` (time-series section) · `src/components/PlotBuilder.jsx`  
**Suggestion:** The time-series ACF/PACF and line chart in ExplorerModule's Statistics/Time-series tab expose no label controls. Add a collapsible "Labels" panel (title, x-axis label, y-axis label, legend title) mirroring the Labels panel already implemented in `PlotBuilder.jsx`. For the Explore tab's custom SVG charts this means threading `labels` props into the chart render; for any chart delegated to PlotBuilder the Labels panel is already there. Scope to the time-series tab first (confirmed gap), verify ACF/PACF chart titles separately.  
**Invariants:** Inline styles via `C`; no external UI libraries.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` — `INTERPRET_REGRESSION_PROMPT`  
**Suggestion:** The prompt already has estimator-specific rules (ESTIMATOR TYPE RULES J–R). Add a new rule: when the model spec contains a `log(y)` outcome or a `log(x)` regressor (detectable from `result.yVar`/`result.xVars` by checking for a `"log_"` prefix or the pipeline step `log`), the narrative should frame the coefficient as a semi-elasticity or elasticity ("a 1% increase in X is associated with a β% change in Y"), not a linear marginal effect. The spec carries `yVar` and `xVars` — surface those in the user-prompt block so Claude can detect log transforms without reading the raw data.  
**Invariants:** All AI calls through `AIService.js`; never add raw `fetch` to Anthropic API elsewhere. `src/math/` and `src/core/` must remain React-free.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work  
**File:** `src/ExplorerModule.jsx` (time-series / group reference line)  
**Suggestion:** The group reference feature (a horizontal or vertical reference line for a selected comparison group) likely fails because its value is sourced from filtered rows that are out of sync with the current chart data, or because the reference-group selector rebuilds on every filter change and loses its selection. Reproduce the bug by selecting a group reference, then changing the filter — if the line disappears or shows the wrong value, the root cause is stale state. Fix: compute the reference-group aggregate once from `allRows` (the unfiltered data), not from `filteredRows`; store the chosen reference-group id (not the computed value) in state.  
**Invariants:** None.

---

### [low] · General · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` — `TOUR_STEPS` array  
**Suggestion:** The `TOUR_STEPS` registry (9 steps) and `HintBox` copy in each module are hand-written prose and were last audited 2026-07-30. The tour still references two-month-old UI descriptions (Pipeline on the left, WLS as a sidebar estimator, 11 geoms). Do a pass to: (1) update step targets to match current DOM selectors; (2) update all count references to say "including X, Y, Z" instead of a bare number; (3) verify the 8 HintBox module panels (Data, Clean, Explore, Model, Report, Calculate, Simulate, Spatial) match the current estimator list, SE types, and geoms. This is documentation only — no logic change.  
**Invariants:** Working convention: "Never write a count into help copy — name the things instead."

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** `FeatureTab` renders each sub-section (case_when, mutate, lag, etc.) with local `useState`, which is wiped on every unmount. Persist the in-progress form state (column selections, expression strings, case rows) to `sessionStorage` keyed by `pid + sectionName`. On mount, hydrate from `sessionStorage`; on each change, write to `sessionStorage`. Clear on explicit "Cancel" or after a successful "Add to pipeline" step. This pattern is already used for the pipeline-collapse preference in `History.jsx` (sessionStorage keyed by dataset id).  
**Invariants:** IndexedDB for pipeline/data storage; `sessionStorage` is acceptable for transient UI state that should not outlive the tab. No localStorage for new code.

---

### [medium] · General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` currently maintains a global ordered list of artifacts (`getArtifactOrder`/`saveArtifactOrder`). Extend the schema to support named groups: `{ id, name, artifactIds[] }`, stored alongside the flat list. `ModelBufferBar` already renders the pinned-model chips — add a "New group" button that prompts for a name and buckets selected pins. The comparison table/plot builder then filters to the active group. Phase this: step 1 is group creation + persistence; step 2 is comparison scoped to a group.  
**Invariants:** Persistence via `indexedDB.js`; no localStorage. Inline styles via `C`.

---

## Pending Browser Validation (code-complete, needs Franco confirmation)

These items are implemented but not yet confirmed by browser testing. They are not in the open-bug count above but should be verified before being closed.

| Date | Module | Description | File | BugTriage status |
|------|--------|-------------|------|-----------------|
| 2026-06-10 | Model | Plot Builder resize + PNG export aesthetics | `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` |
| 2026-06-11 | Model | Y~Ŷ, residual-vs-fitted, Q-Q plots for OLS | `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` |
| 2026-06-13 | Spatial | Spatial heatmap (Leaflet KDE layer) | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` | code-complete `5e105529` |
| 2026-06-14 | Model | Diagnostics frozen to estimation result (no mutation on sidebar edit) | `src/components/ModelingTab.jsx` | code-complete `35f4b5a6` |
| 2026-07-09 | Model | N-way FE with arbitrary categorical variables | `src/math/PanelEngine.js` (`runFEMulti`) | likely fixed (`045f5cda`) |
| 2026-07-09 | Model | FE with interactions | `src/math/PanelEngine.js`, `src/pipeline/runner.js` | likely fixed (`045f5cda`) |
| 2026-07-22 | Model | P-values wrong for insignificant estimates | `src/math/LinearEngine.js` (`betainc`/`tCDF`) | likely fixed (`216b1ecb`) |
| 2026-07-30 | Data | Data viewer arrange/sort by column | `src/App.jsx` (DataViewer), `src/services/data/sortRows.js` | code-complete |
| 2026-08-01 | Data | Join right dataset pipeline not applied | `src/WranglingModule.jsx`, `src/pipeline/datasetContext.js` | code-complete |
| 2026-08-01 | Clean | `any()` in `grouped_mutate` expressions | `src/pipeline/runner.js`, `src/pipeline/groupExpr.js` | code-complete |

---

## Features (priority subset)

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx` · `src/components/calculate/workbench/Workbench.jsx`  
**Suggestion:** The Calculate workbench already supports derivatives, integrals, and optimization. Add a `limit as x→a` operation: evaluate the expression at increasingly close values of `x` from both sides and report the approached value (numerical limit). Emit `limit(expr, x, a)` (sympy), `Limit(expr, x, a)` (sympy in Python), and a comment in R/Stata since base R has no symbolic limit. Medium complexity; mirrors the existing integral bounds UX.  
**Invariants:** Zero React in `src/math/`; all math must live in pure JS.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx` · `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** Each pipeline step card in `History.jsx` already knows its `type`. When the user clicks a step card's "edit" icon (or right-clicks), look up `type` in `STEP_REGISTRY` to find which tab and sub-section owns it, then dispatch a tab-change event (`setActiveTab`) and scroll to or open the relevant section. This needs a mapping table `stepType → { tab, section }`. Start with the most common step types (filter, rename, log, lag, case_when, group_summarize) and handle unknown types gracefully (no-op).  
**Invariants:** Inline styles via `C`; no external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set, the plot legend currently shows color swatches from the observable10 palette but may not always show the mapped category values clearly, especially for many categories. Ensure: (1) the legend text is the raw category label (not a numeric index); (2) the legend is scrollable when categories exceed ~10; (3) when hovering a data point, the tooltip shows the color-mapped value. The legend rendering already exists (`appendSvgLegend` in `src/components/tabs/spatial/plot/legend.js`); the PlotBuilder uses Observable Plot's built-in legend, so this may require switching from `plot.legend()` to a custom HTML legend for long lists.  
**Invariants:** Inline styles via `C`; no external UI libraries.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models — using R/Python arguments as commands in the UI, for all estimators  
**File:** `src/components/modeling/ModelPlots.jsx` · `src/components/ModelingTab.jsx`  
**Suggestion:** Binned scatter plots (partial plots / added-variable plots) currently expose no configuration. Add a small "Plot options" collapsible panel when a `PartialPlot` is rendered: number of bins (slider, default 20), polynomial degree for the fit line (1/2/3), and a toggle between partial residual and raw scatter. Map these to equivalent `rdplot`/`binscatter2` arguments so the replication script emits them verbatim. This is a display-only change — the underlying `runOLS` call is unchanged.  
**Invariants:** Inline styles via `C`; no external UI libraries.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes — 21MB, over 900k rows  
**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js` · `src/pipeline/duckdbRunner.js`  
**Suggestion:** Per CLAUDE.md, the DuckDB strategy is code-complete and browser-validated for CSV via DuckDB Fase 0–8. The crash at 900k rows is likely still occurring for non-CSV formats (`.dta` / `.rds` / `.xlsx` / `.shp`) that bypass the DuckDB fast path. BugTriage 2026-06-10 notes: "fixed 2026-06-10 for `.dta` — other browser-only formats remain future work." The pending fix is to route large `.rds`, `.xlsx`, and `.shp` files through the same OPFS-Parquet pipeline already used for CSV/DTA. `DataStudio.parseFile` branches at the format level; add a post-parse step that registers any large result (>10MB) as a DuckDB Arrow table and writes it to OPFS. This is a follow-on to the existing Fase X4 work.  
**Invariants:** Data must stay client-side; no server-side processing. CSP `connect-src` must include every CDN origin fetched at runtime — if DuckDB fetches any additional extension for these formats, add the origin (see the documented `extensions.duckdb.org` CSP bug).

---

## Other

### · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 — `China,CHN,2010,2425.47...`  
**File:** `src/DataStudio.jsx` · `src/services/data/parsers/` (CSV path)  
**Suggestion:** The error is a malformed or encoding-broken row that DuckDB rejects outright. BugTriage marks this "fix-later — resilient-import pass". When the error fires, surface which line number failed (already in the error message), offer a "Skip bad rows" option (DuckDB's `IGNORE_ERRORS=true`), and show a preview of the skipped rows so the user can decide. This is a UX addition, not a new parser. File: `DataStudio.jsx` in the CSV load branch.  
**Invariants:** None specific.

---

## Suggested action order

1. **Simulate replication script** (`generateSimScript` in `SimulateTab.jsx`) — high severity, high user impact, and a single-file fix; the DGP section is already correct and the gap is append-only.
2. **Browser-validate the 10 pending items** (see Pending Validation table) — they are all code-complete; Franco should open the app and confirm each, then close the BugTriage rows and add strikethroughs in ClaudeFB.md.
3. **Stata workbench** (`exportScript.js` `buildStata`) — high severity, reproducible defect in a pure export utility with no engine risk; pinpoint the exact failing operation (likely Mata block formatting or `st_numscalar` usage) before writing the fix.
4. **LaTeX table width** (`latexTable.js`) — medium severity, one-liner `\resizebox` wrapper; low risk, high visibility for the reporting workflow.
5. **Time-series group references** (`ExplorerModule.jsx`) — medium severity, stateful UI bug; reproduce first (filter change → line disappears?), then fix reference-group state to use `allRows` aggregate.
6. **Save transformation progress** (`FeatureTab.jsx`) — medium severity, `sessionStorage` per `pid + sectionName`; no engine risk, directly unblocks the multi-step wrangling workflow.
7. **Direct elasticity interpretation** (`Prompts/index.js`) — medium severity, prompt-only change with no engine impact; add the log-variable detection rule to `INTERPRET_REGRESSION_PROMPT`.
