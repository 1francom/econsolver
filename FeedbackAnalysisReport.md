# Feedback Analysis Report — 2026-08-03

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **20 open bugs** (15 status=open in BugTriage · 5 fix-later) · **11 open features** · **1 open UX/Design** · **1 open performance (validation pending)** · **0 open other**
- 4 additional items are **code-complete but browser-validation pending Franco** — listed at end of Bugs section.
- 2 items marked "likely fixed" in BugTriage (N-way FE, FE interactions, commit `045f5cda`) — need Franco's browser confirmation.
- No new feedback items since 2026-07-19 batch.

---

## Bugs

### [critical] · Export · 2026-07-05
**Feedback:** "Replication code for CS DiD 2021 does not work"
**File:** `src/ReportingModule.jsx` (`_buildModelScript`) and `src/services/export/rScript.js` (`transpileModel`, case `"CallawayCS"`, line 1432)
**Suggestion:** Root cause confirmed by code inspection: `_buildModelScript` builds `config.model` from `result.spec` but never maps `spec.treatCol` (the first-treatment-period column, `gname` in `att_gt()`). `transpileModel` destructures `treatCol` from `model`, gets `undefined`, and `rStr(undefined)` returns `""` — so the generated `att_gt(gname = "")` call fails immediately in R. Additionally, `csEstMethod`, `csAnticipation`, and `csBasePeriod` live on the top-level result object (not in `spec`), so they default silently to `"dr"`, `0`, `"varying"` regardless of user settings. Fix: in `_buildModelScript`, add `treatCol: spec.treatCol ?? null` to the model config; also forward `csEstMethod: model.csEstMethod ?? "dr"`, `csAnticipation: model.csAnticipation ?? 0`, `csBasePeriod: model.csBasePeriod ?? "varying"` from the top-level result. Also audit the Python and Stata paths for a `CallawayCS` case — a silent fall-through to `default` emits only a comment instead of runnable code.
**Invariants:** Export functions are pure JS — no React imports in `rScript.js`. `src/math/CallawayEngine.js` must stay React-free.

---

### [high] · Export · 2026-07-05
**Feedback:** "Replication codes of the plots must have the lines to load datasets as well"
**File:** `src/services/export/plotScript.js` (`buildGgplot`, `buildMatplotlibPlot`); `src/components/PlotBuilder.jsx` (line 1526, `copyPlotScript`); `src/ExplorerModule.jsx` (line 2465, `PlotBuilder` mount without `scriptPreamble`)
**Suggestion:** `buildGgplot` and `buildMatplotlibPlot` never emit a dataset load line — they start directly with `library(ggplot2)` / `import matplotlib`. The geo variants (`buildGeoGgplot`, `buildGeoMatplotlib`) already call `geoLoadLines` because they accept a `datasets` array, but the non-geo builders receive no filename or load-options context. Fix: extend `buildGgplot` and `buildMatplotlibPlot` to accept an optional `{ filename, loadOpts }` param and prepend `buildRLoadLine` / `buildPyLoadLine` calls from `src/services/export/loadLine.js` (already imported in `plotScript.js`). In `ExplorerModule.jsx` (~line 2465), pass a `scriptPreamble` callback to `PlotBuilder` that invokes `buildRLoadLine`/`buildPyLoadLine`/`buildStataLoadLine` with the active dataset's `loadOpts` and `filename`.
**Invariants:** `loadLine.js` is the single source for load-line generation — do not duplicate logic inline. No React in `plotScript.js`.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** "Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group"
**File:** `src/pipeline/runner.js` (case `"group_summarize"`, line 528, `fn:"expr"` mode at line 1270); `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `runner.js` already supports `fn:"expr"` mode with group-aware helpers (`mean(v)`, `sum(v)`, `count()`, etc.) that can express cross-group arithmetic. However, `ReshapeTab.jsx` exposes **no UI surface for this mode** — there is no "Group & Summarize" sub-tab at all; users can only access `group_summarize` steps through the NL command bar or by importing a pipeline JSON. Fix: add a "Group & Summarize" section in `ReshapeTab.jsx` with group-by column pickers and an aggregation list (mean/sum/count/sd/min/max), plus a togglable "Expression" mode text input for cross-group formulas. The runner infrastructure is complete — this is purely a UI gap.
**Invariants:** Pipeline steps must replay on `rawData`, never mutate in place. Any new step type must be registered in `registry.js` and validated by `pipelineReliabilityValidation.mjs`.

---

### [high] · Export / Calculate · 2026-07-17
**Feedback:** "Stata workbench doesn't work completely"
**File:** `src/components/calculate/workbench/exportScript.js` (`buildStata`, lines 79–131)
**Suggestion:** Root cause confirmed by code inspection: the generated Mata code has invalid print syntax. Derivative, integral, optimize, and solveZero computation results are emitted as bare tuple expressions — e.g. `("d${name}/d${ax} @midpoint", value)` — which create row-vector literals in Mata but do not print anything; the output is silently discarded. Fix: replace each `("label", value)` with `printf("label: %f\n", value)` or `display(sprintf("label = %g", value))`. Separately, the `for` loop bodies for integral/optimize/solveZero use braces (`{ ... }`) — these are valid Mata, but confirm Stata's native `for` syntax accepts block braces (they do in Mata; double-check). The R and Python paths in the same file appear structurally correct.
**Invariants:** Pure export-layer change, no React, no math engine changes. `exportScript.js` is in `src/components/calculate/workbench/` — pure JS, no React imports.

---

### [medium] · Data · 2026-05-10 *(fix-later)*
**Feedback:** "Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47..."
**File:** `src/DataStudio.jsx` (CSV parse path); `src/services/data/parsers/`
**Suggestion:** A single malformed row aborts the full import rather than surfacing a recovery UI. Add a `try/catch` around the row-level parse loop, collect error rows with their original line numbers, and display a post-import summary banner ("N rows skipped — click to download error log") rather than failing the whole load. Follow the `diagnoseFit.js` / `diagnoseRDD.js` per-row error collection pattern. BugTriage tracks as fix-later (post-launch resilient-import pass). For DuckDB-backed large files, `IGNORE_ERRORS = TRUE` in the `READ_CSV` call collects per-row errors into a separate error table.
**Invariants:** OPFS Parquet cache must still be written for valid rows. Non-destructive — never mutate `rawData`.

---

### [medium] · Export · 2026-07-05
**Feedback:** "Adjust the size of LaTeX tables when the number of columns increases"
**File:** `src/services/export/latexTable.js` (~line 168, `\begin{tabular}`)
**Suggestion:** The generated `tabular` environment has no width constraint — with 5+ model columns it overflows the page margin. Wrap the `\begin{tabular}` … `\end{tabular}` block in `\resizebox{\textwidth}{!}{...}` when `colsN > 3` (column count is available at generation time in `buildStargazer`). Alternatively add `\setlength{\tabcolsep}{4pt}` before the environment and reduce font to `\small` for moderate cases. Add `\usepackage{graphicx}` to the emitted preamble comment when the resizebox path is taken.
**Invariants:** `latexTable.js` is pure JS, no React imports.

---

### [medium] · Math/Export · 2026-07-06
**Feedback:** "Add vector for running more than 1 hypothesis and save them to replicate in R"
**File:** `src/math/ModelHypothesis.js`; `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently runs a single linear hypothesis test per user interaction with no save mechanism. Add a "Save test" button that appends the current coefficient selection + null values to a local `savedHypotheses[]` array in panel state (or in `result.spec` for pin/restore persistence). Add an "Export all saved tests" button that calls `generateModelHypothesisScript()` for each saved test and concatenates the output as a block. The R/Stata/Python snippet generators in `ModelHypothesis.js` already exist — only the accumulation UI and batch-export call are new.
**Invariants:** `ModelHypothesis.js` must stay React-free (it is in `src/math/`). UI state belongs in `CoefficientTestPanel.jsx`. Persist saved tests in the model buffer alongside `result.spec` via `indexedDB.js`, not `localStorage`.

---

### [medium] · AI · 2026-07-09
**Feedback:** "Add direct elasticity interpretation"
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`, line ~99 and 353–354)
**Suggestion:** The prompt already handles log–log arc elasticity and log–level semi-elasticity. "Direct elasticity" likely refers to the **point elasticity at sample means** for a levels-levels regression: ε = β × (x̄/ȳ). Add a rule block to `INTERPRET_REGRESSION_PROMPT`: when the model is OLS/WLS with untransformed Y and X, and `result.means` contains column means, compute and report ε = β × x̄/ȳ alongside the standard marginal effect interpretation. Flag clearly that this is a point elasticity evaluated at sample means, distinct from the arc elasticity of a log–log specification. The means are available in the result object passed to `interpretRegression`.
**Invariants:** All Anthropic calls go through `AIService.js`. Changes to `Prompts/index.js` must NOT expand `SHARED_CONTEXT` (the cached block) — elasticity rules go in the user payload injected by `interpretRegression`, not in `SHARED_CONTEXT`.

---

### [medium] · UI / Explore · 2026-07-09
**Feedback:** "Add labels for the Time series plot, title, axis, etc."
**File:** `src/ExplorerModule.jsx` (time series section, line ~1300–1480)
**Suggestion:** The time series line-chart view renders without title, x-axis label, or y-axis label controls. Add three `<input type="text">` fields above the chart (chart title, x-label, y-label) in the same panel. Pass values to the Observable Plot `Plot.lineY` / `Plot.line` call via `Plot.plot({ title, x: { label }, y: { label } })`. Store as local state; include them in the pinned-plot payload so `renderPinnedPlot` restores them. The ACF/PACF `SvgACF` uses raw SVG — add a `title` prop rendered as an SVG `<text>` element there separately.
**Invariants:** Inline styles only via `C` from `shared.jsx`. No external UI libraries.

---

### [medium] · Math · 2026-07-17
**Feedback:** "Add chi-square for linearhypothesis"
**File:** `src/math/ModelHypothesis.js`; `src/components/modeling/CoefficientTestPanel.jsx` (line 210)
**Suggestion:** `CoefficientTestPanel` currently displays `χ²(df) = value` (line 210-214) from `jointResult.chiSq`. Looking at `ModelHypothesis.js` (line 203), `chiSq` is already computed. The user request most likely refers to **also exposing the F-statistic** alongside chi-square (as R's `car::linearHypothesis` does — it reports both). Add `fStat = chiSq / df` with `df1 = q, df2 = n - k` and a p-value via `fCDF` (already in `LinearEngine.js`) to the return object of the hypothesis test. In `CoefficientTestPanel`, render both the F-stat and chi-sq rows. Also confirm `generateModelHypothesisScript()` emits `car::linearHypothesis(fit, hypothesis, test = "F")` in R (which reports both).
**Invariants:** `ModelHypothesis.js` is in `src/math/` — no React imports. Use the existing `betainc`/`betacf`-based `fCDF` from `LinearEngine.js` for the F p-value.

---

### [medium] · UI / General · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time"
**File:** `src/services/Persistence/artifactOrder.js`; `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend the per-project order array in `artifactOrder.js` with a `groupId` field per artifact entry. Add a group manager in `ModelBufferBar.jsx` (or a new `ArtifactGroupPanel.jsx`) that lets users create named groups (e.g., "Robustness checks", "Main results"), drag artifacts between groups, and collapse/expand groups to hide members from the comparison view. Persist group assignments in IndexedDB alongside the existing artifact order via `saveArtifactOrder`, keyed by `pid`. Backward compatibility: artifacts with no `groupId` remain in an implicit "All" group.
**Invariants:** Use `indexedDB.js` for persistence, not `localStorage`. Inline styles via `C`. No external UI libraries.

---

### [medium] · UI / Clean · 2026-07-17
**Feedback:** "Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases"
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab` sub-sections (case_when, mutate, if_else) use local `useState` for in-progress form values that reset on unmount. Add a `useEffect` that serializes the in-progress draft state to `sessionStorage` keyed by `pid` + sub-panel name on every change, and restores from it on mount. `sessionStorage` is appropriate here (ephemeral, per-session, small). Clear the draft key when the user clicks "Add to Pipeline" (step is committed). This does not touch IndexedDB — draft UI state is not pipeline data.
**Invariants:** IndexedDB is for pipeline/data storage. `sessionStorage` is acceptable for ephemeral draft UI state. Never use `localStorage` for pipeline or data storage (CLAUDE.md invariant). Never mutate `rawData` or `pipeline` state from draft persistence.

---

### [medium] · UI / Data · 2026-07-17
**Feedback:** "Add arrange functions in the data viewer"
**File:** `src/ExplorerModule.jsx` (data grid rendering section)
**Suggestion:** Add `sortCol`/`sortDir` local state to the data viewer section of `ExplorerModule`. Clicking a column `<th>` cycles null → asc → desc with a sort icon (▲/▼/⇅). For JS-backed tables (≤ 500-row preview), sort the display slice in-component. For DuckDB-backed tables, issue `SELECT * FROM "${table}" ORDER BY "${col}" ${dir} LIMIT 500` via the DuckDB singleton. Display-only — does not add a pipeline step. Add a tooltip: "Sort is display-only — to sort the full dataset persistently, use Reshape → Arrange."
**Invariants:** Display sort must never mutate `rawData`, trigger `runner.js`, or produce a pipeline step. DuckDB queries must go through `src/services/data/duckdb.js` singleton.

---

### [fix-later] · Clean / Clean · 2026-06-14
**Feedback:** "R `%in% c(...)` membership syntax is not accepted by the filter expression UI"
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder)
**Suggestion:** The filter builder accepts simple `col op value` comparisons but not R-style `%in% c(...)` syntax. Surface a "one of these values" multi-select chip in FilterBuilder that maps to an `op:"in"` array-membership check in `runner.js`'s filter case. This is a UX improvement, not a parser feature — avoid eval-ing arbitrary R expressions in the browser.
**Invariants:** Filter steps replay on `rawData` via `runner.js`. No arbitrary code execution.

---

### [fix-later] · Model · 2026-06-14
**Feedback:** "RDD result lacks an R-style `summary(rdrobust)` compact table"
**File:** `src/math/CausalEngine.js`; `src/components/ModelingTab.jsx` (RDD result section)
**Suggestion:** `runSharpRDD` already returns `h`, `b`, `LATE`, `se`, `pval`, `ci95`, `n_left`, `n_right`, `kernel`, `polyOrder`. Wire these into a compact display table in the RDD result section that mirrors `summary(rdrobust(y,x))` output (effective N left/right, bandwidth, coef, SE, z, p, CI). Pure UI display enhancement — no new estimation needed. Could live in a new `RDDSummaryTable.jsx` in `src/components/modeling/`.
**Invariants:** No React in `src/math/`. New component goes in `src/components/modeling/`. Inline styles via `C`.

---

### [fix-later] · Spatial · 2026-05-30
**Feedback:** "Allow spatial filters for plots"
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add a column-value filter panel above the geo-plot canvas (similar to CleanTab's FilterBuilder), applied client-side to the rows before passing to the SVG layer renderer. A bounding-box spatial filter (min/max lat/lon crop) would also be useful. Routed to V4 spatial plotting pass.
**Invariants:** Filter is display-only — does not add a pipeline step or mutate `rawData`.

---

### [fix-later] · Model · 2026-06-14
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/services/export/latexTable.js`; `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` already accepts a `varLabels` option. Expose a collapsible per-coefficient label editor in the Report tab's model-table panel with an editable text field per coefficient name. Persist the `varLabels` map per `pid` in IndexedDB and pass it through to `buildStargazer`. Math and export logic already support this; only the UI edit surface is missing.
**Invariants:** `latexTable.js` is pure JS, no React. Persist via `indexedDB.js`, not `localStorage`.

---

### [needs-confirmation] · Math / Model · 2026-07-09
**Feedback:** "Add options for fixed effects, not just entity and time, but also variables like country, state, sector" + "Add also FE with interactions"
**File:** `src/math/PanelEngine.js` (`runFEMulti`); `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage marks both as "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation." Franco should confirm in the browser: (a) `ModelConfiguration` exposes an N-way FE picker accepting any dataset column, not just the declared panel entity/time; (b) `factor_interactions` pipeline steps can be used as FE dimensions. If the UI path is broken, the fix is in `ModelConfiguration.jsx`'s FE dimension section — `runFEMulti` and `demeanByFE` in `PanelWithinEngine.js` already support N-way FE.
**Invariants:** N/A (pending browser confirmation).

---

### Code-complete items pending browser validation

| Item | Commit | Severity | Browser test needed |
|------|--------|----------|---------------------|
| Plot Builder resize + PNG export aesthetics | `35f4b5a6` | medium | Drag-resize handle visible and functional; PNG at non-default height has correct dimensions |
| Y~Ŷ / Residual-vs-Fitted / Q-Q plots for OLS | `35f4b5a6` | high | Run OLS → 3 diagnostic plots appear; switch to Logit → plots hide; restore pinned OLS → plots reappear |
| Coefficient plots/diagnostics frozen to `result.spec` | `35f4b5a6` | high | Add variable after estimation → existing plots do not change |
| Spatial heatmap (Leaflet KDE layer restored) | `5e105529` | medium | Open Spatial → Map → add Heatmap layer → KDE cells render; legend and HTML export work |

---

## Features

### Model · 2026-07-09
**Feedback:** "Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster"
**File:** `src/components/modeling/shared.jsx` (`VarPanel`)
**Suggestion:** Add a text-filter `<input>` inside `VarPanel` that narrows the visible chip list as the user types (case-insensitive substring match on column name). Show the input only when `vars.length > 10`. State is local to `VarPanel` — no global state or persistence change.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

### General · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time"
**File:** `src/services/Persistence/artifactOrder.js`; `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** See Bugs section above (same item, cross-cutting bug/feature). The new `groupId` field in `artifactOrder.js` is the key structural addition.
**Invariants:** IndexedDB persistence, not `localStorage`.

---

### Explore · 2026-07-11
**Feedback:** "Add more labels in tables or boxes when plotting by color"
**File:** `src/components/PlotBuilder.jsx` (legend section, Labels panel)
**Suggestion:** When `aes.color` is set, expose an optional "Show value labels" toggle in the Labels tab. When enabled, append a `Plot.text(data, { x, y, text: colorCol, fontSize: 9 })` mark to annotate points with their category value. Add a `legendTitle` text field to let users override the auto-derived column name as the legend header. Cap at N=50 labelled points to avoid visual collision.
**Invariants:** Observable Plot CDN already loaded. Inline styles via `C`.

---

### Clean · 2026-07-17
**Feedback:** "Add an option to click on one pipeline step and go to the corresponding subsection automatically"
**File:** `src/components/wrangling/History.jsx`; `src/WranglingModule.jsx`
**Suggestion:** Build a static `STEP_TYPE_TO_TAB` map in `WranglingModule` (e.g., `case_when` → FeatureTab/if-else sub; `filter` → CleanTab/FilterBuilder; `join` → MergeTab; `pivot_longer` → ReshapeTab). Add an "✎ edit" icon on each History card; clicking fires `onJumpToStep(step)` to `WranglingModule`, which sets `activeTab` and passes the step's current params as a pre-populated draft to the target subsection. The user still clicks "Apply" to commit — no auto-commit.
**Invariants:** Non-destructive — navigation does not replay or auto-commit anything. Follow the registry invariant (all 53+ step types in `registry.js` should have a mapping entry, even if some map to a "closest tab" fallback).

---

### Calculate · 2026-06-19
**Feedback:** "Add limits"
**File:** `src/components/tabs/CalculateTab.jsx`; `src/components/calculate/workbench/operations.js`
**Suggestion:** Add a `"limit"` operation type to `operations.js` alongside existing `"deriv"`, `"integral"`, `"optimize"`, `"solveZero"`. Use Richardson extrapolation for finite limits (evaluate at `c ± ε` and `c ± ε/2`, extrapolate to `ε→0`). Use large-value substitution for limits at ±∞. Display result as `lim_{x→c} f(x) = L`. Export to R as `pracma::lim(f, c)`, Python as `sympy.limit(f, x, c)`, Stata as a numeric approximation with a note. Fix the Stata `printf` syntax bug (see Bugs section) in the same pass.
**Invariants:** Math logic goes in pure JS, not inside the React component. `operations.js` must stay React-free.

---

### Calculate · 2026-07-04
**Feedback:** "Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)"
**File:** `src/components/tabs/CalculateTab.jsx`; `src/components/calculate/workbench/WorkbenchCanvas.jsx`
**Suggestion:** Add a "Constants" list alongside the function equations panel. Each entry: label + numeric value. Render as `Plot.ruleY([value])` horizontal mark on the canvas (this pattern is already used in `PlotBuilder.jsx` for the `hline` geom). Export to R as `geom_hline(yintercept = c)`, Python as `ax.axhline(y=c)`, Stata as `yline(c)`.
**Invariants:** Inline styles via `C`. Observable Plot CDN already loaded.

---

### General · 2026-07-05
**Feedback:** "Add plots with 3 axis"
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 does not natively support a third axis. Pragmatic near-term scope: implement a **secondary Y axis** mode that renders two separate `Plot.plot()` instances with a shared x-domain, aligned side-by-side or overlaid. This covers the main use case (two metrics at different scales on the same time axis). Defer full 3D scatter to a post-MVP WebGL pass. Document the limitation clearly in the UI — "3D scatter is planned for V2; use dual-axis for two-scale line charts."
**Invariants:** Observable Plot CDN already loaded. No additional charting libraries — any WebGL future pass needs a CDN load + CSP `connect-src` update.

---

### Clean · 2026-07-19
**Feedback:** "Add a function for Country Codes — iso2/iso3/country-name crosswalk transform"
**File:** `src/components/wrangling/FeatureTab.jsx`; `src/pipeline/runner.js`; `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step backed by a static ISO 3166-1 lookup table (~250 rows, ~15 KB) bundled as a JS module at `src/math/countryCodes.js`. The step maps an existing column (from iso2, iso3, or English name) to a new column in the desired format. Expose in `FeatureTab.jsx`. Register in `runner.js`, `registry.js`, and all three script translators: `countrycode::countrycode()` in R, `pycountry` in Python, `kountry` via SSC in Stata. For fuzzy name matching, reuse `fuzzyGroups` in `src/components/wrangling/utils.js`.
**Invariants:** New step must be registered in `registry.js` (kept in sync with `runner.js`) and validated by `pipelineReliabilityValidation.mjs`. `countryCodes.js` lives in `src/math/` — pure JS, no React.

---

### General · 2026-07-19 *(design session needed first)*
**Feedback:** "Think about how to construct lists, save them and use them during the session when filtering or constructing variables"
**File:** `src/services/session/sessionState.jsx` (likely home); `src/services/Persistence/indexedDB.js`
**Suggestion:** Scope must be decided before implementation: (a) what is a "list" — a named ordered set of string/numeric values; (b) where do lists live — session-scoped React context in `SessionStateProvider` alongside `datasets` is natural; (c) how do they surface — a `%in% listName` shorthand in FilterBuilder, a lookup in `case_when` conditions, a chip in the variable selector. Implementation sketch once scoped: add a `lists` key to `sessionState`, expose a "List Manager" panel accessible from the toolbar, wire `runner.js` filter/case_when cases to resolve named lists at step-run time (not step-creation time). Persist per `pid` in a new `session_lists` IndexedDB store.
**Invariants:** Lists must be `pid`-scoped. If persisted, use `indexedDB.js`, not `localStorage`. `sessionState.jsx` is React Context — no imports from it in `src/math/` or `src/core/`.

---

### Model · 2026-06-06 *(fix-later)*
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/services/export/latexTable.js`; `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` already accepts a `varLabels` option. Expose a collapsible per-coefficient label editor in the Report tab's model-table panel. Persist the `varLabels` map per `pid` in IndexedDB. See Bugs section fix-later entry above.
**Invariants:** `latexTable.js` is pure JS, no React. `indexedDB.js` for persistence.

---

## UX / Design

### General · 2026-07-17
**Feedback:** "Guide tour must be updated"
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array, line 11)
**Suggestion:** `TOUR_STEPS` has 9 entries. The Model step lists estimators but omits Sun-Abraham, Callaway-Sant'Anna, N-way FE, Bacon decomposition, GMM/LIML, Fuzzy RDD — all shipped since the original tour. Update the Model step text to reflect the current estimator roster from `EstimatorSidebar.jsx`. The Spatial step should reference the Analyze/Map/Plot sub-tab structure. The Calculate step should mention the Workbench and CAS capabilities. Content-only edits — no new tour cards needed.
**Invariants:** Inline styles via `C`. No external UI libraries.

---

## Performance

### Data · 2026-05-08 *(code-complete, browser benchmark pending)*
**Feedback:** "Big datasets generate crashes (21MB) over 900k rows"
**File:** `src/DataStudio.jsx`; `src/services/data/duckdb.js`
**Suggestion:** CLAUDE.md marks Fase X4 as code-complete for `.dta` files (parse once → DuckDB Arrow table → 500-row UI preview → OPFS Parquet cache). Browser-validate: load a >10MB CSV, confirm ⚡ badge appears, confirm tab is responsive within 2s, navigate through Clean → Explore → Model without stalls. If lag persists, investigate whether the full `rawData` JS object is inadvertently passed as React state — only `rawData._duckdb` (the pointer) should trigger re-renders, not the full row array. Other browser-parsed formats (`.rds`, `.xlsx`, `.shp`) still parse entirely in JS — extend the DuckDB on-load path to those as a follow-up.
**Invariants:** Data never leaves the browser (privacy-first). OPFS cache uses `saveRawData`/`loadFromOPFSKey` in `indexedDB.js`. Never truncate `rawData` before passing to `runner.js`.

---

## Suggested action order

1. **Fix CS DiD replication code** (`ReportingModule.jsx` `_buildModelScript`: add `treatCol`, `csEstMethod`, `csAnticipation`, `csBasePeriod` fields to `config.model`) — confirmed root cause, surgical fix, makes the CallawayCS estimator's export completely runnable.
2. **Fix plot replication missing load lines** (`plotScript.js`: accept `{ filename, loadOpts }`, prepend `buildRLoadLine`/`buildPyLoadLine`; `ExplorerModule.jsx`: pass `scriptPreamble` to PlotBuilder) — affects every user who exports an Explore plot for replication.
3. **Fix Stata workbench Mata print syntax** (`exportScript.js`: replace `("label", value)` with `printf()` / `display()`) — the Stata tab of the Calculator workbench is non-functional as written.
4. **Add cross-group arithmetic to group_summarize** (`ReshapeTab.jsx`: expose `fn:"expr"` UI with formula text input) — directly unblocks ATE-by-means, a core DiD workflow the runner already supports.
5. **Browser-validate the 4 code-complete items** (Plot Builder resize, OLS residual plots, diagnostics freeze, spatial heatmap) — zero coding required, unblocks Franco's sign-off on already-shipped work.
6. **LaTeX table auto-resize for wide models** (`latexTable.js`: `\resizebox{\textwidth}{!}{...}` when `colsN > 3`) — one-liner, high polish value for report exports.
7. **Add variable searcher to VarPanel** (`shared.jsx` `VarPanel`: text filter chip when `vars.length > 10`) — low-effort, high-frequency pain point for datasets with many columns.
8. **Persist FeatureTab draft state** (`FeatureTab.jsx`: `sessionStorage` on keystroke + restore on mount) — removes daily friction for case_when / if_else form writing.
9. **Add sort controls in data viewer** (`ExplorerModule.jsx`: `sortCol`/`sortDir` state, click `<th>`, DuckDB `ORDER BY` for large tables) — frequently requested, safe display-only change.
10. **Add F-statistic alongside chi-square in hypothesis tests** (`ModelHypothesis.js`: add `fStat = chiSq / df`; `CoefficientTestPanel.jsx`: render both rows) — trivial math extension using existing `betainc`/`betacf` helpers.
