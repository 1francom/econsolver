# Feedback Analysis Report — 2026-07-25

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **6 open bugs** · **12 open features** · **9 open UX / design** · **1 open performance** · **2 open other**

Cross-reference notes: Numerous ClaudeFB.md entries that appeared open (no strikethrough, no ✓) were resolved via BugTriage.md. 28 items remain genuinely unresolved after cross-reference. Two BugTriage entries marked "likely fixed" (FE N-way, FE interactions) need Franco browser confirmation before they can be closed.

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work  
**File:** `src/services/export/rScript.js` (line 1432, `CallawayCS` case) · `src/math/CallawayEngine.js`  
**Suggestion:** The R script block at `rScript.js:1432` destructures `csEstMethod` and `csAnticipation` from `model` but `wrapResult` in `estimationDispatch.js` (line 397) only stores `yVar, xVars, entityCol, timeCol, treatCol, compGroup` — so `csEstMethod` always defaults to `"dr"` and `csAnticipation` to `0` regardless of the user's actual settings. Extend the `wrapResult` spec to also include `csEstMethod`, `csAnticipation`, `csInfMethod`, and `csNBoot`. Additionally verify the `did` package's `att_gt()` argument names in the emitted snippet match the installed version's API.  
**Invariants:** `CallawayEngine.js` is in `src/math/` and must stay React-free. Export script generation is pure string logic — no UI imports.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/ExplorerModule.jsx` (line 2631, PlotBuilder mount) · `src/services/export/loadLine.js`  
**Suggestion:** `PlotBuilder` accepts a `scriptPreamble` prop but `ExplorerModule.jsx:2631` mounts it without passing one, so no data-load line is prepended to the generated R/Python code. Wire `scriptPreamble` from `ExplorerModule` by building a closure that calls `buildRLoadLine` / `buildPyLoadLine` (already in `services/export/loadLine.js`) using the active dataset's `loadOpts` and `filename`. Check the ModelingTab PlotBuilder mount for the same gap and fix in the same pass.  
**Invariants:** none

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx` · `src/services/export/stataScript.js`  
**Suggestion:** No specific failure was reported; start by auditing the generated Stata do-file for OLS, FE, 2SLS, and Poisson against Stata 17+. Known failure modes: `xtreg` with two-way cluster is a syntax error (use `reghdfe` — already handled for that case), `csdid` package requires `net install` (emit the install comment), and `vce(hc2)` is unsupported by `ivregress` (emit a fallback note). Cross-check the 168-cell SE matrix in `seTolerances.js` — any Stata column that throws should be caught there.  
**Invariants:** none

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group  
**File:** `src/pipeline/runner.js` (line 528, `group_summarize` case) · `src/components/wrangling/ReshapeTab.jsx`  
**Suggestion:** `group_summarize` only supports within-group aggregations (`mean`, `sum`, `sd`, etc.) — it cannot compute cross-group differences like `mean(Y | treat=1) − mean(Y | treat=0)`. Options: (a) add a `"diff"` aggregation type with a condition-column and pair-of-group-values config; (b) introduce a separate `"cross_group_diff"` step type; (c) document a two-step workaround (group_summarize → pivot_wider → mutate). Option (c) is zero-risk and immediately actionable; option (a) is the cleanest long-term solution.  
**Invariants:** New step types require a matching `registry.js` entry and must pass `pipelineReliabilityValidation.mjs`. Steps must replay on `rawData` non-destructively.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 — malformed row in multi-column dataset  
**File:** `src/DataStudio.jsx` (CSV parse path) · `src/services/data/parsers/`  
**Suggestion:** This specific file likely has an unescaped thousands-separator comma or stray quote in a numeric cell (e.g. `2,425.47`). The immediate fix is to surface the line number and raw text in the error toast so users know which row to repair externally. The durable fix is resilient import (see next item).  
**Invariants:** none

---

### [medium] · Data · 2026-06-14
**Feedback:** Malformed CSV row can abort import instead of surfacing recovery options  
**File:** `src/DataStudio.jsx` · `src/services/data/parsers/`  
**Suggestion:** Wrap each CSV row parse in a try/catch, accumulate `{ lineNumber, rawText }` tuples, and after loading clean rows present a recovery banner ("N rows skipped — download error report"). Combine with the 2026-05-10 fix since they share the same root cause and fix location.  
**Invariants:** Never mutate `rawData` in place. CSV parsing is in the JS path via `DataStudio.parseFile` (DuckDB handles `.parquet`).

---

## UX / Design

### medium · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js` (line 168, fixed `tabular` format) · `src/ReportingModule.jsx`  
**Suggestion:** `latexTable.js` emits a fixed `\begin{tabular}` with no page-width guard. When the model has many regressors the table overflows the page. Wrap the `tabular` in `\resizebox{\textwidth}{!}{...}` when `colCount > 6`, or use `\begin{adjustbox}{max width=\textwidth}` with a `\usepackage{adjustbox}` preamble line. Also add `\usepackage{booktabs}` to make the output publishable-grade.  
**Invariants:** none

---

### medium · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `ModelHypothesis.js` runs one hypothesis at a time. Add a "Save hypothesis" button in `CoefficientTestPanel.jsx` that accumulates hypotheses to a local list (part of the estimation spec so it persists with pinned models), then generates a batched R snippet using `car::linearHypothesis(fit, c("h1", "h2", ...))` — this runs a joint Wald test over all constraints in one call.  
**Invariants:** `ModelHypothesis.js` is in `src/math/` — must remain React-free.

---

### medium · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)  
**Suggestion:** The prompt covers IRR (Poisson), semi-elasticity (log-level), and linear marginal effects. Add a rule for log-log specifications: when both Y and an X are log-transformed (detectable from `ln_*`/`log_*` naming in the spec dict or the pipeline's `log` step history), interpret coefficients as constant elasticities (`β₁ = % change in Y per 1% change in X`). Also offer the elasticity at the mean formula for linear specs as an optional annotation.  
**Invariants:** All Anthropic API calls go through `AIService.js` — do not add a raw `fetch` to Anthropic elsewhere.

---

### medium · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis  
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `ModelHypothesis.js` already computes Wald F (`F = (Rβ−r)'(RVR')⁻¹(Rβ−r) / q`). Add chi-square via `χ² = q × F`, p-value via `1 − chi2CDF(χ², q)`. Expose a radio in `CoefficientTestPanel.jsx` — "F-stat" (default for OLS/WLS) vs "χ²" (default for Logit/Probit/Poisson where df is undefined). The math is one line.  
**Invariants:** `ModelHypothesis.js` is in `src/math/` — must remain React-free.

---

### medium · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` already owns a global artifact order per project. Extend the schema to include an optional `groupId` on each artifact entry. Add a "New group" button in `ModelBufferBar.jsx` that creates a named group; allow a group picker or drag-and-drop to assign pinned models/plots/maps. Filter comparisons to the active group. Comparison math is unchanged.  
**Invariants:** Persistence in `indexedDB.js`, not localStorage. Groups are cosmetic metadata and must not affect the non-destructive pipeline.

---

### medium · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to continue  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** `FeatureTab.jsx` keeps all subsection state (if_else, case_when, lag/lead, etc.) in local `useState` hooks that reset on every unmount. Lift the draft state to `WranglingModule.jsx` (one level up) so it survives tab switches, or use `sessionStorage` keyed by `pid` for transient draft persistence. Per CLAUDE.md, `localStorage` is deprecated for pipeline/data storage — `sessionStorage` is acceptable for UI drafts.  
**Invariants:** Do not use localStorage. IndexedDB is for pipeline/data persistence.

---

### medium · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** Add a column-header click handler in the data viewer table that toggles ascending/descending sort on the displayed preview rows. This is a local display sort — no pipeline step is created. Distinguish clearly in the UI: "sort view" (local, no pipeline effect) vs "arrange step" (pipeline, replays on full data). The `arrange` pipeline step already exists in Clean for the durable case.  
**Invariants:** Display-only — never mutate `rawData` or `filteredRows` in place.

---

### medium · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/ExplorerModule.jsx` (time series rendering) · `src/components/PlotBuilder.jsx`  
**Suggestion:** The time series plot in the Explore tab is rendered without Observable Plot's axis title options. Add a labels section (title, x-axis, y-axis) mirroring `PlotBuilder.jsx`'s existing "Labels" panel, and wire the values through to the Observable Plot spec. If the time series chart is rendered directly in `ExplorerModule.jsx` rather than via PlotBuilder, either port it to PlotBuilder or add the label controls inline.  
**Invariants:** none

---

### low · UI · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array, line 11)  
**Suggestion:** `TOUR_STEPS` has 9 steps (per CLAUDE.md) but many features shipped since: Spatial tab subtabs, Bacon decomposition, Callaway & Sant'Anna, Sun & Abraham, PlotBuilder in Explore, DatasetManager, NLCommandBar, model buffer. Update `TOUR_STEPS` with correct element selectors and descriptions. Low per-step effort — text and selector updates only.  
**Invariants:** none

---

## Features

### Explore · 2026-05-19
**Feedback:** Add filter option accepting R-style `filter(regionname %in% c("Spain", "Basque Country"))` syntax  
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder)  
**Suggestion:** Extend the filter expression evaluator to preprocess `x %in% c("a","b")` into `["a","b"].includes(x)` before passing to the expression sandbox. This targets R-fluent users and is low-risk — the replacement happens before `isSafeExpr` and within the existing sandboxed eval.  
**Invariants:** The `isSafeExpr` guard in `FeatureTab.jsx` must apply here too. Do not allow arbitrary eval outside the existing sandbox.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/math/CausalEngine.js` · `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** `runSharpRDD` already returns coefficients, SEs, bandwidth, kernel, polynomial order, and sample sizes. Add a compact `RDDSummaryTable` component in `ModelPlots.jsx` that renders these in the format `rdrobust` prints: point estimate, CI bounds, h_left/h_right, N_left/N_right. This is purely a display component — no new math required.  
**Invariants:** none

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` · `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**Suggestion:** Add a per-layer row-filter expression input in `GeoLayerConfig.jsx` (a simple column-comparison UI, not free-form expression) that filters the layer's rows before passing to Observable Plot's geo marks. Deferred to V4 per the X5 plan.  
**Invariants:** Spatial tab is self-contained. Display filter only — no pipeline step required.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)"  
**File:** `src/components/modeling/resultDisplay.jsx`  
**Suggestion:** Add an "Edit labels" button alongside the LaTeX table that opens an inline editable grid of `{ columnName, displayLabel }` pairs. Store the mapping in `spec.labelOverrides` on the pinned model (persisted via IDB `model_buffer`). Add `labelOverrides` to the export whitelist per the CLAUDE.md invariant (three sites: `CodeEditor.jsx`, `ReportingModule.jsx`, and every `transpileModel()` call site across the three exporters).  
**Invariants:** Export spec whitelist requires THREE edits per CLAUDE.md — CodeEditor, ReportingModule, and all `transpileModel` call sites.

---

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** Add a "Limit" operation type to the Calculate workbench. Expose fields for expression, variable, and target value (including ∞). The underlying CAS may already support `limit()`. Display the result with step-by-step rule labeling (L'Hôpital, squeeze theorem) if available.  
**Invariants:** none

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** When a user enters a numeric value or named constant as a "function", detect that the expression is constant (evaluate at two different x values and check equality), then render it as a horizontal line at `y = c` rather than as a curve. Low risk — one detection branch before the curve renderer.  
**Invariants:** none

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot 0.6 does not natively support a third y-axis. The standard approach is a dual-y chart: two overlaid Plot specs with transparent backgrounds sharing an x-axis, primary y on the left and secondary y on the right with independent domain. Add a `scaleY2` aesthetic mapping option and a secondary y-axis in the Labels panel as the minimum viable feature. Scope with Franco before implementing.  
**Invariants:** PlotBuilder uses Observable Plot 0.6 CDN — no external UI libraries.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a filter input above the variable list in `VariableSelector.jsx` that filters displayed columns by substring match. Use `useMemo` to derive the filtered display list from the search query without mutating the props. Pure UI enhancement — selection state is unchanged.  
**Invariants:** none

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When a layer has `aes.color` set, add an optional "show mark labels" toggle that appends a `Plot.text()` mark rendering the color dimension's value at the centroid or last-point of each series. Mirrors Observable Plot's own labeled line/bar chart examples.  
**Invariants:** none

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`  
**Suggestion:** Each step in `History.jsx` knows its type. Add a jump button on each step row that emits an `onJumpToStep(step)` callback (prop-threaded from `WranglingModule`). `WranglingModule` maintains a `stepType → { tab, subsection }` mapping and switches tabs accordingly. `History.jsx` must not know about tab routing directly.  
**Invariants:** none

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** Add a `"country_code"` transform using a small static JSON lookup table (iso2 ↔ iso3 ↔ full name, ~250 rows) bundled in `src/pipeline/data/countryCodes.json`. For fuzzy matching, reuse `fuzzyGroups` in `wrangling/utils.js`. Coordinate scope with Franco: strict lookup (low risk, deterministic) vs fuzzy match (more useful, more complex).  
**Invariants:** New pipeline step requires a `registry.js` entry and must pass `pipelineReliabilityValidation.mjs`.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session  
**File:** `src/services/session/sessionState.jsx` · Design pending  
**Suggestion:** The natural home is a new `lists` map in `SessionStateProvider` — a keyed collection of `{ name, values[] }`. Lists could be persisted in IDB alongside pipelines, exposed in FilterBuilder's `%in%` syntax as named references (e.g. `country %in% @eu_countries`), and in `case_when`/`if_else` forms. The blocker is designing the creation/edit UI surface (DatasetManager section vs. a new session sidebar tab). Recommend a design session with Franco before writing code.  
**Invariants:** Persistence in `indexedDB.js`, not localStorage.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js` · `src/pipeline/duckdbRunner.js`  
**Suggestion:** DuckDB-Wasm integration (Fase X4) is code-complete for `.dta` files: files >10 MB are parsed once, registered as Arrow-backed DuckDB tables, reduced to a 500-row UI preview, and persisted via OPFS Parquet. Status: awaiting Franco's browser benchmark on a real 900k-row dataset. If the benchmark still crashes, investigate the OPFS Parquet re-load path and confirm `SharedArrayBuffer` is available (requires COOP/COEP headers in `vercel.json`).  
**Invariants:** DuckDB as the data boundary — never pull the full Arrow result into JS objects. No server-side computation (privacy-first constraint).

---

## Other

### · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 · Original line: China,CHN,2010,2425.47...  
**File:** `src/DataStudio.jsx` (CSV parse path)  
**Suggestion:** Covered by the malformed-CSV resilience fix in the Bugs section above. For this specific case, also consider adding a pre-import numeric format detector: if a column mixes `1234.56` and `1,234.56` formats, offer to strip thousands-separator commas before parsing.  
**Invariants:** none

---

### · Security · 2026-06-13 / 2026-06-14
**Feedback:** XSS injection attempts via feedback form (two separate payloads)  
**File:** Feedback ingestion path — confirmed fixed 2026-06-14 per BugTriage  
**Suggestion:** Fixed — `sanitizeFeedback()` escapes `&<>"'` at write time. Follow-up recommendation: add a Supabase trigger/constraint to enforce sanitization server-side, since client-side sanitization is bypassable by direct POST. This was noted in BugTriage as a recommended follow-up and remains undone.  
**Invariants:** Any future feedback viewer must render description as escaped text, never raw innerHTML.

---

## Needs browser confirmation

The following BugTriage entries are marked "likely fixed" (PR #104, commit `045f5cda`) but have not been confirmed by Franco:

- **FE must accept any categorical variable (country, state, sector)** — `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`. N-way FE shipped. Franco should test with a non-entity non-time categorical FE column.
- **FE with interactions** — same files. FE interactions shipped. Franco should test a `sector × year` interaction in the FE spec.

If confirmed broken, root cause is likely `ModelConfiguration.jsx`'s FE column picker restricting to panel entity/time columns only.

---

## Suggested action order

1. **Wire dataset load lines into PlotBuilder replication code** (`ExplorerModule.jsx` line 2631, `loadLine.js`) — one `scriptPreamble` prop, immediately unblocks a common user request
2. **Fix Stata workbench** (`stataScript.js`, `CodeEditor.jsx`) — high-visibility core feature, audit per-model-type output against Stata 17+
3. **Fix CS DiD 2021 R replication code** (`rScript.js:1432`, `estimationDispatch.js` line 397) — extend `wrapResult` spec with `csEstMethod` / `csAnticipation`
4. **Extend group_summarize for cross-group math** (`runner.js`, `ReshapeTab.jsx`) — ATE computation is a fundamental econometric operation currently impossible in the pipeline
5. **Save form draft state in FeatureTab on navigation** (`FeatureTab.jsx`) — common UX friction, simple `sessionStorage` fix keyed by `pid`
6. **Add chi-square option for hypothesis tests** (`ModelHypothesis.js`, `CoefficientTestPanel.jsx`) — extends existing math with one line; high demand
7. **Fix LaTeX table page-width overflow** (`latexTable.js` line 168) — one `\resizebox` wrapper; blocks publishing workflows for wide models
8. **Add arrange/sort to data viewer** (`ExplorerModule.jsx`) — display-only column-header sort, low risk, highly expected
9. **Add variable searcher in variable sidebar** (`VariableSelector.jsx`) — pure filter UI, scales modeling workflow to many-variable datasets
10. **Add direct elasticity interpretation** (`Prompts/index.js`) — one new rule; high academic value for log-log regressions
