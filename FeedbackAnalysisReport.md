# Feedback Analysis Report — 2026-08-14

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary

- **12 open bugs** · **5 code-complete (browser validation pending)** · **5 deferred (fix-later)** · **7 open features** · **1 open performance**

Total open items analysed: **30**

---

## Bugs

---

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work  
**File:** `src/components/modeling/CodeEditor.jsx` (lines 36–67) · `src/services/export/rScript.js` (line 1435)  
**Suggestion:** `CodeEditor.buildScript` builds `config.model` from `result.spec` but never forwards the CS DiD-specific fields `treatCol`, `compGroup`, `csEstMethod`, and `csAnticipation`. In `rScript.js` the `CallawayCS` case destructures `treatCol` from `model` and gets `undefined`, emitting `gname = "undefined"` which breaks the `att_gt()` call immediately. Fix: add `treatCol: spec.treatCol ?? null`, `compGroup: spec.compGroup ?? "nevertreated"`, plus the other CS fields to `config.model` in `CodeEditor.jsx`; mirror the same additions in `pythonScript.js` and `stataScript.js` `CallawayCS` cases.  
**Invariants:** None violated — pure export-layer fix.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/services/export/plotScript.js` — `buildGgplot` (line 343), `buildMatplotlibPlot` (line 798), `buildStataPlot` (line 935)  
**Suggestion:** The spatial/geo plot builders already have `geoLoadLines` / `geoPyLoadLines` that prepend `buildRLoadLine` / `buildPyLoadLine` calls; the regular plot builders have no equivalent. Add a `filename` / `dataset?.loadOpts` parameter to `buildGgplot`, `buildMatplotlibPlot`, and `buildStataPlot`, and emit a load line at the top of each script (e.g. `df <- read_csv("file.csv")`). `PlotBuilder.jsx` already passes `allDatasets` to the exporters via `plotScript.js` so the filename is available.  
**Invariants:** None violated.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/calculate/workbench/exportScript.js` — `buildStata` (lines 80–116)  
**Suggestion:** Two independent bugs: (a) `twoway function y = expr, range(x0 x1)` silently uses Stata's built-in `x` as the free variable — if the user's axis is named `q`, `L`, etc., the function plot is wrong. Add a `rename xvar to ${ax}` idiom or use `generate ${ax} = …` with a `twoway connected` fallback, or emit a comment warning. (b) The Mata numeric operation blocks (derivative at lines 97–98, integral 100–103, optimize 105–108, root-scan 109–111) reference `xm`, `hh`, `n`, `a`, `b`, `sI`, etc. without declaring them as `real scalar` — Mata strict mode rejects undeclared variables. Add `real scalar xm, hh; …` declarations at the top of each Mata block.  
**Invariants:** None violated — pure export-layer fix; math files untouched.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group  
**File:** `src/ExplorerModule.jsx` (group & summarize UI, around line 1181) · `src/pipeline/runner.js` (`group_summarize` case, lines 528–588)  
**Suggestion:** The runner supports `mean | sum | count | min | max | sd | median | quantile` per group, but there is no cross-group arithmetic (e.g. `mean(Y | treat=1) − mean(Y | treat=0)`). Least-invasive solution: add a `diff` aggregation type to `runner.js`'s `group_summarize` that accepts two group-value literals (`by` must contain exactly one column) and computes the difference between two named group means in a pivot-then-subtract pattern. Alternatively, guide the user in the UI to first run `group_summarize` (mean by treat), then `pivot_wider` on the treat column, then `mutate` the difference — and expose this as a preset "Mean Difference" template in ExplorerModule's group-summarize panel.  
**Invariants:** New runner case must replay on `rawData` (non-destructive). No React in `runner.js`.

---

### [high] · UI · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS  
**File:** `src/components/modeling/ResidualPlots.jsx` · `src/components/ModelingTab.jsx`  
**Status:** ⚠️ Code-complete 2026-06-14 (`35f4b5a6`) — browser validation pending Franco. No further engineering action until confirmed.  
**Suggestion:** If Franco reports they do not appear, check that `result.spec` (not the live sidebar state) is threaded through to `ResidualPlots` — the fix in `35f4b5a6` was specifically to freeze the OLS/WLS display to the frozen `result.spec`.  
**Invariants:** None.

---

### [high] · UI · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models  
**File:** `src/components/ModelingTab.jsx` · `src/components/modeling/DiagnosticsPanel.jsx`  
**Status:** ⚠️ Code-complete 2026-06-14 (`35f4b5a6`) — browser validation pending Franco. No further engineering action until confirmed.  
**Invariants:** None.

---

### [high] · Export · 2026-07-17
**Feedback:** HC2 is not replicated in the code (generated R uses `vcov = "hetero"` instead of `sandwich::vcovHC`)  
**File:** `src/services/export/rScript.js` · `src/services/export/stataScript.js` · `src/services/export/pythonScript.js`  
**Status:** ⚠️ Code-complete 2026-07-18 (168-cell matrix verified) — browser validation pending Franco.  
**Invariants:** None.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js` (lines 162–178)  
**Suggestion:** `buildStargazer` emits a plain `\begin{tabular}{...}` block with no width guard. Wrap it in `\resizebox{\textwidth}{!}{\begin{tabular}...\end{tabular}}` when the column count exceeds a threshold (e.g. > 5 models). The column format string (`colFmt`) already exists and its length is known at generation time — no external dependency needed.  
**Invariants:** None — pure LaTeX string generation.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/components/modeling/CoefficientTestPanel.jsx` · `src/math/ModelHypothesis.js`  
**Suggestion:** The current panel runs one joint Wald test and discards it on re-selection. Add a small saved-tests list (local state, array of `{label, indices, h0s, chiSq, df, chiSqPval}`), a "Save test" button that appends the current result, and a section that renders each saved test with its `generateModelHypothesisScript` R/Python/Stata snippet. This is pure UI state — `waldTest` in `ModelHypothesis.js` is already correct and reusable.  
**Invariants:** No React in `ModelHypothesis.js`.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/ExplorerModule.jsx` — `TimeSeriesTab` (line 1291)  
**Suggestion:** `TimeSeriesTab` renders an SVG with auto-generated axis text derived from column names; there are no user-editable label fields. Add `[chartTitle, setChartTitle]`, `[xLabel, setXLabel]`, `[yLabel, setYLabel]` state vars and three small text inputs below the column selectors (mirrors the ExplorerModule histogram's `plotTitle`/`xLab`/`yLab` inputs, lines 892–929). The SVG chart already has a fixed-height `PAD.t = 24` header — increase to 40 if `chartTitle` is set and render it there.  
**Invariants:** None.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` — `INTERPRET_REGRESSION_PROMPT`  
**Suggestion:** The current prompt rules cover IRR (Poisson), log-linear (semi-elasticity), and marginal effects but do not compute or mention elasticities for log-log specifications (both Y and X log-transformed → coefficient IS the elasticity). Add a rule: when `log(Y)` is the dependent variable and one or more `log(X)` regressors are present, state the coefficient as an elasticity (% change in Y per 1% change in X) and compare it to 1 (elastic/inelastic/unit-elastic). The model type and variable names are already injected into the prompt.  
**Invariants:** All Anthropic calls must go through `AIService.callClaude`; prompt changes live in `Prompts/index.js` only.

---

### [medium] · UI · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems  
**File:** `src/components/PlotBuilder.jsx` · `src/components/ModelingTab.jsx`  
**Status:** ⚠️ Code-complete 2026-06-14 (`35f4b5a6`) — browser validation pending Franco.  
**Invariants:** None.

---

### [medium] · UI · 2026-06-13
**Feedback:** Heatmap must be fixed  
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx` · `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`  
**Status:** ⚠️ Code-complete 2026-06-14 (`5e105529`) — browser validation pending Franco.  
**Invariants:** None.

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** `artifactOrder.js` maintains a flat ordered list (`getArtifactOrder`/`saveArtifactOrder`) with no concept of groups. Add an optional `groupId` field to each artifact entry, a group-label store (array of `{id, name}` saved to IndexedDB alongside the artifact order), and a group-header row renderer in `ModelBufferBar.jsx`. Dragging artifacts between groups updates `groupId`. This is an IndexedDB schema extension — add it as a new field with `null` default so existing saved orders are not broken.  
**Invariants:** Persistence must use `indexedDB.js`. No localStorage.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing  
**File:** `src/components/wrangling/FeatureTab.jsx` (lines 450–543, `ConditionBuilder` component)  
**Suggestion:** `ConditionBuilder`'s state (`cwNN`, `cwCases`, `cwDefault`, `ifeNN`, etc.) is local `useState` with no persistence — it resets on every remount when the user navigates away. Lift the draft state one level to `FeatureTab` (or `WranglingModule`) so it survives tab switches within the same session. For persistence across page reloads, store the draft as a JSON value in `sessionStorage` keyed by `pid` (not IndexedDB — drafts are ephemeral, not pipeline state).  
**Invariants:** Pipeline steps must replay on `rawData`; draft state is pre-step and does not touch the pipeline. Do not use `localStorage` for pipeline/data storage — `sessionStorage` for ephemeral UI drafts is acceptable.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer  
**File:** `src/ExplorerModule.jsx` (DataViewer table render)  
**Suggestion:** Add click-to-sort to the DataViewer column headers. A `[sortCol, setSortCol]` + `[sortDir, setSortDir]` state (asc/desc toggle on repeated click) is sufficient. For DuckDB-backed datasets the sorted rows should be fetched via `getTablePage` with an `ORDER BY` clause (that function already supports `ORDER BY` for non-filter cases — see comment at ExplorerModule line ~326), keeping the sort inside SQL rather than sorting the 500-row preview.  
**Invariants:** Never sort the 500-row preview in JS and present it as the full-dataset sort. Use DuckDB SQL `ORDER BY` for large datasets.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` — `TOUR_STEPS` (lines 11–66)  
**Suggestion:** The tour step for "Model" (line 39) lists "14 estimators: OLS · WLS · FE · FD · TWFE · 2×2 DiD · 2SLS/IV · Sharp RDD · Logit · Probit · GMM · LIML · Synthetic Control" — this count is stale (Sun-Abraham, Callaway-Sant'Anna, Poisson FE, N-way FE, Fuzzy RDD, Event Study and more have shipped). Per CLAUDE.md working convention, **never write a count** — name the major families instead ("OLS/WLS · Fixed Effects family · IV/2SLS · RDD · DiD/Staggered-DiD · Non-linear · GMM"). Similarly the "8 tabs" mention in the Clean step should describe tabs by name so it doesn't rot on every addition. The Spatial step also pre-dates pipeline integration (`sp_*` steps and geocoding).  
**Invariants:** None.

---

### [low] · Data · 2026-06-14 (deferred)
**Feedback:** Parse error: CSV Error on Line: 1583 (malformed row aborts import)  
**File:** `src/services/data/parsers/` · `src/DataStudio.jsx`  
**Status:** Fix-later (post-launch). Resilient-import pass: retain original row number and surface a "skip / repair" preview.  
**Invariants:** None.

---

### [low] · UX · 2026-06-14 (deferred)
**Feedback:** R-style `%in% c(...)` syntax is not accepted by the filter expression UI  
**File:** `src/components/wrangling/CleanTab.jsx` · `src/pipeline/predicate.js`  
**Status:** Fix-later. `predicate.js` already has an `in` operator — the gap is parsing the R `%in% c(...)` surface syntax and mapping it to the canonical `{op:"in", value:[...]}` predicate.  
**Invariants:** `predicate.js` is the single owner of filter operators — never re-implement operator evaluation elsewhere.

---

### [low] · UX · 2026-06-14 (deferred)
**Feedback:** RDD result lacks an R-style `summary(rdrobust)` compact table  
**File:** `src/math/CausalEngine.js` · `src/components/ModelingTab.jsx` (RDD result panel)  
**Status:** Fix-later. Presentation enhancement — `CausalEngine.runSharpRDD` already returns all the fields needed for an rdrobust-style table; it just needs a dedicated result sub-component.  
**Invariants:** None.

---

### [low] · UX · 2026-06-14 (deferred)
**Feedback:** Plot builder has no spatial row-filter control scoped to a layer  
**File:** `src/components/tabs/spatial/plot/`  
**Status:** Fix-later (routed to V4).  
**Invariants:** None.

---

### [low] · UX · 2026-06-14 (deferred)
**Feedback:** Allow users to edit LaTeX table variable names (RDD always shows "D (treatment)", "running − c", etc.)  
**File:** `src/components/modeling/` (result display) · `src/services/export/latexTable.js`  
**Status:** Fix-later. Add an optional `labelMap: {colName: displayName}` field to the model result spec; `buildStargazer` and the in-app table renderer both read from it.  
**Invariants:** None.

---

## Features

---

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx` · `src/components/calculate/workbench/`  
**Suggestion:** The equation workbench exports `ops.deriv`, `ops.integral`, `ops.solveZero`, `ops.optimize` per equation. Add `ops.limit` with a `limitAt` value and a direction (left/right/both). The JavaScript CAS already uses nerdamer — check whether `nerdamer.limit(expr, ax, val)` is available; if not, emit a numeric `(f(val+ε) + f(val-ε)) / 2` approach in exports with a clear comment. Add R `Deriv`-package `limit()` or SymPy `sp.limit()` in the R/Python exports.  
**Invariants:** No React in math files.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/calculate/workbench/`  
**Suggestion:** The equation canvas already renders multi-function plots. A constant `f(x) = c` where the user sets no axis variable but picks a numeric value should plot as a horizontal line. Alternatively, add a "hline" entry type alongside equations: `{ kind: "hline", value: 5, label: "c" }`. Mirroring PlotBuilder's existing `hline` geom (which already handles this pattern) would give a consistent UX.  
**Invariants:** None.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis  
**File:** `src/components/PlotBuilder.jsx` (Observable Plot) · potentially a new 3D plot surface  
**Suggestion:** Observable Plot 0.6 does not support 3D — a true 3-axis (surface/scatter) plot requires either a WebGL layer (three.js / deck.gl) or a static SVG projection. The lowest-effort addition is a 2.5D scatter (x, y, and a third variable mapped to size or color) using PlotBuilder's existing `aes.color` and a new `aes.size` channel. Full 3D surface would be a new tab/panel. Recommend scoping to size-mapped scatter first; 3D surface as a follow-on after launch.  
**Invariants:** No external UI libraries beyond Observable Plot CDN already in use.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a `<input type="search" placeholder="Filter…">` above the variable list in `VariableSelector`. Filter the rendered column chips to those whose names include the search string (case-insensitive). No new state beyond a `[query, setQuery]` per selector instance. Clear the query on variable selection.  
**Invariants:** None.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/ExplorerModule.jsx` (legend rendering in categorical/scatter views) · `src/components/PlotBuilder.jsx`  
**Suggestion:** In ExplorerModule's categorical bar chart (`SvgBarChart`) and in PlotBuilder's color-grouped plots, the legend currently shows a swatch + the group label text. Add the group's count (n) and optionally the mean of the y-axis variable to each legend entry, matching R's `ggplot2` `stat_summary` label style. For PlotBuilder, this would require a pre-aggregation pass over the data per group.  
**Invariants:** None.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx` · `src/pipeline/runner.js` · `src/pipeline/registry.js`  
**Suggestion:** Add a `country_code` pipeline step type with a `from` field (`"name"`, `"iso2"`, or `"iso3"`) and a `to` field. Bundle a compact static JSON lookup table (~250 entries, ~15 kB) directly in the repo (no CDN call, consistent with privacy-first design). The step emits a new column. Register it in `registry.js` and add a translator in `stepTranslators.js`. Expose a "Country codes" section in FeatureTab under a new "Identifier" subsection.  
**Invariants:** New step must replay on `rawData` (non-destructive). No React in `runner.js`. Registry must stay in sync with runner.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
**File:** New cross-module concern — `src/services/session/sessionState.jsx` (most natural home)  
**Suggestion:** Add a `lists` key to the session context (alongside `datasets`): `{ name: string, values: any[] }[]`. Expose a "Lists" drawer in the sidebar (similar to DatasetManager) where users can create/edit named lists. The filter `in` operator in `predicate.js` and the `case_when` builder in `FeatureTab` could offer a "use a saved list" button that resolves to the stored array. Persistence via `indexedDB.js` alongside the existing coach-chat store.  
**Invariants:** Session state must go through `sessionState.jsx`. Persistence via `indexedDB.js`, not `localStorage`.

---

## Performance

---

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` · `src/services/data/duckdb.js` · `src/pipeline/duckdbRunner.js`  
**Status:** Fase X4 code-complete (OPFS Parquet cache, lazy init, 500-row preview) — browser benchmark pending Franco. The DuckDB performance strategy documented in CLAUDE.md (Parquet-first, OPFS persistence, display limit ≠ computation limit) addresses this. No new engineering action until Franco confirms the benchmark.  
**Invariants:** All computation on full dataset; display limited to 200–500 rows (virtualized). No server-side computation.

---

## Items that may already be fixed (BugTriage status stale)

| Item | Evidence | Recommendation |
|------|----------|----------------|
| [2026-07-17] Add chi-square for `linearHypothesis` | `CoefficientTestPanel.jsx` line 210 already renders χ²(df) and p-value from `waldTest()` which is chi-square based | Close in BugTriage — already implemented. If user wanted F-statistic as well, that's a separate feature request. |
| [2026-07-09] FE must accept any categorical variable | N-way FE shipped in `045f5cda` (#104); `runFEMulti` in `PanelEngine.js` + FE picker in `ModelConfiguration.jsx` | Needs Franco browser confirmation then BugTriage close |
| [2026-07-09] FE with interactions | Same commit — `factor_interactions` step + `materializeFEInteraction` in `feInteraction.js` | Needs Franco browser confirmation then BugTriage close |
| [2026-07-05] Share Project does not work | Fixed 2026-07-14 (`20260714120000_fix_share_blobs_name_ambiguity.sql`, browser-verified) | BugTriage status already "fixed" — no action |
| [2026-07-13] Shared projects can't be opened | Same fix as above | BugTriage status already "fixed" — no action |

---

## Suggested action order

1. **Fix CS DiD replication code** (`CodeEditor.jsx` + `rScript.js` / `pythonScript.js` / `stataScript.js`) — single-session bug, high user impact for the primary staggered-DiD workflow, small surgical fix.
2. **Add dataset load lines to regular plot replication exports** (`plotScript.js` `buildGgplot` / `buildMatplotlibPlot` / `buildStataPlot`) — 3-line fix per function; without it every plot script fails to run.
3. **Fix Stata workbench Mata variable declarations** (`exportScript.js` `buildStata`) — numeric operations silently fail; declare `real scalar` types and handle non-`x` axis variables.
4. **Fix LaTeX table `\resizebox` wrapping** (`latexTable.js`) — trivial string wrap, prevents table overflow on multi-model specs.
5. **Add time series label controls** (`ExplorerModule.jsx` `TimeSeriesTab`) — 3 text inputs + SVG title render, high UX payoff.
6. **Browser-validate 5 code-complete items** (Plot Builder resize, Y~Yhat plots, heatmap, diagnostics freeze, HC2 replication) — code already in; Franco needs to confirm and close BugTriage entries.
7. **Variable searcher in variable selector** (`VariableSelector.jsx`) — 5 lines; high payoff for wide datasets.
8. **Multiple hypothesis tests save** (`CoefficientTestPanel.jsx`) — add saved-test list; leverages existing `waldTest` + `generateModelHypothesisScript`.
9. **group_summarize mean-difference** (`ExplorerModule.jsx` + `runner.js`) — enables ATE-style cross-group arithmetic.
10. **Case_when draft persistence** (`FeatureTab.jsx`) — lift draft state to parent or sessionStorage; prevents user data loss.
