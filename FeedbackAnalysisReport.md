# Feedback Analysis Report — 2026-07-06

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **6 open bugs** · **8 open features** · **0 open UX/Design** · **1 open performance** · **0 open other**

Cross-reference note: many ClaudeFB.md items that lack strikethrough/✓ markers are
confirmed fixed in BugTriage.md. This report covers only items whose BugTriage status
is `open`, `code-complete (browser validation pending)`, or `fix-later`, plus any
ClaudeFB items not yet mentioned in BugTriage at all.

---

## Bugs

### critical · Model · 2026-07-02
**Feedback:** When using DuckDB, the number of observations reported in model output
uses the 500-row browser preview, not the full raw row count — affects n, degrees of
freedom, t-statistics, F-statistics, and p-values.

**File:** `src/components/ModelingTab.jsx` (lines 914–934, 1861–1868);
`src/pipeline/duckdbRunner.js` (line 686); `src/services/data/duckdb.js`

**Suggestion:** The SQL fast-path correctly reads `rowCount` from
`cleanedData._duckdb.rowCount`. The JS fallback at line 1861 calls `getFullRows()` which
extracts the full Arrow table before passing rows to the math engine. The regression
most likely surfaces in `_runEstimation` when it is called with the full-row array but
internally derives `n` from something other than its argument rows — audit every engine
branch (FE, FD, TWFE, EventStudy, Callaway, SunAbraham, Spatial) inside
`_runEstimation` for any reference to the closure-level `rows` variable (the 500-row
preview) rather than the passed argument. Confirm that `EstimationResult.n` is always
set from the argument, not the closure.

**Invariants:** SE type must be passed explicitly to every engine call; `rows` (the
preview) must never substitute for the full dataset when computing statistical output.

---

### high · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables
for a new estimation; they must remain frozen until re-estimation and reappear correctly
when restoring pinned models.

**File:** `src/components/ModelingTab.jsx` (commit `35f4b5a6` — code-complete,
browser validation pending)

**Suggestion:** The fix in `35f4b5a6` freezes OLS/WLS display to `result.spec` so
sidebar edits cannot retroactively mutate the displayed result. Browser validation by
Franco is the remaining blocker. Recommend triggering a session with a pinned OLS
model, then changing X variables, and confirming the original coefficient plot and
diagnostics remain unchanged until the user explicitly re-estimates.

**Invariants:** `result.spec` is the canonical frozen state for a completed estimation;
no UI-driven state change should mutate it.

---

### high · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.

**File:** `src/components/modeling/ResidualPlots.jsx`;
`src/components/ModelingTab.jsx` (lines 2563–2565, 2619–2621, 2867)

**Suggestion:** `ResidualVsFitted` and `QQPlot` are imported and wired at the correct
tab slots. The actual regression — resolved by `35f4b5a6` — was that the display read
from live state rather than `result.spec`, causing the plots to blank out when the user
touched the sidebar. Browser validation is the remaining blocker; run an OLS and confirm
all three plots appear and remain stable through sidebar edits.

**Invariants:** `src/math/` and `src/core/` have zero React imports; the plot
components live in `src/components/modeling/` as intended.

---

### medium · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed.

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`;
`src/components/tabs/spatial/map/SpatialLayerEditor.jsx` (line 207 — heatmap branch)

**Suggestion:** The heatmap layer was restored in `5e105529` as a Leaflet map layer
with KDE cells, palette/opacity controls, legend, and HTML export. Browser validation
is the remaining blocker. Verify that adding a heatmap layer type in
`SpatialLayerEditor` renders correctly on the Leaflet map, honours the colour palette,
and exports to HTML without errors.

**Invariants:** Leaflet is loaded via the CDN singleton in
`src/components/tabs/spatial/shared/leaflet.js`; do not duplicate the load call.

---

### medium · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able
to resize it; PNG export should not have size/aesthetic problems.

**File:** `src/components/PlotBuilder.jsx` (line 1626 — `resize: "vertical"` handle);
`src/components/ModelingTab.jsx`

**Suggestion:** The plot viewport was given `resize: "vertical"` CSS with bounded
responsive canvas sizing so PNG captures user-selected dimensions. Browser validation
by Franco is the remaining blocker. Test resizing the in-model Plot Builder vertically
and exporting as PNG; confirm the PNG dimensions match the visible viewport rather than
a hardcoded fallback.

**Invariants:** Inline styles only via the `C` object from `src/components/wrangling/shared.jsx`;
no external CSS.

---

### medium · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 ·
Original Line: China,CHN,2010,2425.47… (line truncated)

**File:** `src/DataStudio.jsx` (CSV parse branch); `src/services/data/parsers/`

**Suggestion:** The current CSV parser aborts on a malformed row instead of surfacing
a recovery UI. Add a resilient-import mode: catch the row-level parse error, record the
original row number and raw text, skip or quarantine the row, and present the user with
a preview of skipped rows alongside a "continue with N skipped rows" / "abort" choice.
The `_duckdb` path already handles large CSVs — implement this hardening there first
since it is the path most likely to encounter data-quality issues at scale.
Marked `fix-later` in BugTriage — schedule after launch.

**Invariants:** Non-destructive pipeline — `rawData` must preserve the parse error
information as metadata; do not silently truncate rows. Use IndexedDB, not localStorage,
for any quarantine state.

---

## Features

### Clean · 2026-07-01
**Feedback:** Add confirmation buttons for all critical functions like clearing all the
datasets, clearing all the pipeline, signing out, etc.

**File:** `src/components/workspace/DatasetManager.jsx` (lines 51–75 — `CascadeConfirm`
already exists); `src/WranglingModule.jsx` (lines 328, 738 — `confirmDeleteStep`);
`src/App.jsx` (lines 1833–1834 — `clearAllProjects`/`clearAllPipelines`; line 2282 —
`signOut`)

**Suggestion:** A `CascadeConfirm` component already exists in `DatasetManager.jsx`
for per-dataset deletes. Extend the same pattern to: (1) "Clear all datasets"
(App.jsx lines 1833–1834); (2) "Clear entire pipeline" in `WranglingModule`; (3) "Sign
out" (App.jsx line 2282 — add a modal warning that unsaved state will be lost). Reuse
the existing `CascadeConfirm` shape or extract it to a shared utility in
`src/components/wrangling/shared.jsx`.

**Invariants:** Inline styles via `C` object only; no external UI libraries.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a
constant value alongside function curves).

**File:** `src/components/tabs/CalculateTab.jsx`

**Suggestion:** When the user enters an expression that evaluates to the same value for
all x (e.g. `y = 3` or a named parameter like `c = 4.2`), detect this by evaluating
at two distinct x points and comparing. Render it as a horizontal rule on the existing
function graph canvas (`Plot.ruleY` if using Observable Plot, or a plain SVG `<line>`).
Label the rule with the expression text. No change to any math engine is required —
only the graph-rendering section of `CalculateTab.jsx` needs updating.

**Invariants:** `src/math/` and `src/core/` must not import React; all new rendering
logic stays in `CalculateTab.jsx`.

---

### Calculate · 2026-06-19
**Feedback:** Add limits.

**File:** `src/components/tabs/CalculateTab.jsx`

**Suggestion:** Add a "Limit" mode to the existing single-equation workbench (alongside
Derivative/Integral/Optimize). Accept a point `a` (or `±∞`) and compute one-sided
limits numerically by evaluating `f(a ± ε)` for a shrinking sequence of ε. For
discontinuities, surface left-limit ≠ right-limit explicitly. Display the result as a
KaTeX expression: `lim_{x→a} f(x) = L`. The `["lim","\\lim_{x\\to 0}"]` symbol
shortcut already exists at line 309 — wire it to a real computation. Keep the logic
self-contained in `CalculateTab.jsx`, no additions to `src/math/`.

**Invariants:** Zero React imports in `src/math/` and `src/core/`.

---

### Explore · 2026-05-19
**Feedback:** Add filter `(regionname %in% c("Spain (Espana)", "Basque Country (Pais
Vasco)"))` for filter option.

**File:** `src/components/wrangling/CleanTab.jsx`; `src/services/exprEvalService.js`

**Suggestion:** Extend the filter expression parser in `exprEvalService.js` to
recognise R-style `x %in% c(...)`. Parse the `c(...)` argument as a comma-separated
string or number list and emit a JS `Set.has` predicate. Alternatively, add an explicit
"Member of set" filter operator in the `FilterBuilder` UI alongside existing `==`/`!=`/`>`/`<`
operators — this avoids the need for users to type R syntax. Marked `fix-later` in
BugTriage.

**Invariants:** Pipeline steps must replay on `rawData` — the value list must be
serialised into the step definition so `runner.js` can re-apply it deterministically.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust)` — Call:
rdrobust, Sharp RD estimates using local polynomial regression.

**File:** `src/components/ModelingTab.jsx` (RDD results display, around line 3295);
`src/math/CausalEngine.js`

**Suggestion:** `CausalEngine.runSharpRDD` already returns `h` (bandwidth), LATE,
SE, p-value, N left/right of cutoff, and CI. Render these in a compact two-row table
modelled on rdrobust's `summary()` output: columns for Conventional, Bias-corrected,
Robust; rows for Coeff, SE, t-stat, p-value, CI bounds. Place this above the existing
bandwidth-sensitivity chart. No math engine changes needed. Marked `fix-later` in
BugTriage.

**Invariants:** None specific — this is a presentation change only.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept.

**File:** `src/components/modeling/ModelConfiguration.jsx`;
`src/math/LinearEngine.js`

**Suggestion:** Add a `noIntercept: boolean` field to the model spec, surfaced as a
checkbox in `ModelConfiguration.jsx`. In `LinearEngine.runOLS` (and `runWLS`, `runFE`,
`runFD`), when `noIntercept` is true, skip prepending the column of ones to the design
matrix X. Propagate the flag through `EstimationResult.spec` so replication scripts
honour it — in `rScript.js` emit `lm(y ~ x - 1, ...)`. Every estimator branch in
`ModelingTab.estimate()` must forward the flag. Marked `fix-later` in BugTriage;
requires careful `df` accounting since the intercept is counted in `k` for most branches.

**Invariants:** SE type must still be passed explicitly; `src/math/` has zero React
imports — the flag enters via the spec argument.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots.

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`;
`src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Add a per-layer attribute filter control in `GeoLayerConfig` (column,
operator, value). Store it on the layer definition object. In `SpatialGeoPlot`, filter
`rows` against the predicate before the geometry-parsing step. For spatial/bbox filters,
parse WKT bounds and test containment. Marked `fix-later` — route to V4 after launch.

**Invariants:** Inline styles only; no CSS files.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always
shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column
names.

**File:** `src/components/ModelingTab.jsx` (single-model results section);
`src/services/export/latexTable.js` (line 126 — `varLabels` lookup already exists);
`src/components/modeling/ModelComparison.jsx` (lines 331, 489 — `varLabels` state
already implemented for comparison mode)

**Suggestion:** `ModelComparison.jsx` already implements a `varLabels` map with inline
text inputs per variable (line 489) that feed into `latexTable.buildStargazer` via the
`varLabels` option. Expose the same mechanism in the single-model LaTeX export button
in the main `ModelingTab` results panel: render an editable label per coefficient name,
store overrides in a local `varLabels` map, and pass it to `buildStargazer`. For RDD,
pre-populate defaults from `result.spec` column names (e.g. actual `treatVar` and
`runningVar` values) rather than hardcoded strings. Marked `fix-later` in BugTriage.

**Invariants:** Inline styles only; session-only state is acceptable for MVP.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows.

**File:** `src/DataStudio.jsx`; `src/services/data/duckdb.js`

**Suggestion:** Fase X4 is code-complete per the X5 reconciliation — large datasets
are parsed once, registered as Arrow-backed DuckDB tables, reduced to a 500-row UI
preview, and persisted via OPFS Parquet cache. The sole remaining blocker is browser
benchmark validation by Franco (`plans/2026-05-22-fase-x4-performance.md`). No new
code is suggested until the benchmark confirms or refutes the fix. If validation fails,
the next lever is OPFS lazy loading: keep the Parquet file across sessions so
re-opening a project skips CSV re-import entirely.

**Invariants:** Privacy-first — computation must remain entirely client-side.
`vercel.json` must include `Cross-Origin-Opener-Policy: same-origin` and
`Cross-Origin-Embedder-Policy: require-corp` for `SharedArrayBuffer` support.

---

## Suggested action order

1. **Fix DuckDB 500-row n bug** [critical · 2026-07-02] — wrong n corrupts every
   statistical output for large-dataset users; audit `_runEstimation` callsites for any
   path that still reads the closure-level `rows` preview rather than the passed argument
   (`src/components/ModelingTab.jsx`).

2. **Browser-validate four code-complete fixes** [high/medium · 2026-06-14,
   2026-06-11, 2026-06-13, 2026-06-10] — all four items have merged code but lack
   Franco's browser sign-off; a single test session covering OLS diagnostics, pinned-model
   freeze, spatial heatmap, and Plot Builder resize closes them all.

3. **Add confirmation dialogs for destructive operations** [feature · 2026-07-01] —
   highest-priority open feature; `CascadeConfirm` in `DatasetManager.jsx` only needs
   to be reused for sign-out, clear-all, and full-pipeline-clear actions.

4. **Plot constants in Calculate graph** [feature · 2026-07-04] — small, self-contained
   change in `CalculateTab.jsx` with no engine dependency.

5. **Add limit computation** [feature · 2026-06-19] — new Calculate mode; a numerical
   approach keeps it entirely within `CalculateTab.jsx` with no `src/math/` changes.

6. **CSV resilient-import** [medium bug · fix-later] — prevents data-loss frustration
   on malformed World Bank / OECD CSVs; schedule post-launch.

7. **R `%in%` filter syntax** [feature · fix-later] — low implementation effort;
   extend `exprEvalService.js` and `FilterBuilder` UI.

8. **RDD rdrobust summary table** [feature · fix-later] — presentation only; all data
   already available from `CausalEngine.runSharpRDD`.

9. **LaTeX variable label editing (single-model)** [feature · fix-later] — reuse the
   `varLabels` pattern from `ModelComparison.jsx`.

10. **Estimate without intercept** [feature · fix-later] — engine-wide contract change;
    requires careful `df` accounting across all estimator branches.
