# Feedback Analysis Report — 2026-07-24

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 11 open bugs · 13 open features · 3 open UX/Design · 1 open performance · 2 open other
- 4 additional items are code-complete but awaiting Franco's browser validation (noted below)
- Cross-reference: many ClaudeFB.md items without strikethrough are confirmed resolved in BugTriage or the X5 reconciliation table; those are excluded from this report.
- **New since 2026-07-23:** No new ClaudeFB.md rows added today. Newly closed items: `[2026-05-25] Estimate without intercept` (DONE 2026-07-18 per CLAUDE.md — BugTriage not yet updated; excluded from features); HC2 R-export fix (code-complete 2026-07-18, ~~struck through~~ in ClaudeFB). N-way FE + FE interactions (both marked "likely fixed" in BugTriage; moved to Other/confirmation-only).

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`)
**Suggestion:** The `CallawayCS` R export block destructures `{ treatCol, entityCol, timeCol, compGroup }` from the model object, but `ModelingTab._runEstimation` stores those as `csTreatCol`, `csEntityCol`, `csTimeCol`, `csCompGroup` in the spec. This mismatch produces `undefined` in the generated `att_gt()` call. Fix: align destructuring to `model.csTreatCol ?? model.treatCol` (and equivalent for entity/time/compGroup) in `rScript.js`. Also verify the Stata path in `stataScript.js` line 1127 — it reads `entityCol`/`timeCol`/`treatVar` as outer-scope vars; confirm they are correctly resolved for the CS branch. Run the emitted R script verbatim and compare against `CallawayEngine.js` output.
**Invariants:** `rScript.js` and `stataScript.js` are pure JS — no React, no Anthropic API calls.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group_summarize, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (line 528, `group_summarize` case) and `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` aggregates single columns per group but cannot express cross-group arithmetic. Minimum viable fix: add a tooltip in `ReshapeTab.jsx` describing the two-step idiom — `group_summarize → pivot_wider(names_from=treatment) → mutate(ATE = treated − control)`. Optionally add a `"diff"` aggregation mode in `runner.js` that, given exactly two group levels and a `refLevel` field, returns the difference of group means as a single output row. If adding `"diff"`, also register it in `registry.js` and add translator cases in `rScript.js`/`pythonScript.js`/`stataScript.js`.
**Invariants:** `runner.js` must replay on `rawData` — never mutate in place. Any new step type requires a `registry.js` entry (T5 in `pipelineReliabilityValidation.mjs` enforces this). No React imports in `runner.js`.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/services/export/stataScript.js` and `src/components/modeling/CodeEditor.jsx`
**Suggestion:** The 2026-07-18 HC2/HC3 pass fixed many sites but BugTriage still shows this open. Audit every `case` branch in `stataScript.js` for: (a) null/empty returns for recently-added estimators (Sun-Abraham, Callaway-Sant'Anna, N-way FE via `reghdfe`, CR2/CR3); (b) `xtreg` with two cluster variables (invalid Stata syntax) — the `reghdfe` two-way cluster fallback may be incomplete for non-panel models; (c) undefined column names (symptom of the CS DiD field-mismatch bug above). Run a matrix check across all estimator × SE-type combos for the Stata language and confirm no branch returns empty or throws.
**Invariants:** `stataScript.js` is pure JS. `seType` must be honoured per the explicit-SE invariant in CLAUDE.md.

---

### [high] · Explore · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/services/export/plotScript.js` and `src/ExplorerModule.jsx`
**Suggestion:** `plotScript.js` already imports `buildRLoadLine`/`buildPyLoadLine` and uses them in the geo-plot path. The regular `buildGgplot` path for PlotBuilder receives only `{ dfVar }` without `filename` or `loadOpts`, so load lines are silently omitted. Fix: in `ExplorerModule.jsx` where PlotBuilder is mounted (~line 2234), pass `allDatasets={allDatasets}` if not already present; in `PlotBuilder.jsx`'s `copyPlotScript`, pass `datasets: allDatasets` into `buildGgplot`/`buildMatplotlibPlot` so it can call `buildRLoadLine(filename, loadOpts)` at the top of the script. The Explorer SVG charts (histogram, bar, time series) have no R/Python path at all — add a simple "Copy as R" button for each that calls `buildRLoadLine` + the appropriate `ggplot2` block.
**Invariants:** `plotScript.js` is pure JS. Load lines must honour per-dataset `loadOpts` (haven/readr/xlsx) — never hardcode `read.csv`.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`
**Suggestion:** `buildStargazer` emits `\begin{tabular}{l r r...}` with no overflow protection. When `models.length > 3`, wrap the `tabular` block in `\resizebox{\linewidth}{!}{ ... }` and add a `% Requires: \usepackage{graphicx}` preamble comment. This is a single conditional wrap inside `buildStargazer`; no caller changes needed.
**Invariants:** `latexTable.js` is pure JS — no React imports. All callers (`ReportingModule.jsx`, `ModelComparison.jsx`) pass through unchanged.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/components/modeling/CoefficientTestPanel.jsx` and `src/math/ModelHypothesis.js`
**Suggestion:** `CoefficientTestPanel` currently shows only the last test result and has no history. Add a `savedTests[]` array to local `useState`. After each test run, add a "Save" button that pushes `{ h0s, indices, chiSq, fStat, chiSqPval, fPval }` into the array. Render saved tests as a compact list below the test builder. Add a "Copy all as R" button that concatenates the existing `car::linearHypothesis` snippet for each test. `ModelHypothesis.js` already generates valid R snippets — no math changes needed.
**Invariants:** `savedTests` is React component state — not IndexedDB (transient per session). `src/math/ModelHypothesis.js` must remain zero-React.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` (`TimeSeriesTab` function, ~line 1291)
**Suggestion:** `TimeSeriesTab` has no label controls (unlike `SvgHistogram` which accepts `title`/`xLabel`/`yLabel`). Add three text inputs using the existing `labInput` helper pattern (~line 830): chart title, x-axis label, y-axis label. Thread them into the SVG render: add a centered title `<text>` at the top and axis label `<text>` elements along each axis (matching `SvgHistogram`'s pattern at ~lines 242–245). Include labels in the `onPin` payload so pinned charts carry them.
**Invariants:** Inline styles via `C` object from `shared.jsx`. No external UI libraries.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` and `src/services/AI/AIService.js` (`interpretRegression`)
**Suggestion:** The prompt already contains log-log elasticity and log-level semi-elasticity rules (lines ~99, 353–354), but those require the data dictionary to carry unit annotations. Add a detection step in `interpretRegression`: inspect `spec.yVar` and `spec.xVars` for log-transformation markers (`ln_`, `log_`, `_log` prefix/suffix, or a `log` pipeline step in `spec.pipeline`). Inject a `VARIABLE TYPES` block into the user prompt listing which variables are logged. Also add a client-side elasticity computation — `ε = β × mean(X)/mean(Y)` at the mean — drawn from `result.means` (already available), surfaced as a tooltip or collapsible row below each coefficient without an additional AI call.
**Invariants:** All Anthropic calls go through `AIService.js`. Detection logic belongs in `AIService.js`, not in `src/math/`. The `SHARED_CONTEXT` cached system block must not be changed.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis
**File:** `src/math/ModelHypothesis.js` (lines 203–213) and `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `testLinearHypothesis` already computes `chiSq` and `chiSqPval` for the joint Wald test, and `CoefficientTestPanel.jsx` already displays them (line ~211). For the single-coefficient t-test, add `chiSq: tStat ** 2` and `chiSqDf: 1` to its return value and display it alongside the t-stat row (asymptotically `t² → χ²(1)`). Also add the joint F-stat `F = chiSq / q` as a supplementary line in the joint-test panel for parity with R's `linearHypothesis` dual output. If the request is for a separate Pearson chi-square test of independence (categorical × categorical), add a new `chiSquareIndependence` export to `ModelHypothesis.js` using a standard contingency-table formula and expose it in a sibling panel.
**Invariants:** `ModelHypothesis.js` must remain zero-React. The joint Wald computation path in `runner.js` is unaffected.

---

### [medium] · Data · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx` (data table header area)
**Suggestion:** Add `sortCol: string | null` and `sortDir: "asc" | "desc"` to `ExplorerModule` local state. Clicking a column header cycles unsorted → asc → desc; add a ↑↓ glyph in active headers using `C.teal`. Apply `[...displayRows].sort(...)` on the 200-row display slice before rendering. For DuckDB-backed tables, issue `SELECT ... FROM table ORDER BY col dir LIMIT 500` to reflect full-dataset order. This is display-only — must NOT add an `arrange` pipeline step or modify `rawData`.
**Invariants:** Must not modify `rawData`. Display sort is view-only. The existing `arrange` step handles permanent sorts.

---

### [low] · Data · 2026-05-10 (Other)
**Feedback:** Parse error: CSV Error on Line 1583 — `China,CHN,2010,2425.47...` (truncated)
**File:** `src/DataStudio.jsx` (CSV parse branch) and `src/services/data/parsers/`
**Suggestion:** BugTriage classifies this as "fix-later — post-launch resilient-import pass". Wrap the CSV parse loop in per-row try/catch; on error record `{ _rowError: true, _rawLine, _rowIndex }` and continue. After parse, surface a dismissible warning banner ("N rows skipped — row numbers: [list]") and exclude error rows from the Arrow schema passed to DuckDB. For large files, the DuckDB path already uses `IGNORE_ERRORS=TRUE` — verify that option is set and that skipped rows are surfaced to the user.
**Invariants:** Error rows must NOT propagate into `rawData` or the DuckDB Arrow schema. Skip list is transient state — do not persist to IndexedDB.

---

### [low] · General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array, line 11)
**Suggestion:** `TOUR_STEPS` predates several shipped features: N-way FE, Sun-Abraham/CS DiD estimators, Bacon decomposition panel, CR2/CR3 inference options, NL command bar, and the Report tab AI script. Audit each entry against the current 7-tab layout from `WorkspaceBar.jsx` and `APP_CAPABILITY_MAP`. Add steps for Spatial (pipeline integration) and Report (AI narrative + unified script) if missing. Remove any step that references UI elements no longer at the described selector path. Per CLAUDE.md: any new tab added to the tour must also appear in `APP_CAPABILITY_MAP`.
**Invariants:** `HelpSystem.jsx` is a UI-only file. Any new tab referenced must have a matching entry in `src/services/AI/appCapabilityMap.js`.

---

## Items awaiting browser validation (code-complete)

No new code needed — Franco should validate these and close them in BugTriage.

| # | Bug | Commit | Responsible file(s) |
|---|-----|--------|---------------------|
| CC-1 | Plot Builder resize + PNG export aesthetics | `35f4b5a6` | `src/components/PlotBuilder.jsx`, `ModelingTab.jsx` |
| CC-2 | OLS Y~Ŷ / residual-vs-fitted / Q-Q plots missing | `35f4b5a6` | `src/components/modeling/ResidualPlots.jsx`, `ModelPlots.jsx` |
| CC-3 | Diagnostics frozen to `result.spec` (no sidebar mutation) | `35f4b5a6` | `src/components/ModelingTab.jsx` |
| CC-4 | Spatial heatmap restored (Leaflet KDE layer) | `5e105529` | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` |

---

## Features

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))`
**File:** `src/components/ModelingTab.jsx` (RDD result section) — new presentational component
**Suggestion:** All values are already in `EstimationResult` (bandwidth `h`, `N_left`, `N_right`, `tau`, `se`, `pval`, `ci95`, kernel, polynomial order). Add a `RDDSummaryBlock` presentational component that formats them as the standard rdrobust console block. Wire it above the coefficient table in `ModelingTab.jsx`. No math changes needed.
**Invariants:** Display-only component; inline styles via `C`. No imports from `src/math/` in the UI component.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows generic names regardless of actual columns
**File:** `src/services/export/latexTable.js` (existing `varLabels` option ~line 22) and `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` already accepts a `varLabels: { [varName]: displayLabel }` option but `ReportingModule.jsx` never populates it. Add a small "Edit labels" panel in the Report tab with a text input per variable name in the current model. Pass the state into `buildStargazer`. Persist `varLabels` per project in IndexedDB alongside the model buffer.
**Invariants:** IndexedDB for persistence — never localStorage. `latexTable.js` is pure JS. UI additions go in `ReportingModule.jsx`.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider so users can find variables faster with large datasets
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a filter `<input placeholder="Filter…">` at the top of each variable list panel. On keystroke, filter the chip list to case-insensitive substring matches. Hide the input when `headers.length ≤ 10`. Purely a rendering filter on existing state — no parent changes needed.
**Invariants:** Inline styles via `C` from `src/components/modeling/shared.jsx`. No external autocomplete libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/ExplorerModule.jsx` (bar chart and distribution legend)
**Suggestion:** When a color-by column is active, extend the legend to show per-group `(n = X)` and mean-Y value. For the bar chart (`SvgBarChart`), add count text at the end of each bar. Group sizes are already available from the `groupedSeries` / `items` state — no new aggregation needed.
**Invariants:** Inline styles via `C`. No external libraries.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click one pipeline step to jump to its corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx` and `src/WranglingModule.jsx`
**Suggestion:** Add a "✏ Edit" icon to each step card in `History.jsx` that fires a new `onJumpToStep(step)` callback. `WranglingModule` maps `step.type` to the correct tab using the `STEP_REGISTRY` category field (cleaning → "clean", feature → "feature", reshape → "reshape", merge → "merge") and switches the active tab. Start with tab navigation only — per-step form pre-population can follow incrementally. Clicking "Jump to" must not trigger a pipeline replay.
**Invariants:** Non-destructive pipeline — the step array must not be mutated by navigation. Tab switching is view-state only.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a `limit(expr, x, to)` function using numeric approximation: evaluate `expr` at `x = to ± ε` for progressively smaller ε (`[1e-4, 1e-6, 1e-8]`) and return the converging value. Flag non-converging cases explicitly. Keep the math helper in `src/math/` (pure JS, no React) and import it into `CalculateTab.jsx`.
**Invariants:** Pure math helper must live in `src/math/` or `src/core/` — zero React imports there.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants alongside function curves (e.g. horizontal line for a constant value)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When an expression evaluates to the same value across all x (i.e. it is a constant scalar), render it as a horizontal dashed `<line>` across the x domain at `y = value` with a label `"name = value"`. The existing SVG canvas already draws function curves — add a branch in the render loop for numeric constants. No new dependencies needed.
**Invariants:** Inline styles via `C`. Existing SVG canvas only — no charting libraries.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx` and `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a `grid` layer type to `GeoLayerConfig` that accepts a dataset column containing WKT polygon cells (output of the `sp_grid_assign` pipeline step). Route them through the existing `parseWktRings` helper in `src/components/tabs/spatial/shared/wkt.js` to draw them in the SVG canvas. Palette and opacity controls are already handled by the layer editor.
**Invariants:** Geometry math lives in `src/math/SpatialEngine.js` — no new geometry logic in UI components.

---

### Clean · 2026-05-19 (fix-later)
**Feedback:** Add filter with R-style `%in% c("Spain", "Basque Country")` syntax
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder)
**Suggestion:** Add a pre-parse rewrite in the filter expression input handler that transforms `col %in% c("a","b")` → `["a","b"].includes(col)` before storing the step. Use a regex with proper quoted-string tokenisation. Surface `%in%` in the filter UI hint text. The stored step spec must hold the rewritten JS-compatible expression for replay correctness.
**Invariants:** Stored `filter` step must be JS-evaluable to replay on `rawData`. Non-destructive pipeline invariant applies.

---

### Model · 2026-05-21 (fix-later)
**Feedback:** Allow spatial filters for plots (BugTriage: routed to V4)
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add a bounding-box clip to `SpatialGeoPlot` that hides points/polygons outside a user-drawn rectangle. On the SVG side, clip via `<clipPath>`. This is display-only — it must NOT add a pipeline step. Low priority per BugTriage (V4 slot).
**Invariants:** Display-layer filter only. No pipeline mutation.

---

### Model · 2026-06-14 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names
*(See Features section above — same item)*

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis (3D plots)
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Full WebGL 3D would require a new dependency and violate the zero-external-UI-library invariant. Pragmatic alternatives: (1) add a "bubble" geom that maps a third variable to marker size; (2) add a `facet` option splitting one variable into a grid of 2D panels — both expose three dimensions without 3D. Flag this as a design decision for Franco before implementation.
**Invariants:** No external UI component libraries. All styles inline via `C` object.

---

### Clean · 2026-07-19 (needs scoping with Franco)
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk
**File:** `src/components/wrangling/FeatureTab.jsx` and a new `src/services/data/countrycodes.js`
**Suggestion:** Implement as a `recode_country` pipeline step with a bundled ISO-3166 crosswalk JSON (~250 entries, ~15 KB) in `countrycodes.js`. Step takes `sourceCol` and `targetFormat` (`iso2 | iso3 | name`); runner looks up each value. Expose in `FeatureTab.jsx` under a "Country Codes" chip. Fuzzy-name matching via existing `fuzzyGroups` can be offered as an opt-in toggle — confirm scope with Franco before enabling (false-match risk).
**Invariants:** New pipeline step requires `runner.js` case + `registry.js` entry + translator cases in all three export scripts. No runtime fetch — crosswalk must be static. `pipelineReliabilityValidation.mjs` T5 enforces registry sync.

---

## UX / Design

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js` and `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Add a `groupId: string | null` field to artifact order entries. In `ModelBufferBar.jsx`, render models under collapsible group headers and limit comparison to the active group. MVP: add a group label chip per pinned model card with an inline-edit name; drag-and-drop between groups is a follow-up. Persist `groupId` in the existing `v9 model_buffer` IndexedDB store via `artifactOrder.js`.
**Invariants:** IndexedDB for persistence — never localStorage. Inline styles via `C`.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections (e.g. case_when lost when navigating away)
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a `useEffect` that serialises draft state (`mode`, expression, `cwCases`, column name) to `sessionStorage` keyed by `${pid}::featureTab:draft` on every change (debounced ~300 ms). Hydrate from that key on mount; clear on successful `onAdd`. Show a small teal dot on the Feature tab when an unsaved draft exists.
**Invariants:** `sessionStorage` only for transient drafts — never IndexedDB or localStorage (CLAUDE.md deprecates localStorage for pipeline/data storage). Draft state must never flow into `runner.js` as a committed step.

---

### Data · 2026-07-17
**Feedback:** Add arrange/sort functions in the data viewer
*(See Bugs section — also filed as a UX item)*
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Column-header click-to-sort (display-only). See Bugs section for full detail.
**Invariants:** Must not modify `rawData` or add pipeline steps.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/services/data/duckdb.js` and `src/DataStudio.jsx`
**Suggestion:** BugTriage and CLAUDE.md note that Fase X4 (DuckDB push-down for large CSVs via OPFS Parquet cache) is code-complete but browser-benchmarked only partially. No new code suggested. Franco should validate: (1) tab-transition no longer hangs after loading a 900k-row CSV; (2) pipeline steps run on the full table (not the 500-row preview); (3) OPFS Parquet round-trip persists between sessions.
**Invariants:** Per CLAUDE.md performance strategy: `Cross-Origin-Embedder-Policy: require-corp` must remain in `vercel.json` for `SharedArrayBuffer`. Computation must always use the full dataset — never the display truncation.

---

## Other (open, not yet actionable)

- **Session lists feature** [2026-07-19 · General] — Cross-module "save a list and use it in filters/variable builders". No natural home yet in the current state model (`sessionState.jsx`?). Needs design session with Franco before implementation. File not located — manual investigation needed.
- **N-way FE / FE interactions browser confirmation** [2026-07-09 · Model] — BugTriage lists these as "likely fixed" (`045f5cda`/\#104). No code change suggested; Franco should browser-verify the categorical-column FE dimension picker and the interaction chip in `ModelConfiguration.jsx`.

---

## Suggested action order

1. **Fix CS DiD R replication field name mismatch** (`rScript.js` line 1432 `CallawayCS` destructuring) — high-visibility, the only committed estimator with a broken export; produces `undefined` column names in the generated R script.
2. **Browser-validate the 4 code-complete items** (CC-1 through CC-4 table) — zero implementation; Franco closes all 4 in one session.
3. **Fix group_summarize cross-group math** (`runner.js` + tooltip in `ReshapeTab.jsx`) — blocks the most common ATE workflow without leaving the tool; minimum viable fix is a UI tooltip at zero engineering cost.
4. **Audit Stata workbench end-to-end** (`stataScript.js`) — the HC2/HC3 pass fixed many sites but the issue is still open; a 168-cell matrix check (estimator × SE-type × language) will catch remaining invalid syntax.
5. **Add dataset load lines to plot replication code** (`plotScript.js` + `ExplorerModule.jsx`) — the PlotBuilder geo path already does this; extending to the regular plot path and Explorer SVG charts closes the gap.
6. **Add time series label controls** (`ExplorerModule.jsx` `TimeSeriesTab`) — small self-contained addition (3 text inputs + 3 SVG `<text>` nodes); unblocks basic chart customisation for reports.
7. **Variable searcher in VariableSelector** (`VariableSelector.jsx`) — trivial filter input; high UX value for large datasets.
8. **Persist FeatureTab draft state** (`FeatureTab.jsx` → `sessionStorage`) — ~15-line fix; eliminates a recurring workflow interruption when writing case_when transforms.
9. **LaTeX table `\resizebox` wrap** (`latexTable.js`) — single-line conditional; high polish for multi-model thesis tables.
10. **Update guide tour** (`HelpSystem.jsx`) — pure text update; high go-to-market value for LMU institutional demos.
