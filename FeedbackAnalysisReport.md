# Feedback Analysis Report — 2026-09-12

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 8 open bugs · 13 open features · 3 open UX/Design · 1 open performance · 1 open other

> **Cross-reference note:** Several ClaudeFB.md items appear without strikethrough yet have a corresponding `fixed` entry in BugTriage.md (e.g. `.dta` loading 2026-05-13, grid clipping 2026-05-13, spatial auto-fit 2026-05-30). Those are omitted here — only items confirmed open in BugTriage OR absent from BugTriage entirely are listed.

---

## Bugs

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (no dedicated simulate exporter exists yet)
**Suggestion:** Add a `SimulateExporter` that serializes the full Simulate session: DGP draw params (`dgpScript.js` expressions), any Monte Carlo loop (sample size, n_reps), and any downstream sample tests or estimation run on simulated data. The DGP already exports R/Python/Stata via `distExprR`/`distExprPy`/`distExprStata`; the gap is stitching the draw into the downstream analysis steps. Follow the `generateCleanScript` preamble pattern already used by PlotBuilder.
**Invariants:** All script generation must go through the three per-language exporters in `src/services/export/`. No React in `src/math/` or `src/core/`.

---

### [high] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** `CodeEditor.jsx` defines the Stata tab (`id: "stata"`) but BugTriage confirms it is not fully functional. Audit which estimator-specific blocks in `stataScript.js` are incomplete (known gap: two-way clustered SE uses `reghdfe` but is marked UNVERIFIED; `ibn.` factor coding for through-the-origin under no-intercept is flagged UNVERIFIED; `joinby` for many-to-many merges is labeled UNVERIFIED). Systematically test each estimator path against a live Stata session to find and stub/note broken branches rather than leaving them silent. Surface a per-language warning chip in CodeEditor when a generated snippet carries a `NOTE:` marker, so users know before copying.
**Invariants:** No raw `fetch` to Anthropic API; all AI calls via `AIService.js`. Inline styles via `C` object only.

---

### [high] · Performance / Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`
**Suggestion:** The `.dta` path was fixed (DuckDB registration for >10MB, 2026-06-10) but the BugTriage notes "other browser-only formats remain future work." The same DuckDB pipeline should be applied to `.rds`, `.xlsx`, and `.shp` parsers: after in-browser parse, write the Arrow table to DuckDB, persist via OPFS Parquet, and hand the module a `_duckdb` reference instead of the full JS object array. This eliminates the ~15 s tab-transition lag and the 21MB crash. See the existing `.dta` branch in `DataStudio.jsx` as the template.
**Invariants:** Data never leaves the browser; the DuckDB-Wasm privacy boundary must be preserved. Always pass `_duckdb` through `onComplete` (the DuckDB-observation-count bug from 2026-07-02 was caused by omitting it).

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` (groupBy/time series render block ~lines 675–769), `src/components/PlotBuilder.jsx`
**Suggestion:** The time series section builds group subsets via `rows.filter(r => r[groupBy] === g)` but the "reference group" colour/legend entry is not rendered or computed. Implement a reference-group chip (similar to the "None" chip for groupBy) that designates one group level as the baseline for visual comparison — draw it in a neutral colour so non-reference groups stand out. The legend and CSV export path should also carry the group name.
**Invariants:** No external UI libraries; inline styles only via `C`.

---

### [medium] · Model / Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently runs one hypothesis at a time without persisting results. Add a "Saved hypotheses" list (array of `{label, Rmat, r, testType}` objects, kept in component state and IndexedDB per model uuid). A "Save this test" button appends the current hypothesis; a "Copy all to R" button calls `generateJointHypothesisScript` once per saved entry and concatenates the output. This aligns with the `car::linearHypothesis` pattern `modelHypothesisRValidation.R` already validates.
**Invariants:** Persistence via `src/services/Persistence/indexedDB.js` only, never localStorage.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.
**File:** `src/ExplorerModule.jsx` (time series render section), `src/components/PlotBuilder.jsx`
**Suggestion:** The time series section in ExplorerModule renders via Observable Plot but exposes no label controls (title, x-axis label, y-axis label). Add a collapsible "Labels" panel — matching the one already in `PlotBuilder.jsx` for point/line geoms — with title, x-label, y-label, and caption fields. Wire these into the Plot spec's `caption` / axis `label` options. The replication script should emit `labs(title=, x=, y=)` in R and the matplotlib equivalents.
**Invariants:** No external UI libraries; inline styles only.

---

### [medium] · General / Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` emits a fixed `{tabular}` environment. For wide tables (more than ~5 regressors), wrap in `\resizebox{\linewidth}{!}{…}` or emit `adjustbox` (requires the `adjustbox` LaTeX package). Alternatively, auto-switch to a `longtable` or `threeparttable` layout for >6 columns. Add the emitted package list (`\usepackage{adjustbox}` or `booktabs`/`longtable`) as a comment header in the generated snippet so users know what to add to their preamble.
**Invariants:** None — this is a pure export/text-generation change.

---

### [medium] · Data / Pipeline · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 — malformed CSV row aborts import
**File:** `src/services/data/parsers/` (CSV path in `DataStudio.jsx`), `src/DataStudio.jsx`
**Suggestion:** BugTriage marks this "fix-later — resilient-import." When PapaParse (or the DuckDB CSV reader) encounters a malformed row, catch the per-row error rather than aborting the whole parse. Surface a "N rows skipped — bad format" banner in the Data tab header (matching the style of the OPFS-mismatch banner). Offer a "Download skipped rows" button so users can inspect and repair them. The row indices should be preserved so the original line number (e.g. line 1583) is shown.
**Invariants:** Non-destructive: malformed rows must never silently disappear; the user must be told explicitly.

---

## Features

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators
**File:** `src/components/PlotBuilder.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Expose `nbins`, `line_type` (linear/quadratic/none), `by` (conditional binscatters by group), and a raw-scatter toggle as model-plot config options. The existing `ModelPlots.jsx` partial-plot and RDD-plot sections can be extended. Mirror `rdplot`'s `nbins`/`p` options and `binsreg::binsreg()`'s `binsregselect` for reference. Emit the corresponding R argument names in the replication script via the `specExtras` whitelist in `CodeEditor.jsx`.
**Invariants:** `specExtras` → export needs the THREE whitelist edits (CodeEditor, ReportingModule, each transpileModel call site).

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small `<input type="search">` above the variable chip list in `VariableSelector.jsx`. Filter the displayed chips in real time by matching against the column name/label. Keep the filtered list sorted alphabetically; highlight the matching substring. This is a pure UI addition — no engine or pipeline change needed.
**Invariants:** Inline styles only; no external UI libraries.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` already has a `type` field (from `STEP_REGISTRY`). Map step types to their tab/section: e.g. `filter|drop_na|fill_na|recode` → Clean tab, `log|sq|std|dummy|lag|mutate|case_when` → Feature tab, `group_summarize|pivot_longer|pivot_wider` → Reshape tab. Clicking the step row (or a dedicated ✏️ icon) dispatches a tab-change event to `WranglingModule`. Pre-populate the relevant sub-panel with the step's existing params so the user lands in an edit-ready state.
**Invariants:** Pipeline state is immutable mid-edit — do not mutate a step directly on click; open a copy in the panel and only commit on "Apply".

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`, `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Add a `sampleN` slider (1000 … full n, log scale) to the plot config panel that randomly downsamples the rows passed to the Observable Plot spec. For model plots this allows dense scatterplots (residual vs fitted, partial plots) to be legible without losing the fitted line, which is always computed on the full dataset. The slider should live in PlotBuilder's existing "Labels" / "Axis" collapsible, not in the pipeline.
**Invariants:** Downsampling is view-only; it must never affect the estimation results or the pipeline.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When a user defines a numeric constant (e.g. `c = 5`), detect it in the expression evaluator and offer a "Plot constant" toggle that draws a horizontal `ruleY([5])` mark in the same graph panel alongside the function curves. This reuses Observable Plot's `Plt.ruleY` — the same mechanism already used by PlotBuilder's `hline` geom.
**Invariants:** No external UI libraries; inline styles via `C`.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js`, `src/components/modeling/results/` (RDD result display)
**Suggestion:** BugTriage marks this "fix-later — presentation enhancement." The `runSharpRDD` result already contains bandwidth, kernel, polynomial order, and all three conventional SE estimates. Render a collapsed `rdrobust`-style ASCII table in the RDD result panel: Coef | SE (Conventional) | SE (Robust) | CI left | CI right | p-value, rows for the point estimate at each side of the cutoff. The existing `EstimationResult` carries all needed fields; no engine change is required.
**Invariants:** None — UI-only addition.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set, the current legend shows swatches but no count or mean label per group. Add an optional data-label chip (off by default) that overlays each series' final-point label or adds a small count annotation to the legend swatch row. This mirrors ggplot's `ggrepel::geom_label_repel` pattern. The R export already handles `aes(color=)` — extend it to emit `geom_label_repel` when the option is on.
**Invariants:** Inline styles only.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** BugTriage marks this "fix-later — route to V4 plots." Implement a bounding-box (rectangle drag) or polygon-draw filter on the Leaflet map that writes a `filter` pipeline step (using `predicate.js` operators `gte/lte` on lat/lon columns) into the active dataset's pipeline. Use `predicateToSQL` for DuckDB-backed datasets. This reuses existing pipeline infrastructure and keeps the operation reproducible via `sp_*` step translators.
**Invariants:** Any new pipeline step must be added to `runner.js`, `registry.js`, and all three script translators simultaneously. Never re-implement filter operators outside `predicate.js`.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a new pipeline step type `country_code` with params `{inCol, fromFormat, toFormat}` where `fromFormat` and `toFormat` are `iso2 | iso3 | name | numeric`. Ship a static lookup table (< 50 KB) in `src/math/countryLookup.js` (pure JS, no React) keyed by all four formats. The FeatureTab gets a new "Country Code" sub-panel with two dropdowns and a column picker. Export to R (`countrycode` package), Python (`pycountry`), Stata (`kountry` SSC).
**Invariants:** Static lookup table must live in `src/math/` or `src/core/` with zero React imports.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/services/export/latexTable.js`, `src/components/modeling/results/` (RDD result display)
**Suggestion:** BugTriage marks this "fix-later." Add an optional `labelMap: {[varName]: string}` parameter to the `buildStargazer` / `latexTable` call sites. In the Model result panel, render an ✏️ inline on each coefficient row that opens a small text input to override the display name. Persist overrides per model uuid in IndexedDB. The LaTeX export reads `labelMap` from the stored model metadata.
**Invariants:** Persistence via `indexedDB.js` only. Export changes go through `latexTable.js`; no raw LaTeX assembly elsewhere.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis (3D plots)
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** Observable Plot 0.6 does not support 3D. The simplest approach is a colour-mapped 2D plot (x, y, colour = z) as a first step, which is already supported by the `tile` geom and colour ramps. True 3D would require loading a separate CDN library (e.g. Plotly.js from `cdnjs.cloudflare.com`) as an optional geom type. Note: Plotly adds ~3 MB to the page weight; consider gating behind a "3D" geom chip that lazy-loads the script only when selected.
**Invariants:** External scripts may only load from the CSP allowlist (`cdnjs.cloudflare.com`, `cdn.jsdelivr.net/npm/`). Add any new CDN origin to BOTH `script-src` AND `connect-src` in `vercel.json` (the triple-bite pattern documented in CLAUDE.md).

---

## UX / Design

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend the `artifactOrder` store schema to include a `groups: [{id, label, artifactIds[]}]` array alongside the flat `order` array. Add a "New group" button to `ModelBufferBar` that prompts for a label, then lets users drag model chips into named groups. The comparison mode (`ModelComparison.jsx`) should scope to the active group by default, with a toggle to compare across groups. `getArtifactOrder`/`saveArtifactOrder` in `artifactOrder.js` carry the group structure transparently.
**Invariants:** Persistence via `indexedDB.js` only; the v9 `model_buffer` store is already the right owner. No localStorage.

---

### Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Each transformation sub-panel (case_when, mutate, lag, etc.) has its own controlled-input state that resets on unmount. Add a `useEffect` in each sub-panel that writes the current draft to `sessionStorage` under a key scoped to `pid + tabName + stepType` (e.g. `draft_<pid>_feature_case_when`). On mount, restore from that key. Clear the draft when the step is successfully applied or explicitly cancelled. This is view-state persistence, not pipeline mutation, so it is safe under the non-destructive pipeline invariant.
**Invariants:** `sessionStorage` is appropriate here (view draft, not pipeline data); `indexedDB.js` is required only for pipeline/data storage. Never write partial/invalid steps to the pipeline.

---

### General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array)
**Suggestion:** `TOUR_STEPS` in `HelpSystem.jsx` references UI elements that may have moved or been renamed since the 9-step tour was written (WorkspaceBar tabs, the Explore/Model tab split, N-way FE, Callaway–Sant'Anna, etc.). Audit each step's `target` selector against the current DOM, update the `content` text to reflect the current feature set, and add steps for the Spatial and Simulate tabs. Follow the CLAUDE.md convention: `title` prop = module name (not "How to…"), `HintBox` trigger reads "Help".
**Invariants:** None — UI copy only.

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile for `.rds`, `.xlsx`, `.shp`), `src/services/data/duckdb.js`
**Suggestion:** *(See also the Bugs section entry — this is listed here to match the original ClaudeFB "Performance" category.)*  For `.rds`, `.xlsx`, and `.shp`, after in-browser parse write the JS array to a DuckDB in-memory table via `conn.insertArrowTable` or a bulk `INSERT` batch, then persist the OPFS Parquet cache as the `.dta` path already does. The `DataStudio` parseFile switch already has the `.dta` pattern to follow. Gate the code-path behind the same `size > 10_000_000` threshold. Add a "Converting to columnar format…" spinner in the Data header during the initial write.
**Invariants:** DuckDB-Wasm `SharedArrayBuffer` requires `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` in `vercel.json`. The OPFS Parquet extension (`extensions.duckdb.org`) must be in `connect-src` (already fixed but verify it stays present). Data never leaves the browser.

---

## Suggested action order

1. **Stata workbench [high]** — High-visibility gap that blocks replication for Stata users; audit broken branches in `stataScript.js` and surface warning chips for stubs.
2. **Simulate replication script [high]** — Replication fidelity is a core value proposition; extend the script generator to cover the full Simulate session beyond the DGP draw.
3. **Time series group references [medium]** — Broken feature in a heavily-used Explore panel; reference-group rendering is a small Observable Plot change.
4. **Variable searcher in modeling sidebar [medium]** — Low-effort, high-impact UX: a single `<input type="search">` filter in `VariableSelector.jsx` unblocks sessions with many columns.
5. **Draft persistence for transformation subsections [medium]** — Recurring pain point (`case_when`, `mutate`); straightforward `sessionStorage` save on each sub-panel's `useEffect`.
6. **Guide tour update [low]** — Quick copy pass; deferred only because it is purely editorial.
