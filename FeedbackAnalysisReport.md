# Feedback Analysis Report — 2026-08-13

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 11 open bugs · 16 open features · 4 open UX/Design · 1 open performance · 0 open other

---

## Bugs

### [high] · Export / Replication · 2026-07-05
**Feedback:** "Replication code for CS DiD 2021 does not work"
**File:** `src/services/export/rScript.js` (~line 1432), `src/services/export/stataScript.js` (~line 1127), `src/services/export/pythonScript.js` (~line 1217)
**Suggestion:** The `case "CallawayCS"` in `rScript.js` destructures `csEstMethod` and `csAnticipation` from `model`, but `estimationDispatch.js` does NOT store these fields in the spec (only `yVar`, `xVars`, `entityCol`, `timeCol`, `treatCol`, `compGroup`, `csDefaultView` are saved). They always default to `"dr"` and `0` regardless of what the user configured. Add `csEstMethod`, `csAnticipation`, `csBasePeriod`, `csInfMethod`, `csNBoot` to the spec written by `wrapResult(...)` in `estimationDispatch.js` (~line 397), then read them in all three translators. Verify the generated `did` package R call runs verbatim in R ≥ 4.1.
**Invariants:** All AI calls go through `AIService.js` — no change needed. Script generation is pure-JS export code, no invariant risk.

---

### [high] · Export / Replication · 2026-07-05
**Feedback:** "Replication codes of the plots must have the lines to load datasets as well"
**File:** `src/services/export/exploreStatScript.js`, `src/components/explore/ExplorePinBar.jsx`
**Suggestion:** `transpileExploreStat()` generates plot code referencing a bare `df` variable but never emits a load line. Thread `filename`, `dataLoadOpts`, and `allDatasets` into the function (the same way `generateRScript` already receives them). Prepend a `buildRLoadLine(filename, dataLoadOpts)` / `buildPyLoadLine()` / `buildStataLoadLine()` call (already implemented in `src/services/export/loadLine.js`) before the plot body. The callers in `ExplorePinBar.jsx` must pass the session's dataset metadata — either via a new prop from `ExplorerModule.jsx`, or by reading the session's active dataset from `sessionState`.
**Invariants:** `loadLine.js` already exists — reuse it, do not duplicate load-logic. The single-egress AI rule is not involved.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** "Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group"
**File:** `src/pipeline/runner.js` (`group_summarize` case ~line 528), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` collapses rows to one row per group using `mean`, `sum`, `sd`, etc. — each aggregate produces a single group-scoped row, so cross-group arithmetic (mean\_treatment − mean\_control) is impossible in one step. Two complementary approaches: (a) add a `"expr"` aggregate type whose `col` field is a JS-safe expression evaluated after all per-group values are computed (e.g. `agg_treatment_mean - agg_control_mean` on the output frame); or (b) add a `pivot_wider` → `mutate` pattern and document the two-step idiom in ReshapeTab's HintBox. Approach (a) is more powerful and keeps the pipeline single-step. The runner's `case "group_summarize"` already iterates `s.aggs` — a new `fn:"expr"` case could resolve column references to the group's output row. Add a matching entry to `STEP_REGISTRY`.
**Invariants:** Pipeline steps must replay on `rawData` (non-destructive) — this is satisfied as long as the expression is pure. No React in math files — the expression evaluator must live in `runner.js` or a helper in `src/core/`, never in a component.

---

### [high] · Export · 2026-07-17
**Feedback:** "Stata workbench doesn't work completely"
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** The user report is vague, but the most likely gap is that specific estimators (e.g. CallawayCS, Synthetic Control, N-way FE interactions, CR2/CR3 SE types) emit placeholder comments or incomplete Stata code while R/Python are complete. Audit `stataScript.js` for `case` branches that fall through to `# no Stata equivalent` or produce empty strings, then compare against the corresponding R branches. Focus on: panel models with `feCols` arrays >2, `seType:"CR2"/"CR3"` (which correctly note no Stata equivalent but may do so in an unformatted way), and the `CallawayCS` `csdid` block (Stata's `csdid` package has a different syntax from the R `did` package — verify the emitted call matches `csdid` 1.x). Also verify that CodeEditor's Stata tab passes the full `seType` and `clusterVar` to `generateStataScript()`.
**Invariants:** None directly at risk; stataScript.js is pure export code with no React or IndexedDB dependencies.

---

### [medium] · Export · 2026-07-05
**Feedback:** "Adjust the size of LaTeX tables when the number of columns increases"
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer()` currently wraps the table in a bare `\begin{tabular}` with no width guard. When a model comparison has many columns, the table overflows the page margin. Wrap the entire tabular environment in `\resizebox{\textwidth}{!}{...}` (from the `graphicx` package, which is nearly universal) when the column count exceeds a threshold (e.g. > 5 model columns). Add `\usepackage{graphicx}` to the preamble if not already present. Guard this with a `narrowMode` flag so single-model exports remain unchanged (no unnecessary wrapping).
**Invariants:** None — pure LaTeX string generation.

---

### [medium] · Model · 2026-06-10
**Feedback:** "Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems"
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Per BugTriage, this is **code-complete** (`35f4b5a6`): the viewport has a native vertical resize and bounded responsive canvas for PNG capture. Browser validation is pending Franco. No further code change needed — priority is Franco confirming the fix.
**Invariants:** None.

---

### [medium] · Model · 2026-06-11
**Feedback:** "Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS"
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Per BugTriage, this is **code-complete** (`35f4b5a6`): `YFittedPlot`/`ResidualVsFitted`/`QQPlot` are wired and now rendered from the frozen `result.spec` so sidebar edits cannot mutate the displayed model. Browser validation is pending Franco. No further code change needed.
**Invariants:** None.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** "Heatmap must be fixed"
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Per BugTriage, this is **code-complete** (`5e105529`): heatmap restored as a Leaflet layer with KDE cells, palette/opacity controls, legend, HTML export, and 5k-point cap. Browser validation is pending Franco.
**Invariants:** None.

---

### [medium] · Model · 2026-06-14
**Feedback:** "Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models."
**File:** `src/components/ModelingTab.jsx`
**Suggestion:** Per BugTriage, this is **code-complete** (`35f4b5a6`): OLS/WLS display now frozen to `result.spec`. Browser validation is pending Franco.
**Invariants:** None.

---

### [medium] · Model · 2026-07-09
**Feedback:** "Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable" and "Add also FE with interactions"
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Per BugTriage, both items are **likely fixed** in `045f5cda` (#104): N-way FE accepting any categorical columns was shipped. Status needs **Franco confirmation** before closing. If confirmed, mark both BugTriage rows as `fixed`. If not confirmed, investigate whether `ModelConfiguration.jsx` exposes arbitrary categorical columns as FE dimensions.
**Invariants:** N-way FE uses `runFEMulti`/`demeanByFE` in `PanelWithinEngine.js` — changes must stay in `src/math/`, no React imports.

---

### [medium] · Data · 2026-05-10
**Feedback:** "Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47..."
**File:** `src/DataStudio.jsx` (CSV parse path), `src/services/data/parsers/`
**Suggestion:** A single malformed row currently aborts the entire CSV import with no recovery option. Add a `skipErrors: true` / resilient mode where DuckDB or the JS parser skips unparseable rows, logs them by line number in a `parseWarnings` array, and surfaces them as a dismissible banner after import (e.g. "3 rows skipped — line 1583, 1602, 1814"). Per BugTriage this is flagged `fix-later` (post-launch). BugTriage note: "retain original row number and offer skip/repair preview."
**Invariants:** DataStudio must not mutate `rawData` in place — any recovered data should go through the existing parse→register→preview pipeline.

---

## Features

### Model · 2026-07-06
**Feedback:** "Add vector for running more than 1 hypothesis and save them to replicate in R"
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already computes Wald tests one at a time. Extend `CoefficientTestPanel.jsx` to maintain an ordered list of named hypothesis rows (each row: constraint string + test type + result), rendered as a small table. A "Copy all to R" button should emit a block of `linearHypothesis(fit, c("β1 = 0", "β2 = β3"))` calls from `car` — one per saved hypothesis. Persist the list in component state (lost on unmount) or in the model's pinned spec for replication export. Add to `ModelHypothesis.js` a `serializeHypotheses(list)` helper that the R/Stata/Python exporters can consume.
**Invariants:** `ModelHypothesis.js` is in `src/math/` — keep it React-free. Any UI state belongs in `CoefficientTestPanel.jsx`.

---

### Model · 2026-07-17
**Feedback:** "Add chi-square for linearhypothesis"
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already computes `chiSq` and `chiSqPval` via `pchisq`. The engine output likely already contains both statistics, but `CoefficientTestPanel.jsx` may display only the F/t variant. Audit what `CoefficientTestPanel` renders from the hypothesis result object and add a χ² row alongside the F-stat row (or offer a toggle between F and χ² — both are valid for Wald tests, χ² being the large-n asymptotic variant). The math requires no change; only the UI result display needs updating.
**Invariants:** None beyond the no-React-in-math-files rule (already satisfied).

---

### Explore · 2026-07-09
**Feedback:** "Add labels for the Time series plot, title, axis, etc."
**File:** `src/ExplorerModule.jsx` (`TimeSeriesTab` component ~line 1291)
**Suggestion:** `TimeSeriesTab` has no title, x-label, or y-label inputs. Add three optional text inputs (Title, X axis, Y axis) below the existing controls, stored in component state. Pass them to the SVG rendering block as `text` elements: title centered above the chart, x-label below the x-axis, y-label rotated left of the y-axis. Also store them in the pinned recipe so `exploreStatScript.js` can emit `ggplot2::labs(title=..., x=..., y=...)` — the `rLabs()` helper in `exploreStatScript.js` already reads `p.title`/`p.xLabel`/`p.yLabel` from the pin params.
**Invariants:** None.

---

### Model · 2026-07-09
**Feedback:** "Add direct elasticity interpretation"
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)
**Suggestion:** The prompt already covers log-log (β = elasticity), log-level (β×100 = semi-elasticity), and level-log (β/100) functional forms. What likely remains is: (a) a "compute point elasticity at the mean" UI control for level-level models (elasticity = β × x̄ / ȳ), which can be added as a read-only display in the result panel using `result.meanX` and `result.meanY` — these would need to be computed in the estimation engines; (b) ensuring the AI narrative calls these out explicitly when the user has a level-level model and both variables are measured in natural units. Start with (b) by adding a LEVEL-LEVEL elasticity hint to `INTERPRET_REGRESSION_PROMPT` (compute point elasticity = β × mean(X)/mean(Y) and name it).
**Invariants:** All AI calls go through `AIService.js` — prompt changes only in `Prompts/index.js`. Engine-side mean computation (for (a)) belongs in `src/math/LinearEngine.js`, no React.

---

### Model · 2026-07-09
**Feedback:** "Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster"
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small text `<input>` at the top of the variable chip list that filters visible chips by substring match on column name. Store filter text in local state; the chip list renders `headers.filter(h => h.toLowerCase().includes(q))`. No backend change needed — pure UI filter on the existing headers prop. Clear the filter on dataset change.
**Invariants:** Inline styles via `C` — no CSS files.

---

### Explore · 2026-07-11
**Feedback:** "Add more labels in tables or boxes when plotting by color"
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set in PlotBuilder, the legend shows color swatches but no value labels inside the plot (e.g. data labels or annotated group names on the lines). Add an optional `showGroupLabels` toggle in the Labels panel: when on, render the last-point label for each series directly on the chart (right-aligned near the final x-value for line charts, inside bars for bar charts) using `Plot.text` marks. In ExplorerModule's `SvgBarChart` / `SvgTimeSeries`, when `grpCol` is set, add a series legend row below the chart listing each group's color swatch and label.
**Invariants:** None.

---

### Clean · 2026-07-17
**Feedback:** "Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step"
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** History.jsx renders the pipeline sidebar. Each step has a `type` from the registry. Add a `onJumpToStep(step)` callback from `WranglingModule.jsx` that (a) reads the step's registry category (`cleaning`, `features`, `reshape`, `merge`) to determine which tab to activate, and (b) scrolls/focuses the relevant subsection within that tab. For example, a `log` step (category `features`) should switch to the Feature tab and expand the log/sqrt transforms section. Store a `lastJumpedStep` in WranglingModule state to highlight the row. The step-type→tab-section mapping can live in `pipeline/registry.js` as an optional `uiSection` field.
**Invariants:** No React in `src/pipeline/` — the mapping is metadata only; all React state lives in WranglingModule/History.

---

### Clean · 2026-07-19
**Feedback:** "Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform"
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step in `runner.js` with fields `col`, `fromFormat` (`"name"|"iso2"|"iso3"`), `toFormat`, and `outputCol`. Bundle a static lookup table (~250 rows: ISO 3166-1 alpha-2, alpha-3, English name) as `src/core/data/countryCodes.js` (pure JS, no fetch). Wire a new UI section in `FeatureTab.jsx` with From/To format dropdowns and column selector. Add the registry entry and a translator in `stepTranslators.js` (R: `countrycode::countrycode(df$col, origin=..., destination=...)`, Python: `pycountry`, Stata: `kountry`). Add it to the NL catalogue exclusion list if it shouldn't be AI-suggested yet.
**Invariants:** `runner.js` must replay on `rawData` — the lookup table must be deterministic (no fetch). No React in `src/core/data/`.

---

### Calculate · 2026-07-04
**Feedback:** "Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)"
**File:** `src/components/calculate/workbench/WorkbenchCanvas.jsx`, `src/components/calculate/workbench/EquationsPanel.jsx`
**Suggestion:** A constant `k = 5` evaluated in the workbench should produce a horizontal line at y=5 when its plot checkbox is toggled. The canvas already iterates active equations and renders SVG polylines. For an equation whose RHS evaluates to a finite number (not a function of x), render it as a dashed horizontal rule spanning the x-domain rather than iterating x-values. Check `typeof result === "number" && isFinite(result)` in the plotting loop. Add a label at the right edge of the line ("k = 5").
**Invariants:** Inline styles only via `C`. No external plotting library — this uses the existing SVG canvas.

---

### Calculate · 2026-06-19
**Feedback:** "Add limits"
**File:** `src/components/tabs/CalculateTab.jsx`, `src/components/calculate/workbench/operations.js`
**Suggestion:** Add a `limit` operation to `operations.js`: given an expression `f(x)` and a target `x → a` (including `±Infinity`), evaluate numerically by sampling `f(a ± ε)` for decreasing ε (e.g. `[1e-4, 1e-6, 1e-8]`) and check convergence. If both sides converge to the same value, return it; otherwise flag left/right limits separately. Expose via a "Limit" section in CalculateTab (expression field, approach-point field, direction toggle). LaTeX output: `\lim_{x \to a} f(x) = L`. Note: symbolic limits require a CAS — numeric limits are feasible immediately.
**Invariants:** No React in `src/core/` — put limit logic in `operations.js` or a new `src/core/calculus/limits.js`.

---

### Model · 2026-05-21
**Feedback:** "Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression"
**File:** `src/components/modeling/results/` (no dedicated RDD result component exists), `src/math/CausalEngine.js`
**Suggestion:** `CausalEngine.runSharpRDD` returns coefficients, SE, bandwidth, n left/right, and the kernel. Format these into an rdrobust-style summary block within the existing RDD result display: show a two-row table (Conventional / Robust) with columns `Coef`, `Std. Err.`, `z`, `P>|z|`, `[95% CI]`, and below it a metadata block (`Bandwidth: h=..., N(left)=..., N(right)=..., Kernel: Triangular, Poly order: p=...`). Add a `RDDSummaryTable` component alongside the existing result display in `ModelingTab.jsx` (~line 2620) for the `SharpRDD` result type.
**Invariants:** Display-only — no math changes. Inline styles via `C`.

---

### Explore · 2026-05-19
**Feedback:** "Add filter(regionname %in% c('Spain', 'Basque Country')) for filter option"
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** The filter UI already supports `in` and `nin` operators (mapped from `OPERATORS` in `predicate.js`). The gap is that the `FilterBuilder` UI in `CleanTab.jsx` may not surface a multi-value input for the `in` operator — users need a tag-input or comma-separated text field to enter multiple values. If the operator is `in`/`nin`, render a tag-input component that splits on comma, trims whitespace, and stores as an array in `step.values`. The runner's `evalPredicate` already handles arrays for `in`/`nin`. Also add the `predicateToSQL` support if not already there for the array case.
**Invariants:** `predicate.js` is the single owner of condition logic — do not re-implement filter operators in `CleanTab.jsx`. Only UI wiring goes in the component.

---

### Spatial · 2026-05-30
**Feedback:** "Allow spatial filters for plots"
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** Add a row-filter control (bounding box or column-value predicate) to the spatial plot and map tabs. The Leaflet map tab can offer a "Show only points within current viewport" toggle using `map.getBounds().contains(latlng)`. The SVG plot tab can expose a column/value dropdown (same `FilterBuilder` atom from `atoms.jsx`) that filters `rows` before passing to the layer renderer. Both should be stateless (no pipeline step) — they only affect the current view.
**Invariants:** No pipeline mutation — spatial filters in the plot tab must not write a pipeline step. If the user wants a persistent filter, they should use Clean → Filter.

---

### Model · 2026-06-06
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/export/latexTable.js`
**Suggestion:** Add an optional `labelMap: Record<string, string>` field to the pinned model spec. In `resultDisplay.jsx` expose a small ✎ icon next to each coefficient row that opens an inline edit for its display label; the edited label is stored in `labelMap`. Thread `labelMap` into `buildStargazer()` so the LaTeX table uses the user labels instead of raw column names. Also apply `labelMap` in the R/Stata/Python exporters for variable display in result tables.
**Invariants:** `labelMap` must be part of the model spec stored in IndexedDB (not localStorage). Inline styles via `C`.

---

### General · 2026-07-05
**Feedback:** "Add plots with 3 axis"
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 (used by PlotBuilder) supports a second Y axis via `y2` channel on specific marks (e.g. `Plot.lineY(..., {y: "col1"})` + `Plot.lineY(..., {y2: "col2"})`). Add a secondary Y-axis option in the PlotBuilder layer panel (a toggle per layer "Use right Y axis") that routes that layer's Y aesthetic to `y2`. The `Plot.plot()` call needs `y2: { ... }` scale config. A true third Z axis would require a non-Observable-Plot approach; start with dual Y only and document the limitation.
**Invariants:** PlotBuilder uses Observable Plot via CDN — no npm install. No external UI libraries.

---

### General · 2026-07-19
**Feedback:** "Think about how to construct lists, save them and use them during the session when filtering or constructing variables — cross-module feature"
**File:** `src/services/session/sessionState.jsx`, `src/services/Persistence/indexedDB.js`
**Suggestion:** This is a design-first feature. Proposed shape: a `sessionLists` map in `sessionState.jsx` (name → string[]) available to Clean filter `in`/`nin` and Feature `case_when` expressions. Expose a "Lists" panel in the DatasetManager or a new sidebar pane. Persist lists in IndexedDB under the project ID. Before implementation, Franco should confirm the primary use case (filter shortlist vs. expression variable list) to scope the UI surface.
**Invariants:** Persist in `indexedDB.js` (not localStorage). No list-resolution logic in `src/math/` or `src/core/`.

---

## UX / Design

### General · 2026-07-17
**Feedback:** "Guide tour must be updated"
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array, ~line 11)
**Suggestion:** The tour step for "Model" still says "14 estimators" and lists OLS/WLS/FE/FD/TWFE/2×2 DiD/2SLS/Sharp RDD/Logit/Probit/GMM/LIML/SC — missing Sun-Abraham, Callaway-Sant'Anna, N-way FE, and Poisson FE. The "Report" step doesn't mention replication bundle or the multi-dataset workspace script. The "Calculate" step doesn't mention the Stata workbench. Per CLAUDE.md working conventions: never write a count ("14 estimators") — name the things instead. Update all 9 steps to reflect the current feature set (add a Callaway/Sun-Abraham step or fold into the DiD mention, update Calculate to mention limits/derivatives/Stata, update Spatial to mention pipeline integration).
**Invariants:** `TOUR_STEPS` is a plain JS array in a React file — no architectural constraints. Never write a count into help copy.

---

### General · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time"
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` already manages a global ordered list of artifact IDs. Extend the schema to include an optional `groupId` per artifact and a `groups` list (name, color, order). In `ModelBufferBar.jsx` add a "New group" button and a drag-handle to move pinned models between groups. Groups collapse/expand to reduce visual noise. Persist group assignments in IndexedDB via `saveArtifactOrder`. No cross-module coordination needed — the buffer bar already owns the artifact list.
**Invariants:** Persist in IndexedDB, not localStorage.

---

### Clean · 2026-07-17
**Feedback:** "Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases"
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** When the user navigates away from a partially-filled transform form (e.g. a `case_when` with 2 of 5 cases written), the state is lost on unmount. Lift the in-progress form state up to `WranglingModule.jsx` (or to a `useRef` that survives tab switches) so it survives Clean↔Feature tab navigation. Key it by step type (`case_when`, `add_column`, `mutate`). This is purely state-management — the in-progress draft is NOT added to the pipeline until the user clicks "Add step."
**Invariants:** No mutation of `rawData` for in-progress drafts — only the final "Add step" call touches the pipeline.

---

### Data · 2026-07-17
**Feedback:** "Add arrange functions in the data viewer"
**File:** `src/ExplorerModule.jsx` (DataViewer / data table component)
**Suggestion:** The data viewer renders rows in current pipeline order with no sort control. Add a clickable column header that cycles through ascending → descending → unsorted. For DuckDB-backed datasets, issue `SELECT ... ORDER BY col ASC/DESC LIMIT 500` via `getTablePage`'s existing SQL path (it already has an `ORDER BY` parameter per its own comment). For JS datasets, sort `filteredRows` in memory. Store sort state (`{col, dir}`) in component state — not a pipeline step (this is a view-only sort, same as the existing filter banner pattern).
**Invariants:** View-only sort must not write a pipeline `arrange` step. If the user wants persistent sort, they use Clean → Arrange.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** "Big datasets generate crashes (21MB) over 900k rows"
**File:** `src/DataStudio.jsx` (parseFile DuckDB pipeline), `src/services/data/duckdb.js`
**Suggestion:** Per CLAUDE.md and BugTriage, the Fase X4 DuckDB-OPFS path is code-complete but awaiting Franco's browser benchmark. DuckDB already handles large CSVs (>10MB → Arrow-backed table → 500-row preview → OPFS Parquet cache). If crashes still occur, the likely cause is (a) the OPFS write failing silently (see the `extensions.duckdb.org` connect-src CSP bug, which blocks the parquet extension — confirm `vercel.json` `connect-src` includes `extensions.duckdb.org`), or (b) the `.rds`/`.xlsx`/`.shp` parser branches that do not yet route through DuckDB and allocate the full object graph in the JS heap. Priority: run a browser profile on Vercel with a 21MB CSV and a 900k-row `.dta` file and confirm whether the crash is in the parse step or the render step.
**Invariants:** Data must never leave the browser unless cloud sync is opted-in. No server-side computation. DuckDB results must stay as Arrow/DuckDB table references until display time.

---

## Suggested action order

1. **Fix Callaway-Sant'Anna replication code** — add `csEstMethod`/`csAnticipation`/`csBasePeriod` to the `wrapResult` spec in `estimationDispatch.js` and verify all three language exporters read them correctly; this is a targeted 3-file change with high user impact.
2. **Fix plot replication code missing load lines** — thread `filename`/`dataLoadOpts` into `transpileExploreStat()` and prepend `buildRLoadLine()` / equivalents; reuse existing `loadLine.js` — affects every pinned Explore plot export.
3. **Browser-validate the 4 code-complete items** — PlotBuilder resize/PNG (`35f4b5a6`), OLS residual plots (`35f4b5a6`), spatial heatmap (`5e105529`), coefficient-plot freeze (`35f4b5a6`) — these are done in code but unconfirmed in the browser; one Franco session closes all four.
4. **group_summarize cross-group arithmetic** — add `fn:"expr"` aggregate type to runner.js so users can compute ATE as `mean_treatment - mean_control` in one pipeline step; unblocks a core causal-inference workflow.
5. **Time series plot labels** — add title/axis inputs to `TimeSeriesTab` and wire them to `exploreStatScript.js`'s existing `rLabs()` helper; small change, high usability gain for time-series-heavy workflows.
6. **Guide tour update** — update TOUR_STEPS in `HelpSystem.jsx` to name all current estimators (Sun-Abraham, Callaway, Poisson FE), remove count strings, add Stata workbench mention; low effort, high first-impression impact for new users.
7. **Expose chi-square result in CoefficientTestPanel** — `ModelHypothesis.js` already computes it; just surface `chiSq`/`chiSqPval` in the UI alongside the F-stat.
8. **Stata workbench audit** — systematically compare R and Stata export branches for each estimator × SE type combination; fix any branch that emits empty string or wrong syntax.
