# Feedback Analysis Report — 2026-07-27

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **13 open bugs** · **10 open features** · **2 open UX** · **1 open performance** · **1 open other**

Cross-referenced against BugTriage.md. Items marked "code-complete, browser validation pending" are counted as open because Franco has not confirmed them. Items explicitly marked `fixed` or ✓ in both files are excluded.

---

## Bugs

### [high] · export · 2026-07-05
**Feedback:** `Replication code for CS DiD 2021 does not work`
**File:** `src/services/export/rScript.js`, `src/math/CallawayEngine.js`
**Suggestion:** The Callaway & Sant'Anna engine (`CallawayEngine.js`) is implemented but BugTriage confirms its R export is broken. Audit the `transpileModel` call site for the `CS_DiD` estimator in `rScript.js` — check whether `callawayATTs`, `callawayAggregation`, and the `did` package call (`att_gt` + `aggte`) are correctly serialized. Also verify that `generateSubsetRScript` and the multi-model path pass the CS DiD spec through the export. Cross-check the emitted R against the `did` package's `att_gt(...)` signature (group/time column names must match the spec).
**Invariants:** All Anthropic API calls must go through `AIService.js`. Math files (`src/math/`) must have zero React imports.

---

### [high] · export · 2026-07-05
**Feedback:** `Replication codes of the plots must have the lines to load datasets as well`
**File:** `src/components/PlotBuilder.jsx` (mounted from `src/ExplorerModule.jsx` line 2631)
**Suggestion:** `PlotBuilder` accepts an optional `scriptPreamble` prop that, when provided, prepends load code to exported R/Python/Stata scripts. However, `ExplorerModule.jsx` mounts `PlotBuilder` at line 2631 without passing `scriptPreamble`. Add a `scriptPreamble` callback to the Explorer's `PlotBuilder` invocation that calls `buildRLoadLine` / `buildPyLoadLine` / `buildStataLoadLine` (already in `src/services/export/loadLine.js`) for the active dataset. Thread `datasetName` (already passed) and `loadOpts` through from the session state to make the load call format-aware (`.dta` → `haven::read_dta`, etc.).
**Invariants:** None — pure export/UI change, no math or persistence layers touched.

---

### [high] · export · 2026-07-17
**Feedback:** `Stata workbench doesn't work completely`
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** BugTriage lists this as open with no diagnosis. Start by auditing `stataScript.js` for estimator branches that either throw or emit syntactically invalid Stata — recent changes (HC2/HC3, two-way cluster, Driscoll-Kraay HAC) added significant complexity and several edge notes (e.g. `xtreg` with two cluster variables was documented as a known Stata syntax error routed to `reghdfe`). Run the 168-cell SE-type matrix smoke test and check for any `undefined` or error-throwing branches in the Stata column. Also verify the Callaway & Sant'Anna and Sun-Abraham Stata export stubs — those may emit nothing or a non-runnable comment.
**Invariants:** None — export layer only.

---

### [high] · pipeline · 2026-07-17
**Feedback:** `Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group`
**File:** `src/pipeline/runner.js` (line 528, `group_summarize` case), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** The `group_summarize` runner only supports per-group single-column aggregations (mean, sum, count, etc.). It cannot compute cross-group derived stats like ATE = mean(y | treated=1) − mean(y | treated=0) because there is no "difference between two group rows" aggregator. The cleanest fix is a two-step workaround surfaced in the UI: (1) `group_summarize` to produce one row per group with the desired stat, (2) a new `pivot_wider` + `mutate` step to subtract the two rows. Alternatively, add a `"diff_of_groups"` aggregation mode to `group_summarize` that accepts a binary grouping column and emits a single-row difference. Either approach must replay on `rawData` — never mutate the input. The `ReshapeTab.jsx` UI needs to expose whichever option is chosen.
**Invariants:** Pipeline steps must replay on `rawData` (non-destructive). Registry must stay in sync with `runner.js` if a new step type is added.

---

### [high] · ui · 2026-06-11 (code-complete, browser validation pending)
**Feedback:** `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS`
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage marks this code-complete (`35f4b5a6`) — the plots were wired and the frozen-result-state regression was fixed so sidebar edits cannot mutate the displayed model. This item needs Franco's browser validation only. Flag if plots still fail to render after that commit.
**Invariants:** Diagnostic residuals must be computed from the frozen `result.spec` — never from live sidebar state.

---

### [medium] · export · 2026-07-05
**Feedback:** `Adjust the size of LaTeX tables when the number of columns increases`
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** When many regressors or models are compared, the Stargazer-style table overflows. Add a dynamic `\resizebox{\textwidth}{!}{...}` wrapper around the `tabular` environment when the estimated column count exceeds a threshold (e.g. > 6 columns). Alternatively, switch to `\adjustbox{max width=\textwidth}` from the `adjustbox` LaTeX package for safer behavior. The column count is already available from the model's `xCols` and the number of pinned models — check in `ReportingModule.jsx` where `buildStargazer` is called.
**Invariants:** None — export layer only.

---

### [medium] · export · 2026-07-06
**Feedback:** `Add vector for running more than 1 hypothesis and save them to replicate in R`
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` currently runs one hypothesis at a time and does not persist results. Add a "Save test" button that appends the current hypothesis (restriction string + result) to a local list stored in component state (or IndexedDB if persistence across sessions is needed). The R export in `ModelHypothesis.js` already emits `car::linearHypothesis` — extend it to emit a loop or sequential calls for the saved test list. This does not require engine changes.
**Invariants:** Use `indexedDB.js` for persistence if saved tests need to survive page reload — never `localStorage`.

---

### [medium] · math · 2026-07-17
**Feedback:** `Add chi-square for linearhypothesis`
**File:** `src/math/ModelHypothesis.js`
**Suggestion:** `ModelHypothesis.js` currently computes F-statistics for linear restrictions (Wald test). Add a `chi2` variant: χ² = r × F (where r is the number of restrictions), which is asymptotically equivalent and matches `car::linearHypothesis(..., test="Chisq")` in R. Surface it as a toggle chip ("F-stat" / "χ²") in `CoefficientTestPanel.jsx`. The change is purely additive — the underlying matrix algebra (`R β − q`, bread/meat) is identical; only the distribution reference (F(r, df_res) vs χ²(r)) and the label change.
**Invariants:** `src/math/` must have zero React imports — add the math in `ModelHypothesis.js`, toggle in the UI component only.

---

### [medium] · ui · 2026-06-10 (code-complete, browser validation pending)
**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** BugTriage marks code-complete (`35f4b5a6`) — native vertical resize and bounded responsive canvas added. Needs Franco's browser validation. Flag if PNG export still crops or mismatches the visible canvas.
**Invariants:** None.

---

### [medium] · ui · 2026-06-13 (code-complete, browser validation pending)
**Feedback:** `Heatmap must be fixed`
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** BugTriage marks code-complete (`5e105529`) — Leaflet KDE heatmap layer restored with palette/opacity controls, legend, and HTML export. Needs Franco's browser validation.
**Invariants:** None.

---

### [medium] · ui · 2026-07-17
**Feedback:** `Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases`
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` (947 lines) holds all transform sub-panel state in local `useState`. Navigation away triggers a React unmount and wipes the draft. The fix is to lift the per-section draft state (expression string, column name, condition rows for `case_when`) into the parent `WranglingModule.jsx` so it survives tab switches, or use `sessionStorage` keyed by `pid` for lightweight persistence. Avoid `localStorage` per the invariant. The `case_when` editor's condition rows are the most critical since they are the longest to rebuild.
**Invariants:** Use IndexedDB (or `sessionStorage` as a lightweight alternative for transient draft state) — never `localStorage`.

---

### [medium] · ui · 2026-07-17
**Feedback:** `Add arrange functions in the data viewer`
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The data viewer in ExplorerModule renders a paginated table but has no column-header click-to-sort. Add clickable `<th>` elements that set a `{col, dir}` sort state, then pass a sorted slice to the display rows. This is a display-only sort — it must not mutate `rawData` or add a pipeline step. For DuckDB-backed datasets (`rawData._duckdb`), perform the sort via a `SELECT ... ORDER BY col ASC/DESC LIMIT 200` query rather than sorting the JS preview array, to keep the full-table sort correct. Show a sort indicator (↑/↓) on the active column header.
**Invariants:** Never mutate `rawData` in place — display sort only. For DuckDB tables, query via `duckdb.js` singleton.

---

### [medium] · ui · 2026-07-17
**Feedback:** `Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria`
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` manages a flat ordered list of artifact IDs (plots/maps/models). Extend the schema to support named groups: `{ groups: [{ id, label, artifactIds: [...] }] }`. `ModelBufferBar.jsx` would render a group-picker dropdown or collapsible sections. Start small: add "Group" / "Ungroup" actions on pinned models in `ModelBufferBar.jsx` and persist the grouping in `artifactOrder.js` via IndexedDB. Comparison mode only shows artifacts within the same group.
**Invariants:** Persist in `indexedDB.js` — never `localStorage`. All styles must use the `C` object — no external UI libraries.

---

### [low] · ui · 2026-07-17
**Feedback:** `Guide tour must be updated`
**File:** `src/components/HelpSystem.jsx` (constant `TOUR_STEPS`, line 11)
**Suggestion:** Audit `TOUR_STEPS` against the current tab list in `WorkspaceBar.jsx` (7 tabs: Data/Clean/Explore/Model/Simulate/Calculate/Report) plus major features added since the tour was written (Spatial tab, N-way FE, Bacon decomposition, PlotBuilder history, model buffer groups). Add missing steps and retire any that reference removed UI. Keep each step's `target` selector in sync with the actual DOM element it points at.
**Invariants:** None — pure UI copy change.

---

## Features

### Model · 2026-05-11
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx` (weak-IV panel)
**Suggestion:** Anderson-Rubin (AR) CI inverts the test statistic `AR(β₀) = (y − X·β₀)'P_Z(y − X·β₀) / (k_z·σ̂²)` over a grid of `β₀` values to find the set where the null is not rejected at the chosen α level. BugTriage notes `CausalEngine.andersonRubinCI` already exists. Verify it is wired into the IV results display in `ModelPlots.jsx` (the weak-IV panel) and exposed alongside the standard Wald CI. If not wired, add a collapsible "AR Confidence Interval" row in the IV result table.
**Invariants:** Zero React imports in `src/math/`.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** The static geo-plot (SVG/Observable Plot) already supports point and polygon layers. Add a "Grid" layer type that reads pre-computed grid columns (`grid_id`, `lat_idx`, `lon_idx`) from the dataset — these are already produced by the `sp_grid_assign` pipeline step — and renders them as SVG rectangles. Reuse `src/components/tabs/spatial/plot/geo.js`'s existing layer machinery; add a `mkGeoLayer` case for `"grid"` that projects rectangle corners via the existing tile-math helpers in `leaflet.js`.
**Invariants:** No React imports in math files; styling via the `C` object.

---

### Calculate · 2026-06-19
**Feedback:** `Add limits`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limit" mode to the Calculate workbench (alongside the existing Derivative, Integral, Optimize modes) that calls the CAS already wired for derivatives. Most browser CAS libraries (math.js, nerdamer) support `lim(f, x, a)`. Surface it as: expression input, variable picker, approach value, direction toggle (→ / →⁻ / →⁺), and evaluate button. Output the symbolic limit and its numeric approximation.
**Invariants:** No external UI libraries — inline styles via `C`. No CSS files.

---

### Calculate · 2026-07-04
**Feedback:** `Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add an explicit "Add constant" layer type in the equation list that inserts a `{ type: "const", value: 0, label: "", color }` entry and renders it as a dashed horizontal line on the canvas. This is simpler and more predictable than auto-detecting flat functions. No engine changes needed.
**Invariants:** None.

---

### Model · 2026-07-09
**Feedback:** `Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster`
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small text input above the variable chip list in `VariableSelector.jsx` that filters chips by substring match. The chip list already maps `headers` to `Chip` components — wrap the map in a `.filter(h => h.toLowerCase().includes(query))` driven by the search input state. No backend change needed.
**Invariants:** Inline styles via `C` — no CSS or external UI library.

---

### Explore · 2026-07-11
**Feedback:** `Add more labels in tables or boxes when plotting by color`
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** When `aes.color` is set, Observable Plot's default legend shows only a color swatch next to the value. Add a `showColorLabels` toggle that, when on, renders value labels directly on/beside each series using Plot's `text` mark positioned at the last data point of each series. For categorical bar plots in `ExplorerModule`, add value counts or percentages inside/above each bar segment.
**Invariants:** None.

---

### Explore · 2026-07-09
**Feedback:** `Add labels for the Time series plot, title, axis, etc.`
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** `PlotBuilder` already has `title`, `xLabel`, and `yLabel` state wired to Observable Plot's `title`/`x.label`/`y.label` options (visible from the Labels panel). If the time-series plots in `ExplorerModule` are rendered outside PlotBuilder (custom SVG or ACF/PACF paths), those need their own label inputs — add a label section mirroring the PlotBuilder pattern.
**Invariants:** None.

---

### Clean · 2026-07-17
**Feedback:** `Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step`
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Add a "Jump to" icon on each step in `History.jsx` that, when clicked, (1) activates the corresponding tab (e.g. `group_summarize` → Reshape tab) and (2) pre-selects the step for editing. Emit a `onJumpToStep(step)` callback from `History.jsx` and handle it in `WranglingModule.jsx` by setting the active tab. A step-type → tab mapping is straightforward from the step categories in `runner.js`.
**Invariants:** None.

---

### Clean · 2026-07-19
**Feedback:** `Add a function for Country Codes — iso2/iso3/country-name crosswalk transform`
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Add a `country_code` pipeline step backed by a small static lookup table (ISO 3166-1 α-2 / α-3 / country name, ~249 entries). Spec: `{ type: "country_code", col, from: "name"|"iso2"|"iso3", to: "name"|"iso2"|"iso3", newCol }`. Runner implementation is a simple map lookup — no CDN dependency. Surface it in `FeatureTab.jsx` under the Formats section. Registry must be updated in sync with runner (enforced by `pipelineReliabilityValidation.mjs`).
**Invariants:** Pipeline steps must replay on `rawData` — no in-place mutation. Registry must stay in sync with `runner.js`.

---

### General · 2026-07-19
**Feedback:** `Think about how to construct lists, save them and use them during the session when filtering or constructing variables`
**File:** `src/services/session/sessionState.jsx` (natural home for session-scoped lists)
**Suggestion:** Design a session-scoped named-list store in `sessionState.jsx`: `{ lists: { [name]: string[] } }` with `SET_LIST` / `DELETE_LIST` actions. Expose a `ListPicker` atom in `wrangling/atoms.jsx` that lets filter and `case_when` expressions reference a stored list by name. Persist via IndexedDB under the project key so lists survive reload. This is a design-first feature — agree on scope (session vs project lists) with Franco before implementing.
**Invariants:** Persist in `indexedDB.js` — never `localStorage`. `sessionState.jsx` is React Context — zero math imports.

---

## UX / Design

### Model · 2026-06-06 (fix-later)
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** File not located precisely — likely `src/components/ModelingTab.jsx` or `src/services/export/latexTable.js` (look for hardcoded "D (treatment)" string)
**Suggestion:** Add a `labelMap: { [colName]: string }` field to the model spec that overrides display names in the LaTeX table. Surface it as an editable grid in the "Report" tab or a small "Edit labels" popover in `ModelComparison.jsx`. Thread `labelMap` through `buildStargazer`. RDD's hardcoded "D (treatment)" label should derive from the actual column name in `CausalEngine.js`.
**Invariants:** None.

---

### Model · 2026-07-09
**Feedback:** `Add direct elasticity interpretation`
**File:** `src/services/AI/Prompts/index.js` (line 99, INTERPRET_REGRESSION_PROMPT); UI display in `src/components/ModelingTab.jsx`
**Suggestion:** The AI prompt already includes log-log elasticity and semi-elasticity rules (lines 99, 353–354). The feedback likely requests a non-AI UI display: show a computed elasticity value directly in the coefficient table when the spec is log-log (both Y and X are logged). In `ModelingTab.jsx`, read `spec.xCols` for `log_` prefix and `spec.yCol` for `log_`, compute the point elasticity (= coefficient for log-log), and display it in a dedicated table column alongside the β. A simple scalar computation — no engine change needed.
**Invariants:** `SE type` must be passed explicitly if computing elasticity SE via delta method. No hardcoded SE type inside engine or display functions.

---

## Performance

### Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** BugTriage routes this to "Fase X4 code-complete; browser benchmark pending Franco." The DuckDB OPFS Parquet cache path is implemented and CSP has been patched to include `extensions.duckdb.org` in `connect-src`. No further code changes are suggested — the outstanding action is Franco's browser benchmark with a 900k-row file to confirm the 15-second tab-transition lag is resolved. Flag any new crash reports at this size after the benchmark.
**Invariants:** Data never leaves the browser — no server-side computation. Display limited to 200–500 rows; computation always runs on the full DuckDB table.

---

## Other

### Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (CSV parse path), `src/services/data/parsers/`
**Suggestion:** A single malformed or over-long row is aborting the entire CSV import. BugTriage triages this as "resilient-import UX after launch" (fix-later). Suggested fix: wrap the per-row parse loop in a try/catch that collects bad rows with their line numbers and continues, then surfaces a recoverable warning panel ("N rows skipped — download problem rows") instead of a fatal parse error. Additive to the existing parser; does not affect the non-destructive pipeline invariant.
**Invariants:** None — parser is outside the pipeline replay chain.

---

## Items needing browser validation from Franco

The following are code-complete but await Franco's browser confirmation before they can be closed:

| Item | Commit | What to test |
|------|--------|-------------|
| Plot Builder resize + PNG export | `35f4b5a6` | Resize the ModelingTab plot builder vertically; export PNG and confirm it matches the visible canvas size |
| OLS residual/diagnostic plots | `35f4b5a6` | Run OLS, add/remove variables, verify residual plots stay frozen until re-estimation |
| Spatial heatmap | `5e105529` | Open Map tab in Spatial, add a heatmap layer, confirm KDE rendering, palette, and HTML export |
| N-way FE categorical variables + interactions | `045f5cda` (#104) | Estimate FE with `country`, `sector` as FE dimensions; also estimate with an interaction FE |
| Confirm popovers on destructive actions | — | Click "Clear all pipeline", "Sign out", "Delete project" and verify confirmation dialogs appear |
| HC2/HC3 + seType in multi-model scripts | — | Estimate OLS with HC2, export R script; confirm `sandwich::vcovHC(...,"HC2")` appears |

---

## Suggested action order

1. **Fix CS DiD (Callaway & Sant'Anna) replication code** (`rScript.js`) — research users actively replicating PS6/PS7 results hit this immediately; the engine is complete but the export is broken.
2. **Fix plot replication load lines** — add `scriptPreamble` with `buildRLoadLine` in `ExplorerModule.jsx`'s `PlotBuilder` invocation (line 2631); one-line fix with high reproducibility impact.
3. **Diagnose and fix Stata workbench** — audit `stataScript.js` against the 168-cell SE-type matrix; Stata is the primary tool for LMU institutional users.
4. **Fix group_summarize cross-group math (ATE)** — core pipeline gap blocking common DiD and RCT workflows; add either a `diff_of_groups` aggregation or a documented two-step pattern exposed in `ReshapeTab.jsx`.
5. **Browser-validate code-complete items** — OLS residual plots, Plot Builder resize, and spatial heatmap are code-complete (`35f4b5a6`, `5e105529`); validate before they age further.
6. **Add chi-square to linear hypothesis tests** — small math addition to `ModelHypothesis.js`; unblocks PS1-style Wald vs chi-square comparison exercises.
7. **Add Anderson-Rubin CI display** — verify `CausalEngine.andersonRubinCI` is wired to the IV weak-instrument panel in `ModelPlots.jsx`.
8. **Add arrange/sort to data viewer** — medium effort, high daily-use impact for all users browsing large datasets.
