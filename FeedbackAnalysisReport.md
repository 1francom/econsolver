# Feedback Analysis Report — 2026-09-15

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **11 open bugs** · **12 open features** · **4 open UX / Design** · **1 open performance** · **2 open other**
- Total: 30 open items analysed across ClaudeFB.md and BugTriage.md
- 3 items marked "likely fixed — needs Franco confirmation" (`p-values`, `N-way FE categorical`, `FE interactions`)
- 1 BugTriage conflict found: "estimate without intercept" is `fix-later` in BugTriage but `DONE 2026-07-18` in CLAUDE.md — BugTriage needs manual status update

---

## Bugs

### [critical] · Model · 2026-07-22 / 2026-07-28
**Feedback:** P-values with small numbers — t-value near zero reported as `p < 0.001 ***`; `beta=0.0001, se=0.002 cannot give p<0.001`; OLS p-values wrong for insignificant estimates.
**File:** `src/math/LinearEngine.js` (`tCDF`, `betainc`, `betacf`)
**Suggestion:** Commit `216b1ecb` is believed to fix this via Numerical Recipes' continued-fraction `betainc` (Lentz's method). Franco has not browser-confirmed the fix yet — close this once he verifies a t-stat near zero shows `p ≈ 1` on a large-n regression. Duplicate BugTriage rows 78/84/85 should be merged and closed at the same time.
**Invariants:** Math-only fix in `src/math/` — no React allowed. Verify `calcEngine.js` `_incompleteBeta` does NOT share the old Riemann-sum body (it had an independent copy; BugTriage row 73 says it was fixed on 2026-07-30 alongside `waldTest`).

---

### [high] · Calculate / Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely.
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** The most likely root cause is that `stataScript.js` silently uses wrong or missing field names for certain estimator specs (the CS DiD fix on 2026-07-30 found exactly this pattern). Audit every estimator branch in `stataScript.js` against what `estimationDispatch.js` actually puts on `spec`, and run the 168-cell SE-type matrix validation (`seTolerances.js`) against the Stata column to confirm no column emits identical output across all SE types. Also check `CodeEditor.jsx`'s field whitelist for any Stata-specific fields still absent.
**Invariants:** All Anthropic calls go through `AIService.js` — never add raw `fetch` to Anthropic API inside `CodeEditor`. Stata emitters that have no native equivalent must emit a `NOTE:` comment rather than silently dropping the option.

---

### [high] · Model / Math · 2026-07-09
**Feedback:** FE must accept any categorical variable (country, state, sector), not just entity/time.
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage says "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation." Before closing, verify that the UI in `ModelConfiguration.jsx` allows a user to pick arbitrary column names (not just columns previously declared as entityCol/timeCol in `PanelTab`) as FE dimensions in the multi-way FE path. If the column picker still filters to panel-declared columns only, add a second "any column" FE picker.
**Invariants:** Engine (`src/math/`) must not import React. SE type must be passed explicitly — `runFEMulti` must not assume `"classical"` internally.

---

### [high] · Model / Math · 2026-07-09
**Feedback:** Add also FE with interactions.
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions` step)
**Suggestion:** BugTriage says "likely fixed — FE interactions shipped in `045f5cda`." Confirm via a regression that uses `c × time_fe` interaction (e.g. `female × ggi` inside `runFEMulti`) and compare to R `fixest::feols(y ~ x + x:fe2 | fe1)`. The LSDV singularity diagnosis bug (fixed 2026-09-06) was triggered by exactly this pattern, so also verify the new projection-based absorbed-regressor check works correctly for an interacted FE model.
**Invariants:** Interaction-only terms under no-intercept have a known bug in `expandInteractions` (see CLAUDE.md "Pending #2") — flag to user if their interaction+FE model also has no intercept, rather than silently returning wrong coefficients.

---

### [high] · Simulate / Export · 2026-07-28
**Feedback:** Update the replication script — currently it replicates only the DGP, not downstream analysis (resampling/tests/estimation on the simulated data).
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (script generators)
**Suggestion:** `SimulateTab.jsx` uses `dgpScript.js` expressions for the DGP draw, but the StatWorkspace resampling/SampleTestPanel sections have their own state that is never serialised into a script. Add a `buildSimulateScript()` function (mirror of `generateCleanScript`) that: (1) emits the DGP draw using `dgpScript.js` R/Python/Stata expressions, (2) appends the resampling/bootstrap/Monte Carlo loop body from `StatWorkspace`'s current config, and (3) emits the estimation call if a model was run on simulated data. Wire it to the existing "Copy" button in SimulateTab.
**Invariants:** Pure-JS math in `src/math/dgpDraw.js` and `src/math/dgpScript.js` must not import React. Script output must call `callClaude` only through `AIService.js` if any AI generation is involved.

---

### [medium] · General / Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases.
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** In `buildStargazer()` (and `buildAttgtLatex()`), detect when the number of model columns exceeds ~5 and automatically switch to `\resizebox{\textwidth}{!}{...}` wrapping, or emit `\begin{adjustbox}{max width=\textwidth}`. Also consider emitting `\small` font-size and `\setlength{\tabcolsep}{4pt}` for wide tables. The threshold (column count or total char width) can be a constant at the top of `latexTable.js`.
**Invariants:** Export-only change — no engine math, no React in `src/services/export/`. Must not break the existing 21-cell emitted-output smoke harness.

---

### [medium] · Model / Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R.
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel.jsx` currently runs one hypothesis at a time with no persistence. Add a "Save test" button that appends the current test (restriction string + null vector + result) to a local array in component state (or `sessionState.jsx` if cross-session persistence is required). Render the saved list as a collapsible table below the test form. Wire `generateJointHypothesisScript()` to emit all saved tests as a sequential block in R/Python/Stata. `ModelHypothesis.js` already produces the per-test script snippets — the gap is only accumulation and rendering.
**Invariants:** Persistence must go through `indexedDB.js` if cross-session — never `localStorage` for model/pipeline storage. Each test must carry `seType` explicitly.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis labels, etc.
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** `PlotBuilder.jsx` already has a "Labels" panel with `plotTitle`, `xLabel`, `yLabel` fields wired to Observable Plot's `plot({ caption, x: { label }, y: { label } })`. The time series path in Explore likely bypasses PlotBuilder or overrides its labels. Check `ExplorerModule.jsx`'s ACF/PACF and time series render paths and confirm they read from the same labels state. If they use a bespoke chart component, add a small `<LabelsSection>` using the shared atoms from `src/components/tabs/spatial/shared/atoms.jsx`.
**Invariants:** UI change only — no math files touched. Inline styles via the `C` object from `shared.jsx`.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series plot don't work.
**File:** `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The "group" reference in a time series plot likely means the colour/facet grouping column doesn't carry through to the legend or line colour when the x-axis is a date/time variable. Inspect the `aes.color` → `Plot.lineY({stroke: d => d[aes.color]})` path for the time series geom specifically. A likely culprit is that the time series branch constructs its own `plotData` without forwarding `aes.color`, or that the Observable Plot `color` scale is not defined when `aes.color` is set on a `lineY` (requires explicit `color: { domain, range }` in the plot options).
**Invariants:** UI + PlotBuilder only. No changes to `src/math/` or `src/core/`.

---

### [medium] · Model / AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation.
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)
**Suggestion:** Add a new rule block in `INTERPRET_REGRESSION_PROMPT` for log-log and log-level models. When both Y and X are log-transformed (detectable from variable names or from the spec `pipeline` field containing `log` steps), the coefficient IS the elasticity — interpret it as "a 1% increase in X is associated with a β% change in Y." When only X is logged (log-level), interpret as "(β/100)% change in Y per 1-unit change in X." Derive the rule from the user's pipeline steps carried in `snapshot.pipeline`, which is already injected into the prompt by `serializeSnapshot`.
**Invariants:** All Anthropic calls must go through `callClaude()` in `AIService.js`. Prompt changes must preserve the `SHARED_CONTEXT` cached block and the `anthropic-beta: prompt-caching-2024-07-31` header. Model routing stays at `MODEL_ADVISOR` (Sonnet) for interpretation calls.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, navigating away to check variable names, then returning to continue.
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a per-section draft state saved to `sessionStorage` keyed by `<pid>_<datasetId>_<sectionName>` (mirrors the pattern already used for pipeline column visibility). On mount, restore the draft inputs; on "Add step" or "Cancel", clear the draft. This is purely a UX convenience — it does NOT add a new pipeline step until the user confirms. The draft should survive tab navigation but not a full page reload (sessionStorage is the right scope, not IndexedDB).
**Invariants:** Draft state must NEVER be treated as a committed pipeline step and must not flow into `runner.js`. Styling via `C` object only.

---

### [medium] · General / UX · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps so not everything is compared at the same time.
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend `artifactOrder.js` with a `groupId` field on each artifact entry. Add a "New group" button in `ModelBufferBar.jsx` that creates a named group and assigns subsequently pinned artifacts to it. A group pill in the bar allows toggling visibility of all artifacts in the group at once — only the active group's models flow into `compRows` for the G13 coefficient comparison plot. Persist group assignments via `saveArtifactOrder` (already goes through IndexedDB).
**Invariants:** Persistence goes through `indexedDB.js` — never `localStorage`. Grouping is metadata only and must not change the estimation result data.

---

## Features

### Clean · 2026-06-14
**Feedback:** R-style `%in% c(...)` membership syntax is not accepted by the filter expression UI.
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** `predicate.js` already has an `in` operator (`normalizeOp` maps several strings to `"in"`). The UX gap is in `CleanTab.jsx`'s filter builder — users who type `%in% c("a","b")` are hitting the free-text expression path, not the structured `FilterBuilder`. Either add a parser in `NLCommandBar.jsx`'s preprocessing that rewrites `%in% c(...)` to a structured `{op:"in", value:[...]}` predicate node before dispatch, or add a tooltip in the filter UI documenting the app's own syntax (`values = a, b, c` or the IN chip).
**Invariants:** `predicate.js` is the single owner of all filter operators — never re-implement a filter operator anywhere else. Any new operator normalization goes into `normalizeOp`.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map.
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** `SpatialGeoPlot.jsx` already supports WKT polygon layers. A grid output from the pipeline (rect or H3 cells) is a dataset with WKT geometry — it can be added as an additional layer the same way any polygon dataset is. The missing piece is auto-detecting when a loaded dataset looks like a grid (column named `cell_id` matching the `<latIdx>_<lonIdx>` or `h<res>_…` pattern from `SpatialEngine.js`) and offering it as a layer with a pre-set fill palette. No new math needed.
**Invariants:** `SpatialEngine.js` is in `src/math/` — it must remain React-free. All new UI in `src/components/tabs/spatial/plot/`.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots — filter the displayed features by a column value.
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add a "Filter" chip per layer in `GeoLayerConfig.jsx` that opens an inline predicate builder (reuse `FilterBuilder` from `CleanTab.jsx` — it's already decoupled from the pipeline). The filter produces a `{col, op, value}` predicate applied client-side via `evalPredicate` from `predicate.js` before rows reach the SVG/Leaflet renderer. Do NOT write a custom filter evaluator — use `predicate.js` as the canonical implementation.
**Invariants:** `predicate.js` is the single owner of filter logic. This filter is view-only — it must NOT write a `filter` pipeline step.

---

### Calculate · 2026-06-19
**Feedback:** Add limits (e.g. `lim as x→∞ f(x)`, `lim x→0 sin(x)/x`).
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The Calculate tab already has symbolic differentiation and integration via a CAS. Add a `limit(expr, var, point)` command that uses the same CAS engine (or a new worker call to `exprEvalService.js`) to evaluate limits symbolically, falling back to numerical evaluation (L'Hôpital's-rule iteration or two-sided approach from the left and right). Display the result in the same LaTeX output panel. Add an entry to the Calculate HintBox and export a `limit` command to the LaTeX export path.
**Invariants:** CAS logic must stay in the worker / pure JS — no React in math files. LaTeX export should not bypass `buildStargazer`; add a separate `buildCalcLatex` if needed.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the function graph — draw a horizontal line for a constant value alongside function curves.
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** In the function graph section of `CalculateTab.jsx`, allow entries that are numeric constants (detected by parsing — no variable) to be drawn as `Plot.ruleY([value])` (a horizontal line) rather than a parameterised curve. A constant `c = 3.5` would show as a labelled horizontal rule at y = 3.5, visually matching ggplot's `geom_hline`. No new math needed — this is Observable Plot syntax already used in `PlotBuilder.jsx`.
**Invariants:** UI change only. Styling via `C` object. Use Observable Plot CDN already loaded in the app, not a new library.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axes (3D or secondary y-axis).
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 does not support 3D natively, so a full 3D scatter is out of scope for this library. A more tractable interpretation is a **dual y-axis plot** (secondary axis on the right). Observable Plot's `Plot.plot({ y: ..., fy: ... })` does not support dual axes, but two overlaid marks with different scale transforms can approximate it. The simpler path is to expose a "y2" aesthetic mapped to a second scale, rendered as a separate mark group with right-axis ticks — matching ggplot's `sec.axis`. Evaluate whether the use-case is primarily for time series (common request for dual-axis), and document that true 3D is deferred.
**Invariants:** No external 3D library — CSP allowlist currently blocks unpkg.com and esm.sh. If a CDN library is needed, it must be served from cdnjs.cloudflare.com or cdn.jsdelivr.net.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable selector so users can type and find variables faster.
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a filter `<input>` at the top of `VariableSelector.jsx`'s variable list that filters displayed column names client-side by substring match (case-insensitive). The current filtered list still emits chips; clicking a chip adds it to Y/X/W as before. This is a pure UI change — `filteredHeaders = allHeaders.filter(h => h.includes(searchTerm))` — no engine, no pipeline change.
**Invariants:** Inline styles via `C` object. No external UI library.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color.
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, Observable Plot renders a colour legend automatically for categorical variables (it appears when `color: { legend: true }` is passed to `plot()`). The gap is likely that the legend option is not being forwarded. Additionally, when `showValues` is enabled on a bar or tile geom, colour-by plots should show the group label on each element. Check if `color: { legend: true }` is present in the `plot({ ... })` call when `aes.color` is non-null, and add it if missing.
**Invariants:** PlotBuilder UI only — no `src/math/` changes. Observable Plot 0.6 CDN.

---

### Clean · 2026-07-17
**Feedback:** Click on one pipeline step and navigate to the corresponding subsection automatically.
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` already has a `type` field from `STEP_REGISTRY`. Add a `stepTypeToTab` lookup that maps step categories (Cleaning → "Clean", Features → "Feature", Reshape → "Reshape", Merge → "Merge") to the corresponding tab name, then call `setActiveTab(stepTypeToTab[step.type])` when a history item is clicked. Optionally also scroll the relevant sub-panel into view via a `ref`. This is a UX-only change — no pipeline logic.
**Invariants:** Tab state lives in `WranglingModule.jsx` — the `History` component must signal upward via a callback prop, not import `WranglingModule` state directly. Inline styles via `C`.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — ISO2/ISO3/country-name crosswalk transform.
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a new "Country Code" transform in `FeatureTab.jsx`'s Formatting section. Bundle a small static lookup table (ISO 3166-1 alpha-2 / alpha-3 / official name — ~250 rows, ~15KB JSON) as `src/data/iso3166.json`. The step writes a new column of the target format by looking up each row's value in the table. Register as a new `mutate`-based pipeline step (no new step type needed if it maps cleanly to `add_column` with a lookup expression). Add a NL synonym in `serializeAllowedSteps` so the AI coach can suggest it.
**Invariants:** Static data file belongs in `src/data/`, not `src/math/`. Pipeline steps must replay on `rawData` — the lookup table must be bundled, not fetched at runtime (no network call inside `runner.js`).

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models — arguments equivalent to R and Python commands.
**File:** `src/components/modeling/ModelPlots.jsx`
**Suggestion:** The existing binned scatter (partial plot / AVP) has no controls for bin count, bin width, or confidence interval display. Add a small controls row below the plot with: `bins` slider (10–100), CI toggle (95% / 90% / none), and a "raw scatter" vs "binned scatter" toggle. These map directly to `rdplot`/`binscatter` options that users familiar with R would recognise. The bin computation belongs in a helper function inside the component (not `src/math/`) since it is purely display-logic.
**Invariants:** Component-only change. No new engine call. Inline styles via `C`.

---

## UX / Design

### Guide tour outdated · General · 2026-07-17
**Feedback:** Guide tour must be updated.
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`)
**Suggestion:** Audit `TOUR_STEPS` against the current 7-tab workspace (Data/Clean/Explore/Model/Simulate/Calculate/Report). Per CLAUDE.md working convention: never write a count — name the things. Update step descriptions to match current UI labels (e.g. "Choose Model" dropdown is now grouped by estimator family; the W selector is controls-only, not "weights/instruments/controls"). The HintBox title prop should be the module name only, not "How to …". This is a docs/copy-only change.
**Invariants:** `TOUR_STEPS` is in `HelpSystem.jsx` — no `src/math/` changes. Update `APP_CAPABILITY_MAP` in `appCapabilityMap.js` in the same commit if any new tabs are documented.

---

### Transformation progress lost · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections.
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** (See also Bugs section above.) This is both a bug (data loss) and a UX improvement. Priority: medium. The sessionStorage draft pattern (keyed per `pid + datasetId + sectionName`) is the right approach and adds no architectural complexity.
**Invariants:** Inline styles via `C`. Draft state must NOT flow into `runner.js`.

---

### BugTriage status conflict — `estimate without intercept`
**Feedback:** BugTriage row (2026-06-14) shows `fix-later`, but CLAUDE.md documents "DONE 2026-07-18 — regression through the origin."
**File:** `BugTriage.md`
**Suggestion:** Update the BugTriage row status from `fix-later` to `fixed (2026-07-18 — see CLAUDE.md "regression through the origin")`. No code change needed — this is a documentation sync.
**Invariants:** None — documentation only.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows.
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** BugTriage X5 reconciliation marks this "routed — Fase X4 code-complete; browser benchmark pending Franco." The DuckDB Parquet cache (OPFS) and the 500-row preview split are already implemented. The remaining gap from CLAUDE.md performance strategy is: (1) auto-convert large CSV to Parquet on first load (not just `.parquet` uploads), (2) virtualized table rendering (currently at 200-row display cap — verify this is active for all DuckDB-backed paths), and (3) precomputed column stats via SQL (not JS array iteration). If crashes still occur, instrument with `console.time` around `parseFile` to isolate whether the crash is during parse, DuckDB import, or React render.
**Invariants:** Full dataset (900k rows) must always be available to `runner.js` and `duckdbRunner.js` — never truncate `rawData` before passing it to the pipeline. Preview truncation is for DISPLAY only.

---

## Other

### · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 · Original Line: `China,CHN,2010,2425.47...`
**File:** `src/services/data/parsers/`, `src/DataStudio.jsx`
**Suggestion:** This is a CSV with a mid-file malformed row (truncated in the feedback). The parser currently aborts on the first error. Implement resilient parsing: wrap each row in a try-catch and collect bad rows into a `skipped[]` array, then surface a banner "N rows skipped due to parse errors" after load, similar to how `rdata.js` handles non-tabular bindings. This matches the "fix-later" BugTriage entry (2026-06-14, Malformed CSV row aborts import) and should be handled in the same pass.
**Invariants:** The full valid-row dataset must be passed to DuckDB / `rawData` intact. Never silently truncate — always surface the skip count to the user.

---

### · Data · 2026-06-14
**Feedback:** Malformed CSV row can abort import instead of surfacing recovery options.
**File:** `src/services/data/parsers/`, `src/DataStudio.jsx`
**Suggestion:** Same root cause as the 2026-05-10 parse error above — handle together. Add `{ rows, skipped }` return shape from CSV parse, display skip count in a dismissible banner, and offer a "Download skipped rows" link for the user to inspect. The banner component pattern is already established in `OutputPanel.jsx` (Spatial tab).
**Invariants:** No runtime fetch inside parsers. Parsers are browser-only — no Node.js APIs.

---

## Suggested action order

1. **Confirm p-value fix** — Franco browser-test a large-n OLS and verify a t-stat near zero gives `p ≈ 1`; close 3 duplicate BugTriage rows if confirmed; reopen `LinearEngine.js` / `calcEngine.js` immediately if not (`src/math/LinearEngine.js`)
2. **Fix Stata workbench** — audit all estimator branches in `stataScript.js` against `estimationDispatch.js` spec fields; run the 168-cell SE-matrix validation; this is the single most-reported functional gap for export users (`src/services/export/stataScript.js`, `src/components/modeling/CodeEditor.jsx`)
3. **Complete Simulate replication script** — add `buildSimulateScript()` covering DGP + resampling + test + estimation loop; this unlocks a full reproducibility story for the Simulate tab which is otherwise a dead end for replication (`src/components/tabs/SimulateTab.jsx`)
4. **Confirm N-way FE + interactions** — Franco browser-test with a non-entity/non-time categorical FE column and an FE×covariate interaction, compare to R `fixest::feols` (`src/math/PanelEngine.js`)
5. **Add time series label controls** — small PlotBuilder change that directly improves the Explorer UX for every time series user (`src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`)
6. **Fix time series group references** — diagnose why `aes.color` grouping breaks on the time series geom; likely a missing `stroke` accessor in the `lineY` path (`src/components/PlotBuilder.jsx`)
7. **Sync BugTriage for "estimate without intercept"** — zero code change, just mark fixed in `BugTriage.md` so future agents don't re-report it
8. **Multiple hypothesis tests accumulation** — add save/list to `CoefficientTestPanel.jsx`; most of the hard math (`generateJointHypothesisScript`) is already done (`src/components/modeling/CoefficientTestPanel.jsx`)
9. **LaTeX table auto-resize** — one-line `\resizebox` wrapper in `buildStargazer`; high-impact for users comparing many models (`src/services/export/latexTable.js`)
10. **Guide tour update** — low-effort copy edit with high onboarding impact for new institutional users (`src/components/HelpSystem.jsx`)
