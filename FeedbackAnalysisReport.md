# Feedback Analysis Report — 2026-08-20

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **13 open bugs** (8 high/medium priority, 5 low/fix-later) · **10 open features** · **4 open UX/Design** · **1 open performance** (code-complete, browser validation pending) · **2 "likely fixed, needs browser confirmation"**

Total open items analysed: **28**

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** "Replication code for CS DiD 2021 does not work"
**File:** `src/services/export/rScript.js` (lines 1432–1470, `case "CallawayCS"`)
**Suggestion:** The `CallawayCS` export case exists and emits `att_gt()` / `aggte()` R code, but there is a probable comment syntax error on line 1438 (`/ Build xformla:` — single `/` instead of `//`). Separately, validate that `compGroup`, `csEstMethod`, `csAnticipation`, and the `xformla` field round-trip correctly from `CallawayEngine.js`'s result spec to the exporter's destructuring — a field name mismatch there would emit `undefined` silently. Run the `callawayValidation.js` harness and verify the emitted R script runs verbatim with `did::att_gt()`.
**Invariants:** All replication code goes through `src/services/export/rScript.js`; math stays in `src/math/CallawayEngine.js` (zero React imports). Prompt-caching invariant not affected.

---

### [high] · Export · 2026-07-05
**Feedback:** "Replication codes of the plots must have the lines to load datasets as well"
**File:** `src/services/export/plotScript.js` and `src/components/PlotBuilder.jsx`; `src/services/export/loadLine.js` already exists with `buildRLoadLine`/`buildPyLoadLine`/`buildStataLoadLine`.
**Suggestion:** `plotScript.js` does not currently invoke `loadLine.js` helpers to prepend a `read.csv`/`pd.read_csv`/`import delimited` block before the plot code. Wire the active dataset's `loadOpts` (available via `sessionSnapshot` / `DatasetMeta.loadOpts`) into `PlotBuilder`'s "Copy R/Python/Stata" export paths, calling the same `buildXLoadLine` helpers already used by model replication scripts. Precedent: `generateWorkspaceScript` in `rScript.js` already does this.
**Invariants:** Load-line helpers already handle format-specific readers (`haven::read_dta`, `read_delim` with delimiter, Excel with sheet names). Follow that contract exactly.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** "Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group"
**File:** `src/pipeline/runner.js` (`group_summarize` step), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` aggregates one column per group but cannot express cross-group arithmetic (e.g., `mean(Y | treat=1) − mean(Y | treat=0)`). The two-sample long-format test already exists in `SampleTestPanel.jsx` / `SampleTests.js` (`twoSampleMeanTest` with `splitByGroup`). Expose a "difference" aggregator in `group_summarize` that accepts a grouping variable and a binary comparison column, computes both group means, and returns the difference as a new column. Alternatively, add a dedicated `group_diff` pipeline step type (new registry entry, runner case, and UI row in `ReshapeTab`). The CLAUDE.md bug-fixed section documents `splitByGroup` as the correct shape; the fix here is surfacing it in the pipeline step, not duplicating the math.
**Invariants:** Pipeline steps must replay on `rawData`; never mutate in place. New step type requires a matching `registry.js` entry — run `pipelineReliabilityValidation.mjs` after adding it.

---

### [high] · Export · 2026-07-17
**Feedback:** "Stata workbench doesn't work completely"
**File:** `src/components/calculate/workbench/exportScript.js` (`buildStata` function, line 80+), `src/components/calculate/workbench/Workbench.jsx`
**Suggestion:** `buildStata` emits a comment saying "Stata has no symbolic engine — this uses native function plots + numeric Mata." The likely failure is that the Mata/Stata output doesn't compile for all expression types (e.g., integrals, derivatives, symbolic equations). Audit `buildStata` against the full equation types the workbench supports (numeric, symbolic, integral, derivative, optimize) and either emit runnable Mata stubs for each, or emit an explicit `* Not supported in Stata: <type>` comment so users know which parts are unavailable rather than getting broken code. The R and Python paths in `exportScript.js` can serve as the feature-set reference.
**Invariants:** Calculate tab code lives in `src/components/calculate/workbench/`. No math logic in JSX; the `operations.js` / `workbenchStore.js` layer holds expressions.

---

### [medium] · Export · 2026-07-05
**Feedback:** "Adjust the size of LaTeX tables when the number of columns increases"
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` likely emits a fixed-width `tabular` environment. When the number of model columns exceeds ~5, the table overflows a standard page. Add a column-count heuristic: if `cols > 4`, switch from `tabular` to `tabularx` with `\textwidth` total width and `X` column spec for variable columns, or insert `\resizebox{\textwidth}{!}{...}` wrapping, or emit `\footnotesize` + `\setlength{\tabcolsep}{4pt}` preamble lines. R's `modelsummary` handles this via `output="latex"` with `\begin{talltblr}` — check `INTERPRET_REGRESSION_PROMPT` for the existing LaTeX footnote conventions and keep the emitted packages consistent.
**Invariants:** None specific to architectural invariants, but the LaTeX output must compile standalone; test with `pdflatex`.

---

### [medium] · Export · 2026-07-06
**Feedback:** "Add vector for running more than 1 hypothesis and save them to replicate in R"
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently runs a single hypothesis test; the result is displayed but not accumulated or exported. Add a "Save test" button that appends each test (hypothesis string + result) to a panel-level list, persisted in `modelBuffer` or a local state array. Export all saved tests as a block in the R/Python/Stata replication script, using the existing `waldTest` / `linearHypothesis` snippet generator in `ModelHypothesis.js`. The export block should call `linearHypothesis(fit, c(...))` once per saved hypothesis.
**Invariants:** All Anthropic calls go through `AIService.js`. This feature is pure math + UI; no AI call needed.

---

### [medium] · UI · 2026-07-09
**Feedback:** "Add labels for the Time series plot, title, axis, etc."
**File:** `src/ExplorerModule.jsx` (`TimeSeriesTab`, approximately line 1291+)
**Suggestion:** `TimeSeriesTab` renders a line chart and ACF/PACF plot but exposes no label controls. Add three text inputs (title, x-axis label, y-axis label) to the `TimeSeriesTab` control panel, following the same pattern as `SvgHistogram` (which already accepts `title`, `xLabel`, `yLabel` props). Wire them into the SVG render and into the `onPin` payload so pinned time-series plots carry the labels into compare view. The label state should also be included in any replication export.
**Invariants:** UI-only change; no math files affected.

---

### [medium] · Math · 2026-07-17
**Feedback:** "Add chi-square for linearhypothesis"
**File:** `src/math/ModelHypothesis.js` (line 179 — `waldTest` already exists), `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `waldTest` already computes a Wald statistic. Under the null, the Wald statistic is asymptotically chi-square(q) where q is the number of restrictions. Add a `chiSqTest` wrapper that returns the chi-square p-value (`pChisq = 1 − F(W, q)`) using the existing `chiSquareCDF` or adapting `fCDF`. Expose it as a radio toggle in `CoefficientTestPanel` ("F-test" / "Chi-square test"), emitting the appropriate statistic and degrees of freedom. The chi-square form is standard for GMM, Poisson, and large-n regressions.
**Invariants:** Pure math; zero React in `src/math/`. `seType` must be passed explicitly — chi-square test must honour the same SE structure as the parent estimation.

---

### [medium] · Data · 2026-06-14 *(fix-later)*
**Feedback:** "Parse error: CSV Error on Line: 1583 — Original Line: China,CHN,2010,2425.47..." (malformed row aborts import)
**File:** `src/services/data/parsers/` (CSV parser path in `src/DataStudio.jsx`), DuckDB CSV reader for large files
**Suggestion:** For DuckDB-backed large CSV loads, DuckDB's `read_csv_auto` stops on the first type error. Add `ignore_errors=true` to the DuckDB `read_csv` call and follow it with a `SELECT COUNT(*) WHERE ... IS NULL` to detect and surface dropped rows. For the JS fallback parser, wrap each row's parse in try/catch and accumulate malformed rows into a warning list, then surface a dismissible banner: "N rows skipped due to parse errors — download error log." Retain the original row numbers in the log.
**Invariants:** DuckDB is the boundary for large data; do not pull full Arrow result into JS. Any skiplist/error-log must not materialise the full table.

---

### [low] · UI · 2026-07-17
**Feedback:** "Guide tour must be updated"
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array), `src/App.jsx`
**Suggestion:** `TOUR_STEPS` is a hand-maintained array with 9 steps. Audit it against current tab names (Data / Clean / Explore / Model / Simulate / Calculate / Report / Spatial) and new features (Sun-Abraham, C&S DiD, Bacon decomposition, N-way FE, Synthetic Control, PlotBuilder geoms, spatial pipeline). Remove any step referencing a removed/renamed tab. Update screenshots/tooltip text to reflect current UI. The CLAUDE.md working convention says HintBox titles must be module names not "How to <verb>"; apply the same to tour card titles.
**Invariants:** `TOUR_STEPS` is UI-only; no math or persistence.

---

### [low] · UX · 2026-06-14 *(fix-later)*
**Feedback:** "R-style `%in% c(...)` membership syntax not accepted by the filter expression UI"
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/pipeline/predicate.js` (`evalPredicate`), `src/pipeline/stepValidator.js`
**Suggestion:** The canonical filter operator for set membership is already `in` (from `predicate.js`'s `OPERATORS`). The gap is that users typing R-style `%in% c(...)` in an expression field get a parse error. Add a thin front-end sugar layer in `FilterBuilder` that detects `%in%` / `!%in%` input and converts it to `{op: "in", value: [...]}` before committing the predicate — no change to `evalPredicate` or `predicateToSQL`. Alternatively, document the `in` operator chip in the `HintBox`.
**Invariants:** `predicate.js` is the single owner of the condition language — never re-implement filter operators elsewhere.

---

### [low] · UX · 2026-06-14 *(fix-later)*
**Feedback:** "RDD result lacks an R-style `summary(rdrobust)` compact table — showing Call, estimates, h, N, CIs, p-value"
**File:** `src/components/modeling/results/` (RDD result display), `src/math/CausalEngine.js` (`runSharpRDD` / `runFuzzyRDD`)
**Suggestion:** `CausalEngine` already returns LATE, SE, CI bounds, bandwidth, and kernel. Add an `RDDSummaryTable` sub-component in `src/components/modeling/results/` that formats these into a compact table mirroring `rdrobust`'s `summary()` output: columns for Conventional / Bias-corrected / Robust rows × `Coef`, `SE`, `t`, `P>|t|`, `[lo, hi]`. Mount it above the existing results display.
**Invariants:** UI-only; RDD math is complete and validated.

---

### [low] · UX · 2026-06-14 *(fix-later)*
**Feedback:** "Allow users to edit LaTeX table variable names — RDD always shows 'D (treatment)', 'running − c', 'D x (running − c)' regardless of actual column names"
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/export/latexTable.js`
**Suggestion:** Add an optional `labelMap: {colName: displayLabel}` field to the model spec (defaulting to column names). Expose a small inline editor in the model result header (pencil icon per coefficient row). Persist `labelMap` in the model buffer alongside the spec. Pass it into `latexTable.js` and replication script generators to replace raw column names in all outputs. RDD-specific aliases (`D (treatment)` etc.) can be the initial default values for the label map.
**Invariants:** `labelMap` must be treated as a display-only decoration; the underlying `spec` and `colName` references in the math engines must never change.

---

## Features

### Model · 2026-07-09
**Feedback:** "Add direct elasticity interpretation"
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`, elasticity rules at lines 99 and 353–354), `src/components/modeling/resultDisplay.jsx`
**Suggestion:** The AI narrative already handles log-log and log-level elasticity rules in the prompt. The gap is that elasticity is never computed and shown directly in the result table — users want a numeric cell showing "elasticity at mean = 0.23" for each coefficient. For OLS: add an optional `MeanElasticity` column in the coefficient table computed as `β × mean(X) / mean(Y)` (partial-elasticity at the sample mean). For log-log models this equals `β` exactly, so it's only meaningfully different for level-level. Wire the computation into `EstimationResult` as an optional `elasticities` array, populated by `LinearEngine` when requested. Guard it behind a "Show elasticities" toggle in `InferenceOptions`.
**Invariants:** Computation in `src/math/LinearEngine.js` (no React); UI in `resultDisplay.jsx`. `seType` is already stored in `EstimationResult` — elasticity SE via delta method should also inherit it.

---

### Model · 2026-06-14
**Feedback:** "Allow users to edit LaTeX table variable names — currently RDD always shows 'D (treatment)', etc."
*(See bug entry above — this overlaps with the UX fix-later item. The feature request is the editable label map; implementation path is the same.)*

---

### Calculate · 2026-06-19
**Feedback:** "Add limits"
**File:** `src/components/calculate/workbench/operations.js`, `src/components/calculate/workbench/Workbench.jsx`
**Suggestion:** Add a `limit` operation type to the workbench that evaluates `lim_{x→a} f(x)` numerically (evaluate `f(a ± ε)` for small ε, check agreement to n digits) and symbolically (using the existing CAS/KaTeX expression pipeline). Surface it as a new equation-card type with an "x →" input field. For one-sided limits add L/R buttons. Export: R uses `pracma::limit` or numerical eval; Python uses `sympy.limit`; Stata emits a numerical approximation in Mata.
**Invariants:** Pure JS math in `operations.js`; no React in the computation. The workbench's `SessionTabs` and `EquationCard` pattern should be followed for the new card type.

---

### Calculate · 2026-07-04
**Feedback:** "Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)"
**File:** `src/components/calculate/workbench/WorkbenchCanvas.jsx`, `src/components/calculate/workbench/EquationsPanel.jsx`
**Suggestion:** A constant `y = c` is a horizontal line. Detect when an equation reduces to a single numeric value (no `x` dependency) and render it as a `ruleY([value])` layer in the canvas (Observable Plot style) rather than attempting to trace it as a function. Alternatively, expose a dedicated "Horizontal line" / "Vertical line" shortcut button in `EquationsPanel`. Export: `geom_hline(yintercept=c)` in R, `plt.axhline(c)` in Python, `yline c` in Stata.
**Invariants:** Canvas rendering in `WorkbenchCanvas.jsx`; no math logic in JSX.

---

### General · 2026-07-05
**Feedback:** "Add plots with 3 axis (3D plots)"
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** Observable Plot 0.6 (current CDN) does not support 3D. Implementing genuine 3D would require either Three.js or Plotly. Given the privacy-first, CSP-constrained architecture (no external `fetch` at runtime), the safest path is to add Plotly as a static script tag in `index.html` (or a lazy CDN singleton matching the Leaflet/proj4 pattern), then add a `scatter3d` geom type in `PlotBuilder` that routes to `Plotly.newPlot`. This is significant scope — recommend deferring until after the export-fidelity and pipeline bugs above are cleared.
**Invariants:** If Plotly is loaded from CDN, it must be added to `connect-src` in `vercel.json` (same class of bug as the DuckDB/Leaflet CSP incidents). A static import avoids the `connect-src` risk entirely and is preferred.

---

### Model · 2026-07-09
**Feedback:** "Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster"
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a filter `<input type="text">` at the top of the variable list in `VariableSelector`. On each keystroke, filter the displayed column chips to those whose name includes the search string (case-insensitive). Persist the search string in local component state; clear on dataset change. No backend change needed.
**Invariants:** UI-only; inline styles via `C` object.

---

### Explore · 2026-07-11
**Feedback:** "Add more labels in tables or boxes when plotting by color"
**File:** `src/components/PlotBuilder.jsx` (legend rendering, `PlotCanvas`)
**Suggestion:** When `aes.color` is set, the current legend shows color swatches without value labels or counts. Enhance the legend to show the category label alongside the swatch and optionally the count of observations per category (`n=X`). For continuous color scales, add tick labels to the gradient bar. This mirrors ggplot's `labs(color=...)` + `guides(color=guide_legend(label=TRUE))` behaviour.
**Invariants:** UI-only; no math.

---

### Clean · 2026-07-17
**Feedback:** "Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step"
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx` (tab router)
**Suggestion:** Each step card in `History.jsx` already has an `onClick` for select/deselect. Add a secondary action (e.g., a pencil icon or double-click) that reads the step's `type` from `STEP_REGISTRY` and maps it to the corresponding tab (e.g., `rename`/`drop`/`filter` → Clean tab, `log`/`lag`/`dummy` → Feature tab). Emit a navigation event (via a callback prop `onNavigateTo`) that `WranglingModule` handles by switching the active tab and optionally scrolling to the relevant section.
**Invariants:** Navigation logic in `WranglingModule.jsx` (tab state); `History.jsx` only fires the callback. No pipeline mutation.

---

### Clean · 2026-07-19
**Feedback:** "Add a function for Country Codes — iso2/iso3/country-name crosswalk transform"
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js` (new step type)
**Suggestion:** Add a small static lookup table (ISO 3166-1 alpha-2 / alpha-3 / country name triples — ~250 rows, ~12 KB) bundled as a JS constant. Expose a `country_code` pipeline step in `FeatureTab` with three fields: source column (detected via `guessCountryCol` heuristic), source format (`name`/`iso2`/`iso3`), and target format. Register in `registry.js` and add a runner case. Export: R uses `countrycode::countrycode()`; Python uses `pycountry`; Stata uses `kountry`.
**Invariants:** New step requires registry + runner sync (`pipelineReliabilityValidation.mjs` T5). Static lookup: zero API calls, zero privacy concern.

---

### General · 2026-07-19
**Feedback:** "Think about how to construct lists, save them and use them during the session when filtering or constructing variables"
**File:** `src/services/session/sessionState.jsx` (session context), `src/pipeline/predicate.js`
**Suggestion:** Add a `namedLists` map to `SessionStateProvider` context: `{ listName: string[] }`. Expose a "Lists" panel in the DatasetManager or WorkspaceBar where users can create, name, and edit string/numeric lists. Expose them in `FilterBuilder` as a special `in_list: listName` operator that resolves the list at run-time from session state. In `predicateToSQL`, resolve `in_list` by inlining the array as `IN (v1, v2, ...)`. Export: R uses a named vector `c(...)`; Python a Python list; Stata a local macro. The list should also be available in `case_when` and `mutate` expression fields.
**Invariants:** `predicate.js` is the single owner of filter operators; add `in_list` there. IndexedDB persistence via `indexedDB.js` — not localStorage. Lists are session-scoped by default; persist per `pid` if the user pins them.

---

### Spatial · 2026-05-20
**Feedback:** "Add grids to the plot, not only in the OMS map"
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** The Leaflet map tab (`SpatialPlotTab.jsx`) already renders grids. The static geo-plot tab (`SpatialGeoPlot.jsx`) uses Observable Plot / SVG with WKT geometry. Add a `grid` layer type to `GeoLayerConfig` that accepts a grid-ID column (in `<latIdx>_<lonIdx>` or H3 `h<res>_<q>_<r>` format from the spatial pipeline) and renders each cell as a filled SVG rectangle via `parseWktRings` / `wktToLeaflet`. Cell fill can be mapped to a numeric column with the existing `buildColorScale` helper.
**Invariants:** `src/components/tabs/spatial/shared/` helpers (`wkt.js`, `color.js`) are already pure JS — reuse, do not duplicate.

---

## UX / Design

### General · 2026-07-17
**Feedback:** "Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria"
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/components/explore/ExplorePinBar.jsx`
**Suggestion:** Add an optional `group` field to the artifact order entry (`makeArtifactId` / `parseArtifactId` in `artifactOrder.js`). In `ModelBufferBar` and `ExplorePinBar`, render a group selector chip that lets users name and assign a group; render groups as collapsible sections. The comparison and export views should scope to the selected group by default, with an "All" override. Persist group assignments in IndexedDB alongside the existing artifact order.
**Invariants:** Persistence via `indexedDB.js` only. Group names are display-only decorations; underlying artifact IDs are unchanged.

---

### Clean · 2026-07-17
**Feedback:** "Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases"
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab` currently holds form state in local React state that is lost on tab navigation. Lift each transformation sub-section's draft state (case_when rows, mutate expression, lag parameters, etc.) into a `draftState` object in `WranglingModule.jsx` (keyed by step type and sub-section), so it survives tab switches. Alternatively, write drafts to `sessionStorage` under a `pid`-scoped key on every change and restore on mount. Do NOT use `localStorage` (deprecated per architectural invariant).
**Invariants:** Use `sessionStorage` (not `localStorage`, not IndexedDB) for ephemeral draft state — IndexedDB is for committed pipeline/data. No pipeline steps are created until the user explicitly confirms.

---

### Data · 2026-07-17
**Feedback:** "Add arrange functions in the data viewer"
**File:** `src/ExplorerModule.jsx` (`DataViewer` section), `src/services/data/duckdb.js` (`getTablePage`)
**Suggestion:** `getTablePage` already accepts an `orderBy` SQL fragment for the paginated view. Add a sort control to the Data Viewer column headers (click to toggle `ASC`/`DESC`/unsorted), passing the chosen `{col, dir}` to `getTablePage` as `ORDER BY col DIR`. For DuckDB-backed tables this is a zero-materialization SQL push-down. For JS-fallback tables, sort the JS rows array in-place before slicing for pagination. Surface a small ▲/▼ indicator on the active sort column header.
**Invariants:** DuckDB as the data boundary — never pull the full Arrow result into JS for sorting. The JS fallback path should only run for the 500-row preview datasets.

---

### Spatial · 2026-06-14 *(fix-later)*
**Feedback:** "Plot builder has no spatial row-filter control scoped to a layer"
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/pipeline/predicate.js`
**Suggestion:** Add a `layerFilter` field to each geo layer's config in `GeoLayerConfig`. Compile it through `predicate.js`'s `evalPredicate` on the layer's row-set before rendering. For the Leaflet map tab, an analogous filter already exists in the pipeline; this is about exposing it per-layer in the static SVG geo-plot. Route this to V4 spatial work after launch.
**Invariants:** `predicate.js` is the single owner of the condition language — reuse `evalPredicate`, never re-implement.

---

## Items "likely fixed — needs browser confirmation"

| Date | Module | Feedback | BugTriage status | Suggested action |
|------|--------|----------|-----------------|-----------------|
| 2026-07-09 | Model | FE must accept any categorical variable (country, state, sector) | "likely fixed — N-way FE shipped in #104; needs confirmation" | Franco: test an FE spec with `sector` and `country` as FE dimensions (not entity/time), verify coefficients and SE match `fixest::feols(y ~ x \| sector + country)` |
| 2026-07-09 | Model | FE with interactions not supported | "likely fixed — FE interactions shipped in #104; needs confirmation" | Franco: test `factor_interactions` step → FE model; check `src/core/generate/feInteraction.js` wiring into `ModelConfiguration` |

Also: **BugTriage is out of sync with CLAUDE.md on one item** — "Estimate without intercept" is marked `fix-later` in BugTriage but "DONE 2026-07-18" in CLAUDE.md. Update BugTriage status to `fixed`.

---

## Suggested action order

1. **Fix CS DiD 2021 replication code** (`rScript.js` line 1438 comment syntax + spec field round-trip) — replication fidelity is core to the research-tool value prop.
2. **Add dataset load lines to plot replication exports** (`plotScript.js` + `loadLine.js`) — same class of bug as the model export gap fixed in 5b; high user-facing impact.
3. **Fix group_summarize cross-group math** (`runner.js` + `ReshapeTab.jsx`) — blocks the most common panel-data workflow (ATE computation).
4. **Fix Stata workbench completeness** (`exportScript.js` `buildStata`) — audit and emit stubs/comments per unsupported type.
5. **Add time-series plot label controls** (`ExplorerModule.jsx` `TimeSeriesTab`) — small, self-contained, high usability gain.
6. **Add chi-square variant to hypothesis test** (`ModelHypothesis.js`) — two-line math extension to existing `waldTest`.
7. **Save FeatureTab draft state** (`WranglingModule.jsx` / `FeatureTab.jsx`) — high usability; prevents losing work mid-transformation.
8. **Add sort/arrange to Data Viewer** (`ExplorerModule.jsx` + `duckdb.js`) — `getTablePage` already supports `ORDER BY`; mostly UI wiring.
9. **Confirm N-way FE browser validation** (no code change needed — Franco browser-tests with categorical FE).
10. **Update guide tour** (`HelpSystem.jsx` `TOUR_STEPS`) — low effort, addresses onboarding gap.
