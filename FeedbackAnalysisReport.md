# Feedback Analysis Report — 2026-08-11

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **14 open bugs** · **10 open features** · **1 open performance** · **4 open other (code-complete / browser-validation pending)**
- Grand total: **29 open items** analysed across ClaudeFB.md and BugTriage.md

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 (Callaway & Sant'Anna) does not work  
**File:** `src/services/export/rScript.js` (lines ~1432–1480), `src/math/CallawayEngine.js`  
**Suggestion:** The `CallawayCS` case in `rScript.js` destructures `yVar`, `treatCol`, `entityCol`, `timeCol`, `xVars`, `compGroup`, `csEstMethod`, `csAnticipation` directly from `model` at the top of the switch block — but the wired result wraps all spec fields under `model.spec` (not the root), so every field arrives as `undefined` and the emitted code has empty string arguments. Add a `const spec = model.spec ?? model; const { yVar: csY, … } = spec;` line before the destructure, mirroring the pattern already used in other estimator cases in the same file. Verify the R output uses the `did` package `att_gt()` signature verbatim and runs against a known Callaway benchmark fixture.  
**Invariants:** None violated. This is a pure export fix — no changes to `src/math/` required.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group_summarize — e.g. ATE as difference in means between treatment and control group  
**File:** `src/pipeline/runner.js` (`group_summarize` step, lines ~528–593), `src/components/wrangling/ReshapeTab.jsx`  
**Suggestion:** Add an `"expr"` aggregate function to `group_summarize` that accepts a user-supplied JS expression evaluated after the within-group stats are computed, allowing cross-group arithmetic. Alternatively, add a new `"diff_means"` aggregate type that takes `(col, conditionCol, treatValue, controlValue)` and computes the mean difference — this simpler scoped form would cover the ATE use case without introducing a full expression evaluator into the reshape step. Wire the new option into ReshapeTab's aggregation picker UI. The engine-level `group_summarize` handler already has all within-group values available at reduction time, so the change is contained to the `case "group_summarize"` block and the UI.  
**Invariants:** Non-destructive pipeline invariant — keep using `rawData` replay. No React in runner.js. Do not add UI imports to runner.js.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/components/PlotBuilder.jsx` (line 2631 in ExplorerModule mount), `src/services/export/loadLine.js`  
**Suggestion:** In `ExplorerModule.jsx`, where `PlotBuilder` is mounted (line ~2631), pass a `scriptPreamble` function that calls `buildRLoadLine(filename, loadOpts)` / `buildPyLoadLine` / `buildStataLoadLine` for the `"r"`, `"python"`, `"stata"` language argument respectively. The `loadLine.js` module already exposes all three builders and PlotBuilder's prop already accepts and calls `scriptPreamble(language)`. The only missing wire is passing it from ExplorerModule into the PlotBuilder mount — a one-line fix per language, using the existing `filename` and `loadOpts` props available in ExplorerModule's scope.  
**Invariants:** None violated. `loadLine.js` is already the single owner of load-line generation.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/calculate/workbench/exportScript.js` (`buildStata` function, lines ~79–135), `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** The Stata export only handles `twoway function` plots; system-of-equations solving, algebraic roots, and algebraic steps are silently omitted without a user-facing warning. The Mata `mata: ... end` blocks are structurally correct but the closing `end` for the top-level Mata session is only emitted per-equation — a multi-equation export leaves dangling Mata context. Specifically: (1) emit a single `mata:` / `mata mosave` block spanning all equations rather than re-entering Mata per equation, since re-entry is valid but confusing; (2) for operations (`solveZero`, `optimize`, `integral`, `deriv`) inside Mata, the display parentheses `("label", value)` syntax is only valid in interactive Mata — `printf()` or `st_numscalar()` should be used in batch mode; (3) add an upfront comment header listing which operations were successfully translated and which (e.g. symbolic solve, constraints) were skipped. These are targeted fixes inside `buildStata`, no architectural changes needed.  
**Invariants:** No React in `exportScript.js`. No Anthropic API calls. Pure-JS export function — changes are confined to `buildStata`.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js`  
**Suggestion:** Wrap the `tabular` environment in an `adjustbox` block when the column count exceeds a threshold (e.g. 7 columns): `\begin{adjustbox}{max width=\textwidth}` / `\end{adjustbox}`. This requires adding `\usepackage{adjustbox}` to the emitted preamble, which `latexTable.js` already generates. Alternatively, prepend `\small` or `\footnotesize` inside the table group as a lighter touch (no extra package). The column format string `{llrrrlll}` is hardcoded and grows linearly with model count — replace it with a dynamic builder that reads the actual column array length.  
**Invariants:** None violated. Pure export utility — no pipeline or math changes.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** Extend `CoefficientTestPanel` to maintain a list of pending hypothesis rows (each with its own coefficient selector + H₀ value), accumulating them before running. The `waldTest` function in `ModelHypothesis.js` already accepts an array of indices and H₀ values, so the math supports joint tests — only the UI currently enforces a single row. On the export side, emit all tests as a list of `linearHypothesis(fit, c("β=0", "γ=1"), test="Chisq")` calls using the `car` package. Storing tests in an array alongside the model result lets CodeEditor include them in the replication block.  
**Invariants:** None. `ModelHypothesis.js` is pure JS, no React. Export via `rScript.js` — single API egress choke point untouched.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)  
**Suggestion:** Add a new RULE in `INTERPRET_REGRESSION_PROMPT` for explicit elasticity requests: when the result's functional form is log-level or log-log, Claude should prominently report both the direct β interpretation AND the converted elasticity/semi-elasticity value (e.g. "a 1% increase in X → β% change in Y" for log-log). More importantly, surface the dependent variable's transformation in the `result.spec` context sent to the prompt — currently the prompt receives variable names but may not reliably receive the transformation type (log vs levels). Add a `depTransform: "log" | "level"` field to the estimation result so the prompt can reference it without guessing from column names.  
**Invariants:** All Anthropic calls go through `AIService.js` — do not add a raw `fetch` call.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot — title, axis, etc.  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** The Labels panel in PlotBuilder already has `title`, `xLabel`, `yLabel` fields for other geoms. The time-series geom (`line` with a time mapping) should respect these same label fields, which it may already receive via `buildGgplot` — verify the time-series rendering path passes `plot.title`/`plot.xLabel`/`plot.yLabel` into the Observable Plot `Plot.plot({title, x:{label}, y:{label}})` options. If the label section is hidden or disabled for this geom, remove that guard. Additionally, expose a caption/subtitle field for the time series given its common use in presentations.  
**Invariants:** None. UI-only change in PlotBuilder.jsx.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linear hypothesis  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `ModelHypothesis.js` already imports `pchisq` and the `waldTest` function computes a `chiSq` statistic (line ~206), but CoefficientTestPanel only surfaces the F-test path via `model.testStatLabel`. Add a test-type selector chip ("F-test" / "χ²") to `CoefficientTestPanel` and pass the selected type to the hypothesis runner. For χ², use `pchisq(R'(RVR')⁻¹R, q)` directly from the existing `waldTest` return value rather than re-implementing. The math is already present — only the UI gate and result display need updating.  
**Invariants:** `ModelHypothesis.js` is pure JS — no React imports.

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`  
**Suggestion:** Extend `artifactOrder.js` with a `groupId` field on each artifact entry (defaulting to `"default"`). Add a thin group-management layer: `createArtifactGroup(pid, name) → groupId`, `assignToGroup(pid, artifactId, groupId)`, `getActiveGroup(pid)`. In `ModelBufferBar.jsx`, add a group selector dropdown at the top of the buffer strip; the buffer renders only artifacts in the active group. The `makeArtifactId`/`parseArtifactId` contract is unchanged — groups are purely a display filter on top of the existing order array.  
**Invariants:** Use `indexedDB.js` for persistence. No localStorage.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections (case_when, etc.)  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** Add a `useEffect` that writes the current draft state of each sub-panel (case_when conditions, mutate expression, if_else branches) into `sessionStorage` keyed by `pid + ":featureTab:draft"`. On mount, read the draft and restore it if present. This is a session-scoped UX enhancement — it survives tab navigation within the same session but is intentionally not persisted to IndexedDB. The draft should be cleared on `onAdd()` (once a step is confirmed). Use `JSON.stringify`/parse with a try-catch, since sessionStorage can throw on full storage.  
**Invariants:** IndexedDB is for pipeline/data storage; session-level UI drafts in sessionStorage are an acceptable narrow exception. Do not use localStorage.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer  
**File:** `src/ExplorerModule.jsx`, `src/services/data/duckdb.js` (`getTablePage`, line ~86)  
**Suggestion:** (1) In `getTablePage`, add optional `sortCol` and `sortDir` parameters and emit `ORDER BY "${sortCol}" ${sortDir}` in the SQL query when provided. (2) In the data viewer section of ExplorerModule, render a clickable sort indicator on each column header that cycles through `none → asc → desc`. On sort-state change, call the paginated fetcher with the new sort params. For non-DuckDB (JS preview) datasets, apply `[...rows].sort(...)` in JS. This matches how `getFilteredRowCount` was added alongside `getTablePage` for the filter fix — follow the same pattern.  
**Invariants:** Non-destructive pipeline: this is a display sort only. `rawData` and the pipeline are untouched. Full dataset column sort for DuckDB must happen in SQL, not in JS (performance rule).

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line: 1583 — malformed row aborts import  
**File:** `src/DataStudio.jsx` (parseFile path), `src/services/data/parsers/`  
**Suggestion:** Wrap the CSV parser row loop in a try-catch that collects malformed rows (line number + raw content) rather than aborting. After parsing, surface a dismissible banner: "N rows had parse errors — they were skipped. Download error log?" with a link to a generated CSV of the raw bad lines. This matches the "skip/repair preview" called out in BugTriage. The DuckDB import path is more resilient (it surfaces DuckDB's own error) — the JS CSV path (`papaparse` or native) needs a similar fallback mode. Prioritise the UX: show what was loaded and what was skipped, never abort silently or lose the whole file.  
**Invariants:** Non-destructive: the pipeline still replays on rawData. The error log is ephemeral UI state, not persisted.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array)  
**Suggestion:** Review the current 9 TOUR_STEPS entries against the modules added since the last tour update: N-way FE, Callaway & Sant'Anna, Sun & Abraham, Bacon decomposition, .RData loading, CR2/CR3 SE, Goodman-Bacon panel, PlotBuilder geom_tile + facet_wrap. Add or update steps for: (a) the Multi-estimator strategy selector; (b) the SE type chips in InferenceOptions; (c) the spatial pipeline integration; (d) the CodeEditor replication scripts. Per CLAUDE.md convention, do not write counts into the tour copy — name the features instead. The HintBox title is the module name, not "How to <verb>".  
**Invariants:** None. UI-only change.

---

### [low] · Model · 2026-07-09
**Feedback:** FE must accept any categorical variable (country, state, sector), not just entity/time / FE with interactions not supported  
**File:** `src/components/modeling/ModelConfiguration.jsx` (`FEColumnPicker`, line ~60), `src/math/PanelEngine.js`  
**Suggestion:** The `FEColumnPicker` already allows any column in `panel.feCols` — the N-way FE shipped in `045f5cda` covers this. However, users must first click "Set/Update panel index" in the Panel tab and explicitly add their categorical columns to `feCols` before they appear in the FE picker. This discoverability gap is the likely root of the reports. Add a one-sentence HintBox note in ModelConfiguration or EstimatorSidebar: "To use any categorical column as a fixed effect, first add it to the Panel tab's 'Fixed-effect columns' list." Status: likely fixed in engine; UX discoverability needs a nudge.  
**Invariants:** None violated.

---

## Code-Complete / Pending Browser Validation

These items are coded but not browser-confirmed by Franco. They should be treated as open until validated.

### [medium] · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab; PNG export should not have size/aesthetic problems  
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`  
**Status:** Code-complete (`35f4b5a6`) — browser validation pending Franco.  
**Suggestion:** No new code needed. Franco should validate resizing and PNG export dimensions in the browser; if issues remain after validation, target the `ResizeObserver` and `downloadCombinedPNG` paths.

### [medium] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS  
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`  
**Status:** Code-complete (`35f4b5a6`) — frozen `result.spec` rendering fix applied. Browser validation pending Franco.

### [medium] · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed  
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`  
**Status:** Code-complete (`5e105529`) — KDE heatmap layer restored. Browser validation pending Franco.

### [medium] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation  
**File:** `src/components/ModelingTab.jsx`  
**Status:** Code-complete (`35f4b5a6`) — display frozen to `result.spec`. Browser validation pending Franco.

---

## Features

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`, `src/components/calculate/workbench/operations.js`  
**Suggestion:** Add a `limit` operation to the workbench — one-sided (`x → a⁺` / `x → a⁻`) and two-sided. Numerically, evaluate the function at `[a ± ε, a ± ε/2, ..., a ± 1e-8]` and report the converging sequence. Symbolically, check if the CAS (if present) exposes L'Hôpital or simple substitution. For indeterminate forms, report the limit value and whether it is left/right/two-sided. Wire as a new `ops.limit` flag per equation in `workbenchStore.js`, mirroring the existing `ops.deriv`/`ops.integral` flags.  
**Invariants:** No React in math/core files. `src/math/` untouched — this is a workbench UI + operations.js concern.

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph alongside function curves  
**File:** `src/components/calculate/workbench/WorkbenchCanvas.jsx`, `src/components/calculate/workbench/EquationsPanel.jsx`  
**Suggestion:** Detect equations whose expression evaluates to a finite number (i.e. a constant — no free variable). For such equations, draw a horizontal rule at `y = c` across the plot range, labeled with the constant's value. This is simpler than a function curve — no sampled points needed, just a `ctx.strokeLine(x0, cPx, x1, cPx)` in the canvas draw loop. Constant detection: if `parseFloat(expr)` or evaluation with an arbitrary `x` returns the same value regardless of `x`, treat as constant. Emit a `horizontal_line` Stata/R/Python annotation in the export as well (`abline(h=c)` / `axhline` / `yline`).  
**Invariants:** Pure UI + canvas change. No impact on `src/math/` or `src/core/`.

### General · 2026-07-05
**Feedback:** Add plots with 3 axes (3D plots)  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot (current dependency) does not support 3D — this would require a separate 3D library (Plotly, Three.js) loaded via CDN. Given the existing strict CSP and no-external-UI-library convention, this is a high-cost addition. Recommend scoping to a `scatter3d` geom using Plotly CDN loaded lazily, with `plotly.js` added to the CSP `script-src` and `connect-src`. Add `frame-src` for any embedded Plotly surfaces. This is a medium-effort feature with a clear use case (bivariate regressions, spatial visualisation). Flag for a dedicated design session.  
**Invariants:** New CDN origin must be added to `vercel.json` CSP in BOTH `script-src` AND `connect-src` — see CLAUDE.md DuckDB/CSP bug notes.

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so users can type and find variables faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add an inline text input at the top of each variable picker list (Y, X, W) that filters the displayed variable chips by substring match. On focus, expand the list; on blur/clear, restore full list. Debounce input by ~150 ms. This is a straightforward UI enhancement — no changes to estimation logic. Pattern already exists in other pickers in the codebase (e.g. EstimatorSidebar's grouped dropdown uses text filtering).  
**Invariants:** None. UI-only change in VariableSelector.jsx.

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set, the current legend shows color swatches only. Extend the legend to include the count (`n`) and optionally the mean of the Y variable per group. In PlotBuilder's legend build path, aggregate the current `rows` by the color dimension and attach the stat as a sub-label under each swatch. This matches ggplot's `geom_label`/`annotate` pattern for grouped plots. Keep the legend compact — show count by default, mean as an optional toggle in the Labels panel.  
**Invariants:** Legend stats must be computed from the filtered/rendered rows, not rawData — consistent with the "display limit ≠ computation limit" rule. For DuckDB-backed datasets use SQL aggregation, not JS array iteration over the full set.

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** In `History.jsx`, each step card already shows its step type. Map step types to their source tab + sub-tab (e.g. `"filter"` → Clean tab → Filters; `"log"` → Feature tab → Transforms). Emit an `onNavigate(tab, subtab)` callback when a step card is clicked. In `WranglingModule.jsx`, handle `onNavigate` by setting the active tab and sub-tab state. This replaces the current behaviour where clicking a history step only highlights it for undo. Steps with no natural sub-tab (e.g. AI transforms) navigate to the tab root.  
**Invariants:** None. WranglingModule tab routing is already centrally managed — adding a callback is safe.

### Model · 2026-05-21
**Feedback:** RDD summary table — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** Add a collapsible `RDDSummaryTable` component under the RDD coefficient display (similar to the existing `BaconPanel` pattern). The table should show: bandwidth (IK-optimal), kernel, polynomial order, N left/right of cutoff, effective N in bandwidth, sharp/fuzzy designation, and the LATE with SE and CI. The data is already in the estimation result — it is a display-only enhancement, no new math required. Model the layout after R's `rdrobust::summary.rdrobust` output. Wire into `ModelPlots.jsx` under the RDD result section.  
**Invariants:** Display component only — no changes to `CausalEngine.js` math.

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — RDD always shows "D (treatment)", "running − c", etc.  
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`  
**Suggestion:** Add a `labelOverrides: { [origName: string]: string }` map in the ReportingModule report state, persisted to IndexedDB per project. Pass it to `buildStargazer`/`latexTable.js` and substitute before emitting column headers. Surface a small inline edit (pencil icon → input → confirm) next to each variable name in the current coefficient table preview. This is a metadata-only feature; the underlying estimation result is unchanged.  
**Invariants:** Persist via `indexedDB.js`, not localStorage.

### Explore · 2026-05-19
**Feedback:** Add filter with R-style `%in% c(...)` syntax  
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`  
**Suggestion:** Expose an `"in"` (membership) operator in the FilterBuilder that takes a comma-separated value list (or multi-select pill input). The `predicate.js` `evalPredicate` already handles the `"in"` operator via `Array.isArray(val) && val.includes(cellVal)`. The missing piece is the CleanTab UI for entering the list and the `predicateToSQL` translation (`col IN ('a','b','c')`). `predicateToSQL` already has the `in`/`nin` cases. Add `"in"` to the operator dropdown, render a `<textarea>` for the value list, and parse on commas + newlines. The `%in% c(...)` is R syntax — the underlying operator is `"in"`, already defined.  
**Invariants:** `predicate.js` is the single owner of condition evaluation — do not re-implement `in` logic in CleanTab.

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**Suggestion:** Add a per-layer bounding-box or attribute filter in `GeoLayerConfig.jsx`: a lat/lon rectangle editor (min_lat/max_lat/min_lon/max_lon) plus a simple attribute filter (column, operator, value) using `evalPredicate` from `predicate.js`. Apply the filter client-side before passing rows to the geom layer. This is a display-only filter — it does not add a pipeline step. Route to a V4 plots milestone as BugTriage suggests; do not block current release.  
**Invariants:** Use `predicate.js`'s `evalPredicate` for consistency — never re-implement filter operators.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes at 21 MB / 900k rows  
**File:** `src/services/data/duckdb.js`, `src/DataStudio.jsx`  
**Suggestion:** The DuckDB WASM integration is the primary mitigation (Fase X4 code-complete per X5 reconciliation; browser benchmark pending Franco). The residual risk is for non-CSV formats: `.rds`, `.RData`, `.xlsx`, and `.shp` still parse entirely in JS memory before a DuckDB handoff, making large files of those formats vulnerable to heap exhaustion. The fix pattern for `.dta` (parse → DuckDB table → OPFS cache) should be applied to the remaining formats. Priority order: `.xlsx` (SheetJS expands to JS objects — highest memory risk), `.rds`/`.RData` (parser already streams XDR but accumulates rows in JS), `.shp` (geometry WKT strings are verbose). For each format, apply the same pattern: parse to an Arrow batch, call `duckdb.loadArrow(batch)`, return the 500-row preview, persist OPFS.  
**Invariants:** Zero server-side computation. OPFS for caching. No plaintext data upload. `connect-src` in CSP must include any WASM extension origins (see DuckDB `extensions.duckdb.org` bug in CLAUDE.md).

---

## Suggested Action Order

1. **CS DiD replication code broken** — One-line spec unwrap fix in `rScript.js`; blocks a core use case for the LMU target audience. Zero risk, high impact.
2. **Plot replication missing load lines** — One-function wire in ExplorerModule's PlotBuilder mount; `loadLine.js` already has all three builders. Trivially safe.
3. **Browser-validate 4 code-complete items** — PlotBuilder resize, OLS residual plots, spatial heatmap, frozen diagnostics. All are coded; Franco should confirm and close these.
4. **group_summarize cross-group math (ATE)** — Add `"diff_means"` aggregate type to runner.js; unblocks a core econometric workflow (ATE via means comparison) without touching math engines.
5. **Data viewer sort** — Add `sortCol`/`sortDir` to `getTablePage` SQL + column-header click in ExplorerModule; contained, frequently requested, no invariants at risk.
6. **Stata workbench Mata display fix** — Replace `("label", value)` interactive display with `printf` inside Mata for batch mode; targeted and low-risk.
7. **LaTeX table auto-width** — Add `adjustbox` wrapper in `latexTable.js`; one-line packaging change.
8. **Guide tour update** — Low-effort copy update to `HelpSystem.jsx`; high visibility for new users.
9. **Chi-square hypothesis UI** — Expose the existing `pchisq`/`waldTest` χ² path in CoefficientTestPanel; the math already exists.
10. **Plot constants in Calculate** — Extend the canvas draw loop for constant-valued equations; narrow, self-contained change.
