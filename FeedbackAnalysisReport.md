# Feedback Analysis Report — 2026-08-18

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **13 open bugs** · **12 open features** · **2 open UX / Design** · **1 open performance** · **1 open other**

Items marked "likely fixed — needs confirmation" in BugTriage appear in this report with a `⚠️ confirm` flag.

---

## Bugs

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work
**File:** `src/services/export/rScript.js` (case "CallawayCS", line ~1432), `src/math/CallawayEngine.js`
**Suggestion:** The R export block exists and calls `att_gt()` correctly, but it destructures model fields directly from the top-level model object (`model.yVar`, `model.treatCol`, etc.) rather than from `model.spec` where `runCallawayCS` stores them (the result has `type: "CallawayCS"` with spec fields on the inner object). Verify the destructuring path in `rScript.js` `case "CallawayCS"` matches what `CallawayEngine.js` returns — specifically that `treatCol` / `compGroup` / `csEstMethod` / `csAnticipation` are reached correctly. Additionally confirm the `gname` field maps to the "first period treated" integer column R's `did` package expects, not a binary treatment indicator.
**Invariants:** Export helpers must call `did::att_gt` with valid R syntax — no raw `fetch` or AI calls in the exporter. All Anthropic calls go through `AIService.js` only.

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/services/export/plotScript.js`, `src/ExplorerModule.jsx` (PlotBuilder mount at line ~2631)
**Suggestion:** `plotScript.js` already imports `buildRLoadLine` / `buildPyLoadLine` from `loadLine.js` and uses them for *geo* layers (the `geoLoadLines` helper), but the standard `buildGgplot` / `buildMatplotlibPlot` / `buildStataPlot` paths do not emit a load block when the source dataset is known. `PlotBuilder` receives `datasetName` from `ExplorerModule` (line 2631: `datasetName={filename}`) but `scriptPreamble` is not passed — add a `scriptPreamble` prop that wraps `buildRLoadLine(filename, loadOpts)` / equivalents, so every exported plot script opens with the correct `read_csv` / `read_dta` / `pd.read_csv` call. Follow the pattern already used by `ReportingModule`'s `AIUnifiedScript`.
**Invariants:** Load-line helpers live in `src/services/export/loadLine.js`; do not duplicate the load logic inline. Styles via `C` object only.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/tabs/CalculateTab.jsx` (Stata tab, line ~66), `src/services/export/stataScript.js`
**Suggestion:** The Stata tab in CalculateTab renders (line 66: `language === "stata"`) but the calculus/CAS output path does not have a Stata equivalent — symbolic engines (`cas/`, `calcEngine.js`) produce JavaScript expressions that map to R/Python but not to Stata's `display` / `mata` commands. Audit which compute modes (derivatives, integrals, optimization, algebra) are routed through `stataScript.js` and which fall through to a generic render. For modes with no Stata mapping, show an explicit "Not available in Stata — use R or Python" notice rather than rendering broken or empty output.
**Invariants:** No React imports in `src/math/` or `src/core/`. All styles via `C` object. The Stata tab must never silently emit code that doesn't run.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (`group_summarize` case, line ~528), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` outputs one row per group with aggregate statistics (mean, sum, etc.) but has no way to compute *cross-group* differences in a single step — the user must `group_summarize` → `pivot_wider` → `mutate`, which is error-prone (pivot column names become `0`/`1` for a binary treatment, then `mutate`'s identifier filter at runner.js:945 silently drops them). Add a "difference in means" formula mode to `ReshapeTab`'s summarise UI that takes an outcome column and a binary group column and emits a `group_summarize` step paired with an internal `pivot_wider` → `mutate` (or a dedicated two-sample summary step) that produces one row with `ATE = mean_treated − mean_control`, `n_treated`, `n_control`, and a t-stat. The math already exists in `SampleTests.js` (`twoSampleMeanTest` with a `splitByGroup` input path).
**Invariants:** Pipeline steps must replay on `rawData` — never mutate in place (`runner.js` is source of truth). No React in `src/math/`.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js` (`buildStargazer`, line ~168)
**Suggestion:** `buildStargazer` emits `\begin{tabular}{l r r r ...}` with a fixed `colFmt` — when comparing many models (5+), this overflows a standard `\textwidth`. Wrap the tabular environment in `\resizebox{\textwidth}{!}{ ... }` conditionally when `colsN > 3`, or switch to `tabularx` with `X` columns for wide tables. Also consider emitting `\small` or `\footnotesize` before the tabular block when many models are present. Either approach matches the Stata `esttab` / R `modelsummary` default behaviour.
**Invariants:** The export helper is pure JS (`src/services/export/latexTable.js`) — no React, no side effects. Style changes go in the returned string only.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` runs one hypothesis at a time; the result is not persisted. Add a "Save test" button that appends the current test spec (`{coef, op, value, result}`) to a local `savedTests` array (component state, serialised to IndexedDB under the project's `model_buffer`). Render the saved list below the input panel, and have `generateModelHypothesisScript` (already in `ModelHypothesis.js`) accept an array of test specs so the R/Python/Stata replication block emits all saved tests in sequence (`linearHypothesis(fit, ...)` once per test). Keep the single-test UI as the primary path.
**Invariants:** Persist to `indexedDB.js` — never `localStorage`. SE type must remain explicit; hypothesis scripts must honour the model's `seType`.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` (`TimeSeriesTab` component, line ~1291)
**Suggestion:** `TimeSeriesTab` builds an SVG line chart inline (`SvgMiniTimeSeries` / the full `TimeSeriesTab` canvas) with no title or axis-label inputs. Add three text inputs (title, x-axis label, y-axis label) to the existing toolbar row above the chart, wired into the SVG render — emit `<text>` elements at the standard positions (centred above plot, centred below x-axis, rotated left of y-axis). For pinned plots, include the label values in the pin object so saved comparisons carry the annotations.
**Invariants:** Inline styles via `C` / `T` tokens only. No external chart libraries — the chart is rendered in plain SVG.

---

### [medium] · Math · 2026-07-09 ⚠️ confirm
**Feedback:** Add options for fixed effects, not just entity and time, but also variables like country, state, sector must be allowed to be FE, every categorical variable
**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** BugTriage marks this as "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation." Verify that `ModelConfiguration.jsx` exposes a free-form categorical FE picker (not just a hard-coded entity/time pair) and that `runFEMulti` is reachable from the UI for non-standard FE dimensions. If the picker only offers entity and time, extend it to accept any column as an additional FE dimension (passing it through `feVars` to `runFEMulti` / `duckdbWithin` TWFE path).
**Invariants:** SE type must be passed explicitly — never hardcoded inside engine functions.

---

### [medium] · Math · 2026-07-09 ⚠️ confirm
**Feedback:** Add also FE with interactions
**File:** `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions` step)
**Suggestion:** BugTriage marks this "likely fixed — FE interactions shipped in `045f5cda` (#104); needs confirmation." Verify that `factor_interactions` pipeline step output columns are correctly picked up by the FE estimator path and that the UI in `ModelConfiguration.jsx` allows using an interaction term as a fixed-effect dimension. If not yet wired, the `factor_interactions` runner output is already available in `rawData` — ensure `PanelEngine` or `duckdbWithin` treats those columns as additional FE groupings.
**Invariants:** Zero React imports in `src/math/`. FE within-demean must replay on `rawData` — no in-place mutation.

---

### [medium] · AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`, line ~99 and ~353)
**Suggestion:** The prompt already handles log-log (elasticity) and log-level (semi-elasticity) in the functional-form rules (lines ~99, ~353). The gap is that the interpretation is inferred indirectly from the *variable name* (e.g., if the y or x column name starts with `ln_`/`log_`). Add an explicit UI control in `ModelConfiguration.jsx` or `InferenceOptions.jsx` — a "Functional form" selector (level-level / log-level / level-log / log-log) — that stamps a `functionalForm` field on `spec`. Thread this into `interpretRegression`'s user payload so the AI prompt receives an authoritative override rather than guessing from column names.
**Invariants:** All Anthropic calls go through `AIService.js`. No raw `fetch` to the Anthropic API.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearHypothesis
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already computes `chiSq` and `chiSqPval` in `waldTest()` (using `pchisq` from `calcEngine.js`), and `CoefficientTestPanel.jsx` displays them for *joint* tests (multiple coefficients). For *single-coefficient* tests the panel currently shows only the `t`/`z` statistic and its p-value. Expose the chi-square equivalent (`t²` / `z²`, df = 1) alongside the existing display — both are shown by R's `linearHypothesis(..., test = "Chisq")`. Add a "Test statistic" toggle (t/z · Chi²) to the panel header, or show both statistics in the same row separated by a slash.
**Invariants:** `src/math/` is pure JS — no React. Styles via `C` object.

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** `artifactOrder.js` tracks a flat global order (`getArtifactOrder` / `saveArtifactOrder`). Add a `groupLabel` field to each artifact entry and expose a "New group" button in `ModelBufferBar.jsx` that prompts for a group name and assigns it to subsequently pinned artifacts. Render artifacts inside collapsible group rows. Groups should be persisted via IndexedDB alongside the existing artifact order.
**Invariants:** Persist to `indexedDB.js` — never `localStorage`. Styles inline via `C` object.

---

### [medium] · Data · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx` (data viewer / DataViewer component)
**Suggestion:** The data viewer currently has no column-sort control. Add clickable sort arrows to each header cell (ascending / descending / off cycle). For DuckDB-backed datasets (large tables) sort via `ORDER BY` inside `getTablePage` — the SQL path already generates paginated queries and accepts `WHERE` clauses, so adding `ORDER BY colName ASC/DESC` before the `LIMIT/OFFSET` is straightforward. For JS-backed datasets, sort the `filteredRows` array before slicing the page. Persist the sort key and direction in component state only (not IndexedDB), consistent with how the column filter state is managed.
**Invariants:** Never pull the full Arrow result into JS for a DuckDB table. Sort must be pushed into SQL (`ORDER BY`) for large datasets. Styles via `C` object.

---

## Features

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`, `src/math/calcEngine.js`
**Suggestion:** The Calculate tab's CAS already handles derivatives, integrals, and optimisation. Add a "Limit" input mode (expression, variable, limit point, direction ±) that calls a `computeLimit(expr, variable, point, direction)` in `calcEngine.js`. For one-sided and two-sided limits at finite points, symbolic evaluation via `nerdamer` / the existing CAS is sufficient; for limits at ±∞ use L'Hôpital or asymptotic expansion. Export as `lim_{x→a} f(x)` in LaTeX and equivalent R/Python notation.
**Invariants:** `src/math/calcEngine.js` is pure JS — no React. Inline styles via `C` object.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The equation plotter already accepts function expressions. Allow bare numeric expressions (e.g. `y = 3.14`) as special-case horizontal-line entries — detect a constant by evaluating the expression on a test `x` grid and checking if the output is constant; if so, plot it as a dashed `hline` layer with a label. Alternatively, add an explicit "Constant line" input row beneath the function list. No change to `calcEngine.js` needed — this is a rendering concern only.
**Invariants:** Inline styles via `C` object. No external plotting library beyond the existing Observable Plot CDN already used in PlotBuilder.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`
**Suggestion:** A second y-axis (dual-axis chart) is the most common "3-axis" request. Observable Plot 0.6 supports `fy` channels but not a true second y-axis; a practical approach is to normalise the secondary series to the primary y-scale (z-scoring or min-max) and note the secondary axis in the legend, matching the common R `ggplot2` / `scales::sec_axis` pattern. Alternatively, use two overlaid `svg` layers with independent y scales drawn in the righthand margin. The export path in `plotScript.js` would emit `sec.axis` in ggplot or a twin-axes idiom in matplotlib/Stata. Scope and approach should be confirmed with Franco before implementation given the complexity.
**Invariants:** Inline styles via `C` object. Observable Plot CDN only — no new CDN dependencies without updating `connect-src` in `vercel.json`.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a filter text input at the top of each variable panel (Y, X, W) that does case-insensitive substring matching on `headers`. When the input is non-empty, show only matching headers in the list. Clear the filter on variable selection. This is a pure UI change requiring only `useState` for the filter string — no engine or pipeline changes needed.
**Invariants:** Inline styles via `C` / `T` tokens. No external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, the legend currently shows only the color swatch and the category label. Add a `showCount` toggle in the legend options panel that appends `(n = …)` to each legend entry — computed from a `groupBy(rows, r => r[colorCol])` count. Additionally, add a `showMean` toggle that appends the mean of the y-variable per color group. Emit these annotations in the ggplot export as a `labs(caption = ...)` or a `geom_text` call on a summarised data frame.
**Invariants:** Inline styles via `C` object. Counts/means computed in JS from `rows` — do not add a DuckDB query for this display-side concern.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` has a `type` (e.g., `case_when`, `log`, `filter`). Add a "jump to" button (↗ icon) on hover that fires a `onJumpToStep(step)` callback up to `WranglingModule`. `WranglingModule` maps `step.type` to the correct tab and sub-section (using the existing `STEP_REGISTRY` category and a new `stepToTab` lookup table), sets the active tab state, and scrolls the relevant panel into view. No new pipeline logic is needed — this is purely a navigation / focus concern.
**Invariants:** Inline styles via `C` object. Pipeline replay must still go through `runner.js` — this feature does not alter step execution.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Scope confirmed as iso2/iso3/country-name crosswalk (Franco's note in ClaudeFB). Implement as a new `FeatureTab` transform section: a static lookup table (~250 rows, bundled as a JS object in `src/data/countryCodesLookup.js`) with iso2, iso3, and common English name. Expose a "Country Code" panel in the Feature tab that lets users pick source column, source format (name/iso2/iso3), and target format, and emits a `mutate` step with the mapping pre-computed. R replication: `countrycode::countrycode(col, origin, destination)`.
**Invariants:** Static lookup bundled in the repo — no external fetch at runtime. Styles via `C` object.

---

### Model · 2026-06-14 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/services/export/latexTable.js` (`buildStargazer`, `varLabels` parameter), `src/components/ModelingTab.jsx`
**Suggestion:** `buildStargazer` already accepts a `varLabels` option map but it is never populated by callers. Add a "Label variables" panel in `ModelingTab`'s export section (or in `CodeEditor.jsx` under the LaTeX tab) where users can type a display name for each coefficient. Store the `varLabels` map in model buffer state and pass it through to every `buildStargazer` / `buildAttgtTable` call. The RDD hard-coded labels (`D (treatment)`, `running − c`) live in `latexTable.js` and should instead be derived from `result.varNames`.
**Invariants:** Export helpers in `src/services/export/` are pure JS — no React. Persist label map to `indexedDB.js` alongside the model buffer.

---

### Spatial · 2026-06-14 (fix-later)
**Feedback:** Spatial filters for plots — plot builder has no spatial row-filter control scoped to a layer
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Each layer in `GeoLayerConfig` already has filter UI for the map tab (`SpatialLayerEditor`). Mirror the `FilterBuilder`-style predicate UI into `GeoLayerConfig` for the Plot tab so users can limit a layer to rows matching a column condition (e.g., `country == "Germany"`). The filter predicate compiles through `predicate.js` (`evalPredicate`) and is applied client-side to the layer's rows before the SVG render. For DuckDB-backed layers, push the WHERE clause into the page query.
**Invariants:** Use `predicate.js` as the single owner of condition evaluation — never re-implement a filter operator elsewhere.

---

### Clean · 2026-06-14 (fix-later)
**Feedback:** R-style `%in% c(...)` membership syntax not accepted by the filter expression UI
**File:** `src/components/wrangling/CleanTab.jsx` (`FilterBuilder`), `src/pipeline/predicate.js`
**Suggestion:** `FilterBuilder` uses a structured condition language (`in` / `nin` operators with a comma-separated value input) that already handles membership testing. The gap is that users familiar with R try to type raw expressions instead of using the dropdown. Two approaches: (1) add an expression-parsing mode that detects `%in% c(...)` syntax and translates it to the `{op: "in", vals: [...]}` predicate tree before storing the step, or (2) improve the UI's inline hint ("Use the 'in' operator with comma-separated values" instead of a bare text input). Approach (2) is lower risk since approach (1) requires a mini-parser.
**Invariants:** `predicate.js` is the canonical single owner of condition language — never re-implement filter evaluation elsewhere. `predicateToSQL` must have a permissive-default-free implementation (no `TRUE` fallback for unknown operators).

---

## UX / Design

### UX · General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array, line ~11)
**Suggestion:** `TOUR_STEPS` is a hand-maintained registry. Audit it against the current tab set (Data / Clean / Explore / Model / Simulate / Calculate / Spatial / Report) and the estimator list. Remove references to UI elements that no longer exist, add tour stops for features added since the last update (N-way FE, Callaway CS DiD, synthetic control, CR2/CR3, Bacon decomposition, PlotBuilder tile/facet). Keep each stop to one sentence per feature per CLAUDE.md convention — "never write a count; name the things."
**Invariants:** No new dependencies. `HintBox` trigger always reads "Help"; `title` prop is the module name. Inline styles via `C` object.

---

### UX · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab` manages its draft state (`mode`, `cwCases`, `cwNN`, etc.) in local React state that is lost on tab switch. Lift this draft state into `WranglingModule`'s state (or into a `useRef`-backed draft cache keyed by `(pid, subTab)`) so navigating away and back restores the in-progress form. This is a state-management change — no pipeline or engine changes. Avoid `localStorage`; use `sessionStorage` keyed by `${pid}:featureTab:draft` (consistent with how other session-scoped ephemeral state is handled elsewhere in the repo).
**Invariants:** Use `indexedDB.js` for durable persistence. Session-only drafts may use `sessionStorage` keyed by `pid`. No `localStorage` for pipeline/data storage.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** BugTriage marks this as "Fase X4 code-complete; browser benchmark pending Franco." The DuckDB-WASM path, OPFS Parquet caching, and 500-row UI preview are all implemented. The remaining gap is browser-side validation — the OPFS restore path (large CSV → Parquet → OPFS → reload → `read_parquet`) has not been confirmed to work across tab-close/reload cycles for the 900k-row fixture. Priority action: validate the end-to-end OPFS cache cycle with a real 21 MB CSV on Vercel (not local dev, since `vercel.json` CSP headers differ). Separately check that `extensions.duckdb.org` is in `connect-src` (the Parquet extension fetch that broke large-file COPY is documented in CLAUDE.md under the CSP bug history).
**Invariants:** No server-side computation — plaintext data never leaves the browser. DuckDB is the data boundary; never pull the full Arrow result into JS objects.

---

## Other

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47… (malformed/truncated row aborts import)
**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/`
**Suggestion:** BugTriage labels this "fix-later — post-launch resilient-import pass." When a CSV parse fails mid-file, the entire import is aborted. Add a try/catch around the row-by-row parse step that collects malformed row numbers and continues; after parsing, surface a banner: "Imported N rows — M rows skipped (malformed). Row numbers: 1583, …". Provide a "Download skipped rows" button so users can inspect and fix the original data. This matches pandas' `error_bad_lines=False` (now `on_bad_lines='warn'`) behaviour.
**Invariants:** Inline styles via `C` object. No server-side processing — all parsing happens in the browser.

---

## Items fixed in BugTriage / X5 reconciliation but not yet marked ✓ in ClaudeFB

The following ClaudeFB bullets lack strikethrough and ✓ but are confirmed fixed in BugTriage or the 2026-06-14 X5 reconciliation table. They should be marked ~~struck through~~ with `✓` in a future ClaudeFB update pass:

- [2026-05-11 11:39] Anderson-Rubin confidence intervals (`CausalEngine.andersonRubinCI`, X5: verified fixed)
- [2026-05-11 14:38] Multiple statistical tests (StatWorkspace / SampleTests, X5: verified fixed)
- [2026-05-11 12:00] Graph does not fit screen (`35f4b5a6`, BugTriage: fixed)
- [2026-05-18 15:08] Resampling / bootstrap / sampling to the mean (`Resampling.js`, X5: verified fixed)
- [2026-05-19 11:53] Monte Carlo single variables (SimulateTab single-variable mode, X5: verified fixed)
- [2026-05-19 08:18] Color references in PlotBuilder (BugTriage: fixed 2026-05-30)
- [2026-05-20 09:50] Dataset deleted — variables should disappear (BugTriage: fixed)
- [2026-05-21 12:10] p-value calculation wrong (BugTriage: fixed 2026-06-05)
- [2026-05-21 12:12] Variables auto-reestimate (BugTriage: fixed 2026-06-01)
- [2026-05-21 12:13] Add option to clean current model (BugTriage: fixed)
- [2026-05-22 11:33] Calculate graph from 0 (BugTriage: fixed)
- [2026-05-25 09:25] Only 1 equation plotted (BugTriage: fixed)
- [2026-05-25 20:29] Estimate without intercept (CLAUDE.md: DONE 2026-07-18)
- [2026-05-30 14:47] Spatial auto-fit to unit size (BugTriage: fixed — `fitBounds`)
- [2026-06-02 11:24] Calculate LaTeX crash (BugTriage: fixed 2026-06-05)
- [2026-06-02 13:45] Interpretation per estimator (BugTriage: fixed 2026-06-03)
- [2026-06-03 07:14] Add geometry to plot points (X5: verified fixed)
- [2026-06-03 07:18] Show steps for integrals/derivatives (X5: verified fixed)
- [2026-06-04 15:25] Persistence across sessions of pinned models (X5: verified fixed — IDB model_buffer)
- [2026-06-06 08:44] Derivatives with endogenous functions (X5: verified fixed)
- [2026-06-08 14:03] Studentization for permutation tests (X5: verified fixed)
- [2026-06-10 09:06] Plot Builder resize (BugTriage: code-complete 2026-06-14; browser validation pending)
- [2026-06-11 04:28] Y~Yhat / Residual / Q-Q (BugTriage: code-complete 2026-06-14; browser validation pending)
- [2026-06-13 11:15] Spatial heatmap (BugTriage: code-complete 2026-06-14; browser validation pending)
- [2026-06-14 11:13] 2SLS Poisson interpretation (BugTriage: fixed 2026-06-14)
- [2026-06-14 11:20] Diagnostics mutate with sidebar (BugTriage: code-complete 2026-06-14; browser validation pending)
- [2026-06-17 08:51] Pipeline applies only if dataset selected in Clean (BugTriage: fixed 2026-06-25)
- [2026-07-05 12:00] Share Project RLS error (BugTriage: fixed 2026-07-14)
- [2026-07-13 13:53] Shared projects can't be opened (BugTriage: fixed 2026-07-14)
- [2026-07-17 12:16] HC2 not replicated in R code (BugTriage: code-complete 2026-07-18)

---

## Suggested action order

1. **Fix CS DiD 2021 replication code** — audit spec field destructuring in `rScript.js` `case "CallawayCS"` vs. `CallawayEngine.js` result shape; high impact for academic users who run staggered DiD and need a working R script.
2. **Add dataset load lines to plot replication code** — `ExplorerModule.jsx` does not pass `scriptPreamble` to `PlotBuilder`; without it every exported plot script is unrunnable. Core go-to-market value for replicability.
3. **Fix Stata workbench in CalculateTab** — audit which compute modes produce no Stata output and add explicit "not available in Stata" notices; the silent blank tab erodes trust in the export pipeline.
4. **Enable group-level ATE computation in `group_summarize`** — high-frequency academic use case (treatment/control mean difference); the underlying math already exists in `SampleTests.js`.
5. **Add sort controls to the data viewer** — low effort (SQL `ORDER BY` or JS sort), high day-to-day usability win.
