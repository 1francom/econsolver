# Feedback Analysis Report — 2026-06-07

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 8 open bugs · 17 open features · 0 open UX · 1 open performance · 1 open other

Cross-reference note: Several ClaudeFB.md items lack strikethrough/✓ yet BugTriage marks them as `fixed` (e.g. "graph does not fit screen", "Grids do not work", "dataset deletion variables", "variables auto-reestimate", "add option to clean model"). Those are treated as resolved here. Items still `open`, `pending`, or `needs-repro` in BugTriage are included as open.

---

## Bugs

### critical · needs-repro · Calculate · 2026-06-02
**Feedback:** `crash latex export`
**File:** `src/components/tabs/CalculateTab.jsx` — `MathPad` component, functions `copyLatex()` (~line 283), `downloadTex()` (~line 288), `openOverleaf()` (~line 291)
**Suggestion:** Three export paths exist: (1) `navigator.clipboard.writeText` — throws `DOMException` if called from a non-HTTPS context or when clipboard permission is denied; (2) `downloadTex` — relies on a `download()` helper that must be imported and in scope; (3) `openOverleaf` — POST form to `https://www.overleaf.com/docs`, will silently fail under strict CSP or popup blockers. Wrap all three handlers in `try/catch` and surface the error in component state so the user sees a message rather than a white crash. Request a browser console screenshot from Franco to confirm which path triggers.
**Invariants:** Inline styles only via `C` object — no CSS files.

---

### critical · needs-repro · Model · 2026-05-21
**Feedback:** `**URGENT** There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible`
**File:** `src/math/LinearEngine.js` — `pValue(t, df)` (line 80–82); `src/math/EstimationResult.js` — coefficient table layout
**Suggestion:** A 2026-05-26 audit of all 11 engines and wrappers found no t/p desync — `pValue(0.49, 100)` returns ≈ 0.625, matching R. BugTriage notes the most likely cause is a column-read error: the coefficient table renders `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig` and the user likely read the `β̂` column as the `t` column. Request a screenshot and the estimator name from Franco before any code change. If a real desync is confirmed, verify that `tStats` and `pVals` in the return of `runOLS` are zipped from the same index array and that `wrapResult` in `EstimationResult.js` does not reorder them.
**Invariants:** `src/math/` must have zero React imports.

---

### high · pending · Data · 2026-05-13
**Feedback:** `.dta files are wrong loaded and readed`
**File:** `src/services/data/parsers/stata.js` — `parseStata()` (~line 223)
**Suggestion:** The parser supports formats 117–119 (Stata 13–15). A separate `.dta` bug (duplicate column names in format 119) was fixed 2026-06-03, but BugTriage still lists this 2026-05-13 entry as `pending`, meaning a distinct loading issue remains. The most common silent corruption sources after the stride fix are: (1) `strL` large-string values (formats 118/119) whose `v`/`o` address pairs are not resolved correctly; (2) value-label decoding where the label map is applied to the wrong column index. Add a test fixture with the user's original file (or a synthetic format-118 .dta with value labels) and trace the parsed output against Stata's `list` command.
**Invariants:** `src/services/data/parsers/stata.js` is pure JS — no React.

---

### high · open · Report · 2026-05-25
**Feedback:** `AI does not recognize the pipeline steps in the Script generation`
**File:** `src/services/AI/sessionSnapshot.js` (`buildSessionSnapshot`, `serializeSnapshot`); `src/ReportingModule.jsx` — `AIUnifiedScript._buildModelScript()` (~line 764); `src/services/AI/AIService.js` — `generateUnifiedScript()`
**Suggestion:** CLAUDE.md pending item 5b documents this as implemented but marked `VALIDATION PENDING 2026-05-30`. The snapshot is built in `ReportingModule` via `useMemo` and threaded into `generateUnifiedScript`. Two likely failure modes: (1) `cleanedData.pipeline` is empty when the user lands directly on the Report tab without visiting Wrangling first — guard with a non-null check before building the snapshot; (2) the serialized snapshot block is not reaching the AI user prompt — add a `console.log(snapshot)` gate in `AIUnifiedScript._buildModelScript` and confirm the block is non-empty. Also confirm `UNIFIED_SCRIPT_PROMPT` rule 8 (load-opts verbatim) is present in `src/services/AI/Prompts/index.js`. Franco should validate by loading a `;`-delimited CSV then checking the R/Python/Stata script outputs.
**Invariants:** All Anthropic API calls must go through `AIService.js` — no raw `fetch` elsewhere.

---

### high · open · Model · 2026-05-28
**Feedback:** `Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators`
**File:** `src/components/modeling/EstimatorSidebar.jsx` — estimator list (~line 26–29); `src/components/modeling/runners/estimationDispatch.js` — `SunAbraham` dispatch branch (~line 255); `src/math/NonLinearEngine.js` — `runSunAbraham`
**Suggestion:** `CallawayCS` already has its own sidebar entry ("CS DiD") and is fully wired. `runSunAbraham` is implemented and dispatched in `estimationDispatch.js`, but it has no standalone entry in `EstimatorSidebar.jsx` — it is only reachable via the "EventStudy + Poisson" combo. Add `{ id: "SunAbraham", label: "SA DiD", group: "Panel", desc: "Sun & Abraham (2021) heterogeneity-robust event study — panel required", color: "#6ec8b4" }` to the estimator list. The dispatch branch at `effModel === "SunAbraham"` already exists. Update `APP_CAPABILITY_MAP` in `src/services/AI/appCapabilityMap.js` per CLAUDE.md convention.
**Invariants:** `APP_CAPABILITY_MAP` must be updated when a new estimator is added to the sidebar.

---

### medium · open · Data · 2026-06-05
**Feedback:** `Don't round the numbers when inserting, deleting or overwriting`
**File:** `src/ExplorerModule.jsx` — cell-edit handlers; the file has 96 occurrences of `toFixed`/`parseFloat`/`round`/`toPrecision`
**Suggestion:** When a user edits a cell, the value is likely coerced through `parseFloat(val).toFixed(n)` before being written back to the data array, causing irreversible precision loss. The fix: store the raw string value on write and apply display formatting only in the `<td>` renderer, never on the write path. Audit the `onCellEdit` / `setCellValue` handlers and remove any `toFixed`/`round` calls from there.
**Invariants:** Raw values must flow through `saveRawData` in `indexedDB.js` unchanged.

---

### medium · open · Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` — DuckDB CSV loader (files > 10 MB routed to DuckDB); `src/services/data/duckdb.js` — `loadCSV`
**Suggestion:** DuckDB's CSV sniffer can fail on mixed-quoting or inconsistent column counts at specific rows. As a fallback, retry the `read_csv` call with `IGNORE_ERRORS=TRUE` + `SAMPLE_SIZE=0` (full scan) and surface a warning banner showing affected rows. For the primary path, count tokens on the offending line against the header and emit a user-friendly error identifying line number and the raw token. No BugTriage entry exists — create one.
**Invariants:** DuckDB is the correct path for large CSVs. Do not fall back to the JS parser for files > 10 MB.

---

### low · open · Spatial · 2026-06-06
**Feedback:** `Geocoding directions might be upgraded — geocoder address resolution quality needs improvement`
**File:** `src/components/tabs/spatial/analyze/GeocodeSection.jsx`
**Suggestion:** The Photon/Komoot default uses a München bounding-box preset that silently constrains all queries to Bavaria. Expose the bbox preset as a user-configurable field and clear the default when the user types a non-Munich location. Add a fallback chain: Photon → Nominatim (custom URL if configured in Settings — the advanced opt-in already exists). Optionally display a confidence/importance score from the Photon response so users can identify low-quality matches.
**Invariants:** Advanced Nominatim endpoint is already opt-in in Settings — respect that flow.

---

## Features

### Model · 2026-05-11
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js` — alongside `run2SLS`; `src/components/modeling/results/IVResults.jsx`
**Suggestion:** Anderson-Rubin CI inverts the AR test statistic by grid-searching `α` values where the AR F-statistic falls below the critical value. Add `runAndersonRubin(rows, yCol, xCols, zCols, seOpts)` to `CausalEngine.js` returning `{ lo, hi, fStat }`. Wire the result into `IVResults.jsx` as an additional CI row labelled "AR CI (weak-IV robust)". The `coachingTriggers.js` already flags weak first stage — hook the AR CI display to that trigger so it appears contextually.
**Invariants:** `src/math/CausalEngine.js` must have zero React imports.

---

### Model · 2026-05-11
**Feedback:** `Add multiple statistics tests`
**File:** `src/math/SampleTests.js` — already has one-sample t, variance χ², two-sample t, paired t, proportion tests, correlation; `src/components/tabs/statsim/SampleTestPanel.jsx`
**Suggestion:** The math engine already covers the most common tests. The gap is in the UI — `SampleTestPanel.jsx` likely only surfaces a subset. Extend it to expose: two-sample mean (pooled/Welch toggle), paired t, two-proportion z, correlation test. Add a "Test type" dropdown with each test showing its null hypothesis, formula, and result. The highest-value addition not yet in the math layer is chi-square goodness-of-fit (categorical frequencies) — add `chiSquareGoFTest(observed, expected)` to `SampleTests.js`.
**Invariants:** Math additions to `src/math/SampleTests.js` must be pure JS, zero React.

---

### Simulate · 2026-05-18
**Feedback:** `Add resampling, bootstrapping, sampling to the mean`
**File:** `src/components/tabs/SimulateTab.jsx`; `src/math/Resampling.js` — `bootstrapMean`, `subsampleMean`, `permutationTwoSampleMean` (implemented; currently imported only by `CalculateTab.jsx`, not by `SimulateTab.jsx`)
**Suggestion:** Import the three functions from `Resampling.js` into `SimulateTab.jsx`. Add a "Resampling" sub-mode alongside the existing OLS/Single MC modes. UI: column picker, method selector (bootstrap / subsample / permutation), iteration count, run button, histogram of resampled statistics with CI bands. Reuse the `MCHistogram` component already present in `SimulateTab`.
**Invariants:** `src/math/Resampling.js` is pure JS — `SimulateTab` may import freely.

---

### Explore · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/pipeline/runner.js` — `filter` step; `src/components/wrangling/CleanTab.jsx` — `FilterBuilder`; `src/pipeline/registry.js`
**Suggestion:** Add an `in` operator to the `filter` step. In `runner.js`, when `op === "in"`, evaluate `values.includes(row[col])`. In `FilterBuilder`, add an "is one of" option that renders a textarea for comma-separated values with auto-quoted string detection. Update `registry.js` to document the new operator, and update `stepValidator.js` to accept `op: "in"` with `values: string[]`.
**Invariants:** Pipeline must replay on `rawData` without mutation. Registry must stay in sync with `runner.js`.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`; `src/components/tabs/spatial/shared/wkt.js` — `parseWktRings`
**Suggestion:** The Map tab (Leaflet) already supports grid overlays. The Plot tab (Observable Plot / SVG) does not. Add a "Grid" layer type in `GeoLayerConfig.jsx` that renders rectangular or H3 grid cell WKT geometry as SVG `<polygon>` elements via the existing `parseWktRings` helper. Grid data comes from a user-selected WKT column (output of the Spatial → Grid analyze step). No new math needed.
**Invariants:** None specific.

---

### Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** New `src/components/modeling/results/RDDResults.jsx`; `src/math/CausalEngine.js` — `runSharpRDD` / `runFuzzyRDD` result shape
**Suggestion:** Add a `RDDSummaryTable` component (modelled on R's `rdrobust` summary) showing: cutoff, bandwidth `h`, effective N left/right, polynomial order `p`, kernel type, LATE estimate, SE, CI, p-value, and first-stage F (fuzzy only). All fields already exist in the result object (`h`, `n`, `coef`, `se`, etc.). Mount this above the generic coefficient table when `result.type === "RDD"` or `"FuzzyRDD"`. Follow the `IVResults.jsx` pattern.
**Invariants:** New component in `src/components/modeling/results/` — inline styles via `C`. Math result shape unchanged.

---

### Model · 2026-05-25
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/math/LinearEngine.js` — `runOLS()`; `src/components/modeling/ModelConfiguration.jsx` or `src/components/modeling/InferenceOptions.jsx`
**Suggestion:** Add an optional `noIntercept: boolean` to the config/seOpts object. In `runOLS`, when `noIntercept` is true, omit the leading column of ones from X and adjust df to `n - k` (not `n - k - 1`). Propagate the flag through `runWLS`, `run2SLS`, and `runFE`/`runFD`. Expose as a checkbox labelled "No constant / suppress intercept" in `ModelConfiguration.jsx`. Guard: display a warning if the running variable is not demeaned when intercept is suppressed.
**Invariants:** `seType` must still be passed explicitly and is independent of `noIntercept`. Math change in `src/math/` only — wired via config object, not hardcoded inside engines.

---

### Spatial · 2026-05-30
**Feedback:** `Geometry transformations and show EPSG`
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`; `src/components/tabs/spatial/shared/crs.js` — `PRESET_CRS`, `isProjectedWKT`
**Suggestion:** (1) EPSG display: add an `EPSG_CODE` companion map alongside `PRESET_CRS` in `crs.js` (e.g. `"UTM 32N" → "EPSG:32632"`) and display it in `CRSTransformSection` after a successful transform. (2) Geometry transformations (scale, translate, rotate): add a `GeometryTransformSection.jsx` in `src/components/tabs/spatial/analyze/` that applies affine operations to WKT columns via proj4 (already loaded via CDN singleton). Follow the `onResult(rows, newCols)` contract.
**Invariants:** All analyze sections communicate via `onResult(rows, newCols)` — no direct state mutations.

---

### Spatial · 2026-05-30
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a "Filter rows" panel local to the Plot tab allowing users to pick a column and a value/range to subset data before rendering. This is display-only (non-destructive, not written to the pipeline). Store as `colFilter: { col, op, val }` in component state and apply when building the SVG layer data array. A simple eq/range/in operator is sufficient.
**Invariants:** Must not mutate `rawData` — display-layer filter only.

---

### Data · 2026-06-01
**Feedback:** `Add an option to edit/assign column names directly in the Data Viewer`
**File:** `src/ExplorerModule.jsx` — data table header row
**Suggestion:** Make column headers double-click editable. On confirm, emit a `rename` pipeline step (already in `runner.js`) so the rename is non-destructive and reproducible. The step appears in the pipeline history sidebar. If the dataset is DuckDB-backed, `duckdbRunner.js` already handles `rename` steps.
**Invariants:** Use `rename` step in `runner.js` — never mutate `rawData` directly. Persistence via `indexedDB.js`.

---

### Simulate · 2026-06-02
**Feedback:** `You could add different types of quantiles like in R, but it might not be necessary`
**File:** `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** Low priority. Add quantile type 7 (R default) and type 6 (SAS/SPSS) as a dropdown in the statistics section. Implement `quantileType6(sorted, p)` / `quantileType7(sorted, p)` as pure helpers in `src/math/SampleTests.js` or a new `stats.js` utility. Default to type 7 to match R.
**Invariants:** Pure JS math helpers only — no React in `src/math/`.

---

### Spatial · 2026-06-03
**Feedback:** `Add geometry to plot points, not just lat and lon`
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`; `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`; `src/components/tabs/spatial/shared/wkt.js` — `wktToLeaflet`, `parseWktRings`
**Suggestion:** Extend `GeoLayerConfig` with a "position from" toggle: "Lat/Lon columns" vs "WKT geometry column". For POINT WKT, extract coordinates via `wktToLeaflet`. For POLYGON/MULTIPOLYGON, render as SVG polygons via `parseWktRings` (already used in the geo plot canvas). The `guessWktCol` helper in `guess.js` can pre-select the WKT column automatically.
**Invariants:** `wkt.js` edits must not break existing callers in `SpatialPlotTab.jsx`.

---

### Calculate · 2026-06-03
**Feedback:** `Add a "show steps button" for integrals, derivatives and optimizations`
**File:** `src/components/tabs/CalculateTab.jsx`; `src/math/symbolicDiff.js`; `src/math/calcEngine.js`
**Suggestion:** For derivatives: if `symbolicDiff.js` returns an AST, add a `showSteps` mode that renders the chain/product/quotient rule decomposition as a KaTeX-rendered list. For integrals: `integrate()` in `calcEngine.js` uses a quadrature rule — add a `stepTrace` option that logs interval evaluations, then render the Riemann sum decomposition in a collapsible panel. For root-finding (`solveRootAuto`), display Newton iteration steps. All rendering goes in `CalculateTab.jsx`.
**Invariants:** `src/math/calcEngine.js` and `src/math/symbolicDiff.js` are pure JS — step rendering stays in the UI layer.

---

### Model · 2026-06-04
**Feedback:** `Persistence across sessions of pinned models`
**File:** `src/services/Persistence/indexedDB.js` — `STORE_MODEL_BUFFER` (v9 store, already exists, keyed by `pid`); `src/services/modelBuffer.js`
**Suggestion:** The IndexedDB `model_buffer` store was created in schema v9. The gap is that `pinnedModels` state in `ModelingTab`/`ModelBufferBar` is not persisted to or restored from this store. Add `saveModelBuffer(pid, pinnedModels)` and `loadModelBuffer(pid)` helpers in `indexedDB.js` (mirror the `savePipeline`/`loadPipeline` pattern). Call `saveModelBuffer` on every `pinnedModels` change and `loadModelBuffer` on `DataStudio` mount. Use `trimResult` from `src/services/Persistence/trimResult.js` to keep payloads small.
**Invariants:** IndexedDB only — never localStorage. Results must be trimmed via `trimResult.js` before storage.

---

### Spatial · 2026-06-05
**Feedback:** `Allow for colour selection with the "color by" option, useful for scales also`
**File:** `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`; `src/components/tabs/spatial/shared/color.js` — `CAT_PALETTE`, `buildColorScale`
**Suggestion:** `buildColorScale` in `color.js` already handles categorical and sequential scales. Expose a palette selector in `GeoLayerConfig` with: (1) categorical options (Observable 10, Tableau 10) for string columns, and (2) sequential/diverging scales (viridis, RdBu, inferno) for numeric columns with a domain min/max input. Pass the chosen palette name as a prop to `buildColorScale`.
**Invariants:** Inline styles via `C`. No external UI component libraries.

---

### Calculate · 2026-06-06
**Feedback:** `Add derivatives with endogenous functions (e.g. f(x) = 2x² − c(x), compute dc/dx symbolically)`
**File:** `src/math/symbolicDiff.js`; `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The earlier [2026-05-08] "derivations of parameters in a function" request was marked ✓ DONE, but this 2026-06-06 item is more specific: symbolic differentiation through user-defined function calls (where `c(x)` is itself defined by the user). If `symbolicDiff.js` only handles built-in transcendental functions, extend it to accept a `userFunctions: Map<name, exprAST>` argument. When the differentiator encounters a call to a user-defined function, it expands the definition inline before applying chain-rule differentiation.
**Invariants:** `src/math/symbolicDiff.js` is pure JS — no React.

---

### Model · 2026-06-06
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/math/CausalEngine.js` — `runSharpRDD` / `runFuzzyRDD`, hardcoded `varNames` strings
**Suggestion:** Replace the hardcoded strings with dynamic labels derived from the actual column names already available in the function signature: e.g. `treatCol` → `"D (treatment)"` becomes `treatCol`; `"running − c"` becomes `` `${runningCol} − ${cutoff}` ``; `"D x (running − c)"` becomes `` `${treatCol} × (${runningCol} − ${cutoff})` ``. These column names are already passed in. The `varNames` array is propagated automatically through `wrapResult` to the UI and LaTeX export — no further changes needed downstream.
**Invariants:** Change is in `src/math/CausalEngine.js` only — pure JS, zero React imports.

---

## Performance

### high · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` — file load handler; `src/services/data/duckdb.js` — DuckDB singleton; `src/WranglingModule.jsx` — dual-path runner
**Suggestion:** Per CLAUDE.md performance strategy: (1) Auto-convert large CSVs to Parquet on first DuckDB load (`COPY ... TO ... (FORMAT PARQUET)`) and cache in OPFS — eliminates re-import cost on every session. (2) Confirm `vercel.json` includes `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` for `SharedArrayBuffer` / threaded WASM support. (3) Verify `WranglingModule` caps the React table at 500 rows and never passes the full `rawData` array to a React component for render. (4) Add a progress indicator during the DuckDB import phase so users do not interpret a busy tab as a crash.
**Invariants:** Computation always runs on the full dataset in `runner.js` / `duckdbRunner.js` — display truncation must never affect pipeline output.

---

## Other

### Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` — DuckDB CSV loader; `src/services/data/duckdb.js`
**Suggestion:** This is a DuckDB sniffer error on a well-formed World Bank panel CSV. The most likely cause is a trailing comma, BOM character (`﻿`), or CRLF/LF inconsistency on line 1583. Implement a pre-flight check: scan the first and last 100 rows for delimiter consistency, BOM markers, and line-ending mixing. Surface the raw offending line in the error UI so the user can identify and fix it. Add a BugTriage entry for tracking — this item has none.
**Invariants:** DuckDB is the correct loader for large CSVs — do not silently skip rows.

---

## Suggested action order

1. **Wire Sun-Abraham as a standalone sidebar estimator** (`EstimatorSidebar.jsx` one-line insertion + `APP_CAPABILITY_MAP` update) — engine is fully validated, dispatch branch already exists; lowest-effort, highest-visibility fix for a critical bias concern.
2. **Validate and ship session-snapshot → pipeline script generation (CLAUDE.md §5b)** — open since 2026-05-25; implementation exists but is unvalidated; blocks users from replicating work. Requires browser test on `;`-delimited CSV / Excel-with-sheet / .dta.
3. **Fix `.dta` loading regression in `stata.js`** — pending since 2026-05-13; a second `.dta` bug was fixed in June but this earlier entry remains `pending`; likely affects all format-117/118 users with value labels or `strL` columns.
4. **Fix cell-editor rounding in `ExplorerModule.jsx`** — medium severity but high user-trust impact; move all `toFixed`/`round` calls from the write path to the display path.
5. **Request p-value crash reproducer from Franco** — `critical/needs-repro` since 2026-05-21; if confirmed real, the fix is in `pValue()` in `LinearEngine.js`; if confirmed layout confusion, close the triage entry and add a column header tooltip.
