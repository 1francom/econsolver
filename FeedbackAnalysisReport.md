# Feedback Analysis Report — 2026-09-25

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **10 open bugs** · **14 open features** · **0 open UX/Design** · **1 open performance** · **1 open other**
- Total: **26 open items** analysed
- Source: ClaudeFB.md (cross-referenced with BugTriage.md; items marked ✓ or ~~strikethrough~~ excluded; items listed as "fixed" in BugTriage treated as resolved even when ClaudeFB entry lacks strikethrough)

---

## Bugs

### [high] · Export/Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`
**Suggestion:** The BugTriage entry is open with no fix. The 2026-09-17 estimator sweep fixed eleven specific do-file defects, but the entry pre-dates that sweep and was never formally closed. Cross-check the sweep results: re-run `tools/validation/stataEstimatorSweep.mjs` against the current `main` and confirm all 33 "ok" cases still pass; close this entry if they do, or triage any new failures as separate rows in BugTriage.
**Invariants:** Stata scripts are generated by `stataScript.js` and `stepTranslators.js`; new emission sites must go through `services/export/stataJoin.js` and `services/export/stataFactors.js` single owners, never added inline.

---

### [high] · Export/Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP
**File:** `src/components/tabs/SimulateTab.jsx` (script builder ~lines 565–720), `src/services/export/` (no dedicated simulate exporter exists)
**Suggestion:** `SimulateTab.jsx` already emits R/Python/Stata code for the DGP draw but stops there. Extend the `buildRScript`/`buildPyScript`/`buildStataScript` closures inside `SimulateTab.jsx` (lines ~560–730) to append downstream analysis: MonteCarlo loop, sample test calls, and any estimation block the user has configured. A dedicated `src/services/export/simulateScript.js` module following the `rScript.js` pattern would keep SimulateTab free of multi-hundred-line emission code and allow the `checkPipelines.mjs`-style harness to validate it independently.
**Invariants:** The new exporter is pure JS — zero React imports; all Anthropic calls remain in `AIService.js`; SE type must be passed via `seType` argument, not hardcoded inside any engine call it invokes.

---

### [high] · UI/Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work
**File:** `src/ExplorerModule.jsx` (spaghetti render ~lines 1393+, 2775+), `src/components/PlotBuilder.jsx`
**Suggestion:** The "spaghetti" (panel time series) pin path builds group-coloured lines via Observable Plot. Check that the `color` channel accessor resolves `entityCol` correctly when the dataset is DuckDB-backed (the full table versus the 500-row preview); the group accessor may be hitting a missing field on the preview rows. Reproduce by loading a >10 MB panel dataset, declaring entity/time, switching to Explore → Time Series, and selecting a group — if the group lines collapse to one, the preview/full table mismatch is the cause.
**Invariants:** None specific; display-layer change only.

---

### [medium] · Export/General · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js` (function `buildStargazer`), `src/ReportingModule.jsx`
**Suggestion:** `buildStargazer` emits a fixed-width `tabular` environment with no column-count-aware scaling. Add a `\resizebox{\textwidth}{!}{…}` wrapper around the `tabular` when the number of models exceeds a threshold (e.g. ≥ 4), or switch to a `tabularx` with `X`-width columns. Both approaches keep the table within `\textwidth` regardless of how many models are compared. The threshold and the strategy (resizebox vs tabularx) are a design choice — resizebox is simpler and zero-dependency.
**Invariants:** Pure JS string builder — no React imports, no Anthropic API calls.

---

### [medium] · Export/Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js` (`generateJointHypothesisScript`), `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** The `CoefficientTestPanel.jsx` Joint tab currently supports one hypothesis at a time. Add a "Save test" button that appends the current `{restrictions, nullValues}` to a session-level array stored in component state (or `sessionState.jsx`), and a "Copy all tests" button that feeds the array to a new `generateBatchHypothesisScript()` in `ModelHypothesis.js` emitting multiple `car::linearHypothesis` calls in one block. The test list should persist only for the session (not IndexedDB) since it is model-specific.
**Invariants:** `ModelHypothesis.js` is in `src/math/` — zero React imports. State for the test list belongs in the component or `sessionState.jsx`, not in the math file.

---

### [medium] · UI/Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` (time-series sub-tab config area), `src/components/PlotBuilder.jsx` (labels panel)
**Suggestion:** The PlotBuilder already has a labels panel (title, x-label, y-label) and threads them through all three exporters via `plotScript.js`. The Explore time-series sub-tab bypasses PlotBuilder and renders directly; add a compact `LabelsRow` sub-component (reuse the PlotBuilder's existing `Lbl`/`TextInput` atoms from `wrangling/shared.jsx`) to collect `{title, xLabel, yLabel}` and pass them as Plot mark options in the spaghetti render path.
**Invariants:** Display-layer change only; no math file involvement.

---

### [medium] · AI/Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`), `src/services/AI/AIService.js`
**Suggestion:** Extend the `INTERPRET_REGRESSION_PROMPT` rule set with a new rule triggered when the model detects a log-log or log-level specification (identifiable from `spec.yVar` and `spec.xVars` prefixed with `log_` or named via `result.varNames`). The rule should instruct the AI to interpret the slope as a percentage elasticity (log-log) or semi-elasticity (log-level). This is purely a prompt change inside `SHARED_CONTEXT` or the user block — no engine code needed. All AI calls must go through `callClaude()` in `AIService.js`.
**Invariants:** All Anthropic calls must go through `AIService.js`; prompts live in `services/AI/Prompts/index.js`; no new raw `fetch` calls to the API.

---

### [low] · UI/General · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` array, line ~11)
**Suggestion:** `TOUR_STEPS` is a hand-maintained array of 9 steps. Audit each step against the current tab names and feature set: the working convention requires that every new tab/sub-tab gets a `TOUR_STEPS` entry in the same change that ships it. Steps likely stale since the last audit include the Simulate, Spatial, and Report tabs. Update step text, tab references (`tab`, `subTab` fields), and add any tabs added since the last update.
**Invariants:** Display-layer only; no math or API involvement. Per the working conventions, the `HintBox` trigger text must remain "Help" and the `title` prop must be the module name, not a verb phrase.

---

### [medium] · UI/General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/components/explore/ExplorePinBar.jsx`
**Suggestion:** `artifactOrder.js` already provides `makeArtifactId`/`getArtifactOrder`/`saveArtifactOrder` as the persistence layer. Extend the stored order record from a flat array to `{ groups: [{id, label, artifactIds}], ungrouped: [] }`. `ModelBufferBar.jsx` and `ExplorePinBar.jsx` would render a collapsible group header row and allow drag-reorder within and between groups. A "New group" button and an "Assign to group" chip on each pinned item completes the interaction. Keep the default view showing ungrouped items exactly as today so existing projects are unaffected.
**Invariants:** Persistence goes through `indexedDB.js`; the `artifactOrder` key already uses the `pipelines` store (see `artifactOrder.js:44`). No React in persistence layer files.

---

### [medium] · UI/Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` manages transform draft state locally; navigating away unmounts the component and React drops the state. Lift the in-progress draft (the partially-filled `case_when` / `if_else` / `mutate` form) into the parent `WranglingModule.jsx` state or into a `useRef` persisted in `sessionStorage` keyed by `${pid}_${datasetId}_featureDraft`. The simplest approach is `sessionStorage` (serialisable, survives tab switches within the same browser session, cleared on close). On mount, `FeatureTab` reads back the draft and pre-fills its form; on unmount or on step commit, it clears the key.
**Invariants:** Persistence goes to `sessionStorage` (not `localStorage`), scoped by `pid` + dataset id. No IndexedDB involvement needed for draft state.

---

## Features

### Clean · 2026-05-19
**Feedback:** Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/pipeline/predicate.js`
**Suggestion:** The canonical predicate language in `predicate.js` already has an `in` operator (e.g. `{op:"in", values:[…]}`). The gap is that users typing R-style `%in% c(...)` syntax get no recognition. Either add a thin parser in `CleanTab.jsx`'s NL input that converts `%in% c("a","b")` to `{op:"in", values:["a","b"]}`, or expand the NL command bar to recognise the pattern and emit the correct pipeline step. Do NOT re-implement the filter operator — route through `predicate.js` exclusively.
**Invariants:** `predicate.js` is the single owner of all filter operators — never re-implement `evalPredicate` or `predicateToSQL` elsewhere.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/math/CausalEngine.js` (`runSharpRDD`), `src/components/modeling/results/` (or inline in ModelPlots)
**Suggestion:** `runSharpRDD` already returns bandwidth, kernel, polynomial order, LATE, SE, and confidence interval. Add a `formatRDDSummary(result)` helper that formats these into the compact rdrobust table layout (Cutoff, N (left/right), h (left/right), b (left/right), rho, LATE, p-value) and render it as a `<pre>` block or a small inline table in the RDD result panel, above the existing coefficient display.
**Invariants:** The formatter is pure JS — no React imports; rendering goes in the component layer only.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** The static plot tab (`SpatialGeoPlot`) renders WKT geometry via SVG. Add a per-layer bounding box or attribute filter control in `GeoLayerConfig.jsx` that compiles to a `predicate.js` filter applied to that layer's rows before the SVG marks are built. Reuse `predicateToSQL` or `evalPredicate` from `predicate.js` — do not add a separate filter evaluation path.
**Invariants:** `predicate.js` is the single owner of the condition language. The filter applies to the plot's display rows only, not to `rawData`.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/export/latexTable.js`
**Suggestion:** Add an optional `labelMap: {[coefName]: string}` field to `buildStargazer(models, { labelMap })` and thread it from a new editable grid in the Report tab's table section. The grid shows coefficient names on the left, editable text on the right. When non-empty, `buildStargazer` substitutes before emission. Store `labelMap` in component state in `ReportingModule.jsx`; it does not need IndexedDB persistence.
**Invariants:** `latexTable.js` is pure JS (no React); the editable UI belongs in `ReportingModule.jsx`.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" mode alongside Derivative/Integral/Optimize. Use the existing symbolic CAS layer (or `mathjs` which is already bundled if present) to evaluate `lim_{x→a} f(x)` by series-expanding around the point or evaluating at `a ± ε`. Display the result and emit an R/Python/Stata code snippet (R: `limit(f, x, a)` via `Ryacas`; Python: `sympy.limit(expr, x, a)`). If the CAS cannot resolve it symbolically, fall back to numerical evaluation at `a ± 1e-8`.
**Invariants:** `CalculateTab.jsx` is a UI component; math logic belongs in a pure helper or the existing CAS utility — no React in math helpers.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** The CalculateTab function graph already renders multiple equations. Treat a constant `c = 3` as a horizontal-line function `y = 3` at parse time — if the expression contains no `x` dependency, render it as `Plot.ruleY([value])` or a flat line geom rather than a curve. No new UI control needed; the existing equation list handles it.
**Invariants:** Display-layer change only.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Observable Plot 0.6 does not natively support a third (right-hand) y-axis. Implement a dual-y approach: layer the second-y series as a separate `Plot.plot` overlaid via absolute positioning with a transparent background, and draw its y-axis on the right edge. This is a known workaround for Observable Plot dual-axis charts. Scope the feature to point/line geoms on the right axis (bars on a secondary axis require more complex scale negotiation). Export in R as `ggplot2 + scale_y_continuous(sec.axis=…)`.
**Invariants:** Display-layer only; no math file involvement.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small search `<input>` at the top of the variable list in `VariableSelector.jsx` that filters the displayed column names by substring match (case-insensitive). The filtered list is purely display-side — no changes to the underlying data or pipeline. Clear the search when a variable is selected. This is a pure UI addition with no state implications beyond component-local `useState`.
**Invariants:** Display-layer only.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js`
**Suggestion:** When `aes.color` is set, PlotBuilder already switches to an `observable10` or custom palette. Extend the legend to show the count or mean of the y-variable per color group as a sub-label beneath each swatch. In Observable Plot this is straightforward with a `Plot.legend({ …, label: … })` extension or a custom `Plot.text` mark. Update `plotScript.js` to emit the equivalent ggplot2 `scale_color_discrete(labels=…)` or Python legend annotation.
**Invariants:** Display-layer only; `plotScript.js` is a pure JS emitter.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each step in `History.jsx` already knows its `type` (from `STEP_REGISTRY`). Map step type to the corresponding tab+sub-tab pair (e.g. `filter` → Clean tab, `log`/`std` → Feature tab → Transforms sub-tab, `join` → Merge tab) in a static lookup in `History.jsx`. Add an `onClick` handler on each history row that calls a new `onJumpToStep(type)` prop, implemented in `WranglingModule.jsx` to `setActiveTab`/`setActiveSub`. No changes to runner or registry needed.
**Invariants:** `History.jsx` is a UI component; routing logic belongs in `WranglingModule.jsx` via the existing tab-state setters.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — needs scope: iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js` (new `country_code` step type), `src/pipeline/registry.js`
**Suggestion:** Add a small static lookup table (≈250 rows, iso2/iso3/name, ~15 KB as a JS object) bundled inline in a new `src/services/data/countryCodes.js`. Wire it as a new pipeline step `country_code` in `runner.js`/`registry.js` with `{sourceCol, fromFormat:"name"|"iso2"|"iso3", toFormat:"iso2"|"iso3"|"name"}`. Surface in `FeatureTab.jsx` under Formatting or a new "Crosswalk" sub-section. Export to R via `countrycode::countrycode(df$col, "country.name", "iso3c")`.
**Invariants:** The lookup table is pure JS data, no React. New step type must be added to both `runner.js` AND `registry.js` simultaneously (T5 of `pipelineReliabilityValidation.mjs` enforces this). Step replays on `rawData` — never mutates in place.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** `src/services/session/sessionState.jsx` (new `lists` key), `src/components/wrangling/CleanTab.jsx`
**Suggestion:** This is a design-first item — no single file owns it yet. The natural home is a new `lists` entry in `SessionStateProvider`'s context (alongside `datasets`), as an array of `{name, values: string[]}`. Lists would be created from the Distinct Values panel (a "Save as list" button) and consumed in FilterBuilder's `in`/`not in` operator via a list-picker dropdown. Persist to IndexedDB as a project-level store. No pipeline step needed — lists compile to the existing `{op:"in", values:[…]}` predicate at filter time. This is a design proposal; suggest a planning session with Franco before implementation.
**Invariants:** Persistence goes to `indexedDB.js` (not `localStorage`). List consumption must route through `predicate.js` — not a new evaluator.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/PlotBuilder.jsx`
**Suggestion:** The post-estimation Plot Builder (G10 templates in ModelingTab) already supports point/line geoms over result-augmented rows. Add a "Binned scatter" template that bins the residuals or predicted values using Observable Plot's `bin` transform, with configurable `thresholds` (number of bins) exposed as a slider. Map to R's `ggplot2::stat_summary_bin` or `binsreg::binsreg` in the export.
**Invariants:** Display-layer; no math engine involvement. Export emission goes through `plotScript.js`.

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** Add a "Sample N obs" slider or text input to the PlotBuilder toolbar that caps the displayed rows to N (drawn randomly without replacement from the full set). This is a VIEW-only control — it never changes `rawData` or the pipeline. Observable Plot will render fewer marks, speeding up large scatter plots significantly. Export emits a `dplyr::slice_sample(n=N)` / `df.sample(n=N, random_state=0)` line as a NOTE, since the reduced sample is for display only.
**Invariants:** Display-layer only; the sample is never written back to `rawData`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx` (parseFile), `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Per CLAUDE.md, the DuckDB Fase X4 performance pass (2026-09-17) delivered significant improvements (FreezeWhenHidden, rawToCleaned caching, buildInfo on Float64Array, CSV/TSV parse in a worker). The original 2026-05-08 crash report pre-dates these fixes and was never formally marked resolved in ClaudeFB. Confirm with Franco whether a 21MB/900k CSV still crashes today on the current `main`; if it does, the likely remaining bottleneck is the CSV worker parse path (`workers/parse.worker.js`) or DuckDB table registration for very wide CSV files. If confirmed fixed, mark the ClaudeFB entry with ✓.
**Invariants:** Per the DuckDB strategy in CLAUDE.md, the full dataset must always be passed to `runner.js` / `duckdbRunner.js` — never truncate `rawData` before pipeline steps.

---

## Other

### Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...
**File:** `src/services/data/parsers/tabular.js` (CSV tokenizer), `src/DataStudio.jsx`
**Suggestion:** No BugTriage entry exists for this item — it needs triage before suggestion. The error message suggests DuckDB rejected a malformed row (likely a quoted field containing an embedded newline or a locale decimal separator). Ask Franco to re-share the file or provide the exact value at line 1583 to reproduce. If reproducible, the fix belongs in `DataStudio.jsx`'s DuckDB CSV import options (`QUOTE`, `ESCAPE`, `DECIMAL` options on `COPY … FROM`).
**File not located — manual investigation needed.**

---

## Suggested action order

1. **Verify Stata workbench completeness** — re-run `tools/validation/stataEstimatorSweep.mjs` against current `main` and close or re-triage the 2026-07-17 bug; it is the highest-severity open bug and may already be fixed by the 2026-09-17 sweep.
2. **Extend Simulate replication script to cover downstream analysis** — the DGP-only script is a high-severity export gap that blocks research reproducibility for any Simulate workflow.
3. **Fix time series group references in Explore** — a confirmed medium-severity UI bug causing incorrect display in the spaghetti/panel time series; likely a DuckDB-preview vs full-table mismatch.
4. **LaTeX table column-count scaling** — medium priority, frequently encountered when comparing 5+ models; straightforward `\resizebox` fix.
5. **Batch hypothesis tests with replication export** — blocks paper-reproducibility for users running multiple joint tests; `ModelHypothesis.js` already has the single-test foundation to build on.
6. **Variable searcher in ModelingTab** — low-effort, high-impact for users with 50+ column datasets; pure UI, no math involvement.
7. **CalculateTab: plot constants** — trivial to implement, closes a natural user expectation when entering a constant expression.
8. **FeatureTab draft state persistence** — medium UX fix; saves users from losing partially-written case_when expressions.
9. **Country codes transform step** — small static lookup table, high value for cross-country panel users; adds a pipeline step so it follows existing patterns.
10. **Guide tour update** — low severity but blocks first-time user onboarding; audit takes ≈1 hour.
