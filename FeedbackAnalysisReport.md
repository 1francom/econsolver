# Feedback Analysis Report — 2026-06-26

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- 5 open bugs · 4 open features · 2 open UX/Design · 1 open performance · 0 open other

---

## Bugs

### [high] · Model · 2026-06-14

**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.

**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`

**Status:** Code-complete in commit `35f4b5a6` (OLS/WLS display frozen to `result.spec` so sidebar edits cannot mutate it). **Browser validation by Franco is pending** — this item cannot be closed until confirmed in the live app.

**Suggestion:** Open the deployed app, run an OLS estimate, add/remove a variable in the sidebar, and verify that the coefficient plot, residual-vs-fitted, and diagnostics panel do NOT change until "Estimate" is clicked again. Then restore a pinned model and confirm the correct result reappears. Only mark resolved once both paths pass.

**Invariants:** Frozen result state must read from `result.spec` (immutable after estimation), never from live sidebar state — consistent with the non-destructive pipeline invariant.

---

### [high] · Model · 2026-06-11

**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.

**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`

**Status:** Code-complete in commit `35f4b5a6` (plots were already wired; frozen-result regression fixed). **Browser validation by Franco is pending.**

**Suggestion:** Load any dataset, run OLS, and verify that the three diagnostic plot tabs (Y vs Ŷ, Residual vs Fitted, Q-Q) all render without errors and display non-empty data. If any tab is blank, check that `result.spec.residuals` / `result.spec.fitted` arrays are populated before passing to the plot components.

**Invariants:** Plot data must derive from the estimation result snapshot — never recomputed from live `rawData` or sidebar variable state.

---

### [medium] · Model · 2026-06-10

**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems.

**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`

**Status:** Code-complete in commit `35f4b5a6` (native vertical resize + bounded responsive canvas). **Browser validation by Franco is pending.**

**Suggestion:** Open the embedded Plot Builder in ModelingTab, drag the resize handle to different heights, build a chart, then export to PNG and verify the export dimensions match what is on-screen. Check that the ResizeObserver that drives `PlotBuilder`'s canvas size fires correctly on the drag event.

**Invariants:** Inline styles only via the `C` object — no CSS resize classes.

---

### [medium] · Spatial · 2026-06-13

**Feedback:** Heatmap must be fixed.

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`

**Status:** Code-complete in commit `5e105529` (Leaflet KDE heatmap layer restored with palette/opacity controls, legend, HTML export, 5 k-point cap). **Browser validation by Franco is pending.**

**Suggestion:** Open the Spatial > Map tab, add a Heatmap layer, configure lat/lon columns and radius, and verify the KDE cells render on the Leaflet map. Export to HTML and confirm the legend appears. Also exercise the opacity slider to rule out render-cycle issues.

**Invariants:** The heatmap is a Leaflet overlay — it must not import React into `SpatialEngine.js` or any file under `src/math/`.

---

### [medium] · Data · 2026-05-10

**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 — a malformed row aborts the entire import instead of surfacing a recovery option.

**File:** `src/DataStudio.jsx` (CSV parse branch), `src/services/data/parsers/`

**Status:** Triaged as fix-later (post-launch resilient-import pass). No implementation yet.

**Suggestion:** In `DataStudio.jsx`, wrap the CSV row-parsing loop in a try/catch per row. On parse failure, collect `{lineNumber, rawLine, error}` into a `parseErrors` array, skip the row, and continue. After import, if `parseErrors.length > 0` surface a modal showing the bad rows with Skip / Repair options. This prevents a single corrupt line from blocking a large import. The fix should respect the DuckDB dispatch boundary — large files already route through `duckdb.js`, so the resilient path must be implemented both in the JS fallback and in the DuckDB `registerCSV` call (use DuckDB's `IGNORE_ERRORS` option for the SQL path).

**Invariants:** Parsed data must still flow through `rawData` unchanged; never mutate rows in the parser.

---

## Features

### Explore · 2026-05-19

**Feedback:** Add filter support for R-style `%in% c("Spain (Espana)", "Basque Country (Pais Vasco)")` membership expressions in the filter UI.

**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder), `src/services/exprEvalService.js`

**Status:** Triaged as fix-later (post-launch expression-language compatibility).

**Suggestion:** In `exprEvalService.js`, add a pre-processing step that rewrites `col %in% c("a", "b")` to `["a","b"].includes(col)` before passing the expression to the evaluator. Also update `FilterBuilder`'s condition-row UI to include a new `is in` operator that emits this pattern from a multi-select input, so users who do not know R syntax can still build set-membership filters.

**Invariants:** Filter expressions run client-side against `rawData` via the pipeline `filter` step in `runner.js` — the rewrite must happen before the step is recorded in the pipeline, so replays are deterministic.

---

### Model · 2026-05-25

**Feedback:** Add an option to estimate supported regression families without an intercept.

**File:** `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js`, `src/math/PanelEngine.js`, `src/math/NonLinearEngine.js`

**Status:** Triaged as fix-later — requires engine-wide spec/export compatibility.

**Suggestion:** Add a `noIntercept: boolean` field to the model spec. In `ModelConfiguration.jsx`, expose a checkbox under the existing inference options. In each engine (`LinearEngine`, `PanelEngine`, `NonLinearEngine`) guard the intercept column insertion with `if (!noIntercept)` and adjust degrees of freedom accordingly (`k` decreases by 1). Also update the replication script generators (`rScript.js`, `pythonScript.js`, `stataScript.js`) to emit `lm(y ~ x - 1, ...)` / `sm.OLS(y, X)` / `regress y x, noconstant` as appropriate.

**Invariants:** `seType` must still be passed explicitly — do not hardcode SE variant inside the engine. The `noIntercept` flag belongs in the spec object, not the engine function signature at a global level.

---

### Spatial · 2026-05-30

**Feedback:** Allow spatial filters for plots (row-level filter scoped to a map/plot layer).

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Status:** Triaged as fix-later — route to V4 after launch.

**Suggestion:** In `GeoLayerConfig.jsx`, add a collapsible "Filter rows" panel per layer, similar to CleanTab's `FilterBuilder`. On change, filter `layer.rows` before passing them to the layer renderer in `SpatialGeoPlot.jsx`. This avoids touching `rawData` and keeps the filter ephemeral (display-only), consistent with the non-destructive pipeline rule.

**Invariants:** Filtering is display-only for the plot; it must NOT write a pipeline `filter` step or mutate `rawData`.

---

### Calculate · 2026-06-19

**Feedback:** Add limits (mathematical limits, e.g. `lim_{x→0} sin(x)/x`).

**File:** `src/components/tabs/CalculateTab.jsx`

**Status:** New item — no BugTriage entry yet. No implementation exists (zero matches for `limit` / `lim(` in `CalculateTab.jsx`).

**Suggestion:** Extend the Calculate workbench with a `limit(expr, var, point)` operation card (similar to the existing Derivative/Integral cards). Accept the expression, the variable, and the approach point (including `±Infinity`). If the current CAS library supports L'Hôpital or series expansion natively, wire the result and show intermediate symbolic steps when the "Show steps" toggle is active.

**Invariants:** All computation must stay client-side (privacy-first). Any symbolic limit logic must live in a pure JS utility file — no React imports, no Anthropic API calls for this operation.

---

## UX / Design

### Model · 2026-05-21

**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))` — showing Call, Kernel, Bandwidth, Effective N, RD Estimate, Robust SE, z-value, p-value.

**File:** `src/math/CausalEngine.js` (result object), `src/components/modeling/ModelPlots.jsx` or `src/components/ModelingTab.jsx` (render site)

**Status:** Triaged as fix-later — presentation enhancement.

**Suggestion:** Add an `rddSummary` field to the `EstimationResult` returned by `runSharpRDD` and `runFuzzyRDD` in `CausalEngine.js`, containing `{ call, kernel, bandwidth, effN_left, effN_right, estimate, robustSE, z, p }`. In the RDD results display area in `ModelingTab.jsx`, add a collapsible `RDD Summary` card that renders this as a styled table. This is purely a presentation layer change — no math changes required.

**Invariants:** The `rddSummary` field must be part of the `EstimationResult` shape — math files (`CausalEngine.js`) must stay React-free. Inline styles only via `C`.

---

### Model · 2026-06-06

**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names.

**File:** `src/components/modeling/resultDisplay.jsx`, `src/ReportingModule.jsx`

**Status:** Triaged as fix-later — post-launch reporting metadata feature.

**Suggestion:** In `resultDisplay.jsx`, add an editable label map `{ [internalVarName]: string }` stored in component state (and optionally persisted per model in IndexedDB). Render each coefficient row label as an inline-editable text field (double-click to edit, Enter to confirm). Pass the label map through to `ReportingModule`'s LaTeX Stargazer generator so that `buildStargazer` substitutes display names in the `\multicolumn` headers.

**Invariants:** The label map must be stored separately from `EstimationResult` — never mutate the result object. Persistence must go through `indexedDB.js`, not `localStorage`. Inline styles only via `C`.

---

## Performance

### [high] · Data · 2026-05-08

**Feedback:** Big datasets generate crashes (21 MB, over 900 k rows).

**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`

**Status:** Triaged as "routed" — Fase X4 is code-complete but the browser benchmark by Franco is still pending. The `.dta` path was confirmed fixed (2026-06-10), but other large-file formats (CSV/TSV) remain unconfirmed.

**Suggestion:** Franco should load the 21 MB / 900 k row CSV in the deployed Vercel build and confirm: (1) the file imports without crash, (2) the DuckDB ⚡ badge appears in the Wrangling header, (3) the tab-transition lag is ≤2 s. If crashes still occur, the likely culprit is JS object allocation during the 500-row preview materialisation — ensure preview queries use `USING SAMPLE 500 ROWS` rather than pulling the full Arrow result into a JS array.

**Invariants:** The full 900 k rows must remain in DuckDB — pipeline steps (filter, log, z-score, lag) must execute on the full DuckDB table via `duckdbRunner.js`. Never truncate `rawData` before passing to `runner.js`.

---

## Suggested action order

1. **Franco: browser-validate the `35f4b5a6` / `5e105529` code-complete items** — Diagnostics frozen state, Y~Ŷ/Residual/Q-Q plots, Plot Builder resize, and Spatial Heatmap are all implemented but unconfirmed. These four items block closure of the highest-traffic model and spatial bugs.
2. **Franco: run the Fase X4 browser benchmark** (21 MB / 900 k row CSV) — confirms or reopens the most-reported crash; result determines whether a follow-up performance sprint is needed.
3. **Implement resilient CSV import** (malformed row recovery, `DataStudio.jsx` + DuckDB `IGNORE_ERRORS`) — medium-severity data-loss risk; affects any user with slightly dirty field data.
4. **Add `noIntercept` option to engines** — medium complexity; required for common econometric use cases; spec change touches `LinearEngine`, `PanelEngine`, `NonLinearEngine`, and three replication-script generators.
5. **Add RDD summary table** — low complexity, pure presentation; high value for academic users comparing output to R's `rdrobust` printout.
6. **Editable LaTeX variable labels** — low complexity, post-launch polish; unblocks reporting for thesis users who need custom display names in coefficient tables.
7. **R-style `%in%` filter** — medium complexity; requires `exprEvalService.js` rewrite + `FilterBuilder` new operator; significant UX value for users migrating from R.
8. **Calculate: Add limits** — new CAS feature; complexity depends on existing symbolic library capabilities; no urgency before launch.
9. **Spatial filters for plots** — deferred to V4; lowest urgency among all open items.
