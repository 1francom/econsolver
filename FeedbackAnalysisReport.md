# Feedback Analysis Report — 2026-07-17

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **5 open bugs** · **5 open features** · **0 open UX** · **1 open performance** · **1 open other**
- 4 of the 5 open bugs are code-complete pending browser validation by Franco (`35f4b5a6` / `5e105529`)
- 2 ClaudeFB entries marked "open" are already implemented in code — should be struck through

---

## Bugs

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...

**File:** `src/DataStudio.jsx` (parseFile CSV/TSV branch)

**Suggestion:** The CSV parser has no per-row error isolation — a malformed row (mismatched quotes, extra delimiter) aborts the entire import. Add a try/catch around the per-row tokenise call; on failure, record `{ _rowError: true, _rawLine: line, _rowIndex: i }` and continue. After parsing, if any error rows exist, surface a non-blocking warning banner listing the skipped rows by number and offer a "Skip bad rows / Abort" choice rather than aborting unconditionally. Exclude error rows from the Arrow table passed to DuckDB to keep column-type consistency.

**Invariants:** `_rowError` sentinel rows must not leak into the DuckDB Arrow schema. The pipeline must continue to replay on `rawData` (non-destructive — no mutation of the raw parse result). Persistence must use `indexedDB.js`, never `localStorage`.

---

### [high] · Model · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems

**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Code-complete per commit `35f4b5a6` — native vertical resize with bounded canvas sizing is wired; PNG now captures user-selected dimensions. This item requires a browser confirmation pass only — no code change expected. Franco should confirm: (1) a drag handle appears at the plot's bottom edge; (2) PNG download reflects the user-resized height; (3) Observable Plot's dark-mode patch does not bleed into the exported image.

**Invariants:** All styles must use the `C` object from `shared.jsx` — no hardcoded hex or external CSS.

---

### [high] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS

**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Code-complete per commit `35f4b5a6` — `ResidualVsFitted` and `QQPlot` components in `ResidualPlots.jsx` are already wired; the fix freezes OLS/WLS diagnostics to the estimation's frozen `result.spec` so sidebar edits cannot clear the panels. Requires browser validation. Franco should confirm: (1) after running OLS all three panels appear under Diagnostics; (2) toggling a variable in the sidebar does not redraw or clear the panels; (3) restoring a pinned model restores its correct diagnostic plots. If panels still fail to appear, check whether `result.spec` is correctly threaded into `<ResidualPlots>` at the `ModelingTab.jsx` call site.

**Invariants:** Residuals must be read from the frozen `result` object — never re-computed from live `cleanedData`. `ResidualPlots.jsx` is a pure renderer and must not import from `src/math/` or trigger a new estimation.

---

### [medium] · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`

**Suggestion:** Code-complete per commit `5e105529` — Heatmap restored as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and a deterministic 5k-point cap. Requires browser validation. Franco should confirm: (1) selecting "Heatmap" in `SpatialLayerEditor` renders a visible KDE layer on the map; (2) palette and opacity sliders update the layer live; (3) HTML export produces a self-contained file with the heatmap rendered. If the layer is invisible, confirm the Leaflet heatmap CDN plugin URL is listed in `connect-src` (not only `script-src`) in `vercel.json` — see the DuckDB CSP bug documented in CLAUDE.md.

**Invariants:** Any runtime `fetch()` to a new CDN host must be added to `connect-src` in `vercel.json`.

---

### [high] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.

**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`

**Suggestion:** Code-complete per commit `35f4b5a6` — a frozen `result.spec` is captured at estimation time and drives all displayed plots, not live sidebar state. Requires browser validation. Franco should confirm: (1) after estimation the coefficient plot shows the estimated spec; (2) toggling any variable in the sidebar does not alter the displayed plots; (3) clicking a pinned model in the buffer restores its coefficient plot and diagnostics exactly. If still broken, verify `ModelPlots.jsx` reads `result.spec.xVars` not the live `xVars` state.

**Invariants:** `result.spec` must be treated as immutable after `estimate()` sets it. No math calls may be triggered from `useEffect` hooks that track sidebar variable state.

---

## Other

### [medium] · Data (parse failure) · 2026-05-10
*(listed under "Other" section in ClaudeFB.md — functionally a bug)*

See **Bugs → [medium] · Data · 2026-05-10** above. The item is cross-listed here for completeness because ClaudeFB.md places it in the "Other" section rather than "Bugs".

---

## Features

### Model · 2026-05-21 (fix-later)
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression

**File:** `src/math/CausalEngine.js`, `src/components/ModelingTab.jsx` (RDD result display section)

**Suggestion:** `CausalEngine.js` already returns all needed values: `bw` (bandwidth), `N_left`, `N_right`, `tau` (LATE), `se`, `pval`, `ci95`. Add a `RDDSummaryTable` presentational component in `src/components/modeling/results/` that renders an rdrobust-style compact block: bandwidth, N left/right of cutoff, coefficient, SE, t-stat, p-value, 95% CI. Wire it into the RDD results section in `ModelingTab.jsx` below the existing estimate card. No new math is required and no changes to `EstimationResult.js` are needed.

**Invariants:** Display formatting only (`toFixed(4)` etc.). No new fields in `EstimationResult.js`. No React imports in `src/math/`.

---

### Model · 2026-05-25 (fix-later)
**Feedback:** Add an option to estimate without intercept

**File:** `src/math/LinearEngine.js` (line 128: `const k = xCols.length + 1; // includes intercept`), `src/components/modeling/ModelConfiguration.jsx`

**Suggestion:** Add a `noIntercept` boolean to the estimation spec (default `false`). In `LinearEngine.js`, when `noIntercept === true`, skip prepending the ones column to `X` and use uncentered R² (`1 − SSR / SST_uncentered`). Thread the flag through `ModelConfiguration.jsx` as a checkbox and through `estimate()` in `ModelingTab.jsx`. The DuckDB suff-stats paths in `src/services/data/` (`duckdbOLS`, `duckdbWLS`, `duckdbIV`) also prepend a literal `1` column in SQL — each must conditionally omit it. Update replication scripts: `rScript.js` → `lm(y ~ x - 1)`, `pythonScript.js` → omit `add_constant`, `stataScript.js` → `reg y x, noconstant`. For FE/FD, `noIntercept` is a no-op (demeaning absorbs the intercept).

**Invariants:** `seType` must still be passed explicitly to the engine — `noIntercept` does not change the SE contract. No hardcoded SE behavior inside math functions. No React in `src/math/`.

---

### Model · 2026-06-06 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names

**File:** `src/services/export/latexTable.js` (line 22: `varLabels = {}`, line 126: `display = varLabels[v] ?? v`), `src/ReportingModule.jsx`

**Suggestion:** `buildStargazer` already accepts a `varLabels: Record<string, string>` option and resolves `display = varLabels[v] ?? v` at line 126 — the rendering contract is complete. The only missing piece is a UI in `ReportingModule.jsx` to populate the map: render each coefficient name as a lightly styled inline-editable `<span>` that writes into a `labelMap` state on blur; pass `labelMap` to `buildStargazer` on every table regeneration. For RDD, pre-populate `labelMap` from `result.spec` column names so the table is correct by default. This is low-effort — no changes to math files or `EstimationResult.js`.

**Invariants:** `labelMap` is display-only session state. It must never propagate to `EstimationResult.js` or any engine. Stored in React state, not IndexedDB.

---

### Explore / Clean · 2026-05-19 (fix-later)
**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option

**File:** `src/services/exprEvalService.js`

**Suggestion:** `exprEvalService.js` exists and handles filter expression evaluation (confirmed by Grep). Add a pre-processing rewrite step before the expression reaches the eval worker: transform `col %in% c("a","b")` → `["a","b"].includes(col)` using a regex (`/(\w+)\s*%in%\s*c\(([^)]+)\)/g`). Quoted strings with commas require a proper token split (split on `,` not inside quotes). Also support the negation `!col %in% c(...)` → `!["a",...].includes(col)`. This is < 30 lines, requires no changes to `runner.js`, `registry.js`, or the pipeline contract.

**Invariants:** The `filter` pipeline step in `runner.js` must not be modified — it passes the expression string to the worker unchanged. All rewriting happens inside `exprEvalService.js` only. Filter steps remain non-destructive and replay on `rawData`.

---

### Spatial · 2026-05-30 (fix-later)
**Feedback:** Allow spatial filters for plots

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** Add a per-layer attribute filter control to `GeoLayerConfig.jsx`: a column selector plus value/range input. On change, pass pre-filtered rows (computed in JS via a `.filter()` on the layer's source rows) instead of the full dataset to the layer painter in `SpatialGeoPlot.jsx`. This is a view-only filter — no pipeline step, no `rawData` mutation, no new DuckDB query. Defer geometry-intersection filtering (which would require `SpatialEngine.js` spatial join logic) to a follow-up V4 item.

**Invariants:** Filtered rows must never be written to `rawData` or create a pipeline step — spatial filter is view-only and scoped to the plot tab. Non-destructive pipeline rule must hold.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows

**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`

**Suggestion:** Fase X4 is code-complete — DuckDB-Wasm + OPFS Parquet cache + SQL suff-stats push-down (Fase 0–8) are all implemented. This item requires a browser benchmark by Franco on a 900k-row fixture, not a code change. Franco should time: (a) tab transition `< 5 s`; (b) OLS estimate with 5 covariates `< 5 s`; (c) `group_summarize` `< 5 s`; (d) CSV export. Diagnostic: open DevTools → `window.__duckdb` to confirm the singleton is initialised; `window.__scCache` to check suff-stats cache hit rate. If tab-transition lag persists, the most likely remaining bottleneck is the initial Arrow materialisation in JS before DuckDB registration — streaming the CSV directly via `registerFileBuffer` instead of parsing it in JS first would eliminate that spike. For `.rds` and `.xlsx` files (still bypassing DuckDB), a follow-up should add the same 10 MB DuckDB-register path that `.dta` received in 2026-06-10.

**Invariants:** All computation must stay client-side (privacy-first; no server egress). Display is capped at 500 rows but estimation and export must use the full DuckDB table. `SELECT *` on large tables is banned per CLAUDE.md DuckDB strategy.

---

## Items appearing open in ClaudeFB.md that are already implemented

These bullets have no strikethrough but the features are present in code. Franco should update ClaudeFB.md to strike them through.

| ClaudeFB.md date | Feedback text | Evidence |
|---|---|---|
| 2026-06-19 08:43 | Add limits | `src/math/calcEngine.js` exports `limit()` (lines 229–284); `CalculateTab.jsx` imports it (line 18) and renders the "lim Limit" tool tab (line 1586); full input/compute/result UI at lines 2161–2194. |
| 2026-07-04 12:37 | Allow plotting constants in the graph (horizontal line for a constant value) | `src/components/calculate/workbench/workbenchStore.js` initialises `refLines: []`; `ViewControls.jsx` (lines 30–39) manages `addRef`/`updateRef`/`removeRef` for `h`/`v` reference lines; `WorkbenchCanvas.jsx` (lines 146–150) draws them behind curves. |

---

## Suggested action order

1. **Browser-validate commit `35f4b5a6`** — one session closes three open bugs simultaneously: OLS diagnostic plots (Y~Yhat / residual / QQ), coefficient-plot freeze, and PlotBuilder resize/PNG export.
2. **Browser-validate commit `5e105529`** — closes the spatial heatmap bug.
3. **Strike through stale ClaudeFB.md bullets** — mark ✓ the "Add limits" (2026-06-19) and "Allow plotting constants" (2026-07-04) entries once browser-confirmed; cleaner tracking going forward.
4. **Add `%in%` rewrite in `exprEvalService.js`** — < 30 lines, no `runner.js` changes, high user-visible impact; unblocks common R-style membership filter patterns.
5. **Wire `varLabels` UI in `ReportingModule.jsx`** — `buildStargazer` already accepts the map at `latexTable.js:22`; only a simple editable label list in the Report tab is missing; low effort, high polish.
6. **Add no-intercept toggle** — `LinearEngine.js` line 128 + `ModelConfiguration.jsx` checkbox + DuckDB SQL suff-stats paths + replication scripts; medium effort, high research value.
7. **RDD rdrobust-style summary table** — new presentational component only; no new math; medium effort.
8. **Resilient CSV import** — per-row error isolation in `DataStudio.jsx` + skip/repair dialog; medium effort, unblocks users with messy real-world exports.
