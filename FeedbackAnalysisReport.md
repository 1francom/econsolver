# Feedback Analysis Report — 2026-08-12

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **15 open bugs** (5 high · 8 medium · 2 low) · **3 code-complete pending browser validation** · **5 fix-later (low)** · **9 open features** · **1 open performance** · **0 other**

Items marked **[validation pending]** are code-complete per BugTriage.md and require Franco to do browser verification before closing.

---

## Bugs

### high · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work  
**File:** `src/services/export/rScript.js` (line ~1432 `CallawayCS` case), `src/math/CallawayEngine.js`  
**Suggestion:** The `att_gt()` call in `rScript.js:1432–1470` is likely missing the correct `control_group` mapping from the Litux `compGroup` option ("nevertreated" | "notyettreated") or `tname` is being inferred incorrectly. Run the generated R snippet against `did::att_gt` and compare the error message; then patch the field mapping. Additionally confirm `aggte(out, type="dynamic")` parameters (min_e, max_e) match the displayed event study horizon.  
**Invariants:** Export functions are pure JS — no React imports. Changes to rScript.js must not affect the math engine.

---

### high · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well  
**File:** `src/services/export/plotScript.js` (`buildGgplot` line ~343, `buildMatplotlibPlot` line ~798, `buildStataPlot` line ~935), `src/components/PlotBuilder.jsx` (line ~24 imports `buildGgplot`)  
**Suggestion:** `geoLoadLines()` (line ~546 in plotScript.js) already generates dataset load lines for geo-layer plots but is not called from `buildGgplot`/`buildMatplotlibPlot`/`buildStataPlot`. Thread `datasets` into those three builders (already done for the geo path) and prepend `buildRLoadLine`/`buildPyLoadLine` lines from `src/services/export/loadLine.js` for the active dataset before the ggplot/matplotlib body. Mirror the pattern in `src/services/export/rScript.js` where `buildRLoadLine` is called.  
**Invariants:** `loadLine.js` is the single owner of load-line generation — do not re-implement a reader lookup inside plotScript.js.

---

### high · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group  
**File:** `src/pipeline/runner.js` (line ~528 `group_summarize`), `src/components/wrangling/ReshapeTab.jsx`  
**Suggestion:** The `group_summarize` step currently supports `mean | sum | count | min | max | sd | median | quantile` but not cross-group arithmetic (e.g. mean(Y | T=1) − mean(Y | T=0)). The cleanest approach is a new `fn: "diff_means"` aggregation that accepts a `splitCol` and `splitVal` pair, computes the group mean for each split value, and returns the difference — keeping the diff inside a single `group_summarize` call rather than requiring a multi-step workaround. Add the aggregation case in runner.js and a UI row in `ReshapeTab.jsx` similar to the existing `quantile` row that reveals `splitCol`/`splitVal` pickers when selected.  
**Invariants:** Pipeline steps must replay on rawData — they must not store derived data. The SE type is irrelevant here; no engine changes needed.

---

### high · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/calculate/workbench/Workbench.jsx`, `src/components/calculate/workbench/WorkbenchCanvas.jsx`  
**Suggestion:** The workbench exposes R, Stata, and Python tabs (Workbench.jsx line ~222). Reproduce the failure in the Stata tab — likely candidates are: (a) `generateStataScript` throwing on a Calculate-specific model shape that is different from the modeling pipeline format, or (b) the workbench's expression-to-Stata translation not handling symbolic functions (CAS output) the same way the R tab does. Audit `SessionTabs.jsx` to confirm the Stata script path receives the same session object as R, then diff what the R tab sends against the Stata tab. File not fully located for the Stata-specific export path — check `src/components/calculate/workbench/SessionTabs.jsx` and trace the Stata generation call.  
**Invariants:** All Anthropic calls go through AIService.js; do not add a raw fetch to the calculate workbench.

---

### high · UI · 2026-06-11 [validation pending]
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS  
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/components/ModelingTab.jsx`  
**Suggestion:** Code marked complete in commit `35f4b5a6` — plots are wired and the frozen-result regression is fixed. **Action required:** Franco must browser-validate OLS residual plots, Y~Ŷ scatter, and Q-Q plot all render correctly and remain frozen when sidebar variables are changed.  
**Invariants:** Residual plots read from the frozen `result.spec` — they must not re-run estimation on variable change.

---

### medium · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js` (line ~168 `\begin{tabular}`), `src/ReportingModule.jsx`  
**Suggestion:** When the column count exceeds ~6 models, the `{tabular}` environment overflows the page width in standard A4/Letter LaTeX. Wrap the tabular body in `\resizebox{\textwidth}{!}{...}` when `colsN > 5`, or add a `\setlength{\tabcolsep}{4pt}\small` preamble. Alternatively offer a `landscape` toggle that wraps in `\begin{landscape}` from the `pdflscape` package (emit the `\usepackage{pdflscape}` hint as a comment). The `latexTable.js` colFmt string at line ~168 drives column width — this is the correct place to apply scaling.  
**Invariants:** None — export is pure JS with no React or engine dependencies.

---

### medium · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `ModelHypothesis.js` already computes `chiSq` + `chiSqPval` (line ~203). The missing piece is a UI affordance in `CoefficientTestPanel.jsx` to (a) add multiple hypothesis rows and (b) emit a batch of `car::linearHypothesis(fit, c("β1 = 0", "β2 = 0"))` calls in the R snippet. Add an "Add hypothesis" button in `CoefficientTestPanel.jsx`, store an array of hypothesis specs in component state, run each through `testLinearHypothesis`, and update the R export to emit a single `car::linearHypothesis` call with a character vector of constraints.  
**Invariants:** Math must stay in `ModelHypothesis.js` (pure JS, no React). SE type is already passed explicitly; do not hardcode a SE variant in the new multi-hypothesis path.

---

### medium · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/components/PlotBuilder.jsx` (Labels panel section, line ~1692 toolbar), `src/ExplorerModule.jsx`  
**Suggestion:** PlotBuilder already has a Labels panel for other geoms. Audit whether `line` geom (used for time series) has its title/axis label fields wired to the Observable Plot `Plot.plot({ x: {label:...}, y: {label:...}, title:... })` call. If the controls exist but are not threaded into the time-series `Plt.lineY` call, add them. If they are missing entirely, add title/x-axis/y-axis text inputs alongside the existing colour picker in the Labels panel and thread them into `Plot.plot()` options.  
**Invariants:** No CSS files — style via inline styles with `C` constants. Observable Plot is loaded from CDN (`Plt` singleton) — do not import it as a package.

---

### medium · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis  
**File:** `src/math/ModelHypothesis.js` (line ~203 `chiSq` computation), `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** `ModelHypothesis.js` already computes a chi-square statistic (`chiSq`, `chiSqPval`) from the Wald matrix product. The gap is in the display — `CoefficientTestPanel.jsx` likely shows only the F-statistic. Add a `χ²(q)` row alongside the F row, displaying `chiSq` and `chiSqPval` from the existing return object. For asymptotic tests (large n, HC/clustered SE) the chi-square is the more appropriate statistic, so consider showing both with a note on which is preferred under the chosen SE type.  
**Invariants:** `ModelHypothesis.js` is pure JS (zero React). Do not add `pchisq` calls — it already imports from `calcEngine.js`.

---

### medium · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/indexedDB.js`  
**Suggestion:** `artifactOrder.js` manages a flat ordered list of artifact IDs. Extend the IndexedDB schema (or `artifactOrder.js`) to support an optional `groupId` per artifact, with a separate `groups` list keyed by `pid`. In `ModelBufferBar.jsx`, add a "New group" action that creates a named group and a drag-to-group affordance (or a group picker dropdown on each card). Filter comparisons by the active group. This is a medium-scope UI feature requiring IDB schema changes — ensure the IDB version is bumped and a migration is written in `indexedDB.js`.  
**Invariants:** Persistence uses IndexedDB (`indexedDB.js`) — never localStorage. Group metadata must be stored per project (`pid`-scoped), matching `saveArtifactOrder`'s existing key pattern.

---

### medium · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** Add a `draftState` object in `WranglingModule.jsx` (or `FeatureTab.jsx`'s own `useState`) that persists the in-progress step configuration for each FeatureTab sub-panel (case_when, mutate, lag/lead etc.). On sub-panel unmount, write the current form state to `draftState[subPanelKey]`; on mount, restore from it. Keep this in React state only (no IDB persistence needed since it resets on pipeline apply) — the goal is preserving across tab navigation within the same session, not cross-session.  
**Invariants:** Draft state must never mutate `rawData` or `pipeline` — it is UI-only ephemeral state.

---

### medium · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** The data viewer currently renders a static table preview. Add a column-header click handler that toggles sort direction (asc/desc) for that column. For DuckDB-backed datasets, issue `getTablePage(table, {orderBy: col, orderDir: "asc"|"desc"})` to the existing DuckDB query path — `getTablePage` already accepts an `ORDER BY` clause. For non-DuckDB data, sort the JS array in the preview. Display a sort indicator (↑/↓) on the active sort column. This is display-only — it does not add an `arrange` pipeline step.  
**Invariants:** Non-destructive — do not mutate `rawData`. Sorting is presentation-layer only.

---

### medium · UI · 2026-06-10 [validation pending]
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems  
**File:** `src/components/PlotBuilder.jsx` (line ~1893 resize style), `src/components/ModelingTab.jsx`  
**Suggestion:** Code marked complete in commit `35f4b5a6` — native CSS `resize: vertical` is set on the plot viewport. **Action required:** Franco must verify that the resizable handle is visible, that drag-to-resize works, and that the PNG export captures the user-selected dimensions without aesthetic artifacts.  
**Invariants:** None.

---

### medium · UI · 2026-06-13 [validation pending]
**Feedback:** Heatmap must be fixed  
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`  
**Suggestion:** Code marked complete in commit `5e105529` — Heatmap restored as a Leaflet layer with KDE cells, palette/opacity controls, legend, and HTML export. **Action required:** Franco must browser-validate that the Heatmap layer type renders correctly, that the palette/opacity controls work, and that the 5k-point cap is respected.  
**Invariants:** None.

---

### medium · Data · 2026-06-14 (fix-later)
**Feedback:** Parse error: CSV Error on Line: 1583 — malformed CSV row aborts import  
**File:** `src/DataStudio.jsx` (parseFile entry), `src/services/data/parsers/` (CSV branch)  
**Suggestion:** Wrap the CSV parse loop in a try/catch-per-row (or use the existing DuckDB CSV reader's `IGNORE_ERRORS = TRUE` option for large files). On error, collect the bad row numbers and surface a post-parse summary: "N rows skipped due to parse errors — rows 1583, 2041, …". For DuckDB-backed files, `read_csv_auto(..., ignore_errors=true)` already handles this gracefully. The fix-later tag is appropriate — prioritise after launch.  
**Invariants:** Non-destructive — never mutate rawData on partial parse failure.

---

### low · UX · 2026-06-14 (fix-later)
**Feedback:** R-style `%in% c(...)` membership syntax not accepted by the filter expression UI  
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`  
**Suggestion:** `predicate.js` owns the canonical condition language (`OPERATORS`). The `in` operator already exists in the system — the gap is likely that `CleanTab.jsx`'s filter builder emits an "in" step with a comma-separated string, but users typing `%in% c("a","b")` directly into an expression field get a parse error. Add a note/tooltip in the filter UI that the correct form is the "in list" operator chip, not R-style syntax. A full parser for R expressions is out of scope; improving the tooltip is the minimal fix.  
**Invariants:** `predicate.js` is the single owner of filter operators. Do not add a new operator or re-implement operator logic elsewhere.

---

### low · UX · 2026-06-14 (fix-later)
**Feedback:** RDD result lacks an R-style `summary(rdrobust)` compact table  
**File:** `src/math/CausalEngine.js` (`runSharpRDD`), `src/components/modeling/results/` (RDD result display)  
**Suggestion:** `runSharpRDD` in `CausalEngine.js` already returns bandwidth, kernel, polynomial order, LATE, and SE. Add a compact `rdrobust`-style table component in the RDD result panel (similar to the Bacon decomposition panel) that formats: Cutoff, N left/right, h left/right, b left/right, LATE, SE, z, p, CI. This is a presentation change only — no math changes needed.  
**Invariants:** Math files (`src/math/`) must have zero React imports. Build the display component in `src/components/modeling/`.

---

### low · UX · 2026-06-14 (fix-later)
**Feedback:** Spatial plot builder has no spatial row-filter control scoped to a layer  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`  
**Suggestion:** Add an optional "Filter rows" field in `GeoLayerConfig.jsx` that accepts a column + operator + value and filters the layer's data before rendering. Reuse `evalPredicate` from `src/pipeline/predicate.js` — do not re-implement a filter operator. This is display-only and does not add a pipeline step.  
**Invariants:** `predicate.js` owns `evalPredicate` — never re-implement filter logic in a component.

---

### low · UX · 2026-06-14 (fix-later)
**Feedback:** LaTeX coefficient table display names cannot be edited independently of source columns  
**File:** `src/components/modeling/resultDisplay.jsx`  
**Suggestion:** Add an editable label map (column name → display label) stored in component state alongside the result. Render a small "rename" icon on each coefficient row in the table; clicking opens an inline text input. On export, `latexTable.js` should accept a `labelMap` argument and substitute display labels for raw column names. Keep the label map in session state only — do not persist to IDB.  
**Invariants:** None.

---

### low · UI · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS` registry)  
**Suggestion:** Audit the `TOUR_STEPS` array against the current `WorkspaceBar.jsx` 7-tab layout (Data/Clean/Explore/Model/Simulate/Calculate/Report) and verify each step's selector, title, and description reflects the current UI. Per CLAUDE.md working convention, do not write counts into tour copy ("14 estimators" etc.) — name things instead. Also cross-check `APP_CAPABILITY_MAP` in `appCapabilityMap.js` while editing, as the two documents tend to drift together.  
**Invariants:** Tour steps are UI-only; no math or pipeline changes.

---

## Features

### Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`)  
**Suggestion:** Extend the estimator-type rules in `INTERPRET_REGRESSION_PROMPT` with an elasticity block: when both Y and X are log-transformed (detectable via column name prefixes `log_`/`ln_` or via the `dict` variable type hints), instruct Claude to interpret the coefficient directly as a point elasticity (∂ ln Y / ∂ ln X). When only X is log-transformed, interpret as semi-elasticity (% change in Y per 1-unit change in X). Expose this as an inferred annotation rather than a user toggle — keep the prompt extension consistent with the existing `ESTIMATOR TYPE RULES` block structure.  
**Invariants:** All AI prompts live in `AIService.js`/`Prompts/index.js`. Do not add a raw Anthropic fetch elsewhere.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a search/filter input at the top of the variable chip panel in `VariableSelector.jsx`. On input, filter the displayed chips to those whose column name includes the typed substring (case-insensitive). No backend changes — this is a pure React `useState` filter on the `headers` array. Keep the full chip list in state; only hide non-matching chips via `display: none` so the selection state is unaffected.  
**Invariants:** Inline styles only (no CSS files). Use the `C` color constants.

---

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** Add a "Limit" operation to the Calculate workbench (`src/components/calculate/workbench/`) alongside derivatives and integrals. The math engine for one-sided and two-sided limits already exists as part of the CAS (symbolic differentiation via L'Hôpital would cover 0/0 indeterminate forms). Expose it in the workbench UI as a `lim(expr, x, a, direction)` input and display the result step-by-step, consistent with how derivatives show applied rules.  
**Invariants:** Math logic must live in a pure JS file under `src/math/` or `src/components/calculate/workbench/` — zero React imports in math files.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`, `src/components/calculate/workbench/WorkbenchCanvas.jsx`  
**Suggestion:** The workbench canvas renders function curves. Support constant expressions (those whose parsed AST has no variable node) by treating them as a horizontal line `y = c` over the current x-domain. This requires no new UI — a constant entered as an equation should simply render as a horizontal line. Add a detection step before the function-plot render: if the parsed expression is constant, fall back to `Plt.ruleY([c])` (Observable Plot's horizontal rule) rather than evaluating at each x point.  
**Invariants:** Observable Plot is loaded from CDN — do not import as an npm package.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot 0.6 supports a second Y axis via `Plot.plot({ y2: {...} })` and marks bound to `fy`. A simpler short-term approach: add a "secondary Y axis" toggle in PlotBuilder's axis options that scales a second layer to a separate Y range and draws it on the right axis. Map it to Observable Plot's `y2` channel. This scopes the feature to a second Y axis (dual-axis chart) rather than true 3D, which Observable Plot does not support and would require a different rendering library.  
**Invariants:** Observable Plot is loaded from CDN as `Plt`. No CSS files — inline styles only.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`  
**Suggestion:** When `aes.color` is set, PlotBuilder already renders a legend via Observable Plot's default color scale. The missing element is a richer legend: show the actual count/proportion for each color category either as a text annotation per dot/line cluster or as a legend table. Add an `aes.colorLabel` option (boolean, off by default) that when enabled appends a `Plot.text` mark placing each category's label+count at the centroid of that group's data, or produces a standalone legend table beside the plot.  
**Invariants:** Legend rendering must work with the existing `observable10` palette and the dark-theme patch. No CSS files.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** Each step in the History sidebar has a type (e.g. `case_when`, `log`, `filter`). Add a "✎ Edit" action on each step card that, when clicked, (a) scrolls the WranglingModule to the correct tab and sub-panel and (b) pre-populates the form fields from the step's spec. The tab routing is already done via `WranglingModule`'s tab state — add a `jumpToStep(step)` callback prop on `History.jsx` and a step-type → tab/subpanel mapping object in `WranglingModule.jsx`. Pre-populating the form fields requires each FeatureTab/CleanTab sub-panel to accept an `editDraft` prop.  
**Invariants:** The pipeline must remain non-destructive. Clicking "Edit" loads the step into the UI form but does not apply until the user explicitly submits.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** Add a "Country Code" section to FeatureTab alongside the existing transforms. Embed a static lookup table (~250 entries: ISO 3166-1 alpha-2, alpha-3, numeric, and English name). Expose three modes: (a) name → iso2, (b) name → iso3, (c) iso2 ↔ iso3. On apply, emit a `mutate` or `add_column` pipeline step that maps the source column through the lookup. For fuzzy country-name matching, reuse the existing `fuzzyGroups` utility from `src/components/wrangling/utils.js`.  
**Invariants:** The lookup table is pure static JS — no external API calls. The transform emits a standard pipeline step type (`mutate` or `add_column`) so it replays correctly on rawData.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
**File:** `src/services/session/sessionState.jsx` (design needed)  
**Suggestion:** This is a cross-module feature requiring a design session. A session-scoped list store in `sessionState.jsx` is the most natural fit (consistent with how `datasets` is already session-scoped there). Each list would have a name and an array of values. `predicate.js` would need a new `in_list` operator that resolves the list name at runtime. Not started — recommend a design decision on scope (session-only vs. IDB-persisted) before implementation.  
**Invariants:** If persisted to IDB, use `indexedDB.js` — never localStorage. `predicate.js` must remain the single owner of filter operators.

---

## UX / Design

_(The fix-later items in the Bugs section above — R %in% syntax, RDD compact table, spatial plot filter, editable LaTeX labels — are the primary UX/polish items. They are tracked there under low severity.)_

---

## Performance

### Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx` (parseFile branch), `src/services/data/duckdb.js`  
**Suggestion:** DuckDB integration for CSV/Parquet (Fase X4) and `.dta` (2026-06-10) is code-complete per CLAUDE.md. The remaining concern is browser benchmark validation. Per `plans/2026-05-22-fase-x4-performance.md`, large `.rds`/`.xlsx`/`.shp` files still bypass the DuckDB path and materialize fully in JS. The next step is to extend `parseFile` in `DataStudio.jsx` to auto-convert large `.rds`, `.xlsx`, and `.shp` results to an Arrow-backed DuckDB table (replicating the `.dta` path added 2026-06-10). This would eliminate the tab-transition lag for all large formats.  
**Invariants:** DuckDB as data boundary — never pull the full Arrow result into JS objects. OPFS cache key must not be overwritten from in-memory state alone (see bug fixed 2026-07-22 in CLAUDE.md).

---

## Suggested action order

1. **[High] group_summarize cross-group arithmetic** — blocks ATE computation entirely (runner.js `group_summarize`); small, self-contained fix with high teaching value for LMU use-case.
2. **[High] Plot replication code missing load lines** — core reproducibility promise of the platform; straightforward extension of `plotScript.js` using existing `buildRLoadLine`/`buildPyLoadLine` from loadLine.js.
3. **[High] CS DiD 2021 replication code broken** — CallawayEngine is implemented and validated; the gap is in the rScript.js export mapping; audit `att_gt()` parameter mapping against the R `did` package docs.
4. **[Validation] Browser-validate three code-complete items** — OLS residual plots, PlotBuilder resize, and spatial heatmap are all commit-complete; Franco should clear these quickly to close them out.
5. **[High] Stata workbench diagnosis** — identify the specific Stata tab failure in the Calculate workbench to scope the fix (may be a one-line missing function call or a deeper expression-translation gap).
6. **[Medium] Chi-square display** — the math is already computed in ModelHypothesis.js; only the CoefficientTestPanel.jsx display needs updating.
7. **[Medium] LaTeX table auto-scaling** — `\resizebox` or `landscape` wrapper in latexTable.js; < 10 lines.
8. **[Medium] Arrange in data viewer** — column-header click sort; DuckDB path already supports ORDER BY.
9. **[Medium] Save transform draft state** — FeatureTab.jsx local useState; prevents user frustration from navigation loss.
10. **[Feature] Variable searcher** — pure UI filter on VariableSelector.jsx chips; no backend changes.
