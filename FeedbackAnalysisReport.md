# Feedback Analysis Report — 2026-08-26

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary

- **14 open bugs** · **12 open features** · **0 open UX** · **1 open performance** · **0 open other**
- 3 additional items are code-complete but awaiting browser validation by Franco
- 2 bug-triage rows marked "likely fixed" — require in-browser confirmation before closing

---

## Bugs

---

### [high] · Export · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 (Callaway & Sant'Anna) does not work
**File:** `src/services/export/rScript.js`, `src/math/CallawayEngine.js`
**Suggestion:** `rScript.js` has no `case "CallawayCS":` (or equivalent) in its estimator switch — confirmed by `grep -n "callaway\|CallawayEngine" src/services/export/rScript.js` returning no matches. The R replication block needs to emit `did::att_gt()` + `aggte()` calls wired to the `CallawayEngine` result shape. Python should use `csdid`; Stata lacks a native port so a clear note is appropriate. Follow the pattern of the Sun-Abraham block already in `rScript.js`.
**Invariants:** All export logic must live in `src/services/export/`; `src/math/CallawayEngine.js` is pure JS with no React imports (do not add UI code there).

---

### [high] · Export · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well
**File:** `src/services/export/plotScript.js`, `src/components/PlotBuilder.jsx`
**Suggestion:** `buildGgplot()` / `buildMatplotlibPlot()` / `buildStataPlot()` in `plotScript.js` do not prepend a dataset load line. The geo-plot path already calls `buildRLoadLine` (line 4 of `plotScript.js`). The non-geo `buildGgplot` caller in `PlotBuilder.jsx` should pass the active `dataset` (filename + `loadOpts`) and call `buildRLoadLine`/`buildPyLoadLine` from `loadLine.js` at the top of each language block, matching the model-script pattern.
**Invariants:** None specific — follows the existing `loadLine.js` abstraction already imported in `plotScript.js`.

---

### [high] · Export · 2026-07-17
**Feedback:** Stata workbench doesn't work completely
**File:** `src/services/export/stataScript.js`, `src/components/modeling/CodeEditor.jsx`
**Suggestion:** The bug is vague but likely refers to: (a) estimator branches in `stataScript.js` that fall through to `unknown step` comments, or (b) `CodeEditor.jsx` failing to pass all required spec fields into `transpileModel()`. Run `npm run lint:undef` and check all `stataScript.js` estimator cases — any branch that throws or produces broken syntax should be identified. Pay attention to multi-way FE (`reghdfe`), IV-Poisson, and Callaway & Sant'Anna (Stata lacks a canonical port — emit a clear comment rather than broken code). Adding a smoke test to the existing 168-cell SE/estimator matrix in `stataScript.js`'s `__validation__` that asserts no branch throws would prevent regressions.
**Invariants:** All Anthropic API calls go through `AIService.js` only — `stataScript.js` must not call the API.

---

### [high] · Pipeline · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise — e.g. ATE as difference in means between treatment and control group
**File:** `src/pipeline/runner.js` (`group_summarize`), `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** The CLAUDE.md "key bugs" section documents the root cause: `group_summarize → pivot_wider → mutate` has a silent trap (columns named `0`/`1` dropped by the `mutate` identifier filter). The fix was documented as adding a `splitByGroup` path + a long-format input-mode toggle to `twoSampleMeanTest`. Implement an "ATE" or "between-group difference" mode in `ReshapeTab.jsx` that calls `twoSampleMeanTest` with the split outcome/group columns directly, bypassing the pivot step entirely. No new engine code needed — the math is already in `SampleTests.js`.
**Invariants:** Pipeline steps replay on `rawData` via `runner.js`; any new step must be added to both `runner.js` and `registry.js`.

---

### [medium] · Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`
**Suggestion:** `latexTable.js` currently emits a fixed-width `tabular` environment. For wide tables (> 5–6 columns), switch to `tabularx` with `\textwidth` as the target width, or emit a `\resizebox{\textwidth}{!}{...}` wrapper around the `tabular`. The column count is available from the model array passed to `buildStargazer`. Add a branch: if `models.length > 5`, wrap in `\resizebox`.
**Invariants:** None — pure string generation; no UI or engine code.

---

### [medium] · Math · 2026-07-09
**Feedback:** Add direct elasticity interpretation — display elasticity in the coefficient table
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/AI/Prompts/index.js`
**Suggestion:** The AI narrative in `INTERPRET_REGRESSION_PROMPT` already handles log-log/log-level elasticity narratively. The gap is in the UI: add a computed "Elasticity / Semi-elasticity" column to the coefficient table in `resultDisplay.jsx` that is populated when the spec's functional form is detectable (log-transformed Y or X variables, identified via `result.spec.yVar` / `result.spec.xVars` naming convention `ln_*` or `log_*`). Show `β` (elasticity) for log-log and `β × 100` (% change) for log-level, with a tooltip explaining the formula.
**Invariants:** `src/math/` files must not import React; this calculation belongs in `resultDisplay.jsx` or a helper in `src/services/`.

---

### [medium] · Math · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis tests
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `ModelHypothesis.js` already implements the Wald statistic; add a `method: "chi2"` path that uses the asymptotic χ²(q) distribution (the Wald statistic divided by the number of restrictions, not by `q`, times `q`) instead of the F(q, df) distribution. Wire a toggle in `CoefficientTestPanel.jsx` for "F-test" vs "Chi-square" — the latter is standard for large samples and GMM. Add the χ²-based p-value to the `StatInferenceScript` emitters so R and Python replication code matches.
**Invariants:** `src/math/ModelHypothesis.js` must have zero React imports.

---

### [medium] · Export · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** Change `CoefficientTestPanel.jsx` to maintain a list of hypothesis specs (array of `{label, Rmat, rVec, result}`) rather than a single active one. Add an "Add to list" button that appends the current hypothesis. The R replication emitter in `statInferenceScript.js` should iterate the list and emit a `linearHypothesis(fit, ...)` call per entry. Persisting this list should use `IndexedDB` (via `indexedDB.js`) keyed to the current `pid`, not `localStorage`.
**Invariants:** Persistence must use `indexedDB.js`, never `localStorage`.

---

### [medium] · UI · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`
**Suggestion:** The Plot Builder's Labels panel already provides `title`, `xLabel`, and `yLabel` controls for most geoms. If the Time Series sub-tab in `ExplorerModule.jsx` uses its own SVG path (separate from `PlotBuilder.jsx`), it likely bypasses those controls. Identify the time-series rendering path in `ExplorerModule.jsx` and wire the same `title`/`xLabel`/`yLabel` props that `PlotBuilder` already exposes.
**Invariants:** None — UI-only change.

---

### [medium] · UI · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Extend `artifactOrder.js`'s artifact metadata with an optional `groupId` string. Add a lightweight group-management UI to `ModelBufferBar.jsx`: a "New Group" button creates a named slot, and artifact tiles can be dragged or assigned into groups. Only the active group's artifacts are visible in comparisons. Persist groups in `IndexedDB` alongside `artifactOrder` data.
**Invariants:** Persistence via `indexedDB.js` only.

---

### [medium] · UI · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** `FeatureTab.jsx` holds its `CaseWhenBuilder` state in local `useState`. When the user navigates away (tab switch triggers unmount), this state is lost. Lift the draft state to `WranglingModule.jsx` (or to a `useRef` inside `FeatureTab` that survives tab switches without unmounting by using CSS `display:none` instead of conditional rendering). The simpler option is to serialize the draft to `sessionStorage` keyed by `pid` on every keystroke and restore it on mount.
**Invariants:** Do not use `localStorage` for pipeline/data storage — `sessionStorage` (transient, session-scoped) is acceptable for ephemeral draft state; `IndexedDB` for anything that should survive a page refresh.

---

### [medium] · UI · 2026-07-17
**Feedback:** Add arrange functions in the data viewer
**File:** `src/ExplorerModule.jsx`
**Suggestion:** The Data Viewer already calls `getTablePage` with `ORDER BY` support (see CLAUDE.md DuckDB strategy notes). Wire a column-header click handler that toggles ascending/descending sort for that column and passes the sort key + direction into `getTablePage`'s SQL `ORDER BY` clause. For the JS (non-DuckDB) path, sort `rows` client-side before slicing to the page. Persist the sort state in component `useState`; reset to default on dataset change.
**Invariants:** DuckDB sort must remain server-side (SQL `ORDER BY`) — never pull the full dataset into JS to sort.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line: 1583 — malformed row aborts the entire import
**File:** `src/services/data/parsers/` (CSV path), `src/DataStudio.jsx`
**Suggestion:** Wrap the per-row parsing loop in a try/catch. On parse error, record the failing line number and original text, skip the row, and continue. After parsing, if any rows were skipped, surface a dismissible warning banner in `DataStudio.jsx` listing the count and first 3 offending line numbers. Offer a "Download skipped rows" option (a CSV blob) so the user can inspect them separately. This matches the "resilient-import" pattern noted in BugTriage.
**Invariants:** The raw parsed data must always be treated as the canonical source — do not mutate in place; build a clean array excluding bad rows.

---

### [low] · UI · 2026-07-17
**Feedback:** Guide tour must be updated
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS)
**Suggestion:** Audit `TOUR_STEPS` against the current tab structure (Data / Clean / Explore / Model / Simulate / Calculate / Report / Spatial). Add or update entries for: N-way FE, Callaway & Sant'Anna, Sun-Abraham, Synthetic Control, Spatial pipeline ops, and the PlotBuilder tile/facet geoms. Per CLAUDE.md's working convention: never write a count into tour copy — name the things instead.
**Invariants:** None — copy change only.

---

### [pending browser validation] · UI · 2026-06-10
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete per BugTriage (`35f4b5a6`). Franco should verify the vertical resize handle works and that PNG output respects the chosen dimensions with correct aspect ratio. No further code change anticipated; close if browser-validated.
**Invariants:** None.

---

### [pending browser validation] · UI · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** Code-complete per BugTriage (`35f4b5a6`). Diagnostics now render from frozen `result.spec`. Franco should verify all three plots render correctly for a fresh OLS estimation and that sidebar variable edits do not mutate the displayed plot.
**Invariants:** None.

---

### [pending browser validation] · UI · 2026-06-13
**Feedback:** Heatmap must be fixed
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`
**Suggestion:** Code-complete per BugTriage (`5e105529`). Franco should verify KDE heatmap renders with the correct palette/opacity controls and that the legend displays. No further code change anticipated; close if browser-validated.
**Invariants:** None.

---

## Features

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression
**File:** `src/components/modeling/results/`, `src/math/CausalEngine.js`
**Suggestion:** The RDD engine already computes bandwidth, polynomial order, kernel, n left/right, and the LATE estimate with CI. Build a `RDDSummaryTable` component (alongside `IVResults.jsx`, `PanelResults.jsx`) that renders these in an `rdrobust`-style block: bandwidth, effective n, polynomial order, kernel, bias-corrected estimate, SE, CI, and p-value. Mount it collapsibly below the existing estimate tiles in `ModelingTab.jsx`.
**Invariants:** `src/math/` files must have zero React imports.

---

### Clean / Pipeline · 2026-05-19
**Feedback:** Add filter(`regionname %in% c("Spain","Basque Country")`) for filter option
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/predicate.js`
**Suggestion:** The `IN` / `NOT IN` operators already exist in `predicate.js` (`in`/`nin` operators). The gap is the UI: `FilterBuilder` in `CleanTab.jsx` should expose a multi-value input (comma-separated or tag chips) when the operator is `in`/`nin`, converting to an array that `evalPredicate` and `predicateToSQL` already handle. No engine changes needed.
**Invariants:** `predicate.js` is the single owner of all filter operators — do not re-implement filter logic in the component.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add a filter sidebar to `SpatialGeoPlot.jsx` that composes a `predicate.js` condition tree. On apply, filter the layer's source rows using `evalPredicate` before passing them to `geo.js` for rendering. This mirrors the Clean tab's `FilterBuilder` component, which can be reused directly. Persist the filter spec with the plot entry so pinned plots remember their filter.
**Invariants:** Use `predicate.js`'s `evalPredicate` — do not re-implement filter operators.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c" regardless of actual column names
**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/export/latexTable.js`
**Suggestion:** Add an optional `displayNames` map (varName → label) to the model spec. In `CoefficientTestPanel.jsx` or a dedicated "Edit labels" modal, let the user rename displayed variables. Feed `displayNames` into `buildStargazer` / `latexTable.js` when generating the LaTeX table. Persist `displayNames` as part of the pinned model entry in `IndexedDB`.
**Invariants:** `latexTable.js` is a pure string generator — no React imports.

---

### Calculate · 2026-06-19
**Feedback:** Add limits
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Extend `calcEngine.js` (or the CAS/symbolic layer it uses) with a `limit(expr, var, val, direction)` function using L'Hôpital or epsilon-delta substitution. Expose it in `CalculateTab.jsx` as a new verb alongside `integrate` / `differentiate` / `optimize`, with a direction toggle (from left / from right / two-sided). The LaTeX export already works for other operations; extend it with a `\lim_{x \to a}` template.
**Invariants:** `src/math/` engine files must have zero React imports; all new CAS logic goes there.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** When a defined symbol resolves to a scalar (no free variables), render it as a horizontal rule on the function graph. This is a special case of the existing `hline` / `vline` geom already in `PlotBuilder.jsx` — either reuse that rendering logic directly or add a "constant" detection step in the `CalculateTab` graphing path that injects a horizontal line at `y = constant`. No new engine work needed.
**Invariants:** None.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axes
**File:** File not located — manual investigation needed. Likely `src/components/PlotBuilder.jsx` or a new component.
**Suggestion:** Observable Plot (the current rendering engine) does not support a native Z axis / 3D surface plot. Options: (a) add a 3D scatter / surface tab powered by a CDN-loaded Plotly.js (self-contained, no server egress — acceptable under the privacy-first constraint); (b) restrict to 2.5D (bubble chart encoding a third variable as dot size, already supported via `aes.size`). Option (b) costs nothing and is already available; option (a) requires a new CDN import and a `connect-src` CSP addition. Recommend option (b) with a clear UX label before investing in Plotly.
**Invariants:** All CDN fetches at runtime require a matching `connect-src` entry in `vercel.json` (see the DuckDB-WASM CSP bug history in CLAUDE.md).

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a text `<input>` above the variable chip list in `VariableSelector.jsx` that filters the displayed chips by a case-insensitive substring match on the column name. Use `useState` for the filter text; reset it when the dataset changes. This is a pure UI filter — the underlying variable list and pipeline remain unchanged.
**Invariants:** None — component-local state only.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When a layer has `aes.color` set, the legend currently shows the color swatch next to the category label. Extend the legend in `PlotBuilder.jsx` to optionally show aggregate statistics (count, mean of Y) per group alongside the swatch, toggleable via a "Show stats in legend" checkbox in the Labels panel. This information is already available in the Plot's underlying data array.
**Invariants:** None.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`
**Suggestion:** Each pipeline step entry in `History.jsx` already knows its `type`. Map step types to their originating tab (e.g. `filter` → "Clean", `case_when` → "Feature", `group_summarize` → "Reshape", `join` → "Merge"). Add an onClick handler to the step pill that emits an `onNavigate(tabName)` event up to `WranglingModule.jsx`, which sets the active tab. Optionally, also highlight the relevant UI subsection (e.g. scroll the FilterBuilder into view).
**Invariants:** Navigation events belong in `WranglingModule.jsx` — `History.jsx` should emit, not navigate directly.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a small static lookup table (iso2 ↔ iso3 ↔ country name, ~250 rows, ~15 KB) as a JSON constant in a new `src/services/data/countryLookup.js`. Expose it in `FeatureTab.jsx` as a new "Country Code" transform mode: the user selects an input column (e.g. country name) and a target format (ISO2 / ISO3 / Name), and the step emits an `add_column` step calling a `countryCode(val, "iso3")` helper. Fuzzy matching is optional — exact match covers the common case.
**Invariants:** New step types must be added to both `runner.js` and `registry.js`; pure JS lookup, no React in the data module.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables
**File:** File not located — design decision needed before implementation.
**Suggestion:** Lists (e.g. `["Spain", "Germany", "France"]`) are a cross-module concept: used in `filter` (`IN` operator), `case_when` (condition values), and potentially `if_else`. The natural owner is `sessionState.jsx` (React context already shared across all modules). A `lists` key in session state would hold `{id, name, values[]}` entries, saved to `IndexedDB` under the project `pid`. The `FilterBuilder`'s `IN` chip input (see Country Codes feature above) can include a "Use saved list" picker as a first integration point. Design this before writing code — the cross-module contract is the hard part.
**Invariants:** Persistence via `indexedDB.js`; `sessionState.jsx` is the cross-module context owner.

---

## Performance

---

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** Per CLAUDE.md, Fase X4 is code-complete but browser benchmark is pending Franco. The fix routes large CSV/TSV through DuckDB on parse, reduces the UI preview to 500 rows, and caches via OPFS Parquet. No further code change should be needed; Franco should run the 900k-row benchmark to confirm sub-2s tab transitions and close this item. If the crash still reproduces, the most likely gap is `display limit ≠ computation limit` being violated somewhere — check that no code path passes the full 900k-row JS array to a React component for rendering.
**Invariants:** Never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`; truncation is a display-only concern.

---

## Suggested action order

1. **CS DiD 2021 replication code** (`rScript.js`) — a core estimator with zero export coverage; highest user-visible correctness gap in the export track.
2. **Plot replication code missing dataset load lines** (`plotScript.js`) — affects every PlotBuilder export; simple fix using `buildRLoadLine` already imported in the file.
3. **Stata workbench audit** (`stataScript.js`) — blocks replicate-in-Stata workflows; scope the audit with a lint pass first before writing new code.
4. **group_summarize cross-group math / ATE** (`runner.js`, `ReshapeTab.jsx`) — common econometrics use case; engine (`SampleTests.js`) already handles it, only the UI wiring is missing.
5. **Browser-validate three code-complete items** (Plot Builder resize, Residual plots, Spatial heatmap) — zero code needed if validation passes; clears the backlog of `35f4b5a6`/`5e105529` items.
6. **Variable searcher in VariableSelector** — low effort, high usability gain for users with many columns.
7. **LaTeX table width adjustment** (`latexTable.js`) — self-contained string change, low risk.
8. **case_when draft preservation** (`FeatureTab.jsx`) — high UX value; simplest fix is `sessionStorage` on keystroke.
9. **Chi-square variant for Wald tests** (`ModelHypothesis.js`) — small math addition, needed for GMM/large-sample workflows.
10. **Data viewer sort / arrange** (`ExplorerModule.jsx`) — infrastructure is already in `getTablePage`; needs a header click handler only.
