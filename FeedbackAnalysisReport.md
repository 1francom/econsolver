# Feedback Analysis Report — 2026-05-31

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **9 open bugs** · **9 open features** · **1 open UX/Design** · **1 open performance** · **1 open other**
- Total: **21 open items** analysed
- 6 ClaudeFB.md items are missing ✓ marks but confirmed fixed in BugTriage.md — flagged in the discrepancies section.

---

## Bugs

### critical (needs reproducer) · Model · 2026-05-21
**Feedback:** `**URGENT** There is an error with p-value calculation, t-value = 0.49 but p-value < 0.001, impossible`
**File:** `src/math/LinearEngine.js` (lines 80–87, `pValue` + `tCDF`); `src/components/ModelingTab.jsx` (line 355, `CoeffTable`)
**Suggestion:** The audit trail in BugTriage.md found no actual desync in the 11 engines — `pValue(0.49, 100)` yields ≈ 0.625, which matches R. The most likely cause is a UI misread: the `CoeffTable` column order is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig`; "0.49" would be the *coefficient* column, not the *t* column. Ask Franco for a screenshot with the estimator name before changing any math code. If a real desync is confirmed, inspect how `tStats`/`pVals` are assigned in `EstimationResult.js` (`clean()` at line 251) for the specific estimator — verify that `tStats` and `pVals` always come from the same engine call and that no array index shift exists.
**Invariants:** Any fix must stay inside `src/math/` with zero React imports; SE type must remain an explicit argument.

---

### high · Model · 2026-05-28
**Feedback:** `Redefine the event study model separating in Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators`
**File:** `src/math/PanelEngine.js` (current `runEventStudy` at line 222); `src/components/modeling/EstimatorSidebar.jsx` (estimator dropdown); `src/components/ModelingTab.jsx` (event-study branch)
**Suggestion:** The generic TWFE event study is biased under staggered adoption (Callaway-Goodman-Bacon critique). Add two new engines: (1) **Sun & Abraham (2021)** — interaction-weighted estimator: interact cohort dummies × relative-time dummies, average with cohort shares as weights. (2) **Callaway & Sant'Anna (2021)** — group-time ATT(g,t) estimated via outcome regression or doubly-robust, then aggregated. Both should live in `PanelEngine.js` as `runSunAbraham` and `runCallawaySantanna` (pure JS, zero React). Wire them into `EstimatorSidebar.jsx` as separate entries under the Panel group and add new `EventStudySA` / `EventStudyCS` branches in `ModelingTab.jsx`. The existing `EventStudyPlot` in `ModelPlots.jsx` can be reused — it consumes `result.eventCoefs`.
**Invariants:** Pure JS in `src/math/`; `seType` must be accepted as an explicit argument; do not mutate `rawData`.

---

### high · Data · 2026-05-13
**Feedback:** `.dta files are wrong loaded and readed`
**File:** `src/services/data/parsers/stata.js`
**Suggestion:** The parser supports formats 113–119 but the legacy path (formats 113–116, Stata 8–12) is the less-tested branch. Common failure modes: (a) `strL` (long string) variables in format 117+ may have incorrect byte-offset arithmetic in `parseStrls`; (b) value-label lookup may fail silently if the label name is UTF-8 encoded in format 118/119 — audit the `value_labels` block reader; (c) date display formats (`%td`, `%tm`) convert raw integers to JS `Date` but may be off-by-one on the Stata epoch (1960-01-01). Add a smoke test in `engineValidation.js` or a dedicated `stataValidation.js` that loads a known .dta fixture and checks column count, row count, and at least one date column value.
**Invariants:** Parser is pure JS in `src/services/data/parsers/`; no React imports.

---

### high · Report · 2026-05-25
**Feedback:** `AI does not recognize the pipeline steps in the Script generation`
**File:** `src/ReportingModule.jsx` (line 961, `snapshot` useMemo); `src/services/AI/sessionSnapshot.js` (line 76, `trimPipeline`); `src/services/AI/Prompts/index.js` (`UNIFIED_SCRIPT_PROMPT` rule 8)
**Suggestion:** This matches pending CLAUDE.md item 5b (marked VALIDATION PENDING 2026-05-30). `buildSessionSnapshot` reads `cleanedData?.pipeline` but the `cleanedData` object in `ReportingModule.jsx` arrives via `onComplete` — verify that `WranglingModule` is correctly populating `pipeline` in that payload (it should pass `pipeline` in the `onComplete` arg alongside `loadOpts`). The `snapshot` `useMemo` at line 961 only has `[cleanedData, rawResult]` as deps; if `cleanedData` arrives before the pipeline is fully populated the snapshot will be stale. Add `pipeline` explicitly to the `onComplete` payload in `WranglingModule.jsx` and verify it flows through `DataStudio.jsx` into `ReportingModule.jsx`. Test by loading a `;`-delimited CSV, adding two pipeline steps, then running AI script generation — the R output should call `read_delim(..., delim=";")` and reproduce both steps.
**Invariants:** All AI calls go through `AIService.js`; `SHARED_CONTEXT` cached block must remain present; no raw `fetch` to Anthropic elsewhere.

---

### high · Spatial · 2026-05-20
**Feedback:** `Grids do not work`
**File:** `src/components/tabs/SpatialTab.jsx` (line 1966, `makeGrid` call inside `GridSection`); `src/math/SpatialEngine.js` (line 430, `makeGrid`)
**Suggestion:** `GridSection` calls `makeGrid(wkt, gl.cellsize, gl.clipBorder)` but `gl.clipBorder` is `undefined` when the user hasn't toggled the clip option — the default `true` applies but the WKT may be empty or malformed if no boundary column was selected. Add a guard: if `boundaryWkt` is empty/null, show a clear error message rather than throwing. Additionally audit whether `parseRings` inside `makeGrid` correctly handles WKT coming from the column picker (it may be raw coordinates rather than a WKT string if the user selects a lat/lon pair column by mistake). The 25,000-cell safety cap may also cause silent truncation without user feedback.
**Invariants:** `SpatialEngine.js` is pure JS in `src/math/` — zero React imports. Error surface goes in `SpatialTab.jsx`.

---

### medium · General · 2026-05-20
**Feedback:** `When a dataset is deleted, all variables should disappear from all modules without having to select another dataset`
**File:** `src/components/workspace/DatasetManager.jsx` (line 404, `onRemoveDataset` call); `src/DataStudio.jsx` (line 866, `removeDataset` imperative handle); `src/components/ModelingTab.jsx` (line 1529, `useEffect` on `cleanedData`)
**Suggestion:** When `onRemoveDataset(id)` fires, `DataStudio` switches `activeId` to `primaryId`, which should trigger `onComplete` with the primary dataset's `cleanedData`. That in turn should re-run `ModelingTab`'s `useEffect(() => { setXVars([]); ... }, [cleanedData])`. The bug is likely a race: `setActiveId` is async, so `onComplete` may not fire before the next render cycle — `WranglingModule`'s `cleanedData` stays stale at the deleted dataset's value. Fix: after switching `activeId`, explicitly call `onComplete` with the new primary dataset's cleaned output, or dispatch a `DATASET_SWITCHED` event that `ModelingTab` listens to. Alternatively, pass `activeDatasetId` down to `ModelingTab` and add it to the `useEffect` dep array so any change of active dataset always resets variable selectors.
**Invariants:** Persistence uses `indexedDB.js`; styles use `C` inline; no localStorage.

---

### medium · Model · 2026-05-21
**Feedback:** `When selecting and eliminating variables, the model reestimates automatically — fix that`
**File:** `src/components/ModelingTab.jsx` — variable selector `onChange` handlers; `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Audit `VariableSelector.jsx` and the `onChange` props passed from `ModelingTab` — replace any immediate `estimate()` call with a `specDirty` flag (e.g. `setSpecDirty(true)`) that only triggers a re-run when the user explicitly clicks the Estimate button. The Estimate button should visually indicate the dirty state (e.g. gold border) to signal that the spec has changed since the last run.
**Invariants:** Inline styles via `C`; no CSS files.

---

### medium · Spatial · 2026-05-30
**Feedback:** `Adjust size respectively to the spatial unit(s) — geometry/polygon display sizing not scaling with unit type`
**File:** `src/components/tabs/SpatialTab.jsx` (map rendering section, around line 2600+)
**Suggestion:** The map viewport uses a fixed zoom/bounds rather than fitting to the bounding box of the rendered geometry. After geometry is loaded or a spatial operation completes, compute the union bounding box of all rendered WKT geometries and call `map.fitBounds(bbox, { padding: 20 })`. For the non-Leaflet SVG path in PlotBuilder, set the Observable Plot `projection` domain to the geometry's extent. Also consider auto-scaling stroke width relative to the bounding-box diagonal so polygon borders don't overwhelm small features.
**Invariants:** No external UI libraries; inline styles via `C`.

---

### other · Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47...`
**File:** `src/DataStudio.jsx` (line 314, DuckDB large-CSV path; line 318, small-CSV `parseCSV` path)
**Suggestion:** The "Invalid Input Error: CSV Error" message is a DuckDB error, suggesting the file exceeded the 10MB threshold and went through `loadLargeCSV`. DuckDB's CSV reader is stricter about embedded newlines and multi-byte UTF-8 in numeric columns. Wrap the DuckDB import in a try/catch and fall back to the JS `parseCSV` parser with an auto-detected delimiter. Surface the line number and offending raw bytes in the user-visible error. Also check whether `detectDelimiter` handles the file correctly — World Bank CSV exports sometimes use `,` with quoted numeric fields that contain `,` as thousand separators.
**Invariants:** Parser lives in `DataStudio.jsx` or `src/services/data/parsers/`; no React in parser logic.

---

## Features

### Model · 2026-05-11 — ⚠️ ALREADY IMPLEMENTED
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js` (line 28, `andersonRubinCI`; lines 201–204, called in `run2SLS`); `src/components/ModelingTab.jsx` (lines 907–935, displayed when `second.arCI && xVars.length === 1`)
**Suggestion:** The AR CI is already computed and displayed for the single-endogenous-variable 2SLS case. The feature request may be stale, or the user may not have found the output (it appears in the 2SLS results block below the coefficient table). Consider adding a tooltip or more prominent callout that explicitly labels the block "Weak-IV robust confidence interval (Anderson-Rubin)" to improve discoverability. No new code is needed.
**Invariants:** None.

---

### Model · 2026-05-11
**Feedback:** `Add multiple statistics tests`
**File:** `src/math/LinearEngine.js`; `src/core/diagnostics/`; `src/components/modeling/DiagnosticsPanel.jsx`
**Suggestion:** Priority candidates: (1) **Hausman test** (FE vs RE endogeneity) — implement using the Mundlak auxiliary regression approach; (2) **Sargan–Hansen J test** for instrument validity — already partially in `GMMEngine.js` as `jStat`/`jPval`; surface it in `DiagnosticsPanel.jsx` for 2SLS/GMM results; (3) **Wald / F-test for joint restrictions** — add a user-specified contrast matrix input. All implementations belong in `src/core/diagnostics/` or `src/math/`; surface results in `DiagnosticsPanel.jsx`.
**Invariants:** Pure JS in `src/math/` and `src/core/`; zero React imports in those files.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/SpatialTab.jsx` (PlotBuilder integration); `src/components/PlotBuilder.jsx`
**Suggestion:** Extend the PlotBuilder layer system with a `geom: "grid"` layer type that renders rectangular grid cell outlines using `makeGrid` from `SpatialEngine.js`. Pass the grid WKT array as an Observable Plot `geo` mark with stroke-only fill. The layer config panel should expose cellsize (km) and an optional boundary column. Wire a "Add grid overlay" button in `SpatialTab.jsx` that pre-populates a PlotBuilder grid layer from the current grid result.
**Invariants:** No CSS files; styles via `C`; no React in `SpatialEngine.js`.

---

### Simulate · 2026-05-18
**Feedback:** `Add resampling, bootstrapping, sampling to the mean`
**File:** `src/components/tabs/SimulateTab.jsx`
**Suggestion:** Add a "Bootstrap" sub-mode alongside the existing Monte Carlo mode. The user picks: a data column, a statistic (mean / median / slope / OLS coefficient), number of resamples (B, default 1000), and confidence level (95%). Draw B samples with replacement, compute the statistic each time, display the bootstrap distribution as a histogram (reuse `MCHistogram`), and report the percentile CI. For the regression-coefficient bootstrap, call `runOLS` from `LinearEngine.js` inside the loop. Keep computation in a `useCallback` within `SimulateTab.jsx`; no new dedicated math file is needed for basic bootstrap.
**Invariants:** If a dedicated math helper is added it must have zero React imports.

---

### Explore · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/components/wrangling/CleanTab.jsx` (FilterBuilder section); `src/pipeline/runner.js` (`filter` step handler); `src/pipeline/registry.js`
**Suggestion:** Add an `in` / `not in` operator to the existing `filter` step. In `CleanTab.jsx`, when the user selects the `in` operator, replace the single-value input with a multi-value tag input (enter-to-add chips). In `runner.js`, handle `op: "in"` by checking `allowedValues.includes(String(row[col]))`. `registry.js` needs no changes since the step type remains `filter`. This is a small additive change to existing filter infrastructure.
**Invariants:** `runner.js` replays on `rawData` without mutation; no localStorage.

---

### Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/ModelingTab.jsx` (RDD results render block); `src/components/modeling/ModelPlots.jsx`
**Suggestion:** Render an `rdrobust`-style summary panel after `runSharpRDD` returns, showing: effective observations (left / right of cutoff), bandwidth (h), kernel type, polynomial order, point estimate (LATE), robust SE, 95% CI, and p-value. All values are already present in the `EstimationResult`. Create a `<RDDSummaryTable>` sub-component in `ModelPlots.jsx` or inline in the RDD results section of `ModelingTab.jsx`. Use the `mono` + `C` inline-style pattern matching the existing `CoeffTable`.
**Invariants:** Inline styles only; no external table libraries.

---

### Model · 2026-05-25
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/math/LinearEngine.js` (line 128, `runOLS`); `src/components/modeling/ModelConfiguration.jsx`; `src/services/export/rScript.js`, `stataScript.js`, `pythonScript.js`
**Suggestion:** Add an optional `{ noIntercept: true }` flag to `runOLS`, `runWLS`, and `runOLSFromSuffStats`. When set, skip prepending the ones column in the design matrix and adjust degrees of freedom to `df = n - k` (not `n - k - 1`). In the UI, add a toggle checkbox in `ModelConfiguration.jsx` labeled "No intercept (suppress constant)". Pass the flag through `ModelingTab`'s `estimate()` into the engine call. Update replication scripts: `rScript.js` → `lm(y ~ . - 1, ...)`, `stataScript.js` → `reg y x, noconstant`, `pythonScript.js` → `sm.OLS(y, X)` without `add_constant`.
**Invariants:** `seType` must still be passed explicitly; no React imports in `LinearEngine.js`.

---

### Spatial · 2026-05-30
**Feedback:** `Geometry transformations and show EPSG`
**File:** `src/components/tabs/SpatialTab.jsx` (CRS reprojection section, line 1882+; `PRESET_CRS` at line 122); `src/math/SpatialEngine.js`
**Suggestion:** The proj4 CDN is already loaded lazily and `applyCrs` handles reprojection. The missing pieces: (1) display the detected/user-specified EPSG code as a read-only badge next to the CRS input; (2) expand `PRESET_CRS` with common codes (EPSG:3857 Web Mercator, EPSG:4258 ETRS89, common UTM zones); (3) add pure-JS geometry operations to `SpatialEngine.js` — point buffer (circular polygon), centroid (arithmetic mean of ring vertices), and convex hull (Graham scan) — using existing haversine utilities; surface them in `SpatialTab.jsx` as a "Transform" accordion section.
**Invariants:** `SpatialEngine.js` is pure JS; CDN loads stay in `SpatialTab.jsx`.

---

### Spatial · 2026-05-30
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/SpatialTab.jsx`; `src/pipeline/runner.js`; `src/pipeline/registry.js`
**Suggestion:** Add a `bbox` variant to the existing `filter` step (`op: "bbox"` with `minLat/maxLat/minLon/maxLon` params) in `runner.js` and `registry.js`. In `SpatialTab.jsx`, expose a bounding-box draw UI (two lat/lon input pairs, or a map drag handle) that emits a `filter` step with `op: "bbox"`. The filtered dataset flows back through the pipeline and is available in PlotBuilder via the existing dataset selector. This keeps the non-destructive pipeline invariant without a new step type.
**Invariants:** `runner.js` replays on `rawData`; new `op` variant must be registered in `registry.js`; no mutation of `rawData`.

---

## UX / Design

### Model · 2026-05-21
**Feedback:** `Add an option to clean the current model`
**File:** `src/components/ModelingTab.jsx` (top action bar area); `src/components/modeling/EstimatorSidebar.jsx`
**Suggestion:** Add a "Clear" (×) button near the Estimate button in `ModelingTab.jsx` that resets `yVar`, `xVars`, `wVars`, `zVars`, `result`, `panelFE`, and `panelFD` to their empty/null initial values. Use the existing `Btn` component pattern from `src/components/wrangling/shared.jsx`. Style with a muted `C.red` or `C.border2` color to distinguish it from the primary Estimate action. This is entirely a UI-state reset — no math or pipeline changes needed.
**Invariants:** Inline styles via `C`; no CSS files.

---

## Performance

### Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx` (line 314, DuckDB large-CSV path); `src/services/data/duckdb.js` (Parquet conversion)
**Suggestion:** Per CLAUDE.md agreed rules 2–3: (1) **Auto-convert large CSV to Parquet on first load** — after `loadLargeCSV` succeeds, run `COPY (SELECT * FROM table) TO 'data.parquet' (FORMAT PARQUET)` and re-register the table from the Parquet file; (2) **Persist the Parquet to OPFS** via the existing `opfsCacheKey` mechanism so reopening the project skips re-import; (3) **Audit all `duckdb.query(...)` calls** — the 900k-row crash is most likely caused by materialising the full result as a JS object array somewhere in `WranglingModule.jsx` or the Explorer display path. Ensure every display query returns only a paginated 500-row slice (CLAUDE.md rules 5 + 7).
**Invariants:** Data never leaves the browser; no server-side computation; IndexedDB/OPFS for persistence only.

---

## Discrepancies — BugTriage.md fixed but ClaudeFB.md not marked ✓

The following items are confirmed fixed in BugTriage.md but their ClaudeFB.md entries still lack a ✓ suffix. The feedback-collector agent should back-patch these on its next run:

| ClaudeFB date | Item | BugTriage status |
|---|---|---|
| 2026-05-11 | The graph does not fit the screen | fixed |
| 2026-05-13 | Grids: adjust size of border grids by cutting them | fixed 2026-05-18 (Sutherland-Hodgman) |
| 2026-05-22 | Fix the function graph from 0 | fixed |
| 2026-05-25 | Only 1 equation is plotted | fixed |
| 2026-05-19 | Color references in PlotBuilder for `color = X` | fixed 2026-05-30 |
| 2026-05-19 | Montecarlo for single variables | fixed 2026-05-30 |

---

## Suggested action order

1. **[critical] Confirm or close p-value bug** — request screenshot + estimator name from Franco; if math is genuinely wrong, fix `pValue`/`tCDF` in `LinearEngine.js` lines 80–87.
2. **[high] Sun-Abraham & Callaway-Sant'Anna event study** — implement `runSunAbraham` + `runCallawaySantanna` in `PanelEngine.js`; current generic TWFE event study produces biased ATTs under staggered treatment adoption.
3. **[high] Report AI pipeline recognition (CLAUDE.md item 5b)** — verify `WranglingModule` passes `pipeline` in its `onComplete` payload; validate on Vercel with a `;`-delimited CSV + pipeline steps.
4. **[high] .dta file loading** — audit `parseStrls` byte-offset arithmetic and value-label UTF-8 handling in `stata.js`; add fixture-based smoke test.
5. **[high] Spatial grids broken** — add guard for empty/null `boundaryWkt` in `GridSection` (`SpatialTab.jsx` line 1966); audit `makeGrid` input validation.
6. **[medium] Dataset deletion variable clearing** — ensure `onComplete` fires with primary dataset after `removeDataset` in `DataStudio.jsx` so `ModelingTab` resets selectors.
7. **[medium] Auto-reestimate on variable selection** — introduce `specDirty` flag in `ModelingTab.jsx`; call `estimate()` only on explicit button press.
8. **[medium] Performance: 900k-row crash** — implement CSV → Parquet auto-conversion + OPFS persistence per CLAUDE.md rules 2–3; audit materialisation of full Arrow result in display paths.
9. **[feature] No-intercept option** — small addition to `runOLS`/`runWLS` flag + `ModelConfiguration.jsx` toggle + update 3 replication scripts.
10. **[feature] Bootstrap / resampling** — add Bootstrap sub-mode to `SimulateTab.jsx` reusing `MCHistogram` and calling `runOLS` from `LinearEngine.js`.
