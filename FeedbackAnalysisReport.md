# Feedback Analysis Report — 2026-06-13

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary

- **2 open bugs** · **17 open features** · **0 open UX/Design** · **1 open performance** · **1 open other**
- **21 truly open items** across ClaudeFB.md and BugTriage.md
- **19 housekeeping discrepancies**: items not struck through in ClaudeFB.md that BugTriage records as `fixed`. These need a ~~strikethrough~~ + ✓ pass in ClaudeFB.md to stay in sync.

---

## Bugs

### [high] · Model · 2026-06-11
**Feedback:** `Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS`
**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** `ResidualPlots.jsx` already defines `ResidualVsFitted` and `QQPlot` components. The gap is likely a condition guard in `ModelingTab.jsx` that gates these plots on non-OLS result types (e.g. `result.type === "FE"`). Extend the condition to include `"OLS"` and `"WLS"`. A `Y~Ŷ` scatter is an additional Observable Plot point layer using `result.fittedValues` vs `result.y`; add it as a third plot in `ResidualPlots.jsx`. Confirm `fittedValues` and `residuals` are present on the `EstimationResult` returned by `LinearEngine.js` — if not, compute them there before returning.
**Invariants:** All math (fitted values, residuals) must live in `src/math/LinearEngine.js`, not in the React component. Inline styles only via the `C` object.

---

### [medium] · Model · 2026-06-10
**Feedback:** `Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems`
**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`
**Suggestion:** In `ModelingTab.jsx`, the `PlotBuilder` panel uses a fixed height. Replace with a `resize: "vertical", overflow: "auto"` inline style wrapper div and set `minHeight`/`maxHeight` bounds. For PNG export, `PlotBuilder.jsx` reads dimensions via `ResizeObserver` — pin the export to a fixed `1200×800` canvas independent of the visible panel size by passing explicit `exportWidth`/`exportHeight` props into the `Plot.plot({width, height})` call used inside the PNG path only.
**Invariants:** Inline styles only. No CSS files or Tailwind.

---

## Features

### Model · 2026-05-11
**Feedback:** `Construct Anderson-Rubin confidence intervals, which are valid regardless of instrument strength`
**File:** `src/math/CausalEngine.js`, `src/components/ModelingTab.jsx`, `src/components/modeling/results/IVResults.jsx`
**Suggestion:** Add `runAndersonRubin(data, y, x, z, options)` in `CausalEngine.js`. The AR CI inverts the test: for a grid of β₀ values compute the F-stat of the reduced-form regression of `y − β₀·x` on `z` and controls; the 95% CI is the set of β₀ values where that F-stat falls below the critical value. Expose as an optional toggle in `IVResults.jsx` (`[Show AR CI]` button) that runs the grid inversion and displays the interval alongside the standard 2SLS CI. This is post-estimation display only — no new pipeline step is needed.
**Invariants:** Zero React imports in `CausalEngine.js`. `seType` must still be passed explicitly; AR CI is a separate procedure and does not use a SE type argument internally.

---

### Model · 2026-05-11
**Feedback:** `Add multiple statistics tests`
**File:** `src/math/SampleTests.js`, `src/components/tabs/statsim/SampleTestPanel.jsx`
**Suggestion:** This is under-specified — most likely refers to adding pre-model sample tests. Good candidates: two-sample t-test (independent + paired), Mann-Whitney U, Kolmogorov-Smirnov two-sample, chi-squared goodness-of-fit, and Fisher's exact test. Add the math in `SampleTests.js` (pure JS) and extend `SampleTestPanel.jsx`'s test-type selector with the new entries. Clarify scope with the user before implementing.
**Invariants:** No React imports in `SampleTests.js`. SE type is not applicable — these are hypothesis tests.

---

### Simulate · 2026-05-18
**Feedback:** `Add resampling, bootstrapping, sampling to the mean`
**File:** `src/math/Resampling.js`, `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** `Resampling.js` already exists (permutation is implemented). Add a `bootstrap(data, statFn, B=1000)` function that draws `B` i.i.d. samples with replacement and returns the bootstrap distribution, percentile CI, and SE. Wire it into the Resampling section of `StatWorkspace.jsx` as a new "Bootstrap" mode alongside the existing permutation test. Reuse the `MCHistogram` pattern from `SimulateTab.jsx` for the distribution display.
**Invariants:** No React in `Resampling.js`. Use `indexedDB.js` if results need to be persisted — never localStorage.

---

### Explore · 2026-05-19
**Feedback:** `Add filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")) for filter option`
**File:** `src/components/wrangling/CleanTab.jsx`, `src/pipeline/runner.js`, `src/pipeline/registry.js`
**Suggestion:** Extend the existing `filter` step type to support an `in` operator for string/categorical columns. In `CleanTab.jsx`'s `FilterBuilder`, add an `"is one of"` operator that renders a multi-value chip input for string columns. Step payload: `{ op: "in", values: ["Spain (Espana)", ...] }`. In `runner.js`'s `filter` handler, add `case "in": return values.includes(String(row[col]))`. No new step type is needed in `registry.js` — this extends an existing step.
**Invariants:** Pipeline steps must replay on `rawData` — the filter logic in `runner.js` must be pure and stateless.

---

### Spatial · 2026-05-20
**Feedback:** `Add grids to the plot, not only in the OMS map`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a "Grid overlay" layer option in `GeoLayerConfig.jsx`. When enabled, compute a regular lat/lon grid from the data bounding box (re-using `SpatialEngine.js`'s existing rect/H3 grid logic or a simple degree-interval grid) and render it as SVG `<line>` elements inside `GeoPlotCanvas.jsx`. Expose a configurable grid cell size (degree interval or H3 resolution) in the layer options.
**Invariants:** Inline styles only via the `C` object. No new CSS files.

---

### Model · 2026-05-21
**Feedback:** `Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression`
**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/ModelPlots.jsx`, `src/math/CausalEngine.js`
**Suggestion:** Add a `<RDDSummaryTable>` component (or a new `src/components/modeling/results/RDDResults.jsx`) that renders a block styled after `rdrobust`'s console output: method, kernel, bandwidth, N left/right of cutoff, effective N, polynomial order, LATE, SE, t-stat, p-value, 95% CI. `CausalEngine.js` already returns all these in the `EstimationResult`; the work is purely UI. Render it as a collapsible section below the RDD scatter plot in `ModelPlots.jsx`.
**Invariants:** No math logic in the component. Inline styles via `C`.

---

### Model · 2026-05-25
**Feedback:** `Add an option to estimate without intercept`
**File:** `src/math/LinearEngine.js`, `src/components/ModelingTab.jsx`, `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** Add an optional `noIntercept: boolean` flag to `runOLS`, `runWLS`, and `runFE` in `LinearEngine.js`/`PanelEngine.js`. When true, skip prepending the ones column to the design matrix. Add a checkbox "Suppress constant (no intercept)" in `ModelConfiguration.jsx` and thread the flag through `ModelingTab.jsx`'s `estimate()` into `modelOpts`. Note that R² interpretation changes without an intercept — add a footnote in the result display.
**Invariants:** `seType` must still be passed explicitly. No React imports in `LinearEngine.js` or `PanelEngine.js`.

---

### Spatial · 2026-05-30
**Feedback:** `Geometry transformations and show EPSG`
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx`, `src/components/tabs/spatial/shared/crs.js`
**Suggestion:** `CRSTransformSection.jsx` and `crs.js` already handle projection transforms via `proj4`. Add an EPSG display panel that reads `PRESET_CRS` from `crs.js` and shows the EPSG code and proj4 string for the selected projection. For WKT geometry column reprojection, extend `CRSTransformSection.jsx` to accept a WKT geometry column, parse cells with `wkt.js`, reproject coordinate pairs via `proj4`, and write back reprojected WKT via an `add_column` or `vector_assign` pipeline step.
**Invariants:** Reprojection output must go through a pipeline step (`add_column`/`vector_assign`) in `runner.js` — no in-place mutation of `rawData`.

---

### Spatial · 2026-05-30
**Feedback:** `Allow spatial filters for plots`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`
**Suggestion:** Add a "Spatial filter" collapsible panel in `SpatialGeoPlot.jsx` accepting: (1) a bounding box (min/max lat/lon inputs), and (2) a polygon WKT drawn or pasted by the user. Before rendering, filter rows whose point coordinates fall inside the bounding box/polygon using `SpatialEngine.js`'s existing point-in-polygon logic. Apply to the display layer only — this is not a pipeline step, so `runner.js` is unchanged.
**Invariants:** No mutation of `rawData`. Display-only filtering inside the Spatial tab.

---

### Data · 2026-06-01
**Feedback:** `Add an option to edit/assign column names directly in the Data Viewer`
**File:** `src/ExplorerModule.jsx`
**Suggestion:** Add an inline-editable column header to the data table in `ExplorerModule.jsx`. On double-click of a header cell, replace the label with an `<input>` that commits on `Enter`/blur. On commit, emit a `rename` pipeline step (`{ type: "rename", from: oldName, to: newName }`) via the existing `onPipelineStep` callback so the rename is replay-safe and appears in the audit trail. The `rename` step type already exists in `runner.js`.
**Invariants:** Rename must go through a `rename` pipeline step in `runner.js` — never mutate `rawData` headers directly.

---

### Simulate · 2026-06-02
**Feedback:** `You could add different types of quantiles like in R, but it might not be necessary`
**File:** `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** Low priority. In the distribution/quantile section of `StatWorkspace.jsx`, add a dropdown for quantile interpolation type (types 1–9 matching R's `quantile()` function). Type 7 (R default, linear interpolation) is sufficient for most use cases. Types 4–9 are a small pure-JS addition (interpolation formula only — no new library). Extract the quantile math to a helper function in `src/math/` or `src/core/` to keep it testable.
**Invariants:** Computation must be in a pure helper with no React imports, per `src/math/` and `src/core/` invariants.

---

### Spatial · 2026-06-03
**Feedback:** `Add geometry to plot points, not just lat and lon`
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`, `src/components/tabs/spatial/shared/wkt.js`
**Suggestion:** In `GeoLayerConfig.jsx`, add a "WKT geometry column" selector (auto-detected via `guessWktCol` from `guess.js`). When a WKT column is selected, parse cells in `GeoPlotCanvas.jsx` using the existing `wktToLeaflet`/`parseWktRings` helpers in `wkt.js` and render polygons/lines as SVG paths. The `mkGeoLayer` helper in `geo.js` likely already handles this path — verify and connect it to the column selector.
**Invariants:** Inline styles only. No new CSS files.

---

### Calculate · 2026-06-03
**Feedback:** `Add a "show steps button" for integrals, derivatives and optimizations`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Check which symbolic math library `CalculateTab.jsx` uses. If it exposes step-by-step output (e.g., `nerdamer` has `step()` mode), add a collapsible `<StepBreakdown>` panel rendering intermediate expressions in LaTeX. If the library does not support steps, route the expression to the AI via `callClaude` in `AIService.js` with a prompt requesting a step-by-step derivation in LaTeX format, then render the response in the existing LaTeX renderer.
**Invariants:** All Anthropic API calls must go through `AIService.js` only — never add a raw `fetch` to the Anthropic API inside `CalculateTab.jsx`.

---

### Model · 2026-06-04
**Feedback:** `Persistence across sessions of pinned models`
**File:** `src/services/Persistence/indexedDB.js`, `src/services/modelBuffer.js`, `src/components/ModelingTab.jsx`
**Suggestion:** `indexedDB.js` already has a `model_buffer` store (v9, keyed by `pid`). The gap is that `ModelingTab.jsx` does not call `saveModelBuffer` on pin/unpin and `loadModelBuffer` on mount. Add: on every pin/unpin event, call `saveModelBuffer(pid, pinnedModels.map(trimResult))`; on `ModelingTab` mount call `loadModelBuffer(pid)` and rehydrate `pinnedModels` state. `trimResult` must be used to strip raw data before persisting (privacy + size). Add a note in the UI: "Pinned model estimates persist across sessions; raw data does not."
**Invariants:** Use `indexedDB.js` — never localStorage. Always use `trimResult` before persisting to avoid storing raw data blobs.

---

### Calculate · 2026-06-06
**Feedback:** `Add derivatives with endogenous functions (e.g. f(x) = 2x² − c(x), compute dc/dx symbolically)`
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** This requires the symbolic engine to support user-defined function references. If the current library (e.g. `math.js`) supports it via scope objects, define `c` as a symbolic placeholder and differentiate with respect to `x`. If not, use the AI path: pass the expression to `callClaude` via `AIService.js` with a prompt requesting the symbolic derivative; display the result in the LaTeX panel. Add a distinct "Symbolic ∂" button to differentiate from numerical differentiation.
**Invariants:** All AI calls through `AIService.js` only.

---

### Model · 2026-06-06
**Feedback:** `Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names`
**File:** `src/math/CausalEngine.js` (line ~579), `src/services/export/latexTable.js`, `src/components/modeling/ModelConfiguration.jsx`
**Suggestion:** In `CausalEngine.js:579`, the `varNames` array hardcodes display names. Change defaults to use actual column names (`treatCol`, `runningVar`) with the current descriptive strings as fallbacks for terse names. For the LaTeX table, add an editable label map in `ModelConfiguration.jsx` (collapsible "Variable labels" section with one `<input>` per regressor). Pass the map through `ModelingTab` → `latexTable.js` → `buildStargazer` to replace raw column names with user labels.
**Invariants:** The label map is UI-only metadata — never mutate `EstimationResult.coefNames`. Inline styles via `C` only.

---

### Simulate · 2026-06-08
**Feedback:** `Add studentization for permutation tests`
**File:** `src/math/Resampling.js`, `src/components/tabs/statsim/StatWorkspace.jsx`
**Suggestion:** Add `studentizedPermutation(data, statFn, seFn, B)` in `Resampling.js` where `seFn` computes the within-permutation SE (typically a bootstrap SE of the statistic under each permutation), making the test robust to heteroskedasticity. Wire it into `StatWorkspace.jsx` as a checkbox "Studentize test statistic" on the permutation test panel. Cap at B=199 permutations × 99 inner bootstrap draws for browser performance; consider a `setTimeout`-based chunking approach or a Web Worker for larger B to avoid blocking the main thread.
**Invariants:** No React imports in `Resampling.js`. Long-running computation (B>500) must not block the main thread.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** `Big datasets generate crashes (21MB) over 900k rows`
**File:** `src/DataStudio.jsx`, `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js`
**Suggestion:** The 2026-06-10 fix routed large `.dta` files through DuckDB + OPFS, but large CSV/TSV files still materialise the full parsed array in JS. In `DataStudio.jsx`, apply the same threshold check (~10 MB) to CSV/TSV: route them through `duckdb.js`'s loader and reduce to a 500-row preview for display. Also audit `duckdbRunner.js`'s step coverage — only 11 of 35 step types use SQL; the remaining 24 fall back to JS on the full materialised array and will still OOM at 900k rows. Priority for remaining step SQL push-down: `add_column`, `filter`, `normalize_cats`, `group_transform`, `pivot_longer`.
**Invariants:** Never truncate `rawData` before `runner.js`/`duckdbRunner.js` — display limit ≠ computation limit. No server-side computation (privacy-first). OPFS caching must be used for Parquet persistence per CLAUDE.md rule 3.

---

## Other

### Data · 2026-05-10
**Feedback:** `Parse error: Invalid Input Error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47... (truncated in source)`
**File:** `src/DataStudio.jsx` (CSV parsing branch)
**Suggestion:** This error comes from DuckDB's CSV reader rejecting a row — likely an unquoted embedded comma, a BOM character, or a type mismatch (e.g. a string cell in a numeric column). In `DataStudio.jsx`, wrap the DuckDB CSV import in a try/catch and on failure retry with `AUTO_DETECT=FALSE, IGNORE_ERRORS=TRUE` to load with best-effort and flag bad rows rather than crashing. Surface the offending line number and content to the user in an inline error banner so they can fix it in the source file. Never expose raw DuckDB error strings to the user.
**Invariants:** Retry logic stays in `DataStudio.jsx` (the UI boundary), not inside `duckdb.js`.

---

## Housekeeping note

The following **19 items** appear open in `ClaudeFB.md` (no strikethrough, no ✓) but are recorded as `fixed` in `BugTriage.md`. A housekeeping pass to add ~~strikethrough~~ + ✓ in `ClaudeFB.md` is recommended:

| Date | Module | Item |
|------|--------|------|
| 2026-05-11 | Explore | The graph does not fit the screen |
| 2026-05-13 | Data | .dta files are wrong loaded and readed |
| 2026-05-13 | Spatial | Grids: adjust the size of border grids by cutting them |
| 2026-05-20 | Spatial | Grids do not work |
| 2026-05-20 | General | Dataset deletion — variables should disappear from all modules |
| 2026-05-21 | Model | p-value bug — t=0.49 but p<0.001 (URGENT) |
| 2026-05-21 | Model | Variables auto-reestimate on selection/elimination |
| 2026-05-21 | Model | Add option to clean/reset current model |
| 2026-05-22 | Calculate | Function graph does not start from 0 |
| 2026-05-25 | Calculate | Only 1 equation is plotted |
| 2026-05-19 | Explore | Color references in PlotBuilder for `color = X` |
| 2026-05-19 | Simulate | Monte Carlo for single variables |
| 2026-05-30 | Spatial | Adjust size respectively to the spatial unit(s) |
| 2026-05-31 | Data | Parse error: schools_with_coordinates.xlsx — SheetJS CDN |
| 2026-05-31 | Data | OECD proxy error 406 |
| 2026-06-01 | Data | Not all .dta columns recognized — replicated entries |
| 2026-06-02 | Calculate | Crash on LaTeX export |
| 2026-06-02 | Model | AI interpretation does not adapt to estimator (Poisson) |
| 2026-06-06 | Spatial | Geocoding directions quality (enhancement, not a defect per Franco 2026-06-10) |

---

## Suggested action order

1. **OLS residual plots missing** (`ResidualPlots.jsx` + `ModelingTab.jsx`) — high severity; core diagnostic output expected for every OLS run; likely a one-line condition guard fix.
2. **Plot Builder resize + PNG export** (`PlotBuilder.jsx` + `ModelingTab.jsx`) — medium severity; affects publication-ready output workflow; recently reported.
3. **Big dataset CSV crash** (`DataStudio.jsx`) — high severity for the target audience; route large CSV/TSV through DuckDB same as `.dta`.
4. **Pinned model persistence** (`indexedDB.js` + `modelBuffer.js`) — the IDB store exists but is not read/written on session start/end; likely a one-function wiring gap.
5. **RDD hardcoded variable names in LaTeX table** (`CausalEngine.js:579` + `latexTable.js`) — high-frequency user friction; affects every RDD table export.
6. **Estimate without intercept** (`LinearEngine.js` + `ModelingTab.jsx`) — common econometric need; straightforward flag addition.
7. **Anderson-Rubin CI for 2SLS** (`CausalEngine.js`) — research-critical for IV validity checking; post-estimation display only, no new step type.
8. **Housekeeping: sync ClaudeFB.md strikethroughs** — 19 resolved items still appear open; creates false signal for future analyst runs.
