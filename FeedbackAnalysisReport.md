# Feedback Analysis Report — 2026-07-28

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

---

## Summary

- **10 open bugs** · **13 open features** · **2 open UX** · **1 open performance** · **2 open other**
- **4 additional items** are code-complete but await Franco browser validation (marked ⏳)
- **2 items** (N-way FE, FE interactions) are "likely fixed" per BugTriage — browser confirmation pending

---

## Bugs

---

### [high] · Model · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work.
**File:** `src/math/CallawayEngine.js` · `src/components/ModelingTab.jsx:843` · `src/components/modeling/CodeEditor.jsx:28`
**Suggestion:** `CallawayEngine.js` returns a result with no top-level `spec` property (result object at line 291–314 has `type`, `attgt`, `aggregations`, etc., but no `spec`). `ModelingTab.jsx:843` guards with `if (dispatch?.result?.spec)` before attaching `specExtras`, so `yVar`, `treatCol`, `entityCol`, `timeCol` etc. are never attached. `CodeEditor.jsx:28` then resolves `const spec = result.spec ?? {}`, gets `{}`, and all model fields become empty strings. Fix: in `_runEstimation` (ModelingTab.jsx), detect the `CallawayCS` type and initialise `dispatch.result.spec = {}` before the Object.assign guard, pre-seeding it with the CS-specific fields (`csTreatCol[0]`, `csEntityCol[0]`, `csTimeCol[0]`, `csXCols`, `csEstMethod`, `csAnticipation`, `csBasePeriod`, `csCompGroup`).
**Invariants:** Zero React in math files — fix must stay in ModelingTab.jsx or CodeEditor.jsx only.

---

### [high] · Explore · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well.
**File:** `src/ExplorerModule.jsx:2631` · `src/components/PlotBuilder.jsx:1528`
**Suggestion:** `PlotBuilder` accepts a `scriptPreamble` function prop that prepends a load line to the copied script (see `copyPlotScript` at line 1528). `ExplorerModule.jsx:2631` mounts PlotBuilder without this prop, so no load line is emitted. Fix: write a `scriptPreamble` callback inside ExplorerModule that calls `buildRLoadLine` / `buildPyLoadLine` / `buildStataLoadLine` from `src/services/export/loadLine.js` (same helpers already used in ReportingModule) using the dataset's `filename` and `loadOpts`, and pass it as the `scriptPreamble` prop.
**Invariants:** None — load line builders already exist; this is wiring only.

---

### [high] · Explore · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise — e.g. ATE by taking the difference in means between treatment and control group.
**File:** `src/pipeline/runner.js:528` · `src/components/wrangling/ReshapeTab.jsx`
**Suggestion:** `group_summarize` collapses rows to one-per-group using per-column aggregations (`mean`, `sum`, `count`, `min`, `max`, `sd`, `median`, `quantile`). It cannot express cross-group arithmetic (e.g. `mean(Y | T=1) − mean(Y | T=0)`) because there is no post-collapse mutate stage. Options: (a) add an optional `derived` array to the step spec — entries `{ name, expr }` evaluated after grouping using the existing `mutate` expression evaluator in `runner.js`; (b) expose a UI hint in ReshapeTab documenting the "group_summarize then mutate + pivot_wider" workaround. Option (a) is cleaner and more discoverable.
**Invariants:** Non-destructive pipeline — step must replay on `rawData` without mutation.

---

### [high] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely.
**File:** `src/components/calculate/workbench/exportScript.js:79`
**Suggestion:** `buildStata` generates Mata blocks with print calls like `("d${name}/d${ax} @midpoint", (f(xm+h)-f(xm-h))/(2*h))` (line 98) and `("integral", sI*h)` (line 103). These are not valid Mata display statements — bare parenthesised tuples do not print in Mata's non-interactive mode; use `printf("%s %9.6g\n", "label", value)` instead. Additionally, each equations's `mata:` block at line 92 needs `mata end` to close the session (the current `end` on line 112 closes the function definition, not the session, when the Mata section spans multiple equations). Check that scalar injection (line 94, `st_numscalar(name)`) is syntactically inside the function body's `{...}` braces.
**Invariants:** None — export file only.

---

### [medium] · General · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases.
**File:** `src/services/export/latexTable.js:168`
**Suggestion:** `latexTable.js` emits a bare `\begin{tabular}` with no width constraint; wide tables overflow the page margin. Fix: wrap the `tabular` environment with `\adjustbox{max width=\linewidth}{...}` (add `\usepackage{adjustbox}` to the generated preamble comment), or switch the first text column to `tabularx` with `X` column type. Both approaches are standard and compatible with most journal templates.
**Invariants:** None.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R.
**File:** `src/math/ModelHypothesis.js` · `src/components/modeling/CoefficientTestPanel.jsx`
**Suggestion:** `CoefficientTestPanel` currently runs a single joint Wald test. Extend to maintain an array of saved tests (each with constraint vector + label) in component state. Display all saved tests in a results list. The R export should emit one `car::linearHypothesis(fit, ...)` call per saved test, inheriting the model's `seType`/vcov to match the platform's SE choice.
**Invariants:** SE type must be passed explicitly — the R export must honour `seType` (not default to classical).

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.
**File:** `src/ExplorerModule.jsx` · `src/components/PlotBuilder.jsx`
**Suggestion:** PlotBuilder already has `title`/`xLabel`/`yLabel` state (lines 1371–1373). Identify which code path in ExplorerModule renders the time-series panel — if it uses a bespoke SVG chart rather than PlotBuilder, add a label row (`<input>` for title, x-axis label, y-axis label) above that chart and thread them into the SVG render. If ExplorerModule's time-series tab already delegates to PlotBuilder, confirm the label inputs are visible in that tab's layout.
**Invariants:** Styles via `C` object; no external UI libraries.

---

### [medium] · Model · 2026-07-17
**Feedback:** Add chi-square for linearhypothesis.
**File:** `src/math/ModelHypothesis.js:178` · `src/components/modeling/CoefficientTestPanel.jsx:211`
**Suggestion:** `ModelHypothesis.js` already computes `chiSq` and `chiSqPval` (lines 203–213), and `CoefficientTestPanel.jsx` renders chiSq at line 211. This may be a UX discoverability issue rather than a missing feature — the chi-square row might be visually de-emphasised or hidden in a collapsed section. Verify the chi-square block is visible by default. If the user wants to choose chi-square vs. F as the primary test statistic, add a chip toggle in the panel header (`F-stat | χ²`).
**Invariants:** None.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing.
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** FeatureTab's in-progress draft state (case_when rows, partial formula text) is lost on unmount when the user navigates to another tab. Fix: hoist the draft to a `useRef` in `WranglingModule.jsx` (no re-render cost) and restore it on FeatureTab remount, keyed by the active sub-panel type. Alternatively, use `sessionStorage` keyed by `pid + subPanelId`.
**Invariants:** Persistence must use IndexedDB or in-memory state — not `localStorage` for pipeline/data storage. `sessionStorage` is acceptable for transient UI draft state.

---

### [medium] · Data · 2026-07-17
**Feedback:** Add arrange functions in the data viewer.
**File:** `src/App.jsx:414` (DataViewer component)
**Suggestion:** `DataViewer` has no sort state. Add `sortCol`/`sortDir` state; clicking a column header cycles `null → asc → desc`. For DuckDB-backed data, append `ORDER BY col ASC/DESC` to `fetchDuckDBPage` queries. For JS-backed data, sort the 500-row preview array in-memory. Display a small sort arrow on the active header. This is a display-only affordance — no `arrange` pipeline step is emitted.
**Invariants:** Display limit ≠ computation limit. Do not emit a pipeline step; this is a viewer convenience only.

---

### [medium] · Data · 2026-05-10 (fix-later)
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line 1583 — malformed row aborts the full import.
**File:** `src/DataStudio.jsx` · `src/services/data/parsers/`
**Suggestion:** Catch row-level parse errors in the CSV path (both DuckDB and JS reader), collect the offending row indices, and offer the user a preview of bad rows with skip/repair/truncate options. BugTriage labels this "fix-later / post-launch resilient-import pass" — tag accordingly.
**Invariants:** None.

---

## Code-complete, browser validation pending ⏳

These items are implemented in code but have not yet been browser-validated by Franco.

---

### ⏳ [medium] · Model · 2026-06-10
**Feedback:** Plot Builder in ModelingTab is not resizable; PNG export has size/aesthetic problems.
**File:** `src/components/PlotBuilder.jsx` · `src/components/ModelingTab.jsx`
**Suggestion:** Commit `35f4b5a6` claims a native vertical resize handle and bounded canvas sizing for PNG. **Franco: test drag-to-resize in ModelingTab, PNG download at custom dimensions. Validate no regression in ExplorerModule's PlotBuilder.**

---

### ⏳ [high] · Model · 2026-06-11
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS.
**File:** `src/components/modeling/ResidualPlots.jsx` · `src/components/ModelingTab.jsx`
**Suggestion:** Commit `35f4b5a6` claims plots were wired but a stale-result bug prevented display; now frozen to `result.spec`. **Franco: run OLS, confirm all three diagnostic plots appear; add/remove a variable without re-estimating — plots must not mutate.**

---

### ⏳ [medium] · Spatial · 2026-06-13
**Feedback:** Heatmap must be fixed.
**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`
**Suggestion:** Commit `5e105529` claims heatmap restored as a Leaflet layer with KDE cells and palette controls. **Franco: load a point dataset, open Map tab, add a Heatmap layer, verify render and controls.**

---

### ⏳ [medium] · Model · 2026-06-14
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables without re-estimation.
**File:** `src/components/ModelingTab.jsx`
**Suggestion:** Commit `35f4b5a6` claims results are frozen to `result.spec`. **Franco: estimate, change the variable selector without re-estimating, confirm plots stay frozen; restore a pinned model, confirm the original plots return.**

---

## Features

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform.
**File:** `src/components/wrangling/FeatureTab.jsx`
**Suggestion:** Add a "Country Code" sub-panel to FeatureTab alongside the existing log/z-score/lag transforms. Bundle a static `iso2↔iso3↔name` lookup table (~250 entries, ~15 kB) as a JS object. User picks source column, source format, and target format. Emit a `mutate` step with an inline lookup. Fuzzy name-matching can reuse `fuzzyGroups` from `wrangling/utils.js`.
**Invariants:** Emit a standard pipeline step (`mutate`) — no new step type needed.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary(rdrobust(...))`.
**File:** `src/math/CausalEngine.js` · `src/components/modeling/`
**Suggestion:** `runSharpRDD` already returns bandwidth, kernel, polynomial order, n-eff left/right, LATE, SE, and CIs. Add a compact summary table panel below the coefficient table mirroring `summary.rdrobust`'s layout. Display-only change in the results render component.
**Invariants:** None.

---

### Clean · 2026-05-19 (fix-later)
**Feedback:** Add filter syntax `regionname %in% c("Spain", "Basque Country")`.
**File:** `src/components/wrangling/CleanTab.jsx`
**Suggestion:** Add an `in list` operator to the filter builder. User provides a comma-separated value string; runtime evaluates `values.includes(row[col])`. Emits a `filter` step with `op: "in"` and a `values` array.
**Invariants:** Non-destructive pipeline — must replay on `rawData`.

---

### Spatial · 2026-05-30 (fix-later, deferred V4)
**Feedback:** Allow spatial filters for plots.
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`
**Suggestion:** Add lat/lon bounding-box inputs in the spatial plot sidebar that filter the layer data before rendering, without requiring a spatial join pipeline step. Deferred to V4 per BugTriage.
**Invariants:** None — pure display layer.

---

### Model · 2026-06-14 (fix-later)
**Feedback:** Allow users to edit LaTeX table variable names.
**File:** `src/services/export/latexTable.js` · `src/ReportingModule.jsx`
**Suggestion:** Add a "Display labels" metadata editor in the ReportingModule's LaTeX tab where users can override the auto-generated row label for each coefficient. Store overrides in model buffer metadata. Feed them into `buildStargazer` / `latexTable.js` as a `labels` mapping.
**Invariants:** None — metadata only.

---

### Calculate · 2026-06-19
**Feedback:** Add limits to the calculator.
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Add a "Limits" entry to the calculator's operation menu. Use the existing symbolic/CAS engine for closed-form limits; fall back to `(f(x0+ε) − f(x0-ε)) / 2ε` for numeric approximation. R export: `Deriv::Limit()` or `calculus::limit()`.
**Invariants:** None.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (horizontal line for a constant alongside function curves).
**File:** `src/components/tabs/CalculateTab.jsx`
**Suggestion:** Recognise a constant expression (no variable dependence) in the equation workbench and render it as a horizontal line using `Plot.ruleY([c])`. In the ggplot export, emit `geom_hline(yintercept = c)`.
**Invariants:** None.

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axes.
**File:** `src/ExplorerModule.jsx` · `src/components/PlotBuilder.jsx`
**Suggestion:** For a dual-y axis (most likely use case), Observable Plot 0.6 supports a right-side y axis via a second `y` scale with `axis: "right"`. Add an "Axis" dropdown per layer in PlotBuilder (left / right). In the ggplot export, use `scale_y_continuous(sec.axis = sec_axis(...))`. Clarify with Franco before building whether 3-axis means dual-y or true 3D.
**Invariants:** None.

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider so users can type and find variables faster.
**File:** `src/components/modeling/VariableSelector.jsx`
**Suggestion:** Add a small filter `<input>` at the top of each variable panel (Y, X, Controls) that filters displayed chips to those matching the typed string (case-insensitive). State is local; does not affect the model spec.
**Invariants:** Styles via `C` object; no external UI libraries.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color.
**File:** `src/components/PlotBuilder.jsx`
**Suggestion:** When `aes.color` is set, show the group's `n` (or aggregate value) next to each color swatch in the legend. In Observable Plot, append a text mark near each legend entry. In the R export, use `ggplot2::geom_label` or `stat_summary` for data labels.
**Invariants:** None.

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically.
**File:** `src/components/wrangling/History.jsx` · `src/WranglingModule.jsx`
**Suggestion:** Each step entry in History has a `type` field. Add a step-type→tab/subtab routing map in WranglingModule. On step click in History, call this router to navigate the UI to the correct panel (e.g. `log` → FeatureTab → Transforms; `filter` → CleanTab → Filter Builder). Navigation only — does not replay the pipeline.
**Invariants:** Non-destructive — UI navigation only.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables.
**File:** Needs design — `src/services/session/sessionState.jsx` is the natural home.
**Suggestion:** Add a `lists` map (`name → string[]`) to session state in `sessionState.jsx`. Expose a "Session Lists" panel in the DatasetManager or workspace bar. Integrate list names as an `in_list` operator in the filter builder and FeatureTab's case_when conditions. Persist per-`pid` via `indexedDB.js`. Design scope (session-only vs. persisted, text vs. numeric ranges) should be confirmed with Franco first.
**Invariants:** Persistence via `indexedDB.js` — not `localStorage`.

---

## UX / Design

---

### General · 2026-07-17
**Feedback:** Guide tour must be updated.
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array)
**Suggestion:** Review the current 9 TOUR_STEPS against the live UI for stale selectors/descriptions. Add steps for the most-used new features: spatial tab, report AI script, plot builder, model buffer bar, CS DiD. Keep total steps ≤ 12.
**Invariants:** Styles via `C` object.

---

### General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps so not everything is compared at the same time.
**File:** `src/services/Persistence/artifactOrder.js` · `src/components/modeling/ModelBufferBar.jsx`
**Suggestion:** Add a `groups` record (`name → artifactId[]`) to the persisted artifact order schema in `artifactOrder.js`. `ModelBufferBar` gains a "New Group" button and drag-to-group affordance. When comparing, only the active group's artifacts are included.
**Invariants:** Persistence via `indexedDB.js` (already the store for artifact order).

---

## Performance

---

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21 MB / 900k rows).
**File:** `src/DataStudio.jsx` · `src/pipeline/duckdbRunner.js` · `src/services/data/duckdb.js`
**Suggestion:** Fase X4 is code-complete per CLAUDE.md. Outstanding work is **browser benchmark validation**: open a 900k-row CSV, confirm ⚡ DuckDB badge appears, tab-switch to Model and Explore with no freeze or crash, verify OPFS Parquet cache persists across reload. If still crashing: profile in DevTools, look for JS object materialisation of the full Arrow result (DuckDB-as-data-boundary invariant violation), and enforce a virtualised row cap in DataViewer.
**Invariants:** DuckDB as data boundary — never materialise the full Arrow result into JS objects.

---

## Other

---

### [low] · 2026-07-19
**Feedback:** Session lists (lists saved cross-module for filtering/variable construction) — design item, not started.
**File:** See Features section for suggested approach.
**Suggestion:** Design session with Franco needed to confirm scope before building.

---

### [informational] · Security · 2026-06-13
**Feedback:** Two XSS injection attempts via feedback form.
**File:** Supabase feedback ingestion path.
**Suggestion:** BugTriage marks this fixed (2026-06-14, sanitize-on-write). Outstanding hardening: a **server-side Supabase check constraint or trigger** validating `feedback.description` at the DB layer, since client sanitization is bypassable by direct POST. Franco action — no client codebase change needed.

---

## Suggested action order

1. **Fix CS DiD 2021 replication** — `ModelingTab.jsx:843`: initialise `spec = {}` on CallawayCS result and pre-seed CS fields before the `Object.assign` guard. Highest-impact one-line bug: blocks every staggered-DiD user from copying usable R code.
2. **Add dataset load lines to PlotBuilder replication scripts** — Pass `scriptPreamble` using `buildRLoadLine` / `buildPyLoadLine` into `ExplorerModule.jsx:2631`. Exported plot scripts are currently unrunnable without the load line.
3. **Browser-validate 4 code-complete items** (Franco action): PlotBuilder resize/PNG (`35f4b5a6`), OLS diagnostic plots (`35f4b5a6`), spatial heatmap (`5e105529`), frozen diagnostics (`35f4b5a6`). Closes 4 open items with zero code changes.
4. **Fix Stata workbench export** — Replace bare tuple print calls with `printf(...)` in `exportScript.js:97–111`; ensure `mata end` closes the Mata session correctly. Straightforward syntax fix.
5. **Fix LaTeX table width overflow** — Wrap `tabular` with `\adjustbox{max width=\linewidth}` in `latexTable.js:168`. One-line change, high quality-of-life for multi-model reporting.
6. **Add sort/arrange to DataViewer** — Add `sortCol`/`sortDir` state in `App.jsx:414` DataViewer; DuckDB path uses `ORDER BY` in page queries; JS path sorts the 500-row preview.
7. **Fix group_summarize cross-group math** — Add optional `derived` array to `group_summarize` spec, evaluated post-collapse using the existing `mutate` evaluator in `runner.js`.
8. **Validate N-way FE and FE interactions** — BugTriage marks both "likely fixed in #104". Franco: browser-confirm FE with country/sector categorical variables and FE with `*` interaction terms.
9. **Performance browser benchmark** — Franco: open a 21 MB / 900k-row CSV, confirm ⚡ badge and OPFS Parquet cache. Closes the oldest open item.
10. **Variable searcher in model sidebar** — Add filter `<input>` to `VariableSelector.jsx`. Low effort, high usability gain for wide datasets.
