# Feedback Analysis Report — 2026-09-07

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **11 open bugs** · **14 open features** · **0 open UX** · **1 open performance** · **0 open other**
- **6 items code-complete but pending browser validation by Franco**

---

## Bugs

### [high] · Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely  
**File:** `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js`  
**Suggestion:** Audit the Stata code-generation path end-to-end for the currently available estimators. The most likely failure modes are: (1) `seType` variants (clustered/two-way/HAC) emitting incorrect Stata `vce()` syntax after the 2026-07-18 SE overhaul — `xtreg` and `areg` only accept one cluster variable and may have been left emitting two-variable `vce(cluster a b)` instead of routing to `reghdfe`; (2) `ibn.`/`ib#.` factor-coding for no-intercept models (marked UNVERIFIED in CLAUDE.md); (3) the multi-model/subset Stata script generators not receiving updated `seType`/`clusterVar` after the July pass. Run each estimator's Stata tab in CodeEditor against a known dataset and collect every line that Stata reports as a syntax error or produces wrong output.  
**Invariants:** stataScript.js must not add raw `fetch` to Anthropic API; SE type must be passed explicitly, not hardcoded.

---

### [high] · Simulate · 2026-07-28
**Feedback:** Update the replication script, currently it replicates only the DGP  
**File:** `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (script generators)  
**Suggestion:** The Simulate tab's "Copy replication code" path likely only serializes the DGP draw parameters from `dgpScript.js`, not the downstream pipeline (resampling/sample tests/estimation) that the user configures. Extend the script generator to inspect the currently-active Simulate sub-tab (DGP params → resampling → sample tests → MonteCarlo) and compose a full end-to-end R/Python/Stata script: data generation with the exact seed and parameter values, followed by the selected analysis. The existing `generateCleanScript` pattern (pipeline steps in order) is the model to follow; `StatWorkspace` state and `SampleTestPanel` config need to be serialized into the snapshot.  
**Invariants:** Zero React imports in `src/math/`; all Anthropic calls go through `AIService.js` only; persistence via IndexedDB not localStorage.

---

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: CSV Error on Line: 1583 · Original Line: China,CHN,2010,2425.47… (truncated in source)  
**File:** `src/DataStudio.jsx`, `src/services/data/parsers/`  
**Suggestion:** A malformed or truncated row (e.g., a row whose last field is cut mid-value) causes the entire import to abort with a raw DuckDB/parse error. BugTriage marks this as fix-later with the note "resilient-import UX after launch." The fix is to surface the failing row number and raw content in a human-readable banner ("Row 1583 could not be parsed — skip and continue?") rather than aborting the import, following the existing `describeLoadError()` pattern. DuckDB's `read_csv` accepts `ignore_errors=TRUE` to skip malformed rows and return a count; the UI can offer a "skip bad rows" option.  
**Invariants:** Privacy-first — no data sent server-side; DuckDB queries stay client-side.

---

### [medium] · General/Export · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases  
**File:** `src/services/export/latexTable.js`, `src/ReportingModule.jsx`  
**Suggestion:** When a model comparison table has many covariates or many model columns, the LaTeX output overflows the text width. Add a column-count threshold (e.g., >5 columns or >8 rows) that automatically switches from `tabular` to `tabularx` with a `\textwidth`-relative `X` column, or inserts a `\resizebox{\textwidth}{!}{…}` wrapper around the `tabular` environment. Both are standard LaTeX patterns used by `modelsummary`; check what `buildStargazer()` currently emits and add the `tabularx` or `resizebox` path as a conditional. Do NOT change the math; this is a pure formatting/export change.  
**Invariants:** None — latexTable.js is a pure string builder with no React or math imports.

---

### [medium] · Model · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R  
**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`  
**Suggestion:** The current `CoefficientTestPanel` runs one hypothesis at a time with no persistent list. Add a "Saved tests" accumulator: each test run appends a row (`{label, Rbeta, R, q, F, chi2, p, seType}`) to a state array; a mini-table displays saved tests; and the "Copy R" / "Copy Stata" / "Copy Python" buttons produce a script that runs ALL saved tests in a loop (using `car::linearHypothesis` for R, `test` for Stata, `wald_test` for Python). Saved tests should persist in `result.savedTests` in the pinned model record so they survive reload via IndexedDB. One owner for the replication script: `generateJointHypothesisScript()` already exists in `ModelHypothesis.js` and needs to be extended to accept a list rather than a single hypothesis.  
**Invariants:** SE type must be threaded explicitly; persistence via IndexedDB, not localStorage.

---

### [medium] · Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.  
**File:** `src/ExplorerModule.jsx` (time-series sub-tab, around lines 1306–1430)  
**Suggestion:** The time-series line chart currently renders with no controls for title, y-axis label, or x-axis label. Add three optional text inputs to the time-series config panel (beside the existing `yCol`/`tCol`/`grpCol` selectors): "Chart title", "Y-axis label", "X-axis label". These should be passed to `SvgLineChart` (or its Observable Plot equivalent) as SVG `<text>` marks, matching how `PlotBuilder`'s label panel already works. The pin/compare payload should persist these fields so pinned time-series plots re-render with the user-set labels.  
**Invariants:** No external UI libraries; inline styles via the `C` object from `shared.jsx`.

---

### [medium] · Model · 2026-07-09
**Feedback:** Add direct elasticity interpretation  
**File:** `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT)  
**Suggestion:** The AI narrative does not currently distinguish level-level, log-level, level-log, and log-log specifications and so misses the opportunity to quote the elasticity directly (e.g., "a 1% increase in X is associated with a β% change in Y"). Extend `INTERPRET_REGRESSION_PROMPT`'s ESTIMATOR TYPE RULES block with a new section: if both `y` and relevant `x` columns share a `log_` prefix (detectable from `varNames` in the result), label the coefficient as a "percentage-to-percentage elasticity." For log-level and level-log add the standard semi-elasticity phrasing. This is a prompt-only change; no math files are touched.  
**Invariants:** All Anthropic calls go through `AIService.js`; prompts live in `services/AI/Prompts/index.js`; `SHARED_CONTEXT` must remain the cached block on every call.

---

### [medium] · General · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time and users can group them by their criteria  
**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/modelBuffer.js`  
**Suggestion:** Extend `artifactOrder.js`'s `getArtifactOrder`/`saveArtifactOrder` schema to include an optional `groupLabel` field per artifact id. Add a "Group" chip to `ModelBufferBar`'s per-model toolbar that opens a small text input (inline rename pattern, like the existing dataset rename). The model comparison filter (`compRows` in `ModelingTab`) then offers a group selector so only the chosen group's pinned models feed the G13 coefficient comparison. IndexedDB persistence is already wired via the `model_buffer` store; the group label needs to be included in `trimResult`'s projection so it survives reload.  
**Invariants:** Persistence via IndexedDB (`indexedDB.js`), not localStorage; SE type passed explicitly.

---

### [medium] · Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases  
**File:** `src/components/wrangling/FeatureTab.jsx`  
**Suggestion:** `FeatureTab.jsx` holds `case_when` state (cases array, output column name) in local `useState`, which is lost whenever the parent `WranglingModule` re-renders with a different tab or the user navigates away. Persist the in-progress `case_when` cases and the current `mutate`/`if_else` expression to `sessionStorage`, keyed by `pid + ":" + datasetId`. Restore on mount. This is intentionally not IndexedDB (these are ephemeral drafts, not committed pipeline steps); `sessionStorage` is the right tier here — it survives tab navigation but not a reload, which is exactly what "coming back to continue" means. Pattern is already established by `SubsetManager`'s `sessionStorage` usage.  
**Invariants:** Pipeline steps replay on rawData (runner.js) — this is only for draft state, NOT a new step type. No React in math files.

---

### [medium] · Explore · 2026-07-28
**Feedback:** References of the group in time series doesn't work  
**File:** `src/ExplorerModule.jsx` (`aggregateTimeSeries` and `fetchAggregateTimeSeriesSQL`, lines 123–170; `grpCol` state, line 1306)  
**Suggestion:** The `grpCol` grouping in the time-series chart has two paths: a JS path (`aggregateTimeSeries`) and a SQL path (`fetchAggregateTimeSeriesSQL`). The failure is likely in the SQL path — when `grpCol` is set, the CTE groups by `(grpCol, tCol)` but the returned `grp` key may be mismapped to series labels, or the DuckDB query may fail silently when `grpCol` contains special characters or `NULL` values. Check: (1) does `String(r.grp ?? "")` in the SQL path handle null groups the same as `String(r[grpCol] ?? "")` in the JS path; (2) is there a race condition between the `grpCol` state update and the DuckDB fetch effect; (3) does the chart's `SvgMultiLineChart`/Observable Plot rendering actually receive the multi-series structure when the SQL path runs?  
**Invariants:** No React in math files; src/core/ is pure JS.

---

### [low] · General · 2026-07-17
**Feedback:** Guide tour must be updated  
**File:** `src/components/HelpSystem.jsx` (TOUR_STEPS array, line 11+)  
**Suggestion:** The tour was written before several features shipped (N-way FE, Callaway-Sant'Anna, CS DiD, geom_tile, facet_wrap, Sun & Abraham, the Pipeline hide toggle, the Distinct Values panel, and the group_summarize two-sample test). Audit `TOUR_STEPS` against the current module list in `WorkspaceBar.jsx` and the estimator list in `EstimatorSidebar.jsx`; add or update steps for any tab or major estimator not currently mentioned. Per CLAUDE.md convention: never write a count into tour copy — name the things, don't count them.  
**Invariants:** No external UI libraries; inline styles via `C` from `shared.jsx`.

---

## Features

### Calculate · 2026-06-19
**Feedback:** Add limits  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** Add a `lim(expr, var, value)` function to the calculator's CAS/expression evaluator. The most practical approach is to integrate with the existing symbolic engine (if it already wraps a CAS like `algebrite` or `nerdamer`) by exposing a `limit()` call, or to implement one-sided numeric limits via repeated evaluation approaching the target value from left and right. Surface it in the function palette alongside `diff` and `integrate`.  
**Invariants:** Zero React imports in `src/math/`; all styles inline via `C`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)  
**File:** `src/components/tabs/CalculateTab.jsx`  
**Suggestion:** Constants (e.g., `c = 5`) are already supported in the expression evaluator; the gap is rendering them as a horizontal line on the function graph. When the user defines a constant and adds it to the plot, treat it as `f(x) = c` (a degenerate function). The `hline` geom already exists in `PlotBuilder.jsx` — extract the rendering logic or call it directly. The replication export should emit `geom_hline(yintercept = c)` in R.  
**Invariants:** No CSS files; no Tailwind; inline styles only.

---

### Explore · 2026-05-19
**Feedback:** Add `filter(regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)"))` for filter option  
**File:** `src/pipeline/predicate.js`, `src/components/wrangling/CleanTab.jsx`  
**Suggestion:** The `in` operator already exists in `predicate.js` (canonical operator `"in"`, `normalizeOp` maps it from `"in"` and `"nin"`). The gap is that the filter UI (`FilterBuilder` / `CleanTab`) does not offer a multi-value input widget for `in`/`nin`. Add a tag-input or comma-separated text field that appears when the user selects the "in" or "not in" operator, converting the comma-separated input into an array for `cond.value`. `predicateToSQL` already handles arrays for `in`/`nin`.  
**Invariants:** `predicate.js` is the single owner of the condition language — never re-implement a filter operator anywhere else.

---

### Explore · 2026-07-11
**Feedback:** Add more labels in tables or boxes when plotting by color  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** When `aes.color` is set to a categorical column, the color legend currently shows raw column values. Add an optional "Legend title" text input in the Labels panel (already exists for x/y axis labels) that is passed to Observable Plot's `color: { legend: true, label: "..." }` option. Additionally, for bar charts grouped by color, show count/percentage labels inside or above each bar segment when the `showValues` toggle is on — extend the existing `bar` value-label logic to support stacked/grouped configurations.  
**Invariants:** No external UI libraries; the PlotBuilder must not introduce a new CDN dependency.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx` or a new `RDDSummary.jsx`  
**Suggestion:** `runSharpRDD` already returns the bandwidth (`h`), kernel, polynomial order, effective sample sizes, and the LATE estimate. Add a compact summary panel below the RDD coefficient table (mirroring `rdrobust`'s output format) showing: cutoff, kernel, polynomial order, bandwidth (h), N left/right, and the bias-corrected/conventional/robust confidence intervals when available. This is a display-only change — all values already exist in `EstimationResult`.  
**Invariants:** No React in `src/math/`; display components live in `src/components/modeling/`.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names  
**File:** `src/ReportingModule.jsx`, `src/services/export/latexTable.js`  
**Suggestion:** The hardcoded RDD row labels come from wherever `buildStargazer`/`buildLatexTable` maps coefficient names to display strings. Add an editable `varLabels` map (a JSON object `{ colName: displayName }`) that is pre-populated with auto-generated labels (the column name itself, or the RDD convention for RDD models) but is fully user-editable via a small inline table in the Report module. This map is serialized into `spec` and stored per pinned model so labels persist. The exporters already accept a `varNames` / `coefLabels` concept — wire the user's map there.  
**Invariants:** Spec → export needs the THREE whitelist edits documented in CLAUDE.md (CodeEditor.jsx whitelist, ReportingModule.jsx equivalent, and every `transpileModel` call site).

---

### Model · 2026-07-09
**Feedback:** Add a variable searcher in the variable slider, so if there are a large amount users can type and find them faster  
**File:** `src/components/modeling/VariableSelector.jsx`  
**Suggestion:** Add a small debounced text input at the top of the `VarPanel` that filters the variable chip list in real time (case-insensitive substring match on column name and dictionary label). The filter state lives in local `useState`; it is not part of the spec. Clear the input when the dataset changes. This is a pure UX change — no math, no pipeline, no export impact.  
**Invariants:** No external UI libraries; inline styles via `C`; no React in `src/math/`.

---

### Model · 2026-07-22
**Feedback:** Add more options to plot the binned scatter plots after running models using the arguments of R and Python as commands in the UI, for all estimators  
**File:** `src/components/modeling/ModelPlots.jsx`, `src/components/PlotBuilder.jsx`  
**Suggestion:** The binned scatter ("partial plot" / "binscatter") currently uses fixed binning. Expose controls for: number of bins (slider), bandwidth (for kernel smoothing), whether to show confidence intervals, and optionally control for FE or covariates before binning (a checkbox that runs the residualize-and-bin approach, matching R's `binsreg` package). Each control should correspond directly to a `binsreg` argument so the replication script can emit the exact call.  
**Invariants:** No new CDN dependencies unless already allowlisted; SE type passed explicitly to any auxiliary OLS run for residualization.

---

### Model · 2026-07-22
**Feedback:** Allow to adjust (mainly reduce) the zoom of the plots, by changing the number of observations  
**File:** `src/components/PlotBuilder.jsx`, `src/components/modeling/ModelPlots.jsx`  
**Suggestion:** Add a "Max points" slider (100–all, default all) that randomly samples the result rows before passing them to the chart. Random sampling must be seeded or be a top-N slice so it is reproducible and stable across re-renders; true random sampling on every render would make the plot flicker. The slider affects only the DISPLAY — the model coefficients and the replication script always use the full sample.  
**Invariants:** Slicing is view-only, never touches rawData or the pipeline; runner.js must always see the full dataset.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/map/SpatialPlotTab.jsx`  
**Suggestion:** Add a bounding-box or polygon-selection filter to the spatial plot and map tabs that restricts which rows are rendered. The filter is a view-level operation (like the Explore tab's `QuickFilter`) — it does NOT become a pipeline step. For the map tab, use Leaflet's `map.on('moveend')` to derive the current viewport bounds and filter the layer data; for the static SVG geo-plot, add a lat/lon range slider pair in the layer config panel. Disclose the view filter as a NOTE in the replication script rather than emitting it as a filtering step.  
**Invariants:** View filters must not mutate rawData or add pipeline steps; spatial pipeline integration is its own track (`sp_*` step types in runner.js).

---

### Clean · 2026-07-17
**Feedback:** Add an option to click on one pipeline step and go to the corresponding subsection automatically, so users can easily modify a step  
**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`  
**Suggestion:** Each step card in `History.jsx` should expose a "✎ Edit" icon that (1) selects the step for undo/redo inspection (already exists) and (2) dispatches a tab-switch event to `WranglingModule` to navigate to the subsection that owns that step type (e.g., `case_when` → Feature tab → case_when sub-section, `filter` → Clean tab → Filter Builder). Use a lookup table `{ stepType → { tab, subSection } }` driven by `STEP_REGISTRY`. The subsection scroll or expand is triggered via a `useRef`/`useImperativeHandle` on the target sub-component.  
**Invariants:** No new pipeline step type; the pipeline itself is unchanged — this is a navigation affordance only.

---

### Clean · 2026-07-19
**Feedback:** Add a function for Country Codes — iso2/iso3/country-name crosswalk transform  
**File:** `src/components/wrangling/FeatureTab.jsx`, `src/pipeline/runner.js` (new `iso_lookup` step or `mutate` extension)  
**Suggestion:** Implement as a small static lookup table (ISO 3166-1 alpha-2, alpha-3, and English name — ~250 rows, embeddable inline as a JSON constant). Expose it as a new `mutate` formula helper `iso(col, "to_iso3")` / `iso(col, "to_name")` detectable in `FeatureTab`'s expression evaluator, so it fits the existing `mutate` step type without requiring a new registry entry. Alternatively, a dedicated `FeatureTab` UI card (like the existing `Dummy` card) that lets the user pick input column and output format. Fuzzy matching is out of scope for the first version.  
**Invariants:** Static lookup table must live in `src/math/` or `src/pipeline/` (pure JS, no React); no external API call.

---

### General · 2026-07-19
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables  
**File:** `src/services/session/sessionState.jsx`  
**Suggestion:** This requires a design decision before implementation: lists are most useful as named string/number sets referenceable across modules (Clean filter `%in% myList`, Feature `case_when`, Explore filter). The natural home is a session-scoped context entry in `sessionState.jsx` (`lists: { [name]: values[] }`), persisted to IndexedDB alongside pipelines. A small "Lists" panel (either a new tab in `DatasetManager` or a floating panel) lets users create, name, and edit lists. `FilterBuilder` and `FeatureTab` gain a "from list" option. This is a cross-module design task; no single file owns it.  
**Invariants:** Persistence via IndexedDB, not localStorage; lists must be project-scoped (keyed by `pid`).

---

### General · 2026-07-05
**Feedback:** Add plots with 3 axis  
**File:** `src/components/PlotBuilder.jsx`  
**Suggestion:** Observable Plot 0.6 (the CDN version already loaded) does not natively support a third y-axis, but supports a dual-y via two separate SVG `<g>` layers with different `y` scales and a manually-drawn right-axis. Implement a "Secondary Y" toggle in the PlotBuilder layer config that maps selected layers to the right scale and renders a second y-axis tick mark on the right side of the plot frame. Label both axes with the respective column name. The replication exports emit `ggplot` with `sec_axis()` in R and a `twinx()` `Axes` in Python. A full 3D (x/y/z) chart is out of scope and requires a new library.  
**Invariants:** No new CDN beyond the allowlisted ones; Observable Plot 0.6 is already loaded from cdnjs.cloudflare.com.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx`, `src/pipeline/duckdbRunner.js`, `src/services/data/duckdb.js`  
**Suggestion:** Fase X4 (OPFS Parquet cache + DuckDB-as-data-boundary) is code-complete per the BugTriage note but has not been browser-validated by Franco. This item should be scheduled for a focused browser benchmark session: load the 21MB / 900k-row file, verify it routes to DuckDB (look for the ⚡ badge), confirm the OPFS Parquet cache is written (check `loadFromOPFSKey` in the console), and reload to confirm the cache restores the full row count without the 500-row preview regression documented in CLAUDE.md. If the OPFS cache write is still failing silently, check `connect-src` CSP for `extensions.duckdb.org` (documented as causing `RuntimeError: function signature mismatch`).  
**Invariants:** Full row count must be available to `runner.js` and all estimators; display is capped at 500 rows but computation never is (CLAUDE.md DuckDB rule 5).

---

## Pending browser validation (code-complete, not confirmed)

The following items are marked code-complete in BugTriage but have not been verified by Franco in the browser. They are NOT open bugs but should be validated before being closed:

| Item | File | Commit/Note |
|------|------|-------------|
| Plot Builder resize + PNG export aesthetics (2026-06-10) | `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx` | `35f4b5a6` |
| Y~Yhat / Residual-vs-Fitted / Q-Q plots for OLS (2026-06-11) | `src/components/modeling/ResidualPlots.jsx` | `35f4b5a6` |
| Spatial heatmap display (2026-06-13) | `src/components/tabs/spatial/map/SpatialPlotTab.jsx` | `5e105529` |
| N-way FE accepts any categorical variable (2026-07-09) | `src/math/PanelEngine.js` (`runFEMulti`) | `045f5cda` (#104) |
| FE with interactions (2026-07-09) | `src/math/PanelEngine.js`, `src/pipeline/runner.js` | `045f5cda` (#104) |
| P-value fix for insignificant estimates (2026-07-22/28) | `src/math/LinearEngine.js` (`tCDF`/`betainc`) | `216b1ecb` |

---

## Suggested action order

1. **[high] Confirm p-value fix `216b1ecb`** — critical math bug (false significance); ask Franco to verify with a known-insignificant coefficient before any other model validation is trusted.
2. **[high] Fix Stata workbench** (`CodeEditor.jsx`, `stataScript.js`) — complete feature non-functional; blocks replication workflow for Stata users.
3. **[high] Fix Simulate replication script** (`SimulateTab.jsx`, export generators) — the script is the core replication value-prop for the Simulate tab; emitting only the DGP draw is a broken deliverable.
4. **[medium] Fix time series group references** (`ExplorerModule.jsx`) — an existing feature that regressed; narrow debugging task in `fetchAggregateTimeSeriesSQL`.
5. **[medium] Add LaTeX table width scaling** (`latexTable.js`) — small change with high visibility in the Report module.
6. **[medium] Persist in-progress transformation drafts** (`FeatureTab.jsx`) — frequent user pain point; low implementation cost with `sessionStorage`.
7. **[medium] Add multiple hypothesis tests + R export** (`ModelHypothesis.js`, `CoefficientTestPanel.jsx`) — extends an already-validated engine with a UX layer.
8. **[medium] Add time series label controls** (`ExplorerModule.jsx`) — trivial to add; high usability gain.
9. **[medium] Add elasticity interpretation to AI narrative** (`Prompts/index.js`) — prompt-only change; zero risk to math invariants.
10. **[low] Update guide tour** (`HelpSystem.jsx`) — high-visibility for new LMU users; low implementation cost.
11. **[medium] Malformed CSV recovery UX** (`DataStudio.jsx`) — fix-later per BugTriage; schedule after core feature gaps are closed.
12. **[perf] Browser-validate Fase X4 DuckDB OPFS cache** (`DataStudio.jsx`) — coordinate with Franco for a dedicated benchmark session.
