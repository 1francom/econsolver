# Feedback Analysis Report — 2026-08-08

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **11 open bugs** (4 code-complete / browser-validation pending, 7 confirmed open) · **13 open features** · **2 open UX** · **1 open performance** · **1 open other**
- Total actionable items: **28**

---

## Bugs

### [high] · Export / Model · 2026-07-05
**Feedback:** Replication code for CS DiD 2021 does not work

**File:** `src/services/export/rScript.js` (line 1432, `case "CallawayCS"`), `src/components/modeling/CodeEditor.jsx` (lines 36–68)

**Suggestion:** `CodeEditor.jsx` builds the `model` config object from generic `spec` fields (`yVar`, `entityCol`, `timeCol`, `treatVar`, `xVars`) but the `CallawayCS` case in `rScript.js` destructures Callaway-specific keys — `treatCol` (the first-treatment-period column, not the binary indicator), `compGroup`, `csEstMethod`, and `csAnticipation` — directly from `model`. None of these are forwarded by `CodeEditor`, so `att_gt()` gets `gname = undefined` and the generated R script is immediately unrunnable. Fix: add `treatCol: spec.treatCol ?? null`, `compGroup: spec.compGroup ?? "nevertreated"`, `csEstMethod: spec.csEstMethod ?? "dr"`, and `csAnticipation: spec.csAnticipation ?? 0` to CodeEditor's model object (alongside existing fields at line 36), then verify `CallawayEngine.runCallawayCS` stamps all four into `result.spec` (it does — check `src/math/CallawayEngine.js` lines 65–79).

**Invariants:** None violated. CodeEditor's model config is not a React component and does not touch `src/math/`.

---

### [high] · Export / Explore · 2026-07-05
**Feedback:** Replication codes of the plots must have the lines to load datasets as well

**File:** `src/services/export/plotScript.js` (`buildGgplot` at line 343), `src/components/PlotBuilder.jsx` (`copyPlotScript` at line 1525)

**Suggestion:** `buildGgplot` and `buildMatplotlibPlot` already accept a `dfVar` option but not `{ dataset, datasets }`, so they cannot emit a load line. `copyPlotScript` calls `buildGgplot(entry, { dfVar })` with no dataset reference (line 1537). The geo variant (`buildGeoGgplot`) already supports load lines via `geoLoadLines()` at line 600 — use that as the template. Steps: (1) thread `datasetId` and `datasets` from `PlotBuilder`'s props (both already arrive as props: `datasetId` is at line 1366, `datasets` is not currently passed in but lives in `WranglingModule`/`ExplorerModule`). (2) Add a `dataset` lookup inside `buildGgplot`/`buildMatplotlibPlot`/`buildStataPlot` that calls `buildRLoadLine`/`buildPyLoadLine`/`buildStataLoadLine` (already in `loadLine.js`) and prepends it when a filename is known. The comment at plotScript.js:961 already says `"* assumes the relevant dataset is loaded"` — that assumption needs to become a concrete load call.

**Invariants:** All AI calls must remain through `AIService.js` — not relevant here. Pure JS export logic; no React in plotScript.js.

---

### [high] · Export / Calculate · 2026-07-17
**Feedback:** Stata workbench doesn't work completely

**File:** `src/components/calculate/workbench/exportScript.js` (`buildStata`, lines 79–117)

**Suggestion:** The generated Mata code has three bugs that cause syntax errors when run in Stata: (1) **Invalid Mata print syntax** — `("d${name}/d${ax} @midpoint", ...)` and `("integral", ...)` and `("root near", ...)` are not valid Mata statements; Mata requires `printf(...)` or `display ...` for output. Replace with `printf("d${name}/d${ax} @midpoint: %f\n", ...)`. (2) **Undeclared Mata scalars** — `xm`, `hh`, `sI`, `n`, `a`, `b`, `h`, `ng`, `ag`, `bg`, `hg`, `bestX`, `bestF`, `nr`, `ar`, `br`, `hr`, `prev`, `px` are used without `real scalar` declarations; Mata in strict mode rejects this. Add declarations at the top of each Mata function or at the program level. (3) **Scalar retrieval inside function body** — `st_numscalar("${p.name}")` is correct, but the outer `scalar ${p.name} = ...` command must run before `mata:` — verify the emitted ordering doesn't place the `mata:` block before the scalar definitions. The R and Python exports have no such issues.

**Invariants:** `src/math/` and `src/core/` must not import React — not relevant here (pure JS export function).

---

### [high] · Pipeline / Explore · 2026-07-17
**Feedback:** Basic math operations cannot be executed in group and summarise, like ATE by taking the difference in means between treatment and control group

**File:** `src/pipeline/runner.js` (line 528, `group_summarize` case), `src/components/wrangling/ReshapeTab.jsx`

**Suggestion:** `group_summarize` only supports per-group statistics on a single column (mean, sum, count, etc.). Users want cross-group derived metrics — e.g., ATE = mean(Y | treatment=1) − mean(Y | treatment=0). This requires a two-step pattern: (1) group-summarize to produce a `{group, mean_Y}` table, then (2) pivot or self-join to compute the difference. The simplest approach is to add a "difference between groups" aggregation variant in `ReshapeTab.jsx` that compiles into two chained runner steps (`group_summarize` + a `mutate`/`pivot_wider` diff step), avoiding any runner.js core change. Alternatively, expose a post-aggregation `diff_col` spec option in the step (`diffCol`, `refLevel`) that the runner handles in the `group_summarize` case after building the per-group table. Document that the output is a wide table with one column per group value.

**Invariants:** Steps must replay on `rawData` — any new sub-step must be non-destructive and serializable to registry.js.

---

### [high] · Export / General · 2026-07-05
**Feedback:** Adjust the size of LaTeX tables when the number of columns increases

**File:** `src/services/export/latexTable.js` (line 168, `\begin{tabular}`)

**Suggestion:** `buildStargazer` uses a fixed `colFmt = "l" + " r".repeat(colsN)` and wraps in a bare `\begin{tabular}`. With many models (6+), columns overflow the page width. Wrap the tabular in `\resizebox{\textwidth}{!}{...}` (requires `\usepackage{graphicx}`) or switch to `tabularx` with `X` columns that fill the available text width. The cleaner LaTeX convention for wide tables is `\adjustbox{max width=\textwidth}{...}` from the `adjustbox` package. Emit a `\usepackage{adjustbox}` comment at the top of the generated output and wrap the `\begin{tabular}...\end{tabular}` block. This is a two-line change to `buildStargazer`.

**Invariants:** None violated.

---

### [medium] · Model / UI · 2026-06-10 (code-complete, pending browser validation)
**Feedback:** Fix the size of the Plot Builder in ModelingTab — users should be able to resize it; PNG export should not have size/aesthetic problems

**File:** `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Commit `35f4b5a6` is recorded as code-complete in BugTriage. **Action needed:** Franco must browser-validate that (a) the plot viewport has a visible drag handle for vertical resize, (b) PNG export captures the user-set dimensions without clipping or misaligned canvas, and (c) the fix does not regress on the "bar value labels on panel frame" issue fixed 2026-07-29 (which also involved canvas sizing). If browser validation confirms it is working, mark this item closed in BugTriage and strike it through in ClaudeFB.md.

**Invariants:** None.

---

### [medium] · Model / UI · 2026-06-11 (code-complete, pending browser validation)
**Feedback:** Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS

**File:** `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx`

**Suggestion:** Commit `35f4b5a6` is recorded as code-complete. The fix was that OLS/WLS diagnostics now render from the estimation's frozen `result.spec` rather than mutable sidebar state. **Action needed:** Browser-validate that all three plots (Y~Ŷ scatter, Residuals vs Fitted, Q-Q) appear in the Residual Plots panel after an OLS estimation, remain frozen when variables are changed in the sidebar, and are correctly restored when a pinned model is selected. If validated, close the BugTriage row and strike the ClaudeFB.md entry.

**Invariants:** None.

---

### [medium] · Spatial / UI · 2026-06-13 (code-complete, pending browser validation)
**Feedback:** Heatmap must be fixed

**File:** `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx`

**Suggestion:** Commit `5e105529` is recorded as code-complete (Leaflet heatmap layer with KDE, palette/opacity controls, legend, HTML export, 5k-point cap). **Action needed:** Browser-validate that the Heatmap option appears in the layer-type picker, that the rendered heatmap is visually correct on a real lat/lon dataset, and that the HTML export opens correctly. If validated, close the row and update ClaudeFB.md.

**Invariants:** None.

---

### [medium] · Model / UI · 2026-06-14 (code-complete, pending browser validation)
**Feedback:** Coefficient plots and diagnostics change when users add/remove variables for a new estimation; they must remain frozen until re-estimation and reappear correctly when restoring pinned models.

**File:** `src/components/ModelingTab.jsx`, `src/components/modeling/DiagnosticsPanel.jsx`

**Suggestion:** Commit `35f4b5a6` is recorded as code-complete. **Action needed:** Browser-validate two paths: (1) add a variable after estimation → coefficient plot and heteroskedasticity panel must not update until the user presses Estimate again; (2) pin a model, switch to a new spec, then click the pinned model in the buffer bar → all plots and diagnostics must restore to the pinned result exactly. If validated, close the row.

**Invariants:** None.

---

### [medium] · Model / UI · 2026-07-06
**Feedback:** Add vector for running more than 1 hypothesis and save them to replicate in R

**File:** `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`

**Suggestion:** `CoefficientTestPanel.jsx` currently shows one joint Wald test at a time; there is no way to queue multiple tests or copy all of them as a batch R script. Add a "Save test" button that pushes the current test configuration (selected coefficients + contrast vector) into a local `savedTests` array (React state). Render saved tests as a collapsible list, each with a delete button. Add a "Copy all as R" button that calls `generateModelHypothesisScript("r", result, ...)` once per saved test and concatenates the snippets. The `generateModelHypothesisScript` function in `ModelHypothesis.js` already supports this — it's purely a UI state addition.

**Invariants:** State must not be persisted to IndexedDB for now (session-local is acceptable). Do not pass an array of tests to the math engine — run each independently.

---

### [medium] · Model / Math · 2026-07-09 (likely fixed, confirmation needed)
**Feedback:** Add options for fixed effects — not just entity and time, but also variables like country, state, sector; add also FE with interactions

**File:** `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx`

**Suggestion:** BugTriage records both items as "likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation." CLAUDE.md documents the N-way FE engine as validated (18/18 checks pass, 2026-07-11). **Action needed:** Franco should confirm in the browser that (a) the model configuration UI exposes an "Additional FEs" multi-select that accepts arbitrary categorical columns (not just entity/time), and (b) interaction terms between FE dimensions appear in the spec output and replication scripts. If confirmed, strike both ClaudeFB.md entries.

**Invariants:** Any FE column must be validated as categorical before the engine receives it. The engine itself (pure JS in `src/math/`) must remain React-free.

---

### [low] · Data / Import · 2026-05-10
**Feedback:** Parse error: CSV Error on Line 1583 · Invalid Input Error (truncated in source)

**File:** `src/DataStudio.jsx` (parseFile CSV branch), `src/services/data/parsers/`

**Suggestion:** The current CSV parser aborts the entire import when it encounters a malformed row. BugTriage marks this "fix-later." Add a try/catch around the row-level parse, collect bad rows with their original line numbers into a `skippedRows` array, and surface a recoverable warning banner: "N rows were skipped due to parse errors (lines: 1583, …). Continue with partial data or cancel?" The partial dataset should still be usable. The DuckDB path already handles some resilience; for the JS path, add a `resilient` parse option that skips bad rows rather than throwing.

**Invariants:** Pipeline must still replay on `rawData` (the cleaned partial result); `rawData` with skipped rows must be a valid table (no undefined values in skipped positions).

---

## Features

### Model / AI · 2026-07-09
**Feedback:** Add direct elasticity interpretation

**File:** `src/services/AI/Prompts/index.js` (`INTERPRET_REGRESSION_PROMPT`, ~lines 99–120)

**Suggestion:** The prompt already interprets log–log coefficients as elasticities and log–level as semi-elasticities (see lines 99, 353–354). What's missing is **automatic detection**: the AI receives variable names and functional-form clues but not an explicit "this variable was log-transformed" flag. Add a `functionalForm` field to the `metadataExtractor` output (detecting `log_`, `ln_`, `log(` prefixes in variable names) and pass it in the user payload to `interpretRegression`. The prompt's Rule F (log–log elasticity rule) then becomes reliably triggered. Without this detection, users whose column names don't start with `log_` get generic coefficient interpretation instead of elasticity language.

**Invariants:** Anthropic calls go through `AIService.js` only. Metadata extraction lives in `src/core/validation/metadataExtractor.js` (pure JS, no React).

---

### UX / Clean · 2026-07-17
**Feedback:** Save progress of uncompleted work in transformation subsections — e.g. writing a case_when, going back to Clean to see variable names, and coming back to case_when to continue writing the cases

**File:** `src/components/wrangling/FeatureTab.jsx` (lines 453–590, `ConditionalBuilder`)

**Suggestion:** `ConditionalBuilder`'s `mode`, `cwNN`, `cwCases`, `cwDefault`, `ifeCond`, `ifeTrueVal`, `ifeFalseVal` are all `useState` — they reset to defaults whenever the component unmounts (tab switch). Lift this state to the parent `FeatureTab` (or a sibling `useRef` in `WranglingModule`) so the draft is preserved across tab navigation. Alternatively, serialize the draft to `sessionStorage` under a `pid`-scoped key (e.g. `feat_draft_${pid}`) in a `useEffect` and restore it on mount. Use `sessionStorage` (not `localStorage`) — it's session-scoped and contains no pipeline data, so the IndexedDB-only invariant does not apply to ephemeral draft state.

**Invariants:** Draft state must be scoped per `pid` to avoid cross-project bleed (the stale-closure bug documented in CLAUDE.md). Do not store draft in IndexedDB — session storage for transient UI state is appropriate.

---

### UX / Data · 2026-07-17
**Feedback:** Add arrange functions in the data viewer

**File:** `src/App.jsx` (`DataViewer` component, line 414), `src/services/data/duckdb.js` (`getTablePage`, line 86)

**Suggestion:** `DataViewer` renders column headers as plain `<th>` elements with no sort interaction, and `getTablePage(tableName, offset, limit)` has no ORDER BY parameter. Add: (1) a `sortCol` + `sortDir` state pair in `DataViewer`; (2) clickable `<th>` elements that cycle `null → "asc" → "desc"` and display a ↑↓ indicator; (3) an `orderBy` parameter to `getTablePage` that injects `ORDER BY "${col}" ASC/DESC` into the SQL (safe — col name from `headers` array, already validated); (4) for the JS (non-DuckDB) path, a client-side `[...rows].sort(...)` on the current page. This is entirely additive — no pipeline changes needed since arranging the viewer is a display-only operation, separate from the pipeline `arrange` step.

**Invariants:** View sort must not mutate `rawData` or add a pipeline step — it is a display-only operation. The DuckDB `getTablePage` ORDER BY clause must only use column names already present in the table's header list (never user-supplied free text).

---

### Explore · 2026-07-09
**Feedback:** Add labels for the Time series plot, title, axis, etc.

**File:** `src/ExplorerModule.jsx` (time series sub-tab, ~lines 1306–1580)

**Suggestion:** The time series `SvgLineChart` component renders without a title or axis-label surface. The histogram (`SvgHistogram`) already has `title`, `xLabel`, `yLabel` props with SVG `<text>` rendering (lines 244–301) — copy the same pattern. Add three text inputs above the chart (Title, X-axis label, Y-axis label) that feed into `SvgLineChart` as new optional props, and thread them into the pinned-plot spec so `renderPinnedPlot` can restore them. The ACF/PACF and ADF sub-tabs can receive the same treatment in a follow-up.

**Invariants:** None. Pure UI change in `ExplorerModule.jsx`.

---

### General / UX · 2026-07-17
**Feedback:** Create groups for pinned models, plots, and maps, so not everything is compared at the same time

**File:** `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx`, `src/services/Persistence/indexedDB.js`

**Suggestion:** Add a `groupLabel` string field to each artifact's metadata (model buffer entries, saved plots, saved maps). Expose a small "Group" text input on each buffer card in `ModelBufferBar.jsx` that writes the label via the existing `saveArtifactOrder` path. Render buffer cards grouped by label (ungrouped cards fall into a default "All" group). The IndexedDB `model_buffer` schema already uses a per-project store — add `groupLabel` as a nullable field in the v9 schema without a migration (IndexedDB handles missing fields gracefully). This is a medium-scale UI feature; scope to model buffer only first, then extend to plots and maps in a follow-up.

**Invariants:** Persistence must go through `indexedDB.js` — no localStorage. `artifactOrder.js` is the right place for ordering/grouping metadata.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with `summary()` — Call: rdrobust Sharp RD estimates using local polynomial regression

**File:** `src/components/modeling/` (results display), `src/math/CausalEngine.js`

**Suggestion:** `runSharpRDD` already returns all the necessary fields (`h`, `bwType`, `n_left`, `n_right`, `coef`, `se`, `pval`, `ci_l`, `ci_u`, `polyOrder`, `kernel`). Add a compact `RDDSummaryTable` sub-component that replicates the `rdrobust` console layout: bandwidth info row, N (left/right), polynomial order, kernel, then the coefficient table with conventional / bias-corrected / robust SE rows. This is pure display — no math changes needed. Wire it just below the main coefficient display in the RDD result panel.

**Invariants:** None. Display component only.

---

### Model · 2026-06-06
**Feedback:** Allow users to edit LaTeX table variable names — currently RDD always shows "D (treatment)", "running − c", "D x (running − c)" regardless of actual column names

**File:** `src/components/modeling/resultDisplay.jsx`, `src/services/export/latexTable.js`

**Suggestion:** `buildStargazer` already accepts a `varLabels` option object (line 25: `const { showFirstStage = false, varLabels = {} } = options;`) but it's never populated by callers. Add a collapsible "LaTeX variable labels" editor in the Code/Report tab that lets users type display names for each variable; store the mapping in component state and pass it as `varLabels` to `buildStargazer`. The RDD hardcoded names (`D (treatment)`, `running − c`, `D × (running − c)`) are set in `latexTable.js` where it derives RDD variable names — those can be overridden by populating `varLabels` from `result.spec.yVar` / `result.spec.runningVar` / `result.spec.treatVar`.

**Invariants:** None. Purely cosmetic / export-time override.

---

### Calculate · 2026-06-19
**Feedback:** Add limits

**File:** `src/math/calcEngine.js`, `src/components/calculate/workbench/operations.js`, `src/components/calculate/workbench/EquationCard.jsx`

**Suggestion:** Limits (lim x→a f(x)) can be approximated numerically: evaluate f at a sequence of points approaching `a` from both sides (left: `a − ε`, `a − ε/10`, ...; right: `a + ε`, `a + ε/10`, ...) and check convergence. Add a `computeLimit(exprFn, axis, a, side)` function in `calcEngine.js`, expose it as an `ops.limit` toggle with an `at` field in the `EquationCard` UI, and emit the result in `ResultsPanel`. For the R export, emit `pracma::lim(function(x) ..., a)`; for Python, `sympy.limit(expr, x, a, '+')`. Undefined/oscillating limits should surface as `"limit does not exist"`.

**Invariants:** `calcEngine.js` is pure JS (no React). The function must not call `AIService.js`.

---

### Calculate · 2026-07-04
**Feedback:** Allow plotting constants in the graph (e.g. draw a horizontal line for a constant value alongside function curves)

**File:** `src/components/calculate/workbench/Workbench.jsx`, `src/components/calculate/workbench/WorkbenchCanvas.jsx`

**Suggestion:** This is simpler than it looks: a constant `k` is just a function of the form `y = k` with no `x` dependency. The canvas already plots `y = expr` curves — a constant expression evaluates to the same value for all `x`. The only change needed is to ensure the workbench store allows an equation with a pure numeric expression (no variable reference) and that the canvas renders it as a horizontal line rather than a degenerate curve. Add a UI hint under the equation input ("enter a numeric constant like `5` to plot a horizontal line"). No new math needed.

**Invariants:** None.

---

### Explore · 2026-05-19
**Feedback:** Add filter(`regionname %in% c("Spain (Espana)", "Basque Country (Pais Vasco)")`) for filter option

**File:** `src/components/wrangling/CleanTab.jsx` (`FilterBuilder`), `src/pipeline/predicate.js`

**Suggestion:** The `in` / `nin` operators already exist in `predicate.js` and `FilterBuilder`. The request appears to be for a more ergonomic multi-value entry UI — a tag-input component where users type values and press Enter to add them to the `in` list, rather than a raw comma-separated text field. BugTriage marks this "fix-later." A tag-input is a small UI addition to `CleanTab.jsx`'s `FilterBuilder` when the selected operator is `in` or `nin`.

**Invariants:** All filter evaluation goes through `predicate.js` — do not re-implement operator logic in `CleanTab.jsx`. The single-owner invariant for `evalPredicate` / `predicateToSQL` must be respected.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots

**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/plot/GeoLayerConfig.jsx`

**Suggestion:** BugTriage marks this "fix-later — route to V4 plots." When addressed, add a per-layer `filterExpr` field in `GeoLayerConfig` that compiles through `predicate.js`'s `evalPredicate` and is applied client-side before rendering the layer's features. For DuckDB-backed large datasets, compile to `predicateToSQL` and run a filtered `getTablePage` query. The architecture mirrors what the Clean tab's `FilterBuilder` already does.

**Invariants:** Do not re-implement operator evaluation outside `predicate.js`.

---

### General · 2026-07-17
**Feedback:** Think about how to construct lists, save them and use them during the session when filtering or constructing variables

**File:** `src/services/session/sessionState.jsx`, `src/services/Persistence/indexedDB.js`

**Suggestion:** This is a design-level feature with no natural home yet (noted as such in ClaudeFB.md). Suggested scope: a session-scoped "Named Lists" store in `sessionState.jsx` (`namedLists: Map<string, any[]>`) that persists to a new `named_lists` IDB store. Expose a "Lists" panel in a future sidebar or within the Clean tab's filter builder as an autocomplete source. This is a cross-module feature that needs a design session with Franco before implementation to decide where the UI lives and how lists integrate with `predicate.js`'s `in` operator.

**Invariants:** Persistence must use IndexedDB, not localStorage.

---

## UX / Design

### UX · 2026-07-17
**Feedback:** Guide tour must be updated

**File:** `src/components/HelpSystem.jsx` (`TOUR_STEPS`, line 11)

**Suggestion:** `TOUR_STEPS` has 9 steps (per CLAUDE.md) but the app has grown significantly since the tour was written — new tabs (Spatial, Simulate, Calculate), new estimators (CS DiD, Sun & Abraham, GMM, Synthetic Control, N-way FE), and new modules (SubsetManager, NLCommandBar, PlotBuilder). Review each step's `selector`, `title`, and `body` against the current DOM, remove any step pointing at a renamed/removed element, and add steps for Spatial, the PlotBuilder, and the AI coach. Also update `WorkspaceBar.jsx`'s tour button tooltip if it still says "9 steps." Per CLAUDE.md: "Never write a count into help copy — name the things instead."

**Invariants:** None. Pure UI text update.

---

### UX · 2026-07-17
**Feedback:** Clean: Add an option to click on one pipeline step and go to the corresponding subsection automatically

**File:** `src/components/wrangling/History.jsx`, `src/WranglingModule.jsx`

**Suggestion:** Each step in `History.jsx` knows its `type` (e.g., `case_when`, `log`, `filter`, `join`). Add an `onJumpToStep(type)` callback prop that `WranglingModule.jsx` handles by mapping `type → tab` (`filter` → Clean, `log`/`case_when` → Feature, `join` → Merge, `arrange` → Reshape) and programmatically switching the active tab. A simple `TYPE_TO_TAB` lookup object is sufficient. Wire the step card with a "✏ Edit" icon that calls `onJumpToStep`. Note: this navigates to the tab but doesn't pre-populate the form with the step's values — that would require lifting each sub-tab's form state, which is a larger follow-up.

**Invariants:** None. Tab routing stays in `WranglingModule.jsx`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows

**File:** `src/DataStudio.jsx` (parseFile, OPFS Parquet path), `src/services/data/duckdb.js`

**Suggestion:** BugTriage records this as "Fase X4 code-complete; browser benchmark pending Franco." The DuckDB Parquet cache path was implemented to eliminate the 15s tab-transition lag for large files. **Action needed:** Franco should benchmark a real 21MB / 900k-row CSV: (1) load time on first import, (2) tab-transition speed after OPFS cache is written, (3) whether the session crashes at any point. If the benchmark passes, close the BugTriage row and note the resolution. If it crashes, the crash is likely the initial Arrow materialization of 900k rows during DuckDB `CREATE TABLE AS SELECT` — the fix would be to stream via chunked Arrow IPC rather than a single `await db.all()`.

**Invariants:** Full dataset must always be used for pipeline steps (runner.js / duckdbRunner.js) — only the 500-row preview is shown in the UI. Computation limit ≠ display limit.

---

## Suggested action order

1. **Fix CS DiD 2021 replication code** — two-line CodeEditor.jsx change; actively broken feature used regularly by the LMU target audience. (`src/components/modeling/CodeEditor.jsx`)
2. **Fix plot replication load lines** — replication fidelity is the core value proposition; a script that doesn't load data isn't a script. (`src/services/export/plotScript.js`, `src/components/PlotBuilder.jsx`)
3. **Fix Stata workbench export** — three concrete syntax bugs (invalid Mata print, undeclared scalars, missing declarations) make the generated `.do` file fail to run. (`src/components/calculate/workbench/exportScript.js`)
4. **Browser-validate the 4 code-complete items** — Plot Builder resize, Residual Plots, Heatmap, Diagnostics freeze — all marked code-complete since 2026-06-14; these block Franco from closing 4 BugTriage rows.
5. **Fix group_summarize cross-group math** — the ATE use-case is a basic econometrics workflow; the lack of diff-in-means in a single step is a notable gap for the LMU tutorial audience.
6. **Add FeatureTab draft persistence** — low-risk `sessionStorage` pattern already established elsewhere; high UX impact (users regularly switch tabs to look up column names mid-transform).
7. **Add LaTeX table auto-resize** — one-line `\adjustbox` wrapper; makes the comparison table usable when users pin 6+ models.
8. **Update guide tour** — low code cost, high first-impression impact for new LMU department users.
