# Feedback Analysis Report — 2026-06-04

> Auto-generated by feedback-analyst agent. Suggestions only — not implemented.

## Summary
- **4 open bugs** · **10 open features** · **0 open UX** · **1 open performance** · **1 open other**
- **Total open items analysed: 16**
- **19 sync-gap items** fixed in BugTriage / codebase but not yet marked ✓ in ClaudeFB.md — see Appendix

---

## Bugs

### [critical · needs-repro] · Model · 2026-05-21
**Feedback:** URGENT — t-value = 0.49 but p-value < 0.001, impossible  
**File:** `src/math/LinearEngine.js:80–142`, `src/math/EstimationResult.js`  
**Suggestion:** The 2026-05-26 BugTriage audit found no desync in any of the 11 engines; `pValue(0.49, 100) = 0.625` matches R. The most likely cause is a **column-alignment misread**: the CoeffTable header order is `Variable | β̂ | (SE) | CI 2.5% | CI 97.5% | t | p | sig`, so a user reading column 1 (β̂ = 0.49) may have confused it with the t-column. Request a screenshot + estimator name from Franco to confirm. If a genuine desync is found, audit `CoeffTable` in `src/components/ModelingTab.jsx` around line 354 where `(results.testStats ?? results.tStats)` is used — verify the index alignment matches the `varNames` array.  
**Invariants:** `pValue()` is pure JS in `LinearEngine.js:80` — no React, consistent with zero-React rule for `src/math/`.

---

### [critical · needs-repro] · Calculate · 2026-06-02
**Feedback:** crash latex export  
**File:** `src/components/tabs/CalculateTab.jsx:289–292`  
**Suggestion:** The crash path from BugTriage is "not located." The LaTeX download at line 289 is a simple string concatenation passed to a `download()` helper. The most likely failure vectors are: (1) the `download` utility receiving `undefined` if `latex` state is empty; (2) a malformed KaTeX string causing a downstream crash if `katex.renderToString` is called before download. Add a `try/catch` around the `download()` call and surface the error to the user. Also confirm that the `katex.renderToString` at line 272 has its own try/catch; verify the error boundary prevents propagation to the outer component. BugTriage suspects this may have been transient — add logging to confirm.  
**Invariants:** None — UI-only change; no math or API calls involved.

---

### [high] · Data · 2026-05-13
**Feedback:** .dta files are wrong loaded and readed  
**File:** `src/services/data/parsers/stata.js`  
**Suggestion:** BugTriage status is "pending." The `d11d34c` commit fixed the format-119 variable-name stride (33→129), but the original 2026-05-13 report may refer to a different format version or encoding issue. Audit all format code paths (115/116, 117/118, 119) in `stata.js` to ensure the corrected stride is consistently applied and that value labels are resolved after the fix. Add a regression-test fixture with a format-117 .dta file containing ≥32 variables to prevent future stride regressions. The `_loadOpts` capture introduced in CLAUDE.md item 5b should surface `engine: "readstat"` for downstream script generation.  
**Invariants:** `src/services/data/parsers/stata.js` is pure JS — no React imports allowed.

---

### [high] · Report · 2026-05-25
**Feedback:** AI does not recognize the pipeline steps in the Script generation  
**File:** `src/ReportingModule.jsx:968`, `src/services/AI/sessionSnapshot.js`, `src/services/AI/Prompts/index.js`  
**Suggestion:** This matches CLAUDE.md pending item 5b ("VALIDATION PENDING 2026-05-30"). The `buildSessionSnapshot` call at `ReportingModule.jsx:968` is wired but needs end-to-end validation: (1) open a project with 3–4 pipeline steps (filter → log → z-score → lag), (2) trigger AI script generation in the Report tab, (3) check whether the emitted R/Python/Stata script walks those steps in order. If steps are absent, inspect `serializeSnapshot` in `sessionSnapshot.js` — confirm the `pipeline` step array (not the `sessionLog`) is serialized under the key name that `UNIFIED_SCRIPT_PROMPT` rule 8 references. The most likely bug is that `sessionLog` (the cross-module operation log) is passed instead of the ordered `pipeline` array from `WranglingModule`.  
**Invariants:** All Anthropic calls must route through `AIService.js:callClaude()` only — no raw `fetch` to the Anthropic API elsewhere.

---

## Features

### Model · 2026-05-11
**Feedback:** Add multiple statistics tests  
**File:** `src/math/SampleTests.js`, `src/components/tabs/statsim/SampleTestPanel.jsx`  
**Suggestion:** `SampleTests.js` exports: one-sample t, variance χ², parameter t/z, two-sample t, paired t, one/two proportion z, correlation (Pearson/Spearman), and variance-ratio F. Missing tests commonly needed in applied work: one-way ANOVA (SSB/SSW F-test for k groups), Kruskal-Wallis (non-parametric k-sample rank test), chi-square goodness-of-fit, and Kolmogorov-Smirnov one-sample. Add these as pure-JS exports in `SampleTests.js` following the existing `{stat, p, df, note}` return contract, then wire them into `SampleTestPanel.jsx` with a dropdown selector.  
**Invariants:** `src/math/SampleTests.js` must remain zero-React. Do not hardcode any degrees of freedom — parameterise from the data.

---

### Spatial · 2026-05-20
**Feedback:** Add grids to the plot, not only in the OMS map  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/math/SpatialEngine.js`  
**Suggestion:** The Observable-Plot geo-plot tab currently renders only point/polygon layers from WKT columns. Add a "Grid overlay" toggle that calls `buildRectGrid` or the H3 path from `SpatialEngine.js` using the visible bounding box, renders grid cell outlines as SVG polygon paths, and optionally fills cells by an aggregated column value. Expose grid resolution and aggregate column in `GeoLayerConfig.jsx`. Reuse `buildColorScale` from `src/components/tabs/spatial/shared/color.js` for the fill scale.  
**Invariants:** `src/math/SpatialEngine.js` must remain zero-React. All styling via the `C` object.

---

### Model · 2026-05-21
**Feedback:** Add for RDD a summary table as in R with summary() — Call: rdrobust Sharp RD estimates using local polynomial regression  
**File:** `src/components/ModelingTab.jsx:4344`, `src/math/CausalEngine.js`  
**Suggestion:** The current RDD display shows LATE, SE, p-value, and a coefficient table. The rdrobust `summary()` format additionally reports: (1) N left/right of cutoff, (2) bandwidth selector + kernel + VCE method, (3) three CI rows — conventional, bias-corrected, and robust — in a fixed-width table. `CausalEngine.js:runSharpRDD` already returns `h`, `n`, kernel type, and the conventional LATE/SE. To add bias-corrected and robust CIs: implement the local-linear bias estimator (using pilot bandwidth `b` and a higher-order polynomial fit) and the robust SE (conventional SE inflated by squared bias). Add a `<RDDSummaryTable>` component at `ModelingTab.jsx:4344` rendering the three rows in a monospace grid.  
**Invariants:** Bias-corrected / robust CI math belongs in `CausalEngine.js` (zero-React). UI table in `ModelingTab.jsx`.

---

### Model · 2026-05-25
**Feedback:** Add an option to estimate without intercept  
**File:** `src/math/LinearEngine.js:109–146`, `src/components/ModelingTab.jsx`, `src/components/modeling/ModelConfiguration.jsx`  
**Suggestion:** `LinearEngine.runOLS` unconditionally prepends a column of ones. Add an `opts.intercept = true` parameter (default `true`); when `false`, skip the ones column. Expose a checkbox "No intercept (suppress constant)" in `ModelConfiguration.jsx`. Pass the flag through `estimate()` in `ModelingTab.jsx`. Also patch `runWLS` and `run2SLS`; mark `runFE`/`runFD` as "not applicable" (intercept is absorbed by demeaning). When no intercept, use `R² = 1 − SSR / Σyᵢ²` (not the centered formula) and flag via `_r2Note`.  
**Invariants:** `seType` must still be passed explicitly. Math changes in `src/math/` only — no React. UI in `src/components/`.

---

### Spatial · 2026-05-30
**Feedback:** Geometry transformations and show EPSG  
**File:** `src/components/tabs/spatial/analyze/CRSTransformSection.jsx:71–72`  
**Suggestion:** The CRS dropdowns are hardcoded to `["EPSG:4326", "EPSG:32721"]` — only two options. Replace with a free-text input (or searchable select seeded from ~30 common EPSG codes: UTM zones, national grids, Web Mercator 3857, etc.). The `loadProj4` function in `crs.js` already supports arbitrary PROJ strings; the UI is the only bottleneck. Also display the inferred current CRS of the loaded data as a read-only badge alongside the source dropdown (e.g., detected from WKT SRID or `.prj` sidecar metadata).  
**Invariants:** `crs.js` is pure JS — no React. UI change only in `CRSTransformSection.jsx`.

---

### Spatial · 2026-05-30
**Feedback:** Allow spatial filters for plots  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`  
**Suggestion:** Add a "Filter" panel in the geo-plot tab allowing users to define a bounding box (entered as coordinates or drawn interactively) or a categorical column filter to limit visible features. Internally, derive a `filteredRows` array from the full `rows` prop — this is a display-only filter and must not mutate `rawData` or create a pipeline step. Pass `filteredRows` to `mkGeoLayer` in `geo.js` instead of the full `rows` array.  
**Invariants:** Must not mutate `rawData`. No pipeline step created — display-only filter. Styling via `C` object.

---

### Data · 2026-06-01
**Feedback:** Add an option to edit/assign column names directly in the Data Viewer  
**File:** `src/ExplorerModule.jsx`  
**Suggestion:** Column headers in the Data Viewer are static text. Add a double-click handler on each `<th>` to toggle an inline `<input>`. On blur/Enter, dispatch a `rename` pipeline step (already implemented in `runner.js`) via the wrangling callback so the rename is non-destructive and replays on `rawData`. This keeps the pipeline as the source of truth.  
**Invariants:** Must create a `rename` pipeline step — never mutate `rawData` in place (non-destructive pipeline invariant). Use `indexedDB.js` for persistence.

---

### Simulate · 2026-06-02
**Feedback:** You could add different types of quantiles like in R, but it might not be necessary  
**File:** `src/components/tabs/statsim/StatWorkspace.jsx:285`, `src/math/` (new helper)  
**Suggestion:** R provides 9 quantile interpolation types (Hyndman & Fan 1996). Currently the workspace uses a single quantile path. Add a `quantileType` dropdown (1–9) in the quantile panel and implement `quantileHF(sorted, p, type)` as a ~30-line pure JS function in `src/math/`. Highlight types 6 (Excel-compatible) and 7 (R default) in the UI.  
**Invariants:** Math helper in `src/math/` — zero-React. No SE-type logic needed.

---

### Spatial · 2026-06-03
**Feedback:** Add geometry to plot points, not just lat and lon  
**File:** `src/components/tabs/spatial/plot/SpatialGeoPlot.jsx`, `src/components/tabs/spatial/shared/wkt.js`  
**Suggestion:** The geo-plot tab requires separate lat/lon columns for point layers. Add a "Geometry source" toggle — when WKT is selected, skip the lat/lon pickers and parse coordinates using `wktToLeaflet` already in `wkt.js`. For non-point geometries (polygons, lines) render the full outline using `leafletPolygonLatLngs`. Expose this toggle in `GeoLayerConfig.jsx`.  
**Invariants:** Reuse `wkt.js` — do not duplicate parsing logic. Styling via `C` object.

---

### Calculate · 2026-06-03
**Feedback:** Add a "show steps button" for integrals, derivatives and optimizations  
**File:** `src/components/tabs/CalculateTab.jsx`, `src/math/symbolicDiff.js`  
**Suggestion:** Add a collapsible "Show steps" panel. For **derivatives**, extend `symbolicDiff.js` with a `diffWithSteps(expr)` function that returns an array of `{rule, latex}` objects (product rule, chain rule, etc.) — the differentiation tree already exists. For **numerical integration**, emit quadrature nodes/weights or Riemann sum terms. Render the step array in `CalculateTab.jsx` as a KaTeX-rendered list. The `latexName` export already in `symbolicDiff.js` can be reused for variable formatting.  
**Invariants:** `symbolicDiff.js` is in `src/math/` — must stay zero-React. Step collector returns plain objects; rendering lives in `CalculateTab.jsx`.

---

## Performance

### [high] · Data · 2026-05-08
**Feedback:** Big datasets generate crashes (21MB) over 900k rows  
**File:** `src/DataStudio.jsx:184–204`, `src/services/data/duckdb.js:188`  
**Suggestion:** The crash likely occurs at JS object materialisation when something upstream calls `extractAllRows()` on the full 900k-row DuckDB table. Audit `WranglingModule.jsx` to ensure all tab switches use the DuckDB async path when `rawData._duckdb` is present and never fall back to full JS-array iteration. Verify `PREVIEW_ROWS` is capped at 200–500 rows for display. The highest-leverage fix with zero code changes: enable the COOP/COEP headers in `vercel.json` (`Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Embedder-Policy: require-corp`) to unlock `SharedArrayBuffer` and DuckDB-Wasm multithreaded mode, roughly halving peak memory — this is specified in CLAUDE.md §DuckDB performance strategy rule 4 and may not yet be deployed.  
**Invariants:** Full dataset must always be available inside DuckDB for pipeline replay — never truncate `rawData` before passing to `runner.js` or `duckdbRunner.js`. Display capped at 200–500 rows; computation runs on the full table.

---

## Other

### [medium] · Data · 2026-05-10
**Feedback:** Parse error: Invalid Input Error: CSV Error on Line: 1583  
**File:** `src/services/data/duckdb.js:188`  
**Suggestion:** This is a DuckDB `read_csv` failure with `auto_detect=true`. Auto-detect sniffs column types from the first N rows; if row 1583 has a type mismatch (text in a column detected as integer), DuckDB throws. Fix options: (1) add `sample_size=-1` to scan the whole file before type detection; or (2) add a try/catch that retries with `all_varchar=true` and shows a warning ("column types coerced to string — use Clean → Type-cast to convert"). Also improve the error message surfaced to the user by extracting the line number and column name from the DuckDB error object.  
**Invariants:** Large-dataset performance rule applies — avoid materialising all rows into JS on retry. Catch at the DuckDB SQL layer and report to UI.

---

## Suggested action order

1. **Get a reproducer for the p-value mismatch** — ask Franco for a screenshot + estimator name before touching math code; very likely a label misread, not a math bug.
2. **Fix the LaTeX export crash** — add try/catch around `download()` at `CalculateTab.jsx:289`; low effort, critical UX.
3. **Validate Report AI pipeline-script generation (item 5b)** — test `buildSessionSnapshot` end-to-end with a multi-step pipeline; highest-effort open bug, pending since 2026-05-30.
4. **Enable COOP/COEP headers in `vercel.json`** — unblocks DuckDB `SharedArrayBuffer` / multithreaded mode, directly addresses the 900k-row crash with zero math/UI changes.
5. **Fix remaining .dta loading edge cases** — add a ≥32-variable format-117 fixture to `stata.js` to catch stride regressions.
6. **Add no-intercept option** — small math change in `LinearEngine.js`, high practical demand in econometrics.
7. **RDD rdrobust-style summary table** — bias-corrected and robust CI rows are the main gap vs. R's `summary(rdrobust(...))`.
8. **Run ClaudeFB.md cleanup pass** — mark the 19 resolved items in the Sync Gaps table to keep the log accurate.

---

## Appendix — Sync Gaps (ClaudeFB.md not marked ✓)

Items confirmed resolved per BugTriage.md or git log but still appearing as open in ClaudeFB.md:

| Date | Item | Evidence |
|------|------|----------|
| 2026-05-11 | Explore — graph does not fit the screen | BugTriage: fixed |
| 2026-05-13 | Spatial — grid border clipping | BugTriage: fixed 2026-05-18 (Sutherland-Hodgman) |
| 2026-05-20 | Spatial — grids do not work | BugTriage: fixed 2026-06-01 |
| 2026-05-20 | General — dataset delete variables persist | BugTriage: fixed (DatasetManager.jsx:406–418) |
| 2026-05-21 | Model — auto-reestimate on variable toggle | BugTriage: fixed 2026-06-01 |
| 2026-05-21 | Model — add option to clean current model | BugTriage: fixed (Clear result button, ModelingTab.jsx:3573) |
| 2026-05-22 | Calculate — function graph from 0 | BugTriage: fixed |
| 2026-05-25 | Calculate — only 1 equation plotted | BugTriage: fixed |
| 2026-05-19 | Explore — color references in PlotBuilder | BugTriage: fixed 2026-05-30 |
| 2026-05-19 | Simulate — Monte Carlo for single variables | BugTriage: fixed 2026-05-30 |
| 2026-05-30 | Spatial — adjust size to spatial unit | BugTriage: fixed (map.fitBounds) |
| 2026-05-31 | Data — xlsx CDN fetch error | BugTriage: fixed 2026-06-01 (static import) |
| 2026-05-31 | Data — OECD 406 error | BugTriage: fixed 2026-06-03 |
| 2026-06-01 | Data — .dta columns loaded twice | BugTriage: fixed 2026-06-03 |
| 2026-06-02 | Model — Poisson/estimator interpretation | BugTriage: fixed 2026-06-03 |
| **2026-05-28** | **Model — event study (Sun-Abraham/Callaway-Sant'Anna)** | **Shipped: `d11d34c` (Sun-Abraham) + `ec7aed3` (Callaway) — BugTriage also needs updating** |
| **2026-05-11** | **Model — Anderson-Rubin confidence intervals** | **Implemented: `ModelingTab.jsx:908–929`** |
| **2026-05-18** | **Simulate — resampling/bootstrapping** | **Implemented: `Resampling.js` + `StatWorkspace.jsx:23`** |
| **2026-05-19** | **Explore — `%in%` multi-value filter** | **Implemented: `CleanTab.jsx:598,683` "in list" chip UI** |
