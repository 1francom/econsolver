# Bug Triage

> Auto-generated by feedback-collector agent. Edit status manually.

| Date | Module | Description | Severity | Category | Probable File | Status |
|------|--------|-------------|----------|----------|---------------|--------|
| 2026-05-08 | Data | Data from World Bank and OECD don't load | high | data | `src/services/data/fetchers/worldBank.js`, `src/services/data/fetchers/oecd.js` | fixed |
| 2026-05-08 | Data | In the data viewer I cannot see all columns, the arrows are for the rows instead. | medium | ui | `src/components/` (DataViewer / ExplorerModule.jsx) | fixed |
| 2026-05-08 | Explore | Values of distributions don't change with the filter, just the graph | high | pipeline | `src/ExplorerModule.jsx` | fixed |
| 2026-05-08 | Data | "Parse error: Unsuppoerted Stata format. Only 117/118 supported" Error with .dta files | high | data | `src/services/data/parsers/stata.js` | fixed |
| 2026-05-08 | Data | DuckDB-WASM integration missing (user-flagged high priority) | high | pipeline | `src/services/data/duckdb.js`, `src/pipeline/duckdbRunner.js` | fixed |
| 2026-05-08 | Model | Can't run an OLS with simulated data | critical | math | `src/math/LinearEngine.js`, `src/components/ModelingTab.jsx` | fixed |
| 2026-05-10 | Model | Allow the light theme button in the project session | low | ui | `src/App.jsx`, `src/components/workspace/WorkspaceBar.jsx` | fixed |
| 2026-05-11 | Data | Edit cells button does not work | high | ui | `src/ExplorerModule.jsx` | fixed (minor bugs remain) |
| 2026-05-11 | Explore | The graph does not fit the screen | medium | ui | `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx` | fixed |
| 2026-05-11 | Model | McRary plot does not work — McCrary density test could not be computed | high | math | `src/math/CausalEngine.js`, `src/components/modeling/ModelPlots.jsx` | fixed |
| 2026-05-13 | Data | .dta files are wrong loaded and readed | high | data | `src/services/data/parsers/stata.js` | fixed 2026-06-10 — the 420k fixture is format 115; its five-byte expansion-field terminator was under-read by four bytes, shifting every row. Also fixed negative float/double missing detection and hardened formats 117–119 map offsets, mixed storage widths, format-118 name stride, and `%td/%tm/%tq/%th/%tw/%ty` conversion. The supplied fixture declares `%9.0g` for every column (confirmed by pandas), so its `birthdate`/`begindate` correctly remain numeric; date conversion was validated with generated 117/118 files that declare Stata date formats. |
| 2026-06-10 | Data | **PERF: large .dta (and .rds/.xlsx/.shp) bypass DuckDB → ~15s tab-transition lag** | high | perf | `src/DataStudio.jsx` (parseFile dta branch :403), `src/services/data/duckdb.js` | fixed 2026-06-10 for `.dta` — files over 10 MB are parsed once, registered as an Arrow-backed DuckDB table, reduced to a 500-row UI preview, and persisted through the existing OPFS Parquet cache. Other browser-only formats remain future work. |
| 2026-05-13 | Spatial | Multipolygon geometry does not work | high | ui | `src/components/tabs/SpatialTab.jsx`, `src/math/SpatialEngine.js` | fixed |
| 2026-05-13 | Spatial | Grids: adjust the size of border grids by cutting them | medium | ui | `src/components/tabs/SpatialTab.jsx`, `src/math/SpatialEngine.js` | fixed (2026-05-18 — Sutherland-Hodgman polygon clipping in `clipRectToRings`) |
| 2026-05-13 | Model | Show clearer errors — "Matrix is singular or insufficient data" too vague | medium | ui | `src/components/ModelingTab.jsx`, `src/math/LinearEngine.js` | fixed |
| 2026-05-21 | Report | `TypeError: Cannot read properties of undefined (reading 'map')` in SigCallout when opening Report tab with an FE/FD model active | high | ui | `src/ReportingModule.jsx` | fixed (2026-05-21 — `normaliseResult` unwraps `{type:"FE/FD", fe, fd}` bundles to the flat inner result) |
| 2026-05-20 | Spatial | Grids do not work | high | ui | `src/components/tabs/SpatialTab.jsx`, `src/math/SpatialEngine.js` | fixed (2026-06-01) |
| 2026-05-20 | General | When a dataset is deleted, all variables should disappear from all modules without having to select another dataset | medium | ui | `src/components/workspace/DatasetManager.jsx`, `src/services/session/sessionState.jsx` | fixed (onSelectDataset(fallback) called on delete — DatasetManager.jsx:406–418) |
| 2026-05-20 | Explore | ACF/PACF light theme missing | low | ui | `src/ExplorerModule.jsx` | fixed |
| 2026-05-21 | Model | p-value calculation reported broken — t-value = 0.49 with p < 0.001 | critical→needs-repro | math | `src/math/LinearEngine.js`, `src/math/EstimationResult.js`, `src/core/inference/` | fixed (2026-06-05 — confirmed by Franco) |
| 2026-05-21 | Model | Variables auto-reestimate on selection/elimination toggle — should require explicit re-run | medium | ui | `src/components/ModelingTab.jsx` | fixed (2026-06-01) |
| 2026-05-21 | Model | Add option to clean / reset the current model | low | ui | `src/components/ModelingTab.jsx`, `src/components/modeling/EstimatorSidebar.jsx` | fixed ("Clear result" button — ModelingTab.jsx:3573) |
| 2026-05-22 | Calculate | Function graph does not start from 0 | medium | ui | `src/components/tabs/CalculateTab.jsx` | fixed |
| 2026-05-25 | Calculate | Only 1 equation is plotted when multiple are defined | medium | ui | `src/components/tabs/CalculateTab.jsx` | fixed |
| 2026-05-25 | Report | AI does not recognize pipeline steps in Script generation (matches CLAUDE.md pending item 5b) | high | ai | `src/ReportingModule.jsx`, `src/services/AI/AIService.js`, `src/services/AI/Prompts/index.js` | fixed (2026-06-09 — code verified complete: WranglingModule.onComplete includes pipeline; buildSessionSnapshot serializes it; UNIFIED_SCRIPT_PROMPT injects it) |
| 2026-05-19 | Explore | Color references in PlotBuilder for `color = X` mapping | medium | ui | `src/components/PlotBuilder.jsx` | fixed (2026-05-30 — auto-default to observable10 scheme + dark-mode legend patch when aes.color is set) |
| 2026-05-19 | Simulate | Montecarlo simulation for single variables | medium | ui | `src/components/tabs/SimulateTab.jsx` | fixed (2026-05-30 — OLS/Single mode toggle; single mode: pick variable + statistic mean/sd/var/median/min/max; MCHistogram reused) |
| 2026-05-28 | Model | Event study needs to be split into Sun-Abraham and Callaway-Santana heterogeneity-robust DiD estimators (currently only generic TWFE event study, which is biased under staggered treatment) | high | math | `src/math/PanelEngine.js`, `src/components/ModelingTab.jsx`, `src/components/modeling/EstimatorSidebar.jsx` | fixed (2026-06-09) |
| 2026-05-30 | Spatial | Adjust size respectively to the spatial unit(s) — geometry/polygon display sizing not scaling with unit type | medium | ui | `src/components/tabs/SpatialTab.jsx`, `src/math/SpatialEngine.js` | fixed (map.fitBounds auto-fits to geometry bounds — SpatialPlotTab.jsx:113) |
| 2026-05-31 | Data | Parse error: schools_with_coordinates.xlsx — Failed to fetch dynamically imported module from cdn.sheetjs.com/xlsx-0.20.3 | high | data | `src/DataStudio.jsx` (SheetJS CDN loader) | fixed (2026-06-01 — static import * as XLSX from "xlsx") |
| 2026-05-31 | Data | OECD proxy error 406 for DSD_NAAG@DF_NAAG_I — server rejects JSON accept header, needs content-type negotiation fix | high | data | `supabase/functions/oecd-proxy/index.ts` | fixed (2026-06-03 — Codex: fetchWithFallback tries v2→v1.0.0→application/json; deployed to Supabase) |
| 2026-06-01 | Data | Not all columns of .dta are recognized — columns loaded twice appear as replicated entries without names | high | data | `src/services/data/parsers/stata.js` | fixed (2026-06-03 — Codex: format-119 variable name stride 33→129) |
| 2026-06-02 | Calculate | Crash on LaTeX export | critical | export | `src/ReportingModule.jsx` | fixed (2026-06-05 — confirmed by Franco) |
| 2026-06-02 | Model | AI interpretation does not adapt to estimator-specific semantics (e.g. Poisson coefficients should be shown as incidence rate ratios / semi-elasticities, not linear marginal effects) | high | ai | `src/services/AI/Prompts/index.js`, `src/services/AI/AIService.js` | fixed (2026-06-03 — ESTIMATOR TYPE RULES J–R added to INTERPRET_REGRESSION_PROMPT; result.type threaded into user prompt) |
| 2026-06-05 | Data | Numbers are rounded when inserting, deleting or overwriting cells in the data viewer | medium | ui | `src/ExplorerModule.jsx` | fixed (2026-06-09) |
| 2026-06-06 | Spatial | Geocoding directions might be upgraded — geocoder address resolution quality needs improvement | low | data | `src/components/tabs/spatial/analyze/GeocodeSection.jsx` | works — enhancement only (Franco confirms geocoding functions 2026-06-10; Photon/Komoot precision is a provider limit, not a defect) |
| 2026-06-10 | Model | Plot Builder in ModelingTab is not resizable by the user; PNG export has size/aesthetic problems | medium | ui | `src/components/PlotBuilder.jsx`, `src/components/ModelingTab.jsx` | code-complete 2026-06-14 (`35f4b5a6`) — plot viewport now has native vertical resize with bounded responsive canvas sizing, so PNG captures the user-selected dimensions. Browser validation pending Franco. |
| 2026-06-11 | Model | Y~Y_hat, Residual vs fitted, and Q-Q plot missing for OLS | high | ui | `src/components/modeling/ModelPlots.jsx`, `src/components/modeling/ResidualPlots.jsx`, `src/components/ModelingTab.jsx` | code-complete 2026-06-14 (`35f4b5a6`) — all three plots were already wired; fixed the actual regression: OLS/WLS diagnostics now render from the estimation's frozen `result.spec`, so sidebar edits and pinned-model restore cannot mutate the displayed model. Browser validation pending Franco. |
| 2026-06-13 | Spatial | Heatmap display broken | medium | ui | `src/components/tabs/spatial/map/SpatialPlotTab.jsx`, `src/components/tabs/spatial/map/SpatialLayerEditor.jsx` | code-complete 2026-06-14 (`5e105529`) — restored Heatmap as a Leaflet map layer with KDE cells, palette/opacity controls, legend, HTML export, and deterministic 5k-point cap. Browser validation pending Franco. |
| 2026-06-13 | Clean | ⚠️ SECURITY: XSS injection attempt via feedback form — JS payload (alert-based PoC). Feedback description field accepts and stores arbitrary script content. Sanitize on write and on render. | critical | other | feedback form → Supabase ingestion path; any component that renders `feedback.description` | fixed (2026-06-14 — sanitize-on-write at the choke point `feedbackService.submitFeedback`: `&<>` escaped + 5000-char cap, so stored content is inert in the feedback-collector markdown dump and any future viewer. VERIFIED no live render path: the app never renders feedback (FeedbackModal is write-only) and the codebase's only raw-HTML sink is CalculateTab KaTeX `renderToString(..., trust:false)` (XSS-safe), not feedback. Durable invariant documented: any future feedback viewer must render description as escaped text, never raw HTML. Hard server-side boundary (Supabase trigger/validation) recommended as follow-up since client sanitization is bypassable by direct POST) |
| 2026-06-13 | Data | ⚠️ SECURITY: XSS injection attempt via feedback form — HTML `<script>`+`<img onerror>` payload. Same unsanitized ingestion path as above. | critical | other | feedback form → Supabase ingestion path; any component that renders `feedback.description` | fixed (2026-06-14 — same fix as the row above: `sanitizeFeedback` escapes `<img onerror>` / `<script>` to inert entities at write) |
| 2026-06-14 | Model | IV-Poisson AI narrative interprets coefficients as linear 2SLS effects instead of incidence-rate ratios | high | ai | `src/services/AI/Prompts/index.js` | fixed 2026-06-14 (`bc2d7b9d`) — `IVPoisson` now follows the Poisson IRR rule and separately discusses instrument relevance/exclusion. |
| 2026-06-14 | Data | Malformed CSV row can abort import instead of surfacing recovery options | medium | data | `src/services/data/parsers/`, `src/DataStudio.jsx` | fix-later — post-launch resilient-import pass; retain original row number and offer skip/repair preview. |
| 2026-06-14 | Clean | R-style `%in% c(...)` membership syntax is not accepted by the filter expression UI | low | ux | `src/components/wrangling/CleanTab.jsx`, `src/services/exprEvalService.js` | fix-later — post-launch expression-language compatibility. |
| 2026-06-14 | Model | RDD result lacks an R-style `summary(rdrobust)` compact table | low | ux | `src/components/modeling/results/`, `src/math/CausalEngine.js` | fix-later — presentation enhancement; current estimates and diagnostics remain available. |
| 2026-06-14 | Model | User cannot estimate supported regression families without an intercept | medium | math | `src/components/modeling/ModelConfiguration.jsx`, `src/math/LinearEngine.js` | fix-later — requires engine-wide spec/export compatibility, not a pre-launch UI toggle. |
| 2026-06-14 | Spatial | Plot builder has no spatial row-filter control scoped to a layer | low | ux | `src/components/tabs/spatial/plot/` | fix-later — route to V4 plots after launch. |
| 2026-06-14 | Model | LaTeX coefficient table display names cannot be edited independently of source columns | low | ux | `src/components/modeling/resultDisplay.jsx` | fix-later — post-launch reporting metadata feature. |
| 2026-06-16 | Model | Pipeline only applies if dataset is also selected in Clean Module | high | pipeline | `src/pipeline/registry.js`, `src/components/wrangling/ImportPipelineButton.jsx` | fixed 2026-06-25 — root cause was the **Import pipeline** path, not dataset selection (Franco confirmed the active dataset was correct). Importing a previously-exported `pipeline.json` was rejected with "Unknown step types: date_parse" because `date_parse` — a first-class step created by FeatureTab and handled by runner.js/duckdbRunner.js/auditor.js/stepTranslators.js — was never registered in `STEP_REGISTRY`, so it was absent from `STEP_TYPES` and ImportPipelineButton's allow-list aborted the whole import. Fix: added the `date_parse` registry entry (`registry.js`) and ported its R/Py/Stata translators into `services/export/{rScript,pythonScript,stataScript}.js` (logic mirrored from the existing `stepTranslators.js` cases). Hardened `pipelineReliabilityValidation.mjs` with a **reverse runner→registry check** (the existing T5 only checked registry→runner, the blind spot that let this regress) + date_parse smoke; added date_parse fixture to the export integrity harness. Reliability 69/69, export 274/274, lint:undef + build green. |
| 2026-06-26 | Data | Parse error: crime_panel_final_r_300.rds — not a valid R data file (.rds), unrecognised serialisation header | high | data | `src/services/data/parsers/rds.js` | fixed (Franco-confirmed) |
| 2026-06-26 | Data | Parse error: grid_enriched_500.rds — not a valid R data file (.rds), unrecognised serialisation header | high | data | `src/services/data/parsers/rds.js` | fixed (Franco-confirmed) |
| 2026-07-02 | Model | When using DuckDB, the observation count in model output is 500 (preview row cap) instead of the full dataset n — corrupts df, t-stats, F-stat, and p-values for all large-n DuckDB estimations | critical | math | `src/components/ModelingTab.jsx` (cleanedData.length used as n), `src/pipeline/duckdbRunner.js`, `src/services/data/duckdb.js` | fixed (`c9c5bd3e`, #100, 2026-07-02) — root cause: WranglingModule's `proceed()` ("→ Analyze") omitted `_duckdb` from the `onComplete` payload (unlike `onReady`), so ModelingTab saw no table pointer and fell back to the 500-row preview for all n/df/statistics. |
| 2026-07-06 | Sync | Sharing a project with another user fails with an RLS error ("Share not found or you are not the intended recipient.") even when the recipient email is correct | high | security | `supabase/migrations/20260706120000_fix_project_shares_email_case.sql`, `src/services/sync/shareEngine.js` | code-complete 2026-07-06 — root cause: `createShare()` lowercases `recipient_email` before insert, but the `share_recipient_read` (table) and `share_blobs_recipient_read` (storage) RLS policies compared it against the raw, case-preserved `auth.jwt() ->> 'email'` claim. Any recipient whose account email has uppercase characters failed the row check silently (RLS just returns 0 rows, no error detail), so `pullShare()` reported a false "not the intended recipient". Fix: both policies now lower-case the JWT email claim before comparing. **2026-07-06 (2nd pass, "Object not found" on Import):** found a second storage-RLS gap — owners were never granted SELECT on `shares/` blobs (only insert/update/delete), and Supabase Storage reports RLS-denied reads as "Object not found"; this broke owner self-restore, `upsert:true` re-uploads, and `revokeShare`'s `list()` (orphaned artifacts). Migration extended with `share_blobs_owner_select`; `pullShare` now maps the cryptic storage error to an actionable message; `listSharedWithMe` filtered to rows addressed to the signed-in email (owner RLS was leaking your own outgoing shares into "Shared with me"). **Migration applied to live DB by Franco 2026-07-06** — all 7 policies verified present (4× share_blobs_owner_*, share_blobs_recipient_read, share_owner_all, share_recipient_read). **CORRECTION (2026-07-14, Franco browser-tested and hit the bug live):** "present" was not "correct" — `share_blobs_owner_select` and `share_blobs_recipient_read` both had an ambiguous unqualified `name` reference inside their `EXISTS` subquery. `project_shares` already had its own `name` column (project display name, added by `project_shares_add_name`) by the time these two policies were written on 2026-07-06, so Postgres silently bound `name` to the closer inner scope (`project_shares.name`) instead of the intended `storage.objects.name` — no error at `CREATE POLICY` time, just always-false access checks (`split_part(project_name, '/', 2)` is always `''`, never a real token). This meant `share_blobs_owner_select`/`share_blobs_recipient_read` **always denied access**, so shares were never actually reaching storage (`createShare()` failed with "new row violates row-level security policy" on the upsert, and any share that predated the fix had recipients unable to download — "Object not found" — regardless of email match). The "all 7 policies verified present" check only confirmed the policies *existed*, not that their logic was correct. Fixed via `supabase/migrations/20260714120000_fix_share_blobs_name_ambiguity.sql` — both policies now explicitly qualify `objects.name`. Verified against live data (all 3 existing `project_shares` rows' tokens now extract correctly) and Franco confirmed create+import works end-to-end in browser. **Status: fixed, browser-verified 2026-07-14.** |
| 2026-07-05 | Clean | Share Project does not work, it violates some security row level | high | security | `supabase/migrations/20260714120000_fix_share_blobs_name_ambiguity.sql` | fixed 2026-07-14 (same root cause as the 2026-07-06 Sync row above) |
| 2026-07-05 | Clean | Replication code for CS DiD 2021 (Callaway & Sant'Anna) does not work | high | export | `src/components/modeling/CodeEditor.jsx`, `src/components/modeling/runners/estimationDispatch.js`, `src/services/export/{rScript,pythonScript,stataScript}.js` | fixed — 2026-07-30. **Not a math bug** — `CallawayEngine.js` itself was never at fault. Root cause: `CodeEditor.jsx`'s `buildScript()` builds `config.model` from an explicit field whitelist that had zero CallawayCS-specific entries. The exporters referenced `treatVar` for `gname`/`gvar` (a field only DiD/EventStudy use, never set for CallawayCS results) instead of `treatCol` (what `estimationDispatch.js` actually sets) — producing `gname = ""` in R and the literal string `gname = "null"` in Python/Stata, both of which make `att_gt()`/`csdid` fail immediately since neither is a real column. Separately, `estMethod`/`anticipation`/`basePeriod` were never captured in the wrapResult spec at all, so a script would silently run with `est_method="dr"`/`anticipation=0` regardless of what the user actually selected — same "spec → export needs THREE whitelist edits" gotcha this file already documents elsewhere. Fixed by (1) adding the three missing fields to the spec in `estimationDispatch.js`, (2) adding `treatCol`/`compGroup`/`estMethod`/`anticipation`/`basePeriod` to `CodeEditor.jsx`'s whitelist, (3) fixing all three exporters to read the correct field names (R already had `treatCol`/`compGroup` right, only needed the new fields; Python/Stata's `transpileModel` takes destructured params rather than a `model` object, so also needed their parameter lists and single-model call sites extended). Verified two ways: a standalone reproduction of the exact `CodeEditor.jsx`→exporter data flow (16/16 checks, confirmed the pre-fix R/Python output was literally `gname = ""` / `gname = "null"`), and the existing `exportValidation.js` smoke harness (new CallawayCS entry asserting the real column name appears verbatim, not just that `att_gt` is mentioned — a weaker check would have passed even when broken). R validation against the live `did` package was attempted but blocked: CRAN's Windows binary for `did`'s dependency `DRDID` is still 1.2.3 while `did` needs ≥1.3.0, and compiling from source needs Rtools (not installed) — Franco chose not to install Rtools for this. **Two pre-existing, unrelated failures surfaced by the same harness run, not touched here:** `WLS` (Stata expects `[aw=wgt]`, Python expects `smf.wls` — neither appears) and `SpatialRDD` (R expects `_signed_dist`) — confirmed pre-existing via `git stash` (17/19 before this fix, same 2 failures). |
| 2026-07-05 | General | LaTeX table size must adjust when the number of columns increases | medium | export | `src/services/export/latexTable.js`, `src/ReportingModule.jsx` | open |
| 2026-07-05 | Explore | Plot replication code omits the dataset load lines | high | export | `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx`, `src/services/export/plotScript.js`, `src/pipeline/exporter.js` | fixed — 2026-07-30. The three plot builders (`buildGgplot`/`buildMatplotlibPlot`/`buildStataPlot`) only ever emitted the geom body over a caller-supplied frame name; the load line came from whoever composed the script around them. ReportingModule's unified script loads every dataset up front, so plots there were fine — but the Plot Builder's own **"Copy"** button had no such composer, so an Explore plot copied out started at `ggplot(df_drunk)` with `df_drunk` never defined. **The load line alone would still not have reproduced the plot**: the Plot Builder draws POST-pipeline rows, so the cleaning steps that created those columns have to run first. Fix reuses the section `downloadExploreScript` already prepends — `generateCleanScript` (load line + pipeline steps) — via PlotBuilder's existing `scriptPreamble` seam. That seam was single-purpose: a string preamble implied ModelingTab's `plot_df` convention (a frame rebuilt from `fit`), so Explore's `df_<dataset>` needed a second shape. `scriptPreamble` now also accepts `{ code, dfVar }`, resolved by a new pure `resolvePlotPreamble()` in `plotScript.js` (string → `plot_df` unchanged, so every model plot copies exactly as before). Also: `generateCleanScript` gained `preview:false` to drop its `head()`/`list in 1/5` trailer when used as a preamble, and `downloadExploreScript` was passing no `loadOpts` at all — a `;`-delimited CSV exported as plain `read_csv`. The QuickFilter is view-level and never enters the pipeline, so it is disclosed as a NOTE rather than silently dropped (same convention as `exploreStatScript.js`). Harness: `src/services/export/__validation__/plotLoadLineValidation.mjs`, 12/12, negative-controlled (collapsing `resolvePlotPreamble` back to always-`plot_df` fails T4/T5). Unrelated pre-existing failure seen in the same run and NOT touched: `replicationIntegrityValidation` reports `connected_components: missing in R, Py, Stata` (333/1 both with and without this change). Browser validation pending Franco. |
| 2026-07-06 | Model | No way to run more than 1 hypothesis test and save them for R replication | medium | export | `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx` | open |
| 2026-07-09 | Explore | Time series plot missing label controls (title, axis, etc.) | medium | ui | `src/components/PlotBuilder.jsx`, `src/ExplorerModule.jsx` | open |
| 2026-07-09 | Model | FE must accept any categorical variable (country, state, sector), not just entity/time | high | math | `src/math/PanelEngine.js` (`runFEMulti`), `src/components/modeling/ModelConfiguration.jsx` | likely fixed — N-way FE shipped in `045f5cda` (#104); needs confirmation |
| 2026-07-09 | Model | FE with interactions not supported | high | math | `src/math/PanelEngine.js`, `src/pipeline/runner.js` (`factor_interactions`) | likely fixed — FE interactions shipped in `045f5cda` (#104); needs confirmation |
| 2026-07-09 | Model | No direct elasticity interpretation of coefficients | medium | ai | `src/services/AI/Prompts/index.js` (INTERPRET_REGRESSION_PROMPT) | open |
| 2026-07-13 | General | Shared projects can't be opened — "email must exactly match the address the share was sent to" | high | security | `supabase/migrations/20260714120000_fix_share_blobs_name_ambiguity.sql` | fixed 2026-07-14 (same root cause as the 2026-07-06 Sync row above) |
| 2026-07-17 | Calculate | Stata workbench doesn't work completely | high | export | `src/components/modeling/CodeEditor.jsx`, `src/services/export/stataScript.js` | open |
| 2026-07-17 | General | Guide tour must be updated | low | ui | `src/components/HelpSystem.jsx` (TOUR_STEPS) | open |
| 2026-07-17 | Explore | group_summarize cannot do basic math across groups (e.g. ATE as difference in means) | high | pipeline | `src/pipeline/runner.js` (`group_summarize`), `src/components/wrangling/ReshapeTab.jsx` | open |
| 2026-07-17 | Model | Missing chi-square variant for linear hypothesis tests | medium | math | `src/math/ModelHypothesis.js`, `src/components/modeling/CoefficientTestPanel.jsx`, `src/math/calcEngine.js` | fixed — 2026-07-30. `waldTest` computed χ² only; its own doc comment claimed it returned "χ²(q) and F = χ²/q" but no F was ever produced, so the Joint tab could not offer the `test=` choice `car::linearHypothesis` gives you. Now returns both, with `preferred` following R's own rule (F for finite-sample t models, Chisq for ML/z models, which have no residual df to divide by — the F is **omitted rather than faked** there, and the panel hides the toggle instead of showing one that does nothing). New `waldStatistic()` resolves user choice × availability into one display-ready result that both the panel and the emitted script read, so they cannot disagree about which test ran. **The Joint tab also had no "copy test code" path at all** — a linear hypothesis run in Litux was not reproducible anywhere; new `generateJointHypothesisScript()` emits `car::linearHypothesis(fit, c(...), test=)` (R), `fit.wald_test(..., use_f=)` (Python), and `test (…) (…)` (Stata, with a note that `test` picks its own statistic from the estimator and has no `test()` option — stated rather than silently ignored). Non-zero nulls are carried into the constraint strings. **Bug found while verifying, in a SECOND file: `calcEngine.js`'s `_incompleteBeta` was missing Numerical Recipes' symmetry switch** (`x < (a+1)/(a+b+2)`, else evaluate on the complement). This is the same p-value bug class as the Riemann-sum `tCDF`/`fCDF` fixed in `LinearEngine.js` on 2026-07-28 — an independent second copy. `pt` calls it with `x = df/(df+t²)`, which approaches 1 exactly as `\|t\|` approaches 0, so **every insignificant t statistic landed in the divergent branch**: at t = 0.01 the two-sided p came out 0.19 instead of 0.99 (df = 100k), and 0.85 instead of 0.99 even at df = 10. That path feeds the Single tab of this same panel, `SampleTests.js` and the Calculate tab. Fixed non-recursively (x and 1−x are compared against thresholds summing to exactly 1, so a recursive complement form ping-pongs to a stack overflow on an x sitting on its own threshold — observed, not theoretical). After the fix `calcEngine.pt` matches the already-verified `LinearEngine.tCDF` on all 20 (t, df) cells, and the incomplete-beta symmetry identity `I_x(a,b) = 1 − I_{1−x}(b,a)` went from a worst violation of **1.0** to 1.6e-13. Validation: `modelHypothesisRValidation.R` → `modelHypothesisBenchmarks.json` → `modelHypothesisValidation.mjs`, **80/80 vs `car::linearHypothesis` 3.1-3** (R exports its own beta + vcov so waldTest is fed exactly R's inputs; 6dp on the statistic, 4dp on p). Negative-controlled (scaling F by 1.0001 → 19 failures). Cases cover zero and non-zero nulls, a single restriction, the intercept, n = 60k, and a hypothesis that is NOT rejected. Browser validation pending Franco. |
| 2026-07-17 | Model | HC2 not replicated: emitted R uses fixest `vcov="hetero"` (= HC1) instead of `sandwich::vcovHC(fit,"HC2")` | high | export | `src/services/export/rScript.js`, `stataScript.js`, `pythonScript.js` | code-complete 2026-07-18 — R now emits an `lm()` (or `AER::ivreg()` for 2SLS) refit + `sandwich::vcovHC` for HC2/HC3, since fixest's `"hetero"` is HC1 and nothing else; `sandwich`/`AER` added to the emitted package list. Stata switched to native `vce(hc2)`/`vce(hc3)` on `regress`, with a fallback + explicit note for `ivregress`, which has no hc2/hc3 option. Python was already correct via statsmodels. Same pass: Python HAC bandwidth was hardcoded `maxlags=1` → now `floor(4*(n/100)^(2/9))` matching `robustSE.js`; clustered/two-way/HAC silent fallbacks now emit warnings; `seType`/`clusterVar` threaded into the multi-model and per-subset generators, which previously dropped them and always emitted classical SE. Build + lint:undef green; 21-cell emitted-output smoke check passes. **2nd pass, same day — the ~25 remaining hardcoded sites are now fixed too.** The hedge in the first pass ("clustering by entity in panel may be a deliberate convention") was wrong: `computeRobustSE` returns `null` for `seType="classical"`, i.e. the platform reports *classical* SE for FE by default and does not cluster by entity — so the hardcoded `vcov = ~entityCol` was a genuine disagreement, not a convention. All of FE/FD/TWFE/LSDV/EventStudy/PoissonFE, the RDD WLS fallbacks, GMM/LIML and the Poisson/count models now honour `seType` in all three languages, via new per-family mappers (`rPanelHC23Lines`/`rPlmVcovLines`/`rGlmVcovLines` in R, `panelCov`/`pfVcov` in Python, `pVce`/`pVceHdfe` in Stata). Two bugs found and fixed *while writing this pass*: (1) `xtreg y x, fe vce(cluster a b)` is a Stata syntax error — xtreg/areg take one cluster variable, so two-way now routes to `reghdfe` (`pVceHdfe`) with a note on the fallback; (2) panel HAC in R was emitting `vcov="NW"`, but Litux's panel HAC is Driscoll-Kraay (Fase 4b, validated vs `plm::vcovSCC`) — now emits `vcov = DK ~ time`. Verification: 168-cell matrix (7 SE types × 8 estimators × 3 languages) asserting no estimator/language pair emits an identical inference signature across all SE types, and that nothing throws — 0 problems. Franco confirmed the numeric agreement for the first pass. `seTolerances.js` entry `rdd-python-wls-fallback` updated (no longer says the fallback hardcodes HC3). Browser validation pending Franco. |
| 2026-07-17 | General | Need groups for pinned models, plots and maps so not everything is compared at once | medium | ui | `src/services/Persistence/artifactOrder.js`, `src/components/modeling/ModelBufferBar.jsx` | open |
| 2026-07-17 | Clean | Unfinished work in transformation subsections is lost when navigating away (e.g. case_when) | medium | ui | `src/components/wrangling/FeatureTab.jsx` | open |
| 2026-07-17 | Data | No arrange (sort) control in the data viewer | medium | ui | `src/App.jsx` (`DataViewer`), `src/services/data/sortRows.js` (new), `src/services/data/duckdb.js` (`getTablePage`) | fixed — 2026-07-30. (The file this row originally pointed at, `ExplorerModule.jsx`, was wrong — the Data Viewer lives in `App.jsx`.) A ⇅ toggle on every column header cycles asc → desc → off, with a gold `sorted: <col> ▲` chip + clear button in the toolbar, because the header scrolls out of view on a wide table and a silently reordered grid is worse than no sort. `stopPropagation` on the toggle preserves the header's existing click (select column → stats panel). **This is a VIEW sort, not a pipeline step** — it never touches the data, the pipeline or any export, matching how the viewer's existing filter already behaves. **The load-bearing detail is that there are two sort paths that must agree.** A DuckDB-backed unfiltered grid is server-paginated, so sorting the returned rows in JS would have ordered only the 100 rows already on screen and presented that as the sorted table; `getTablePage` therefore takes an optional sort and pushes `ORDER BY … NULLS LAST` into SQL. The JS branch (new pure `sortRows.js`, extracted so it is testable outside React) matches that null rule deliberately: **blanks sort last in BOTH directions**, as in `dplyr::arrange` where NA stays last under `desc()` — a plain comparator floats nulls to the top on a descending sort, which reads as data loss. String collation uses `numeric: true` so `comuna 10` follows `comuna 2`. Validation: `src/services/data/__validation__/sortRowsValidation.mjs`, 16/16, negative-controlled (applying the sign to the blank branch fails the desc-nulls check). **Known inherited limitation, not introduced here:** on a DuckDB dataset with the viewer's filter active, both filter and sort run over the 500-row preview rather than the full table — pre-existing filter behaviour that the sort now shares. Browser validation pending Franco. |
| 2026-07-22 | Model | P-values with small numbers (near-zero t-stat reported as p<0.001) | critical | math | `src/math/LinearEngine.js` (`tCDF`/`betainc`) | likely fixed — `216b1ecb`; needs confirmation |
| 2026-07-22 | Model | Subsets in RDD does not seem to work | high | ui | `src/components/ModelingTab.jsx` (`runAllSubsets`), `src/math/diagnoseRDD.js` (new) | fixed — 2026-07-28, not yet committed; root cause was NOT a math bug (RDD estimates were correct whenever a subset had ≥6 obs. in bandwidth on both sides of the cutoff — verified vs a synthetic repro with true jump=5, LATE≈4.68–4.75) but a silent-failure UX gap: `runAllSubsets` dropped any subset where estimation failed with zero indication, and RDD fails per-subset far more often than other estimators (one-sided subsets → constant treatment dummy → singular design; sparse subsets → <6 obs. in the IK bandwidth window). Fix: new `diagnoseRDD()` (mirrors existing `diagnoseFit.js` pattern) gives a specific reason instead of the old generic "not enough observations" message (which was actively misleading for the one-sided case); `runAllSubsets` now collects failures and renders a post-run summary banner ("N/M subsets added · K failed: `<name>`: `<reason>`"). Same silent-drop pattern also exists in `runSpecCurve` (H8) — not fixed here, flagged separately, out of scope for this change. |
| 2026-07-28 | Simulate | Replication script for Simulate only reproduces the DGP draw, not the downstream analysis (resampling/tests/estimation on the simulated data) | high | export | `src/components/tabs/SimulateTab.jsx`, `src/services/export/` (script generators) | open |
| 2026-07-28 | Explore | References of the group in time series plot don't work | medium | ui | `src/ExplorerModule.jsx`, `src/components/PlotBuilder.jsx` | open |
| 2026-07-28 | Explore | Bar plot renders a stray empty category and bars hang off the bottom of the panel | high | ui | `src/components/PlotBuilder.jsx` | fixed — 2026-07-28. Two bugs, both the SAME CLASS as the documented boxplot/tile bugs; `bar` was simply never added to either exclusion list. (1) `Plt.barY` puts x on a BAND scale, so the numeric `Plt.ruleX([0])` added a stray empty "0" band — a 2-category bar chart rendered 3 slots with the bars pushed right. (2) The auto y-domain `[yMin, yMax]` can EXCLUDE 0, but bars are drawn from a zero baseline — so they hung off the bottom edge and a real 0.117-vs-0.118 difference was blown up to full panel height (ggplot's `geom_col` always keeps 0 in the scale). Fix adds `hasBar` to the `showRuleX` exclusion and clamps the auto domain to include 0 via `autoYMin`/`autoYMax`; an explicitly user-set domain still wins. |
| 2026-07-28 | Explore | Histogram with binwidth=0.001 does not match ggplot: ~2x peak (3500 vs 1650) and gaps between bars, though the bar at BAC=0 matches (~1400 both) | high | ui | `src/components/PlotBuilder.jsx`, `src/services/export/plotScript.js` | fixed — 2026-07-28. **Root cause: bin EDGE placement, not bin arithmetic and not row count.** Confirmed same data (214,558 rows both sides, same column). Stata `.dta` stores reals as 4-byte floats, so widening to a JS double leaves ~1e-8 of noise (`0.005` arrives as `0.00499999988…`). Observable Plot's `interval` puts bin edges exactly at multiples of the width, so every value of a decimal-grid column sits ON an edge and that noise drops it into the previous bin — **measured 217 of 451 grid values (48.1%) misbinned**, which doubles those bars and empties their neighbours. The BAC=0 bar matched because 0 is exact in float32 and there is almost no mass at 0.001–0.002 to contaminate it — that asymmetry is what identified the mechanism. ggplot is immune because `geom_histogram` defaults to `boundary = width/2`, putting values in bin CENTRES; note its own fuzz (`binwidth * 1e-8` = 1e-11 here) is ~3 orders too small to explain the immunity, so copying the fuzz would NOT have worked. Fix: `centredBinThresholds()` reproduces ggplot's centred edges (verified to reproduce ggplot's origin of −0.0005 exactly, and 451 values → 451 bins, 0 doubled), passed as explicit `thresholds`; falls back to Plot's `interval` when edges can't be built (no finite data, or a width needing >20k bins — guarded so an absurd width can't hang the tab). Applied to histogram AND density, which shared the code path. **Ruled out, do not re-investigate:** Plot 0.6.16's `numberInterval` arithmetic is correct here (it takes the multiply path since `1/0.001` is exactly 1000 — verified against the CDN bundle); and there is no row sampling/thinning in the Explore→PlotBuilder path (`plotRows === rows` for non-date x). **Also fixed alongside:** all three exporters ignored `binMode`/`binWidth` and always emitted `bins = 20`, so a binwidth histogram exported a script that did not reproduce it — R now emits `binwidth =`, Stata `width()`, and Python explicit half-width-offset edges via `np.arange` (new `usesNumpy` import flag mirroring `usesSeaborn`). |
| 2026-07-28 | Model | URGENT: beta=0.0001, se=0.002 cannot give p<0.001 with *** (duplicate of the 2026-07-22 p-value bug) | critical | math | `src/math/LinearEngine.js` (`tCDF`/`betainc`) | likely fixed — `216b1ecb`; needs confirmation |
| 2026-07-28 | Model | P-values of OLS wrong for insignificant estimates (duplicate of the 2026-07-22 p-value bug) | critical | math | `src/math/LinearEngine.js` (`tCDF`/`betainc`) | likely fixed — `216b1ecb`; needs confirmation |
| 2026-07-31 | Data | When joining, the pipeline of the right dataset is not applied — changes don't merge | critical | pipeline | `src/WranglingModule.jsx`, `src/pipeline/datasetContext.js` (new) | fixed — 2026-08-01. **Two independent defects, same symptom.** (1) `WranglingModule` built the join context as `Object.fromEntries(allDatasets.map(d => [d.id, d.rawData]))` — every referenced dataset in its ORIGINAL state, so a join pulled the right dataset's raw rows and silently ignored every cleaning step applied to it. Nothing errored; you just merged the wrong data. (2) `runPipelineDuck(...)` was called WITHOUT `context` at all, so on a DuckDB-backed dataset (>10MB) the SQL join translation found no right dataset and returned null, and the JS fallback it hands off to got the same empty context — the join was a total no-op. New `pipeline/datasetContext.js` replays each referenced dataset's own pipeline first, resolving nested references recursively with cycle detection and a depth cap; a cycle or a load failure degrades to raw rows **with a warning surfaced in the header**, never silently. Two design points that are load-bearing: `_duckdb` is forwarded ONLY for datasets with an empty pipeline (its `tableName` points at the RAW table, so passing it through for a processed dataset would reintroduce the same bug one layer down, in SQL); and only datasets the current pipeline actually references are materialised (`only`), since replaying a 900k-row DuckDB dataset's pipeline on every keystroke would be a serious regression — the effect keys on the SET of referenced ids, not on `pipeline`. The pipeline effect now waits for the context instead of running once with an empty one, which would make a join briefly no-op and then flip. Validation: `src/pipeline/__validation__/datasetContextValidation.mjs`, 21/21, with T3 explicitly asserting that the OLD raw-context path reproduces the reported bug (so the other tests can't pass vacuously). Browser validation pending Franco. |
| 2026-07-31 | Clean | `any()` doesn't work in grouped expressions — e.g. `mutate(treat = any(trarrprop != 0 & year == 2015))` | high | pipeline | `src/pipeline/groupExpr.js` (new), `src/pipeline/runner.js` (`grouped_mutate`) | fixed — 2026-08-01. `grouped_mutate`'s expression mode already HAD `any()`/`all()`/`sum()`/…, so this looked like a feature request, but it was a wrong-answer bug: the evaluator injects each column as an ARRAY over the group, which is right for `mean(wage)` and wrong the moment the argument is an expression. R vectorises `trarrprop != 0`; JS does not, so `[0,5] != 0` coerces the array to a string and compares that — `any(...)` returned **0 for every group, with no error**. Reproduced with Franco's exact line before fixing; note a single-row group coincidentally returned the right answer, which is part of why this survived. JS can't overload `!=` or `&`, so the fix is a source transform (`groupExpr.js`): each aggregate call's argument is hoisted into a `const __aggN__ = __reduce(name, rows.map(row => …))` line ahead of the outer expression, with the row DESTRUCTURED so columns are scalars inside the argument. Array bindings stay visible outside, so existing `mean(wage)`-style expressions are untouched. Calls come back in dependency order (inner before outer), which makes `any(wage > mean(wage))` mean what R means — inner is a group scalar, the comparison around it is row-level. String literals are skipped (a quoted `"any("` is not a call), `count()` with no argument keeps the old group-helper path, and `obj.any(1)` is not treated as an aggregate. Reducer keeps the existing convention that the strings `"0"`/`"false"` are falsy, so a text-encoded dummy doesn't make every group true. Only ONE `new Function` remains in the branch — the pre-existing one, unchanged in kind. Validation: `src/pipeline/__validation__/groupExprValidation.mjs`, 28/28, negative-controlled (disabling the transform fails 12 checks). Browser validation pending Franco. |
